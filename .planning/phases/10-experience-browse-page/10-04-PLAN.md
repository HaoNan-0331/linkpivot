---
phase: 10-experience-browse-page
plan: 04
type: execute
gap_closure: true
wave: 1
depends_on: []
files_modified:
  - electron/services/experienceService.ts
  - electron/services/__tests__/experienceService.browse.test.ts
  - electron/main.ts
  - src/components/knowledge/ExperienceTab.tsx
  - src/components/knowledge/ExperienceEditForm.tsx
  - src/components/knowledge/ExperienceDetailModal.tsx
autonomous: true
requirements:
  - BROWSE-02
  - BROWSE-03
  - BROWSE-04
gaps_addressed:
  - CR-01
  - CR-02
  - UAT-test6
  - UAT-test8
  - UAT-test10-1a
  - WR-01
  - WR-02
  - WR-05
out_of_scope_followups:
  - "问题 1b：ai 助手聊天设备范围扩展（独立新需求，本 gap 不动 ai 模块）"
  - "问题 3：标签输入复用已有标签（UX 增强，用户明确讨论后处理）"
  - "WR-03/04/06/07/08/09 与 INFO：本 gap 不修（除非与 5 项必修同文件顺手）"

must_haves:
  truths:
    - "restoreExperience 对 draft id 抛错（红线③ 不变量恢复：draft 必经 confirmDrafts 才能转 published）"
    - "restoreExperience 对不存在 id / 有效（invalid_at IS NULL）经验抛错，仅恢复已失效经验"
    - "历史 severity 列 NULL 但 attrs_enc.severity 有值的经验，经 post-MK 启动钩子回填后 severity 列有明文值，severity 筛选命中"
    - "状态 Select 选「已失效」自动 setIncludeInvalid(true) 且筛 invalid_at<=now 的经验；选「有效」自动 setIncludeInvalid(false)"
    - "ExperienceEditForm 关联设备候选含所有类型设备（去除 ssh/telnet filter）"
    - "tags LIKE 通配符 %/_/反斜杠已转义（含 100%/a_b 等标签的语义匹配正确）"
    - "syncRelateDevices 单事务原子（service 层 setExperienceDevices 受控接口，部分失败不留半成品）"
    - "formatTs 兼容 'yyyy-MM-dd HH:mm:ss' 与 ISO 'T' 两种格式，渲染无字面 T"
  artifacts:
    - path: "electron/services/experienceService.ts"
      provides: "restoreExperience 守卫 + backfillSeverityFromHistory + setExperienceDevices + tags ESCAPE 转义"
      contains: "restoreExperience"
    - path: "electron/main.ts"
      provides: "post-MK 启动钩子调 backfillSeverityFromHistory"
      contains: "backfillSeverityFromHistory"
    - path: "src/components/knowledge/ExperienceTab.tsx"
      provides: "状态 Select 与 includeInvalid 联动 + syncRelateDevices 改单 IPC + formatTs 兼容 ISO"
      contains: "formatTs"
    - path: "src/components/knowledge/ExperienceEditForm.tsx"
      provides: "关联设备候选放开全设备"
    - path: "src/components/knowledge/ExperienceDetailModal.tsx"
      provides: "formatTs 兼容 ISO"
    - path: "electron/services/__tests__/experienceService.browse.test.ts"
      provides: "CR-01 守卫 4 用例 + CR-02 回填 2 用例 + WR-01 转义用例"
  key_links:
    - from: "electron/main.ts"
      to: "electron/services/experienceService.ts#backfillSeverityFromHistory"
      via: "setExperienceMasterKey 之后调用"
      pattern: "backfillSeverityFromHistory"
    - from: "src/components/knowledge/ExperienceTab.tsx"
      to: "window.api.experience.setDevices"
      via: "syncRelateDevices 单 IPC 调用"
      pattern: "setDevices"
---

<objective>
闭环 Phase 10 验收暴露的 5 项必修 gap（CR-01 安全守卫 / CR-02 历史 severity 回填 / 问题 2 状态筛选联动 / 问题 1a 设备 filter 放开 / WR-01+02+05 高优 WARNING）。全部是 Phase 10 既有文件的小改 + 测试补强，不动 phase goal（5 SC + 4 BROWSE 已达成），目标是把红线③安全不变量、数据完整性、UAT major bug、设计意图对齐到可审计的完成度。

Purpose:
- CR-01 恢复红线③ 不变量（draft 不可经 IPC restore 直 published），是安全防线缺失，不可静默放行
- CR-02 治本历史 severity 数据完整性（read/filter 对称，D-10-2「保证历史数据可查」承诺兑现到筛层）
- 问题 2 修 UAT major bug（状态 Select 与 includeInvalid 解耦，用户筛「已失效」查不到）
- 问题 1a 对齐 CONTEXT 设计意图（手动 CRUD 关联设备应含所有类型，T-10-08 mitigation 仅适用 AI 起草，手动录入不适用）
- WR-01/02/05 消除 code review 高优 WARNING（注入面/原子性/时间格式鲁棒性）

Output: 6 文件小改 + 既有 browse.test.ts 补 ~8 用例，三绿门禁全绿零回归。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/phases/10-experience-browse-page/10-VERIFICATION.md
@.planning/phases/10-experience-browse-page/10-REVIEW.md
@.planning/phases/10-experience-browse-page/10-UAT.md
@.planning/phases/10-experience-browse-page/10-CONTEXT.md
@.planning/phases/10-experience-browse-page/10-UI-SPEC.md
@.planning/phases/10-experience-browse-page/10-PATTERNS.md

<interfaces>
<!-- 关键既有契约，executor 直接用，无需再探查 codebase -->

From electron/services/experienceService.ts（被改文件，当前现状）:
- `restoreExperience(id: string): any` — L421-427，UPDATE 无条件 status='published' WHERE id=?（**缺守卫 = CR-01**）
- `invalidateExperience(id: string): any` — L406-412，对称模板（不动 status，只落 invalid_at）
- `listExperiences(opts: ListExperiencesOpts)` — L254-342，severity 直筛 L283-286（**漏历史 fallback = CR-02 筛层**）；tags LIKE L288-292（**未转义 = WR-01**）
- `rowToExperience` severity fallback — L207-213（**只保证读层**）
- `relateDevice(expId, deviceId, relationType='primary')` / `unrelateDevice(expId, deviceId)` — L437-446
- `let MK = ''` + `setExperienceMasterKey(key)` — L27-31（模块级 MK，回填钩子用）
- `_setExperienceDbGetter(fn)` — L39，测试注入 db getter
- `VALID_SEVERITIES = ['critical','high','medium','low','info']` — L51
- `assertTroubleshootingAttrs(attrs, ctx)` — L59-70
- `ListExperiencesOpts` — L125-139（已含 search/severity/tags/deviceId/includeInvalid）
- 测试 MK key 常量：`MK_TEST_KEY = 'test-master-key-32-bytes-ok!!'`（browse.test.ts L45）
- 测试 mock DB：`MemDb` 类 + `mockDbRef` + `insertExpRaw` helper（browse.test.ts L83+）

From electron/main.ts（被改文件）:
- MK 注入顺序：L88 `masterKey = getOrCreateMasterKey()` → L95 `setExperienceMasterKey(masterKey)` → L104-106 `initDatabase/createTables/migrateAndSecure`
- **CR-02 回填钩子插入点：L106（migrateAndSecure）之后、L126 OUIService.preload() 之前**——此时 MK 已注入 + 表已迁移 + MK 可解密 attrs_enc

From electron/ipc/experienceIpc.ts（不改，但扩 setDevices IPC 需在此注册）:
- 全 channel secure 包装范式：`ipcMain.handle('experience:<action>', secure((_e, ...) => fn(...)))`
- `stripEncColumns` — L50-58（WR-09 死代码，本 gap 不删，out_of_scope）

From electron/preload.ts（不改文件实现，仅扩 experience block 一行 setDevices）:
- experience block L124-140 范式：`experience: { list, ..., invalidate, restore, relateDevice, unrelateDevice, ... }`

From src/types/electron.d.ts（扩 setDevices 签名 + listDevices 返回类型已存在）:
- experience block L202-218，`restore: (id) => Promise<Experience>` L209

From src/types/experience.ts（扩 ExperienceListInput.invalidOnly + setDevices 签名已存在 renderer API 中）:
- `ExperienceListInput` — 含 search/severity/tags/status/includeInvalid/deviceId

From src/components/knowledge/ExperienceTab.tsx（被改文件，当前现状）:
- `STATUS_OPTIONS` — L59-62（`{value:'published',label:'有效'}, {value:'invalid',label:'已失效'}`，**status='invalid' 传 service 查不到 = 问题 2 根因**）
- `includeInvalid` state L103 + Switch L465-468
- `loadExperiences` opts 构造 L121-143（传 `status: status as ...`）
- `formatTs(ts)` L79-82（**slice(0,16) 假定空格分隔 = WR-05**）
- `isInvalid(exp)` L84-89（renderer 时间判定，问题 2 修复可复用）
- `syncRelateDevices` L177-191（**Promise.all 非原子 = WR-02**），调用点 `handleSubmitEdit:201-207`
- 状态 Select JSX L437-444（`onChange={(v) => setStatus(v)}`，**不联动 includeInvalid**）

From src/components/knowledge/ExperienceEditForm.tsx（被改文件）:
- 设备候选拉取 useEffect L103-111（**filter connectionType==='ssh'||'telnet' = 问题 1a 根因**，注释 T-10-08 mitigation 沿用 Phase 9）
- `validateDraft` 导出 L59（不动，单一来源）

From src/components/knowledge/ExperienceDetailModal.tsx（被改文件）:
- `formatTs(ts)` L42-46（同 WR-05 问题，与 ExperienceTab 重复定义）
- `isValid`/`isInvalid` L48-53（WR-07 重复，本 gap 不抽 util，out_of_scope，仅修 formatTs）

From src/components/pages/KnowledgeBasePage.tsx（参考范范式）:
- 详情 Modal width 900 L469，formatTs 范式可参考
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: service 层修 CR-01 restore 守卫 + CR-02 severity 回填钩子 + WR-01 tags 转义 + WR-02 setExperienceDevices 原子接口</name>

  <files>electron/services/experienceService.ts, electron/services/__tests__/experienceService.browse.test.ts, electron/ipc/experienceIpc.ts, electron/preload.ts, src/types/electron.d.ts, src/types/experience.ts, electron/main.ts</files>

  <read_first>
    - electron/services/experienceService.ts（实读 L1-200 类型 + L406-470 invalidate/restore/relate 范式 + L254-342 listExperiences）
    - electron/services/__tests__/experienceService.browse.test.ts（实读 L1-90 mock DB 范式 + L530-652 既有 severity/restore 用例格式 + `insertExpRaw`/`MemDb`/`mockNow` helper）
    - electron/main.ts L88-140（MK 注入 + 迁移顺序，CR-02 钩子插入点）
    - electron/ipc/experienceIpc.ts L60-118（secure 包装范式 + relateDevice/unrelateDevice channel）
    - electron/preload.ts L124-140（experience block）
    - src/types/experience.ts + src/types/electron.d.ts（ExperienceListInput / experience API 契约）
    - .planning/phases/10-experience-browse-page/10-REVIEW.md CR-01/CR-02/WR-01/WR-02（修法 + 代码 excerpts）
    - ./CLAUDE.md Conventions（受控接口模式 / 函数式 service / IPC secure 红线 / 迁移幂等 / MAX_BATCH）
  </read_first>

  <behavior>
    - restoreExperience 对不存在 id 抛错（错误信息含 id）
    - restoreExperience 对 status='draft' 抛错（信息提示走 confirmDrafts）
    - restoreExperience 对 invalid_at IS NULL（有效）经验抛错（信息提示无需恢复）
    - restoreExperience 对 invalid 经验（invalid_at 有值）成功，清 invalid_at + status 回 published
    - backfillSeverityFromHistory 对 severity IS NULL AND attrs_enc 非空的历史行，解密 attrs.severity 回填 severity 列（幂等：再跑不重复改）
    - backfillSeverityFromHistory 对 severity 已填的行不动；对 attrs_enc 无 severity 的行不报错（severity 留 NULL）
    - backfillSeverityFromHistory 返回回填行数（可观测）
    - tags 筛选对含 `%`/`_` 的标签正确匹配（如 tag='100%' 不再模式错配所有含 "100 的标签）
    - setExperienceDevices(expId, expectIds) 单事务内 diff（toAdd relateDevice + toRemove unrelateDevice），throw 即 ROLLBACK 无半成品
  </behavior>

  <action>
    **1. restoreExperience 加守卫（CR-01，experienceService.ts L421-427 重写）：**
    先 `SELECT status, invalid_at FROM experiences WHERE id = ?` 取当前行。若 `!cur` throw `经验不存在: ${id}`；若 `cur.status === 'draft'` throw `草稿不可经 restore 发布，请走 confirmDrafts 质量门`；若 `cur.invalid_at == null`（有效） throw `经验当前有效，无需恢复`。否则 UPDATE 改为 `WHERE id = ? AND invalid_at IS NOT NULL`（守卫再加一道 SQL 层防御），run 后 return getExperience(id)。保留原 docstring 但更新注释说明守卫语义（删除"T-10-03 mitigate：status 直回 'published' 无法被滥用"的误导性注释，改为"双层守卫：service 层 status/invalid_at 检查 + SQL WHERE invalid_at IS NOT NULL"）。

    **2. backfillSeverityFromHistory 新增（CR-02 治本，experienceService.ts 受控接口区新增导出函数，紧邻 restoreExperience）：**
    签名 `export function backfillSeverityFromHistory(): { backfilled: number }`。实现：
    - `const conn = db()`
    - 用单条 prepared statement 查所有待回填行：`SELECT id, attrs_enc, severity FROM experiences WHERE severity IS NULL AND attrs_enc IS NOT NULL`（reuse prepared statement）
    - 遍历每行：`decField(row.attrs_enc, MK)` 解密 → `JSON.parse` → 取 `attrs.severity`；若 severity 在 `VALID_SEVERITIES` 内，则 `conn.prepare('UPDATE experiences SET severity = ? WHERE id = ?').run(sev, row.id)` 并计数；解密失败/JSON 解析失败/severity 非法均跳过（不 throw，避免单行脏数据阻塞全量回填，与 setDecryptFailureHandler 可观测策略一致）。
    - 返回 `{ backfilled: count }`。**幂等守卫：WHERE severity IS NULL**——severity 已填的行不进 SELECT，再跑无副作用。
    - 注释明确：此钩子须在 main.ts MK 注入后调用（迁移在 MK 注入前跑，无法在 v10 内回填，PATTERNS caveat 落地）。

    **3. WR-01 tags LIKE 转义（experienceService.ts L288-292 改）：**
    把 `opts.tags.forEach((t) => params.push('%"${t}"%'))` 改为：每个 tag 转义 `\`→`\\`、`%`→`\%`、`_`→`\_`（顺序：先反斜杠），用 `ESCAPE '\\'` 子句。即 `const ors = opts.tags.map(() => "e.tags LIKE ? ESCAPE '\\\\'")`，`params.push('%"' + esc + '"%')`。注意 JS 字符串中 `\\\\` 表示 SQL 中的 `\\`（一个反斜杠字面 + ESCAPE char）。注释标注含 `"` 的 tag 仍需长期改 json_each 子查询（本 gap 不做，记 follow-up）。

    **4. setExperienceDevices 新增（WR-02 原子接口，experienceService.ts 设备关联区新增导出）：**
    签名 `export function setExperienceDevices(expId: string, expectIds: string[]): void`。实现包 `conn.transaction(() => {...})()`：
    - 内部 `const cur = conn.prepare('SELECT device_id FROM exp_device_rel WHERE experience_id = ?').all(expId).map((r:any) => r.device_id)`
    - `const expect = new Set(expectIds)`
    - toAdd = `expectIds.filter((x) => !cur.includes(x))` → 逐个调 `relateDevice(expId, x)`（复用既有，保 INSERT OR IGNORE 幂等）
    - toRemove = `cur.filter((x) => !expect.has(x))` → 逐个调 `unrelateDevice(expId, x)`
    - 单事务保证 throw ROLLBACK，全成全败。**不校验 expectIds 上限**（单经验设备量小，MAX_BATCH 不适用此场景；若需守卫，service 入口加 `if (expectIds.length > 100) throw` —— planner discretion，建议加，对齐 CLAUDE.md 批量上限精神）。

    **5. IPC/preload/d.ts/types 扩 setDevices（WR-02 配套）：**
    - experienceIpc.ts：扩 import `setExperienceDevices`，新增 `ipcMain.handle('experience:setDevices', secure((_e, expId: string, deviceIds: string[]) => setExperienceDevices(expId, deviceIds)))`（紧邻 unrelateDevice channel 后）。
    - preload.ts experience block：新增 `setDevices: (experienceId: string, deviceIds: string[]) => ipcRenderer.invoke('experience:setDevices', experienceId, deviceIds)`。
    - src/types/electron.d.ts experience block：新增 `setDevices: (experienceId: string, deviceIds: string[]) => Promise<void>`。
    - 三向一致（channel 名逐字相等）。

    **6. main.ts CR-02 回填钩子（main.ts L106 之后）：**
    在 `migrateAndSecure()` 之后、`kb_chunks_fts` 自愈之前（或之后均可，但须在 MK 注入 L95 之后），插入：
    ```
    try {
      const r = backfillSeverityFromHistory()
      if (r.backfilled > 0) console.log('[startup] backfill severity from history:', r.backfilled)
    } catch (e) { console.warn('[startup] backfill severity failed (non-blocking):', (e as Error).message) }
    ```
    从 `./services/experienceService` 扩 import `backfillSeverityFromHistory`。try/catch 不阻塞启动（与 FTS5 自愈同范式），失败仅 warn（severity 筛选对历史数据降级，不致命）。

    **7. ExperienceListInput 扩 invalidOnly（src/types/experience.ts，问题 2 service 层配套，但本 task 只声明类型，Task 2 用）：**
    `ExperienceListInput` 加 `invalidOnly?: boolean` 字段。service 层 `ListExperiencesOpts` 同步加 `invalidOnly?: boolean`，listExperiences 内：若 `opts.invalidOnly === true`，conditions push `(e.invalid_at IS NOT NULL AND e.invalid_at <= datetime('now','localtime'))` 且**强制覆盖 includeInvalid=true 语义**（invalidOnly 隐含包含失效）。注释标注问题 2 修复配套。

    **测试补强（browse.test.ts 末尾新增 describe 块，复用既有 MemDb/mockDbRef/insertExpRaw/MK_TEST_KEY/encField/mockNow）：**
    - `describe('Phase 10 gap CR-01: restoreExperience 守卫')`：4 用例（不存在 id 抛错 / draft 抛错 / 有效经验抛错 / invalid 经验成功 + invalid_at 清 NULL + status published）。draft 用例用 `createExperience({ title, category:'product', content })`（默认 draft）后调 restoreExperience 期望 throw。invalid 用例用 createExperience + invalidateExperience + restoreExperience。
    - `describe('Phase 10 gap CR-02: backfillSeverityFromHistory')`：2 用例。（a）历史行 severity NULL + attrs_enc 含 severity='high'，调 backfill 后 SELECT severity='high'，再调一次 backfilled=0（幂等）；（b）severity 已填的行不被改 + attrs_enc 无 severity 的行不报错（severity 留 NULL）。
    - `describe('Phase 10 gap WR-01: tags LIKE 转义')`：1 用例。插入 tags 含 `'100%'` 和 `'100pa'` 两条经验，筛 tags=['100%'] 只命中含 `'100%'` 那条（不误命中 '100pa'）。注意 MemDb 的 LIKE 实现需支持 ESCAPE 子句——若现有 mock 不支持，扩 MemDb.applyConditions 的 LIKE 分支识别 ESCAPE 转义（这是测试支撑代码，非业务，与既有 mock LIKE 实现同源扩展）。
    - `describe('Phase 10 gap WR-02: setExperienceDevices 原子')`：1 用例。建 exp 关联 [A,B]，调 setExperienceDevices(exp, [B,C])，事务后关联 = [B,C]（A 删、C 加）。若 mock transaction 已支持 ROLLBACK 语义（09-01 增强），可补一个 throw 回滚用例（relateDevice 抛错时关联不变）。
  </action>

  <verify>
    <automated>npx vitest run electron/services/__tests__/experienceService.browse.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run electron/services/__tests__/experienceService.browse.test.ts` exit 0，新增 8 用例全 PASS（既有 9 用例不回归）
    - `grep -c "WHERE id = ? AND invalid_at IS NOT NULL" electron/services/experienceService.ts` ≥ 1（CR-01 SQL 守卫落地）
    - `grep -c "草稿不可经 restore" electron/services/experienceService.ts` = 1（draft 抛错分支）
    - `grep -c "export function backfillSeverityFromHistory" electron/services/experienceService.ts` = 1
    - `grep -c "backfillSeverityFromHistory" electron/main.ts` = 2（1 import + 1 调用）
    - `grep -c "ESCAPE" electron/services/experienceService.ts` ≥ 1（WR-01 转义子句）
    - `grep -c "export function setExperienceDevices" electron/services/experienceService.ts` = 1
    - `grep -c "experience:setDevices" electron/ipc/experienceIpc.ts electron/preload.ts src/types/electron.d.ts` 三处各 1（三向一致）
    - `grep -c "invalidOnly" src/types/experience.ts electron/services/experienceService.ts` ≥ 2（类型 + opts 接口）
  </acceptance_criteria>

  <done>
    restoreExperience 双层守卫落地（service 层 status/invalid_at 检查 + SQL WHERE）；backfillSeverityFromHistory 幂等回填钩子 + main.ts post-MK 调用；tags LIKE ESCAPE 转义；setExperienceDevices 单事务原子接口 + IPC/preload/d.ts 三向一致；ExperienceListInput 扩 invalidOnly。8 新 vitest 用例全 PASS 零回归。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI 层修问题 2 状态 Select 联动 includeInvalid + 问题 1a 设备 filter 放开 + WR-05 formatTs 兼容 ISO + WR-02 改用 setDevices 单 IPC</name>

  <files>src/components/knowledge/ExperienceTab.tsx, src/components/knowledge/ExperienceEditForm.tsx, src/components/knowledge/ExperienceDetailModal.tsx</files>

  <read_first>
    - src/components/knowledge/ExperienceTab.tsx（实读 L1-75 常量 + L79-89 formatTs/isInvalid + L91-155 state/loadExperiences/useEffect + L177-223 syncRelateDevices/handleSubmitEdit + L437-468 状态 Select/Switch JSX）
    - src/components/knowledge/ExperienceEditForm.tsx（实读 L1-50 import/常量 + L99-126 设备候选 useEffect + T-10-08 mitigation 注释）
    - src/components/knowledge/ExperienceDetailModal.tsx（实读 L1-60，定位 formatTs L42-46 + isValid/isInvalid L48-53）
    - src/types/experience.ts（ExperienceListInput / Experience API 契约，Task 1 已扩 invalidOnly + setDevices）
    - .planning/phases/10-experience-browse-page/10-UI-SPEC.md §3 筛选 bar（状态 Select 有效/已失效二选一 + Switch 独立 toggle 设计）+ Color 行（状态有效=绿/失效=灰）
    - .planning/phases/10-experience-browse-page/10-UAT.md 测试 6/8/10（问题 2 + 问题 1a 根因 + 期望）
    - .planning/phases/10-experience-browse-page/10-REVIEW.md WR-02/WR-05（修法 + excerpts）
    - ./CLAUDE.md Architecture（renderer 永不收明文凭证，window.api.* 白名单）
  </read_first>

  <behavior>
    - 状态 Select 选「已失效」→ setStatus('invalid') 同时 setIncludeInvalid(true)，列表只显 invalid_at<=now 的失效经验
    - 状态 Select 选「有效」→ setStatus('published') 同时 setIncludeInvalid(false)，列表显有效经验
    - 状态 Select 清空（undefined）→ 不强制 includeInvalid（保持当前 includeInvalid 值，Switch 仍可独立 toggle）
    - Switch「显示已失效」可独立 toggle（on 显含失效，off 仅有效），不受状态 Select 强制（单向联动：状态 Select 影响 Switch，Switch 不影响状态 Select）
    - 选「已失效」时 listExperiences 走 invalidOnly=true 路径（service 返失效行），renderer 不再依赖 status='invalid' 查询
    - ExperienceEditForm 关联设备候选含 connectionType 为 ssh/telnet/web/rdp 全部设备
    - formatTs('2026-08-05 12:00:00') → '2026-08-05 12:00'；formatTs('2026-08-05T12:00:00.000Z') → 'yyyy-MM-dd HH:mm'（本地时区，无字面 T）
    - syncRelateDevices 改单次 window.api.experience.setDevices 调用（不再 Promise.all N IPC）
  </behavior>

  <action>
    **1. 问题 2 状态 Select 联动（ExperienceTab.tsx 状态 Select onChange 改写，L437-444）：**
    把 `onChange={(v) => setStatus(v)}` 改为 `onChange={(v) => { setStatus(v); if (v === 'invalid') setIncludeInvalid(true); else if (v === 'published') setIncludeInvalid(false); }}`。v 为 undefined（清空）时不改 includeInvalid。**Switch 的 onChange 保持 `setIncludeInvalid` 不变**（独立 toggle，单向联动）。注释标注 UI-SPEC §3 + UAT 测试 6/8 修复。

    **2. 问题 2 loadExperiences opts 配套（ExperienceTab.tsx loadExperiences L121-143 改）：**
    把 `status: status as ExperienceListInput['status']` 改为：若 `status === 'invalid'`，传 `invalidOnly: true`（不传 status='invalid'，因失效行 status 列仍 'published'，查不到）；若 `status === 'published'`，传 `status: 'published'` 且 `includeInvalid: false`（强制，覆盖 Switch 状态）；若 status undefined，保持现有 `includeInvalid` 透传。即：
    ```
    const opts: ExperienceListInput = {
      search: search.trim() || undefined,
      category,
      severity,
      status: status === 'published' ? 'published' : undefined,
      invalidOnly: status === 'invalid' ? true : undefined,
      deviceId: deviceId.length > 0 ? deviceId : undefined,
      tags: tags.length > 0 ? tags : undefined,
      includeInvalid: status === 'published' ? false : status === 'invalid' ? true : includeInvalid,
      limit: 100, offset: 0,
    }
    ```
    注意 `includeInvalid: false` 时 service 层 `WHERE invalid_at IS NULL OR > now` 自动过滤，配合 status='published' 双重保险。

    **3. 问题 1a 设备 filter 放开（ExperienceEditForm.tsx L103-111 改）：**
    把 `.filter((d) => d.connectionType === 'ssh' || d.connectionType === 'telnet')` 删除（保留 `.map((d) => ({ id: d.id, name: d.name }))`）。更新注释：原 T-10-08 mitigation 仅适用 AI 起草模块（Phase 8/9，AI 助手仅连 ssh/telnet），手动 CRUD 关联设备应支持全类型（SSH/Telnet/Web/RDP，CONTEXT Constraints 产品功能约束四类远程通道设计意图）。注释标注 threat_model T-10-08 mitigation 移除理由（设备 DTO 已走 rowToDevice 白名单投影，无 `_enc` 密文泄露；关联不等于连接，仅元数据引用）。

    **4. WR-05 formatTs 兼容 ISO（ExperienceTab.tsx L79-82 + ExperienceDetailModal.tsx L42-46 两处同步改）：**
    把 `return ts.length >= 16 ? ts.slice(0, 16) : ts` 改为：
    ```
    const d = new Date(ts.replace(' ', 'T'))
    if (Number.isNaN(d.getTime())) return ts
    const pad = (n: number) => String(n).padStart(2, '0')
    return `${d.getFullYear()}-${pad(d.getMonth() + 1)}-${pad(d.getDate())} ${pad(d.getHours())}:${pad(d.getMinutes())}`
    ```
    （REVIEW.md WR-05 Fix excerpt 直接复刻）。两处独立定义（WR-07 抽 util 是 out_of_scope，本 gap 仅修兼容性不抽）。

    **5. WR-02 syncRelateDevices 改单 IPC（ExperienceTab.tsx L177-191 重写）：**
    把整个 `syncRelateDevices` 函数体改为单次 `window.api.experience.setDevices(expId, nextIds)` 调用（service 层单事务 diff）。删除 `current = await listDevices` 拉取 + `Promise.all` N IPC（service 已在事务内 diff）。新实现：
    ```
    const syncRelateDevices = async (expId: string, nextIds: string[]) => {
      await window.api.experience.setDevices(expId, nextIds)
    }
    ```
    handleSubmitEdit L201-207 调用点不变（仍 `if (relateDevices && relateDevices.length >= 0)` 触发——WR-03 永真条件是 out_of_scope，本 gap 不改；但若 planner discretion 顺手改 `if (relateDevices)` 更清晰，可一并改，标注 WR-03 顺手清）。

    **测试（renderer 组件测试本仓库无 vitest 组件测试基建，验证靠 type-check + build + 手动 UAT；本 task 用 grep + build gate + Task 1 service 测试覆盖逻辑链）：**
    无新增 vitest 用例（renderer 改动靠 `npx tsc -p tsconfig.web.json --noEmit` + `npx vite build` + UAT 人工验收覆盖）。
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npx vite build</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals 全绿）
    - `npx vite build` exit 0
    - `grep -c "setIncludeInvalid(true)" src/components/knowledge/ExperienceTab.tsx` ≥ 1（问题 2 状态 Select 选「已失效」联动）
    - `grep -c "invalidOnly: status === 'invalid'" src/components/knowledge/ExperienceTab.tsx` = 1（loadExperiences 配套）
    - `grep -c "d.connectionType === 'ssh'" src/components/knowledge/ExperienceEditForm.tsx` = 0（问题 1a filter 移除）
    - `grep -c "ts.replace(' ', 'T')" src/components/knowledge/ExperienceTab.tsx` = 1 且 `grep -c "ts.replace(' ', 'T')" src/components/knowledge/ExperienceDetailModal.tsx` = 1（WR-05 两处 formatTs 兼容 ISO）
    - `grep -c "experience.setDevices" src/components/knowledge/ExperienceTab.tsx` = 1（WR-02 单 IPC）
    - `grep -c "Promise.all" src/components/knowledge/ExperienceTab.tsx` ≤ 既有值（syncRelateDevices 的 Promise.all 移除；其他 Promise.all 不受影响）
  </acceptance_criteria>

  <done>
    状态 Select 与 includeInvalid 单向联动 + service invalidOnly 路径落地；ExperienceEditForm 关联设备候选含全类型；两处 formatTs 兼容 ISO；syncRelateDevices 改 setDevices 单 IPC 原子。tsc + vite build 全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: 三绿门禁复跑 + IPC 三向一致 + 威胁模型 grep 验证</name>

  <files>（不改代码，仅验证 Task 1+2 产出，失败时回到 Task 1/2 修）</files>

  <read_first>
    - ./CLAUDE.md Build 约定（tsconfig.web.json strict + noUnusedLocals / build:electron-main esbuild 外部化清单 / vitest 全量）
    - .planning/phases/10-experience-browse-page/10-VERIFICATION.md Behavioral Spot-Checks（三绿门禁复跑范式）
    - electron/ipc/experienceIpc.ts + electron/preload.ts + src/types/electron.d.ts（三向一致 grep 验证点）
  </read_first>

  <action>
    **无代码改动。** 仅执行验证命令，确认 Task 1+2 产出符合三绿门禁 + IPC 三向一致 + 威胁模型 grep。任一失败回到 Task 1/2 修。

    1. **tsc strict gate**：`npx tsc -p tsconfig.web.json --noEmit`（exit 0）
    2. **vite build gate**：`npx vite build`（exit 0，vendor-antd chunk size 警告非错误）
    3. **electron-main esbuild gate**：`npm run build:electron-main`（exit 0，dist-electron/main.js 生成，外部化清单 better-sqlite3/ssh2/telnet-client/electron/pdfjs-dist 不打进 bundle）
    4. **vitest 全量 gate**：`npx vitest run`（exit 0，15 files / 既有 191 + 新增 8 = 199 测试全 PASS，零回归）
    5. **IPC 三向一致**：`grep -c "experience:setDevices"` 在 experienceIpc.ts / preload.ts / electron.d.ts 三处各 = 1（channel 名逐字相等）；`grep -c "experience:restore"` 三处各 = 1（既有不回归）
    6. **CR-01 守卫落地**：`grep -c "AND invalid_at IS NOT NULL" electron/services/experienceService.ts` ≥ 1；`grep -c "草稿不可经 restore" electron/services/experienceService.ts` = 1
    7. **CR-02 钩子调用链**：`grep -c "backfillSeverityFromHistory" electron/main.ts` ≥ 2
    8. **WR-01 ESCAPE**：`grep -c "ESCAPE" electron/services/experienceService.ts` ≥ 1
    9. **问题 1a filter 移除**：`grep -c "connectionType === 'ssh'" src/components/knowledge/ExperienceEditForm.tsx` = 0
    10. **问题 2 联动**：`grep -c "invalidOnly" src/components/knowledge/ExperienceTab.tsx` ≥ 1
    11. **WR-05 formatTs 兼容**：`grep -c "ts.replace(' ', 'T')" src/components/knowledge/ExperienceTab.tsx src/components/knowledge/ExperienceDetailModal.tsx` 两处各 = 1

    所有 grep 用 `grep -v '^#'` 过滤注释行后再计数（CLAUDE.md 命令规范：避免 header prose 自 invalidated；本处 grep token 在代码/类型行内，注释行不影响 ≥ 断言，但 ESCAPE / invalidOnly 等关键字若出现在注释也计——可接受，因 acceptance 已要求代码逻辑存在，注释提及是 planner 标注惯例）。
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npx vite build && npm run build:electron-main && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
    - 四绿门禁（tsc / vite build / build:electron-main / vitest run）全 exit 0
    - vitest run 报告 ≥ 199 测试 PASS（191 既有 + 8 新增），0 fail
    - IPC 三向一致：experience:setDevices 与 experience:restore 在 ipc/preload/d.ts 三处各 1 命中
    - CR-01/CR-02/WR-01/问题 1a/问题 2/WR-05 grep 断言全通过（具体计数见 action 6-11）
  </acceptance_criteria>

  <done>
    三绿门禁 + esbuild 四绿全通过；8 新测试 PASS 零回归；IPC 三向一致；5 项必修 gap 全部 grep 可验证落地。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC (experience:restore) | untrusted renderer 可传任意 id（含 draft id），CR-01 修复前可绕 confirmDrafts 直 published |
| renderer → IPC (experience:setDevices) | untrusted renderer 可传 deviceIds[]（WR-02 修复前 N IPC 非原子） |
| renderer → IPC (experience:list) | untrusted renderer 可传 severity/tags/search（WR-01 修复前 tags LIKE 未转义；WR-06 入参校验 out_of_scope） |
| main 进程 MK → attrs_enc 解密 | CR-02 回填钩子解密历史 attrs_enc，须在 MK 注入后跑 |
| renderer ← IPC (experience:listDevices) | 设备 DTO 已走 rowToDevice 白名单投影（问题 1a 放开 filter 后此边界不变，关联不等于连接） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Elevation of Privilege | restoreExperience（draft→published 绕 confirmDrafts） | mitigate | CR-01：service 层 SELECT status/invalid_at 守卫 + SQL WHERE invalid_at IS NOT NULL 双层；draft/有效/不存在 id 显式 throw；vitest 4 用例覆盖 |
| T-10-04-02 | Tampering | backfillSeverityFromHistory 解密历史 attrs_enc | mitigate | CR-02：幂等 severity IS NULL 守卫 + try/catch 单行失败不阻塞 + 仅在 MK 注入后跑；decField 失败走既有 setDecryptFailureHandler 可观测 |
| T-10-04-03 | Tampering | tags LIKE 未转义 %/_ 通配符 | mitigate | WR-01：ESCAPE '\\' 子句 + 三类元字符转义；vitest 用例覆盖 '100%' 标签语义匹配 |
| T-10-04-04 | Repudiation | syncRelateDevices N IPC 非原子半成品状态 | mitigate | WR-02：setExperienceDevices 单事务（throw ROLLBACK），renderer 改单 IPC；service 层事务包 relate/unrelate diff |
| T-10-04-05 | Information Disclosure | 问题 1a 放开设备 filter 后关联设备候选 | accept | 设备 DTO 已走 rowToDevice 白名单投影（WR-05 既有，无 `_enc` 密文泄露）；关联是元数据引用不等于连接设备（CONTEXT 四类远程通道设计意图）；T-10-08 mitigation 仅适用 AI 起草模块（AI 助手仅连 ssh/telnet），手动 CRUD 不适用 |
| T-10-04-06 | Tampering | renderer 传 status='invalid' 查不到失效经验（bi-temporal 不匹配） | mitigate | 问题 2：状态 Select 联动 includeInvalid + service invalidOnly 路径筛 invalid_at<=now（非 status 列）；UI-SPEC §3 状态 Select 有效/已失效二选一语义对齐 |
| T-10-04-SC | Tampering | npm/pip installs | accept | 本 gap 无新增 npm 依赖（纯改既有文件 + vitest 既有基建），无包 legitimacy gate 触发 |

## Out-of-scope threats（记 follow-up，本 gap 不修）

- WR-04 loadExperiences 请求竞态（renderer fast-switch filter 过期列表覆盖）——记 follow-up
- WR-06 IPC 入参 severity/tags/search 缺枚举/长度校验（untrusted DoS）——记 follow-up
- WR-07 isValid/isInvalid 三方漂移（service/DetailModal/Tab 重复定义）——记 follow-up（本 gap 仅修 formatTs 兼容，不抽 util）
- WR-08 测试 MemDb 重复——记 follow-up
- WR-09 stripEncColumns 死代码——记 follow-up
- 问题 1b：ai 助手聊天设备范围扩展——独立新需求
- 问题 3：标签输入复用已有标签——UX 增强，用户明确讨论后处理
</threat_model>

<verification>
## 三绿门禁 + esbuild（Task 3 全量复跑）

| Behavior | Command | Expected |
| -------- | ------- | -------- |
| tsc strict + noUnusedLocals | `npx tsc -p tsconfig.web.json --noEmit` | exit 0 |
| vite build | `npx vite build` | exit 0（chunk size 警告非错误） |
| electron-main esbuild | `npm run build:electron-main` | exit 0（native 外部化） |
| vitest 全量 | `npx vitest run` | exit 0，≥199 测试全 PASS |

## Gap 闭环验证（5 项必修）

| Gap | 验证手段 | Expected |
| --- | -------- | -------- |
| CR-01 restore 守卫 | vitest 4 用例 + grep `AND invalid_at IS NOT NULL` + `草稿不可经 restore` | 全 PASS + grep 命中 |
| CR-02 severity 回填 | vitest 2 用例 + main.ts grep `backfillSeverityFromHistory` | 全 PASS + 钩子调用链落地 |
| 问题 2 状态联动 | tsc + vite build + grep `invalidOnly` / `setIncludeInvalid(true)` + UAT 测试 6/8 人工复测 | 编译过 + grep 命中 + 人工显失效可筛 |
| 问题 1a 设备 filter | grep `connectionType === 'ssh'` in ExperienceEditForm = 0 + UAT 测试 10 人工复测（候选含 web/rdp） | grep 0 + 人工候选含全类型 |
| WR-01 tags 转义 | vitest 1 用例（'100%' 不误命中 '100pa'） + grep `ESCAPE` | PASS + grep 命中 |
| WR-02 原子 | vitest 1 用例 + grep `setDevices` 三向一致 | PASS + 三向各 1 |
| WR-05 formatTs ISO | grep `ts.replace(' ', 'T')` 两处 + UAT 测试 18（时间格式无字面 T） | 两处各 1 + 人工时间正常 |
</verification>

<success_criteria>
- 5 项必修 gap 全部落地（CR-01 / CR-02 / 问题 2 / 问题 1a / WR-01+02+05），可 grep 可测试可审计
- 三绿门禁 + esbuild 四绿全通过，≥199 vitest 测试 PASS 零回归（既有 191 不破坏）
- IPC 三向一致（experience:setDevices / experience:restore 在 ipc/preload/d.ts 三处逐字相等）
- threat_model 完整：CR-01 红线③ / 问题 1a 设备白名单 / WR-01 注入面 / WR-02 原子性 / CR-02 解密面 全部 disposition 落地
- follow-up 项（问题 1b / 问题 3 / WR-03/04/06/07/08/09）明确 out_of_scope，不在本 gap 纳入
- 迁移幂等（CR-02 回填钩子 severity IS NULL 守卫，重跑无副作用）
</success_criteria>

<output>
Create `.planning/phases/10-experience-browse-page/10-04-SUMMARY.md` when done（含：5 gap 修复点 + 8 新测试清单 + 三绿门禁复跑结果 + threat_model 落地 + follow-up 清单）。
</output>
