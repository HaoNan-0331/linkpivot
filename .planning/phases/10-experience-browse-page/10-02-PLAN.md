---
phase: 10-experience-browse-page
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/knowledge/ExperienceEditForm.tsx
  - src/components/pages/ai/ReviewConfirmModal.tsx
  - src/components/pages/ai/ReviewConfirmEditForm.tsx
autonomous: true
requirements: [BROWSE-03]
tags: [refactor, renderer, form, quality-gate]

must_haves:
  truths:
    - "ExperienceEditForm 公共组件存在并被导出，字段结构与 Phase 9 ReviewConfirmEditForm 完全一致（标题/分类/内容/标签/tags mode + troubleshooting 类 attrs 模板五字段 + 关联设备 Select）"
    - "validateDraft 质量门单一来源（从 ReviewConfirmModal 迁入 ExperienceEditForm 导出），Phase 9 改 import 此处，消除双份漂移"
    - "ExperienceEditForm 接受 initialValue?: Experience（编辑态预填）/ onSubmit / onCancel props，状态由 form 内部 useState 持（uncontrolled，单条编辑，无批量决策列表）"
    - "ExperienceEditForm 去除 Phase 9 专属 UI：查看原始会话按钮 / supersedeOld Checkbox（手动 CRUD 无 UPDATE 语义）"
    - "手动新增提交直 createExperience({...fields, status:'published'})（D-10-1 红线③ 例外：人工录入非 AI 产出，不进 draft 闸口）"
    - "质量门未过 → 提交按钮 disabled（实时 validateDraft 标红 + help 错误串）"
    - "Phase 9 ReviewConfirmModal/ReviewConfirmEditForm 行为不回归（既有 175 测试全绿 + 表单中文化 label/severity/错误串不丢）"
  artifacts:
    - path: "src/components/knowledge/ExperienceEditForm.tsx"
      provides: "公共经验编辑表单组件 + validateDraft 质量门单一来源"
      exports: ["ExperienceEditForm", "validateDraft"]
      min_lines: 150
    - path: "src/components/pages/ai/ReviewConfirmModal.tsx"
      provides: "改 import validateDraft 自 ExperienceEditForm（删除本地 L49-62 定义或保留 re-export）"
      contains: "from '../../knowledge/ExperienceEditForm'"
    - path: "src/components/pages/ai/ReviewConfirmEditForm.tsx"
      provides: "复用 ExperienceEditForm 或仅共享 validateDraft（保守方案优先，降低 Phase 9 回归）"
  key_links:
    - from: "src/components/pages/ai/ReviewConfirmModal.tsx"
      to: "src/components/knowledge/ExperienceEditForm.tsx#validateDraft"
      via: "import { validateDraft }"
      pattern: "import.*validateDraft.*from.*ExperienceEditForm"
    - from: "src/components/knowledge/ExperienceEditForm.tsx"
      to: "window.api.experience.create"
      via: "createExperience 提交（手动新增直 published）"
      pattern: "status: 'published'"
---

<objective>
Phase 10 表单复用基线（D-10-1）：从 Phase 9 ReviewConfirmModal/ReviewConfirmEditForm 抽取 ExperienceEditForm 公共组件 + validateDraft 质量门单一来源，Phase 9 改 import 复用，为 10-03 手动 CRUD（新增/编辑 Modal）备好表单组件。

Purpose: 消除表单 + 质量门双份漂移（CONTEXT D-10-1 + 用户「优先可扩展性」偏好）。手动新增/编辑与 Phase 9 AI 确认弹窗永远走同一表单同一质量门。Wave 3 (10-03) 强依赖此 ExperienceEditForm 组件。

Output: 1 新文件（ExperienceEditForm.tsx）+ 2 跨 phase 重构文件（ReviewConfirmModal/ReviewConfirmEditForm 改 import）。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-experience-browse-page/10-CONTEXT.md
@.planning/phases/10-experience-browse-page/10-UI-SPEC.md
@.planning/phases/10-experience-browse-page/10-PATTERNS.md
@.planning/phases/09-human-review-confirmation/09-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- ExperienceEditForm props 契约（UI-SPEC §5 + PATTERNS §ExperienceEditForm 锁定） -->
interface ExperienceEditFormProps {
  initialValue?: Experience          // 编辑态预填；新增态 undefined
  onSubmit: (fields: ExperienceInput | ExperienceUpdateInput) => void
  onCancel: () => void
}

<!-- validateDraft 质量门单一来源（迁入 ExperienceEditForm，PATTERNS L139-155 excerpt） -->
validateDraft(d: Experience, fields: ExperienceUpdateInput): string[]
- cat === 'troubleshooting': 检 severity/symptoms(resolution 必填
- 其他类: 检 title/content 必填
- 错误串中文：「缺 严重程度」/「缺 故障现象」/「缺 解决办法」/「缺 标题」/「缺 内容」（与 UI-SPEC copy contract 逐字一致）

<!-- CATEGORY_OPTIONS / SEVERITY_OPTIONS 常量（中文化，value 存英文不变，09 cd87077 锁定） -->
- CATEGORY_OPTIONS: troubleshooting=故障排查 / best_practices=最佳实践 / product=产品 / env=环境
- SEVERITY_OPTIONS: critical=致命 / high=高 / medium=中 / low=低 / info=提示

<!-- 关联设备 Select（拉设备列表，PATTERNS L122-134 excerpt） -->
window.api.device.list() → filter connectionType === 'ssh'||'telnet' → map {id, name}
（保守沿用 Phase 9 ssh/telnet 过滤；CONTEXT discretion 提示可放开全设备，本 plan 优先沿用 Phase 9 范式降回归风险）

<!-- createExperience status? 入参（10-01 Task 2 已扩） -->
手动新增提交：createExperience({...fields, status: 'published'})（D-10-1 红线③ 例外）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ExperienceEditForm 公共组件 + validateDraft 单一来源</name>
  <files>src/components/knowledge/ExperienceEditForm.tsx</files>
  <read_first>
    - src/components/pages/ai/ReviewConfirmEditForm.tsx（全文 1-184，字段结构 + 分类切换 attrs 显隐 + 设备 Select + Form.Item validateStatus/help 标红）
    - src/components/pages/ai/ReviewConfirmModal.tsx L49-62（validateDraft 质量门，迁入新文件导出）+ L200 附近（提交按钮 disabled 模式）
    - src/types/experience.ts（Experience / ExperienceInput / ExperienceUpdateInput / ExperienceAttrs / ExperienceCategory DTO）
    - .planning/phases/10-experience-browse-page/10-PATTERNS.md §ExperienceEditForm.tsx 段（L98-188，含 5 段 excerpts + Component props 契约 + delta）
    - .planning/phases/10-experience-browse-page/10-UI-SPEC.md §5 公共组件抽取 + §Copywriting 红线③ 注意
  </read_first>
  <action>
新建 `src/components/knowledge/ExperienceEditForm.tsx`（路径从 pages/ 下一级，与 KnowledgeBasePage 同域）。

**结构（完整复刻 ReviewConfirmEditForm 字段，apply deltas）：**

1. **导出 CATEGORY_OPTIONS / SEVERITY_OPTIONS 常量**（中文化，value 存英文不变，与 09 cd87077 逐字一致）：troubleshooting=故障排查 / best_practices=最佳实践 / product=产品 / env=环境；severity critical=致命 / high=高 / medium=中 / low=低 / info=提示。

2. **导出 validateDraft(d: Experience, fields: ExperienceUpdateInput): string[]**（从 ReviewConfirmModal.tsx L49-62 整体迁入）：troubleshooting 类检 severity/symptoms/resolution 必填，其他类检 title/content 必填；错误串中文与 UI-SPEC copy contract 逐字一致。

3. **ExperienceEditForm 组件**（props: `initialValue?: Experience`, `onSubmit`, `onCancel`）：
   - 内部 useState 持表单状态（uncontrolled，单条编辑；不再像 ReviewConfirmEditForm 经 onChange/decision patch 回主壳）
   - initialValue 预填路径：编辑态从 Experience DTO 反向填 form state（title/category/content/tags/attrs.severity/attrs.symptoms/attrs.root_cause/attrs.resolution/attrs.prevention/关联设备 ids）
   - 字段：标题 Input / 分类 Select（CATEGORY_OPTIONS）/ 内容 TextArea / 标签 Select（mode="tags"，可创建新标签）/ troubleshooting 类显 attrs 模板五字段（严重程度 Select SEVERITY_OPTIONS + 故障现象 + 根本原因 + 解决办法 + 预防措施，分类切换动态呈现 `{cat === 'troubleshooting' && (<>...</>)}`）/ 关联设备 Select（多选，拉 device.list filter ssh/telnet）
   - 关联设备初始化：编辑态从 initialValue 经 props 传入的关联设备 ids 预填（由调用方 ExperienceTab 在打开 Modal 前先 listDevices 取 ids 传入，或 ExperienceEditForm 内部 useEffect 调 listDevices(initialValue.id) 预填——优先内部 useEffect）
   - **去除 Phase 9 专属 UI**：「查看原始会话」按钮（详情 Modal 侧独立入口）、supersedeOld Checkbox（手动编辑无 UPDATE 语义）
   - 实时质量门：`const errs = validateDraft(initialValue ?? emptyExp, currentFields)`；每个 Form.Item `validateStatus={errs.includes('缺 X') ? 'error' : ''}` + `help={errs.includes('缺 X') ? '缺 X' : ''}` 标红
   - 提交按钮 `disabled={errs.length > 0}`（沿用 ReviewConfirmModal L200 disabled 模式）
   - onSubmit 回调签名区分新增/编辑：新增态调 onSubmit({...fields, status: 'published'})（D-10-1 红线③ 例外）；编辑态调 onSubmit({...fields})（不含 status，update 白名单 CR-01 不收 status）—— 由 initialValue 是否存在判定
   - footer 主按钮文案「保存」（**禁止**「确认入库」「发布并待审」等暗示 AI 闸口的措辞，UI-SPEC copy 红线③）
   - patchAttr 保留其余 attrs 字段（ReviewConfirmEditForm L68-70 范式）：`{ ...attrs, [key]: value }`
   - 表单 Modal width 640（UI-SPEC §4），单列 Form `layout="vertical"`——**注意**：ExperienceEditForm 本身是表单内容组件，外层 Modal 由调用方（ExperienceTab）包裹；ExperienceEditForm 不自带 Modal 壳，只渲染 Form + footer Button

格式红线：2 空格缩进 / 单引号 / 无分号 / 尾随逗号（多行）；AntD 桶式 `import { Form, Input, Select, Button, Space } from 'antd'`；icon `from '@ant-design/icons'`。
  </action>
  <verify>
    <automated>
cd "E:/knowlegdge_base/claude/network_toplogy" && npx tsc -p tsconfig.web.json --noEmit && npx vite build && npx vitest run
    </automated>
  </verify>
  <acceptance_criteria>
- `test -f src/components/knowledge/ExperienceEditForm.tsx` exit 0
- `grep -c "export function validateDraft" src/components/knowledge/ExperienceEditForm.tsx` 返回 1
- `grep -c "export default function ExperienceEditForm" src/components/knowledge/ExperienceEditForm.tsx` 返回 1（或 `export function ExperienceEditForm`）
- `grep -c "故障排查" src/components/knowledge/ExperienceEditForm.tsx` 返回 1（CATEGORY_OPTIONS 中文化）
- `grep -c "致命" src/components/knowledge/ExperienceEditForm.tsx` 返回 1（SEVERITY_OPTIONS 中文化）
- `grep -c "缺 严重程度" src/components/knowledge/ExperienceEditForm.tsx` 返回 ≥1（validateDraft 错误串）
- `grep -c "initialValue" src/components/knowledge/ExperienceEditForm.tsx` 返回 ≥2（props + 使用）
- `grep -c "status: 'published'" src/components/knowledge/ExperienceEditForm.tsx` 返回 ≥1（手动新增直 published）
- `grep -c "disabled={errs.length > 0}" src/components/knowledge/ExperienceEditForm.tsx` 返回 ≥1（提交按钮质量门）
- `grep -c "保存" src/components/knowledge/ExperienceEditForm.tsx` 返回 ≥1（footer 文案）
- `grep -c "supersedeOld" src/components/knowledge/ExperienceEditForm.tsx` 返回 0（去除 Phase 9 专属 UI）
- `grep -c "查看原始会话" src/components/knowledge/ExperienceEditForm.tsx` 返回 0（去除）
- tsc + vite build + vitest run 全 exit 0（既有 175 测试不受影响，因 Phase 9 还未改 import）
  </acceptance_criteria>
  <done>ExperienceEditForm 公共组件落地，字段结构完整复刻 ReviewConfirmEditForm，validateDraft 单一来源导出，去除 Phase 9 专属 UI，手动新增直 published，质量门 disabled 提交，footer 文案「保存」，三绿门禁全绿。</done>
</task>

<task type="auto">
  <name>Task 2: Phase 9 ReviewConfirmModal/ReviewConfirmEditForm 改 import 复用 validateDraft（保守方案，单一来源）</name>
  <files>src/components/pages/ai/ReviewConfirmModal.tsx, src/components/pages/ai/ReviewConfirmEditForm.tsx</files>
  <read_first>
    - src/components/pages/ai/ReviewConfirmModal.tsx L49-62（本地 validateDraft 定义，待删除/改 import）+ 全文 import 区
    - src/components/pages/ai/ReviewConfirmEditForm.tsx 全文（确认是否依赖 ReviewConfirmModal 的 validateDraft，决定 ReviewConfirmEditForm 是否也需改 import）
    - src/components/knowledge/ExperienceEditForm.tsx（本 plan Task 1 新建，确认 validateDraft 导出签名一致）
    - .planning/phases/10-experience-browse-page/10-PATTERNS.md §跨 phase 重构 段（L547-562，含保守 vs 优先方案 + Phase 9 不动语义红线）
  </read_first>
  <action>
**保守方案（降低 Phase 9 回归风险）：仅共享 validateDraft 单一来源，ReviewConfirmEditForm 表单字段结构不动。**

1. **ReviewConfirmModal.tsx**：
   - 删除 L49-62 本地 `export function validateDraft(...)` 定义
   - 顶部 import 区追加：`import { validateDraft } from '../../knowledge/ExperienceEditForm'`
   - 保留对 validateDraft 的所有调用点不变（签名逐字一致，无调用方改动）
   - 若 ReviewConfirmModal 有 re-export validateDraft 的下游 import（如 ReviewConfirmEditForm `import { validateDraft } from './ReviewConfirmModal'`），保留 ReviewConfirmModal 内 `export { validateDraft } from '../../knowledge/ExperienceEditForm'` re-export 形式（避免下游 import 路径连锁改动）

2. **ReviewConfirmEditForm.tsx**：
   - 若当前 `import { validateDraft } from './ReviewConfirmModal'`（re-export 链），保持不变（re-export 已指向新单一来源）
   - 若直接定义或从其他路径 import，改 `import { validateDraft } from '../../knowledge/ExperienceEditForm'`

**Phase 9 不动语义红线**（PATTERNS L556）：
- ReviewConfirmEditForm 字段结构不动（UPDATE supersedeOld Checkbox + 查看原始会话 Button 是 Phase 9 专属，**不能丢**）
- 仅 validateDraft 来源切到单一源（消除双份漂移），表单渲染逻辑零改动
- 不将 ReviewConfirmEditForm 薄壳化为渲染 `<ExperienceEditForm>`（保守方案，避免 Phase 9 行为回归）

import 路径：从 `src/components/pages/ai/` 到 `src/components/knowledge/` 是 `'../../knowledge/ExperienceEditForm'`（pages/ai → pages → components → knowledge，两级 `../`）。
  </action>
  <verify>
    <automated>
cd "E:/knowlegdge_base/claude/network_toplogy" && npx tsc -p tsconfig.web.json --noEmit && npx vite build && npm run build:electron-main && npx vitest run
    </automated>
  </verify>
  <acceptance_criteria>
- `grep -c "import { validateDraft } from '../../knowledge/ExperienceEditForm'" src/components/pages/ai/ReviewConfirmModal.tsx` 返回 ≥1（或 ReviewConfirmModal re-export，二者至少其一指向新源）
- ReviewConfirmModal.tsx 不再有本地 `export function validateDraft(` 定义（`grep -c "export function validateDraft(" src/components/pages/ai/ReviewConfirmModal.tsx` 返回 0，除非保留 re-export `export { validateDraft }`）
- ReviewConfirmEditForm.tsx 仍含 supersedeOld Checkbox + 查看原始会话 Button（`grep -c "supersedeOld" src/components/pages/ai/ReviewConfirmEditForm.tsx` ≥1 且 `grep -c "查看原始会话" src/components/pages/ai/ReviewConfirmEditForm.tsx` ≥1，Phase 9 专属 UI 不丢）
- validateDraft 错误串在 ReviewConfirmModal.tsx 不再硬编码（grep `'缺 严重程度'` 在 ReviewConfirmModal.tsx 返回 0，已迁出）
- 既有 175 测试全绿（含 ai.telnetRouting 9 case + service 19 case，Phase 9 表单行为零回归）
- tsc + vite build + build:electron-main + vitest run 全 exit 0
  </acceptance_criteria>
  <done>validateDraft 单一来源迁移完成（ReviewConfirmModal 改 import ExperienceEditForm，删除本地定义或保留 re-export），Phase 9 表单字段结构与专属 UI（supersedeOld/查看原始会话）不回归，既有 175 测试全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer 组件间 | ExperienceEditForm 经 props 接收 initialValue（Experience DTO，已脱敏）+ 调 onSubmit 回传 fields 给调用方（ExperienceTab/AI 确认弹窗） |
| renderer→IPC | onSubmit 内 createExperience/updateExperience 调 window.api.experience.*（经既有 secure IPC，本 plan 不新增 IPC 面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Elevation | 手动新增直 published 绕 AI draft 闸口 | mitigate | 红线③ 例外：D-10-1 设计意图（人工录入非 AI 产出，UI-SPEC copy 红线③ 注意明确）；createExperience status? 入参默认 'draft'，仅手动新增显式传 'published'，AI 起草路径（Phase 8）零改动仍走 draft |
| T-10-08 | Tampering | ExperienceEditForm 关联设备 Select | mitigate | 复用 ReviewConfirmEditForm 既有 device.list + filter ssh/telnet 范式（renderer 只收 Device DTO 明文字段，IPC stripEncColumns 已剥离 _enc，不外泄设备密文） |
| T-10-09 | Information | initialValue 预填 attrs 解密值 | accept | Experience DTO 的 attrs 已由 service rowToExperience 解密回填 + delete attrs_enc（信任边界红线），renderer 收明文 attrs 是设计意图（详情/编辑需展示），不含凭证字段（凭证走 **** 脱敏另径） |
| T-10-10 | Denial | validateDraft 漂移导致质量门绕过 | mitigate | 单一来源（D-10-1 核心）：validateDraft 迁入 ExperienceEditForm 导出，Phase 9 改 import 消除双份；service 层 assertTroubleshootingAttrs 兜底（第三层纵深，不动） |
</threat_model>

<verification>
- 三绿门禁：tsc + vite build + build:electron-main + vitest run 全 exit 0（既有 175 测试零回归）
- validateDraft 单一来源：ReviewConfirmModal 不再本地定义，改 import ExperienceEditForm
- Phase 9 行为不回归：ReviewConfirmEditForm 字段结构 + supersedeOld + 查看原始会话不丢
- 红线③ copy 约束：ExperienceEditForm footer「保存」，无「确认入库」/「发布并待审」措辞
- 质量门 disabled：提交按钮 errs.length > 0 时 disabled
</verification>

<success_criteria>
- ExperienceEditForm 公共组件落地（BROWSE-03 手动新增/编辑表单组件就位）
- validateDraft 质量门单一来源（D-10-1 核心目标，消除 Phase 9/10 双份漂移）
- 手动新增直 published（D-10-1 红线③ 例外落地）
- Phase 9 表单复用 + 行为零回归（既有 175 测试全绿）
- 为 Wave 3（10-03 ExperienceTab 手动 CRUD Modal）备好表单组件
</success_criteria>

<output>
Create `.planning/phases/10-experience-browse-page/10-02-SUMMARY.md` when done
</output>
