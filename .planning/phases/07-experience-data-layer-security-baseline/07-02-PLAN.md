---
phase: 07-experience-data-layer-security-baseline
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - electron/ipc/experienceIpc.ts
  - electron/main.ts
  - electron/preload.ts
  - src/types/experience.ts
  - src/types/electron.d.ts
autonomous: true
requirements: [SEC-01, EXP-01, EXP-02, EXP-03]
tags: [ipc, auth, preload, types, security]

must_haves:
  truths:
    - "所有 experience:* IPC channel 经 secure(...)（特权，鉴权+脱敏）或 safe(...)（登录前，仅脱敏）包装，无漏网 channel"
    - "channel 命名遵循 experience:<action> 格式，action 部分遵循全仓 camelCase 事实约定（experience:list/get/create/update/delete/invalidate/relateDevice/unrelateDevice/listByDevice/listDevices）"
    - "list channel 强制 MAX_BATCH=1000 上限（service 层已实现，IPC 层透传 opts.limit，不绕过）"
    - "renderer 永不收明文凭证/密文 attrs_enc——IPC 返回前 service 已 decField 回填并 delete attrs_enc"
    - "renderer 永不收设备密文列（name_enc 等）——experience:listDevices 在 IPC 边界剥离所有 `_enc` 后缀列后返回"
    - "preload.ts 经 contextBridge 暴露 window.api.experience.* 白名单方法，renderer 只能调白名单 channel"
    - "main.ts 启动时注入 setExperienceMasterKey(masterKey) + 注册 registerExperienceIpc()"
    - "experienceService.ts 的 main.ts 注入点位于 setKbMasterKey 之后（同一 masterKey，注入顺序与既有 6 个 service 一致）"
  artifacts:
    - path: "electron/ipc/experienceIpc.ts"
      provides: "registerExperienceIpc()：所有 experience:* channel 经 secure/safe 包装；listDevices 边界剥离 _enc 列"
      exports: ["registerExperienceIpc"]
    - path: "electron/main.ts"
      provides: "setExperienceMasterKey 注入 + registerExperienceIpc 调用"
      contains: "setExperienceMasterKey"
    - path: "electron/preload.ts"
      provides: "window.api.experience.* 白名单方法暴露"
      contains: "experience:"
    - path: "src/types/experience.ts"
      provides: "Experience/ExperienceInput/ExperienceListResult DTO 类型"
      exports: ["Experience", "ExperienceInput"]
    - path: "src/types/electron.d.ts"
      provides: "ElectronAPI.experience 通道类型声明"
      contains: "experience:"
  key_links:
    - from: "electron/ipc/experienceIpc.ts"
      to: "electron/services/experienceService.ts"
      via: "具名函数调用（函数式 service，非 class 静态方法）"
      pattern: "\\b(createExperience|getExperience|listExperiences|updateExperience|invalidateExperience|deleteExperience|relateDevice|unrelateDevice|listDevicesByExperience|listExperiencesByDevice)\\("
    - from: "electron/ipc/experienceIpc.ts"
      to: "electron/utils/authGuard.ts"
      via: "secure()/safe() 包装每个 handler"
      pattern: "secure\\(|safe\\("
    - from: "electron/main.ts"
      to: "electron/services/experienceService.ts"
      via: "setExperienceMasterKey(masterKey) 启动注入"
      pattern: "setExperienceMasterKey"
---

<objective>
为 Plan 01 建好的 experienceService（函数式）暴露安全的 IPC 网关：所有 experience:* channel 经 secure/safe 鉴权脱敏包装，preload 暴露白名单方法，main.ts 注入 masterKey 并注册 IPC，renderer 侧补类型声明；experience:listDevices 在 IPC 边界剥离设备密文列。

Purpose: 立安全基线——renderer 永不收明文凭证/密文，每个 channel 必经鉴权，channel 命名遵循全仓 camelCase 事实约定（CONVENTIONS.md line 35）。这是 SEC-01/SEC-02 的 IPC 落地点，Phase 8-11 的 IPC 复用此契约。

Output: experienceIpc.ts（10 channel 全 secure 包装 + listDevices 边界脱敏）+ main.ts 注入注册 + preload 暴露 + experience.ts DTO + electron.d.ts 类型。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/ARCHITECTURE.md

# Plan 01 产出（本 plan 直接消费其 service 契约）
@.planning/phases/07-experience-data-layer-security-baseline/07-01-SUMMARY.md

# v1.0 基线源码（read_first，复用其 IPC/preload/main/types 模式）
@electron/ipc/knowledgeBaseIpc.ts
@electron/utils/authGuard.ts
@electron/preload.ts
@electron/main.ts
@src/types/kb.ts
@src/types/electron.d.ts

<interfaces>
<!-- executor 直接用这些契约，无需再探查代码库 -->

From electron/services/experienceService.ts (Plan 01 产出，**函数式 + 模块级 MK，无 class**):
```typescript
export const MAX_BATCH = 1000
export type ExperienceCategory = 'troubleshooting' | 'best_practices' | 'product' | 'env'
export type ExperienceStatus = 'draft' | 'confirmed' | 'published' | 'invalid'
export interface ExperienceAttrs {
  symptoms?: string; root_cause?: string; resolution?: string; prevention?: string
  severity?: 'critical' | 'high' | 'medium' | 'low' | 'info'
}
export interface ExperienceInput {
  title: string; category: ExperienceCategory; content: string
  tags?: string[]; sourceSessionId?: string | null; attrs?: ExperienceAttrs | null
}
export function setExperienceMasterKey(key: string): void
export function createExperience(input: ExperienceInput): any
export function getExperience(id: string): any | null
export function listExperiences(opts: { category?: ExperienceCategory; status?: ExperienceStatus; deviceId?: string; includeInvalid?: boolean; limit?: number; offset?: number }): { rows: any[]; total: number; truncated: boolean }
export function updateExperience(id: string, fields: Partial<{...}>): any
export function invalidateExperience(id: string): any
export function deleteExperience(id: string): void
export function relateDevice(experienceId: string, deviceId: string, relationType?: string): void
export function unrelateDevice(experienceId: string, deviceId: string): void
export function listDevicesByExperience(experienceId: string): any[]   // 返 devices 原始行（含 name_enc 等密文列，IPC 边界剥离）
export function listExperiencesByDevice(deviceId: string, includeInvalid?: boolean): any[]
```

From electron/utils/authGuard.ts:
```typescript
export function secure(handler: (e: any, ...args: any[]) => any): (e: any, ...args: any[]) => Promise<any>
// 未登录 throw '未登录或会话已过期'；handler 抛错 → reject 脱敏 Error（sanitizeMessage 移路径/截断）
export function safe(handler: (e: any, ...args: any[]) => any): (e: any, ...args: any[]) => Promise<any>
// 仅脱敏，不鉴权（登录前 channel 用）
```

From electron/main.ts (注入点位置，line 85-91):
```typescript
masterKey = getOrCreateMasterKey()
setDeviceMasterKey(masterKey)
setTopologyMasterKey(masterKey)
setConnectionMasterKey(masterKey)
setAiMasterKey(masterKey)
setArpMasterKey(masterKey)
setKbMasterKey(masterKey)   // ← 本 plan 在此行后追加 setExperienceMasterKey(masterKey)
// line 132: registerKbIpc()  ← 本 plan 在此行后追加 registerExperienceIpc()
```

From electron/preload.ts (kb 块模式，line 110-123): api 对象内追加 experience 块，方法签名对齐 IPC channel。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 experienceIpc.ts（10 channel 全 secure 包装 + listDevices 边界剥离 _enc 列）+ experience.ts DTO + electron.d.ts 类型</name>
  <files>electron/ipc/experienceIpc.ts, src/types/experience.ts, src/types/electron.d.ts</files>

  <read_first>
    - electron/ipc/knowledgeBaseIpc.ts（IPC 注册完整范例：import service 函数 + import secure + ipcMain.handle('domain:action', secure((e, ...args) => serviceFn(...)))）
    - electron/utils/authGuard.ts（secure/safe 签名与脱敏语义）
    - electron/services/experienceService.ts（Plan 01 产出，**函数式 service 全函数签名** + ExperienceInput/ExperienceAttrs 类型；listDevicesByExperience 返 devices 原始行含 name_enc 等密文列）
    - src/types/kb.ts（DTO 定义模式：snake_case DB 行保留，注释标注消费面）
    - src/types/electron.d.ts（ElectronAPI 接口结构 + 各 channel 块范例，看 kb 块 line 145-158）
  </read_first>

  <action>
**1. src/types/experience.ts**（新建，DTO 严格反推自 experienceService 返回 + IPC 调用面）:
```typescript
import type { PaginatedResult } from './pagination'

export type ExperienceCategory = 'troubleshooting' | 'best_practices' | 'product' | 'env'
export type ExperienceStatus = 'draft' | 'confirmed' | 'published' | 'invalid'

export interface ExperienceAttrs {
  symptoms?: string
  root_cause?: string
  resolution?: string
  prevention?: string
  severity?: 'critical' | 'high' | 'medium' | 'low' | 'info'
}

export interface ExperienceInput {
  title: string
  category: ExperienceCategory
  content: string
  tags?: string[]
  sourceSessionId?: string | null
  attrs?: ExperienceAttrs | null
}

export interface ExperienceUpdateInput {
  title?: string
  category?: ExperienceCategory
  content?: string
  tags?: string[]
  status?: ExperienceStatus
  attrs?: ExperienceAttrs | null
  validAt?: string
  invalidAt?: string | null
  lastVerifiedAt?: string
  reuseCount?: number
}

export interface ExperienceListInput {
  category?: ExperienceCategory
  status?: ExperienceStatus
  deviceId?: string
  includeInvalid?: boolean
  limit?: number
  offset?: number
}

/** DB 行原生 snake_case（service 层 attrs_enc 已解密回填 attrs 并 delete 密文列） */
export interface Experience {
  id: string
  title: string
  category: ExperienceCategory
  content: string
  tags: string[]              // JSON.parse 后
  status: ExperienceStatus
  source_session_id?: string | null
  attrs?: ExperienceAttrs | null
  valid_at: string
  invalid_at?: string | null
  last_verified_at?: string | null
  reuse_count: number
  created_at: string
  updated_at: string
}

export type ExperienceListResult = PaginatedResult<Experience>

/** experience:listDevices 返回的关联设备（IPC 边界已剥离所有 `_enc` 后缀密文列） */
export interface ExperienceRelatedDevice {
  id: string
  // 仅保留 device 非密文字段（name_enc 等 `_enc` 列在 IPC handler 剥离，renderer 永不可见）
  [key: string]: unknown
}
```

**2. electron/ipc/experienceIpc.ts**（新建，10 channel 全 secure 包装——经验属特权操作，全走 secure 不走 safe；channel action 遵循全仓 camelCase 事实约定，复合词 action 用 camelCase）:
```typescript
import { ipcMain } from 'electron'
import {
  createExperience,
  getExperience,
  listExperiences,
  updateExperience,
  invalidateExperience,
  deleteExperience,
  relateDevice,
  unrelateDevice,
  listDevicesByExperience,
  listExperiencesByDevice,
} from '../services/experienceService'
import type { ExperienceInput, ExperienceUpdateInput, ExperienceListInput } from '../../src/types/experience'
import { secure } from '../utils/authGuard'

// IPC 边界剥离设备密文列：listDevicesByExperience 返 devices 原始行含 name_enc 等 `_enc` 后缀列，
// renderer 不可见密文。在 IPC handler 返回前 map 删除所有以 `_enc` 结尾的 key（SEC-02）。
function stripEncColumns(rows: any[]): any[] {
  return rows.map((row) => {
    const safe: Record<string, unknown> = {}
    for (const key of Object.keys(row)) {
      if (!key.endsWith('_enc')) safe[key] = (row as Record<string, unknown>)[key]
    }
    return safe
  })
}

export function registerExperienceIpc() {
  // 经验属特权操作（涉敏感 attrs/凭证片段），全 secure 包装（鉴权 + 异常脱敏）
  ipcMain.handle('experience:list', secure((_e, opts?: ExperienceListInput) =>
    listExperiences(opts || {})))

  ipcMain.handle('experience:get', secure((_e, id: string) =>
    getExperience(id)))

  ipcMain.handle('experience:create', secure((_e, input: ExperienceInput) =>
    createExperience(input)))

  ipcMain.handle('experience:update', secure((_e, id: string, fields: ExperienceUpdateInput) =>
    updateExperience(id, fields)))

  ipcMain.handle('experience:delete', secure((_e, id: string) =>
    deleteExperience(id)))

  ipcMain.handle('experience:invalidate', secure((_e, id: string) =>
    invalidateExperience(id)))

  ipcMain.handle('experience:relateDevice', secure((_e, experienceId: string, deviceId: string, relationType?: string) =>
    relateDevice(experienceId, deviceId, relationType)))

  ipcMain.handle('experience:unrelateDevice', secure((_e, experienceId: string, deviceId: string) =>
    unrelateDevice(experienceId, deviceId)))

  ipcMain.handle('experience:listByDevice', secure((_e, deviceId: string, includeInvalid?: boolean) =>
    listExperiencesByDevice(deviceId, includeInvalid)))

  ipcMain.handle('experience:listDevices', secure((_e, experienceId: string) =>
    stripEncColumns(listDevicesByExperience(experienceId))))
}
```

**注意:** 全部 10 channel 用 `secure(...)`（鉴权 + 异常脱敏）。经验数据天然属登录后特权操作，无登录前 channel（不像 auth:* 需 safe）。service 已改函数式，故 IPC 按需 import 具名函数（createExperience/listExperiences 等），不再 import `ExperienceService` class；**不 import `MAX_BATCH`**——IPC 层透传 opts.limit、不二次校验（service 层 listExperiences 内强制 MAX_BATCH=1000 throw），避免双层校验逻辑漂移，且避免未使用 import 触发 tsconfig.web noUnusedLocals。**channel 命名遵循全仓 camelCase 事实约定:** 复合词 action 用 camelCase（`relateDevice`/`unrelateDevice`/`listByDevice`/`listDevices`），单词 action 不变（`list`/`get`/`create`/`update`/`delete`/`invalidate`），与既有 channel（`kb:listDocuments`/`anomaly:acknowledgeAll`/`oui:addBatch`/`network:getIPDetails`/`arp:collectFromAll`/`export:arpTable`）一致。**experience:listDevices 边界脱敏（SEC-02）:** listDevicesByExperience 返 devices 原始行含 name_enc 等密文列，IPC handler 返回前经 `stripEncColumns` 删除所有 `_enc` 后缀 key，renderer 永不收设备密文（service 不解密设备名，IPC 边界统一剥离，不越域调 device 解密通道）。其余 channel（list/get/create/update/invalidate）返回值不经 IPC 二次处理——service 层 getExperience/listExperiences 已 decField 回填 attrs 并 delete attrs_enc，密文/凭证不外泄。

**3. src/types/electron.d.ts** — 两处改动:
- (a) 顶部 import 追加:
```typescript
import type { Experience, ExperienceInput, ExperienceUpdateInput, ExperienceListResult, ExperienceListInput, ExperienceCategory, ExperienceStatus, ExperienceRelatedDevice } from './experience'
```
- (b) ElectronAPI 接口内（kb 块之后、network 块之前，或末尾）追加 experience 块:
```typescript
experience: {
  list: (opts?: ExperienceListInput) => Promise<ExperienceListResult>
  get: (id: string) => Promise<Experience | null>
  create: (input: ExperienceInput) => Promise<Experience>
  update: (id: string, fields: ExperienceUpdateInput) => Promise<Experience>
  delete: (id: string) => Promise<void>
  invalidate: (id: string) => Promise<Experience>
  relateDevice: (experienceId: string, deviceId: string, relationType?: string) => Promise<void>
  unrelateDevice: (experienceId: string, deviceId: string) => Promise<void>
  listByDevice: (deviceId: string, includeInvalid?: boolean) => Promise<Experience[]>
  listDevices: (experienceId: string) => Promise<ExperienceRelatedDevice[]>
}
```
  </action>

  <verify>
    <automated>cd /e/knowlegdge_base/claude/network_toplogy && grep -c "ipcMain.handle('experience:" electron/ipc/experienceIpc.ts; grep -c "secure(" electron/ipc/experienceIpc.ts; grep -c "safe(" electron/ipc/experienceIpc.ts; grep -c "experience:" electron/preload.ts; grep -c "Experience" src/types/electron.d.ts; grep -c "stripEncColumns" electron/ipc/experienceIpc.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "ipcMain.handle('experience:" electron/ipc/experienceIpc.ts` = 10（10 个 channel 全注册）
    - `grep -oE "'experience:[a-zA-Z-]+'" electron/ipc/experienceIpc.ts | sort -u | wc -l` = 10（10 个 channel 名去重后精确 10；字符类含 `a-zA-Z-` 以匹配 camelCase action 如 `listByDevice`/`listDevices`/`relateDevice`/`unrelateDevice`，词界隔离防 list 被 listByDevice/listDevices 子串重复命中）
    - `grep -c "secure(" electron/ipc/experienceIpc.ts` = 10（每个 handler 都 secure 包装）
    - `grep -c "safe(" electron/ipc/experienceIpc.ts` = 0（无 safe——经验无登录前 channel）
    - `grep "export function registerExperienceIpc" electron/ipc/experienceIpc.ts` 命中 1 次
    - `grep "import {" electron/ipc/experienceIpc.ts | grep "createExperience"` 命中 1 次（函数式 service 按需 import）
    - `grep -c "MAX_BATCH" electron/ipc/experienceIpc.ts` = 0（IPC 层不 import MAX_BATCH，避免 noUnusedLocals）
    - `grep -c "ExperienceService" electron/ipc/experienceIpc.ts` = 0（service 已函数式，无 class 引用）
    - `grep -c "stripEncColumns" electron/ipc/experienceIpc.ts` ≥ 2（定义 1 + listDevices 调用 1，SEC-02 边界脱敏落地）
    - `grep "endsWith('_enc')" electron/ipc/experienceIpc.ts` 命中 1 次（剥离逻辑按 `_enc` 后缀判定）
    - `grep "export interface Experience " src/types/experience.ts` 命中 1 次
    - `grep "export interface ExperienceRelatedDevice" src/types/experience.ts` 命中 1 次
    - `grep "export type ExperienceCategory\|export type ExperienceStatus" src/types/experience.ts` 命中 2 次
    - `grep "ExperienceListResult = PaginatedResult" src/types/experience.ts` 命中 1 次（复用分页信封）
    - `grep "experience:" src/types/electron.d.ts | wc -l` ≥ 10（ElectronAPI.experience 块 10 方法声明）
    - `npx tsc -p tsconfig.web.json --noEmit` 无新增错误（experienceIpc.ts/experience.ts/electron.d.ts 类型干净，noUnusedLocals 通过——MAX_BATCH 未 import 故无未使用告警；preload/main 注入在 Task 2，本 task preload 暂未加 experience 块不报错因 preload 用 unknown）
  </acceptance_criteria>

  <done>
    experienceIpc.ts 注册 10 channel 全 secure 包装（函数式 service 具名 import，无 class、无未使用 MAX_BATCH）；channel 命名 experience:<action> 规范统一，action 遵循全仓 camelCase 事实约定（`'experience:[a-zA-Z-]+'` 精确 10）；experience:listDevices 经 stripEncColumns 边界剥离 `_enc` 密文列（SEC-02 落地）；experience.ts DTO 完整（含 4 态 status/4 类 category 枚举 + ExperienceAttrs 模板字段 + PaginatedResult 复用 + ExperienceRelatedDevice 脱敏类型）；electron.d.ts ElectronAPI.experience 类型声明落地；renderer 经类型安全调用，无 any 绕过。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: main.ts 注入 setExperienceMasterKey + 注册 registerExperienceIpc；preload.ts 暴露 window.api.experience.*</name>
  <files>electron/main.ts, electron/preload.ts</files>

  <read_first>
    - electron/main.ts（line 10-27 import 区：setKbMasterKey + registerKbIpc 范例；line 85-91 masterKey 注入区：setKbMasterKey(masterKey)；line 126-133 IPC 注册区：registerKbIpc()）
    - electron/preload.ts（line 110-123 kb 块结构：api 对象内 `kb: { method: (...args) => ipcRenderer.invoke('channel', ...args) }`）
    - electron/ipc/experienceIpc.ts（Task 1 产出，确认 channel 名清单）
    - electron/services/experienceService.ts（Plan 01，确认 setExperienceMasterKey 导出名）
  </read_first>

  <action>
**1. electron/main.ts** — 四处改动:
- (a) import 区（line 26-27 kb import 之后）追加:
```typescript
import { setExperienceMasterKey } from './services/experienceService'
import { registerExperienceIpc } from './ipc/experienceIpc'
```
- (b) masterKey 注入区（line 91 `setKbMasterKey(masterKey)` 之后）追加:
```typescript
setExperienceMasterKey(masterKey)
```
- (c) IPC 注册区（line 132 `registerKbIpc()` 之后）追加:
```typescript
registerExperienceIpc()
```

**注意:** 注入顺序——setExperienceMasterKey 必须在 initDatabase()/createTables()/migrateAndSecure()（line 100-102）之前？不，看 v1.0 模式：setKbMasterKey 在 line 91（masterKey 注入区），而 initDatabase 在 line 100，createTables 在 line 101。即 masterKey 注入先于建表/迁移，service 模块级 MK 在迁移/建表时已就绪（迁移本身不调 service，但启动后任何 IPC 调 service 时 MK 已注入）。setExperienceMasterKey 同位置追加，保持一致性。MK 用同一个 `getOrCreateMasterKey()` 返回值（与 6 个既有 service 共享 masterKey，与 keyManager safeStorage 落盘一致）。

**2. electron/preload.ts** — api 对象内（kb 块 line 110-123 之后）追加 experience 块:
```typescript
experience: {
  list: (opts?: unknown) => ipcRenderer.invoke('experience:list', opts),
  get: (id: string) => ipcRenderer.invoke('experience:get', id),
  create: (input: unknown) => ipcRenderer.invoke('experience:create', input),
  update: (id: string, fields: unknown) => ipcRenderer.invoke('experience:update', id, fields),
  delete: (id: string) => ipcRenderer.invoke('experience:delete', id),
  invalidate: (id: string) => ipcRenderer.invoke('experience:invalidate', id),
  relateDevice: (experienceId: string, deviceId: string, relationType?: string) => ipcRenderer.invoke('experience:relateDevice', experienceId, deviceId, relationType),
  unrelateDevice: (experienceId: string, deviceId: string) => ipcRenderer.invoke('experience:unrelateDevice', experienceId, deviceId),
  listByDevice: (deviceId: string, includeInvalid?: boolean) => ipcRenderer.invoke('experience:listByDevice', deviceId, includeInvalid),
  listDevices: (experienceId: string) => ipcRenderer.invoke('experience:listDevices', experienceId),
},
```

**注意:** preload 方法签名入参用 `unknown`（input/fields/opts）而非强类型——与 kb 块模式一致（preload.ts line 110-123 用 `unknown`/`ArrayBuffer` 等宽松类型，强类型在 electron.d.ts ElectronAPI 声明，preload 实现层不重复标注，避免 preload/renderer 双份类型漂移）。channel 名与 experienceIpc.ts 的 ipcMain.handle 第一参逐字一致（camelCase action，遵循全仓事实约定）。
  </action>

  <verify>
    <automated>cd /e/knowlegdge_base/claude/network_toplogy && grep -c "setExperienceMasterKey(masterKey)" electron/main.ts; grep -c "registerExperienceIpc()" electron/main.ts; grep -c "import { setExperienceMasterKey }" electron/main.ts; grep -c "import { registerExperienceIpc }" electron/main.ts; grep -c "ipcRenderer.invoke('experience:" electron/preload.ts; npx tsc -p tsconfig.web.json --noEmit 2>&1 | head -10</automated>
  </verify>

  <acceptance_criteria>
    - `grep "import { setExperienceMasterKey } from './services/experienceService'" electron/main.ts` 命中 1 次
    - `grep "import { registerExperienceIpc } from './ipc/experienceIpc'" electron/main.ts` 命中 1 次
    - `grep -c "setExperienceMasterKey(masterKey)" electron/main.ts` = 1（masterKey 注入，紧跟 setKbMasterKey 之后）
    - `grep -c "registerExperienceIpc()" electron/main.ts` = 1（IPC 注册，紧跟 registerKbIpc 之后）
    - `grep -c "ipcRenderer.invoke('experience:" electron/preload.ts` = 10（10 channel 全暴露）
    - preload experience 块紧跟 kb 块（grep -A 20 "experience: {" electron/preload.ts 含 list/get/create/update/delete/invalidate/relateDevice/unrelateDevice/listByDevice/listDevices 10 方法）
    - `npx tsc -p tsconfig.web.json --noEmit` 全绿（main.ts/preload.ts/electron.d.ts 类型干净，noUnusedLocals 不报）
    - 全链路打通：main.ts 注入 MK → registerExperienceIpc 注册 10 channel（secure 包装）→ preload 暴露 window.api.experience.* → electron.d.ts 类型声明 → renderer 可类型安全调用
  </acceptance_criteria>

  <done>
    main.ts 启动注入 setExperienceMasterKey(masterKey)（与 6 个既有 service 共享 masterKey）+ 注册 registerExperienceIpc()（紧跟 registerKbIpc）；preload.ts 暴露 window.api.experience.* 10 方法白名单；renderer 经 contextBridge 仅能调白名单 channel；tsc strict 全绿。
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| Boundary | Description |
|----------|-------------|
| renderer → main（IPC 网关） | experience:* channel 是 renderer 访问经验的唯一入口；每个 handler 经 secure() 鉴权 + 异常脱敏；listDevices 在边界剥离设备密文列 |
| main → DB（service 内部） | experienceService 在 main 进程内解密 attrs_enc 后 delete 密文列，IPC 返回值不含 attrs 密文/明文凭证；listDevicesByExperience 返 devices 原始行含 name_enc，IPC 边界 stripEncColumns 删除 |

## STRIDE 威胁登记

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Spoofing | IPC 鉴权绕过 | mitigate | 10 个 experience:* channel 全部 secure(...) 包装（未登录 throw '未登录或会话已过期'）；无漏网 channel（grep 验证 secure 计数=10）；无 safe 包装的 experience channel（经验无登录前场景） |
| T-07-08 | Information Disclosure | IPC 返回泄露密文/凭证（attrs_enc + 设备 name_enc） | mitigate | **两条脱敏路径:** (1) service 层 getExperience/listExperiences 已 decField 回填 attrs 并 delete attrs_enc 列；(2) **experience:listDevices 返 devices 原始行含 name_enc 等密文列，IPC handler 返回前经 stripEncColumns 删除所有 `_enc` 后缀 key**，renderer 永不收设备密文（service 不解密设备名、IPC 边界统一剥离，不越域调 device 解密通道）；secure 包装的 sanitizeMessage 在 handler 抛错时移除路径/截断 message，不泄露 SQL/内部细节 |
| T-07-09 | Tampering | channel 命名不规范致误绑定 | mitigate | channel 命名遵循全仓 camelCase 事实约定（CONVENTIONS.md line 35），action 部分复合词用 camelCase（如 `experience:listDevices`/`experience:listByDevice`/`experience:relateDevice`/`experience:unrelateDevice`），与既有 channel（`kb:listDocuments`/`anomaly:acknowledgeAll`/`oui:addBatch`/`network:getIPDetails`/`arp:collectFromAll`/`export:arpTable`）一致；channel 全匹配 `'experience:[a-zA-Z-]+'`（字符类含 A-Z 以匹配 camelCase action，词界隔离防 list 被 listDevices 子串重复命中）；preload ipcRenderer.invoke 第一参与 ipcMain.handle 第一参逐字一致（grep 双向校验） |
| T-07-10 | Repudiation | 特权操作无溯源 | accept | 本 phase 不做操作审计日志（Phase 10 浏览页手动 CRUD 的审计需求由后续 phase 评估；ai_system_logs 既有 type 枚举不含 experience，本 phase 不扩）；source_session_id 列已为 AI 起草溯源预埋 |
| T-07-11 | Denial of Service | 批量越权拉全表 | mitigate | list channel 透传 opts.limit 至 service，service 层 MAX_BATCH=1000 强制（throw）；IPC 层不绕过、不重复校验、不 import MAX_BATCH（避免双层逻辑漂移与未使用 import） |
| T-07-12 | Elevation of Privilege | preload 暴露超白名单方法 | mitigate | preload.ts 仅暴露 10 个白名单方法（grep 计数=10），contextBridge.exposeInMainWorld 单一暴露点；renderer 无 nodeIntegration、contextIsolation:true、sandbox:true（架构既有红线） |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 全绿（main/preload/experienceIpc/experience.ts/electron.d.ts 类型干净，noUnusedLocals 通过——IPC 不 import 未使用的 MAX_BATCH）
- grep 验证：10 channel 全 secure 包装、channel 命名规范（`'experience:[a-zA-Z-]+'` 精确 10，action 遵循全仓 camelCase 事实约定）、preload 暴露 10 方法、main.ts 注入 + 注册到位、stripEncColumns 边界脱敏落地
- 全链路打通证明：main.ts setExperienceMasterKey → registerExperienceIpc → secure(handler) → experienceService 函数 → DB；renderer window.api.experience.* → ipcRenderer.invoke → ipcMain.handle；listDevices → stripEncColumns 剥密文
- 安全基线：未登录调任意 experience channel → throw '未登录或会话已过期'（secure 包装保证）；experience:listDevices 返回值无 `_enc` 后缀列
</verification>

<success_criteria>
1. experienceIpc.ts 注册 10 channel 全 secure 包装（函数式 service 具名 import，无 class），channel 命名 experience:<action> 规范统一（精确 10 去重，action 遵循全仓 camelCase 事实约定）
2. experience:listDevices 经 stripEncColumns 边界剥离 `_enc` 密文列，renderer 永不收设备密文（SEC-02 落地）
3. main.ts 启动注入 setExperienceMasterKey(masterKey) + 注册 registerExperienceIpc()（与既有 6 service 同位置同模式）
4. preload.ts 暴露 window.api.experience.* 10 方法白名单，renderer 仅能调白名单
5. experience.ts DTO + electron.d.ts ElectronAPI.experience 类型声明落地，renderer 类型安全调用
6. SEC-01（IPC 鉴权 + 异常脱敏）+ SEC-02（脱敏规范，密文/凭证/设备密文不外泄）IPC 层落地
7. tsc strict 全绿，无 any 绕过，noUnusedLocals 通过（IPC 无未使用 import）
</success_criteria>

<output>
完成后创建 `.planning/phases/07-experience-data-layer-security-baseline/07-02-SUMMARY.md`，记录：
- 10 channel 清单与 secure 包装确认
- experience:listDevices 边界剥离 `_enc` 列决策（SEC-02 设备密文不外泄）
- main.ts 注入点 + preload 暴露面 + electron.d.ts 类型链路
- SEC-01/SEC-02 落地证据（grep 计数 + tsc 绿）
- 供 Phase 8-11 复用的 IPC 契约（channel 名 + DTO 类型路径 + 函数式 service 调用面）
</output>
