---
phase: 13
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/experienceService.ts
  - electron/ipc/experienceIpc.ts
  - tests/unit/experienceListGuard.test.ts
autonomous: true
requirements: [SEC-05]
tags: [security, ipc-validation, dos-prevention, experience, layered-defense]

must_haves:
  truths:
    - "untrusted renderer 调 experience:list 传超长 search（>100 字符）被 IPC 网关层截断，不传超长串给 service 层 LIKE 全表扫描"
    - "untrusted renderer 调 experience:list 传超量 tags（>20 个）被 IPC 网关层截取，单 tag 超 30 字符被截断"
    - "untrusted renderer 调 experience:list 传非法 severity（非 critical/high/medium/low/info）被 IPC 网关层 throw 拒绝（固定集合非法值暴露调用方 bug）"
    - "service 层 listExperiences 现有 limit MAX_BATCH 兜底保留不删（双层防御第二层，防绕 IPC 直调 service 查全表）"
    - "VALID_SEVERITIES 枚举单一来源（service 层导出，IPC 层复用），无 IPC/service 两份枚举 drift"
    - "三红线（IPC secure/safe / 字段加密 _enc / commandSafety.isCommandAllowed）改动后仍生效——experience:list 仍 secure 包装，校验加在 secure 内 throw 经 sanitizeMessage 脱敏"
  artifacts:
    - path: "electron/ipc/experienceIpc.ts"
      provides: "experience:list handler 加 severity 枚举 throw + search 长度截断 + tags 数量/单 tag 长度截断"
      contains: "VALID_SEVERITIES"
    - path: "electron/services/experienceService.ts"
      provides: "export VALID_SEVERITIES 供 IPC 层复用（消 drift）+ listExperiences limit MAX_BATCH 兜底保留不变"
      contains: "export const VALID_SEVERITIES"
    - path: "tests/unit/experienceListGuard.test.ts"
      provides: "SEC-05 mock 单测——超长 search 截断 / 超量 tags 截取 / 非法 severity throw"
      min_lines: 60
  key_links:
    - from: "electron/ipc/experienceIpc.ts"
      to: "electron/services/experienceService.ts VALID_SEVERITIES"
      via: "import { VALID_SEVERITIES } 复用枚举（非第二份手写）"
      pattern: "VALID_SEVERITIES"
    - from: "tests/unit/experienceListGuard.test.ts"
      to: "electron/ipc/experienceIpc.ts experience:list handler"
      via: "_setExperienceDbGetter mock DB 注入 + 直接调 handler 验截断/throw"
      pattern: "experience:list|VALID_SEVERITIES"
---

<objective>
SEC-05：experience:list IPC 网关层加入参校验防廉价 DoS。untrusted renderer 传超长 search / 超量 tags / 非法 severity 不再触发全表 LIKE 扫描，被 IPC 网关层截断/拒绝（体检 WR-06）。

Purpose: listExperiences 的 search LIKE 多词拆分 OR-join（experienceService.ts:284-299），untrusted renderer 传超长 search（如粘整段故障描述 10000 字）或超量 tags 会生成海量 LIKE 子句廉价触发全表 DoS。D-13-5 锁定混合策略：超长 search/超量 tags 钳制截断（用户输入静默容错），非法 severity throw（固定集合非法值暴露 bug）。D-13-7 双层防御职责分明：IPC 网关层完整校验 + service 层仅 limit MAX_BATCH 兜底。

Output: experienceIpc.ts experience:list handler 加校验（钳制 + throw）；experienceService.ts export VALID_SEVERITIES 供复用（消 drift）；mock 单测覆盖截断/throw。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-security-hardening-cluster/13-CONTEXT.md
@.planning/phases/13-security-hardening-cluster/13-PATTERNS.md
@.planning/audits/2026-08-07-health-audit.md

<interfaces>
<!-- SEC-05 改造锚点 + 钳制/throw 先例范式 -->

From electron/services/experienceService.ts（service 层，仅 export VALID_SEVERITIES + 保留 limit 兜底）：
```typescript
const VALID_SEVERITIES = ['critical', 'high', 'medium', 'low', 'info']  // line 51，需改 export
export function listExperiences(opts: ListExperiencesOpts): PaginatedResult<any> {
  let limit = opts.limit
  if (limit == null || limit <= 0) limit = 100
  if (limit > MAX_BATCH) throw new Error('limit 超过 MAX_BATCH 上限')  // 保留不删（D-13-7 双层第二层）
  // severity/search/tags 不在此复查（D-13-7 接受残余风险换简洁）
}
```

From electron/ipc/experienceIpc.ts（IPC 网关层，SEC-05 校验加入点）：
```typescript
import { secure } from '../utils/authGuard'
ipcMain.handle('experience:list', secure((_e, opts?: ExperienceListInput) =>
  listExperiences(opts || {})))  // ← 现状透传，SEC-05 加校验
```

From electron/utils/pagination.ts:19-23（钳制先例 validateLimit——落回默认非 throw）：
```typescript
export function validateLimit(limit, defaultValue, maxCeiling): number {
  const n = Number(limit)
  if (!Number.isInteger(n) || n < 1 || n > maxCeiling) return defaultValue  // 落回默认
  return n
}
```

From electron/ipc/ouiIpc.ts:9-11（消费 validateLimit 钳制风格范式）：
```typescript
ipcMain.handle('oui:getAll', secure((_e, limit?, offset?) =>
  OUIService.getAll(validateLimit(limit, 5000, 50000), validateOffset(offset))))
```

From electron/ipc/experienceIpc.ts:107-112（throw 先例 confirmDrafts——固定集合/上限非法 throw）：
```typescript
ipcMain.handle('experience:confirmDrafts', secure((_e, input: ConfirmDraftsInput) => {
  if (!input || !Array.isArray(input.drafts) || input.drafts.length > MAX_BATCH) {
    throw new Error(`批量上限 ${MAX_BATCH} 条（或入参无效）`)
  }
  return confirmDrafts(input)
}))
```

From electron/services/experienceService.test.ts（_setExperienceDbGetter mock DB 注入范式，SEC-05 测试复用）：
```typescript
export function _setExperienceDbGetter(fn: () => Database.Database): void { dbGetter = fn }
// 测试侧: import { _setExperienceDbGetter } from '../services/experienceService'
// beforeEach: _setExperienceDbGetter(() => mockDb)  ← 注入内存 mock
```

From src/types/experience.ts（severity 合法枚举类型来源，D-13-5 判据）：
```typescript
export interface ExperienceAttrs { severity?: 'critical' | 'high' | 'medium' | 'low' | 'info' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: experienceService.ts export VALID_SEVERITIES（消 drift 单一来源）+ IPC 网关层加 severity/search/tags 校验</name>
  <files>electron/services/experienceService.ts, electron/ipc/experienceIpc.ts</files>

  <behavior>
  - 超长 search（>100 字符）经 experience:list handler 后，传给 listExperiences 的 opts.search 被截断到 ≤100 字符（mock DB 收到截断后 search）
  - 超量 tags（>20 个）经 handler 后，opts.tags 被截取到前 20 个；单 tag >30 字符被截断到 30 字符
  - 非法 severity（如 'BOGUS'/'xxx'）经 handler 后 throw，reject message 含 severity 相关字样
  - 合法 severity（'critical'）经 handler 后正常透传，不 throw
  - search/tags 为空或正常长度时正常透传（钳制不误伤日常搜索）
  </behavior>

  <read_first>
    - electron/ipc/experienceIpc.ts（experience:list handler:64-65 现状透传 + confirmDrafts:107-112 throw 先例 + 全文件 secure 包装基线）
    - electron/services/experienceService.ts（VALID_SEVERITIES:51 现状 const 未 export + listExperiences:258-264 limit MAX_BATCH 兜底保留 + search:284-299 LIKE 多词拆分 OR-join DoS 面 + tags:311-318 LIKE OR-join）
    - electron/utils/pagination.ts:19-23（validateLimit 钳制先例——落回默认非 throw）
    - electron/ipc/ouiIpc.ts:9-11（消费 validateLimit 钳制风格范式）
    - electron/ipc/anomalyIpc.ts:23-27（ids 数组上限 throw 先例）
    - electron/utils/authGuard.ts:31-41（secure 包装 + sanitizeMessage——SEC-05 throw 经此脱敏透出 renderer）
    - src/types/experience.ts（ExperienceListInput:47-63 search/severity/tags 字段定义 + ExperienceAttrs.severity 枚举）
    - .planning/phases/13-security-hardening-cluster/13-PATTERNS.md（§experienceIpc.ts experience:list handler Pattern A/B + §experienceService.ts listExperiences 职责分层）
    - .planning/phases/13-security-hardening-cluster/13-CONTEXT.md（D-13-5 混合策略 + D-13-6 阈值 + D-13-7 双层防御职责）
  </read_first>

  <action>
  按 D-13-5/D-13-6/D-13-7 落 SEC-05：

  **Step 1: experienceService.ts export VALID_SEVERITIES（消 drift 单一来源）**
  将 `const VALID_SEVERITIES = ['critical', 'high', 'medium', 'low', 'info']`（line 51）改为 `export const VALID_SEVERITIES = ['critical', 'high', 'medium', 'low', 'info'] as const`（加 export + as const 让 TS 推断字面量联合类型）。service 层既有 assertTroubleshootingAttrs（line 60-62）/ createExperience 校验（line 174-176）已用此常量，改 export 不影响内部用法。**不改 listExperiences 函数签名/不复查 severity/search/tags**（D-13-7：service 层仅 limit MAX_BATCH 兜底，接受残余风险换简洁）。

  **Step 2: experienceIpc.ts experience:list handler 加校验（IPC 网关层，双层第一层）**
  改 `experience:list` handler（line 64-65），在 `listExperiences(opts || {})` 前加校验逻辑。校验加在 `secure(...)` 内（throw 经 sanitizeMessage 脱敏透出 renderer，无需额外 try/catch）。

  校验逻辑（参照钳制 Pattern A + throw Pattern B）：
  - **search 钳制**（D-13-6：≤100 字符）：若 `opts.search` 是 string 且 `opts.search.length > 100`，截断 `opts.search = opts.search.slice(0, 100)`（静默容错，用户输入场景，参照 validateLimit 落回默认/截断风格非 throw）。
  - **tags 钳制**（D-13-6：≤20 个 + 单 tag ≤30 字符）：若 `opts.tags` 是数组——若 `opts.tags.length > 20`，截取 `opts.tags = opts.tags.slice(0, 20)`；遍历 tags，单 tag 是 string 且 `tag.length > 30`，截断 `tag = tag.slice(0, 30)`（map 生成新数组）。静默容错。
  - **severity throw**（D-13-5：固定集合非法值 throw 暴露 bug）：若 `opts.severity` 非 undefined 且非空字符串且 `!VALID_SEVERITIES.includes(opts.severity as any)`，`throw new Error('severity 非法，合法值: critical/high/medium/low/info')`（参照 confirmDrafts throw 风格，固定集合非法值说明调用方 bug 该暴露）。**复用 service 层导出的 VALID_SEVERITIES**（import from '../services/experienceService'，非第二份手写避 drift）。
  - **limit 兜底**：不复查（service 层 listExperiences 已有 limit MAX_BATCH throw，D-13-7 双层第二层）。

  import 段加 `VALID_SEVERITIES`（从 experienceService import，与既有 MAX_BATCH import 同行或邻近）。

  **红线约束**：
  - handler 仍包在 `secure(...)` 内（红线①不变，校验加在 secure 内 throw 经 sanitizeMessage 脱敏）。
  - service 层 listExperiences limit MAX_BATCH 兜底**保留不删**（D-13-7，防绕 IPC 直调 service）。
  - listExperiences 函数签名 `ListExperiencesOpts` **不改**（D-13-7，避免破坏既有 callers）。
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const s=fs.readFileSync('electron/services/experienceService.ts','utf8');if(!/export const VALID_SEVERITIES/.test(s)){console.error('FAIL: VALID_SEVERITIES 未 export');process.exit(1)}const i=fs.readFileSync('electron/ipc/experienceIpc.ts','utf8');if(!/VALID_SEVERITIES/.test(i)){console.error('FAIL: experienceIpc 未用 VALID_SEVERITIES');process.exit(1)}if(!/slice\(0,\s*100\)/.test(i)){console.error('FAIL: 缺 search 截断到 100');process.exit(1)}if(!/slice\(0,\s*20\)/.test(i)){console.error('FAIL: 缺 tags 截取到 20');process.exit(1)}if(!/severity 非法/.test(i)){console.error('FAIL: 缺 severity throw');process.exit(1)}console.log('OK')" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; npm run build:electron-main</automated>
  </verify>

  <acceptance_criteria>
  - experienceService.ts 含 `export const VALID_SEVERITIES`（grep 命中 1，as const 字面量联合类型）
  - experienceService.ts listExperiences 保留 `limit > MAX_BATCH` throw（grep `limit 超过 MAX_BATCH` 命中 1，D-13-7 双层第二层不删）
  - experienceService.ts listExperiences 函数签名 ListExperiencesOpts 不变（grep `export function listExperiences(opts: ListExperiencesOpts)` 命中 1）
  - experienceIpc.ts experience:list handler 含 search 截断 `slice(0, 100)`（grep 命中 1）
  - experienceIpc.ts experience:list handler 含 tags 截取 `slice(0, 20)`（grep 命中 1）+ 单 tag 截断 `slice(0, 30)`（grep 命中 1）
  - experienceIpc.ts experience:list handler 含 severity throw（grep `severity 非法` 命中 1，throw 在 secure 内）
  - experienceIpc.ts import 含 `VALID_SEVERITIES` from '../services/experienceService'（非第二份手写枚举，消 drift）
  - experienceIpc.ts experience:list handler 仍包在 `secure(...)` 内（grep `secure((_e, opts?: ExperienceListInput)` 命中 1，红线①不变）
  - `npx tsc -p tsconfig.web.json --noEmit` 退出 0（strict + noUnusedLocals）
  - `npm run build:electron-main` 退出 0
  </acceptance_criteria>

  <done>experience:list IPC 网关层加 severity throw + search/tags 钳制（D-13-5 混合策略 + D-13-6 阈值 100/20/30）；VALID_SEVERITIES 单一来源 export 消 drift；service 层 limit MAX_BATCH 兜底保留（D-13-7 双层防御）；tsc + build:electron-main 双绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mock 单测验证 SEC-05 截断/throw（_setExperienceDbGetter 范式）</name>
  <files>tests/unit/experienceListGuard.test.ts</files>

  <behavior>
  - 调 experience:list handler 传 search 超长（200 字符）→ mock DB 收到的 opts.search 长度 ≤100（截断生效）
  - 调 handler 传 tags 超 20 个（30 个）→ mock DB 收到的 opts.tags 长度 ≤20（截取生效）
  - 调 handler 传 tags 含单 tag 超 30 字符（50 字符）→ mock DB 收到的该 tag 长度 ≤30（截断生效）
  - 调 handler 传 severity='BOGUS' → reject throw，message 含 'severity 非法'
  - 调 handler 传 severity='critical'（合法）→ 不 throw，mock DB 收到 severity='critical'
  - 调 handler 传 search 正常长度（15 字符「华为交换机 DHCP」）→ mock DB 收到原样 search（钳制不误伤）
  </behavior>

  <read_first>
    - electron/ipc/experienceIpc.ts（Task 1 改后 experience:list handler——校验逻辑落点位，测试需直接调 handler 验截断/throw）
    - electron/services/experienceService.ts（_setExperienceDbGetter mock 注入钩子 + VALID_SEVERITIES + listExperiences opts 形态）
    - electron/services/experienceService.test.ts:36-59（_setExperienceDbGetter 接入范式——测试侧 import + beforeEach 注入 mock DB）
    - electron/services/__tests__/experienceService.browse.test.ts（listExperiences mock 测试范例，search/severity/tags 入参断言风格）
    - tests/unit/authGuard.test.ts（secure 包装间接测试范式——SEC-05 handler 包在 secure 内，测试需 setAuthenticated(true) 让 secure 放行到校验逻辑）
    - electron/utils/authGuard.ts（setAuthenticated——测试需先 setAuthenticated(true) 否则 secure 未登录拒绝挡在校验前）
    - src/types/experience.ts（ExperienceListInput 字段类型，测试构造入参用）
  </read_first>

  <action>
  按 D-13-8 写 SEC-05 mock 单测（沿用 _setExperienceDbGetter 范式）。

  **测试接入策略**：experience:list handler 包在 `secure(...)` 内，secure 是高阶函数返回 `async (e, ...args) => {...}`。测试需：
  1. `setAuthenticated(true)`（否则 secure 未登录 reject 挡在校验前，测不到 SEC-05 逻辑）
  2. 直接 import registerExperienceIpc 会真注册 ipcMain.handle（vitest 环境无 electron ipcMain）——故**不调 registerExperienceIpc**，而是**把校验逻辑抽到可测单元**或在测试内联 replicate handler 包装逻辑调 listExperiences spy 验证入参。

  **推荐方案（抽校验为纯函数，最高可测性）**：在 experienceIpc.ts 内抽 `export function sanitizeListInput(opts: ExperienceListInput | undefined): ExperienceListInput`（纯函数，做 search/tags 截断 + severity throw，不调 listExperiences）。handler 改为 `secure((_e, opts?) => listExperiences(sanitizeListInput(opts)))`。测试直接调 `sanitizeListInput(...)` 验截断/throw（无需 setAuthenticated/secure 包装/ipcMain mock，纯函数最高 ROI）。

  若 Task 1 已用此抽函数方案，Task 2 直接测 sanitizeListInput；若 Task 1 内联在校验 handler 内，Task 2 需重构抽函数（保持 Task 1 acceptance criteria 仍满足，sanitizeListInput 的存在不破坏 grep 断言）。

  新增 `tests/unit/experienceListGuard.test.ts`，6 个 it（对应 behavior 6 条）：
  1. `sanitizeListInput({ search: 'x'.repeat(200) }).search.length <= 100`（截断超长 search）
  2. `sanitizeListInput({ tags: Array(30).fill('tag') }).tags.length === 20`（截取超量 tags 到 20）
  3. `sanitizeListInput({ tags: ['x'.repeat(50)] }).tags[0].length <= 30`（截断超长单 tag 到 30）
  4. `expect(() => sanitizeListInput({ severity: 'BOGUS' })).toThrow('severity 非法')`（非法 severity throw）
  5. `sanitizeListInput({ severity: 'critical' }).severity === 'critical'`（合法 severity 透传不 throw）
  6. `sanitizeListInput({ search: '华为交换机 DHCP' }).search === '华为交换机 DHCP'`（正常长度 search 不误伤）

  **额外 2 it 补双层防御 + 枚举一致性**：
  7. `listExperiences({ limit: MAX_BATCH + 1 })` reject throw 'limit 超过 MAX_BATCH 上限'（确认 service 层兜底仍在，D-13-7 双层第二层——沿用 experienceService.test.ts 既有范式，本文件 import MAX_BATCH + listExperiences + _setExperienceDbGetter 注入 mock DB）
  8. `VALID_SEVERITIES` deepEqual `['critical','high','medium','low','info']`（枚举一致性，防误改）

  it 7 用 `_setExperienceDbGetter` 注入内存 mock DB（参照 experienceService.test.ts:41-59），无需真 better-sqlite3（规避 DEP-1，plain node mock 套件）。
  </action>

  <verify>
  <automated>npx vitest run tests/unit/experienceListGuard.test.ts</automated>
  </verify>

  <acceptance_criteria>
  - tests/unit/experienceListGuard.test.ts 存在，含至少 8 it（6 SEC-05 截断/throw + service 层兜底 + 枚举一致性）
  - 若 Task 1 抽 sanitizeListInput 纯函数：测试直接调 sanitizeListInput 验 6 case（截断 search/tags/单 tag + throw 非法 severity + 透传合法 severity + 不误伤正常 search）
  - it '超长 search 截断' 断言 sanitizeListInput({search: 200 字符}).search.length <= 100
  - it '超量 tags 截取' 断言 sanitizeListInput({tags: 30 个}).tags.length === 20
  - it '超长单 tag 截断' 断言 sanitizeListInput({tags: [50 字符]}).tags[0].length <= 30
  - it '非法 severity throw' 断言 sanitizeListInput({severity:'BOGUS'}) throw 含 'severity 非法'
  - it '合法 severity 透传' 断言 sanitizeListInput({severity:'critical'}).severity === 'critical'
  - it 'service 层 limit MAX_BATCH 兜底' 用 _setExperienceDbGetter 注入 mock DB，断言 listExperiences({limit: MAX_BATCH+1}) throw 'limit 超过 MAX_BATCH 上限'（D-13-7 双层第二层确认）
  - it 'VALID_SEVERITIES 枚举一致' 断言 deepEqual ['critical','high','medium','low','info']
  - `npx vitest run tests/unit/experienceListGuard.test.ts` 退出 0，8 it 全绿
  - `npx tsc -p tsconfig.web.json --noEmit` 退出 0
  - `npm test` 退出 0 零回归（plain mock 套件，新增 8 it 计入）
  </acceptance_criteria>

  <done>SEC-05 有 mock 单测回归网——超长 search/超量 tags/超长单 tag 截断 + 非法 severity throw + 合法入参透传 + service 层兜底确认 + 枚举一致性，8 it 全绿，三绿门禁全绿零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted renderer → IPC secure 网关 → experienceIpc experience:list → service listExperiences → DB LIKE | SEC-05 校验加在 IPC 网关层（边界第一层），防 renderer 廉价触发全表 LIKE DoS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Denial of Service | experience:list 超长 search 触发海量 LIKE OR-join 全表扫 | mitigate | D-13-5/D-13-6 IPC 网关层 search 截断到 ≤100 字符（钳制静默容错），阻断超长 search 传到 service 层 LIKE 多词拆分 OR-join（experienceService.ts:284-299） |
| T-13-03-02 | Denial of Service | experience:list 超量 tags 触发海量 LIKE OR-join | mitigate | D-13-6 IPC 网关层 tags 截取到 ≤20 个 + 单 tag 截断到 ≤30 字符，阻断超量 tags 传到 service 层 tags LIKE OR-join（experienceService.ts:311-318） |
| T-13-03-03 | Tampering | experience:list 非法 severity 注入 | mitigate | D-13-5 IPC 网关层 severity 枚举校验 throw（固定集合 critical/high/medium/low/info，复用 service 层 VALID_SEVERITIES 单一来源消 drift），非法值 throw 暴露调用方 bug |
| T-13-03-04 | Denial of Service | 绕 IPC 直调 service listExperiences 查全表 | mitigate | D-13-7 双层防御：service 层 listExperiences limit MAX_BATCH 兜底保留（experienceService.ts:262-264 throw），防绕 IPC 网关层校验直调 service 残余风险 |
| T-13-03-05 | Tampering | 三红线（IPC secure/safe / 字段加密 _enc / commandSafety） | accept | SEC-05 校验加在 experience:list secure 包装内（throw 经 sanitizeMessage 脱敏），不改 secure/safe 鉴权；不动字段加密（experience:list 不碰 _enc 列）；不动 commandSafety（list 非命令执行路径）。三红线改动后仍生效 |
| T-13-03-06 | Tampering | VALID_SEVERITIES IPC/service 两份枚举 drift | mitigate | service 层 export VALID_SEVERITIES（单一来源），IPC 层 import 复用非第二份手写，消 drift（D-13-5 + PATTERNS 范式） |
</threat_model>

<verification>
- `npx vitest run tests/unit/experienceListGuard.test.ts` 退出 0，8 it 全绿
- `npx tsc -p tsconfig.web.json --noEmit` 退出 0（strict + noUnusedLocals）
- `npm run build:electron-main` 退出 0
- `npm test` 退出 0 零回归（plain mock 套件 + 新增 8 it）
- experience:list handler 仍 secure 包装（红线①不变）
- service 层 listExperiences limit MAX_BATCH 兜底保留（D-13-7 双层防御第二层不删）
</verification>

<success_criteria>
- SC3（experience:list severity/search/tags 校验防廉价 DoS）：IPC 网关层 search≤100/tags≤20/单tag≤30 截断 + 非法 severity throw，单测 8 it 验证
- SC4（三红线不回退 + 双层防御）：threat_model T-13-03-04/T-13-03-05 论证 service 兜底保留 + secure 包装不变
- D-13-5（混合策略：钳制 search/tags + throw severity）：handler 校验逻辑落地
- D-13-6（阈值 100/20/30）：截断阈值落地
- D-13-7（双层防御职责分明）：IPC 完整校验 + service 仅 limit 兜底，listExperiences 签名不变
- D-13-8（mock 单测）：experienceListGuard.test.ts 8 it 沿用 _setExperienceDbGetter 范式
</success_criteria>

<output>
Create `.planning/phases/13-security-hardening-cluster/13-03-SUMMARY.md` when done
</output>
