---
phase: 13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/connection.ts
  - tests/electron/connectSSH.algorithms.real.test.ts
autonomous: true
requirements: [SEC-03]
tags: [security, ssh, algorithms, electron-test]

must_haves:
  truths:
    - "connection.ts connectSSH 的 SSH 握手算法与 sshConfig.ts SSH_ALGORITHMS 常量逐字一致（零 drift）"
    - "仅支持 curve25519-sha256 kex 的现代 Linux 对端，client.connect 用 SSH_ALGORITHMS 能协商成功（触发 'ready' 事件）"
    - "connectSSH readyTimeout 对齐 SSH_READY_TIMEOUT_MS(30s)，慢设备 10-30s 区间不再触发 readyTimeout"
    - "全仓 3 处 SSH 路径（ai.ts buildSSHConfig / arpCollector.ts executeSSH / connection.ts connectSSH）走同一 algorithms 常量，无第二份算法表"
    - "三红线（IPC secure/safe / 字段加密 _enc / commandSafety.isCommandAllowed）改动后仍生效——SEC-03 仅改 SSH 算法常量复用，不碰鉴权/加密/命令安全层"
  artifacts:
    - path: "electron/services/connection.ts"
      provides: "connectSSH 复用 SSH_ALGORITHMS + SSH_READY_TIMEOUT_MS，删除内联 algorithms 表"
      contains: "algorithms: SSH_ALGORITHMS"
    - path: "tests/electron/connectSSH.algorithms.real.test.ts"
      provides: "SSH_ALGORITHMS 与 curve25519-only 真路径对端协商回归"
      min_lines: 40
  key_links:
    - from: "electron/services/connection.ts"
      to: "electron/utils/sshConfig.ts"
      via: "import { SSH_ALGORITHMS, SSH_READY_TIMEOUT_MS }"
      pattern: "from '\\.\\./utils/sshConfig'"
    - from: "tests/electron/connectSSH.algorithms.real.test.ts"
      to: "electron/utils/sshConfig.ts SSH_ALGORITHMS"
      via: "真路径 ssh2.Client.connect 验证协商"
      pattern: "SSH_ALGORITHMS"
---

<objective>
SEC-03：消除 connection.ts connectSSH 内联 SSH 算法表与 sshConfig.ts SSH_ALGORITHMS 的 drift，让设备终端连接补齐 curve25519-sha256 等现代算法，连通仅支持 curve25519 的现代 Linux。

Purpose: 体检 §1.0 半残留闭环——ai.ts/arpCollector.ts 已走 SSH_ALGORITHMS（含 curve25519），但 connection.ts:115-150 内联表 kex 首项是 ecdh-sha2-nistp256（缺 curve25519），serverHostKey 顺序异（ssh-rsa 在前），致终端连接现代 Linux 失败。D-13-2 锁定：复用常量消 drift（这是上次终端缺 curve25519 的根因）。

Output: connection.ts 改 2 行（删内联 algorithms 表 + readyTimeout 10s→SSH_READY_TIMEOUT_MS）；新增真路径单测验证 SSH_ALGORITHMS 与 curve25519-only 对端协商成功。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-security-hardening-cluster/13-CONTEXT.md
@.planning/phases/13-security-hardening-cluster/13-PATTERNS.md

<interfaces>
<!-- 改造目标形态（analog：electron/services/ai.ts:306-322 buildSSHConfig，已是同改法成品） -->

From electron/utils/sshConfig.ts（只读参照，不改）：
```typescript
export const SSH_READY_TIMEOUT_MS = 30000
export const SSH_ALGORITHMS: ConnectConfig['algorithms'] = {
  kex: ['curve25519-sha256', 'ecdh-sha2-nistp256', ...]  // 首项 curve25519
  cipher: ['aes128-gcm@openssh.com', ..., 'aes128-gcm', 'aes256-gcm', ...]
  serverHostKey: ['ssh-ed25519', 'ecdsa-sha2-nistp256', ..., 'ssh-rsa', 'ssh-dss']
}
```

From electron/services/ai.ts:306-322（analog 全文，connectSSH 改造的目标形态）：
```typescript
import { SSH_READY_TIMEOUT_MS, SSH_ALGORITHMS } from '../utils/sshConfig'
function buildSSHConfig(device: any): ConnectConfig {
  const cfg: ConnectConfig = {
    host: device.ipAddress, port: device.port || 22, username: device.username || 'root',
    readyTimeout: SSH_READY_TIMEOUT_MS,  algorithms: SSH_ALGORITHMS,
  }
  // 密钥/密码分支（connectSSH:154-160 同形，保留不动）
}
```

From tests/electron/_helpers/mockSshServer.ts（Phase 12 真路径套件，SEC-03 测试复用）：
```typescript
export function startMockSshServer(onExec: (cmd: string) => string): Promise<{ port, close }>
// ssh2.Server 起内存级 SSH server，listen(0,'127.0.0.1') loopback，接受任意凭证
// SEC-03 测试改造点：Server({ hostKeys, algorithms: { kex: ['curve25519-sha256'] } }) 限制对端仅 curve25519
```

Phase 12 test:electron 通道（STATE §Accumulated Context 12-01）：
```
cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs run --config vitest.electron.config.ts
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: connectSSH 删内联 algorithms 表复用 SSH_ALGORITHMS + readyTimeout 对齐 SSH_READY_TIMEOUT_MS</name>
  <files>electron/services/connection.ts</files>

  <read_first>
    - electron/services/connection.ts（connectSSH:107-205 当前实现，重点 110-151 内联 algorithms 表 + 114 readyTimeout:10000 + 154-160 密钥/密码分支保留不动）
    - electron/utils/sshConfig.ts（SSH_ALGORITHMS:11 + SSH_READY_TIMEOUT_MS:7，复用源，只读不改）
    - electron/services/ai.ts:1-15 顶部 import 段 + 306-322 buildSSHConfig（analog，照抄 import + config 形态）
    - electron/services/arpCollector.ts:68（第三处 algorithms: SSH_ALGORITHMS 单行参照，全仓复用现状）
    - .planning/phases/13-security-hardening-cluster/13-PATTERNS.md（§connection.ts connectSSH Pattern 全文）
  </read_first>

  <action>
  按 D-13-2 + D-13-3 改 connection.ts connectSSH，照抄 ai.ts:306-322 buildSSHConfig 形态：

  1. **顶部 import 段**加 `import { SSH_ALGORITHMS, SSH_READY_TIMEOUT_MS } from '../utils/sshConfig'`（对齐 ai.ts:8 import 行；若 connection.ts 已有该 import 则跳过此步，但需确认两个常量都被引入）。

  2. **删内联 algorithms 表**（connectSSH:115-150 的 `algorithms: { kex: [...], cipher: [...], serverHostKey: [...] }` 整块），替换为单行 `algorithms: SSH_ALGORITHMS,`。这是 drift 根源——内联表 kex 首项是 ecdh-sha2-nistp256 缺 curve25519，serverHostKey ssh-rsa 在前异于 SSH_ALGORITHMS（ssh-ed25519 在前）。

  3. **readyTimeout 对齐**：`readyTimeout: 10000,`（connectSSH:114）改为 `readyTimeout: SSH_READY_TIMEOUT_MS,`（D-13-3，治同类慢设备超时 drift，只会让慢设备更易连上，风险极低）。

  4. **密钥/密码分支（154-160）保留不动**——`if (device.sshKeyContent) {...} else if (device.sshKeyPath) {...} else { config.password = device.password }` 与 analog ai.ts buildSSHConfig 同形，不改。

  5. **L1 删弱算法显式 defer**：plan 里勿删任何弱算法（group1-sha1/group14-sha1/3des-cbc/blowfish-cbc/ssh-dss 等全保留在 SSH_ALGORITHMS 内）。理由 D-13-1 锁定：运维兼容性优先，删了有连不上老设备风险。SSH_ALGORITHMS 常量已含全部兼容算法（宁可列宽不可漏），connectSSH 复用即同时拿到 curve25519（补）+ 全部老算法（保留）。

  改完后全仓 3 处 SSH 路径（ai.ts buildSSHConfig / arpCollector.ts executeSSH / connection.ts connectSSH）零 drift，全部 `algorithms: SSH_ALGORITHMS` 单行复用。
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const s=fs.readFileSync('electron/services/connection.ts','utf8');const m1=s.match(/algorithms:\s*\{[\s\S]*?kex:/);if(m1){console.error('FAIL: 内联 algorithms 表未删，drift 根源仍在');process.exit(1)}if(!/algorithms:\s+SSH_ALGORITHMS,/.test(s)){console.error('FAIL: 缺 algorithms: SSH_ALGORITHMS');process.exit(1)}if(!/readyTimeout:\s+SSH_READY_TIMEOUT_MS,/.test(s)){console.error('FAIL: 缺 readyTimeout: SSH_READY_TIMEOUT_MS');process.exit(1)}if(!/from\s+'\\.\\./utils\\/sshConfig'/.test(s)){console.error('FAIL: 缺 sshConfig import');process.exit(1)}console.log('OK')" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; npm run build:electron-main</automated>
  </verify>

  <acceptance_criteria>
  - connection.ts 含 `algorithms: SSH_ALGORITHMS,` 单行（grep 命中 1 处）
  - connection.ts 含 `readyTimeout: SSH_READY_TIMEOUT_MS,`（grep 命中 1 处）
  - connection.ts 不再含内联表 `algorithms: {` 后跟 `kex:`（grep `algorithms:\s*\{[\s\S]*?kex:` 命中 0）
  - connection.ts 不再含字面量 `readyTimeout: 10000`（grep 命中 0）
  - connection.ts 顶部 import 含 `from '../utils/sshConfig'` 且引入 SSH_ALGORITHMS + SSH_READY_TIMEOUT_MS
  - connection.ts connectSSH 密钥/密码分支（sshKeyContent/sshKeyPath/password）保留不动（grep `device.sshKeyContent`/`device.sshKeyPath`/`device.password` 各命中 1）
  - 全仓 `algorithms:` 内联表扫描：connection.ts/ai.ts/arpCollector.ts 三文件均无内联 algorithms 对象字面量，全走 SSH_ALGORITHMS 常量
  - `npx tsc -p tsconfig.web.json --noEmit` 退出 0
  - `npm run build:electron-main` 退出 0（esbuild 打包 connection.ts 通过，external ssh2 不变）
  </acceptance_criteria>

  <done>connectSSH 算法配置 100% 复用 SSH_ALGORITHMS 常量（curve25519 自动补齐 + 全部老算法保留），readyTimeout 对齐 SSH_READY_TIMEOUT_MS(30s)；tsc web strict + esbuild main 打包双绿；全仓 SSH 算法表 drift 根源消除。</done>
</task>

<task type="auto">
  <name>Task 2: 真路径单测验证 SSH_ALGORITHMS 与 curve25519-only 对端协商成功</name>
  <files>tests/electron/connectSSH.algorithms.real.test.ts</files>

  <read_first>
    - tests/electron/_helpers/mockSshServer.ts（Phase 12 SSH mock 对端，startMockSshServer(onExec) 返回 {port, close}；SEC-03 测试需改造为限制对端 algorithms）
    - tests/electron/arpCollector.real.test.ts（Phase 12 真路径套件范例，vi.mock 反向范式 + mockSshServer 接入 + handleLeakDetector 句柄兜底）
    - tests/electron/ai.execCommands.real.test.ts（Phase 12 SSH 真路径测试结构参照，new ssh2.Client + connect + ready/exec 流程）
    - electron/utils/sshConfig.ts（SSH_ALGORITHMS 常量，测试 import 验证协商）
    - electron/services/connection.ts（Task 1 改后形态，确认 connectSSH 用 SSH_ALGORITHMS）
    - tests/electron/_helpers/handleLeakDetector.ts（expectNoHandleLeak 默认白名单，SSH 测试复用）
    - vitest.electron.config.ts（Phase 12 test:electron 通道配置，确认 include 采集 tests/electron/**）
  </read_first>

  <action>
  按 D-13-8 借 Phase 12 真路径套件验证 SEC-03 核心目标（SSH_ALGORITHMS 含 curve25519 后能连通现代 Linux）。

  **可行性决策（D-13-8 planner 评估结论）**：connectSSH 全函数带 `termWin: BrowserWindow` 参数（Electron app 未 ready 时测试通道无法造真 BrowserWindow），故**不调 connectSSH 全函数**，而是直接验证核心契约——`new ssh2.Client()` 用 `{ ...基础config, algorithms: SSH_ALGORITHMS }` 与 curve25519-only 对端协商成功触发 'ready'。这正是 SEC-03 改造的语义本质（connectSSH 复用 SSH_ALGORITHMS 后能连现代 Linux），绕开 BrowserWindow 依赖同时 100% 覆盖算法协商路径。

  新增 `tests/electron/connectSSH.algorithms.real.test.ts`（真路径，走 Phase 12 test:electron 通道），3 个 it：

  1. **it 'SSH_ALGORITHMS kex 含 curve25519-sha256 首项'**（常量断言，无网络）：import SSH_ALGORITHMS，断言 `SSH_ALGORITHMS.kex![0] === 'curve25519-sha256'`（验证 SEC-03 复用源常量已含 curve25519，是修复的前提）。

  2. **it 'client.connect 用 SSH_ALGORITHMS 与 curve25519-only 对端协商成功（ready 触发）'**（真路径）：本地起 ssh2.Server 但 `algorithms: { kex: ['curve25519-sha256'] }`（对端仅支持 curve25519，模拟现代 Linux 典型算法集），然后 `new ssh2.Client()` connect `{ host:'127.0.0.1', port, username, password, algorithms: SSH_ALGORITHMS, readyTimeout: SSH_READY_TIMEOUT_MS }`，断言 'ready' 事件触发（协商成功）。**改造前 connection.ts 内联表缺 curve25519 会 KEX 失败**——此 it 是 SEC-03 修复的回归守卫。

  3. **it 'client.connect 用内联旧表（缺 curve25519）与 curve25519-only 对端协商失败（error 触发）'**（反向回归，验证 drift 危害）：同对端，但 client.connect algorithms 用历史内联表（kex 不含 curve25519，从 connection.ts 改造前快照硬编码进测试），断言 'error' 事件触发（KEX 失败）。此 it 锁定 drift 危害，防止未来有人误删 SSH_ALGORITHMS 回退内联表。

  **测试接入范式（参照 arpCollector.real.test.ts）**：
  - 因直接用 ssh2.Client（非 vi.mock ssh2），无需 vi.mock 反向范式——走真 binding 连 mock 对端。
  - mockSshServer 改造：标准 startMockSshServer 不支持自定义对端 algorithms，故本测试**直接内联 ssh2.Server**（参照 mockSshServer.ts:34-52 结构，但 `new Server({ hostKeys:[keyPem], algorithms:{ kex:['curve25519-sha256'] } }, ...)`），不调 startMockSshServer helper。close() 返回 Promise 等 server.close 回调（Pitfall 4 异步 close，照抄 mockSshServer.ts:75-78）。
  - hostKey 用 `crypto.generateKeyPairSync('rsa',{modulusLength:2048})` 随机生成（T-12-01 mitigate，照抄 mockSshServer.ts:31-32）。
  - listen(0, '127.0.0.1') loopback（T-12-02 mitigate）。
  - 每个 it 末尾 `await expectNoHandleLeak()`（复用 handleLeakDetector，默认白名单含 TCPServerWrap/TCPWrap/SimpleWriteWrap，照抄 arpCollector.real.test.ts 句柄兜底）。
  - it 2/3 的 client.connect 用 Promise + 'ready'/'error' 事件 resolve，加超时兜底（防 hang，参照 ai.execCommands.real.test.ts 的 overallTimeout 模式）。
  </action>

  <verify>
  <automated>npm run test:electron -- connectSSH.algorithms.real</automated>
  </verify>

  <acceptance_criteria>
  - tests/electron/connectSSH.algorithms.real.test.ts 存在，含 3 个 it（curve25519 首项常量断言 + curve25519-only 对端协商成功 + 内联旧表协商失败反向回归）
  - it 'SSH_ALGORITHMS kex 含 curve25519-sha256 首项' 断言 `SSH_ALGORITHMS.kex[0] === 'curve25519-sha256'`
  - it '协商成功' 用 `algorithms: SSH_ALGORITHMS` 连 curve25519-only ssh2.Server，断言 'ready' 触发
  - it '协商失败' 用内联旧表（kex 不含 curve25519）连同一对端，断言 'error' 触发（反向回归守卫）
  - 测试直接内联 ssh2.Server（hostKey 随机生成 + listen(0,'127.0.0.1') loopback），不调 startMockSshServer helper（因 helper 不支持自定义对端 algorithms）
  - 每个 it 末尾 `await expectNoHandleLeak()`（复用 Phase 12 handleLeakDetector）
  - `npm run test:electron -- connectSSH.algorithms.real` 退出 0，3 it 全绿
  - `npm run test:electron`（全套件）退出 0，Phase 12 既有 22 it + 本 plan 3 it = 25 it 全绿零回归
  - `npm test`（plain node mock 套件）退出 0 零回归（vitest.config.ts exclude tests/electron/** 不变，本测试不进 plain 套件）
  </acceptance_criteria>

  <done>SEC-03 算法协商有真路径自动化回归——SSH_ALGORITHMS 含 curve25519 与现代 Linux 对端协商成功（改造前会失败的 drift 场景被测试锁定），Phase 12 真路径套件扩展到 25 it，三绿门禁（tsc + build:electron-main + test:electron）全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted renderer → IPC → main connectSSH | 终端连接由 `connection:ssh` secure IPC 触发（登录后特权），renderer 传 deviceId，main 查 device 凭证后调 connectSSH |
| main SSH client → 远程设备 SSH server | SSH 握手跨网络，算法协商在此边界（SEC-03 改造点） |
| 测试 ssh2.Client → 测试 ssh2.Server | tests/electron 真路径，loopback 127.0.0.1，不进安装包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | connection.ts connectSSH algorithms | mitigate | D-13-2 复用 SSH_ALGORITHMS 常量消 drift，补 curve25519-sha256（现代 Linux KEX）+ 全部兼容老算法保留（D-13-1 不删弱算法），既连通现代 Linux 又不削弱老设备兼容 |
| T-13-01-02 | Information Disclosure | SSH 弱算法（L1 group1-sha1/3des-cbc/ssh-dss） | accept | D-13-1 显式 defer：运维兼容性优先（CLAUDE.md「设备安全可控最高优先级」+ sshConfig.ts 注释「宁可列宽不可漏」），删弱算法有连不上老设备风险，运维场景不可接受。未来设备清单确认无老算法依赖可重评估 |
| T-13-01-03 | Denial of Service | connectSSH readyTimeout 10s 慢设备超时 | mitigate | D-13-3 对齐 SSH_READY_TIMEOUT_MS(30s)，治同类 drift（ai/arpCollector 已 30s，终端仍 10s），慢设备 10-30s 区间不再触发 readyTimeout |
| T-13-01-04 | Tampering | 三红线（IPC secure/safe / 字段加密 _enc / commandSafety） | accept | SEC-03 仅改 SSH 算法常量复用（algorithms/readyTimeout 两行），不碰 IPC 鉴权包装（connectSSH 由 connection:ssh secure 触发不变）、不碰字段加密（device 凭证读 encField/decField 不变）、不碰 commandSafety（终端 shell 不经 AI 命令执行层）。三红线改动后仍生效 |
| T-13-01-05 | Tampering | 测试 SSH 凭证/hostKey 泄漏 | mitigate | 测试 hostKey 用 crypto.generateKeyPairSync 随机生成（照抄 mockSshServer T-12-01），listen(0,'127.0.0.1') 严格 loopback（T-12-02），tests/ 不进 electron-builder 安装包 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 退出 0（strict + noUnusedLocals 全绿）
- `npm run build:electron-main` 退出 0（esbuild 打包 connection.ts，external ssh2/better-sqlite3 不变）
- `npm run test:electron` 退出 0，25 it 全绿（Phase 12 既有 22 + SEC-03 新增 3）
- `npm test` 退出 0 零回归（plain mock 套件不变）
- 全仓 SSH 算法表 drift 消除：grep `algorithms:\s*\{[\s\S]*?kex:` 在 connection.ts/ai.ts/arpCollector.ts 三文件命中 0
</verification>

<success_criteria>
- SC1（connection.ts 终端连接含 curve25519 与 SSH_ALGORITHMS 对齐）：connection.ts `algorithms: SSH_ALGORITHMS` 单行 + curve25519-only 对端协商成功 it 绿
- SC4（三红线不回退）：threat_model T-13-01-04 论证 SEC-03 仅改算法常量不碰鉴权/加密/命令安全层
- D-13-1（L1 不删弱算法）：SSH_ALGORITHMS 常量保留全部兼容算法，plan 显式 defer 登记（threat_model T-13-01-02）
- D-13-2（复用常量消 drift）：connection.ts 删内联表走 SSH_ALGORITHMS
- D-13-3（readyTimeout 对齐 30s）：connection.ts `readyTimeout: SSH_READY_TIMEOUT_MS`
- D-13-8（真路径自动化）：connectSSH.algorithms.real.test.ts 3 it 借 Phase 12 test:electron 通道
</success_criteria>

<output>
Create `.planning/phases/13-security-hardening-cluster/13-01-SUMMARY.md` when done
</output>
