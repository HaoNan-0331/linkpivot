---
phase: 13
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/authGuard.test.ts
  - .planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md
autonomous: true
requirements: [SEC-04]
tags: [security, hardening, authguard, captcha, login, ai-limit, defer-triage]

must_haves:
  truths:
    - "pre-release 安全 hardening 5 项（L1/L2/L3/L4/L6）逐项有明确结论——要么修要么显式 defer 登记 reason（SC2 要求，不可静默跳过）"
    - "L6 authGuard 加固确认——secure/safe 包装未登录拒绝 + 异常脱敏保持生效，authGuard 单测覆盖未登录拒绝 case（既有 7 it 扩展确认 safe 未登录不拒绝 + secure 未登录拒绝）"
    - "L4 Login 加固确认——captcha 前置 + 失败 5 次锁定 5min + 通用错误消息（不区分用户存在性）+ 口令强度策略 + auth:* 全 safe 包装，核心已满足"
    - "L3 captcha 加固确认——文本用 CSPRNG（crypto.randomInt）+ 4 位去歧义字符集 + 5min TTL + 一次一删防重放，核心已满足；renderSvg 用 Math.random 仅 SVG 干扰显式 defer（非安全敏感）"
    - "L2 ai limit 甄别——命令执行层 commandSafety.isCommandAllowed 红线③强制（不可绕过），ai:chat secure 包装；审计 28 findings 无独立 L2 finding（仅 p9e 排除清单标签），单机单用户桌面无恶意客户端滥用面，ai 频率/token 限流显式 defer"
    - "L1 删弱算法 defer——D-13-1 锁定运维兼容性优先，不删 group1-sha1/3des-cbc/ssh-dss 等（连 13-01 SEC-03 plan 同 defer）"
    - "三红线（IPC secure/safe / 字段加密 _enc / commandSafety.isCommandAllowed）改动后仍生效——SEC-04 仅扩展 authGuard 单测确认覆盖，不削弱既有鉴权/加密/命令安全层"
  artifacts:
    - path: "tests/unit/authGuard.test.ts"
      provides: "L6 authGuard 单测扩展——safe 未登录不拒绝但脱敏 + secure 未登录拒绝已有 case 确认保持绿"
      contains: "safe"
    - path: ".planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md"
      provides: "SEC-04 五项甄别登记表（L1/L2/L3/L4/L6 各项结论 + reason + 代码/审计佐证）"
      min_lines: 60
  key_links:
    - from: "tests/unit/authGuard.test.ts"
      to: "electron/utils/authGuard.ts"
      via: "secure/safe 单测覆盖未登录拒绝 + 异常脱敏"
      pattern: "secure|safe|setAuthenticated"
    - from: ".planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md"
      to: "SC2 pre-release 5 项"
      via: "逐项结论 + 代码佐证 + 审计 finding 引用"
      pattern: "L1|L2|L3|L4|L6"
---

<objective>
SEC-04：pre-release 5 项安全 hardening（L1/L2/L3/L4/L6）逐项甄别收尾。SC2 要求每项要么修要么显式 defer 登记 reason，不可静默跳过（学 Phase 14 FIX-02 甄别模式）。

Purpose: p9e `260726-p9e` 发版时显式排除这 5 项归「发版后迭代」（p9e-SUMMARY.md:142），health §1.2 列为 medium 优先安全相关。D-13-4 锁定：researcher/planner 据审计 28 findings + 实际代码核对，若判定「已满足/改动伤三红线/成本远超收益」则该项显式 defer 登记，不照单全修。

Output: 经代码核对 + 审计交叉验证，5 项中 L2/L3/L4/L6 核心加固已满足（auth:* safe 包装 w67 已修、authGuard 单测 vcu 已补、captcha CSPRNG 已用、login 锁定已有），仅 L1 显式 defer（D-13-1）；L3 renderSvg Math.random + L2 ai 限流 + L4 失败计数持久化 各项 defer 登记 reason。本 plan 扩展 authGuard 单测确认 safe 未登录行为 + 写 DEFER-LOG 登记 5 项结论。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-security-hardening-cluster/13-CONTEXT.md
@.planning/phases/13-security-hardening-cluster/13-PATTERNS.md
@.planning/audits/2026-07-26-doc-code-audit.md
@.planning/quick/260726-p9e-pre-release-hardening-bump-0-1-2/260726-p9e-SUMMARY.md
@.planning/audits/2026-08-07-health-audit.md

<interfaces>
<!-- SEC-04 甄别锚点（代码核对结论，read_first 已验） -->

From electron/utils/authGuard.ts（L6 加固对象，已加固完整）：
```typescript
export function secure(handler) {
  return async (e, ...args) => {
    if (!authenticated) throw new Error('未登录或会话已过期')  // try 之外，不被脱敏覆盖
    try { return await handler(e, ...args) }
    catch (err) { throw new Error(sanitizeMessage(err?.message || '操作失败')) }
  }
}
export function safe(handler) {  // 仅脱敏不鉴权，auth:* 登录前 handler 用
  return async (e, ...args) => {
    try { return await handler(e, ...args) }
    catch (err) { throw new Error(sanitizeMessage(err?.message || '操作失败')) }
  }
}
export function isAuthenticated(): boolean { return authenticated }  // 0 caller，疑预留
```

From electron/services/auth.ts（L3/L4 加固对象，已较稳）：
```typescript
// L3 captcha：CSPRNG 文本 + 5min TTL + 一次一删
generateCaptcha() { chars[crypto.randomInt(0, chars.length)] ... captchaStore.set(key, {text, expires: Date.now()+5*60*1000}) }
verifyCaptcha(key, input) { ... captchaStore.delete(key); return s.text === input }  // 一次一删防重放
// renderSvg 用 Math.random 做 SVG 干扰线/字符位置/颜色（非安全敏感，文本已 CSPRNG）

// L4 Login：captcha 前置 + 失败锁定 + 通用错误
login(u, p, ck, ci) {
  if (!verifyCaptcha(...)) return {success:false, error:'验证码错误'}
  if (rec?.lockedUntil > Date.now()) return {success:false, error:'登录失败次数过多，请 5 分钟后再试'}
  if (!user || !verifyPasswordSync(...)) { count++; if (count>=5) lockedUntil=now+5min; return {success:false, error:'用户名或密码错误'} }
}
validatePasswordStrength(p) { if (p.length<10) ...; if (!/[a-zA-Z]/||!/\d/) ... }  // ≥10位+字母数字
```

From electron/main.ts:152-160（L4 IPC，auth:* 全 safe 包装——w67 已修审计 finding 6）：
```typescript
ipcMain.handle('auth:getCaptcha', safe(...))
ipcMain.handle('auth:login', safe(async (...) => { const r = await login(...); if (r.success) setAuthenticated(true); return r }))
ipcMain.handle('auth:isFirstRun', safe(() => isFirstRun()))
ipcMain.handle('auth:initAdmin', safe((_e, u, p) => initAdmin(u, p)))
```

From electron/main.ts:192（L2 ai limit，命令安全层红线③）：
```typescript
ipcMain.handle('ai:chat', secure((_e, messages, deviceIds, sessionId) => chat(messages, deviceIds, sessionId)))
// chat → executeCommandsOnDevice → commandSafety.isCommandAllowed 红线③强制（ai.ts:330-336）
```

From tests/unit/authGuard.test.ts（vcu 已补 7 it，含 secure 未登录拒绝 + safe 脱敏）：
```typescript
it('secure rejects when not authenticated ...', ...)  // L6 核心 case 已覆盖
it('safe does not require auth but still sanitizes', ...)  // safe 脱敏已覆盖
```

历史已修项（STATE §Quick Tasks Completed）：
- 260726-w67：P1 加固 auth:* safe 包装 + app.isPackaged + native rebuild/CI 冒烟（修审计 finding 6/7/14）
- 260726-vcu：R5 安全核心单测 commandSafety + authGuard 55/55（修审计 R5/finding 10 doc-drift 证伪 safe() 零 caller）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 authGuard 单测确认 safe 未登录不拒绝 + secure 未登录拒绝 L6 加固覆盖</name>
  <files>tests/unit/authGuard.test.ts</files>

  <read_first>
    - tests/unit/authGuard.test.ts（既有 7 it，重点 line 10-19 secure 未登录拒绝 + line 71-81 safe 脱敏，L6 加固后须保持绿）
    - electron/utils/authGuard.ts（secure:31-41 + safe:44-53 + isAuthenticated:12-24，L6 加固对象）
    - .planning/phases/13-security-hardening-cluster/13-PATTERNS.md（§authGuard.ts secure/safe Pattern + §tests/unit/authGuard.test.ts 扩展 Pattern）
    - .planning/audits/2026-07-26-doc-code-audit.md（§5 R5 安全核心零单测 + §4 finding 10 safe() 零 caller doc-drift）
  </read_first>

  <action>
  按 D-13-8 补 SEC-04 L6 单测（authGuard secure/safe 未登录拒绝）。260726-vcu 已补 7 it（含 secure 未登录拒绝 + safe 脱敏），本 task 在既有基础上**补强 L6 加固确认 case**，确保 secure/safe 包装在未登录态的行为边界有显式断言（防未来误改削弱红线）。

  在 tests/unit/authGuard.test.ts 既有 `describe('authGuard')` 内**追加 3 个 it**（不删既有 7 it）：

  1. **it 'safe does NOT reject when not authenticated (登录前 channel 不要求鉴权)'**：setAuthenticated(false)，`const wrapped = safe(() => 'ok')`，断言 `await wrapped({})` resolves to 'ok'（验证 safe 是登录前 channel，不强制鉴权——这是 auth:getCaptcha/auth:login/auth:isFirstRun/auth:initAdmin 能在未登录态调用的前提，与 secure 区分）。既有 line 71-81 it 只测 safe 脱敏（throw 路径），本 it 补 safe 正常路径未登录不拒绝。

  2. **it 'isAuthenticated returns current auth state (预留查询入口，0 caller 但保留)'**：setAuthenticated(false)，断言 `isAuthenticated() === false`；setAuthenticated(true)，断言 `isAuthenticated() === true`。验证 health §2.2 标记的 `isAuthenticated` 0 caller 函数行为正确（决定保留作预留查询入口，不删——见 DEFER-LOG Task 2 登记）。

  3. **it 'secure sanitizes SQL fragment from error (no internal detail leak)'**：setAuthenticated(true)，`secure(() => { throw new Error('SQLITE_CONSTRAINT: experiences.tags is not unique in /app/db/main.db') })`，断言 throw 的 message 含 '[路径]'（/app/db/main.db 被脱敏）且不含 '/app/db/main.db' 原文。补 SQL 错误路径脱敏确认（既有 it 只测纯 Windows/Unix 路径，本 it 补 SQL 错误含路径的复合场景）。

  **红线约束**：既有 7 it（尤其 line 10-13 secure 未登录拒绝）必须保持绿——L6 加固不可破坏既有 case。追加而非改写。
  </action>

  <verify>
  <automated>npx vitest run tests/unit/authGuard.test.ts</automated>
  </verify>

  <acceptance_criteria>
  - tests/unit/authGuard.test.ts 含新增 3 it（safe 未登录不拒绝 + isAuthenticated 返回当前态 + secure 脱敏 SQL 错误含路径）
  - 既有 7 it 保持不变（grep `secure rejects when not authenticated` 命中 1，line 10-13 case 未删）
  - 新 it 'safe does NOT reject when not authenticated' 断言 safe 未登录态 resolves to 'ok'（非 reject）
  - 新 it 'isAuthenticated returns current auth state' 断言 setAuthenticated(false)→isAuthenticated()===false + setAuthenticated(true)→isAuthenticated()===true
  - 新 it 'secure sanitizes SQL fragment' 断言 throw message 含 '[路径]' 且不含 '/app/db/main.db'
  - `npx vitest run tests/unit/authGuard.test.ts` 退出 0，10 it（既有 7 + 新增 3）全绿
  - `npx tsc -p tsconfig.web.json --noEmit` 退出 0
  - `npm test` 退出 0 零回归（plain mock 套件）
  </acceptance_criteria>

  <done>L6 authGuard 加固有单测确认覆盖——secure 未登录拒绝（既有）+ safe 未登录不拒绝（新增）+ isAuthenticated 行为（新增）+ SQL 错误脱敏（新增），10 it 全绿，红线①不可削弱有回归网兜底。</done>
</task>

<task type="auto">
  <name>Task 2: 写 SEC-04 五项甄别 DEFER-LOG（L1/L2/L3/L4/L6 逐项结论 + reason + 佐证）</name>
  <files>.planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md</files>

  <read_first>
    - .planning/audits/2026-07-26-doc-code-audit.md（§4 finding 6 auth:* IPC + finding 10 safe() 零 caller doc-drift + §5 R5 安全核心零单测——L6 甄别佐证）
    - .planning/audits/2026-08-07-health-audit.md（§1.2 pre-release 14 项发版后迭代 + §2.2 isAuthenticated 0 caller investigate——L6/L4 甄别佐证）
    - .planning/quick/260726-p9e-pre-release-hardening-bump-0-1-2/260726-p9e-SUMMARY.md（line 142 发版后迭代排除清单——L1/L2/L3/L4/L6 原始来源）
    - electron/services/auth.ts（L3 captcha CSPRNG + L4 login 锁定——核心已满足佐证）
    - electron/utils/authGuard.ts（L6 secure/safe 实现——核心已满足佐证）
    - electron/main.ts:152-160,192（L4 auth:* safe 包装 + L2 ai:chat secure 包装——w67 已修佐证）
    - electron/services/ai.ts:330-336（L2 commandSafety.isCommandAllowed 红线③——命令安全层已强制佐证）
    - .planning/STATE.md（§Quick Tasks Completed 260726-w67 + 260726-vcu——历史已修项佐证）
    - .planning/phases/13-security-hardening-cluster/13-CONTEXT.md（D-13-1 L1 defer + D-13-4 甄别退路）
  </read_first>

  <action>
  按 D-13-4 + SC2 写 SEC-04 五项甄别登记文档。每项含：结论（FIXED / DEFER）+ 代码层佐证（file:line）+ 审计 finding 引用 + reason（FIXED 写何时由谁修，DEFER 写为何不修 + 重评估条件）。**不可静默跳过任何一项**（SC2 硬要求）。

  在 `.planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md` 写以下 5 项登记（每项一节，含结论/佐证/reason 三段）：

  **L1 删弱 SSH 算法 → DEFER**
  - 结论：DEFER（不删 group1-sha1/group14-sha1/diffie-hellman-group-exchange-sha1/3des-cbc/blowfish-cbc/ssh-dss）
  - 佐证：sshConfig.ts:11-33 SSH_ALGORITHMS 全保留弱算法；connection.ts/ai.ts/arpCollector.ts 三路径全复用（13-01 SEC-03 plan 落地）
  - 审计引用：p9e-SUMMARY.md:142「L1 弱 SSH 算法」排除清单
  - reason：D-13-1 锁定，运维兼容性优先（CLAUDE.md「设备安全可控最高优先级」+ sshConfig.ts 注释「宁可列宽不可漏连各种厂商/老型号设备」），删弱算法有连不上老设备风险
  - 重评估条件：未来设备清单确认无老算法依赖（全设备支持 curve25519/rsa-sha2-256 等）可重评估删除

  **L6 authGuard 加固 → FIXED（核心已满足，本 plan Task 1 补单测确认）**
  - 结论：FIXED
  - 佐证：authGuard.ts:31-41 secure 未登录 reject 在 try 之外不被脱敏覆盖 + sanitizeMessage:17-24 路径/长度脱敏；main.ts:152-160 auth:* 全 safe 包装；authGuard.test.ts 10 it（含 Task 1 新增 3）
  - 审计引用：finding 10「safe() 零 caller」是 doc-drift（CONCERNS.md 已证伪，现有 4 caller auth:* 全 safe）；R5「安全核心零单测」由 260726-vcu 修（55/55）
  - reason：260726-w67 修 auth:* safe 包装（审计 finding 6）；260726-vcu 补 authGuard 单测（R5）；本 plan Task 1 扩展确认 safe 未登录行为 + isAuthenticated + SQL 错误脱敏。`isAuthenticated` 0 caller（health §2.2）决定**保留作预留查询入口**（未来 renderer 检测登录态可暴露 auth:check IPC，本 phase 不引入新 IPC）

  **L4 Login 加固 → FIXED（核心已满足）**
  - 结论：FIXED
  - 佐证：auth.ts:37-58 login captcha 前置（verifyCaptcha 先验）+ 失败计数 MAX_ATTEMPTS=5 + 锁定 LOCK_MS=5min + 通用错误消息（'用户名或密码错误' 不区分用户存在性）；validatePasswordStrength:31-35 ≥10位+字母数字；initAdmin:64-75 用此策略；main.ts:152-160 auth:* 全 safe 包装
  - 审计引用：p9e-SUMMARY.md:142「L4 Login」排除清单；审计 finding 6 auth:* IPC 已由 w67 修
  - reason：login 锁定 + captcha 前置 + 口令强度 + 通用错误消息 + auth:* safe 包装全已就位。**潜在加固（失败计数内存态不跨重启、无 IP 级限流）DEFER**——单机单用户桌面（CLAUDE.md「单机单用户场景足够」auth.ts:7 注释），无多用户/网络滥用面，内存态失败计数重启重置可接受

  **L3 captcha 加固 → FIXED（核心已满足）+ renderSvg Math.random DEFER**
  - 结论：FIXED（核心）+ renderSvg Math.random DEFER
  - 佐证：auth.ts:12-20 generateCaptcha 文本用 crypto.randomInt（CSPRNG 非 Math.random）+ 4 位去歧义字符集（'ABCDEFGHJKLMNPQRSTUVWXYZ23456789' 无 0/O/1/I）+ 5min TTL（expires: Date.now()+5*60*1000）；verifyCaptcha:22-28 一次一删（captchaStore.delete(key) 防重放）
  - 审计引用：p9e-SUMMARY.md:142「L3 captcha」排除清单
  - reason：captcha 文本生成是安全敏感路径，已用 CSPRNG（crypto.randomInt）+ 防重放（一次一删）+ TTL 过期。**renderSvg:77-85 用 Math.random 做 SVG 干扰线坐标/字符位置/颜色随机化 DEFER**——非安全敏感（仅 SVG 视觉干扰，文本已 CSPRNG 生成，攻击面不在 renderSvg），改用 crypto.randomInt 无安全收益（验证码文本才是攻击目标，SVG 噪声坐标用 Math.random 不影响文本可预测性）

  **L2 ai limit → DEFER（命令安全层已强制 + 单机无滥用面 + 审计无独立 finding）**
  - 结论：DEFER
  - 佐证：main.ts:192 ai:chat secure 包装（登录后特权）；ai.ts:330-336 executeCommandsOnDevice 内 commandSafety.isCommandAllowed 红线③强制（任何未经白名单通过的命令直接拒绝，不依赖调用方是否已校验）；无 messages 数量/token/频率/并发限流（grep MAX_MESSAGES/MAX_TOKENS/rateLimit/throttle 命中 0）
  - 审计引用：p9e-SUMMARY.md:142「L2 ai limit」排除清单（仅标签，审计 28 findings 无独立 L2 finding 描述具体限流要求）
  - reason：单机单用户桌面（CLAUDE.md「单机单用户场景」），无恶意客户端滥用面（用户自己用，不存在 DoS 自己的场景）；命令执行安全层（commandSafety.isCommandAllowed 红线③）已强制，AI 限流即使加也无法绕过命令安全层；AI 调用成本由用户自付（自有 API Key），无平台滥用经济动机。**重评估条件**：若未来转多用户/云部署或审计新增独立 L2 finding 描述具体限流要求（如 token 上限防 context 溢出），重评估加 messages.slice 截断或 token 预算
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md','utf8');['L1','L2','L3','L4','L6'].forEach(t=>{if(!new RegExp('## '+t+'[\\\\s→]').test(s)){console.error('FAIL: 缺 '+t+' 节');process.exit(1)}});['DEFER','FIXED','reason','佐证','审计引用','重评估条件'].forEach(k=>{if(!s.includes(k)){console.error('FAIL: 缺关键字段 '+k);process.exit(1)}});console.log('OK')"</automated>
  </verify>

  <acceptance_criteria>
  - .planning/phases/13-security-hardening-cluster/13-02-DEFER-LOG.md 存在
  - 文档含 L1/L2/L3/L4/L6 五节（grep `## L1`/`## L2`/`## L3`/`## L4`/`## L6` 各命中 1）
  - 每节含「结论（FIXED/DEFER）+ 佐证（file:line）+ 审计引用 + reason + 重评估条件（DEFER 项）」五段
  - L1 节结论 DEFER + 引用 D-13-1 + 运维兼容性 reason
  - L6 节结论 FIXED + 引用 authGuard.ts:31-41 + finding 10 doc-drift 证伪 + 260726-w67/vcu 已修 + isAuthenticated 保留决定
  - L4 节结论 FIXED + 引用 auth.ts:37-58 login 锁定 + w67 已修 auth:* safe 包装
  - L3 节结论 FIXED（核心）+ renderSvg Math.random DEFER + 引用 auth.ts:12-20 CSPRNG + renderSvg 非安全敏感 reason
  - L2 节结论 DEFER + 引用 ai.ts:330-336 commandSafety 红线③ + 单机单用户 reason + 审计无独立 finding
  - SC2 要求满足：5 项无静默跳过，每项有明确结论 + reason
  </acceptance_criteria>

  <done>SEC-04 五项甄别登记完成——L1 DEFER（D-13-1）/L6 FIXED（核心已满足 + Task 1 单测确认）/L4 FIXED（核心已满足）/L3 FIXED 核心 + renderSvg DEFER/L2 DEFER（命令安全层已强制 + 单机无滥用面），SC2「每项要么修要么显式 defer 登记」满足，无静默跳过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted renderer → IPC secure/safe 网关 → main handler | 三红线①IPC 鉴权边界，SEC-04 L6 加固对象 |
| 登录前 renderer → auth:* safe IPC → auth.ts login/captcha | L4 Login + L3 captcha 边界，未登录态可访问 |
| main AI chat → commandSafety.isCommandAllowed → 远程执行 | 三红线③命令安全边界，L2 ai limit 相关 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Spoofing/Tampering | L6 authGuard secure/safe 削弱风险 | mitigate | SEC-04 仅扩展 authGuard 单测确认覆盖（Task 1 追加 3 it），不改 authGuard.ts 生产代码；既有 secure 未登录拒绝 case 保持绿（红线①不可回退） |
| T-13-02-02 | Spoofing | L1 删弱 SSH 算法 | accept | D-13-1 显式 defer：运维兼容性优先（详见 13-02-DEFER-LOG L1 节 + 13-01 threat_model T-13-01-02），未来设备清单确认无老算法依赖重评估 |
| T-13-02-03 | Spoofing | L3 captcha renderSvg Math.random | accept | captcha 文本已用 CSPRNG（crypto.randomInt）+ 防重放（一次一删）+ 5min TTL，核心安全敏感路径已加固；renderSvg Math.random 仅 SVG 视觉干扰非安全敏感（DEFER-LOG L3 节登记） |
| T-13-02-04 | Spoofing/DoS | L4 Login 失败计数内存态不跨重启 | accept | 单机单用户桌面（auth.ts:7 注释「单机单用户场景足够」），无多用户/网络滥用面，内存态失败计数重启重置可接受（DEFER-LOG L4 节登记） |
| T-13-02-05 | DoS | L2 ai 无频率/token 限流 | accept | 命令执行层 commandSafety.isCommandAllowed 红线③强制（不可绕过）；单机单用户无恶意客户端滥用面；审计 28 findings 无独立 L2 finding（DEFER-LOG L2 节登记） |
| T-13-02-06 | Repudiation/Tampering | isAuthenticated 0 caller 预留入口 | accept | health §2.2 标记 investigate，经核对是预留查询入口（未来 renderer 检测登录态可用），非漏洞；Task 1 补单测确认行为正确；本 phase 不引入新 IPC 保留现状 |
| T-13-02-07 | Tampering | 三红线（IPC secure/safe / 字段加密 _enc / commandSafety） | accept | SEC-04 仅扩展 authGuard 单测 + 写 DEFER-LOG 文档，不改任何生产鉴权/加密/命令安全代码；260726-w67 已修 auth:* safe 包装，260726-vcu 已补安全核心单测，三红线加固基线不变 |
</threat_model>

<verification>
- `npx vitest run tests/unit/authGuard.test.ts` 退出 0，10 it（既有 7 + Task 1 新增 3）全绿
- `npx tsc -p tsconfig.web.json --noEmit` 退出 0
- `npm test` 退出 0 零回归（plain mock 套件）
- 13-02-DEFER-LOG.md 含 L1/L2/L3/L4/L6 五节，每节结论 + 佐证 + reason 齐全（SC2 满足）
- 三红线生产代码零改动（git diff electron/utils/authGuard.ts / electron/services/auth.ts / electron/services/ai.ts / electron/main.ts 退出 0——本 plan 仅改测试 + 文档）
</verification>

<success_criteria>
- SC2（pre-release L1/L2/L3/L4/L6 每项要么修要么显式 defer 登记）：13-02-DEFER-LOG.md 五项齐全，无静默跳过
- SC4（三红线不回退）：threat_model T-13-02-01/T-13-02-07 论证 SEC-04 仅扩展单测不改生产鉴权代码
- D-13-1（L1 defer 运维兼容性）：DEFER-LOG L1 节登记
- D-13-4（甄别退路）：5 项逐项 FIXED/DEFER 结论 + reason，不照单全修不静默跳过
- D-13-8（L6 authGuard 单测）：Task 1 扩展 3 it 确认 secure/safe 未登录行为
</success_criteria>

<output>
Create `.planning/phases/13-security-hardening-cluster/13-02-SUMMARY.md` when done
</output>
