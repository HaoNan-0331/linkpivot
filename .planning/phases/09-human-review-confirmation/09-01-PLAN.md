---
phase: 09-human-review-confirmation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/experienceService.ts
  - electron/services/experienceService.test.ts
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03]

must_haves:
  truths:
    - "service 层提供 confirmDrafts 受控接口，单事务原子把采纳的 draft 转 published、UPDATE 草稿按勾选 supersede 旧条目、丢弃草稿 hard delete，全成全败"
    - "service 层提供 listDrafts 接口返回 status='draft' 的暂存草稿列表（支撑 AIPage 待确认角标重开弹窗）"
    - "service 层提供 getSessionMessages 接口返回会话原文明文数组（复用 ai.ts getChatHistory 解密），供 renderer 溯源回链"
    - "confirmDrafts 对采纳的 troubleshooting 草稿二次校验 severity/symptoms/resolution（service 层兜底质量门，与 renderer 标红三层纵深）"
    - "confirmDrafts 不动 CR-01 收紧的 updateExperience 白名单——status 改变只走新增专用接口（draft→published）"
  artifacts:
    - path: "electron/services/experienceService.ts"
      provides: "confirmDrafts / listDrafts / getSessionMessages 三个新 export function"
      contains: "export function confirmDrafts"
    - path: "electron/services/experienceService.test.ts"
      provides: "confirmDrafts/listDrafts/getSessionMessages 单测（内存 mock DB 复用既有 setup）"
      contains: "confirmDrafts"
  key_links:
    - from: "electron/services/experienceService.ts (confirmDrafts)"
      to: "experienceService invalidateExperience / deleteExperience / relateDevice / unrelateDevice"
      via: "单事务内调用同模块受控函数（draft→published + supersede + delete + 设备关联 diff）"
      pattern: "invalidateExperience\\(|deleteExperience\\(|relateDevice\\(|unrelateDevice\\("
    - from: "electron/services/experienceService.ts (getSessionMessages)"
      to: "electron/services/ai.ts getChatHistory"
      via: "import { getChatHistory } from './ai'"
      pattern: "from './ai'"
---

<objective>
Phase 9 服务层落地：在 `experienceService.ts` 新增 3 个函数式受控接口——`confirmDrafts`（批量确认原子事务，D-9-1/D-9-2/D-9-4/D-9-6）、`listDrafts`（D-9-7）、`getSessionMessages`（D-9-5）。

Purpose: 这是 session→permanent 唯一人工闸口的 service 实现（红线③执行点），不动 CR-01 收紧的 `updateExperience` 白名单（status 改变走新增专用接口），为 Plan 02 IPC 层提供可注册的函数面，为 Plan 03 renderer 弹窗提供可调用的后端能力。

Output:
- `electron/services/experienceService.ts` 追加 3 个 export function + `ConfirmDraftItem`/`ConfirmDraftsInput`/`ConfirmDraftsResult` 接口
- `electron/services/experienceService.test.ts` 追加 confirmDrafts/listDrafts/getSessionMessages 单测（复用既有内存 mock DB + device mock 模式，规避 DEP-1）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-human-review-confirmation/09-CONTEXT.md
@.planning/phases/09-human-review-confirmation/09-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From electron/services/experienceService.ts（已在 context 中读全，文件顶部模块级 MK + dbGetter 测试钩子已就位）:

```typescript
let MK = ''
export function setExperienceMasterKey(key: string) { MK = key }
export const MAX_BATCH = 1000
let dbGetter: () => Database.Database = getDatabase
export function _setExperienceDbGetter(fn: () => Database.Database): void { dbGetter = fn }
function db(): Database.Database { return dbGetter() }

// 现有受控状态接口范例（D-9-1 confirmDrafts 同模式新增，不动 update 白名单）：
export function invalidateExperience(id: string): any {
  const conn = db()
  conn.prepare(
    `UPDATE experiences SET invalid_at = datetime('now','localtime'), updated_at = datetime('now','localtime') WHERE id = ?`
  ).run(id)
  return getExperience(id)
}
export function deleteExperience(id: string): void {
  db().prepare('DELETE FROM experiences WHERE id = ?').run(id)
}
export function relateDevice(experienceId: string, deviceId: string, relationType: string = 'primary'): void { ... }
export function unrelateDevice(experienceId: string, deviceId: string): void { ... }
export function listExperiences(opts: ListExperiencesOpts): PaginatedResult<any> { ... } // 已支持 status:'draft' 过滤分支
```

updateExperience 事务包裹范例（confirmDrafts 单事务多写扩展自此模式）:

```typescript
const conn = db()
const tx = conn.transaction(() => {
  conn.prepare(`UPDATE experiences SET ${sets.join(', ')} WHERE id = ?`).run(...params)
})
tx()
```

validateAndStringifyAttrs severity 强校验范例（confirmDrafts 质量门兜底复用同模式）:

```typescript
if (category === 'troubleshooting') {
  if (!attrs || !attrs.severity || !VALID_SEVERITIES.includes(attrs.severity)) {
    throw new Error('troubleshooting 类经验 attrs 必须含合法 severity')
  }
}
```

From electron/services/ai.ts:192-209（getSessionMessages 直接 import 复用，已 decField 解密返明文）:

```typescript
export function getChatHistory(sessionId?: string): Array<{
  id: string
  role: string
  content: string
  deviceId: string | null
  createdAt: string
}>
```

From electron/services/experienceService.test.ts（内存 mock DB + device mock 已就位，本 plan 追加测试复用同 setup）:
- `mockDbRef` / `_setExperienceDbGetter` 注入内存 DB
- `vi.mock('./device', ...)` 拦截 device 模块
- `MemDb` 类复刻 prepare/transaction/exec 子集（confirmDrafts 的单 UPDATE/DELETE 走 prepare，多写走 transaction 包裹 fn()）
- `MK_TEST_KEY` + `setExperienceMasterKey` 注入
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 experienceService.ts 实现 confirmDrafts / listDrafts / getSessionMessages + 接口定义</name>
  <files>electron/services/experienceService.ts</files>
  <read_first>
    - electron/services/experienceService.ts（被修改文件 + invalidateExperience/updateExperience/deleteExperience/relateDevice/unrelateDevice/listExperiences/validateAndStringifyAttrs 全部现有函数范式）
    - electron/services/ai.ts:192-209（getChatHistory 签名，getSessionMessages import 复用）
    - .planning/phases/09-human-review-confirmation/09-CONTEXT.md（D-9-1~D-9-7 全部锁定决策 + 代码层质量门三层纵深定义）
    - .planning/phases/09-human-review-confirmation/09-PATTERNS.md（confirmDrafts 函数签名建议 + 受控接口模式 + db.transaction 单事务多写范例 + Prepared Statement 循环外复用范例）
  </read_first>
  <behavior>
    - confirmDrafts 对 action='adopt' 的 troubleshooting 草稿：attrs 缺 severity 或 severity 非合法枚举 → throw（service 层兜底质量门）
    - confirmDrafts 对 action='adopt' 的 troubleshooting 草稿：symptoms 或 resolution 为空字符串/缺失 → throw
    - confirmDrafts 全部采纳项 + 丢弃项在单事务内执行，部分失败 throw → 整批 ROLLBACK（无半成品）
    - confirmDrafts action='adopt' 的 draft：UPDATE experiences SET status='published'，status 从 'draft' 变 'published'
    - confirmDrafts action='adopt' + supersedeOld=true 且 draft.duplicate_of_exp_id 非空：旧条目 invalidate（invalid_at 落时间）
    - confirmDrafts action='adopt' + supersedeOld=false（默认）：旧条目保留不动（防 AI 误判 UPDATE 实为 ADD 误删）
    - confirmDrafts 设备关联 diff 语义：relateDevices 为 undefined 或空数组 → 不动现有关联（diff 跳过）；仅当 relateDevices 为 length>0 的显式数组时才计算 toAdd/toRemove 调 relateDevice/unrelateDevice（防 renderer 默认空数组静默拆光所有现有关联）
    - confirmDrafts action='discard'：draft 行 hard DELETE FROM experiences（status 仍 draft，不留库）
    - confirmDrafts 返回 { adopted: number, discarded: number, superseded: number } 计数正确
    - confirmDrafts 入参 drafts.length > MAX_BATCH(1000) → throw '批量上限' 错误
    - confirmDrafts 不复活 updateExperience 的 status 字段（grep updateExperience 调用面不含 status）
    - listDrafts 返回 status='draft' 的全部 Experience[]
    - getSessionMessages(sessionId) 返回 getChatHistory 解密后的明文消息数组（role/content）
  </behavior>
  <action>
在 `electron/services/experienceService.ts` 文件末尾（`touchLastVerifiedAt` 函数之后，文件末尾的 Phase 11 复用接口预埋区块）追加：

**1. import 补充**（文件顶部 import 区，紧接现有 `import { getDeviceById } from './device'`）：
```typescript
import { getChatHistory } from './ai'
```

**2. 新增接口定义**（放在 `ExperienceUpdateFields` 接口之后，类型定义区块）：
```typescript
/**
 * Phase 9 D-9-1/D-9-4：confirmDrafts 受控接口入参（draft→published + 可选 supersede 旧条目 + 丢弃）。
 * fields 复用 ExperienceUpdateFields（CR-01 白名单，不含 status）——采纳时若用户在弹窗编辑过字段则一并落库。
 * relateDevices：全量期望关联 device_id 列表（confirmDrafts 内 diff 现有关联后调 relateDevice/unrelateDevice）。
 *   语义：undefined 或空数组 [] 都视为「不动现有关联」（diff 跳过）；只有 length>0 的显式数组才触发 diff。空数组语义已废弃，防 renderer 默认值传播静默拆关联。
 * supersedeOld：D-9-2，UPDATE 草稿（duplicate_of_exp_id 非空）专用，默认 false（防 Phase 8 AI 误判 UPDATE 实为 ADD 误删有效旧条目），
 *   true 时旧条目经 invalidateExperience 软失效（invalid_at 落时间）。
 */
export interface ConfirmDraftItem {
  expId: string
  action: 'adopt' | 'discard'
  fields?: ExperienceUpdateFields
  relateDevices?: string[]
  supersedeOld?: boolean
}
export interface ConfirmDraftsInput { drafts: ConfirmDraftItem[] }
export interface ConfirmDraftsResult { adopted: number; discarded: number; superseded: number }
```

**3. confirmDrafts 实现**（单事务原子，D-9-4）——放文件末尾，照 PATTERNS.md 受控接口模式 + db.transaction 单事务多写范例：
```typescript
/**
 * Phase 9 D-9-1/D-9-4：批量确认草稿——session→permanent 唯一人工闸口（红线③执行点）。
 * 单事务原子（throw ROLLBACK，全成全败）：
 * - action='adopt'：UPDATE status='draft'→'published' + 若 fields 非空则 updateExperience 落编辑字段 + diff relateDevices
 * - action='adopt' + supersedeOld=true 且 draft.duplicate_of_exp_id 非空：invalidateExperience(旧条目) 软失效（D-9-2）
 * - action='discard'：deleteExperience（hard DELETE，D-9-6）
 * 质量门 service 层兜底：adopt 的 troubleshooting 草稿二次校验 severity/symptoms/resolution（与 renderer 标红三层纵深）。
 * 不动 CR-01 收紧的 updateExperience 白名单——status 改变只走本接口（draft→published），不复活 update 的 status 字段。
 */
export function confirmDrafts(input: ConfirmDraftsInput): ConfirmDraftsResult {
  if (!input || !Array.isArray(input.drafts)) {
    throw new Error('confirmDrafts 入参无效：drafts 必须为数组')
  }
  if (input.drafts.length > MAX_BATCH) {
    throw new Error(`批量上限超过 MAX_BATCH（${MAX_BATCH}）`)
  }
  let adopted = 0
  let discarded = 0
  let superseded = 0
  const conn = db()
  const tx = conn.transaction(() => {
    // 循环外 prepare 复用（CONVENTIONS Pattern 4）
    const stmtPublish = conn.prepare(
      `UPDATE experiences SET status = 'published', updated_at = datetime('now','localtime') WHERE id = ?`
    )
    for (const d of input.drafts) {
      if (d.action === 'discard') {
        deleteExperience(d.expId)
        discarded++
        continue
      }
      // action === 'adopt'
      const cur = getExperience(d.expId) as any
      if (!cur) throw new Error(`草稿不存在: ${d.expId}`)
      // 质量门 service 层兜底：adopt 时若用户编辑过 fields 则用 fields，否则用现有 cur 字段
      const finalCategory = d.fields?.category ?? cur.category
      const finalAttrs = d.fields?.attrs !== undefined ? d.fields.attrs : cur.attrs
      const finalTitle = d.fields?.title ?? cur.title
      const finalContent = d.fields?.content ?? cur.content
      // troubleshooting 必填校验（severity/symptoms/resolution）
      if (finalCategory === 'troubleshooting') {
        if (!finalAttrs || !finalAttrs.severity || !['critical', 'high', 'medium', 'low', 'info'].includes(finalAttrs.severity)) {
          throw new Error(`草稿 ${d.expId} troubleshooting 缺合法 severity，无法确认`)
        }
        if (!finalAttrs.symptoms || !String(finalAttrs.symptoms).trim()) {
          throw new Error(`草稿 ${d.expId} troubleshooting 缺 symptoms，无法确认`)
        }
        if (!finalAttrs.resolution || !String(finalAttrs.resolution).trim()) {
          throw new Error(`草稿 ${d.expId} troubleshooting 缺 resolution，无法确认`)
        }
      } else {
        // 轻结构类：title/content 必填
        if (!finalTitle || !String(finalTitle).trim()) {
          throw new Error(`草稿 ${d.expId} 缺 title，无法确认`)
        }
        if (!finalContent || !String(finalContent).trim()) {
          throw new Error(`草稿 ${d.expId} 缺 content，无法确认`)
        }
      }
      // 落编辑字段（若有）——走 updateExperience（CR-01 白名单，不含 status）
      if (d.fields && Object.keys(d.fields).length > 0) {
        updateExperience(d.expId, d.fields)
      }
      // draft→published（专用接口，不复活 update 白名单）
      stmtPublish.run(d.expId)
      adopted++
      // 设备关联 diff：仅当 relateDevices 为 length>0 的显式数组才触发（undefined/空数组都视为不动现有关联，防 renderer 默认空数组静默拆光所有现有关联）
      if (d.relateDevices != null && d.relateDevices.length > 0) {
        const curDevices = listDevicesByExperience(d.expId).map((dev: any) => dev.id)
        const toAdd = d.relateDevices.filter((id) => !curDevices.includes(id))
        const toRemove = curDevices.filter((id) => !d.relateDevices!.includes(id))
        for (const did of toAdd) relateDevice(d.expId, did)
        for (const did of toRemove) unrelateDevice(d.expId, did)
      }
      // D-9-2：UPDATE 草稿（duplicate_of_exp_id 非空）+ supersedeOld=true → 旧条目软失效
      if (d.supersedeOld && cur.duplicate_of_exp_id) {
        invalidateExperience(cur.duplicate_of_exp_id)
        superseded++
      }
    }
  })
  tx()
  return { adopted, discarded, superseded }
}
```

**4. listDrafts 实现**（D-9-7，复用 listExperiences status 分支）：
```typescript
/**
 * Phase 9 D-9-7：列暂存 draft（AIPage 待确认角标入口用，重开确认弹窗）。
 * 复用 listExperiences 的 status='draft' 过滤分支；draft 行 invalid_at 恒 NULL，includeInvalid 取值不影响。
 */
export function listDrafts(): any[] {
  return listExperiences({ status: 'draft', includeInvalid: true, limit: MAX_BATCH, offset: 0 }).rows
}
```

**5. getSessionMessages 实现**（D-9-5，复用 ai.ts getChatHistory 解密返明文）：
```typescript
/**
 * Phase 9 D-9-5：取原始会话原文（用户在确认弹窗内点「查看原始会话」溯源核对）。
 * 复用 ai.ts getChatHistory（已 decField 解密 chat_history.content_enc 返明文）。
 * 信任边界（design D-04）：返明文给 renderer——用户核对自己对话，单机 safeStorage 绑机器，不做 PII 脱敏。
 * sessionId 指向已删/不存在时 getChatHistory 返空数组，由 renderer 提示「原会话已不可查」。
 */
export function getSessionMessages(sessionId: string): Array<{
  id: string
  role: string
  content: string
  deviceId: string | null
  createdAt: string
}> {
  if (!sessionId || typeof sessionId !== 'string') {
    throw new Error('sessionId 无效')
  }
  return getChatHistory(sessionId)
}
```
  </action>
  <verify>
    <automated>npx vitest run electron/services/experienceService.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function confirmDrafts" electron/services/experienceService.ts` 等于 1
    - `grep -c "export function listDrafts" electron/services/experienceService.ts` 等于 1
    - `grep -c "export function getSessionMessages" electron/services/experienceService.ts` 等于 1
    - `grep -c "import { getChatHistory } from './ai'" electron/services/experienceService.ts` 等于 1
    - `grep -c "export interface ConfirmDraftItem" electron/services/experienceService.ts` 等于 1
    - `grep -c "export interface ConfirmDraftsInput" electron/services/experienceService.ts` 等于 1
    - `grep -c "export interface ConfirmDraftsResult" electron/services/experienceService.ts` 等于 1
    - confirmDrafts 内 status='published' 单语句 UPDATE（grep "SET status = 'published'" 等于 1）
    - confirmDrafts 不复活 update 白名单（grep -c "updateExperience" 调用在 confirmDrafts 上下文确认走白名单 fields 不含 status）
    - 单事务包裹（grep "conn.transaction" 命中 confirmDrafts 内 1 处）
    - vitest 全绿 exit 0
  </acceptance_criteria>
  <done>
    - 3 个新 export function 实现并经 service 层兜底质量门校验
    - confirmDrafts 单事务原子（adopt+supersede+discard+设备 diff 全成全败）
    - 不动 CR-01 update 白名单（status 改变只走 confirmDrafts 新接口）
    - getSessionMessages 复用 getChatHistory 明文回链
    - 类型编译无错（无 noUnusedLocals 触发）
  </done>
</task>

<task type="auto">
  <name>Task 2: 在 experienceService.test.ts 追加 confirmDrafts / listDrafts / getSessionMessages 单测（复用既有内存 mock DB）</name>
  <files>electron/services/experienceService.test.ts</files>
  <read_first>
    - electron/services/experienceService.test.ts（完整读，复用既有 MemDb mock + beforeEach setup + vi.mock('./device') 模式 + createExperience 测试用例作为 draft 落库 fixture）
    - electron/services/experienceService.ts（确认 Task 1 实现的函数签名与接口，测试 import 对齐）
    - electron/services/ai.ts:192-209（getChatHistory 签名，getSessionMessages 测试需 vi.mock('./ai') 拦截）
  </read_first>
  <action>
在 `electron/services/experienceService.test.ts` 文件末尾追加新 describe 块。

**mock 策略：**
- `confirmDrafts` / `listDrafts`：复用既有内存 mock DB + `_setExperienceDbGetter` 注入，先调 `createExperience({ status 走 draft（createExperience 内部固定写 'draft'，无需显式传 status }) ... })` 落几条 draft 行作 fixture，再调 `confirmDrafts` 验证。
- `getSessionMessages`：在文件顶部 `vi.mock` 区追加 `vi.mock('./ai', () => ({ getChatHistory: (sid) => mockChatHistory[sid] ?? [] }))`，mock 返固定消息数组，验证 `getSessionMessages('s1')` 返回该数组 + 非法 sessionId throw。
- import 区追加：`confirmDrafts, listDrafts, getSessionMessages` 从 `./experienceService` 导入；新增 `ConfirmDraftsInput` type 导入（如需）。

**测试用例（覆盖 behavior 全部分支）：**

1. `describe('confirmDrafts', ...)`：
   - `it('adopt 单条 draft → status 转 published + 计数 adopted=1')` — 先 createExperience 落 troubleshooting draft（含 severity/symptoms/resolution 完整 attrs），调 confirmDrafts([{expId, action:'adopt'}])，断言 getExperience 返回 status='published' + result.adopted===1。
   - `it('troubleshooting draft 缺 severity → throw（service 层兜底质量门）')` — createExperience 用 best_practices 类（attrs 空）后 UPDATE category 改 troubleshooting 但 attrs 无 severity（或直接 create troubleshooting 时绕过——但 createExperience 已强制 severity，故用 mock 直接插入缺 severity 的 draft 行）→ confirmDrafts adopt throw。
   - `it('troubleshooting draft 缺 symptoms → throw')` — 类似上，attrs.severity 合法但 symptoms 空 → throw。
   - `it('轻结构类 draft 缺 title → throw')` — createExperience 落 best_practices draft，UPDATE title='' → confirmDrafts adopt throw。
   - `it('discard 单条 draft → hard DELETE + 计数 discarded=1')` — createExperience 落 draft，confirmDrafts([{expId, action:'discard'}])，断言 getExperience 返 null + result.discarded===1。
   - `it('adopt + supersedeOld=true + duplicate_of_exp_id 非空 → 旧条目 invalidate（invalid_at 落时间）')` — createExperience 落旧条目 expA（status published，valid），createExperience 落 draft expB 带 duplicateOfExpId=expA.id，confirmDrafts([{expId:expB.id, action:'adopt', supersedeOld:true}])，断言 expA.invalid_at 非 null + result.superseded===1 + expB.status='published'。
   - `it('adopt + supersedeOld=false（默认）+ duplicate_of_exp_id 非空 → 旧条目保留 invalid_at 仍 null')` — 同上但不传 supersedeOld，断言 expA.invalid_at 仍 null + result.superseded===0。
   - `it('单事务原子：adopt 第一条成功 + 第二条 quality 门 throw → 整批 ROLLBACK 第一条不落 published')` — 落 draft1（完整 troubleshooting）+ draft2（缺 severity），confirmDrafts([adopt draft1, adopt draft2]) throw，断言 draft1.status 仍 'draft'（无半成品）。
   - `it('drafts.length > MAX_BATCH → throw 批量上限错误')` — 构造 1001 条 ConfirmDraftItem 数组 → throw。
   - `it('adopt + fields 非空 → updateExperience 落编辑字段（title/content/tags 改动）')` — createExperience draft，confirmDrafts([{expId, action:'adopt', fields:{title:'改后标题', content:'改后内容'}}])，断言 getExperience title/content 已更新 + status='published'。
   - `it('adopt + relateDevices diff → 新增关联 + 移除未列关联')` — 落 draft，先 relateDevice 关联 dev1，confirmDrafts([{expId, action:'adopt', relateDevices:['dev2']}])，断言 dev1 已 unrelate、dev2 已 relate（listDevicesByExperience 返 dev2）。
   - `it('adopt + relateDevices 空数组 → 现有设备关联不变（不动现有关联，防默认值传播拆关联）')` — 落 draft，先 relateDevice 关联 dev1，confirmDrafts([{expId, action:'adopt', relateDevices:[]}])，断言 dev1 仍关联（listDevicesByExperience 仍返 dev1）。
   - `it('adopt + relateDevices=undefined → 现有设备关联不变')` — 落 draft，先 relateDevice 关联 dev1，confirmDrafts([{expId, action:'adopt'}])（不传 relateDevices），断言 dev1 仍关联。

2. `describe('listDrafts', ...)`：
   - `it('返回 status=draft 的全部草稿')` — createExperience 落 2 条 draft + 1 条 manual published（mock 直接 UPDATE），调 listDrafts，断言长度 2 且全 status='draft'。

3. `describe('getSessionMessages', ...)`：
   - `it('返回 getChatHistory 解密明文数组')` — mock getChatHistory('s1') 返 [{id,role:'user',content:'明文1',deviceId:null,createdAt:'...'}]，断言 getSessionMessages('s1') 深等。
   - `it('sessionId 不存在 → 返空数组')` — mock 返 []，断言 getSessionMessages('nope') 深等 []。
   - `it('sessionId 无效（空/非 string）→ throw')` — getSessionMessages('') throw、getSessionMessages(null as any) throw。

**注意：** createExperience 强制 troubleshooting severity（validateAndStringifyAttrs），缺 severity 的 draft 行需绕过 createExperience 直接用 mock DB prepare INSERT 插入（参照 experienceService.test.ts 既有「直接操作 mockDbRef.current 插入」模式，或新增 helper），确保测试能构造 service 层兜底校验的触发场景。
  </action>
  <verify>
    <automated>npx vitest run electron/services/experienceService.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "describe('confirmDrafts'" electron/services/experienceService.test.ts` 等于 1
    - `grep -c "describe('listDrafts'" electron/services/experienceService.test.ts` 等于 1
    - `grep -c "describe('getSessionMessages'" electron/services/experienceService.test.ts` 等于 1
    - confirmDrafts 单事务原子 ROLLBACK 测试存在（grep "ROLLBACK" 或 "整批" 命中 1 处以上）
    - troubleshooting 缺 severity/symptoms/resolution throw 测试各存在
    - vitest 全绿 exit 0（含新增测试 + 既有测试不回归）
    - 总测试数 ≥ 既有数 + 13（confirmDrafts 12 含 relateDevices 空/undefined 不动关联 + listDrafts 1 + getSessionMessages 3，executor 按实际合并近似）
  </acceptance_criteria>
  <done>
    - confirmDrafts 全分支（adopt/discard/supersede/质量门 throw/单事务 ROLLBACK/MAX_BATCH/fields 编辑/relateDevices diff）覆盖
    - listDrafts / getSessionMessages 覆盖
    - vitest 全绿，既有测试不回归
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→IPC→service（confirmDrafts） | 受信 main 进程内，但批量入参 drafts[] 来自 renderer，需校验上限 + 字段合法性 |
| service→ai.ts getChatHistory（getSessionMessages） | 跨 service 域调用，返回会话明文经 IPC 边界回链 renderer（design D-04 明文回链是设计意图，用户核对自己对话） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | confirmDrafts 入参 drafts[] | mitigate | MAX_BATCH=1000 上限 throw + service 层兜底质量门校验（troubleshooting severity/symptoms/resolution 强校验，轻结构 title/content 必填） |
| T-09-02 | Repudiation | confirmDrafts 批量确认操作 | accept | 单事务原子（throw ROLLBACK 全成全败），updated_at 自动落时间审计；运维场景单机无多用户审计需求 |
| T-09-03 | Information Disclosure | getSessionMessages 返会话明文 | accept | design D-04 明确回链展示明文（用户核对自己对话），单机 safeStorage 绑机器，不做 PII 脱敏；secure IPC 鉴权确保仅登录后特权操作可调 |
| T-09-04 | Elevation of Privilege | status 改变绕过 CR-01 白名单 | mitigate | confirmDrafts 不复活 updateExperience 的 status 字段，status 改变只走新增专用接口（draft→published 单语句 UPDATE，照 invalidateExperience 同模式） |
</threat_model>

<verification>
- `npx vitest run electron/services/experienceService.test.ts` 全绿 exit 0
- 3 个新 export function 存在（grep 验证）
- 单事务原子覆盖（throw ROLLBACK 测试存在）
- 不动 updateExperience 白名单（grep updateExperience 调用面不含 status）
</verification>

<success_criteria>
- service 层 confirmDrafts/listDrafts/getSessionMessages 三接口可被 Plan 02 IPC 注册
- 红线③人工闸口落地（确认才 published，service 层兜底质量门拦截残缺条目）
- D-9-1（draft→published 直接转）/ D-9-2（UPDATE 旧条目命运默认不勾）/ D-9-4（单事务原子）/ D-9-6（discard hard DELETE）/ D-9-7（listDrafts）/ D-9-5（getSessionMessages 明文回链）全部 service 层落地
- 不加迁移（复用 experiences 表 + Phase 8 v9 duplicate_of_exp_id 列）
- 测试全绿，既有 103 测试不回归
</success_criteria>

<output>
After completion, create `.planning/phases/09-human-review-confirmation/09-01-SUMMARY.md`
</output>
