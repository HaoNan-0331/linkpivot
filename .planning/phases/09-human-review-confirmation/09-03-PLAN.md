---
phase: 09-human-review-confirmation
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/components/pages/ai/ReviewConfirmModal.tsx
  - src/components/pages/ai/ReviewConfirmEditForm.tsx
  - src/components/pages/ai/SessionMessagesModal.tsx
  - src/components/pages/ai/useAIChat.ts
  - src/components/pages/AIPage.tsx
  - src/components/pages/ai/ChatInput.tsx
autonomous: false
requirements: [REVIEW-01, REVIEW-02, REVIEW-03]

must_haves:
  truths:
    - "用户点「经验总结」后弹窗逐条展示草稿，可编辑标题/分类/内容/attrs 模板字段/标签/关联设备，逐条勾选采纳/丢弃（REVIEW-01）"
    - "缺必填项的草稿（troubleshooting 缺 symptoms/resolution/severity，轻结构缺 title/content）标红且确认按钮禁用（REVIEW-02，renderer 实时校验三层纵深第一层）"
    - "每条草稿可点「查看原始会话」叠层只读子 Modal 展示 source_session_id 会话原文（REVIEW-03 溯源回链）"
    - "用户在弹窗内编辑/勾选所有草稿后点底部「确认采纳 N 条 + 丢弃 M 条」一次性批量提交，单事务原子（采纳 draft→published + UPDATE 勾选的 supersede 旧条目 + 丢弃 hard DELETE）"
    - "UPDATE 草稿（带 duplicate_of_exp_id）默认不勾「标失效旧条目」，用户主动勾才 supersede（D-9-2 防 AI 误判误删）"
    - "AIPage「经验总结」按钮旁显示「待确认 N 条」Badge 角标，点击重开确认弹窗（D-9-7 暂存 draft 不成孤儿）"
  artifacts:
    - path: "src/components/pages/ai/ReviewConfirmModal.tsx"
      provides: "宽 Modal master-detail 主壳（左侧草稿列表勾选 + 标红 + 底部批量提交 + 子 Modal 叠层）"
      contains: "ReviewConfirmModal"
    - path: "src/components/pages/ai/ReviewConfirmEditForm.tsx"
      provides: "右侧编辑表单子组件（title/category/content/tags 字段 + attrs 模板动态字段 + 关联设备 Select + UPDATE 旧条目 supersedeOld Checkbox + 查看原始会话）"
      contains: "ReviewConfirmEditForm"
    - path: "src/components/pages/ai/SessionMessagesModal.tsx"
      provides: "只读子 Modal 展示 source_session_id 会话原文"
      contains: "SessionMessagesModal"
    - path: "src/components/pages/ai/useAIChat.ts"
      provides: "handleSummarize 完成后开 ReviewConfirmModal + pendingDraftCount 角标计数"
      contains: "reviewOpen"
    - path: "src/components/pages/AIPage.tsx"
      provides: "挂载 ReviewConfirmModal + ChatInput 传 Badge 角标"
      contains: "ReviewConfirmModal"
    - path: "src/components/pages/ai/ChatInput.tsx"
      provides: "「经验总结」按钮旁 Badge 角标入口"
      contains: "Badge"
  key_links:
    - from: "src/components/pages/ai/ReviewConfirmModal.tsx"
      to: "window.api.experience.confirmDrafts / listDrafts / get"
      via: "useEffect(open) 拉 draft 列表 + handleOk 调 confirmDrafts 批量提交"
      pattern: "window\\.api\\.experience\\.(confirmDrafts|listDrafts|get)"
    - from: "src/components/pages/ai/SessionMessagesModal.tsx"
      to: "window.api.experience.getSessionMessages"
      via: "useEffect(open + sessionId) 拉会话原文"
      pattern: "window\\.api\\.experience\\.getSessionMessages"
    - from: "src/components/pages/ai/useAIChat.ts handleSummarize"
      to: "ReviewConfirmModal open"
      via: "setReviewOpen(true) + setReviewInitialDraftIds(result.created/updated exp_id 列表)"
      pattern: "setReviewOpen"
---

<objective>
Phase 9 renderer 层落地：新建 ReviewConfirmModal（宽 Modal + master-detail 确认弹窗）+ SessionMessagesModal（只读会话回链子 Modal），扩展 useAIChat（handleSummarize 完成后开弹窗 + 待确认角标计数）+ AIPage（挂载弹窗）+ ChatInput（Badge 角标入口）。

Purpose: 把 Plan 01/02 的 service + IPC 能力变成用户可操作的 UI——人工逐条编辑/勾选/质量门校验后批量确认（红线③ session→permanent 唯一人工闸口的 renderer 执行点），每条可溯源回链产生它的原始会话，未处理草稿经角标入口重开。

Output:
- `src/components/pages/ai/ReviewConfirmModal.tsx`（新建）
- `src/components/pages/ai/SessionMessagesModal.tsx`（新建）
- `src/components/pages/ai/useAIChat.ts`（修改——追加 reviewOpen/reviewInitialDraftIds/pendingDraftCount 状态 + handleSummarize 完成开弹窗）
- `src/components/pages/AIPage.tsx`（修改——挂载 ReviewConfirmModal + 传 Badge）
- `src/components/pages/ai/ChatInput.tsx`（修改——「经验总结」按钮包 Badge）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-human-review-confirmation/09-CONTEXT.md
@.planning/phases/09-human-review-confirmation/09-PATTERNS.md
@.planning/phases/09-human-review-confirmation/09-01-SUMMARY.md
@.planning/phases/09-human-review-confirmation/09-02-SUMMARY.md
@./CLAUDE.md

<interfaces>
<!-- Plan 02 已落地的 renderer 调用面（window.api.experience.*）+ DTO -->

From src/types/electron.d.ts（Plan 02 追加）+ preload（Plan 02 追加）:

```typescript
window.api.experience.confirmDrafts(input: ConfirmDraftsInput): Promise<ConfirmDraftsResult>
window.api.experience.listDrafts(): Promise<DraftSummary[]>
window.api.experience.getSessionMessages(sessionId: string): Promise<SessionMessage[]>
window.api.experience.get(id: string): Promise<Experience | null>
window.api.experience.listDevices(experienceId: string): Promise<ExperienceRelatedDevice[]>
window.api.experience.relateDevice(experienceId, deviceId, relationType?): Promise<void>
window.api.experience.unrelateDevice(experienceId, deviceId): Promise<void>
window.api.experience.summarizeSession(sessionId: string): Promise<DraftingResult>
```

From src/types/experience.ts（Plan 02 追加）:

```typescript
export interface ConfirmDraftItem {
  expId: string
  action: 'adopt' | 'discard'
  fields?: ExperienceUpdateInput
  relateDevices?: string[]
  supersedeOld?: boolean
}
export interface ConfirmDraftsInput { drafts: ConfirmDraftItem[] }
export interface ConfirmDraftsResult { adopted: number; discarded: number; superseded: number }
export type DraftSummary = Experience
export interface SessionMessage {
  id: string; role: string; content: string; deviceId: string | null; createdAt: string
}

// 既有：
export interface Experience {
  id: string; title: string; category: ExperienceCategory; content: string;
  tags: string[]; status: ExperienceStatus; source_session_id?: string | null;
  attrs?: ExperienceAttrs | null; valid_at: string; invalid_at?: string | null;
  last_verified_at?: string | null; reuse_count: number; created_at: string; updated_at: string;
  duplicate_of_exp_id?: string | null;  // Phase 8 v9 列（UPDATE 草稿标注命中旧条目）
}
export interface ExperienceAttrs {
  symptoms?: string; root_cause?: string; resolution?: string; prevention?: string;
  severity?: 'critical' | 'high' | 'medium' | 'low' | 'info';
}
export type ExperienceCategory = 'troubleshooting' | 'best_practices' | 'product' | 'env'
```

<!-- 现有组件范式（已读全） -->

From src/components/pages/ai/CommandConfirmModal.tsx（Modal + open/onCancel + footer 按钮模式 + Tag + 滚动 div overflowY:'auto'）:

```tsx
interface CommandConfirmModalProps { pendingConfirm: ConfirmData | null; onConfirm: (approved: boolean) => void }
export default function CommandConfirmModal({ pendingConfirm, onConfirm }: CommandConfirmModalProps) {
  return (
    <Modal open={!!pendingConfirm} title={...} onCancel={() => onConfirm(false)}
      footer={[<Button key="reject" danger>拒绝</Button>, <Button key="approve" type="primary">确认</Button>]}>
      {/* body */}
    </Modal>
  )
}
```

From src/components/pages/ai/useAIChat.ts（hook 状态 + handler 范式 + handleSummarize 完成点 line 182）:

```typescript
const [summarizing, setSummarizing] = useState(false)
// handleSummarize 完成后（line 182 message.success 后）：
// 改为 setReviewOpen(true) + setReviewInitialDraftIds([...result.created, ...result.updated].map(x => x.exp_id))
// return 块追加 reviewOpen, reviewInitialDraftIds, pendingDraftCount, setReviewOpen, handleReviewSubmitted
```

From src/components/pages/AIPage.tsx:99（Modal 挂载模式）:

```tsx
<CommandConfirmModal pendingConfirm={chat.pendingConfirm} onConfirm={chat.handleConfirm} />
// 追加同款：
<ReviewConfirmModal open={chat.reviewOpen} onClose={() => chat.setReviewOpen(false)} initialDraftIds={chat.reviewInitialDraftIds} onSubmitted={chat.handleReviewSubmitted} />
```

From src/components/pages/ai/ChatInput.tsx:41-50（onSummarize Button + Badge 包裹点）:

```tsx
{onSummarize && (
  <Button icon={<ThunderboltOutlined />} onClick={onSummarize} loading={summarizing}
    disabled={!canSummarize || loading || summarizing}>经验总结</Button>
)}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新建 SessionMessagesModal.tsx（只读会话回链子 Modal）</name>
  <files>src/components/pages/ai/SessionMessagesModal.tsx</files>
  <read_first>
    - src/components/pages/ai/CommandConfirmModal.tsx（被复刻的 Modal 范式：open/title/onCancel/footer + Tag + 滚动 div overflowY:'auto' 风格）
    - src/types/experience.ts（SessionMessage DTO 形态，Plan 02 追加）
    - .planning/phases/09-human-review-confirmation/09-PATTERNS.md（SessionMessagesModal Props 签名 + 滚动容器代码摘录）
    - .planning/phases/09-human-review-confirmation/09-CONTEXT.md（D-9-5 明文回链 + 边界处理「source_session_id 不存在回链提示『原会话已不可查』」）
  </read_first>
  <action>
新建 `src/components/pages/ai/SessionMessagesModal.tsx`：

**Props：**
```tsx
interface SessionMessagesModalProps {
  open: boolean
  sessionId: string | null
  onClose: () => void
}
```

**组件实现要点（照 CommandConfirmModal.tsx Modal 范式 + 只读滚动 body）：**
- `import { Modal, Spin, Empty, Tag } from 'antd'`
- `import { useEffect, useState } from 'react'`
- 组件内 `useState<SessionMessage[]>([])` + `useState<boolean>(false)`（loading）
- `useEffect(() => { if (!open || !sessionId) { setMessages([]); return } setLoading(true); window.api.experience.getSessionMessages(sessionId).then((msgs) => setMessages(msgs)).finally(() => setLoading(false)) }, [open, sessionId])`
- Modal：`<Modal open={open} title="原始会话（只读）" onCancel={onClose} footer={[<Button key="close" onClick={onClose}>关闭</Button>]} width={720}>`
- body：loading 时 `<Spin />`；messages.length === 0 且 sessionId 非空 → `<Empty description="原会话已不可查（会话已删除或无消息）" />`（D-9-5 边界处理）；否则渲染 messages.map：每条 `<div style={{ marginBottom: 8 }}><Tag color={m.role === 'user' ? 'blue' : 'green'}>{m.role}</Tag><div style={{ marginTop: 4, whiteSpace: 'pre-wrap', maxHeight: 60, overflowY: 'auto', fontSize: 13, background: '#f5f5f5', padding: 8, borderRadius: 4 }}>{m.content}</div></div>`
- 外层滚动容器：`<div style={{ maxHeight: 480, overflowY: 'auto' }}>`（长会话滚动）

**类型 import：** `import type { SessionMessage } from '@/types/experience'`

**注意：** 不依赖 `window.api.ai.getSessionMessages`（那是 ai 域 channel），用 `window.api.experience.getSessionMessages`（Plan 02 新增 channel，design D-04 明文回链）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在（test -f src/components/pages/ai/SessionMessagesModal.tsx）
    - `grep -c "export default function SessionMessagesModal" src/components/pages/ai/SessionMessagesModal.tsx` 等于 1
    - `grep -c "window.api.experience.getSessionMessages" src/components/pages/ai/SessionMessagesModal.tsx` 等于 1
    - `grep -c "interface SessionMessagesModalProps" src/components/pages/ai/SessionMessagesModal.tsx` 等于 1
    - `grep -c "原会话已不可查" src/components/pages/ai/SessionMessagesModal.tsx` 等于 1（边界处理提示）
    - `grep -c "overflowY: 'auto'" src/components/pages/ai/SessionMessagesModal.tsx` ≥ 1（滚动容器）
    - tsc 编译无错
  </acceptance_criteria>
  <done>
    - 只读子 Modal 创建，可被 ReviewConfirmModal 点「查看原始会话」叠层打开
    - 拉取会话明文 + 边界处理（空会话提示）
    - tsc 无错
  </done>
</task>

<task type="auto">
  <name>Task 2a: 新建 ReviewConfirmModal.tsx 主壳（宽 Modal + 左侧列表勾选标红 + 底部批量提交 + 子 Modal 叠层；内联最简 EditForm 占位）</name>
  <files>src/components/pages/ai/ReviewConfirmModal.tsx</files>
  <read_first>
    - src/components/pages/ai/CommandConfirmModal.tsx（Modal + open/title/onCancel/footer 范式）
    - src/components/pages/ai/SessionMessagesModal.tsx（Task 1 新建，本任务 import 用作子 Modal 叠层）
    - src/types/experience.ts（Experience / ExperienceUpdateInput / ConfirmDraftsInput / ConfirmDraftsResult / DraftingResult DTO，Plan 02 追加）
    - .planning/phases/09-human-review-confirmation/09-PATTERNS.md（ReviewConfirmModal Props 签名 + master-detail 左右分栏代码摘录 + AddDeviceModal Table 勾选 + handleOk 校验范例）
    - .planning/phases/09-human-review-confirmation/09-CONTEXT.md（D-9-2 默认不勾标失效旧条目 + D-9-3 宽 Modal 80vw + D-9-4 批量一次性提交 + D-9-7 暂存 + 质量门三层纵深第一层 renderer 标红）
  </read_first>
  <action>
新建 `src/components/pages/ai/ReviewConfirmModal.tsx`（本任务只落主壳 + 列表 + 提交逻辑，右侧编辑表单用内联最简占位，Task 2b 提取为独立 `ReviewConfirmEditForm.tsx` 后接线）。

**Props：**
```tsx
interface ReviewConfirmModalProps {
  open: boolean
  onClose: () => void
  initialDraftIds?: string[]            // Phase 8 summarizeSession 完成后传入 created/updated exp_id 列表
  onSubmitted?: (result: ConfirmDraftsResult) => void   // 提交后刷新角标计数
}
```

**state：**
```tsx
const [drafts, setDrafts] = useState<Experience[]>([])              // draft 列表
const [decisions, setDecisions] = useState<Record<string, { action: 'adopt' | 'discard'; supersedeOld: boolean; fields: ExperienceUpdateInput; relateDevices: string[] | undefined }>>({})
const [selectedExpId, setSelectedExpId] = useState<string | null>(null)
const [loading, setLoading] = useState(false)
const [submitting, setSubmitting] = useState(false)
const [sessionModalSessionId, setSessionModalSessionId] = useState<string | null>(null)  // 子 Modal 叠层
```

**useEffect(open || initialDraftIds)：**
```tsx
useEffect(() => {
  if (!open) return
  setLoading(true)
  const fetcher = initialDraftIds && initialDraftIds.length > 0
    ? Promise.all(initialDraftIds.map((id) => window.api.experience.get(id))).then((rs) => rs.filter((x): x is Experience => !!x))
    : window.api.experience.listDrafts()
  fetcher.then((list) => {
    setDrafts(list)
    // 初始化 decisions：默认全部 adopt（用户纵览后可改 discard），supersedeOld 默认 false（D-9-2 防 AI 误判误删）
    const init: typeof decisions = {}
    for (const d of list) {
      init[d.id] = {
        action: 'adopt',
        supersedeOld: false,
        fields: { title: d.title, category: d.category, content: d.content, tags: d.tags, attrs: d.attrs ?? undefined },
        relateDevices: undefined,   // undefined = 不改现有关联（与 service 层 confirmDrafts 守卫 length>0 语义一致）；仅当用户在右侧打开/编辑设备 Select 时才填数组
      }
    }
    setDecisions(init)
    setSelectedExpId(list.length > 0 ? list[0].id : null)
  }).finally(() => setLoading(false))
}, [open, initialDraftIds])
```

**质量门 renderer 实时校验函数（导出，Task 2b EditForm 与本主壳列表标红共用；返回 errors 数组）：**
```tsx
export function validateDraft(d: Experience, fields: ExperienceUpdateInput): string[] {
  const errs: string[] = []
  const cat = fields.category ?? d.category
  const attrs = fields.attrs ?? d.attrs
  if (cat === 'troubleshooting') {
    if (!attrs || !attrs.severity) errs.push('缺 severity')
    if (!attrs || !attrs.symptoms || !String(attrs.symptoms).trim()) errs.push('缺 symptoms')
    if (!attrs || !attrs.resolution || !String(attrs.resolution).trim()) errs.push('缺 resolution')
  } else {
    if (!fields.title || !String(fields.title).trim()) errs.push('缺 title')
    if (!fields.content || !String(fields.content).trim()) errs.push('缺 content')
  }
  return errs
}
const hasBlockingErrors = drafts.some((d) => {
  const dec = decisions[d.id]
  return dec && dec.action === 'adopt' && validateDraft(d, dec.fields).length > 0
})
```

**Modal 结构（宽 Modal 80vw + master-detail flex 左右分栏，D-9-3）——本任务右侧用内联最简占位 `InlineEditFormPlaceholder`，Task 2b 替换为 `<ReviewConfirmEditForm>`：**
```tsx
return (
  <>
    <Modal
      open={open}
      title={`经验草稿确认（${drafts.length} 条）`}
      onCancel={onClose}
      width="80vw"
      footer={[
        <Button key="cancel" onClick={onClose}>取消（暂存，稍后从角标重开）</Button>,
        <Button key="allAdopt" onClick={() => setAll('adopt')}>全选采纳</Button>,
        <Button key="allDiscard" danger onClick={() => setAll('discard')}>全选丢弃</Button>,
        <Button key="submit" type="primary" loading={submitting}
          disabled={hasBlockingErrors || drafts.length === 0}
          onClick={handleSubmit}>
          确认采纳 {drafts.filter(d => decisions[d.id]?.action === 'adopt').length} 条 + 丢弃 {drafts.filter(d => decisions[d.id]?.action === 'discard').length} 条
        </Button>,
      ]}
    >
      {loading ? <Spin /> : drafts.length === 0 ? <Empty description="暂无待确认草稿" /> : (
        <div style={{ display: 'flex', gap: 16, minHeight: 400 }}>
          {/* 左：草稿列表 */}
          <div style={{ width: 360, borderRight: '1px solid #eee', paddingRight: 16, overflowY: 'auto', maxHeight: 480 }}>
            {drafts.map((d) => {
              const dec = decisions[d.id]
              const errs = dec ? validateDraft(d, dec.fields) : []
              return (
                <div key={d.id} onClick={() => setSelectedExpId(d.id)}
                  style={{ padding: 8, marginBottom: 4, cursor: 'pointer',
                    background: selectedExpId === d.id ? '#e6f4ff' : '#fff',
                    border: errs.length > 0 && dec?.action === 'adopt' ? '1px solid #ff4d4f' : '1px solid #d9d9d9',
                    borderRadius: 4 }}>
                  <div style={{ display: 'flex', alignItems: 'center', gap: 8 }}>
                    <Checkbox checked={dec?.action === 'adopt'} onChange={(e) => updateDecision(d.id, { action: e.target.checked ? 'adopt' : 'discard' })} />
                    <span style={{ flex: 1, textDecoration: dec?.action === 'discard' ? 'line-through' : 'none' }}>{dec?.fields.title || d.title}</span>
                  </div>
                  <div style={{ marginTop: 4, display: 'flex', gap: 4, flexWrap: 'wrap' }}>
                    <Tag color="blue">{d.category}</Tag>
                    {d.duplicate_of_exp_id && <Tag color="orange">UPDATE</Tag>}
                    {errs.length > 0 && dec?.action === 'adopt' && <Tag color="red">{errs.join(' / ')}</Tag>}
                  </div>
                  {d.duplicate_of_exp_id && dec?.action === 'adopt' && (
                    <Checkbox style={{ marginTop: 4, fontSize: 12 }} checked={dec.supersedeOld}
                      onChange={(e) => updateDecision(d.id, { supersedeOld: e.target.checked })}>
                      标失效旧条目（默认不勾，主动勾才 supersede）
                    </Checkbox>
                  )}
                </div>
              )
            })}
          </div>
          {/* 右：编辑表单（选中条目）—— Task 2a 内联最简占位，Task 2b 替换为 <ReviewConfirmEditForm> */}
          <div style={{ flex: 1, overflowY: 'auto', maxHeight: 480 }}>
            {selectedDraft ? (
              <InlineEditFormPlaceholder draft={selectedDraft} decision={decisions[selectedDraft.id]} onChange={(patch) => updateDecision(selectedDraft.id, patch)} onViewSession={() => setSessionModalSessionId(selectedDraft.source_session_id ?? null)} />
            ) : <Empty description="选择左侧草稿编辑" />}
          </div>
        </div>
      )}
    </Modal>
    {/* 子 Modal 叠层（D-9-5 溯源回链） */}
    <SessionMessagesModal open={!!sessionModalSessionId} sessionId={sessionModalSessionId} onClose={() => setSessionModalSessionId(null)} />
  </>
)
```

**InlineEditFormPlaceholder（同文件最简占位，Task 2a 阶段保证主壳独立编译；Task 2b 提取完整版到 ReviewConfirmEditForm.tsx 后由主壳 import 替换）：**
```tsx
// 最简占位：仅 title + content 两个受控 Input/TextArea，证明 decisions.fields 双向绑定链路通；完整 attrs 模板/分类切换/关联设备/UPDATE Checkbox 由 Task 2b 落地。
function InlineEditFormPlaceholder({ draft, decision, onChange, onViewSession }: {
  draft: Experience
  decision: { action: 'adopt' | 'discard'; supersedeOld: boolean; fields: ExperienceUpdateInput; relateDevices: string[] | undefined } | undefined
  onChange: (patch: Partial<{ action: 'adopt' | 'discard'; supersedeOld: boolean; fields: ExperienceUpdateInput; relateDevices: string[] | undefined }>) => void
  onViewSession: () => void
}) {
  if (!decision) return null
  return (
    <Form layout="vertical">
      <Form.Item label="标题">
        <Input value={decision.fields.title ?? ''} onChange={(e) => onChange({ fields: { title: e.target.value } as ExperienceUpdateInput })} />
      </Form.Item>
      <Form.Item label="内容">
        <Input.TextArea value={decision.fields.content ?? ''} rows={4} onChange={(e) => onChange({ fields: { content: e.target.value } as ExperienceUpdateInput })} />
      </Form.Item>
      <Button onClick={onViewSession}>查看原始会话</Button>
      <div style={{ marginTop: 8, fontSize: 12, color: '#999' }}>（完整编辑表单由 Task 2b ReviewConfirmEditForm 落地后接线）</div>
    </Form>
  )
}
```

**updateDecision / setAll helpers：**
```tsx
function updateDecision(id: string, patch: Partial<{ action: 'adopt' | 'discard'; supersedeOld: boolean; fields: ExperienceUpdateInput; relateDevices: string[] | undefined }>) {
  setDecisions((prev) => ({ ...prev, [id]: { ...prev[id], ...patch, fields: patch.fields ? { ...prev[id].fields, ...patch.fields } : prev[id].fields } }))
}
function setAll(action: 'adopt' | 'discard') {
  setDecisions((prev) => {
    const next = { ...prev }
    for (const id of Object.keys(next)) next[id] = { ...next[id], action }
    return next
  })
}
```

**handleSubmit（批量提交，调 confirmDrafts IPC，D-9-4）：**
```tsx
async function handleSubmit() {
  if (hasBlockingErrors) return
  setSubmitting(true)
  try {
    const input: ConfirmDraftsInput = {
      drafts: drafts.map((d) => {
        const dec = decisions[d.id]
        return {
          expId: d.id,
          action: dec.action,
          fields: dec.fields,
          relateDevices: dec.relateDevices,   // undefined 或 length>0 数组；service 层 length>0 才 diff
          supersedeOld: dec.supersedeOld,
        }
      }),
    }
    const result = await window.api.experience.confirmDrafts(input)
    message.success(`已采纳 ${result.adopted} 条，丢弃 ${result.discarded} 条${result.superseded > 0 ? `，标失效旧条目 ${result.superseded} 条` : ''}`)
    onSubmitted?.(result)
    onClose()
  } catch (e: unknown) {
    message.error(e instanceof Error ? e.message : String(e))
  } finally {
    setSubmitting(false)
  }
}
```

**import：** `import { Modal, Button, Tag, Checkbox, Spin, Empty, Form, Input, message } from 'antd'` + 类型 `import type { Experience, ExperienceUpdateInput, ConfirmDraftsInput, ConfirmDraftsResult } from '@/types/experience'` + `import { useState, useEffect } from 'react'` + `import SessionMessagesModal from './SessionMessagesModal'`
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在（test -f src/components/pages/ai/ReviewConfirmModal.tsx）
    - `grep -c "export default function ReviewConfirmModal" src/components/pages/ai/ReviewConfirmModal.tsx` 等于 1
    - `grep -c "export function validateDraft" src/components/pages/ai/ReviewConfirmModal.tsx` 等于 1（导出供 EditForm 共用）
    - `grep -c "interface ReviewConfirmModalProps" src/components/pages/ai/ReviewConfirmModal.tsx` 等于 1
    - `grep -c 'width="80vw"' src/components/pages/ai/ReviewConfirmModal.tsx` 等于 1（宽 Modal D-9-3）
    - `grep -c "window.api.experience.confirmDrafts" src/components/pages/ai/ReviewConfirmModal.tsx` 等于 1（批量提交）
    - `grep -c "window.api.experience.listDrafts" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 1（拉暂存 draft D-9-7）
    - `grep -c "hasBlockingErrors" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 2（确认按钮禁用 gate）
    - `grep -c "supersedeOld" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 2（D-9-2 默认 false + Checkbox 切换）
    - `grep -c "标失效旧条目" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 1（D-9-2 UI 文案）
    - `grep -c "SessionMessagesModal" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 1（子 Modal 叠层）
    - `grep -c "relateDevices: undefined" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 1（BLOCKER：默认 undefined 不动关联）
    - tsc 编译无错（占位 EditForm 自洽，主壳独立编译通过）
  </acceptance_criteria>
  <done>
    - 宽 Modal master-detail 主壳创建（左侧列表勾选 + 标红 + 底部批量提交 + 子 Modal 叠层）
    - validateDraft 导出（Task 2b EditForm 复用）
    - D-9-2 supersedeOld 默认 false + Checkbox 切换
    - relateDevices 初始化 undefined（与 service 层 length>0 守卫语义一致，不动现有关联）
    - 内联占位 EditForm 保证主壳独立编译，待 Task 2b 提取完整版接线
    - tsc 无错
  </done>
</task>

<task type="auto">
  <name>Task 2b: 新建 ReviewConfirmEditForm.tsx（右侧编辑表单子组件：字段表单 + attrs 模板动态字段 + 关联设备 Select + UPDATE 旧条目 supersedeOld Checkbox + 查看原始会话）并接线主壳</name>
  <files>src/components/pages/ai/ReviewConfirmEditForm.tsx, src/components/pages/ai/ReviewConfirmModal.tsx</files>
  <read_first>
    - src/components/pages/ai/ReviewConfirmModal.tsx（Task 2a 新建主壳——本任务把内联 InlineEditFormPlaceholder 替换为 <ReviewConfirmEditForm>，并复用其导出的 validateDraft）
    - src/types/experience.ts（Experience / ExperienceUpdateInput / ExperienceAttrs / ExperienceCategory DTO）
    - src/components/pages/ai/useAIChat.ts:88（window.api.device.list 设备拉取 + ssh/telnet 过滤范式，关联设备 Select 复用）
    - electron/services/experienceService.ts（service 层兜底质量门规则参照：troubleshooting severity/symptoms/resolution 强校验，轻结构 title/content 必填——renderer 第一层校验对齐）
    - .planning/phases/09-human-review-confirmation/09-CONTEXT.md（D-9-2 UPDATE 旧条目默认不勾标失效 + Claude's Discretion 分类切换 attrs 模板动态呈现）
  </read_first>
  <action>
**A. 新建 `src/components/pages/ai/ReviewConfirmEditForm.tsx`（独立编辑表单子组件）：**

**Props：**
```tsx
interface ReviewConfirmEditFormProps {
  draft: Experience
  decision: { action: 'adopt' | 'discard'; supersedeOld: boolean; fields: ExperienceUpdateInput; relateDevices: string[] | undefined }
  onChange: (patch: Partial<{ action: 'adopt' | 'discard'; supersedeOld: boolean; fields: ExperienceUpdateInput; relateDevices: string[] | undefined }>) => void
  onViewSession: () => void
}
```

**实现要点：**
- `import { Form, Input, Select, Button, Checkbox, Spin } from 'antd'` + `import { useEffect, useState } from 'react'` + 类型 `import type { Experience, ExperienceUpdateInput, ExperienceCategory } from '@/types/experience'` + `import { validateDraft } from './ReviewConfirmModal'`（复用主壳导出的质量门函数，避免双份漂移）
- 关联设备 Select 拉 `window.api.device.list()`（与 useAIChat.ts:88 一致），仅列 ssh/telnet 类型设备（沿用过滤）；`useEffect` 拉一次缓存到 state。
- `decision.fields` 受控：title（Input）/ category（Select 4 枚举 troubleshooting/best_practices/product/env）/ content（TextArea rows=6）/ tags（Select mode="tags"）/ 关联设备（Select mode="multiple"，value=decision.relateDevices ?? []，onChange 写入 decision.relateDevices——用户首次操作才填数组，触发 service 层 diff）。
- attrs 模板字段（category==='troubleshooting' 显 symptoms/root_cause/resolution/prevention TextArea + severity Select 5 枚举 critical/high/medium/low/info；其余轻结构隐藏 attrs 块）。category 切换时 attrs 字段动态显隐（D-9-3 + Claude's Discretion）。
- UPDATE 旧条目（draft.duplicate_of_exp_id 非空）显 Checkbox「标失效旧条目（默认不勾，主动勾才 supersede）」绑 decision.supersedeOld（D-9-2）。
- 「查看原始会话」Button 调 onViewSession。
- 字段 onChange 经 `onChange({ fields: { <field>: value } })` 增量回写主壳（主壳 updateDecision 已做 fields 浅合并）；关联设备 onChange 经 `onChange({ relateDevices: ids })` 回写。

**示例骨架：**
```tsx
export default function ReviewConfirmEditForm({ draft, decision, onChange, onViewSession }: ReviewConfirmEditFormProps) {
  const [devices, setDevices] = useState<{ id: string; name: string }[]>([])
  useEffect(() => {
    window.api.device.list().then((all: any[]) =>
      setDevices(all.filter((d) => d.type === 'ssh' || d.type === 'telnet').map((d) => ({ id: d.id, name: d.name })))
  }, [])
  const cat = decision.fields.category ?? draft.category
  return (
    <Form layout="vertical">
      <Form.Item label="标题"><Input value={decision.fields.title ?? ''} onChange={(e) => onChange({ fields: { title: e.target.value } as ExperienceUpdateInput })} /></Form.Item>
      <Form.Item label="分类">
        <Select value={cat} onChange={(v: ExperienceCategory) => onChange({ fields: { category: v } as ExperienceUpdateInput })} options={[
          { value: 'troubleshooting', label: '故障排查' }, { value: 'best_practices', label: '最佳实践' }, { value: 'product', label: '产品' }, { value: 'env', label: '环境' }
        ]} />
      </Form.Item>
      <Form.Item label="内容"><Input.TextArea value={decision.fields.content ?? ''} rows={6} onChange={(e) => onChange({ fields: { content: e.target.value } as ExperienceUpdateInput })} /></Form.Item>
      <Form.Item label="标签"><Select mode="tags" value={decision.fields.tags ?? []} onChange={(v: string[]) => onChange({ fields: { tags: v } as ExperienceUpdateInput })} /></Form.Item>
      {cat === 'troubleshooting' && (
        <>
          <Form.Item label="severity"><Select value={decision.fields.attrs?.severity} onChange={(v) => onChange({ fields: { attrs: { ...decision.fields.attrs, severity: v } } as ExperienceUpdateInput })} options={['critical','high','medium','low','info'].map((s) => ({ value: s, label: s }))} /></Form.Item>
          <Form.Item label="symptoms"><Input.TextArea value={decision.fields.attrs?.symptoms ?? ''} rows={3} onChange={(e) => onChange({ fields: { attrs: { ...decision.fields.attrs, symptoms: e.target.value } } as ExperienceUpdateInput })} /></Form.Item>
          <Form.Item label="root_cause"><Input.TextArea value={decision.fields.attrs?.root_cause ?? ''} rows={2} onChange={(e) => onChange({ fields: { attrs: { ...decision.fields.attrs, root_cause: e.target.value } } as ExperienceUpdateInput })} /></Form.Item>
          <Form.Item label="resolution"><Input.TextArea value={decision.fields.attrs?.resolution ?? ''} rows={3} onChange={(e) => onChange({ fields: { attrs: { ...decision.fields.attrs, resolution: e.target.value } } as ExperienceUpdateInput })} /></Form.Item>
          <Form.Item label="prevention"><Input.TextArea value={decision.fields.attrs?.prevention ?? ''} rows={2} onChange={(e) => onChange({ fields: { attrs: { ...decision.fields.attrs, prevention: e.target.value } } as ExperienceUpdateInput })} /></Form.Item>
        </>
      )}
      <Form.Item label="关联设备">
        <Select mode="multiple" value={decision.relateDevices ?? []} onChange={(v: string[]) => onChange({ relateDevices: v })} options={devices.map((d) => ({ value: d.id, label: d.name }))} placeholder="不改关联则留空" />
      </Form.Item>
      {draft.duplicate_of_exp_id && (
        <Form.Item><Checkbox checked={decision.supersedeOld} onChange={(e) => onChange({ supersedeOld: e.target.checked })}>标失效旧条目（默认不勾，主动勾才 supersede）</Checkbox></Form.Item>
      )}
      <Button onClick={onViewSession}>查看原始会话</Button>
    </Form>
  )
}
```

**B. 接线 `src/components/pages/ai/ReviewConfirmModal.tsx`（替换 Task 2a 占位为完整子组件）：**

1. 顶部 import 追加：`import ReviewConfirmEditForm from './ReviewConfirmEditForm'`
2. 主壳右侧占位块（Task 2a 的 `<InlineEditFormPlaceholder ... />`）替换为 `<ReviewConfirmEditForm draft={selectedDraft} decision={decisions[selectedDraft.id]} onChange={(patch) => updateDecision(selectedDraft.id, patch)} onViewSession={() => setSessionModalSessionId(selectedDraft.source_session_id ?? null)} />`
3. 删除 Task 2a 的 `InlineEditFormPlaceholder` 函数定义（已被独立文件替代）。
4. 保留主壳导出的 `validateDraft`（EditForm 复用，不删）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - 文件存在（test -f src/components/pages/ai/ReviewConfirmEditForm.tsx）
    - `grep -c "export default function ReviewConfirmEditForm" src/components/pages/ai/ReviewConfirmEditForm.tsx` 等于 1
    - `grep -c "interface ReviewConfirmEditFormProps" src/components/pages/ai/ReviewConfirmEditForm.tsx` 等于 1
    - `grep -c "import { validateDraft } from './ReviewConfirmModal'" src/components/pages/ai/ReviewConfirmEditForm.tsx` 等于 1（复用主壳质量门）
    - `grep -c "window.api.device.list" src/components/pages/ai/ReviewConfirmEditForm.tsx` 等于 1（关联设备拉取）
    - `grep -c "severity" src/components/pages/ai/ReviewConfirmEditForm.tsx` ≥ 1（troubleshooting attrs 模板）
    - `grep -c "标失效旧条目" src/components/pages/ai/ReviewConfirmEditForm.tsx` ≥ 1（D-9-2 UPDATE Checkbox）
    - 主壳接线后 `grep -c "InlineEditFormPlaceholder" src/components/pages/ai/ReviewConfirmModal.tsx` 等于 0（占位已删）
    - 主壳 `grep -c "ReviewConfirmEditForm" src/components/pages/ai/ReviewConfirmModal.tsx` ≥ 2（import + 使用）
    - tsc 编译无错（noUnusedLocals 全绿——占位删除后无残留引用）
  </acceptance_criteria>
  <done>
    - 独立编辑表单子组件落地（title/category/content/tags + attrs 模板动态字段 + 关联设备 Select + UPDATE supersedeOld Checkbox + 查看原始会话）
    - 复用主壳 validateDraft（无双份漂移）
    - 主壳占位替换为完整子组件，占位函数删除
    - 分类切换 attrs 字段动态显隐（D-9-3）
    - tsc 无错
  </done>
</task>

<task type="auto">
  <name>Task 3: 扩展 useAIChat.ts + AIPage.tsx + ChatInput.tsx 串联（handleSummarize 完成开弹窗 + 待确认 Badge 角标入口）</name>
  <files>src/components/pages/ai/useAIChat.ts, src/components/pages/AIPage.tsx, src/components/pages/ai/ChatInput.tsx</files>
  <read_first>
    - src/components/pages/ai/useAIChat.ts（被修改文件，handleSummarize lines 168-189 完成点 + return 块 lines 191-212 + 现有状态 lines 23-31）
    - src/components/pages/AIPage.tsx（被修改文件，Modal 挂载 line 99 + ChatInput 使用 lines 89-97）
    - src/components/pages/ai/ChatInput.tsx（被修改文件，「经验总结」Button lines 41-50 + Badge 包裹点）
    - src/components/pages/ai/ReviewConfirmModal.tsx（Task 2 新建，确认 Props 签名）
    - src/types/experience.ts（ConfirmDraftsResult / DraftingResult DTO）
    - .planning/phases/09-human-review-confirmation/09-CONTEXT.md（D-9-7 待确认角标 + 暂存重开）
    - .planning/phases/09-human-review-confirmation/09-PATTERNS.md（useAIChat handleSummarize 完成点 line 182 + AIPage Modal 挂载 line 99 + Badge 角标代码摘录）
  </read_first>
  <action>
**A. `src/components/pages/ai/useAIChat.ts` 修改：**

1. 顶部 import 追加类型 + 新弹窗 hook 状态：
```typescript
import type { ConfirmDraftsResult } from '@/types/experience'
// ... 现有 import
```

2. 状态区（line 31 `summarizing` 之后）追加：
```typescript
const [reviewOpen, setReviewOpen] = useState(false)
const [reviewInitialDraftIds, setReviewInitialDraftIds] = useState<string[]>([])
const [pendingDraftCount, setPendingDraftCount] = useState(0)
```

3. `handleSummarize`（lines 168-189）修改完成分支：在 `message.success(...)`（line 182）之后追加开弹窗 + 刷新角标计数：
```typescript
// 原 line 182: message.success(`经验总结完成：${parts.join('，')}（草稿待确认）`)
// 改为：
const draftIds = [...result.created, ...result.updated].map((x) => x.exp_id)
if (draftIds.length > 0) {
  message.success(`经验总结完成：${parts.join('，')}，请逐条确认`)
  setReviewInitialDraftIds(draftIds)
  setReviewOpen(true)
} else {
  message.success(`经验总结完成：${parts.join('，')}（无待确认草稿）`)
}
setPendingDraftCount(draftIds.length)
```
（注意：原本 noop 也算总结完成但不进弹窗，只有 created/updated 有 draft 才开弹窗。）

4. 追加 `handleReviewSubmitted` handler（弹窗提交后刷新角标 + 关闭弹窗 + 重新拉 listDrafts 计数）：
```typescript
const handleReviewSubmitted = useCallback(async (_result: ConfirmDraftsResult) => {
  // 弹窗已 onClose，这里刷新暂存 draft 计数（角标用）
  try {
    const remaining = await window.api.experience.listDrafts()
    setPendingDraftCount(remaining.length)
  } catch {
    setPendingDraftCount(0)
  }
}, [])
```

5. 追加 `openReviewFromBadge`（角标入口点击重开弹窗，D-9-7 暂存重开）：
```typescript
const openReviewFromBadge = useCallback(async () => {
  // 角标入口：拉所有暂存 draft，不限定 initialDraftIds（listDrafts 全量）
  setReviewInitialDraftIds([])
  setReviewOpen(true)
}, [])
```

6. return 块（lines 191-212）追加返回字段：
```typescript
return {
  // ... 现有字段
  reviewOpen,
  reviewInitialDraftIds,
  pendingDraftCount,
  setReviewOpen,
  handleReviewSubmitted,
  openReviewFromBadge,
}
```

7. `UseAIChatReturn` 类型（src/components/pages/ai/types.ts）追加对应字段（若该 interface 在 types.ts，executor 同步扩展；若 useAIChat 返回未显式类型，跳过）。

**B. `src/components/pages/AIPage.tsx` 修改：**

1. import 追加：
```typescript
import ReviewConfirmModal from './ai/ReviewConfirmModal'
```

2. line 99 `<CommandConfirmModal ... />` 之后追加挂载：
```tsx
<CommandConfirmModal pendingConfirm={chat.pendingConfirm} onConfirm={chat.handleConfirm} />
<ReviewConfirmModal
  open={chat.reviewOpen}
  onClose={() => chat.setReviewOpen(false)}
  initialDraftIds={chat.reviewInitialDraftIds}
  onSubmitted={chat.handleReviewSubmitted}
/>
```

3. ChatInput 调用（lines 89-97）追加角标入口 props：
```tsx
<ChatInput
  value={chat.input}
  loading={chat.loading}
  onChange={chat.setInput}
  onSend={chat.handleSend}
  summarizing={chat.summarizing}
  onSummarize={chat.handleSummarize}
  canSummarize={chat.canSummarize}
  pendingDraftCount={chat.pendingDraftCount}
  onOpenReview={chat.openReviewFromBadge}
/>
```

**C. `src/components/pages/ai/ChatInput.tsx` 修改：**

1. import 追加 Badge：
```typescript
import { Input, Button, Badge } from 'antd'
```

2. Props 接口追加：
```typescript
interface ChatInputProps {
  // ... 现有
  pendingDraftCount?: number       // D-9-7 待确认草稿角标
  onOpenReview?: () => void        // 角标点击重开确认弹窗
}
```

3. 函数签名解构追加 `pendingDraftCount, onOpenReview`。

4. 「经验总结」Button（lines 41-50）改为 Badge 包裹（角标 count = pendingDraftCount）：
```tsx
{onSummarize && (
  <Badge count={pendingDraftCount ?? 0} offset={[-4, 4]} color="#faad14">
    <Button
      icon={<ThunderboltOutlined />}
      onClick={onSummarize}
      loading={summarizing}
      disabled={!canSummarize || loading || summarizing}
    >
      经验总结
    </Button>
  </Badge>
)}
{onOpenReview && (pendingDraftCount ?? 0) > 0 && (
  <Button type="link" onClick={onOpenReview}>
    待确认 {pendingDraftCount} 条
  </Button>
)}
```
（角标入口：pendingDraftCount > 0 时显示「待确认 N 条」文字按钮，点击重开 ReviewConfirmModal 拉全量暂存 draft。Badge 包裹经验总结按钮做视觉提示。）
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | tail -15 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reviewOpen" src/components/pages/ai/useAIChat.ts` ≥ 3（state + set + return）
    - `grep -c "pendingDraftCount" src/components/pages/ai/useAIChat.ts` ≥ 3
    - `grep -c "openReviewFromBadge" src/components/pages/ai/useAIChat.ts` ≥ 2
    - `grep -c "handleReviewSubmitted" src/components/pages/ai/useAIChat.ts` ≥ 2
    - `grep -c "setReviewInitialDraftIds" src/components/pages/ai/useAIChat.ts` ≥ 2
    - useAIChat handleSummarize 完成分支调 setReviewOpen(true)（grep "setReviewOpen(true)" 命中 1 处）
    - `grep -c "ReviewConfirmModal" src/components/pages/AIPage.tsx` ≥ 2（import + 挂载）
    - `grep -c "pendingDraftCount" src/components/pages/AIPage.tsx` ≥ 1（透传 ChatInput）
    - `grep -c "onOpenReview" src/components/pages/AIPage.tsx` ≥ 1
    - `grep -c "Badge" src/components/pages/ai/ChatInput.tsx` ≥ 2（import + 使用）
    - `grep -c "待确认" src/components/pages/ai/ChatInput.tsx` ≥ 1（角标入口文案）
    - tsc 严格模式无错（noUnusedLocals 全绿）
    - vite build（npm run build）打包通过 exit 0
  </acceptance_criteria>
  <done>
    - handleSummarize 完成自动开 ReviewConfirmModal（带 created/updated exp_id 列表）
    - AIPage 挂载 ReviewConfirmModal
    - ChatInput 「经验总结」按钮 Badge 角标 + 「待确认 N 条」入口
    - 暂存 draft 经角标入口可重开弹窗（D-9-7）
    - tsc + vite build 双绿
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 9 全栈人工确认流：用户点「经验总结」→ AI 起草 draft（Phase 8）→ 自动弹 ReviewConfirmModal（宽 Modal master-detail，逐条编辑/勾选/质量门标红/查看原始会话/批量提交）→ 单事务原子确认（采纳→published + UPDATE 草稿可勾 supersede 旧条目 + 丢弃 hard DELETE）→ 关闭弹窗刷新「待确认 N 条」角标。
已自动化：service 单测全绿、3 secure IPC channel 注册、tsc + vite build + electron main 打包双绿、组件挂载完成。
  </what-built>
  <how-to-verify>
1. `npm run dev` 启动 Electron 应用，进入 AI 助手页。
2. 选/新建一个 AI 对话，与 AI 聊一段运维内容（含至少 1 个可总结的故障排查/最佳实践内容）。
3. 点「经验总结」按钮：
   - 期望：loading → 自动弹「经验草稿确认（N 条）」宽 Modal，左侧草稿列表，右侧编辑表单，draft 数与 Phase 8 起草一致。
4. 逐条编辑/勾选验证：
   - 右侧改 title/content，左侧标题同步更新。
   - troubleshooting 类 attrs 字段（symptoms/root_cause/resolution/prevention/severity）显隐正确，切其他类只剩轻结构。
   - 删除 troubleshooting 草稿的 severity/symptoms/resolution → 左侧该条标红「缺 severity / 缺 symptoms」，底部「确认采纳」按钮禁用。
   - 填回必填 → 标红消失，按钮恢复可点。
   - UPDATE 草稿（带 duplicate_of_exp_id）左侧显「UPDATE」Tag + 「标失效旧条目」Checkbox（默认不勾）。
5. 点「查看原始会话」：只读子 Modal 叠层展示该 source_session_id 的会话原文（滚动），关闭返回编辑。
6. 勾选若干条 adopt + 若干 discard + 一条 UPDATE 勾 supersedeOld，点底部「确认采纳 N 条 + 丢弃 M 条」：
   - 期望：成功 message「已采纳 X 条，丢弃 Y 条，标失效旧条目 Z 条」。
   - 弹窗关闭，「经验总结」按钮 Badge 角标更新（剩余暂存 draft 计数）。
7. DB 验证（可选）：`npm run dev` 后用 ELECTRON_RUN_AS_NODE 技巧查 experiences 表——确认的 draft status='published'，丢弃的不存在，UPDATE 勾 supersede 的旧条目 invalid_at 非 null。
8. 关闭应用重开，进入 AI 页：若有暂存 draft，「经验总结」按钮旁显「待确认 N 条」入口，点击重开 ReviewConfirmModal 拉全量暂存 draft（D-9-7）。
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues（如质量门标红未触发、批量提交非原子、角标计数不刷新等）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer ↔ window.api.experience.*（contextBridge 白名单） | renderer 经 preload 白名单调用 IPC，永不直接访问 main/masterKey |
| 用户输入 → confirmDrafts IPC | 用户编辑的 fields/title/content 跨边界入 main，renderer 第一层校验 + service 兜底校验 |
| 会话明文回链（getSessionMessages → renderer） | design D-04 明文回链是设计意图（用户核对自己对话），单机 safeStorage 绑机器 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering | ReviewConfirmModal 用户编辑 fields | mitigate | renderer validateDraft 第一层校验（标红+禁用按钮）+ IPC MAX_BATCH + service 层 confirmDrafts 内兜底质量门（severity/symptoms/resolution 强校验 throw），三层纵深防御 |
| T-09-10 | Information Disclosure | SessionMessagesModal 展示会话明文 | accept | design D-04 明确用户核对自己对话的明文回链是设计意图，单机 safeStorage 绑机器无跨机泄露面；secure IPC 鉴权确保仅登录后特权用户可调 |
| T-09-11 | Elevation of Privilege | renderer 绕过质量门直接调 confirmDrafts | mitigate | renderer 校验仅 UX 层，service 层 confirmDrafts 内独立兜底校验 throw（不信任 renderer 入参），CR-01 status 改变只走 confirmDrafts 专用接口（不复活 update 白名单） |
| T-09-12 | Repudiation | 用户确认/丢弃操作无审计 | accept | 单事务原子 updated_at 自动落时间；运维单机场景无多用户审计需求；service 层 adopted/discarded/superseded 计数返回可日志（design §6） |
| T-09-13 | Denial of Service | 弹窗拉超长会话原文卡死 | mitigate | SessionMessagesModal 外层 maxHeight + overflowY:'auto' 滚动容器 + 单条 maxHeight 60px 折叠，超长会话不卡死 UI |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（严格模式 + noUnusedLocals）
- `npm run build` exit 0（vite renderer 打包）
- `npm run build:electron-main` exit 0（main/preload 打包）
- `npx vitest run` 全绿（service + 既有测试不回归）
- 人工 checkpoint：弹窗逐条编辑/质量门标红/查看会话/批量提交/角标入口全链路验证
</verification>

<success_criteria>
- REVIEW-01：弹窗逐条编辑（标题/分类/内容/attrs 模板字段/标签/关联设备）+ 勾选采纳/丢弃 ✅
- REVIEW-02：缺必填项标红 + 确认按钮禁用（质量门 renderer 第一层）+ service 层兜底 ✅
- REVIEW-03：每条可一键回链原始会话（SessionMessagesModal 叠层）✅
- SC1：AI 起草后弹窗逐条展示草稿 + 编辑 + 勾选 ✅
- SC2：缺必填标红阻止确认 ✅
- SC3：一键回链原始会话 ✅
- SC4：确认后 draft→published + 丢弃不留库 + UPDATE 草稿按勾选 supersede ✅
  - **SC4 语义澄清（防 verify 误判）**：SC4「UPDATE 草稿确认后落为对存量条目的更新」实际语义 = 采纳 UPDATE 草稿 = 发布一行新 published（draft→published）+ 旧条目命运由 D-9-2 逐条人工拍板（默认保留 invalid_at 仍 null，用户主动勾选「标失效旧条目」才 supersede → 旧条目 invalid_at 落时间）。**不是「替换/覆盖旧条目」**——旧行始终保留（除非勾选 supersede 才软失效），新行是新 published。verify 时按此语义判定 SC4 达成。
- D-9-2：UPDATE 默认不勾标失效旧条目 ✅
- D-9-7：待确认角标入口暂存重开 ✅
- 红线③：人工是 session→permanent 唯一闸口（不自动确认）✅
</success_criteria>

<output>
After completion, create `.planning/phases/09-human-review-confirmation/09-03-SUMMARY.md`
</output>
