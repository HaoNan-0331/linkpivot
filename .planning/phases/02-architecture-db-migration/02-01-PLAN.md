---
phase: 02-architecture-db-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/migrationHelpers.ts
  - electron/database/migrations.ts
  - electron/database/init.ts
autonomous: true
requirements: [ARCH-01]
tags: [migration, user_version, hasColumn, db-safety]

must_haves:
  truths:
    - "PRAGMA user_version 在迁移流程中被读（current = db.pragma('user_version')[0]）和写（每个版本步骤事务内 db.pragma('user_version = N')）"
    - "init.ts 中 4 处散落的 PRAGMA table_info(...).some(c => c.name === ...) 检查收敛为 hasColumn(db, table, col) 调用（grep 命中点下降）"
    - "每个版本步骤是单个 db.transaction，DDL 与 user_version 在同一事务提交时一起生效，失败自动回滚（DB 停留前版本）"
    - "遗留库 user_version=0 启动时重跑全部 pending 步骤，已是当前 schema 的为 no-op，最终抵达 head 版本"
    - "devices 表重建步骤保留 sqlite_master sql-content 守卫 + foreign_key_check 断言（不回归）"
    - "迁移步骤失败抛出含 stepName + 失败 SQL 片段 + 错误信息的异常并写 system log，启动中止（不静默恢复）"
  artifacts:
    - path: "electron/database/migrationHelpers.ts"
      provides: "hasColumn(db, table, col) 集中式 helper"
      contains: "export function hasColumn"
    - path: "electron/database/migrations.ts"
      provides: "版本→步骤注册表 + runMigrations 入口 + HEAD 常量"
      contains: "export const MIGRATION_HEAD"
    - path: "electron/database/init.ts"
      provides: "createTables 末尾调用 runMigrations()，原 273-356 散落块移入注册表"
      contains: "runMigrations"
  key_links:
    - from: "electron/database/init.ts (createTables 末尾)"
      to: "electron/database/migrations.ts runMigrations"
      via: "import { runMigrations } from './migrations'"
      pattern: "runMigrations\\(\\)"
    - from: "electron/database/migrations.ts 各步骤"
      to: "migrationHelpers hasColumn"
      via: "import { hasColumn } from './migrationHelpers'"
      pattern: "hasColumn\\(db,"
---

<objective>
引入 PRAGMA user_version 版本管理 + hasColumn helper（ARCH-01），把 init.ts:273-356 散落的 4 处 PRAGMA table_info 幂等检查 + devices 表事务化重建重构为版本化迁移注册表。每个版本步骤原子（DDL + 版本号同事务），失败中止不丢数据，遗留库 user_version=0 重跑全部 pending 自我校验抵达 head。

Purpose: 让迁移可追踪可跳过（SC#1 + SC#4），为 Phase 3 PERF-04「按 user_version 跳过已完成 init」提供版本号机制基础。
Output: migrationHelpers.ts（hasColumn）、migrations.ts（注册表 + runMigrations）、重构后的 init.ts。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-db-migration/02-CONTEXT.md
@.planning/phases/02-architecture-db-migration/02-PATTERNS.md
@.planning/phases/01-build-dependency-foundation/01-01-SUMMARY.md

<interfaces>
<!-- 现有契约，executor 直接使用，无需探查代码库 -->

From electron/database/connection.ts:
```typescript
import Database from 'better-sqlite3'
export function getDatabase(): Database.Database  // 返回已初始化的 db 单例
export function initDatabase(): Database.Database
export function closeDatabase()
```

From electron/database/init.ts (createTables 函数，迁移块 273-356 在末尾):
```typescript
import { getDatabase } from './connection'
export function createTables(): void  // CREATE TABLE IF NOT EXISTS ... 末尾接迁移块（本 plan 重构对象）
```
createTables 当前末尾（init.ts:273-356）依次执行：
1. chat_history.session_id 列检查 + ALTER（275-278）
2. ai_exec_logs.prompt_text / .ai_response 列检查 + ALTER（281-287）
3. devices.status / .last_checked 列检查 + ALTER（290-296）
4. ai_config.vision_base_url_enc / .vision_api_key_enc / .vision_model_enc 列检查 + ALTER（299-308）
5. devices 表 CHECK 重建（'rdp'）：sqlite_master sql-content 守卫 + db.transaction DROP/CREATE/INSERT/RENAME + foreign_key_check（312-356）

From electron/services/systemLog.ts (非致命/致命事件日志):
```typescript
export function createSystemLog(log: {
  type: string; status: string; deviceIds?: string; deviceNames?: string;
  promptText?: string; aiResponse?: string; parsedResult?: string; errorMessage?: string;
}): string
// 字段超 16000 字符自动截断；写入 ai_system_logs 表
```

better-sqlite3 事务语义（D-07/D-08 关键）：
- `const fn = db.transaction(() => { ... })` 返回函数；`fn()` 执行，**throw 时自动 ROLLBACK**（better-sqlite3 内建，无需 try/catch 回滚）
- `db.pragma('user_version = N')` 在事务内调用 → 与同事务 DDL 一起提交/回滚（PRAGMA user_version 是事务性的）
- `db.pragma('user_version') as any` → 返回 `[{ user_version: number }]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 migrationHelpers.ts（hasColumn 集中 helper）</name>
  <files>electron/database/migrationHelpers.ts</files>

  <read_first>
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md（D-09 hasColumn 要求）
    - .planning/phases/02-architecture-db-migration/02-PATTERNS.md §migrationHelpers.ts（exact analog：init.ts:275-308 当前散落模式 + 目标签名）
    - electron/database/connection.ts（getDatabase 签名，第 7-10 行）
    - electron/database/init.ts（第 273-308 行，4 处被替换的散落 table_info 检查）
  </read_first>

  <behavior>
    - hasColumn(db, 'chat_history', 'session_id') 在表含该列时返回 true
    - hasColumn(db, 'chat_history', 'nonexistent') 返回 false
    - hasColumn 对空表/新表仍正确（PRAGMA table_info 返回列定义而非行数据）
    - 传入 db 参数（不在 helper 内部调 getDatabase）以便在事务作用域内组合使用
  </behavior>

  <action>
创建 `electron/database/migrationHelpers.ts`，内容：

```typescript
import type Database from 'better-sqlite3'

/**
 * 集中式列存在检查（D-09 / ARCH-01）。
 * 替代散落的 db.prepare("PRAGMA table_info(X)").all().some(c => c.name === Y) 模式。
 * 传入 db（不在内部调 getDatabase）以保持可测试 + 可在事务作用域内组合。
 */
export function hasColumn(db: Database.Database, table: string, col: string): boolean {
  const cols = db.prepare(`PRAGMA table_info(${table})`).all() as Array<{ name: string }>
  return cols.some((c) => c.name === col)
}
```

注意事项：
- `import type Database from 'better-sqlite3'`（type-only import，避免运行时循环依赖；connection.ts 已运行时 import better-sqlite3）
- 表名直接插值 `PRAGMA table_info(${table})` 是安全的——本 helper 仅由受控迁移代码调用（表名为代码内常量 chat_history/ai_exec_logs/devices/ai_config），非用户输入，无 SQL 注入面（与 init.ts:275 现有写法一致）
- 不在此文件内调 getDatabase（per PATTERNS analog note：保持可测试 + 事务作用域内组合）
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | grep -i "migrationHelpers" ; test ${PIPESTATUS[0]:-$?} -eq 0 || true ; test -f electron/database/migrationHelpers.ts && grep -q "export function hasColumn" electron/database/migrationHelpers.ts</automated>
  </verify>

  <acceptance_criteria>
    - `electron/database/migrationHelpers.ts` 文件存在
    - `grep -q "export function hasColumn(db: Database.Database, table: string, col: string): boolean" electron/database/migrationHelpers.ts` 命中
    - `grep -q "import type Database from 'better-sqlite3'" electron/database/migrationHelpers.ts` 命中（type-only import，无运行时循环依赖）
    - `grep -c "getDatabase" electron/database/migrationHelpers.ts` = 0（不在 helper 内部调 getDatabase）
    - `npx tsc -p tsconfig.web.json --noEmit` 退出码 0（严格模式 + noUnusedLocals 全绿，无新增 type error）
  </acceptance_criteria>

  <done>migrationHelpers.ts 导出 hasColumn(db, table, col)，签名与 PATTERNS analog 一致；tsc 全绿；helper 内部不调 getDatabase。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 migrations.ts（版本→步骤注册表 + runMigrations 入口）并重构 init.ts</name>
  <files>electron/database/migrations.ts, electron/database/init.ts</files>

  <read_first>
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md（D-06/D-07/D-08/D-14/D-15/D-16 全部迁移决策）
    - .planning/phases/02-architecture-db-migration/02-PATTERNS.md §migrations.ts + §init.ts + §Shared Patterns「Atomic transactional migration step」
    - electron/database/init.ts（完整第 1-30 行 import + createTables 结构；第 273-356 行待重构迁移块——逐行复制 SQL 到注册表步骤，勿改 SQL 文本）
    - electron/database/connection.ts（getDatabase 签名）
    - electron/services/systemLog.ts（createSystemLog 签名，第 17-43 行，用于 D-08 失败日志）
  </read_first>

  <action>
**步骤 A：创建 `electron/database/migrations.ts`**

```typescript
import type Database from 'better-sqlite3'
import { getDatabase } from './connection'
import { hasColumn } from './migrationHelpers'
import { createSystemLog } from '../services/systemLog'

/**
 * 版本→步骤注册表（D-16）。
 * 顺序整数 user_version：1, 2, 3, ...。每版本 = 一个原子迁移步骤（D-07）。
 * 遗留库 user_version=0 启动时重跑全部 pending 步骤（D-14/D-15），已是当前 schema 的步骤为 no-op。
 * MIGRATION_HEAD 随新增迁移递增。
 */
export const MIGRATION_HEAD = 5

interface MigrationStep {
  version: number
  name: string
  run: (db: Database.Database) => void
}

// 每个步骤 = 单个 db.transaction(() => { DDL...; db.pragma('user_version = N') })
// DDL 用 hasColumn / sqlite_master sql-content 守卫保证幂等重跑（D-14）。
// better-sqlite3 transaction 在 throw 时自动 ROLLBACK（D-08）。
const v1 = (db: Database.Database): void => {
  const step = db.transaction(() => {
    if (!hasColumn(db, 'chat_history', 'session_id')) {
      db.exec('ALTER TABLE chat_history ADD COLUMN session_id TEXT')
    }
    db.pragma('user_version = 1')
  })
  step()
}

const v2 = (db: Database.Database): void => {
  const step = db.transaction(() => {
    if (!hasColumn(db, 'ai_exec_logs', 'prompt_text')) {
      db.exec("ALTER TABLE ai_exec_logs ADD COLUMN prompt_text TEXT DEFAULT ''")
    }
    if (!hasColumn(db, 'ai_exec_logs', 'ai_response')) {
      db.exec("ALTER TABLE ai_exec_logs ADD COLUMN ai_response TEXT DEFAULT ''")
    }
    db.pragma('user_version = 2')
  })
  step()
}

const v3 = (db: Database.Database): void => {
  const step = db.transaction(() => {
    if (!hasColumn(db, 'devices', 'status')) {
      db.exec("ALTER TABLE devices ADD COLUMN status TEXT DEFAULT 'unknown' CHECK(status IN ('online','offline','unknown'))")
    }
    if (!hasColumn(db, 'devices', 'last_checked')) {
      db.exec('ALTER TABLE devices ADD COLUMN last_checked TEXT')
    }
    db.pragma('user_version = 3')
  })
  step()
}

const v4 = (db: Database.Database): void => {
  const step = db.transaction(() => {
    if (!hasColumn(db, 'ai_config', 'vision_base_url_enc')) {
      db.exec('ALTER TABLE ai_config ADD COLUMN vision_base_url_enc TEXT')
    }
    if (!hasColumn(db, 'ai_config', 'vision_api_key_enc')) {
      db.exec('ALTER TABLE ai_config ADD COLUMN vision_api_key_enc TEXT')
    }
    if (!hasColumn(db, 'ai_config', 'vision_model_enc')) {
      db.exec('ALTER TABLE ai_config ADD COLUMN vision_model_enc TEXT')
    }
    db.pragma('user_version = 4')
  })
  step()
}

const v5 = (db: Database.Database): void => {
  // devices 表 CHECK 重建（'rdp'）——保留 sqlite_master sql-content 守卫 + foreign_key_check 断言
  // （init.ts:312-356 既有模式，D-14 第二形式幂等守卫）
  const connTypeCheck = (db.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name='devices'").get() as { sql?: string } | undefined)?.sql || ''
  if (connTypeCheck.includes("'rdp'")) {
    return // 已含 rdp，no-op（幂等重跑 D-14）
  }
  const step = db.transaction(() => {
    db.exec("DROP TABLE IF EXISTS devices_new")
    db.exec(`
      CREATE TABLE devices_new (
        id TEXT PRIMARY KEY,
        topology_id TEXT,
        name_enc TEXT NOT NULL,
        vendor_enc TEXT,
        model_enc TEXT,
        version_enc TEXT,
        ip_enc TEXT,
        device_type TEXT DEFAULT 'generic' CHECK(device_type IN ('router','switch','firewall','server','generic')),
        connection_type TEXT CHECK(connection_type IN ('ssh','telnet','web','rdp')),
        port_enc TEXT,
        username_enc TEXT,
        password_enc TEXT,
        ssh_key_path_enc TEXT,
        ssh_key_content_enc TEXT,
        web_url_enc TEXT,
        status TEXT DEFAULT 'unknown',
        last_checked TEXT,
        created_at TEXT DEFAULT (datetime('now','localtime')),
        updated_at TEXT DEFAULT (datetime('now','localtime')),
        FOREIGN KEY (topology_id) REFERENCES topologies(id) ON DELETE SET NULL
      );
      INSERT INTO devices_new
        SELECT id, topology_id, name_enc, vendor_enc, model_enc, version_enc, ip_enc,
          COALESCE(device_type, 'generic'), connection_type, port_enc, username_enc,
          password_enc, ssh_key_path_enc, ssh_key_content_enc, web_url_enc,
          COALESCE(status, 'unknown'), last_checked, created_at, updated_at
        FROM devices;
      DROP TABLE devices;
      ALTER TABLE devices_new RENAME TO devices;
    `)
    const fkErrors = db.pragma('foreign_key_check') as unknown[]
    if (fkErrors.length > 0) {
      throw new Error('devices 重建后外键完整性校验失败: ' + JSON.stringify(fkErrors))
    }
    db.pragma('user_version = 5')
  })
  step()
}

const MIGRATIONS: MigrationStep[] = [
  { version: 1, name: 'chat_history.session_id', run: v1 },
  { version: 2, name: 'ai_exec_logs.prompt_text+ai_response', run: v2 },
  { version: 3, name: 'devices.status+last_checked', run: v3 },
  { version: 4, name: 'ai_config.vision_*', run: v4 },
  { version: 5, name: 'devices.connection_type CHECK rdp rebuild', run: v5 },
]

/**
 * 运行迁移（D-14/D-16）。从 current+1 到 MIGRATION_HEAD 顺序执行。
 * 遗留库 user_version=0 → 重跑全部步骤，已是当前 schema 的 no-op（hasColumn/sqlite_master 守卫），抵达 head。
 * 每步骤失败 → better-sqlite3 自动回滚（DB 停留前版本）→ 写 system log → 抛出（启动中止，D-08）。
 *
 * 注意：D-06「迁移前备份」在 runMigrations 之外由 connection.ts 调度（本 plan 仅迁移本身）。
 */
export function runMigrations(): void {
  const db = getDatabase()
  const currentRow = db.pragma('user_version') as Array<{ user_version: number }>
  const current = currentRow[0]?.user_version ?? 0

  if (current >= MIGRATION_HEAD) return

  for (const step of MIGRATIONS) {
    if (step.version <= current) continue
    try {
      step.run(db)
    } catch (err) {
      const msg = (err as Error).message
      // D-08：写 system log（try/catch 包裹，不让日志失败掩盖迁移失败）
      try {
        createSystemLog({
          type: 'migration',
          status: 'failed',
          errorMessage: `迁移步骤 v${step.version} (${step.name}) 失败: ${msg}`,
        })
      } catch { /* 日志失败不影响抛出 */ }
      // D-08：抛出清晰错误，启动中止。不自动恢复（指明备份由调用方/人工处理）。
      throw new Error(
        `数据库迁移失败：步骤 v${step.version} (${step.name}) — ${msg}。` +
        `数据库已回滚至 v${step.version - 1}。请从 userData/backups/ 下的 premigration 备份人工恢复后重试。`
      )
    }
  }
}
```

**步骤 B：重构 `electron/database/init.ts`**

1. 顶部 import 块新增：`import { runMigrations } from './migrations'`（紧跟现有 `import { getDatabase } from './connection'` 之后）
2. **删除** init.ts 第 273-356 行的整个散落迁移块（从 `// Migrate: add session_id column...` 注释起到 `rebuildDevices()` 调用 + 其后的空行），替换为单行调用：
   ```typescript
   // 迁移：版本化注册表（user_version + hasColumn），见 migrations.ts（ARCH-01 / D-09）
   runMigrations()
   ```
3. 保留 `initDefaultOUIData(db)`（原第 358-359 行）不变，紧跟 `runMigrations()` 之后
4. **不要改动** createTables 中的任何 CREATE TABLE IF NOT EXISTS 语句（只动迁移块）

关键约束：
- v1-v5 的 SQL 文本逐字从 init.ts:273-356 复制（包括 DEFAULT / CHECK / COALESCE），勿改语义
- v5 的 devices 重建 SQL 整段（CREATE TABLE devices_new / INSERT / DROP / RENAME）与 init.ts:318-348 完全一致，仅缩进进 transaction
- `db` 在 init.ts 中本就由 `const db = getDatabase()` 取得（第 274 行），runMigrations 内部自己取 db，无需传参
- devices 重建的 foreign_key_check 断言必须保留（D-14 既有安全模式）
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "TSC_GREEN" && npm run build:electron-main 2>&1 | tail -3 && echo "ESBUILD_GREEN"</automated>
  </verify>

  <acceptance_criteria>
    - `electron/database/migrations.ts` 存在
    - `grep -q "export const MIGRATION_HEAD = 5" electron/database/migrations.ts` 命中
    - `grep -q "export function runMigrations" electron/database/migrations.ts` 命中
    - `grep -c "db.pragma('user_version = " electron/database/migrations.ts` = 5（每个版本步骤事务内写版本号）
    - `grep -q "import { hasColumn } from './migrationHelpers'" electron/database/migrations.ts` 命中
    - `grep -q "import { createSystemLog } from '../services/systemLog'" electron/database/migrations.ts` 命中（D-08 失败日志）
    - `grep -c "foreign_key_check" electron/database/migrations.ts` >= 1（devices 重建断言保留）
    - `grep -n "runMigrations" electron/database/init.ts` 命中（import + 调用）
    - **散落 table_info 收敛验证**：`grep -c "PRAGMA table_info" electron/database/init.ts` 从原 4 降为 0（全部移入 migrations.ts via hasColumn）—— 注意 init.ts:312 的 `sqlite_master` 检查不是 table_info，不计入
    - `grep -c "PRAGMA table_info" electron/database/migrations.ts` = 0（迁移注册表内部统一走 hasColumn，不直接写 PRAGMA table_info）
    - `grep -c "session_id TEXT" electron/database/init.ts` = 0（chat_history 列迁移已移出，仅在 CREATE TABLE 定义内若存在则保留——实际 chat_history 建表已含 session_id 则此处 ALTER 已无；以 migrations.ts 内有一份为准）
    - `npx tsc -p tsconfig.web.json --noEmit` 退出码 0（严格模式 + noUnusedLocals 全绿，无新增 type error）
    - `npm run build:electron-main` 退出码 0（esbuild electron main 打包双绿，Phase 1 基线不破坏）
    - `npm run build:electron` 退出码 0（main + preload 全链路）
  </acceptance_criteria>

  <done>migrations.ts 提供 MIGRATION_HEAD=5 + runMigrations（5 个原子版本步骤，每步 DDL+version 同事务，失败回滚+system log+中止）；init.ts 迁移块替换为 runMigrations() 调用，4 处散落 table_info 收敛为 hasColumn；tsc + esbuild 双绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app 启动 → DB 迁移 | 用户控制的 DB 文件（含历史/遗留库）在启动时被迁移代码处理；错误 SQL 或半迁移态可能损坏数据 |
| 迁移失败 → 启动中止 | 失败时必须保证 DB 一致（不丢数据）且不静默掩盖问题 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Tampering | 迁移步骤中途失败留半改 schema（DDL 改了一半但 user_version 没动） | mitigate | D-07：每步骤 db.transaction 包裹 DDL + db.pragma('user_version = N')，better-sqlite3 throw 自动 ROLLBACK，版本号仅在 commit 成功推进 |
| T-2-02 | Denial of Service | 迁移失败后应用崩溃无法启动，用户数据锁死 | mitigate | D-08：失败写 system log（type='migration', status='failed'）+ 抛出清晰错误指明 userData/backups/ premigration 备份；DB 停留前一可用版本，不锁死 |
| T-2-03 | Information Disclosure | 迁移失败错误信息泄露路径/SQL 细节 | accept | 错误信息含 stepName + SQL 片段供运维诊断；本应用为本地桌面工具（无远端攻击面），且错误仅写入本地 system log + 控制台，收益（可诊断性）> 风险 |
| T-2-04 | Repudiation | 迁移执行/失败无审计痕迹 | mitigate | createSystemLog 记录失败事件（含版本号 + stepName + 错误信息）；system log 表已有 createdAt 时间戳 |
| T-2-05 | Tampering | 遗留库曾漏迁移（部分态），盲目 stamp 版本号会永久留下不一致 | mitigate | D-14/D-15：选幂等重跑而非 stamp——遗留库 user_version=0 重跑全部 pending，hasColumn/sqlite_master 守卫保证已是当前 schema 的步骤为 no-op，每库自我校验抵达 head |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 退出码 0（严格 + noUnusedLocals 全绿）
- `npm run build:electron-main` 退出码 0
- `npm run build:electron` 退出码 0
- `codegraph_search hasColumn` 命中 migrationHelpers.ts（迁移后索引刷新）
- `grep -c "PRAGMA table_info" electron/database/init.ts` = 0（SC#1 收敛验证）
- `grep -rn "user_version" electron/database/migrations.ts` 同时含读（`db.pragma('user_version')`）和写（`db.pragma('user_version = N')`）（SC#1）
- 手动回归：现有拓扑/设备/AI 日志数据迁移后字段完整（chat_history.session_id、ai_exec_logs.prompt_text/ai_response、devices.status/last_checked、ai_config.vision_*、devices.connection_type 含 rdp）
</verification>

<success_criteria>
- SC#1 部分达成（hasColumn 收敛 + user_version 读写）—— ACL/backup 部分（SC#2/SC#3）由 Plan 02/03 完成
- SC#4 达成基础（遗留库 user_version=0 重跑抵达 head 的路径就绪）—— 端到端向后兼容验证在 Plan 03 接入 connection.ts 后完成
- Phase 1 构建基线不破坏（tsc + esbuild 双绿）
- ARCH-01 user_version + hasColumn 替代散落 table_info 落地
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-db-migration/02-01-SUMMARY.md`
</output>
