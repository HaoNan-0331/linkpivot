---
phase: 02-architecture-db-migration
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - electron/services/backupScheduler.ts
  - electron/database/connection.ts
  - electron/database/init.ts
  - electron/main.ts
autonomous: true
requirements: [ARCH-02]
tags: [backup-scheduler, db-backup, premigration-backup, acl-integration, lifecycle]

must_haves:
  truths:
    - "BackupScheduler 静态类镜像 SchedulerService 结构（start/stop/restart + setInterval + shouldRunNow 启动补跑 + id=1 singleton 配置行 + runTask/executeTask 拆分 + isRunning 在 executeTask 的 finally 重置），默认 24h 周期备份"
    - "BackupScheduler.runTask 调用独立 executeTask()（备份工作隔离其中，isRunning 在 executeTask finally 重置——与 SchedulerService 一致，不静默丢弃并发备份）"
    - "BackupScheduler.executeTask 调用 db.backup(path) 生成周期桶备份（topology-periodic-YYYYMMDD-HHmmss.db.bak），按 periodicRetention=7 FIFO 裁剪"
    - "每个备份文件创建后立即 restrictFilePermissions 收紧 ACL（D-12b）"
    - "迁移前备份（premigration）在 createTables() 之后、runMigrations() 之前生成（topology-premigration-v{from}-to-v{to}-YYYYMMDD-HHmmss.db.bak），捕获 post-基线表 schema+数据；fresh-install 空库跳过（gated on 表非空）"
    - "connection.ts initDatabase 流程：连接 + pragma + ensureBackupsDir + 收紧历史 backups 目录 ACL（D-12a 幂等）；migrateAndSecure() 流程：premigration 备份（gated）→ runMigrations → 收紧活跃 db/wal/shm ACL（D-12a 幂等）"
    - "main.ts app ready 注册 initDatabase() → createTables() → migrateAndSecure() → SchedulerService.start() → BackupScheduler.start()；before-quit 调用 BackupScheduler.stop()（closeDatabase 之前）"
    - "旧库打开后 user_version 自动迁移到 head 且历史数据无丢失（SC#4 向后兼容）"
    - "tsc + esbuild 双绿，Phase 1 构建基线不破坏"
  artifacts:
    - path: "electron/services/backupScheduler.ts"
      provides: "BackupScheduler 静态类（周期备份 + 双桶轮换 + premigration 备份入口 + executeTask 拆分镜像 SchedulerService）"
      contains: "export class BackupScheduler"
    - path: "electron/database/connection.ts"
      provides: "migrateAndSecure（premigration 备份 + runMigrations + ACL 收紧活跃文件）+ initDatabase（连接 + ensureBackupsDir + 历史备份 ACL）"
      contains: "migrateAndSecure"
    - path: "electron/database/init.ts"
      provides: "createTables 删除散落迁移块 273-356（已迁入 Plan 01 migrations.ts）+ 新建 backup_config 表；runMigrations 不在 init.ts 调用（单一调用点 = connection.ts migrateAndSecure）"
      contains: "backup_config"
    - path: "electron/main.ts"
      provides: "app ready 注册 BackupScheduler.start() + migrateAndSecure()；before-quit BackupScheduler.stop()"
      contains: "BackupScheduler"
  key_links:
    - from: "electron/main.ts app.whenReady"
      to: "electron/services/backupScheduler.ts BackupScheduler.start"
      via: "SchedulerService.start() 之后 BackupScheduler.start()"
      pattern: "BackupScheduler\\.start\\(\\)"
    - from: "electron/main.ts app.whenReady"
      to: "electron/database/connection.ts migrateAndSecure"
      via: "createTables() 之后 migrateAndSecure()"
      pattern: "migrateAndSecure\\(\\)"
    - from: "electron/main.ts before-quit"
      to: "electron/services/backupScheduler.ts BackupScheduler.stop"
      via: "BackupScheduler.stop() 先于 closeDatabase()"
      pattern: "BackupScheduler\\.stop\\(\\)"
    - from: "electron/database/connection.ts migrateAndSecure"
      to: "electron/database/migrations.ts runMigrations"
      via: "import { runMigrations } from './migrations'"
      pattern: "runMigrations\\(\\)"
    - from: "electron/database/connection.ts migrateAndSecure"
      to: "electron/services/backupScheduler.ts BackupScheduler.createPremigrationBackup"
      via: "createTables 之后、runMigrations 之前调用（D-06）"
      pattern: "createPremigrationBackup\\("
    - from: "electron/services/backupScheduler.ts executeTask"
      to: "better-sqlite3 db.backup(path)"
      via: "getDatabase().backup(backupPath)"
      pattern: "\\.backup\\("
    - from: "electron/services/backupScheduler.ts executeTask"
      to: "electron/database/acl.ts restrictFilePermissions"
      via: "每备份创建后立即收紧 ACL（D-12b）"
      pattern: "restrictFilePermissions\\("
---

<objective>
集成层（ARCH-02 完整闭环）：(1) `BackupScheduler` 静态类镜像 SchedulerService（含 runTask/executeTask 拆分），定时 `.backup()` + 双桶轮换；(2) `connection.ts` 提供 migrateAndSecure()：createTables() 之后做 premigration 备份（gated on 非空库）→ runMigrations（Plan 01）→ ACL 收紧活跃文件；(3) `init.ts` 删除散落迁移块 + 新建 backup_config 表（init.ts 单一编辑权归属本 plan）；(4) `main.ts` 注册 migrateAndSecure + BackupScheduler 生命周期。

Purpose: 达成 SC#2（ACL 收紧）、SC#3（定时 backup 注册+生成）、SC#4（旧库自动迁移无丢失，端到端），与 Plan 01 的 SC#1 共同闭合 ARCH-01/ARCH-02。
Output: backupScheduler.ts + 改造后的 connection.ts + init.ts + main.ts。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-architecture-db-migration/02-CONTEXT.md
@.planning/phases/02-architecture-db-migration/02-PATTERNS.md
@.planning/phases/02-architecture-db-migration/02-01-SUMMARY.md
@.planning/phases/02-architecture-db-migration/02-02-SUMMARY.md

<interfaces>
<!-- Plan 01 产出契约 -->

From electron/database/migrations.ts (Plan 01):
```typescript
export const MIGRATION_HEAD: number
export function runMigrations(): void  // 从 current+1 跑到 HEAD；失败抛出（已写 system log）+ DB 停留前版本
// 注意：调用前必须已 createTables()（hasColumn 需基线表存在）
```

<!-- Plan 02 产出契约 -->

From src/types/backup.ts (Plan 02):
```typescript
export interface BackupConfig {
  id: number; enabled: boolean; intervalMinutes: number;
  lastRun: string | null; nextRun: string | null;
  periodicRetention: number; premigrationRetention: number;
}
export interface BackupStatus { isRunning: boolean; isTaskRunning: boolean; config: BackupConfig }
export interface UpdateBackupInput { enabled?: boolean; intervalMinutes?: number; periodicRetention?: number; premigrationRetention?: number }
export const DEFAULT_BACKUP_CONFIG: { enabled: true; intervalMinutes: 1440; periodicRetention: 7; premigrationRetention: 5 }
```

From electron/database/acl.ts (Plan 02):
```typescript
export function restrictFilePermissions(filePath: string, label: string): void  // 单文件，幂等，非致命
export function restrictDirPermissions(dirPath: string): void                  // 目录批量
```

<!-- 现有契约（待改造/复用） -->

From electron/database/connection.ts（当前完整内容，本 plan 改造 initDatabase + 新增 migrateAndSecure）:
```typescript
import Database from 'better-sqlite3'
import path from 'path'
import { app } from 'electron'
let db: Database.Database | null = null
export function getDatabase(): Database.Database { if (!db) throw new Error('Database not initialized'); return db }
export function initDatabase(): Database.Database {
  const dbPath = path.join(app.getPath('userData'), 'topology.db')
  db = new Database(dbPath)
  db.pragma('journal_mode = WAL')
  db.pragma('foreign_keys = ON')
  db.pragma('busy_timeout = 5000')
  db.pragma('wal_autocheckpoint = 1000')
  return db
}
export function closeDatabase() { if (db) { db.close(); db = null } }
```

From electron/services/schedulerService.ts（BackupScheduler 的 exact mirror 模板，逐方法对齐——尤其 runTask/executeTask 拆分）:
```typescript
export class SchedulerService {
  private static intervalId: ReturnType<typeof setInterval> | null = null
  private static isRunning = false
  static start(): void { /* getConfig -> if !enabled return -> setInterval(runTask, min*60*1000) -> if shouldRunNow runTask */ }
  static stop(): void { /* clearInterval */ }
  static restart(): void { this.stop(); this.start() }
  static async runNow(): Promise<{...}> { /* if isRunning return; executeTask; return result */ }
  private static async runTask(): Promise<void> {
    if (this.isRunning) return          // <-- isRunning guard 在 runTask
    try {
      await this.executeTask()          // <-- 工作隔离到 executeTask
      this.updateLastRun()
      const config = this.getConfig()
      this.updateNextRun(config.intervalMinutes)
    } catch (error) { console.error('[Scheduler] Task failed:', error) }
  }
  private static async executeTask(): Promise<{...}> {
    this.isRunning = true               // <-- isRunning = true 在 executeTask 入口
    try { /* ...actual work... */ } finally { this.isRunning = false }  // <-- finally 重置
  }
  private static shouldRunNow(config): boolean { /* !lastRun ? true : elapsed >= interval */ }
  static getConfig(): any { /* SELECT * FROM scheduler_config WHERE id=1；无行则 INSERT seed 默认 */ }
  static updateConfig(updates): any { /* UPDATE + restart */ }
  private static updateLastRun(): void { /* UPDATE last_run = datetime('now','localtime') */ }
  private static updateNextRun(min): void { /* UPDATE next_run = ISO */ }
  private static notifyRenderer(channel, data): void { /* BrowserWindow.getAllWindows().send */ }
  static getStatus(): { isRunning; isTaskRunning; config } { /* ... */ }
}
```
scheduler_config schema（init.ts:192-198）：`id INTEGER PRIMARY KEY CHECK (id = 1), enabled INTEGER DEFAULT 0, interval_minutes INTEGER DEFAULT 60, last_run TEXT, next_run TEXT`

From electron/main.ts（hook 点）:
- 第 78-79 行：`initDatabase(); createTables()`（本 plan 改为 `initDatabase(); createTables(); migrateAndSecure()`）
- 第 89 行：`SchedulerService.start()`（其后加 BackupScheduler.start()）
- 第 157 行：`app.on('before-quit', () => closeDatabase())`（改为先 BackupScheduler.stop()）
- 第 4 行：`import { createTables } from './database/init'`（附近加 BackupScheduler + migrateAndSecure import）

better-sqlite3 备份 API（D-04 关键）:
```typescript
db.backup(destinationPath: string): Promise<unknown> | unknown
// 在线一致性快照（WAL 一致），输出单文件（无需 -wal/-shm），不长时间阻塞写
// 注意：better-sqlite3 的 db.backup 是同步方法（返回 Promise 仅在 callback 形式），本 plan 用同步形式 await 亦可
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 BackupScheduler（镜像 SchedulerService + runTask/executeTask 拆分 + 周期备份 + 双桶轮换）</name>
  <files>electron/services/backupScheduler.ts</files>

  <read_first>
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md（D-01 触发时机/默认 24h、D-02 双桶 7/5、D-03 userData/backups/、D-04 db.backup() API、D-05 注册与对齐 SchedulerService、D-12b 备份创建即收紧 ACL）
    - .planning/phases/02-architecture-db-migration/02-PATTERNS.md §backupScheduler.ts（exact analog：逐方法镜像 schedulerService.ts:7-122，**含 runTask/executeTask 拆分**）
    - electron/services/schedulerService.ts（完整 1-122 行，逐方法对照复制结构——**重点看 runTask:37-45 与 executeTask:47-76 的 isRunning 拆分模式**）
    - src/types/backup.ts（Plan 02 产出，BackupConfig/DEFAULT_BACKUP_CONFIG）
    - electron/database/acl.ts（Plan 02 产出，restrictFilePermissions）
    - electron/database/connection.ts（getDatabase 签名）
  </read_first>

  <action>
创建 `electron/services/backupScheduler.ts`，**逐方法镜像 schedulerService.ts 结构**（D-05 exact mirror），**runTask/executeTask 拆分与 isRunning 在 executeTask finally 重置与 SchedulerService 完全一致**（不静默丢弃并发备份，ROBUST），差异仅：executeTask 调 db.backup + 双桶轮换；默认 intervalMinutes=1440；config 表 backup_config + retention 字段。

```typescript
import { BrowserWindow, app } from 'electron'
import path from 'path'
import fs from 'fs'
import { getDatabase } from '../database/connection'
import { restrictFilePermissions } from '../database/acl'
import { createSystemLog } from './systemLog'
import type { BackupConfig } from '../../src/types/backup'
import { DEFAULT_BACKUP_CONFIG } from '../../src/types/backup'

const BACKUPS_DIR = (): string => path.join(app.getPath('userData'), 'backups')

function timestampStr(): string {
  // YYYYMMDD-HHmmss 本地时间
  const d = new Date()
  const pad = (n: number) => String(n).padStart(2, '0')
  return `${d.getFullYear()}${pad(d.getMonth() + 1)}${pad(d.getDate())}-${pad(d.getHours())}${pad(d.getMinutes())}${pad(d.getSeconds())}`
}

export class BackupScheduler {
  private static intervalId: ReturnType<typeof setInterval> | null = null
  private static isRunning = false

  static start(): void {
    if (this.intervalId) return
    const config = this.getConfig()
    if (!config.enabled) return
    const intervalMinutes = config.intervalMinutes ?? DEFAULT_BACKUP_CONFIG.intervalMinutes
    this.updateNextRun(intervalMinutes)
    this.intervalId = setInterval(() => { this.runTask().catch((e) => console.error('[BackupScheduler] interval run failed:', e)) }, intervalMinutes * 60 * 1000)
    if (this.shouldRunNow(config)) this.runTask().catch((e) => console.error('[BackupScheduler] initial run failed:', e))
  }

  static stop(): void {
    if (this.intervalId) { clearInterval(this.intervalId); this.intervalId = null }
  }

  static restart(): void { this.stop(); this.start() }

  // 镜像 SchedulerService.runTask（schedulerService.ts:37-45）：isRunning guard 在 runTask，
  // 实际工作隔离到 executeTask（isRunning 在 executeTask finally 重置）—— 不静默丢弃并发备份。
  private static async runTask(): Promise<void> {
    if (this.isRunning) return
    try {
      await this.executeTask()
      this.updateLastRun()
      const config = this.getConfig()
      this.updateNextRun(config.intervalMinutes)
    } catch (error) {
      console.error('[BackupScheduler] Task failed:', error)
      try {
        createSystemLog({ type: 'backup', status: 'failed', errorMessage: `周期备份失败: ${(error as Error).message}` })
      } catch { /* 日志失败不影响调度 */ }
    }
  }

  // 镜像 SchedulerService.executeTask（schedulerService.ts:47-76）：isRunning=true 在入口、finally 重置。
  // 备份工作（db.backup + ACL + 裁剪 + notify）隔离在此，与 SchedulerService 拆分结构完全对齐（D-05）。
  private static async executeTask(): Promise<void> {
    this.isRunning = true
    try {
      const filename = `topology-periodic-${timestampStr()}.db.bak`
      const backupPath = path.join(BACKUPS_DIR(), filename)
      ensureBackupsDir()
      getDatabase().backup(backupPath) // D-04：在线一致性备份，单文件输出
      restrictFilePermissions(backupPath, filename) // D-12b：备份创建即收紧 ACL
      this.pruneBackups('periodic', this.getConfig().periodicRetention)
      this.notifyRenderer('backup-completed', { type: 'periodic', filename, timestamp: new Date().toISOString() })
    } finally {
      this.isRunning = false
    }
  }

  /**
   * 迁移前即时备份（D-06，强制安全网）。由 connection.ts migrateAndSecure() 在
   * createTables() 之后、runMigrations() 之前调用（捕获 post-基线表 schema+数据，非空库快照）。
   * 命名带 from→to 版本，独立迁移桶（premigrationRetention=5），不混入周期桶裁剪（D-02）。
   */
  static createPremigrationBackup(fromVersion: number, toVersion: number): string {
    const filename = `topology-premigration-v${fromVersion}-to-v${toVersion}-${timestampStr()}.db.bak`
    const backupPath = path.join(BACKUPS_DIR(), filename)
    ensureBackupsDir()
    getDatabase().backup(backupPath)
    restrictFilePermissions(backupPath, filename) // D-12b
    this.pruneBackups('premigration', this.getConfig().premigrationRetention)
    return backupPath
  }

  /** 双桶 FIFO 裁剪（D-02）：周期桶/迁移桶各自按 mtime 滚动保留 N 份，互不干扰 */
  private static pruneBackups(bucket: 'periodic' | 'premigration', retention: number): void {
    try {
      const dir = BACKUPS_DIR()
      const prefix = bucket === 'periodic' ? 'topology-periodic-' : 'topology-premigration-'
      const files = fs.readdirSync(dir)
        .filter((f) => f.startsWith(prefix) && f.endsWith('.db.bak'))
        .map((f) => ({ name: f, mtime: fs.statSync(path.join(dir, f)).mtimeMs }))
        .sort((a, b) => b.mtime - a.mtime) // 新→旧
      const toDelete = files.slice(retention) // 超出 retention 的旧文件
      for (const f of toDelete) {
        try { fs.unlinkSync(path.join(dir, f.name)) } catch { /* 单文件删除失败跳过 */ }
      }
    } catch (error) {
      try { createSystemLog({ type: 'backup', status: 'warning', errorMessage: `备份裁剪失败 (${bucket}): ${(error as Error).message}` }) } catch { /* 非致命 */ }
    }
  }

  private static shouldRunNow(config: BackupConfig): boolean {
    if (!config.lastRun) return true
    try {
      const elapsed = Date.now() - new Date(config.lastRun).getTime()
      return elapsed >= (config.intervalMinutes ?? DEFAULT_BACKUP_CONFIG.intervalMinutes) * 60 * 1000
    } catch { return true }
  }

  static getConfig(): BackupConfig {
    const db = getDatabase()
    const row = db.prepare('SELECT * FROM backup_config WHERE id = 1').get() as any
    if (!row) {
      db.prepare('INSERT INTO backup_config (id, enabled, interval_minutes, periodic_retention, premigration_retention) VALUES (1, ?, ?, ?, ?)').run(
        DEFAULT_BACKUP_CONFIG.enabled ? 1 : 0,
        DEFAULT_BACKUP_CONFIG.intervalMinutes,
        DEFAULT_BACKUP_CONFIG.periodicRetention,
        DEFAULT_BACKUP_CONFIG.premigrationRetention,
      )
      return { id: 1, enabled: DEFAULT_BACKUP_CONFIG.enabled, intervalMinutes: DEFAULT_BACKUP_CONFIG.intervalMinutes, lastRun: null, nextRun: null, periodicRetention: DEFAULT_BACKUP_CONFIG.periodicRetention, premigrationRetention: DEFAULT_BACKUP_CONFIG.premigrationRetention }
    }
    return {
      id: row.id, enabled: Boolean(row.enabled),
      intervalMinutes: row.interval_minutes ?? DEFAULT_BACKUP_CONFIG.intervalMinutes,
      lastRun: row.last_run, nextRun: row.next_run,
      periodicRetention: row.periodic_retention ?? DEFAULT_BACKUP_CONFIG.periodicRetention,
      premigrationRetention: row.premigration_retention ?? DEFAULT_BACKUP_CONFIG.premigrationRetention,
    }
  }

  static updateConfig(updates: { enabled?: boolean; intervalMinutes?: number; periodicRetention?: number; premigrationRetention?: number }): BackupConfig {
    const db = getDatabase()
    const config = this.getConfig()
    const enabled = updates.enabled !== undefined ? (updates.enabled ? 1 : 0) : (config.enabled ? 1 : 0)
    const intervalMinutes = updates.intervalMinutes ?? config.intervalMinutes
    const periodicRetention = updates.periodicRetention ?? config.periodicRetention
    const premigrationRetention = updates.premigrationRetention ?? config.premigrationRetention
    db.prepare('UPDATE backup_config SET enabled = ?, interval_minutes = ?, periodic_retention = ?, premigration_retention = ? WHERE id = 1').run(enabled, intervalMinutes, periodicRetention, premigrationRetention)
    this.restart()
    return this.getConfig()
  }

  private static updateLastRun(): void {
    getDatabase().prepare("UPDATE backup_config SET last_run = datetime('now','localtime') WHERE id = 1").run()
  }

  private static updateNextRun(intervalMinutes: number): void {
    const nextRun = new Date(Date.now() + (intervalMinutes ?? DEFAULT_BACKUP_CONFIG.intervalMinutes) * 60 * 1000).toISOString()
    getDatabase().prepare('UPDATE backup_config SET next_run = ? WHERE id = 1').run(nextRun)
  }

  private static notifyRenderer(channel: string, data: any): void {
    for (const win of BrowserWindow.getAllWindows()) { win.webContents.send(channel, data) }
  }

  static getStatus(): { isRunning: boolean; isTaskRunning: boolean; config: BackupConfig } {
    return { isRunning: this.intervalId !== null, isTaskRunning: this.isRunning, config: this.getConfig() }
  }
}

/** 确保 userData/backups/ 存在（premigration 备份 + 周期备份前调用）。导出供 connection.ts 复用。 */
export function ensureBackupsDir(): string {
  const dir = BACKUPS_DIR()
  if (!fs.existsSync(dir)) {
    fs.mkdirSync(dir, { recursive: true })
  }
  return dir
}
```

关键约束：
- **逐方法镜像 SchedulerService**（start/stop/restart/runTask/executeTask/shouldRunNow/getConfig/updateConfig/updateLastRun/updateNextRun/notifyRenderer/getStatus），方法签名与控制流一致（D-05）
- **runTask/executeTask 拆分必须与 SchedulerService 一致**（WARNING 2）：`runTask` 仅做 `if (isRunning) return` guard + 包裹 executeTask + updateLastRun/updateNextRun；`executeTask` 入口 `isRunning = true`、`finally { isRunning = false }`，备份工作（db.backup + ACL + 裁剪 + notify）全在 executeTask 内。**不在 runTask 内直接 return 丢弃并发备份而不记日志**——并发由 isRunning guard 阻止，但失败走 runTask 的 catch 写 system log（与 SchedulerService runTask catch 对齐）
- **默认 intervalMinutes=1440**（D-01，不是 SchedulerService 的 60）—— 全部用 `?? DEFAULT_BACKUP_CONFIG.intervalMinutes`
- **executeTask 用 `getDatabase().backup(backupPath)`**（D-04 在线一致性，单文件；不要裸拷 .db）
- **每备份创建后立即 restrictFilePermissions**（D-12b，filename 作 label）
- **双桶裁剪互不干扰**（D-02）：pruneBackups 按 prefix 过滤，periodic 桶只裁 `topology-periodic-`，premigration 桶只裁 `topology-premigration-`
- **createPremigrationBackup 是 public static**，由 connection.ts migrateAndSecure 在 createTables 之后、runMigrations 之前调用（D-06），返回备份路径供迁移失败错误信息引用
- **config 表 backup_config**（与 scheduler_config 平行，schema 在 Task 2 init.ts 补建）
- 失败一律 try/catch + createSystemLog(type='backup')（mirrors schedulerService 错误处理）
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npm run build:electron-main 2>&1 | tail -3 && test -f electron/services/backupScheduler.ts && grep -q "export class BackupScheduler" electron/services/backupScheduler.ts && grep -q "private static async executeTask" electron/services/backupScheduler.ts</automated>
  </verify>

  <acceptance_criteria>
    - `electron/services/backupScheduler.ts` 存在
    - `grep -q "export class BackupScheduler" electron/services/backupScheduler.ts` 命中
    - `grep -c "static start(): void" electron/services/backupScheduler.ts` = 1（镜像 SchedulerService）
    - `grep -c "static stop(): void" electron/services/backupScheduler.ts` = 1
    - `grep -q "static restart(): void" electron/services/backupScheduler.ts` 命中
    - **runTask/executeTask 拆分（WARNING 2）**：`grep -q "private static async runTask(): Promise<void>" electron/services/backupScheduler.ts` 命中（runTask 仅 guard + 包裹 executeTask）
    - `grep -q "private static async executeTask(): Promise<void>" electron/services/backupScheduler.ts` 命中（备份工作隔离在 executeTask）
    - `grep -c "await this.executeTask()" electron/services/backupScheduler.ts` >= 1（runTask 调 executeTask，镜像 SchedulerService）
    - `grep -q "this.isRunning = true" electron/services/backupScheduler.ts` 命中（executeTask 入口置位，与 SchedulerService:48 对齐）
    - `grep -c "} finally {" electron/services/backupScheduler.ts` >= 1 且 executeTask 内含 `this.isRunning = false`（finally 重置，与 SchedulerService:75 对齐）
    - `grep -q "if (this.isRunning) return" electron/services/backupScheduler.ts` 命中（runTask guard，与 SchedulerService:38 对齐）
    - `grep -q "shouldRunNow" electron/services/backupScheduler.ts` 命中（启动补跑模式 D-01）
    - `grep -q "getDatabase().backup(backupPath)" electron/services/backupScheduler.ts` 命中（D-04 db.backup API）
    - `grep -q "createPremigrationBackup" electron/services/backupScheduler.ts` 命中（D-06 迁移前备份入口）
    - `grep -q "topology-periodic-" electron/services/backupScheduler.ts` 命中（D-02 周期桶命名）
    - `grep -q "topology-premigration-v" electron/services/backupScheduler.ts` 命中（D-02 迁移桶命名 + 版本）
    - `grep -q "restrictFilePermissions(backupPath" electron/services/backupScheduler.ts` 命中（D-12b 备份即收紧 ACL）
    - `grep -c "restrictFilePermissions" electron/services/backupScheduler.ts` >= 2（周期 + premigration 各一处）
    - `grep -q "pruneBackups" electron/services/backupScheduler.ts` 命中（D-02 轮换）
    - `grep -q "backup_config" electron/services/backupScheduler.ts` 命中（独立 config 表）
    - `grep -q "periodic_retention" electron/services/backupScheduler.ts` 命中（retention 列）
    - `grep -q "DEFAULT_BACKUP_CONFIG.intervalMinutes" electron/services/backupScheduler.ts` 命中（默认 24h via 引用）
    - `grep -q "import { restrictFilePermissions } from '../database/acl'" electron/services/backupScheduler.ts` 命中
    - `grep -q "import type { BackupConfig } from '../../src/types/backup'" electron/services/backupScheduler.ts` 命中
    - `npx tsc -p tsconfig.web.json --noEmit` 退出码 0
    - `npm run build:electron-main` 退出码 0
  </acceptance_criteria>

  <done>BackupScheduler 镜像 SchedulerService（含 runTask/executeTask 拆分 + isRunning 在 executeTask finally 重置，不静默丢弃并发备份），executeTask 调 db.backup + 双桶轮换 + 备份即 ACL；createPremigrationBackup 暴露供 D-06；默认 24h；tsc+esbuild 双绿。</done>
</task>

<task type="auto">
  <name>Task 2: 改造 connection.ts（migrateAndSecure: premigration 备份 gated + runMigrations + ACL 收紧）+ init.ts（删散落迁移块 + backup_config 表）+ main.ts 生命周期</name>
  <files>electron/database/connection.ts, electron/database/init.ts, electron/main.ts</files>

  <read_first>
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md（D-06 迁移前备份强制、D-07 步骤原子、D-08 失败中止指明备份、D-12a 启动重收紧 ACL 幂等、D-13 ACL 失败非致命、§Integration Points 排序）
    - .planning/phases/02-architecture-db-migration/02-PATTERNS.md §connection.ts（self analog：initDatabase 是 hook 点）+ §init.ts（self refactor：散落块移除）
    - electron/database/connection.ts（当前完整 1-24 行，本 plan 改造 initDatabase + 新增 migrateAndSecure）
    - electron/database/init.ts（完整——第 273-356 散落迁移块待删除；第 192-198 scheduler_config 区域 backup_config 表需补建其后；createTables 末尾 initDefaultOUIData 保留）
    - electron/database/migrations.ts（Plan 01：MIGRATION_HEAD + runMigrations 签名；init.ts 散落块 SQL 已迁入此）
    - electron/database/acl.ts（Plan 02：restrictFilePermissions/restrictDirPermissions）
    - electron/services/backupScheduler.ts（Plan 03 Task 1：ensureBackupsDir + BackupScheduler.createPremigrationBackup）
    - electron/main.ts（第 78-89 ready 区、第 157 before-quit、第 3-4 import 区）
  </read_first>

  <action>
**重要定序决策（解决 BLOCKER + WARNING 1，单一 init.ts 编辑权归本 plan）：**

最终启动顺序（main.ts app ready）：
```
initDatabase()      → 连接 + pragma + ensureBackupsDir + 收紧历史 backups 目录 ACL（D-12a）
createTables()      → CREATE TABLE IF NOT EXISTS 基线表（含新 backup_config 表）+ initDefaultOUIData；**散落迁移块 273-356 已删除**（SQL 在 Plan 01 migrations.ts）
migrateAndSecure()  → premigration 备份（gated on 非空库，createTables 之后）→ runMigrations → ACL 收紧 db/wal/shm（D-12a）
```

**为何 premigration 备份在 migrateAndSecure（createTables 之后）而非 initDatabase（WARNING 1）：**
D-06「迁移前备份」的恢复点语义 = 「迁移前的数据态」。把 createPremigrationBackup 放在 migrateAndSecure 内、createTables 之后、runMigrations 之前：
- 旧库（已有数据）：createTables 的 CREATE TABLE IF NOT EXISTS 是 no-op（表早存在）→ premigration 备份捕获「旧数据 + 旧 schema」完整快照，正是 D-06 期望的迁移前恢复点
- fresh-install 空库：createTables 后表刚建为空 → **gated 跳过**（见下），不生成无价值的空快照
- 此顺序满足 D-06「在第一个 pending 迁移步骤前」（runMigrations 在 premigration 备份之后执行）

**fresh-install 空库 gate（WARNING 1 fix）：**
migrateAndSecure 内 premigration 备份前检查「库是否有任何用户数据行」（用 `SELECT count(*) FROM sqlite_master WHERE type='table'` + 抽样 topologies/devices 行数 > 0）；若空库（fresh install）则**跳过 premigration 备份**（空库无数据可恢复，备份无价值），仅在 system log 记一条 info「fresh-install: 跳过 premigration 备份」。旧库（有数据）才生成 premigration 备份。

**步骤 A：改造 `electron/database/connection.ts`**

```typescript
import Database from 'better-sqlite3'
import path from 'path'
import { app } from 'electron'
import { runMigrations, MIGRATION_HEAD } from './migrations'
import { restrictFilePermissions, restrictDirPermissions } from './acl'
import { ensureBackupsDir, BackupScheduler } from '../services/backupScheduler'
import { createSystemLog } from '../services/systemLog'

let db: Database.Database | null = null

export function getDatabase(): Database.Database {
  if (!db) throw new Error('Database not initialized')
  return db
}

export function initDatabase(): Database.Database {
  const dbPath = path.join(app.getPath('userData'), 'topology.db')
  db = new Database(dbPath)
  db.pragma('journal_mode = WAL')
  db.pragma('foreign_keys = ON')
  db.pragma('busy_timeout = 5000')
  db.pragma('wal_autocheckpoint = 1000')

  // D-12a：启动时重收紧历史 backups 目录 ACL（幂等，修正历史宽松权限）
  ensureBackupsDir()
  restrictDirPermissions(path.join(app.getPath('userData'), 'backups'))

  return db
}

export function closeDatabase() {
  if (db) { db.close(); db = null }
}

/**
 * 执行迁移 + 收紧活跃 DB 文件 ACL（D-06/D-12a）。
 * 由 main.ts 在 createTables() 之后调用（确保基线表已建，迁移可 hasColumn 检查 + premigration 备份捕获 post-基线表数据）。
 *
 * 顺序（D-06/D-12a）：
 *   1. 若 current < MIGRATION_HEAD 且库非空 → createPremigrationBackup（D-06 强制安全网，捕获迁移前数据态）
 *   2. runMigrations（Plan 01：原子步骤，失败抛出+system log+中止，DB 停留前版本 D-08）
 *   3. ACL 收紧 db/wal/shm（D-12a 幂等，非致命 D-13）
 *
 * fresh-install（空库）跳过 premigration 备份（无数据可恢复）；旧库（有数据）必备份。
 */
export function migrateAndSecure(): void {
  const conn = getDatabase()
  const currentVersion = (conn.pragma('user_version') as Array<{ user_version: number }>)[0]?.user_version ?? 0

  // D-06：迁移前备份（gated on 非空库）。createTables 之后、runMigrations 之前。
  if (currentVersion < MIGRATION_HEAD) {
    if (hasUserData()) {
      BackupScheduler.createPremigrationBackup(currentVersion, MIGRATION_HEAD)
    } else {
      // WARNING 1 fix：fresh-install 空库无数据可恢复，premigration 备份无价值，跳过并记录
      try {
        createSystemLog({ type: 'backup', status: 'warning', errorMessage: 'fresh-install 空库：跳过 premigration 备份（无数据可恢复），直接执行迁移' })
      } catch { /* 非致命 */ }
    }
  }

  // 迁移（Plan 01 runMigrations：原子步骤，失败抛出+system log+中止，DB 停留前版本 D-08）
  runMigrations()

  // D-12a：活跃 DB 文件每次启动重新收紧（幂等，顺带修正历史宽松权限）
  const dbPath = path.join(app.getPath('userData'), 'topology.db')
  restrictFilePermissions(dbPath, 'topology.db')
  restrictFilePermissions(`${dbPath}-wal`, 'topology.db-wal')
  restrictFilePermissions(`${dbPath}-shm`, 'topology.db-shm')
}

/**
 * 检测库是否有用户数据（用于 gate premigration 备份——fresh-install 空库跳过）。
 * topologies/devices 是核心业务表，任一有行即视为有数据。
 */
function hasUserData(): boolean {
  try {
    const conn = getDatabase()
    const tableCount = (conn.prepare("SELECT count(*) as cnt FROM sqlite_master WHERE type='table'").get() as { cnt: number }).cnt
    if (tableCount === 0) return false // 表都没建（理论上 createTables 后不会发生，防御）
    const topoCount = (conn.prepare('SELECT count(*) as cnt FROM topologies').get() as { cnt: number }).cnt
    if (topoCount > 0) return true
    const devCount = (conn.prepare('SELECT count(*) as cnt FROM devices').get() as { cnt: number }).cnt
    return devCount > 0
  } catch {
    // 表不存在（createTables 异常或 schema 不全）→ 视为无数据，保守返回 false
    return false
  }
}
```

**步骤 B：改造 `electron/database/init.ts`（单一编辑权归本 plan）**

1. **删除** init.ts 第 273-356 行的整个散落迁移块（从 `// Migrate: add session_id column...` 注释起到 `rebuildDevices()` 调用 + 其后的空行）。这些 SQL 已由 Plan 01 迁入 migrations.ts 注册表（v1-v5）。
2. 删除该块内的 `const db = getDatabase()` 行（第 274 行）——仅服务于已删除的迁移块，`initDefaultOUIData(db)` 调用改为 `initDefaultOUIData(getDatabase())`（保留 initDefaultOUIData 行为不变）。
3. **不要** 在 init.ts 添加任何 `runMigrations` import 或调用——runMigrations 的单一调用点是 connection.ts migrateAndSecure（BLOCKER fix：init.ts 与 runMigrations 解耦，单一真相在 connection.ts）。
4. **新增 backup_config 表**（在 createTables 的 CREATE TABLE 区域，紧跟 scheduler_config 之后）：
   ```sql
   CREATE TABLE IF NOT EXISTS backup_config (
     id INTEGER PRIMARY KEY CHECK (id = 1),
     enabled INTEGER NOT NULL DEFAULT 1,
     interval_minutes INTEGER NOT NULL DEFAULT 1440,
     periodic_retention INTEGER NOT NULL DEFAULT 7,
     premigration_retention INTEGER NOT NULL DEFAULT 5,
     last_run TEXT,
     next_run TEXT
   );
   ```
   （DEFAULT 1 = enabled true、1440 = 24h、7/5 retention，与 DEFAULT_BACKUP_CONFIG 一致）
5. 保留 createTables 中所有其他 CREATE TABLE IF NOT EXISTS 语句 + initDefaultOUIData(getDatabase()) 不变。

**步骤 C：改造 `electron/main.ts`**

- 第 78-79 行区域改为：
  ```typescript
  initDatabase()
  createTables()
  migrateAndSecure()   // 新增：迁移前备份(gated) + runMigrations + ACL 收紧（D-06/D-12a）
  ```
- 第 89 行 `SchedulerService.start()` 之后加：
  ```typescript
  BackupScheduler.start()
  ```
- 第 157 行 before-quit 改为：
  ```typescript
  app.on('before-quit', () => { BackupScheduler.stop(); closeDatabase() })
  ```
- import 区（第 3-4 行附近）加：
  ```typescript
  import { initDatabase, closeDatabase, migrateAndSecure } from './database/connection'
  import { BackupScheduler } from './services/backupScheduler'
  ```
  （原 initDatabase/closeDatabase 从 connection import，migrateAndSecure 追加到同一 import）

关键约束：
- **init.ts 不出现 runMigrations**（BLOCKER fix）：不 import、不调用。init.ts 与 runMigrations 完全解耦，单一调用点 = connection.ts migrateAndSecure。`grep -c "runMigrations" init.ts` = 0 是单一真相。
- **init.ts 散落迁移块必须物理删除**（273-356），SQL 已在 Plan 01 migrations.ts；不删除会导致 createTables 与 runMigrations 重复执行迁移（v1-v5 跑两次，虽然幂等但冗余 + 违背 ARCH-01「散落检查收敛」目标）
- **backup_config 表必须 CREATE**（否则 BackupScheduler.getConfig 的 SELECT/INSERT 报 no such table）
- **migrateAndSecure 在 createTables 之后**（hasColumn 需表存在 + premigration 备份需捕获 post-基线表数据 WARNING 1）
- **premigration 备份 gated on hasUserData()**（WARNING 1：fresh-install 空库跳过，不生成空快照）
- **ACL 收紧活跃文件在 migrateAndSecure 末尾**（迁移改了 schema 后再收紧，且 D-12a 幂等无 sentinel）
- **import 用 type-only 不可**（runMigrations/migrateAndSecure/restrictFilePermissions/BackupScheduler 均为运行时调用，普通 import）
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npm run build:electron 2>&1 | tail -5 && echo "BUILD_GREEN" && grep -q "migrateAndSecure" electron/database/connection.ts && grep -q "backup_config" electron/database/init.ts && test "$(grep -c "runMigrations" electron/database/init.ts)" = "0"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "export function migrateAndSecure" electron/database/connection.ts` 命中
    - `grep -q "BackupScheduler.createPremigrationBackup(currentVersion, MIGRATION_HEAD)" electron/database/connection.ts` 命中（D-06，migrateAndSecure 内）
    - `grep -q "currentVersion < MIGRATION_HEAD" electron/database/connection.ts` 命中（仅 pending 时备份）
    - `grep -q "hasUserData()" electron/database/connection.ts` 命中（WARNING 1 fix：fresh-install 空库 gate）
    - `grep -q "fresh-install" electron/database/connection.ts` 命中（空库跳过 premigration 备份的显式记录）
    - `grep -q "ensureBackupsDir()" electron/database/connection.ts` 命中（目录就绪）
    - `grep -q "restrictDirPermissions" electron/database/connection.ts` 命中（历史备份 ACL D-12a）
    - `grep -c "restrictFilePermissions" electron/database/connection.ts` >= 3（db + wal + shm 三处 D-10/D-12a）
    - `grep -q "runMigrations()" electron/database/connection.ts` 命中（迁移在 migrateAndSecure 内，单一调用点）
    - **BLOCKER fix — init.ts 与 runMigrations 解耦，单一真相收敛**：`grep -c "runMigrations" electron/database/init.ts` = 0（init.ts 不 import 不调用 runMigrations；单一调用点在 connection.ts migrateAndSecure）
    - **init.ts 散落迁移块物理删除**：`grep -c "PRAGMA table_info" electron/database/init.ts` = 0（4 处散落检查全部移除，已迁入 Plan 01 migrations.ts via hasColumn）—— 注意 init.ts:312 的 `sqlite_master` 检查也随 devices 重建块一并删除
    - `grep -c "session_id TEXT" electron/database/init.ts` = 0（chat_history 列迁移已移出 init.ts）
    - `grep -c "rebuildDevices" electron/database/init.ts` = 0（devices 重建块移除，已迁入 Plan 01 v5）
    - `grep -q "initDefaultOUIData(getDatabase())" electron/database/init.ts` 命中（原 initDefaultOUIData(db) 改为 getDatabase()，保留行为）
    - `grep -q "CREATE TABLE IF NOT EXISTS backup_config" electron/database/init.ts` 命中（config 表）
    - `grep -q "periodic_retention INTEGER NOT NULL DEFAULT 7" electron/database/init.ts` 命中
    - `grep -q "premigration_retention INTEGER NOT NULL DEFAULT 5" electron/database/init.ts` 命中
    - `grep -q "interval_minutes INTEGER NOT NULL DEFAULT 1440" electron/database/init.ts` 命中（backup_config 默认 24h）
    - `grep -q "migrateAndSecure()" electron/main.ts` 命中（main 调用迁移+ACL，在 createTables 之后）
    - `grep -q "BackupScheduler.start()" electron/main.ts` 命中（D-05 注册）
    - `grep -q "BackupScheduler.stop()" electron/main.ts` 命中（D-05 清理）
    - main.ts before-quit：`grep -q "BackupScheduler.stop(); closeDatabase()" electron/main.ts` 命中（stop 先于 closeDatabase）
    - `npx tsc -p tsconfig.web.json --noEmit` 退出码 0（严格 + noUnusedLocals，init.ts 删除散落块后无 unused）
    - `npm run build:electron` 退出码 0（main + preload 全链路双绿）
    - `npm run build` 退出码 0（tsc + vite + electron 三段全绿，Phase 1 基线不破坏）
  </acceptance_criteria>

  <done>connection.ts 提供 migrateAndSecure（gated premigration 备份 + runMigrations + ACL db/wal/shm）+ initDatabase（连接 + ensureBackupsDir + 历史备份 ACL）；init.ts 删除散落迁移块 273-356 + 新建 backup_config 表 + 不出现 runMigrations（与 Plan 01 真相收敛）；main.ts initDatabase→createTables→migrateAndSecure + BackupScheduler.start/stop 生命周期；tsc+esbuild+全量 build 三绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app 启动 → DB 迁移（premigration 备份点） | 迁移改 schema 前必须有恢复点，否则中途失败数据不可恢复 |
| 定时备份调度 ↔ DB 写入 | db.backup() 与并发写需 WAL 一致性，避免备份捕获半事务态 |
| 备份文件落盘 ↔ 同机其他用户 | 备份含加密凭证/AI日志副本，必须同等 ACL 保护 |
| app 退出 ↔ 进行中备份 | 退出时若备份进行中可能产出截断文件 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-10 | Tampering/数据丢失 | 迁移中途失败，无恢复点致数据永久丢失 | mitigate | D-06：migrateAndSecure 内 current<HEAD 且库非空时 createPremigrationBackup 强制生成恢复点（createTables 之后捕获 post-基线表数据态）；fresh-install 空库跳过（无数据可恢复）；D-08 迁移失败抛出指明该备份路径供人工恢复 |
| T-2-11 | Information Disclosure | 定时备份文件落盘后权限宽松，同机其他用户读取敏感数据副本 | mitigate | D-12b：BackupScheduler.executeTask + createPremigrationBackup 在 db.backup() 后立即 restrictFilePermissions；D-12a：启动时 restrictDirPermissions 重收紧历史备份；双桶命名隔离不影响 ACL |
| T-2-12 | Repudiation | 备份事件无审计 | mitigate | 备份成功 notifyRenderer('backup-completed')；失败 createSystemLog(type='backup', status='failed')；裁剪失败 createSystemLog(status='warning')；fresh-install 跳过 premigration 备份 createSystemLog(status='warning') 记录 |
| T-2-13 | Denial of Service | db.backup() 长时间阻塞写或退出时截断备份；并发备份静默丢失 | mitigate | better-sqlite3 db.backup() 是 WAL 一致性在线备份（D-04），不长时间阻塞写；isRunning guard（runTask）+ finally 重置（executeTask，镜像 SchedulerService）防并发且不静默丢失——失败走 runTask catch 写 system log；非致命错误处理不崩调度循环 |
| T-2-14 | Tampering | 备份文件被同机用户篡改后用于恢复 | mitigate | restrictFilePermissions 剥离继承仅当前用户（Win）/0o600（Unix），其他用户无写位（与 T-2-09 协同） |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 退出码 0
- `npm run build:electron` 退出码 0
- `npm run build` 退出码 0（三段全绿，Phase 1 基线不破坏）
- `codegraph_search BackupScheduler` 命中 backupScheduler.ts；`codegraph_callers BackupScheduler.start` 命中 main.ts
- `codegraph_callers runMigrations` 命中 connection.ts migrateAndSecure（单一调用点，**不命中 init.ts**——BLOCKER fix 验证）
- `codegraph_search backup` 命中 backup 调度（SC#3）
- **init.ts 散落块删除验证（ARCH-01 收敛）**：`grep -c "PRAGMA table_info" electron/database/init.ts` = 0；`grep -c "runMigrations" electron/database/init.ts` = 0（与 Plan 01 真相收敛）
- 端到端向后兼容验证（SC#4）：用 Phase 1 基线库（user_version=0 旧库或新建库）启动 app，确认：
  1. userData/backups/ 目录创建；旧库（有数据 + current<HEAD）含 premigration 备份；fresh-install 空库跳过 premigration 备份（system log 有 warning）
  2. user_version 迁移到 MIGRATION_HEAD=5（`sqlite3 userData/topology.db "PRAGMA user_version"` 返回 5）
  3. chat_history/ai_exec_logs/devices/ai_config 字段齐全，devices.connection_type 含 'rdp'（迁移跑且只跑一次——init.ts 不再跑，migrateAndSecure 跑）
  4. 拓扑/设备/AI 历史数据完整无丢失
  5. db/wal/shm + backups/ 文件 ACL 仅当前用户
  6. 定时备份按 24h（或手动触发）生成 topology-periodic-*.db.bak
</verification>

<success_criteria>
- SC#2 达成：ACL 收紧活跃 db/wal/shm + 备份文件，仅当前用户可读写（restrictFilePermissions + restrictDirPermissions 注册并执行）
- SC#3 达成：BackupScheduler 注册（main.ts start/stop），db.backup() 调用，备份文件按计划生成，双桶轮换
- SC#4 达成：旧库（user_version=0）启动自动迁移到 head，历史数据无丢失（端到端验证）；fresh-install 空库跳过 premigration 备份直接迁移
- SC#1 由 Plan 01 完成（user_version 读写 + hasColumn 收敛 + 散落块 SQL 迁入注册表）—— 本 plan migrateAndSecure 串联 runMigrations 闭合端到端 + init.ts 物理删除散落块完成 ARCH-01 收敛
- ARCH-02 完整闭环：ACL + 定时 backup + premigration 安全网（gated）
- Phase 1 构建基线不破坏（tsc + esbuild + 全量 build 三绿）
- **BLOCKER 解决**：init.ts 与 runMigrations 单一真相收敛——`grep "runMigrations" init.ts` = 0，`grep "runMigrations" connection.ts` >= 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-db-migration/02-03-SUMMARY.md`
</output>
