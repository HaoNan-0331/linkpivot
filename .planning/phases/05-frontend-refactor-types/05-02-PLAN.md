---
phase: 05-frontend-refactor-types
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/pages/TopologyPage.tsx
autonomous: false
requirements: [FE-03]

must_haves:
  truths:
    - "TopologyPage 新增 nodesRef/edgesRef，effect 同步 ref.current = nodes/edges 最新值"
    - "debouncedSave/saveTopology 的 setTimeout 闭包与 save 调用读 nodesRef.current/edgesRef.current 而非闭包捕获变量"
    - "toolbar 注册 effect 与 handleDiscoveryConfirm 等读最新 nodes/edges 的回调改读 ref.current"
    - "useCallback deps 去掉 nodes/edges（callback 不再随拓扑变化重建）"
    - "React Flow useNodesState/useEdgesState 契约不变（不迁 store）"
    - "tsc -p tsconfig.web.json exit 0；拓扑保存/编辑/发现行为人工 HV 无回归"
  artifacts:
    - path: "src/components/pages/TopologyPage.tsx"
      provides: "stale-closure-free TopologyPage（ref-mirror 模式）"
      contains: "nodesRef"
  key_links:
    - from: "TopologyPage debouncedSave/saveTopology"
      to: "nodesRef.current / edgesRef.current"
      via: "callback 体内读 ref"
      pattern: "nodesRef\\.current|edgesRef\\.current"
    - from: "TopologyPage effect（同步 ref）"
      to: "nodes/edges state"
      via: "useEffect ref.current = nodes"
      pattern: "nodesRef\\.current = nodes"
---

<objective>
FE-03 TopologyPage stale closure 修复：ref-mirror 模式（nodesRef/edgesRef），让所有「注册一次但需读最新拓扑」的回调在调用时读 `ref.current` 而非闭包捕获变量。

Purpose: 消除 D-5-4 定位的 stale closure 风险面（debouncedSave setTimeout 闭包、saveTopology、toolbar 注册 effect、handleDiscoveryConfirm）——模式脆弱（当前靠 clearTimeout-on-recreate 维持正确），ref-mirror 使回调注册稳定、读取自明。**不迁 nodes/edges 到 zustand**（D-5-4 红线：React Flow useNodesState/useEdgesState 契约不变，迁 store 触及核心价值「拓扑准确呈现」最高优先级面，风险不抵收益）。

Output: TopologyPage.tsx 改造（独占文件，与 05-01 零冲突）。autonomous=false 因含人工 HV（拓扑保存/编辑/发现无回归，无前端自动化测试基建 D-5-7）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-refactor-types/05-CONTEXT.md
@.planning/phases/05-frontend-refactor-types/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

@src/components/pages/TopologyPage.tsx
@src/stores/topologyToolbarStore.ts
@src/types/topology.ts
</context>

<interfaces>
<!-- TopologyPage 现有 ref 先例（line 24-25，本项目已用 ref-mirror） -->
```typescript
24  const saveTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
25  const isLoadingRef = useRef(false)   // 防 effect 重入（line 29/35/84）
```
<!-- React Flow 契约（line 16-17，不变） -->
```typescript
16  const [nodes, setNodes, onNodesChange] = useNodesState<TopologyNode>([])
17  const [edges, setEdges, onEdgesChange] = useEdgesState<TopologyEdge>([])
```
<!-- toolbar store 回调契约（src/stores/topologyToolbarStore.ts，FE-03 不改 store 契约，仅 callback 体内读 ref） -->
onTopologyChange/onNew/onSave/onDelete/onImport/onExport/onTopologyChange —— 签名不变
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TopologyPage ref-mirror（nodesRef/edgesRef + 全 stale closure 点改造）</name>
  <files>src/components/pages/TopologyPage.tsx</files>
  <read_first>
    - src/components/pages/TopologyPage.tsx（全文，重点 line 14/16/17/24/25/62-81/143-156/158-166/173-199/264 + 全部 `useCallback([...])` deps 含 nodes/edges 的点 line 70/81/141/198/228/237）
    - src/stores/topologyToolbarStore.ts（全文 23 行，确认 callback 契约不变）
    - src/types/topology.ts（TopologyNode/TopologyEdge 类型，ref 类型标注用）
    - 05-PATTERNS.md §8（stale closure 面逐字：debouncedSave/saveTopology/toolbar effect/handleDiscoveryConfirm + 现有 ref 先例）+ §9（toolbar store 全文）
    - 05-CONTEXT.md D-5-4（ref-mirror 决策、不迁 store 红线）
  </read_first>
  <action>
    **(1) 新增 ref 声明（line 24-25 既有 ref 之后）：**
    ```typescript
    const nodesRef = useRef<TopologyNode[]>([])
    const edgesRef = useRef<TopologyEdge[]>([])
    ```
    > 类型用 src/types/topology.ts 的 TopologyNode/TopologyEdge（与 useNodesState 泛型一致）。

    **(2) 新增同步 effect（紧邻 useNodesState/useEdgesState 声明之后）：**
    ```typescript
    // FE-03 (D-5-4): ref-mirror 同步最新 nodes/edges，供注册一次的回调读取，消除 stale closure
    useEffect(() => { nodesRef.current = nodes }, [nodes])
    useEffect(() => { edgesRef.current = edges }, [edges])
    ```

    **(3) 改造 debouncedSave（line 72-81）：**
    - setTimeout 闭包内 `nodes.map((n) => ({ ...n }))` → `nodesRef.current.map((n) => ({ ...n }))`
    - `edges.map((e) => ({ ...e }))` → `edgesRef.current.map((e) => ({ ...e }))`
    - `useCallback` deps `[currentTopologyId, nodes, edges]` → `[currentTopologyId]`（去 nodes/edges，callback 注册稳定）

    **(4) 改造 saveTopology（line 62-70）：**
    - `nodes.map(...)` → `nodesRef.current.map(...)`
    - `edges.map(...)` → `edgesRef.current.map(...)`
    - deps `[currentTopologyId, nodes, edges]` → `[currentTopologyId]`

    **(5) 改造 toolbar 注册 effect（line 143-156）：**
    - effect deps 中含 `saveTopology/handleNew/handleDelete/handleImport/handleExport/handleTopologyChange`，这些 callback 改造后 deps 收敛（不再依赖 nodes/edges），effect 重注册频率下降——保留 deps 中的 callback 引用（callback 现在稳定），但**确认 callback 体内读 ref.current**（即 handleDiscoveryConfirm 等见下）。effect 本身无需读 ref，注册的是 callback 引用。

    **(6) 改造 handleDiscoveryConfirm（line 173-199）及其他读 nodes/edges 的 useCallback：**
    - 全部 `useCallback` 体内读闭包 `nodes`/`edges` 处 → 读 `nodesRef.current`/`edgesRef.current`
    - deps `[..., nodes, edges]` → 去掉 `nodes`/`edges`
    - executor 须逐一审查 PATTERNS §8(d) 列出的全部 deps 含 nodes/edges 的 useCallback（line 70/81/141/198/228/237）：凡是 callback 体内读 `nodes`/`edges` 的，改读 ref + 去 deps；凡是用 `setNodes(eds => ...)`/`setEdges(eds => ...)` 函数式更新（如 handleConnect line 158-166）**无 stale 风险，不改**（函数式更新本就读最新）。

    **(7) 不动的部分：**
    - `useNodesState`/`useEdgesState` 声明（line 16-17）不变。
    - `useState<any[]>(topologies)`（line 14）可顺带收为 `useState<Topology[]>([])`（Claude's Discretion，FE-02 协同——但本 plan 不依赖 05-01 的 electron.d.ts 建模，Topology 类型在 src/types/topology.ts 已存在，直接用）。topologies 收敛为附带项，非 FE-03 核心。
    - React Flow `onConnect={handleConnect}`（line 264）等注册点不变（callback 引用稳定即可）。
    - `isLoadingRef`/`saveTimerRef`（line 24-25）既有 ref 不动。

    **原则：** ref-mirror 是「在调用时读 ref.current」的纯模式修复，**不改变拓扑持久化语义**（保存触发时机、保存内容、保存 API 调用全部不变）。executor 改造后须确认保存行为 byte-for-byte 等价（保存的 nodes/edges 内容 = 当前最新值，只是读取路径从闭包变量改为 ref.current）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "=== ref-mirror 命中 ===" && grep -c "nodesRef\|edgesRef" src/components/pages/TopologyPage.tsx && echo "=== useCallback deps 仍含 nodes/edges 的（应为 0，函数式更新除外） ===" && grep -n "useCallback" src/components/pages/TopologyPage.tsx | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nodesRef\|edgesRef" src/components/pages/TopologyPage.tsx` 输出 ≥ 6（2 声明 + 2 同步 effect + ≥2 读取点）
    - `grep "nodesRef.current = nodes" src/components/pages/TopologyPage.tsx` 命中（同步 effect）
    - `grep "edgesRef.current = edges" src/components/pages/TopologyPage.tsx` 命中
    - `grep "nodesRef.current.map" src/components/pages/TopologyPage.tsx` 命中（debouncedSave/saveTopology 读 ref）
    - debouncedSave 与 saveTopology 的 `useCallback` deps 数组不再含裸 `nodes`/`edges`（executor + 验收时目视 grep `useCallback(` 后 4 行内不含 `, nodes,` / `, edges,`）
    - `useNodesState`/`useEdgesState` 保留（未迁 store）：`grep "useNodesState\|useEdgesState" src/components/pages/TopologyPage.tsx` 命中
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
  </acceptance_criteria>
  <done>nodesRef/edgesRef 建立 + 同步 effect；debouncedSave/saveTopology/handleDiscoveryConfirm 等读 ref.current；useCallback deps 去 nodes/edges；React Flow 契约不变；tsc web 全绿。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    TopologyPage ref-mirror 改造：debouncedSave/saveTopology/toolbar 注册/handleDiscoveryConfirm 等回调改读 nodesRef.current/edgesRef.current，useCallback deps 去 nodes/edges，消除 stale closure。React Flow useNodesState/useEdgesState 契约不变（不迁 store）。
  </what-built>
  <how-to-verify>
    DEP-1 native binding 限制无前端自动化测试（D-5-7），须 Electron 运行时人工验证（与 Phase 2/3 HV 模式一致）。启动 app（npm run dev 或 packaged），打开拓扑页：
    1. **拖拽节点后保存**：拖动一个节点改变位置，等待 1 秒（debouncedSave 触发），刷新页面 → 节点位置应保持（保存读到最新 nodes，非过期闭包）。
    2. **连线后保存**：在两节点间新建一条 edge（onConnect），等待自动保存，刷新 → edge 保持。
    3. **批量快速拖拽**：连续快速拖动同一节点 3+ 次（每次 < 1 秒），等待 1 秒后刷新 → 最终位置正确（验证 debouncedSave clearTimeout + ref 读取协同，不被中间 stale 快照覆盖）。
    4. **拓扑发现 confirm**：触发 AI 拓扑发现 → 弹出 confirm → 确认后 handleDiscoveryConfirm 应用发现结果到画布（读最新 nodes/edges 合并），刷新 → 发现结果保持（无 stale 合并丢失）。
    5. **toolbar 侧边栏交互**：通过侧边栏 toolbar 触发 New/Save/Delete/Import/Export，确认 toolbar 回调读到最新拓扑（Save 与画布一致、Delete 清空正确、Import 后画布更新）。
    6. **无 console 报错**：DevTools Console 全程无 React 警告/报错（特别是 useCallback/useEffect 依赖警告）。
  </how-to-verify>
  <resume-signal>Type "approved"（6 项全过）或描述哪项失败/stale 行为</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-local（拓扑画布） | 本 plan 纯前端，无 IPC/DB/网络新调用；topology.update IPC 既有，调用面不变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Repudiation | 拓扑保存语义 | accept | ref-mirror 不改保存内容/时机/API，仅改读取路径（闭包变量→ref.current），持久化行为 byte-for-byte 等价；HV 步骤 1-3 验证保存正确性。 |
| T-05-02-02 | Tampering | useCallback deps 收敛 | mitigate | deps 去 nodes/edges 后 callback 注册稳定，读取走 ref.current 始终为最新值——消除「注册与调用间窗口」的 stale 风险（D-5-4 意图）。tsc 全绿 + HV 步骤 4-5 验证发现/toolbar 读最新。 |
| T-05-02-03 | Denial of Service | ref 同步 effect 性能 | accept | effect 仅 `ref.current = nodes`（O(1) 赋值，无重渲染副作用），与既有 isLoadingRef/saveTimerRef 同模式，无性能影响。 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals 全绿）
- `npx vitest run` exit 0（既有测试不回归，TopoPage 无单测但全仓库绿）
- grep nodesRef/edgesRef 命中（声明 + 同步 effect + 读取点）
- useCallback deps 无裸 nodes/edges（除函数式更新）
- 人工 HV 6 项全过（拓扑保存/编辑/发现/toolbar 无 stale 回归）
</verification>

<success_criteria>
- TopologyPage ref-mirror 落地（nodesRef/edgesRef + 同步 effect）
- 全部 stale closure 点（debouncedSave/saveTopology/toolbar 注册/handleDiscoveryConfirm）读 ref.current
- React Flow useNodesState/useEdgesState 契约不变（未迁 store，D-5-4 红线守住）
- tsc web 全绿 + 人工 HV 拓扑保存/编辑/发现无回归
- FE-03 Success Criteria #3 满足（回调读最新值，无 stale closure）
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-refactor-types/05-02-SUMMARY.md`
</output>
