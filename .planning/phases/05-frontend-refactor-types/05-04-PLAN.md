---
phase: 05-frontend-refactor-types
plan: 04
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/pages/kb/imageCache.ts
  - src/components/pages/KnowledgeBasePage.tsx
autonomous: false
requirements: [FE-02, FE-04]

must_haves:
  truths:
    - "KnowledgeBasePage 17 处 any 收敛（含 ChunkContent images:any[] → KbImage[]、documents/devices/searchResults/detailDoc/chunk 等用 src/types DTO）"
    - "新建 src/components/pages/kb/imageCache.ts 模块级 LRU + in-flight 去重 + AbortSignal 支持"
    - "ChunkContent 图片加载 effect 改用 AbortController（cleanup 调 controller.abort()，替代 cancelled 标志位）"
    - "ChunkContent 调用 getImage(path, signal) 走缓存层（缓存命中→in-flight 复用→否则 IPC 并入缓存）"
    - "FE-02 类型化先于 FE-04 缓存（同文件串行，FE-04 用 FE-02 的 KbImage 类型）"
    - "tsc -p tsconfig.web.json exit 0；KB 图片加载/切换/卸载人工 HV 无回归"
  artifacts:
    - path: "src/components/pages/kb/imageCache.ts"
      provides: "模块级 LRU 图片缓存 + in-flight 去重 + AbortSignal 取消"
      contains: "export async function getImage"
    - path: "src/components/pages/KnowledgeBasePage.tsx"
      provides: "强类型 + 缓存化 KB 页"
      contains: "AbortController"
  key_links:
    - from: "src/components/pages/KnowledgeBasePage.tsx ChunkContent"
      to: "src/components/pages/kb/imageCache.ts"
      via: "import { getImage }"
      pattern: "getImage"
    - from: "src/components/pages/KnowledgeBasePage.tsx ChunkContent"
      to: "src/types/kb.ts"
      via: "import type { KbImage }"
      pattern: "KbImage"
    - from: "ChunkContent useEffect"
      to: "AbortController.abort()"
      via: "cleanup 调 controller.abort()"
      pattern: "AbortController|controller.abort"
---

<objective>
FE-02 KB 类型化 + FE-04 ChunkContent 取消与缓存（同文件 KnowledgeBasePage.tsx 串行，单 plan 内 Task 1 类型化先、Task 2 缓存后）。

Purpose: TD-1 KB 页 17 处 any 收敛（含 ChunkContent `images: any[]` → `KbImage[]`）+ FRAG-2 图片加载失败静默改善。FE-04 用模块级 LRU（非 per-instance，D-5-6：ChunkContent 频繁 re-mount）+ in-flight 去重 + 客户端 AbortController（D-5-5：better-sqlite3 同步读不可真中断，AbortController 落地为结构化取消标志 + 卸载防 setState + 配合 in-flight 去重）。**不改 IPC `kb:getImageData` 签名**（Phase 4 刚稳定 IPC 契约）。

Output: 新建 imageCache.ts + KnowledgeBasePage.tsx 改造。autonomous=false 因无前端测试基建（D-5-7），须人工 HV 图片加载/切换/卸载。**依赖 05-01**：KbImage/KbDocument/KbChunk 等 DTO 由 05-01 src/types/kb.ts 提供。**同文件串行**：FE-02 类型化（Task 1）→ FE-04 缓存（Task 2），禁止并行。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-refactor-types/05-CONTEXT.md
@.planning/phases/05-frontend-refactor-types/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/05-frontend-refactor-types/05-01-SUMMARY.md

@src/components/pages/KnowledgeBasePage.tsx
@src/types/kb.ts
@src/types/electron.d.ts
@electron/services/ouiService.ts
</context>

<interfaces>
<!-- 05-01 已建立的 kb DTO（本 plan 复用，依赖 05-01 完成） -->
From src/types/kb.ts (05-01 新建):
```typescript
export interface KbImage { id: string; file_path: string; chunk_id?: string | null }
export interface KbChunk { id: string; doc_id: string; title: string; content: string; image_ids?: string[] | string | null; created_at?: string }
export interface KbDocument { id: string; filename: string; ... chunks?: KbChunk[] }
export interface KbStatus { status: string; progress?: number; ... }
export interface KbSearchResult { docId: string; docTitle: string; chunkId: string; chunkTitle: string; content: string; score?: number }
```
From src/types/electron.d.ts (05-01 建模后):
```typescript
kb.getImageData: (imagePath: string) => Promise<string | null>
kb.listDocuments: (deviceId?: string, category?: string) => Promise<KbDocument[]>
kb.getDocument: (docId: string) => Promise<KbDocument | null>
kb.search: (query: string, deviceIds?: string[], topK?: number) => Promise<KbSearchResult[]>
```

<!-- ChunkContent 现状（KnowledgeBasePage.tsx:31-45，逐字，FE-02+FE-04 改造对象） -->
```tsx
31  function ChunkContent({ content, images }: { content: string; images: any[] })  // ← images: any[] → KbImage[]
35    useEffect(() => {
36      if (!images || images.length === 0) return
37      let cancelled = false                                                  // ← FE-04 改 AbortController
38      Promise.all(images.map(async (img: any) => {                           // ← img: any → KbImage
39        try {
40          const data = await window.api.kb.getImageData(img.file_path)       // ← FE-04 改 getImage(img.file_path, signal)
41          if (!cancelled && data) setImgDataMap(prev => ({ ...prev, [img.id]: data }))
42        } catch { /* ignore */ }                                             // ← FRAG-2 顺带：失败 fallback 占位
43      }))
44      return () => { cancelled = true }                                      // ← FE-04 改 controller.abort()
45    }, [images])
```

<!-- 模块级缓存先例（ouiService.ts:4-6,43-56，FE-04 模式照搬） -->
private static vendorMap: Map | null (null=未预载/失败优雅降级) + getVendor 读 Map 或回退查库
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: KnowledgeBasePage FE-02 类型化（17 处 any 收敛，含 ChunkContent images → KbImage[]）</name>
  <files>src/components/pages/KnowledgeBasePage.tsx</files>
  <read_first>
    - Task 1 前置：05-01 完成后的 src/types/kb.ts（KbImage/KbDocument/KbChunk/KbStatus/KbSearchResult）+ src/types/electron.d.ts（kb.* 通道已建模）+ src/types/device.ts（Device，listDocuments 过滤用）
    - src/components/pages/KnowledgeBasePage.tsx（基线 16 处 any，PATTERNS §10 逐字点位 line 31/38/89/91/95/97/114/115/123/179/211/245/385/437/473 等）
    - electron/services/knowledgeBaseService.ts（DB row 字段取证，确认 KbChunk.image_ids 实际形态——string JSON 还是 string[]——以 KB 页 line 245 `chunks.filter((c:any)=>selectedChunks.includes(c.id))` 消费面微调 DTO）
    - 05-PATTERNS.md §10（KB any 点位 + 根因：electron.d.ts kb.* 通道全 Promise<any>，FE-02 先建模后此处收敛）+ §6（ChunkContent 改造点）
    - 05-CONTEXT.md D-5-2/D-5-3（FE-02 范围、复用 DTO 不内联）
  </read_first>
  <action>
    **本 task 仅做类型化（FE-02），不动 ChunkContent 图片加载 effect 的取消逻辑（FE-04 归 Task 2）。** 即 Task 1 完成后 ChunkContent 的 effect 仍保留 `let cancelled = false` + `window.api.kb.getImageData` 直调，仅 `images: any[]`/`img: any` 收为 KbImage。

    **(1) import 类型（顶部）：**
    ```typescript
    import type { KbDocument, KbChunk, KbImage, KbStatus, KbSearchResult } from '@/types/kb'
    import type { Device } from '@/types/device'
    ```

    **(2) ChunkContent 签名（line 31）：**
    `function ChunkContent({ content, images }: { content: string; images: any[] })` → `{ content: string; images: KbImage[] }`

    **(3) 状态收（line 89/91/95/97）：**
    - `useState<any[]>(documents)` → `useState<KbDocument[]>([])`
    - `useState<any[]>(devices)` → `useState<Device[]>([])`
    - `useState<any[]>(searchResults)` → `useState<KbSearchResult[]>([])`
    - `useState<any>(detailDoc)` → `useState<KbDocument | null>(null)`

    **(4) 调用面去 as any（line 114/179/123）：**
    - `setDocuments(list as any[])` → `setDocuments(list)`（kb.listDocuments 已返回 KbDocument[]）
    - `setSearchResults(results as any[])` → `setSearchResults(results)`（kb.search 已返回 KbSearchResult[]）
    - `window.api.device.list().then(list => setDevices(list as any[]))` → `.then(list => setDevices(list))`（device.list 已返回 Device[]），`.catch(() => {})` 保留（FRAG-2 line 123 静默属另一改善，本 task 可加 `console.warn('[kb] device.list 失败')` 但非强制）

    **(5) chunk 类型收（line 211/245/473）：**
    - `const startEdit = (chunk: any)` → `(chunk: KbChunk)`
    - `detailDoc.chunks.filter((c: any) => ...)` → `(c: KbChunk)`，detailDoc 已 KbDocument|null，须先窄化 `if (!detailDoc) return` 或 `detailDoc?.chunks?.filter(...)`（detailDoc 为 null 时 chunks 不存在）
    - `detailDoc.chunks?.map((c: any) => ...)` → `(c: KbChunk)`

    **(6) 渲染面去 any（line 385/437）：**
    - `devices.map((d: any) => ({ value: d.id, label: d.name }))` → `(d: Device)`（去标注，Device 已强类型）或直接去 `(d)`
    - `searchResults.map((r: any, i: number) => ...)` → `(r: KbSearchResult, i: number)`

    **(7) catch 收（多处 catch (e: any)）：**
    - 全部 `catch (e: any)` → `catch (e: unknown)`，`e.message` 窄化为 `(e instanceof Error ? e.message : String(e))`（PATTERNS §Shared Patterns 错误处理，全仓库统一）

    **(8) ChunkContent effect（line 35-45）仅收 img 类型，不动取消逻辑：**
    - `Promise.all(images.map(async (img: any) => {` → `async (img: KbImage)`（保留 `let cancelled = false` + `window.api.kb.getImageData(img.file_path)` 直调 + `catch { /* ignore */ }`，Task 2 接力改造）
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "=== KB any 残留（before 16，FE-02 后应 ≤ 2 仅剩 ChunkContent img 取消相关待 Task 2） ===" && grep -v '^//' src/components/pages/KnowledgeBasePage.tsx | grep -c ": any\|as any\|: any\[\]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/components/pages/KnowledgeBasePage.tsx | grep -c ": any\|as any\|: any\[\]"` 输出 ≤ 1（before 16；残余若仅为 Task 2 待改的 img 取消相关 `any`，可接受，Task 2 后归零）
    - `grep "KbDocument\|KbChunk\|KbImage\|KbSearchResult" src/components/pages/KnowledgeBasePage.tsx` 命中（DTO 引用）
    - `grep "images: KbImage\[\]" src/components/pages/KnowledgeBasePage.tsx` 命中（ChunkContent 签名收）
    - `grep "catch (e: unknown)" src/components/pages/KnowledgeBasePage.tsx` 命中（catch 收敛）
    - `grep "as any\[\]" src/components/pages/KnowledgeBasePage.tsx` 无命中（list as any[] 全去）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
  </acceptance_criteria>
  <done>KB 页 17 处 any 收敛（ChunkContent images→KbImage[]、documents/devices/searchResults/detailDoc/chunk 用 src/types DTO、catch 收 unknown）；仅 ChunkContent 图片 effect 的取消逻辑待 Task 2；tsc web 全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 新建 imageCache.ts + ChunkContent 改 AbortController + 缓存</name>
  <files>src/components/pages/kb/imageCache.ts, src/components/pages/KnowledgeBasePage.tsx</files>
  <read_first>
    - Task 1 完成后的 src/components/pages/KnowledgeBasePage.tsx（ChunkContent effect 当前为 cancelled 标志位 + 直调 IPC，line 35-45 待改造）
    - src/types/kb.ts（KbImage 用于 img 类型，Task 1 已引）
    - electron/services/ouiService.ts（line 4-6,28-40,43-56 模块级 vendorMap 缓存 + 优雅降级 + 回退查库模式，FE-04 照搬为 renderer file-scope Map）
    - 05-PATTERNS.md §6（imageCache 决策约束 D-5-5/D-5-6 + API 形态建议 + ChunkContent 改造点逐字）+ §Shared Patterns（模块级缓存 + 优雅降级）
    - 05-CONTEXT.md D-5-5/D-5-6（AbortController 客户端语义、模块级 LRU 非 per-instance、不改 IPC）
  </read_first>
  <action>
    **(1) 新建 src/components/pages/kb/imageCache.ts**（模块级 LRU + in-flight 去重 + AbortSignal）：
    ```typescript
    // FE-04 (D-5-6): 模块级 LRU 缓存（非 per-instance），ChunkContent 频繁 re-mount 时跨实例复用，
    // 存活卸载。keyed by file_path → base64 data url。LRU 按 count 有界。
    // FE-04 (D-5-5): in-flight 去重 Map，同图并发请求复用同一 Promise（chunk 搜索切换/编辑去重）。
    // 客户端 AbortController：better-sqlite3 同步读不可真中断，AbortController 落地为
    // 结构化取消标志 + 卸载防 setState + 配合 in-flight 去重。

    const CACHE_MAX_ENTRIES = 100  // LRU 容量阈值（Claude's Discretion，D-5-6 count vs bytes 选 count）

    const cache = new Map<string, string>()                  // LRU（Map 保持插入顺序，淘汰最老）
    const inFlight = new Map<string, Promise<string>>()      // in-flight 去重

    /**
     * 取 KB 图片数据。优先读缓存 → 复用 in-flight → 否则调 IPC 并入缓存。
     * signal.aborted 时抛 AbortError（调用方 catch 忽略，不再 setState）。
     */
    export async function getImage(path: string, signal: AbortSignal): Promise<string> {
      // 1. 缓存命中
      const cached = cache.get(path)
      if (cached !== undefined) return cached

      // 2. in-flight 复用（同图并发请求）
      const existing = inFlight.get(path)
      if (existing) return existing

      // 3. 发起新请求
      const p = (async () => {
        try {
          const data = await window.api.kb.getImageData(path)
          if (data) {
            // LRU 淘汰：超容量删最老（Map 迭代顺序 = 插入顺序）
            if (cache.size >= CACHE_MAX_ENTRIES) {
              const oldest = cache.keys().next().value
              if (oldest !== undefined) cache.delete(oldest)
            }
            cache.set(path, data)
            return data
          }
          throw new Error('图片数据为空')
        } finally {
          inFlight.delete(path)   // 请求结束（成功/失败）清 in-flight
        }
      })()
      inFlight.set(path, p)

      // 4. 支持 abort：signal.aborted 后续 setState 被调用方守卫（本函数仍返回 Promise，
      //    由调用方在 then 前判 signal.aborted；better-sqlite3 同步 IPC 无法真中断）
      const onAbort = () => { /* 标志位由调用方读 signal.aborted，此处无需清 in-flight（共享请求） */ }
      if (signal.aborted) onAbort()
      else signal.addEventListener('abort', onAbort, { once: true })

      return p
    }

    /** 测试/切换场景可手动清缓存（可选 export，Claude's Discretion） */
    export function clearImageCache(): void { cache.clear(); inFlight.clear() }
    ```
    > **设计要点：** in-flight 共享 Promise 不因单个调用方 abort 而取消（其他调用方仍待结果）；abort 语义由调用方在 `.then` 前判 `signal.aborted` 决定是否 setState（见 ChunkContent 改造）。LRU 用 Map 插入顺序淘汰最老（O(1)）。cache 命中返回 `data !== undefined` 判定（空字符串图片数据理论不存在，getImageData 返回 string|null）。

    **(2) 改造 ChunkContent effect（KnowledgeBasePage.tsx:35-45）：**
    ```tsx
    useEffect(() => {
      if (!images || images.length === 0) return
      // FE-04 (D-5-5): AbortController 替代 cancelled 标志位，结构化取消
      const controller = new AbortController()
      const signal = controller.signal
      Promise.all(
        images.map(async (img: KbImage) => {
          try {
            const data = await getImage(img.file_path, signal)
            // 卸载/切换后 abort → 不 setState
            if (!signal.aborted && data) {
              setImgDataMap((prev) => ({ ...prev, [img.id]: data }))
            }
          } catch {
            // FRAG-2 顺带：图片失败 fallback 占位（planner 裁量，D-5-5 deferred TTL/进度；
            // 此处可 console.warn('[kb] 图片加载失败:', img.file_path) 提供反馈，非强制 UI 占位）
            console.warn('[kb] 图片加载失败:', img.file_path)
          }
        })
      )
      return () => { controller.abort() }   // 卸载/切换取消在途（结构化）
    }, [images])
    ```
    - 顶部加 `import { getImage } from './kb/imageCache'`（若 KnowledgeBasePage.tsx 在 src/components/pages/，imageCache 在 src/components/pages/kb/，相对路径 `./kb/imageCache`）。
    - `let cancelled = false` + `return () => { cancelled = true }` 删除，改 controller.abort()。
    - `window.api.kb.getImageData(img.file_path)` 直调改 `getImage(img.file_path, signal)`。

    **(3) 不动的部分：** `kb.getImageData` IPC 通道签名不动（D-5-5 红线，Phase 4 刚稳定）；preload.ts/electron.d.ts 不动（05-01 已建模 getImageData: Promise<string|null>）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "=== imageCache 模块 ===" && grep -c "export async function getImage\|const cache\|const inFlight" src/components/pages/kb/imageCache.ts && echo "=== ChunkContent AbortController 命中 ===" && grep -c "AbortController\|controller.abort\|getImage(" src/components/pages/KnowledgeBasePage.tsx && echo "=== KB any 最终（应 0） ===" && grep -v '^//' src/components/pages/KnowledgeBasePage.tsx | grep -c ": any\|as any\|: any\[\]"</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/pages/kb/imageCache.ts` 存在
    - `grep "export async function getImage" src/components/pages/kb/imageCache.ts` 命中
    - `grep "const cache = new Map" src/components/pages/kb/imageCache.ts` 命中（模块级 LRU）
    - `grep "const inFlight = new Map" src/components/pages/kb/imageCache.ts` 命中（in-flight 去重）
    - `grep "AbortController" src/components/pages/KnowledgeBasePage.tsx` 命中（ChunkContent 结构化取消）
    - `grep "controller.abort()" src/components/pages/KnowledgeBasePage.tsx` 命中（cleanup 调 abort，SC #4 grep 点）
    - `grep "getImage(" src/components/pages/KnowledgeBasePage.tsx` 命中（走缓存层，非直调 IPC）
    - `grep "let cancelled = false" src/components/pages/KnowledgeBasePage.tsx` 无命中（旧标志位已删）
    - `grep -v '^//' src/components/pages/KnowledgeBasePage.tsx | grep -c ": any\|as any\|: any\[\]"` 输出 0（FE-02 + FE-04 后 KB any 全清）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
  </acceptance_criteria>
  <done>imageCache.ts 建立（模块级 LRU + in-flight 去重 + AbortSignal）；ChunkContent 改 AbortController + getImage 缓存层；KB any 全清；kb.getImageData IPC 签名不动；tsc web 全绿。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    KB 页 FE-02 类型化（17 处 any → 0）+ FE-04 ChunkContent 改 AbortController + 模块级 LRU 图片缓存 + in-flight 去重。无前端测试基建（D-5-7），须 Electron 运行时人工验证。
  </what-built>
  <how-to-verify>
    启动 app，进入「知识库」页：
    1. **文档列表加载**：列表显示已上传文档（KbDocument[] 类型化后渲染正确，无类型回归）。
    2. **文档详情 chunks**：点开文档 → 显示 chunks 列表（detailDoc.chunks KbChunk[] 渲染，含图片的 chunk 显示图片）。
    3. **图片首次加载**：含图片的 chunk 首次打开 → 图片显示（getImage IPC 调用 + 入缓存）；DevTools Network/Console 观察首次有 kb:getImageData IPC。
    4. **图片缓存命中（同图）**：同一 chunk 关闭再展开，或搜索切换回同一 chunk → 图片立即显示，**无重复 kb:getImageData IPC**（模块级缓存命中，跨 ChunkContent 实例）。
    5. **in-flight 去重**：快速连续展开多个含**相同图片**的 chunk（若存在）→ 同一 file_path 仅 1 次 IPC（in-flight Promise 复用）。
    6. **卸载取消**：展开含图片的 chunk，图片加载中途立即关闭/切换到其他文档 → DevTools Console 无「setState on unmounted」React 警告（AbortController.abort() 生效）；无错误冒泡。
    7. **图片加载失败**：人为破坏图片路径（或删除图片文件）→ Console 有 `[kb] 图片加载失败` warn（FRAG-2 改善，不再完全静默），UI 不崩。
    8. **搜索**：搜索关键词 → searchResults（KbSearchResult[]）渲染正确。
    9. **chunk 编辑/拆分/合并**：startEdit/splitChunk/mergeChunks 操作（KbChunk 类型化后无回归）。
    10. **无 console 报错**：DevTools Console 全程无 React 警告/报错。
  </how-to-verify>
  <resume-signal>Type "approved"（10 项全过）或描述哪项回归</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-local（KB 缓存） | 模块级缓存仅在渲染进程内存，不持久化、不跨进程；IPC kb:getImageData 既有，签名不变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | imageCache 存储内容 | accept | 缓存存图片 base64（文档内容，非凭证/密钥）；KbImage/图片数据为知识库文档附件，低敏；不上传、不持久化、不外发。无凭证入缓存（声明）。 |
| T-05-04-02 | Denial of Service | 模块级 LRU 无界增长 | mitigate | CACHE_MAX_ENTRIES=100 上限 + Map 插入顺序淘汰最老（O(1)）；in-flight Map 在 Promise finally 清除，不泄漏。 |
| T-05-04-03 | Tampering | AbortController 取消语义 | accept | better-sqlite3 同步读不可真中断（D-5-5 已声明）；AbortController 落地为结构化取消标志 + 卸载防 setState，IPC 仍执行完但结果被丢弃；不改 IPC 签名（Phase 4 契约稳定）。 |
| T-05-04-04 | Repudiation | FE-02 类型化削弱运行时校验 | accept | 类型收窄不改 IPC handler 运行时行为；kb.* handler 经 secure() 包装（CONCERNS SEC-1），路径遍历/MIME 防护（SEC-5）在主进程侧不变。 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals，FE-02/04 验收红线）
- `npx vitest run` exit 0（既有测试不回归）
- `npx esbuild ... --bundle`（主进程打包不回归，KB 改动 + imageCache 新建不涉 main）
- KB 页 any 清零（before 16 → 0）
- imageCache.ts grep 命中（getImage + cache + inFlight）
- ChunkContent grep AbortController + controller.abort + getImage 命中（SC #4）
- kb.getImageData IPC 签名不动（grep electron.d.ts/preload.ts 无 kb 通道改动）
- 人工 HV 10 项全过（图片加载/缓存/取消/失败/搜索/编辑）
</verification>

<success_criteria>
- KB 页 17 处 any 全清（FE-02，含 ChunkContent images→KbImage[]，D-5-2）
- imageCache.ts 建立（模块级 LRU + in-flight 去重 + AbortSignal，D-5-5/D-5-6）
- ChunkContent 改 AbortController + 走缓存层（FE-04 SC #4：grep 到 AbortController + abort 调用）
- kb.getImageData IPC 签名不动（D-5-5 红线）
- tsc web 全绿 + 人工 HV 无回归
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-refactor-types/05-04-SUMMARY.md`
</output>
