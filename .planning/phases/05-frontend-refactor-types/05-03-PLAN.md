---
phase: 05-frontend-refactor-types
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/pages/AIPage.tsx
  - src/components/pages/ai/useAIChat.ts
  - src/components/pages/ai/types.ts
  - src/components/pages/ai/ChatSessionList.tsx
  - src/components/pages/ai/ChatMessageList.tsx
  - src/components/pages/ai/ChatInput.tsx
  - src/components/pages/ai/CommandConfirmModal.tsx
autonomous: false
requirements: [FE-01]

must_haves:
  truths:
    - "AIPage 拆分为 4 个独立子组件文件（ChatSessionList/ChatMessageList/ChatInput/CommandConfirmModal）+ 1 个 useAIChat 自定义 hook"
    - "useAIChat hook 持有原 AIPage 的 10+ state 与 7 handler，返回 typed contract（不用 zustand、不用 prop drilling）"
    - "AIPage.tsx 退化为薄编排层（configLoading/hasConfig 守卫 + 调用 useAIChat + 渲染 4 子组件）"
    - "AIPage 原 4 处 any（line 60/61/160/175）在 hook 提取时顺带收敛"
    - "4 子组件 props 强类型化（FE-02 协同，依赖 05-01 的 ChatMessage 联合类型）"
    - "tsc -p tsconfig.web.json exit 0；AIPage 4 子组件交互人工 HV 无回归"
  artifacts:
    - path: "src/components/pages/ai/useAIChat.ts"
      provides: "AI 会话态自定义 hook（page-local，返回 typed contract）"
      contains: "export function useAIChat"
    - path: "src/components/pages/ai/types.ts"
      provides: "AI 子树本地类型（DeviceOption/ChatMsg/ConfirmData）"
      contains: "interface ConfirmData"
    - path: "src/components/pages/ai/ChatSessionList.tsx"
      provides: "会话列表子组件"
      contains: "export default function ChatSessionList"
    - path: "src/components/pages/ai/ChatMessageList.tsx"
      provides: "消息列表子组件（含滚动 effect）"
      contains: "export default function ChatMessageList"
    - path: "src/components/pages/ai/ChatInput.tsx"
      provides: "输入框子组件"
      contains: "export default function ChatInput"
    - path: "src/components/pages/ai/CommandConfirmModal.tsx"
      provides: "命令确认弹窗子组件"
      contains: "export default function CommandConfirmModal"
    - path: "src/components/pages/AIPage.tsx"
      provides: "薄编排层 AIPage"
      contains: "useAIChat"
  key_links:
    - from: "src/components/pages/AIPage.tsx"
      to: "src/components/pages/ai/useAIChat.ts"
      via: "import { useAIChat }"
      pattern: "useAIChat"
    - from: "src/components/pages/ai/ChatSessionList.tsx"
      to: "src/components/pages/ai/types.ts"
      via: "import type ChatSession"
      pattern: "ChatSession"
---

<objective>
FE-01 AIPage 拆分：399 行 → 4 子组件文件 + 1 个 useAIChat 自定义 hook + 本地 types.ts，AIPage 退化为薄编排层。顺带收敛 AIPage 4 处 any（D-5-2：AIPage 由 FE-01 独占，FE-02 不触碰）。

Purpose: TD-2 单文件 399 行 / 16 符号 / 10+ 共享 state 违反单一职责；hook 提取使状态边界清晰、子组件可独立复用。**不用 zustand**（AI 会话态 page-local，引入 aiChatStore 全局单例无收益且污染全局命名空间）、**不用 prop drilling**（10+ state + 6 handler 宽 prop 面）——hook 契约即类型边界（D-5-1）。

Output: 6 新文件（4 子组件 + useAIChat + types）+ AIPage.tsx 重构。autonomous=false 因 FE-01 无前端测试基建（D-5-7），须人工 HV 4 子组件交互。**依赖 05-01**：ChatMessage role 收为 'user'|'assistant' 联合类型后，hook 返回类型才强类型化。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-refactor-types/05-CONTEXT.md
@.planning/phases/05-frontend-refactor-types/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/05-frontend-refactor-types/05-01-SUMMARY.md

@src/components/pages/AIPage.tsx
@src/stores/authStore.ts
@src/types/ai.ts
@src/types/electron.d.ts
</context>

<interfaces>
<!-- 05-01 已建立的 ai 通道类型（本 plan 复用，依赖 05-01 完成） -->
From src/types/ai.ts (05-01 新建):
```typescript
export interface ChatMessage {
  id: string
  role: 'user' | 'assistant'   // ← 已收为联合类型（05-01 Task 1）
  content: string
  deviceId: string | null
  createdAt: string
}
export interface ChatSession { id: string; title: string; deviceId: string | null; createdAt: string }
```
From src/types/electron.d.ts (05-01 建模后):
```typescript
ai.chat: (messages: Array<{ role: 'user'|'assistant'; content: string }>, deviceIds?: string[], sessionId?: string) => Promise<string>
ai.createSession: (title: string, deviceId?: string) => Promise<ChatSession>
ai.listSessions: () => Promise<ChatSession[]>
ai.getSessionMessages: (sessionId: string) => Promise<ChatMessage[]>
ai.confirmCommand: (execId: string, approved: boolean) => Promise<string>
device.list: () => Promise<Device[]>
```

<!-- AIPage 本地 interface（待迁移到 types.ts，PATTERNS §1 逐字 line 9/15/23） -->
```typescript
interface DeviceOption { id: string; name: string; connectionType: string }
interface ChatMsg { id?: string; role: 'user' | 'assistant'; content: string; createdAt?: string;
                   references?: Array<{ docTitle: string; chunkTitle: string; docId: string }> }
interface ConfirmData { type: 'confirm_required'; execId: string;
                        commands: Array<{ deviceName: string; command: string }>;
                        rejectedCommands?: Array<{ command: string; reason: string }>; aiExplanation: string }
```

<!-- 状态组织参照（authStore.ts:13-42，注意是「组织参照」非模式照搬，D-5-1 用 useState 不用 zustand） -->
typed state interface + action 方法签名 + window.api.* IPC 桥
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 新建 src/components/pages/ai/ 目录（types.ts + useAIChat.ts）</name>
  <files>src/components/pages/ai/types.ts, src/components/pages/ai/useAIChat.ts</files>
  <read_first>
    - src/components/pages/AIPage.tsx（重点 line 9-179：本地 interface + 10 state + 7 handler 逐字；line 142-159 handleSend 的 JSON.parse(reply) 分支 confirm_required/kb_answer/普通；line 60/61/160/175 四处 any）
    - src/types/ai.ts（05-01 建，ChatMessage/ChatSession 复用）
    - src/types/device.ts（Device DTO，filter ssh/telnet 用）
    - src/stores/authStore.ts（line 13-42 状态组织参照：typed state + action + window.api 桥）
    - 05-PATTERNS.md §1（useAIChat 决策约束 + 要提取的 state/handler 逐字 + FE-02 顺带收敛 any 点位）
    - 05-CONTEXT.md D-5-1（hook 决策、不用 zustand/prop drilling）
  </read_first>
  <action>
    **(1) 新建 src/components/pages/ai/types.ts**（迁移 AIPage 本地 interface，D-5-1 planner 裁量放此文件）：
    ```typescript
    import type { ChatMessage } from '@/types/ai'

    export interface DeviceOption {
      id: string
      name: string
      connectionType: string
    }

    // 渲染层消息（比 ChatMessage 多 references，handleSend 的 kb_answer 分支附加）
    export interface ChatMsg {
      id?: string
      role: 'user' | 'assistant'
      content: string
      createdAt?: string
      references?: Array<{ docTitle: string; chunkTitle: string; docId: string }>
    }

    export interface ConfirmData {
      type: 'confirm_required'
      execId: string
      commands: Array<{ deviceName: string; command: string }>
      rejectedCommands?: Array<{ command: string; reason: string }>
      aiExplanation: string
    }

    // useAIChat 返回契约（typed boundary，4 子组件经切片消费）
    export interface UseAIChatReturn {
      devices: DeviceOption[]
      selectedDevices: string[]
      sessions: ChatSessionLike[]
      currentSessionId: string | null
      messages: ChatMsg[]
      input: string
      loading: boolean
      pendingConfirm: ConfirmData | null
      setSelectedDevices: (ids: string[]) => void
      setInput: (v: string) => void
      loadData: () => Promise<void>
      handleNewSession: () => Promise<void>
      handleSelectSession: (id: string) => Promise<void>
      handleDeleteSession: (id: string) => Promise<void>
      handleSend: () => Promise<void>
      handleConfirm: (approved: boolean) => Promise<void>
    }
    ```
    > ChatSessionLike 复用 src/types/ai.ts 的 ChatSession，executor 直接 `import type { ChatSession } from '@/types/ai'` 在 UseAIChatReturn 用，无需 ChatSessionLike 别名（此处示意，executor 以实际为准）。Session 列表 title 编辑（updateSessionTitle）若 hook 暴露则补签名。

    **(2) 新建 src/components/pages/ai/useAIChat.ts**（提取 AIPage line 31-179 的 state + handler）：
    ```typescript
    import { useState, useCallback } from 'react'
    import type { ChatSession, ChatMessage } from '@/types/ai'
    import type { Device } from '@/types/device'
    import type { UseAIChatReturn, DeviceOption, ChatMsg, ConfirmData } from './types'

    export function useAIChat(): UseAIChatReturn {
      // 10 state（迁移自 AIPage.tsx:32-41）
      const [devices, setDevices] = useState<DeviceOption[]>([])
      const [selectedDevices, setSelectedDevices] = useState<string[]>([])
      const [sessions, setSessions] = useState<ChatSession[]>([])
      const [currentSessionId, setCurrentSessionId] = useState<string | null>(null)
      const [messages, setMessages] = useState<ChatMsg[]>([])
      const [input, setInput] = useState('')
      const [loading, setLoading] = useState(false)
      const [pendingConfirm, setPendingConfirm] = useState<ConfirmData | null>(null)

      // loadData（迁移自 AIPage.tsx:52，顺带收 line 60/61 的 (d: any)）
      // device.list() 现返回 Device[]（05-01 已建模），filter 不再需 (d: any)
      const loadData = useCallback(async () => {
        const list = await window.api.device.list()
        setDevices(
          list
            .filter((d) => d.connectionType === 'ssh' || d.connectionType === 'telnet')
            .map((d) => ({ id: d.id, name: d.name, connectionType: d.connectionType }))
        )
        const slist = await window.api.ai.listSessions()
        setSessions(slist)
      }, [])

      // loadSessions / handleNewSession / handleSelectSession / handleDeleteSession
      // 逐字迁移 AIPage.tsx:73-116，getSessionMessages 返回 ChatMessage[]（role 已联合类型，05-01），
      // setMessages(msgs.map((m: ChatMessage) => ({ ... }))) 不再需收 role（已收）
      // ...

      // handleSend（迁移自 AIPage.tsx:118-165，含 JSON.parse(reply) 分支）
      // catch (e: any) line 160 → catch (e: unknown) { message.error(e instanceof Error ? e.message : String(e)) }
      // ...

      // handleConfirm（迁移自 AIPage.tsx:167-179）
      // catch (e: any) line 175 → catch (e: unknown) + 窄化
      // ...

      return {
        devices, selectedDevices, sessions, currentSessionId, messages, input, loading, pendingConfirm,
        setSelectedDevices, setInput, loadData, handleNewSession, handleSelectSession,
        handleDeleteSession, handleSend, handleConfirm,
      }
    }
    ```
    > **关键：** hook 不持有 configLoading/hasConfig（这两个属 page 守卫，留 AIPage 编排层，PATTERNS §7 注）；hook 须暴露 loadData 供 AIPage 守卫通过后调用。handleNewSession 返回 session（AIPage.tsx:93）的语义：hook 内创建后 setCurrentSessionId + setMessages([])，调用方无需返回值——executor 以实际调用面微调。

    **(3) FE-02 顺带收敛（D-5-2，AIPage 由 FE-01 独占）：**
    - line 60/61 `(d: any)` → 由 Device[] 强类型 + filter 去标注（如上 loadData）
    - line 160/175 `catch (e: any)` → `catch (e: unknown)` + `(e instanceof Error ? e.message : String(e))`
    - line 101 `m: ChatMessage` 已声明，但 electron.d.ts ChatMessage.role 旧为 string——05-01 已收为联合类型，此处 `.map((m) => ({ role: m.role as 'user'|'assistant', ... }))` 不再需 `as`（已联合类型）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && ls src/components/pages/ai/ && grep -c "export function useAIChat" src/components/pages/ai/useAIChat.ts && grep -c ": any\|as any\|catch (e: any)" src/components/pages/ai/useAIChat.ts src/components/pages/ai/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/pages/ai/types.ts src/components/pages/ai/useAIChat.ts` 两文件存在
    - `grep "export function useAIChat" src/components/pages/ai/useAIChat.ts` 命中
    - `grep "export interface UseAIChatReturn" src/components/pages/ai/types.ts` 命中
    - `grep -c ": any\|as any\|catch (e: any)" src/components/pages/ai/useAIChat.ts` 输出 0（顺带收敛完成）
    - `grep "catch (e: unknown)" src/components/pages/ai/useAIChat.ts` 命中 ≥ 2（handleSend/handleConfirm）
    - `grep "Device\[\]\|device.list" src/components/pages/ai/useAIChat.ts` 命中（Device 强类型，去 (d:any)）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0（此时 AIPage.tsx 尚未重构，可能因 hook 未被引用产生未用 import——Task 2 接力重构 AIPage 后即消；若 tsc 报 hook 未用，executor 须两 task 连续执行，本 verify 在 Task 2 后复跑）
  </acceptance_criteria>
  <done>ai/ 目录建立；types.ts 导出 UseAIChatReturn/DeviceOption/ChatMsg/ConfirmData；useAIChat.ts 实现 10 state + 7 handler，4 处 any 收敛，tsc 在 Task 2 后全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 4 子组件文件 + AIPage 重构为薄编排层</name>
  <files>src/components/pages/ai/ChatSessionList.tsx, src/components/pages/ai/ChatMessageList.tsx, src/components/pages/ai/ChatInput.tsx, src/components/pages/ai/CommandConfirmModal.tsx, src/components/pages/AIPage.tsx</files>
  <read_first>
    - Task 1 完成后的 src/components/pages/ai/{types,useAIChat}.ts
    - src/components/pages/AIPage.tsx（重点 line 42-46 chatEndRef effect、line 181-194 守卫、line 198-396 待提取的 4 段 JSX）
    - src/components/ip-management/ArpTab.tsx line 5（interface XxxProps 形态参照，但不用 api:any）
    - src/components/topology/AddDeviceModal.tsx 或 ConnectionModal.tsx（既有独立 Modal 文件先例，CommandConfirmModal 参照其 open/onConfirm/onCancel 契约 + default export）
    - src/components/pages/KnowledgeBasePage.tsx line 31-86 ChunkContent（同类「props in + 内部 effect + 纯渲染」展示型组件参照，ChatMessageList 结构参照）
    - 05-PATTERNS.md §2-5（4 子组件逐字 JSX 摘录 line 198-240/259-324/326-350/353-396）+ §7（AIPage 守卫保留逻辑）
    - 05-CONTEXT.md D-5-1（子组件须各自独立文件，success criteria #1 验收点）
  </read_first>
  <action>
    **(1) ChatSessionList.tsx**（render-only，迁移 AIPage.tsx:198-240）：
    ```typescript
    import { Button } from 'antd'
    import { PlusOutlined, DeleteOutlined } from '@ant-design/icons'
    import type { ChatSession } from '@/types/ai'

    interface ChatSessionListProps {
      sessions: ChatSession[]
      currentSessionId: string | null
      onSelect: (id: string) => void
      onNew: () => void
      onDelete: (id: string) => void
    }

    export default function ChatSessionList({ sessions, currentSessionId, onSelect, onNew, onDelete }: ChatSessionListProps) {
      // 逐字迁移 AIPage.tsx:199-240 的 JSX（外层 div width:220 + 新建会话 Button + sessions.map）
      // handleSelectSession → onSelect，handleNewSession → onNew，handleDeleteSession → onDelete
    }
    ```

    **(2) ChatMessageList.tsx**（render-only，迁移 AIPage.tsx:42-46 effect + 259-324 JSX）：
    ```typescript
    import { useRef, useEffect } from 'react'
    import { RobotOutlined } from '@ant-design/icons'
    import type { ChatMsg } from './types'

    interface ChatMessageListProps {
      messages: ChatMsg[]
      loading: boolean
    }

    export default function ChatMessageList({ messages, loading }: ChatMessageListProps) {
      const chatEndRef = useRef<HTMLDivElement>(null)
      useEffect(() => { chatEndRef.current?.scrollIntoView({ behavior: 'smooth' }) }, [messages])
      // 逐字迁移 AIPage.tsx:260-324（空态 RobotOutlined、消息气泡按 msg.role、references、loading 思考中、<div ref={chatEndRef} />）
    }
    ```

    **(3) ChatInput.tsx**（event-driven，迁移 AIPage.tsx:326-350）：
    ```typescript
    import { Input, Button } from 'antd'
    import { SendOutlined } from '@ant-design/icons'

    interface ChatInputProps {
      value: string
      loading: boolean
      onChange: (v: string) => void
      onSend: () => void
    }

    export default function ChatInput({ value, loading, onChange, onSend }: ChatInputProps) {
      // 逐字迁移 AIPage.tsx:327-350（TextArea + 发送 Button，onPressEnter shiftKey 判断）
      // setInput → onChange，handleSend → onSend
    }
    ```

    **(4) CommandConfirmModal.tsx**（modal，迁移 AIPage.tsx:353-396）：
    ```typescript
    import { Modal, Tag, Typography } from 'antd'
    import type { ConfirmData } from './types'

    interface CommandConfirmModalProps {
      pendingConfirm: ConfirmData | null
      onConfirm: (approved: boolean) => void
    }

    export default function CommandConfirmModal({ pendingConfirm, onConfirm }: CommandConfirmModalProps) {
      // 逐字迁移 AIPage.tsx:354-396（Modal 标题/footer + 命令 Tag 列表 + rejectedCommands + aiExplanation）
      // open={!!pendingConfirm}，onOk → onConfirm(true)，onCancel → onConfirm(false)
    }
    ```

    **(5) AIPage.tsx 重构为薄编排层**（保留 line 181-194 守卫 + configLoading/hasConfig）：
    ```typescript
    import { useState, useEffect } from 'react'
    import { Spin, ExclamationCircleOutlined } from 'antd'
    import { useAIChat } from './ai/useAIChat'
    import ChatSessionList from './ai/ChatSessionList'
    import ChatMessageList from './ai/ChatMessageList'
    import ChatInput from './ai/ChatInput'
    import CommandConfirmModal from './ai/CommandConfirmModal'

    export default function AIPage() {
      // configLoading/hasConfig 留编排层（page 守卫，PATTERNS §7）
      const [configLoading, setConfigLoading] = useState(true)
      const [hasConfig, setHasConfig] = useState(false)

      const chat = useAIChat()

      useEffect(() => {
        (async () => {
          try {
            const cfg = await window.api.ai.getConfig()
            setHasConfig(!!cfg)
            if (cfg) await chat.loadData()
          } catch (e: unknown) {
            console.error('[ai] loadConfig 失败:', e instanceof Error ? e.message : String(e))
          } finally {
            setConfigLoading(false)
          }
        })()
      }, [])  // chat.loadData 稳定（useCallback deps []），可不入 deps；若 eslint/tsc 警告可加 chat.loadData

      if (configLoading) return <div style={{ textAlign: 'center', paddingTop: 100 }}><Spin size="large" /></div>
      if (!hasConfig) return (/* 逐字 line 185-194 占位 JSX */)

      return (
        <div style={{ display: 'flex', height: '100%' }}>
          <ChatSessionList
            sessions={chat.sessions}
            currentSessionId={chat.currentSessionId}
            onSelect={chat.handleSelectSession}
            onNew={chat.handleNewSession}
            onDelete={chat.handleDeleteSession}
          />
          <div style={{ flex: 1, display: 'flex', flexDirection: 'column' }}>
            <ChatMessageList messages={chat.messages} loading={chat.loading} />
            <ChatInput value={chat.input} loading={chat.loading}
                       onChange={chat.setInput} onSend={chat.handleSend} />
          </div>
          <CommandConfirmModal pendingConfirm={chat.pendingConfirm} onConfirm={chat.handleConfirm} />
        </div>
      )
    }
    ```
    > 原 AIPage line 198-396 的设备选择器（selectedDevices 多选）/session 列表布局，若 selectedDevices 选择 UI 存在，executor 须确认其归属——建议留 ChatSessionList 内或 AIPage 编排层（Claude's Discretion，PATTERNS §1 注 hook 暴露 selectedDevices/setSelectedDevices）。设备选择器 JSX 若在原 AIPage 内联，可单独提为 ChatDeviceSelector 或并入 ChatSessionList——以原 AIPage 实际布局为准，不丢失功能。

    **原则：** 拆分须 byte-for-byte 保留原 AIPage 的渲染与交互（4 子组件 JSX 逐字迁移，仅将 handler 引用从闭包改为 prop）。AIPage.tsx 行数从 399 降至 ~60。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "=== 4 子组件文件 ===" && ls src/components/pages/ai/{ChatSessionList,ChatMessageList,ChatInput,CommandConfirmModal}.tsx && echo "=== AIPage 残余 any（before 4，应 0） ===" && grep -v '^//' src/components/pages/AIPage.tsx | grep -c ": any\|as any\|catch (e: any)" && echo "=== AIPage 行数（应大幅下降） ===" && wc -l src/components/pages/AIPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/components/pages/ai/ChatSessionList.tsx src/components/pages/ai/ChatMessageList.tsx src/components/pages/ai/ChatInput.tsx src/components/pages/ai/CommandConfirmModal.tsx` 4 文件全在
    - 4 子组件均 `export default function`：`grep "export default function" src/components/pages/ai/*.tsx` 命中 4 次
    - `grep "export function useAIChat" src/components/pages/ai/useAIChat.ts` 命中
    - `grep -v '^//' src/components/pages/AIPage.tsx | grep -c ": any\|as any\|catch (e: any)"` 输出 0（before 4，顺带收敛完成）
    - `grep "useAIChat" src/components/pages/AIPage.tsx` 命中（编排层调用 hook）
    - `wc -l src/components/pages/AIPage.tsx` 行数显著下降（399 → 约 60-80，编排层 + 守卫）
    - 4 子组件 props 无 `api: any`：`grep "api: any\|api:any" src/components/pages/ai/*.tsx` 无命中
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals 全绿）
  </acceptance_criteria>
  <done>4 子组件文件建立（各自 default export + 强类型 props）；AIPage 退化为薄编排层（~60 行，调 useAIChat + 渲染 4 子组件 + 守卫）；AIPage 4 处 any 清零；tsc web 全绿。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    AIPage 拆分为 4 子组件（ChatSessionList/ChatMessageList/ChatInput/CommandConfirmModal）+ useAIChat hook + types.ts，AIPage 退化为薄编排层。顺带收敛 AIPage 4 处 any。无前端组件测试基建（D-5-7），须 Electron 运行时人工冒烟。
  </what-built>
  <how-to-verify>
    启动 app，进入 AI 页（须先在「系统设置」配置 AI 服务参数，否则显示守卫占位）。已配置则进入会话界面：
    1. **会话列表**：右侧（或左侧，按原布局）显示会话列表；点「新建会话」→ 新会话高亮、消息区清空。
    2. **切换会话**：点不同会话 → currentSessionId 高亮变化、消息区加载该会话历史消息（getSessionMessages）。
    3. **删除会话**：点会话旁删除图标 → 会话从列表移除。
    4. **发消息**：输入框输入文字，Enter 发送（Shift+Enter 换行）→ 消息出现在列表、loading 思考中、AI 回复后追加；发送时空输入 disabled。
    5. **消息滚动**：多发几条消息超出可视区 → chatEndRef scrollIntoView 自动滚到底部。
    6. **命令确认弹窗**：触发需要 confirm 的 AI 命令（execId/commands）→ CommandConfirmModal 弹出，显示命令 Tag 列表 + aiExplanation；点确认/拒绝 → handleConfirm 流程正确。
    7. **references 显示**：若 AI 回复含 kb_answer 分支（references），消息气泡显示参考来源。
    8. **无 console 报错**：DevTools Console 无 React 警告/报错。
  </how-to-verify>
  <resume-signal>Type "approved"（8 项全过）或描述哪项交互回归</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer-local（AI 子树） | 本 plan 纯前端拆分，不新增 IPC/DB 调用；ai.chat/confirmCommand 既有，调用面不变 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | AIPage 拆分后交互语义 | mitigate | 4 子组件 JSX 逐字迁移（PATTERNS §2-5），handler 引用从闭包改 prop，行为 byte-for-byte 等价；HV 步骤 1-8 验证新建/切换/删除/发送/确认/references 全交互无回归。 |
| T-05-03-02 | Information Disclosure | handleSend/handleConfirm catch 收敛 | accept | `catch (e: unknown) + (e instanceof Error ? e.message : String(e))` 与仓库统一模式；IPC 层 sanitizeMessage 已脱敏（CONVENTIONS §2），渲染层只收 message。 |
| T-05-03-03 | Elevation of Privilege | useAIChat hook 状态边界 | accept | hook page-local（非全局 store），状态隔离于 AI 子树，无跨页面泄漏；与 D-5-1 决策一致（不污染全局命名空间）。 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals，FE-01 验收红线）
- `npx vitest run` exit 0（既有测试不回归）
- `npx esbuild ... --bundle`（主进程打包不回归，AIPage 改动不涉 main）
- 4 子组件文件 + useAIChat + types.ts 建立（glob 命中 6 文件）
- AIPage.tsx any 清零（before 4 → 0）+ 行数 399 → ~60
- 人工 HV 8 项全过（AI 4 子组件交互冒烟）
</verification>

<success_criteria>
- AIPage 拆分为 4 子组件文件 + useAIChat hook + types.ts（FE-01 SC #1）
- AIPage 退化为薄编排层（~60 行）
- useAIChat 返回 typed contract（不用 zustand、不用 prop drilling，D-5-1）
- AIPage 4 处 any 收敛（FE-02 顺带，D-5-2）
- tsc web 全绿 + 人工 HV 无回归
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-refactor-types/05-03-SUMMARY.md`
</output>
