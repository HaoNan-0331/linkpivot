---
phase: 05-frontend-refactor-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/electron.d.ts
  - src/types/kb.ts
  - src/types/ai.ts
  - src/types/device.ts
  - src/types/topology.ts
  - src/types/network.ts
  - src/types/anomaly.ts
  - src/types/oui.ts
  - src/components/ip-management/ArpTab.tsx
  - src/components/ip-management/AnomalyTab.tsx
  - src/components/ip-management/NetworkTab.tsx
  - src/components/ip-management/OuiTab.tsx
  - src/components/pages/SettingsPage.tsx
  - src/components/pages/DevicesPage.tsx
autonomous: true
requirements: [FE-02]

must_haves:
  truths:
    - "src/types/electron.d.ts 全部 Promise<any> 与裸 any 被替换为复用 src/types 的具体 DTO（device/topology/network/anomaly/oui/kb/ai）"
    - "ArpTab/AnomalyTab/NetworkTab/OuiTab 4 文件的 api:any props 与 useState<any*> 收敛为 ElectronAPI 切片 + src/types DTO"
    - "SettingsPage 删除 (window as any).api 绕过，schedulerConfig/schedulerStatus 收为 SchedulerStatus/ScheduleConfig 类型（electron.d.ts 新增 scheduler 通道建模）"
    - "DevicesPage 4 处 catch (e:any) 收为 catch (e:unknown) + 窄化"
    - "tsc -p tsconfig.web.json exit 0（strict + noUnusedLocals 全绿）"
  artifacts:
    - path: "src/types/electron.d.ts"
      provides: "ElectronAPI 强类型契约（消除 23 处 any，含新增 scheduler 通道）"
      contains: "scheduler:"
    - path: "src/types/kb.ts"
      provides: "KB DB 行 DTO（KbDocument/KbChunk/KbImage/KbStatus）"
      contains: "interface KbImage"
    - path: "src/types/ai.ts"
      provides: "AI 会话/消息/discovery Row DTO（ChatMessage role 收为联合类型、DiscoverResult）"
      contains: "interface ChatMessage"
    - path: "src/components/ip-management/ArpTab.tsx"
      provides: "ARP Tab 强类型 props/state"
    - path: "src/components/pages/SettingsPage.tsx"
      provides: "Settings 页强类型（无 (window as any).api）"
  key_links:
    - from: "src/components/ip-management/ArpTab.tsx"
      to: "src/types/electron.d.ts"
      via: "import type { ElectronAPI } / ARPEntry"
      pattern: "ElectronAPI|ARPEntry"
    - from: "src/components/pages/SettingsPage.tsx"
      to: "src/types/oui.ts"
      via: "import type { ScheduleConfig, SchedulerStatus }"
      pattern: "ScheduleConfig|SchedulerStatus"
    - from: "src/types/electron.d.ts"
      to: "src/types/kb.ts"
      via: "import type KbImage/KbDocument/..."
      pattern: "from './kb'"
---

<objective>
FE-02 类型契约 foundation：以 `src/types/electron.d.ts` 建模为锚，复用/补全 `src/types` DTO，收敛 4 个 IP Tab + SettingsPage + DevicesPage 的 `any`。

Purpose: electron.d.ts 是 `window.api` 的唯一类型契约源头（D-5-2/D-5-3）——先收它（一处修复多处受益），下游所有 call site 的 props/state/catch 才能引用真实 DTO。本 plan 是 FE-01（AIPage）、FE-04（KB 缓存）的类型前置依赖，故置于 Wave 1。

Output: electron.d.ts 全建模 + 新建 src/types/{kb,ai}.ts 补缺 DTO + 4 IP Tab + SettingsPage + DevicesPage 清理。**不触碰** AIPage.tsx（FE-01 独占）、KnowledgeBasePage.tsx（FE-02 KB 部分 + FE-04 同文件，归 05-04）、后端 services（milestone 外）、mock-api.ts（dev-only）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-refactor-types/05-CONTEXT.md
@.planning/phases/05-frontend-refactor-types/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

@src/types/electron.d.ts
@src/types/device.ts
@src/types/topology.ts
@src/types/network.ts
@src/types/arp.ts
@src/types/anomaly.ts
@src/types/oui.ts
@src/types/pagination.ts
@src/components/ip-management/ArpTab.tsx
@src/components/ip-management/AnomalyTab.tsx
@src/components/ip-management/NetworkTab.tsx
@src/components/ip-management/OuiTab.tsx
@src/components/pages/SettingsPage.tsx
@src/components/pages/DevicesPage.tsx
</context>

<interfaces>
<!-- 既有 DTO 复用清单（PATTERNS §12，planner 须引用这些导出名，不重复发明） -->

src/types/device.ts:
  - ConnectionType / DeviceType / Device / CreateDeviceDTO / UpdateDeviceDTO
src/types/topology.ts:
  - TopologyNodeData / TopologyNode / TopologyEdgeData / TopologyEdge / Topology
src/types/network.ts:
  - NetworkSegment / IPUsage / IPDetail / CreateNetworkInput / UpdateNetworkInput
src/types/arp.ts:
  - ARPEntry / ARPCollectionResult / ARPScanProgress
src/types/anomaly.ts:
  - ChangeType / IPMACBinding / IPMACChange / ChangeStats / ExcludedIP / CreateExcludedIPInput
src/types/oui.ts:
  - OUIEntry / CreateOUIInput / UpdateOUIInput / OUIStats / ScheduleConfig / SchedulerStatus / SchedulerStatus
src/types/pagination.ts:
  - PaginatedResult<T>（Phase 4 建，list 通道泛型参数复用）

<!-- electron.d.ts 当前 gap（待补）：
  - 3 个 PaginatedResult<any> 泛型参数须收为 IPDetail / IPMACChange / OUIEntry
  - ChatMessage.role: string → 收为 'user' | 'assistant'
  - device/topology/kb/network/anomaly/oui 非 list 通道全 Promise<any> 待建模
  - 缺 scheduler 通道（SettingsPage 用 (window as any).api 绕过）
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: electron.d.ts 全建模 + 新建 src/types/{kb,ai}.ts 补缺 DTO</name>
  <files>src/types/electron.d.ts, src/types/kb.ts, src/types/ai.ts</files>
  <read_first>
    - src/types/electron.d.ts（当前 23 处 any，逐通道建模对象）
    - src/types/device.ts / topology.ts / network.ts / arp.ts / anomaly.ts / oui.ts / pagination.ts（复用 DTO 源，确认导出符号名）
    - src/components/pages/SettingsPage.tsx（确认 scheduler 通道调用面：getConfig/saveConfig/start/stop/getStatus，line 15 `(window as any).api`）
    - src/components/pages/KnowledgeBasePage.tsx（确认 kb.* 通道返回结构，line 31 ChunkContent images、line 89 documents、line 97 detailDoc、line 211 chunk、line 179 searchResults——为 kb.ts DTO 字段取证）
    - src/components/pages/AIPage.tsx（确认 ai.* 通道调用面：line 52 device.list、line 73 listSessions、line 87 createSession、line 96 getSessionMessages、line 118 chat、line 167 confirmCommand——为 ai.ts DTO 字段取证）
    - electron/services/knowledgeBaseService.ts 与 electron/services/ai.ts（DB row 字段取证，仅读不改）
    - 05-PATTERNS.md §11（electron.d.ts 通道清单逐字）+ §10（KB any 点位根因）
  </read_first>
  <action>
    **原则（D-5-3）：复用 src/types 既有 DTO，缺 DTO 就近补 interface XxxRow 到 src/types/<domain>.ts，不在 electron.d.ts 内联，不重复发明类型。**

    **(1) 新建 src/types/kb.ts**（补 KB DB 行 DTO，字段从 knowledgeBaseService.ts DB prepare 语句取证）：
    ```typescript
    export interface KbImage {
      id: string
      file_path: string
      chunk_id?: string | null
    }
    export interface KbChunk {
      id: string
      doc_id: string
      title: string
      content: string
      image_ids?: string[] | string | null
      created_at?: string
    }
    export interface KbDocument {
      id: string
      filename: string
      file_type?: string
      file_size?: number
      category?: string
      device_id?: string | null
      status?: string
      created_at?: string
      chunks?: KbChunk[]
    }
    export interface KbStatus {
      status: string
      progress?: number
      total?: number
      processed?: number
      errorMessage?: string
    }
    export interface KbSearchResult {
      docId: string
      docTitle: string
      chunkId: string
      chunkTitle: string
      content: string
      score?: number
    }
    ```
    > 字段精确度若与实际 DB row 有出入，executor 须以 knowledgeBaseService.ts 的 prepare/INSERT 语句与 KnowledgeBasePage.tsx 消费面（line 179 searchResults、line 211 chunk、line 245 chunks.filter、line 473 detailDoc.chunks）为准微调——这是 Claude's Discretion（D-5-3）。

    **(2) 新建 src/types/ai.ts**（补 AI 行 DTO + 收 ChatMessage.role）：
    ```typescript
    // 复用 electron.d.ts 既有的 ChatMessage/ChatSession，但收 role
    export interface ChatMessage {
      id: string
      role: 'user' | 'assistant'
      content: string
      deviceId: string | null
      createdAt: string
    }
    export interface ChatSession {
      id: string
      title: string
      deviceId: string | null
      createdAt: string
    }
    export interface DiscoverResult {
      nodes: Array<Record<string, unknown>>
      edges: Array<Record<string, unknown>>
      failedDevices: Array<{ deviceId: string; deviceName: string; error: string }>
    }
    ```
    > electron.d.ts 既有 ChatMessage/ChatSession（line 37/45）须迁移到此文件并收 role 联合类型；electron.d.ts 改为 `import type { ChatMessage, ChatSession, DiscoverResult } from './ai'`。AIPage 的 handleSend JSON.parse(reply) 分支 confirm_required/kb_answer 等运行时解析，不在此静态建模（属运行时窄化）。

    **(3) 重写 electron.d.ts**（替换 23 处 any，逐通道复用 DTO）：
    - 顶部 import 整合：`import type { Device, CreateDeviceDTO, UpdateDeviceDTO } from './device'`、`Topology`、`NetworkSegment/IPDetail/IPUsage`、`ARPEntry`（保留）、`IPMACChange/ChangeStats/ExcludedIP`、`OUIEntry/OUIStats/ScheduleConfig/SchedulerStatus`、`Kb*`、`ChatMessage/ChatSession/DiscoverResult`、`PaginatedResult`。
    - 删除文件内联的 ChatMessage（line 37-43）/ChatSession（line 45-50），改 import 自 ./ai。AIConfig/AIExecLog/AISystemLog 保留在本文件（与 AI 主进程紧耦合，无独立 domain）。
    - **device**: `list: () => Promise<Device[]>`、`create: (data: CreateDeviceDTO) => Promise<Device>`、`update: (id: string, data: UpdateDeviceDTO) => Promise<Device>`、`getById: (id: string) => Promise<Device | null>`。
    - **topology**: `list: () => Promise<Topology[]>`、`getById: (id: string) => Promise<Topology | null>`、`create: (data: Partial<Topology>) => Promise<Topology>`、`update: (id: string, data: Partial<Pick<Topology,'nodes'|'edges'>>) => Promise<void>`、`importJson: (json: string) => Promise<Topology>`。
    - **ai**: `chat: (messages: Array<{ role: 'user'|'assistant'; content: string }>, deviceIds?: string[], sessionId?: string) => Promise<string>`、`discoverTopology: (deviceIds: string[]) => Promise<DiscoverResult>`、`getLogs: (limit?: number) => Promise<AIExecLog[]>`、`getChatHistory: () => Promise<ChatMessage[]>`、`saveMessage: (role: 'user'|'assistant', content: string, deviceId?: string|null, sessionId?: string|null) => Promise<void>`、`createSession/listSessions/getSessionMessages` 用 ChatSession/ChatMessage。
    - **kb**: `uploadBuffer: (...) => Promise<{ id: string }>`、`listDocuments: (deviceId?: string, category?: string) => Promise<KbDocument[]>`、`getDocument: (docId: string) => Promise<KbDocument | null>`、`getStatus: (docId: string) => Promise<KbStatus>`、`reprocess: (docId: string) => Promise<{ id: string }>`、`search: (query: string, deviceIds?: string[], topK?: number) => Promise<KbSearchResult[]>`、`getImageData: (imagePath: string) => Promise<string | null>`。
    - **network**: `getAll: () => Promise<NetworkSegment[]>`、`getById: (id: number) => Promise<NetworkSegment | null>`、`create/update: (data: CreateNetworkInput|UpdateNetworkInput) => Promise<NetworkSegment>`、`autoDiscover: () => Promise<NetworkSegment[]>`、`getIPUsage: (networkId: number) => Promise<IPUsage>`、`getIPDetails: (...) => Promise<PaginatedResult<IPDetail>>`（泛型 any → IPDetail）。
    - **anomaly**: `getChanges: (...) => Promise<PaginatedResult<IPMACChange>>`（泛型 any → IPMACChange）、`getStats: () => Promise<ChangeStats>`、`getBindingHistory: (ip: string) => Promise<IPMACBinding[]>`、`getExcludedIPs: () => Promise<ExcludedIP[]>`、`addExcludedIP: (data: CreateExcludedIPInput) => Promise<ExcludedIP>`。
    - **oui**: `getAll: (...) => Promise<PaginatedResult<OUIEntry>>`（泛型 any → OUIEntry）、`search: (keyword: string) => Promise<OUIEntry[]>`、`getById: (id: number) => Promise<OUIEntry | null>`、`add: (data: CreateOUIInput) => Promise<OUIEntry>`、`addBatch: (entries: CreateOUIInput[]) => Promise<number>`、`update: (data: UpdateOUIInput) => Promise<OUIEntry>`、`getAllVendors: () => Promise<Array<{ oui_prefix: string; vendor_name: string }>>`、`getStats: () => Promise<OUIStats>`。
    - **connection**: 保留现有签名（已无 any，line 77-86 不动）。
    - **auth**: 保留现有签名（已无 any）。
    - **新增 scheduler 通道**（D-5-2 收敛 SettingsPage 的前置依赖，PATTERNS §14 标注当前缺失）：
      ```typescript
      scheduler: {
        getConfig: () => Promise<ScheduleConfig>
        saveConfig: (config: ScheduleConfig) => Promise<{ success: boolean; error?: string }>
        start: () => Promise<{ success: boolean; error?: string }>
        stop: () => Promise<{ success: boolean; error?: string }>
        getStatus: () => Promise<SchedulerStatus>
      }
      ```
    > scheduler 字段精确度以 electron/services/backupScheduler.ts 的 getConfig/saveConfig + schedulerIpc.ts handler 签名为准微调（Claude's Discretion）。若 preload.ts 当前未导出 scheduler 桥，executor 须确认 preload.ts 是否暴露 scheduler.*——若 preload 已有但 electron.d.ts 漏标，则补；若 preload 无（仅 SettingsPage 用 (window as any).api 绕过），则在 electron.d.ts 建模后**同步 preload.ts 补 scheduler 桥**（preload.ts 编辑权归本 task，作为 scheduler 类型化的必要配套，非 scope 蔓延）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | grep -v "^[[:space:]]*$" | head -30 && echo "=== electron.d.ts any 残留（应仅注释/泛型 default） ===" && grep -n ": any\|Promise<any>" src/types/electron.d.ts | grep -v "^.*//" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ": any\|Promise<any>" src/types/electron.d.ts` 输出 0（注释行 `//` 开头的不计；用 `grep -v '^//' | grep -c ": any\|Promise<any>"` 验证）
    - `grep "scheduler:" src/types/electron.d.ts` 命中（新增 scheduler 通道）
    - `ls src/types/kb.ts src/types/ai.ts` 两文件存在
    - `grep "interface KbImage" src/types/kb.ts` 命中
    - `grep "role: 'user' | 'assistant'" src/types/ai.ts` 命中（ChatMessage.role 收敛）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
    - preload.ts 若补 scheduler 桥：`grep "scheduler" electron/preload.ts` 命中
  </acceptance_criteria>
  <done>electron.d.ts 23 处 any 全部替换为 src/types DTO；src/types/{kb,ai}.ts 建立并 export 所需 DTO；scheduler 通道补全（含 preload 桥配套）；tsc web 严格模式全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 4 个 IP Tab + SettingsPage + DevicesPage 清 any</name>
  <files>src/components/ip-management/ArpTab.tsx, src/components/ip-management/AnomalyTab.tsx, src/components/ip-management/NetworkTab.tsx, src/components/ip-management/OuiTab.tsx, src/components/pages/SettingsPage.tsx, src/components/pages/DevicesPage.tsx</files>
  <read_first>
    - Task 1 完成后的 src/types/electron.d.ts（已建模，是本 task 的类型源）
    - src/components/ip-management/ArpTab.tsx（基线 11 处 any，PATTERNS §13 逐字点位 line 5/9/10/11/16/17/28/50/59/81/141）
    - src/components/ip-management/{AnomalyTab,NetworkTab,OuiTab}.tsx（基线 4/6/5 处，同模式）
    - src/components/pages/SettingsPage.tsx（基线 6 处：line 15 `(window as any).api`、line 25/26 useState any、line 35/65/76/86/96 catch）
    - src/components/pages/DevicesPage.tsx（基线 4 处全在 catch：line 22/33/44/64）
    - 05-PATTERNS.md §13（4 Tab 点位）+ §14（SettingsPage/DevicesPage 点位）
    - src/types/oui.ts（ScheduleConfig/SchedulerStatus）+ src/types/network.ts/anomaly.ts/oui.ts/arp.ts（DTO 复用）
  </read_first>
  <action>
    **(A) 4 个 IP Tab（统一模式，D-5-3 复用 DTO）：**
    - **ArpTab.tsx**：
      - `interface ArpTabProps { api: any }` → `interface ArpTabProps { api: ElectronAPI }`（import type { ElectronAPI } from '@/types/electron'，或更细切片用 `Pick<ElectronAPI, 'device'|'arp'|'anomaly'>`——Claude's Discretion，建议 ElectronAPI 全量保持简单）。
      - `useState<any[]>(results)` → `useState<ARPEntry[]>([])`（import ARPEntry from '@/types/arp'；若实际结果行含额外字段如 ip/mac/vendor，executor 以实际 IPC 返回为准微调为 `Array<ARPEntry & { vendor?: string }>`）。
      - `useState<any>(stats)` → `useState<ARPScanProgress | null>(null)` 或具体统计 DTO。
      - `useState<any[]>(devices)` → `useState<Device[]>([])`。
      - `api.device.list().then((list: any[]) => { setDevices(list.filter((d: any) => ...)) })` → 去掉 `(list: any[])` 与 `(d: any)` 标注（Device[] 已强类型），filter 内 `d.connectionType` 等访问由类型守卫。
      - `catch (e: any) { message.error('...' + e.message) }` → `catch (e: unknown) { message.error('...: ' + (e instanceof Error ? e.message : String(e))) }`（全仓库 catch 统一此模式，PATTERNS §Shared Patterns 错误处理）。
      - Table 列 `render: (_: any, record: any)` → `render: (_: unknown, record: ARPEntry)`（Ant Design Table render 签名 `_` 用 unknown 兼容，record 用行 DTO）。
      - `rowKey={(row: any) => ...}` → `rowKey={(row: ARPEntry) => ...}` 或去标注。
    - **AnomalyTab.tsx**（4 处）：`api: any` → ElectronAPI；`useState<any[]>(changes)` → `useState<IPMACChange[]>`；`catch (e:any)` → `unknown`+窄化。**Phase 4 已适配点 line 33 `setChanges(c.rows)` 保留**，仅收 rows 元素类型。
    - **NetworkTab.tsx**（6 处）：`api: any` → ElectronAPI；`useState<any[]>(segments/details)` → `NetworkSegment[]` / `IPDetail[]`；catch 收 unknown。**Phase 4 line 34/43 `details.rows` 保留**。
    - **OuiTab.tsx**（5 处）：`api: any` → ElectronAPI；`useState<any[]>(entries)` → `OUIEntry[]`；catch 收 unknown。**Phase 4 line 23 `setEntries(e.rows)` 保留**。

    **(B) SettingsPage.tsx（6 处 + scheduler 通道落地）：**
    - **删除 line 15 `const api = (window as any).api`**，全文 `api.*` 改 `window.api.*`（Task 1 已在 electron.d.ts 建 scheduler 通道 + preload 桥，此处可直接 `window.api.scheduler.getConfig()` 等）。
    - `useState<any>(schedulerConfig)` → `useState<ScheduleConfig>({} as ScheduleConfig)` 或 `useState<ScheduleConfig | null>(null)`（import from '@/types/oui'，executor 选其一保持既有渲染逻辑不崩）。
    - `useState<any>(schedulerStatus)` → `useState<SchedulerStatus>({} as SchedulerStatus)` 或 null。
    - 5 处 `catch (e: any) { ... }` → `catch (e: unknown) { message.error(... + (e instanceof Error ? e.message : String(e))) }`。
    - line 44 `catch { /* ignore */ }` 已是 unknown 无标注，保留。

    **(C) DevicesPage.tsx（4 处全在 catch）：**
    - line 22/33/44/64 四处 `catch (e: any) { message.error(e.message) }` → `catch (e: unknown) { message.error(e instanceof Error ? e.message : String(e)) }`。已用 Device/CreateDeviceDTO 的部分不动。

    **统一原则（PATTERNS §Shared Patterns 错误处理）：** 所有 catch 子句 strict 模式默认 unknown，删 `: any` 即编译通过；`e.message` 须窄化为 `(e instanceof Error ? e.message : String(e))`，全仓库统一此模式。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "=== 6 文件 any 残留（before: 11/4/6/5/6/4） ===" && for f in src/components/ip-management/ArpTab.tsx src/components/ip-management/AnomalyTab.tsx src/components/ip-management/NetworkTab.tsx src/components/ip-management/OuiTab.tsx src/components/pages/SettingsPage.tsx src/components/pages/DevicesPage.tsx; do echo "$(grep -v '^//' "$f" | grep -c ': any\|as any\|: any\[\]')  $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/components/ip-management/ArpTab.tsx | grep -c ": any\|as any\|: any\[\]"` 输出 0（before 11）
    - 4 个 IP Tab 同上输出 0（before 11/4/6/5）
    - `grep -v '^//' src/components/pages/SettingsPage.tsx | grep -c ": any\|as any\|: any\[\]"` 输出 0（before 6）
    - `grep "(window as any)" src/components/pages/SettingsPage.tsx` 无命中（line 15 绕过已删）
    - `grep "scheduler" src/components/pages/SettingsPage.tsx` 命中（window.api.scheduler.* 调用面）
    - `grep -v '^//' src/components/pages/DevicesPage.tsx | grep -c ": any\|as any"` 输出 0（before 4）
    - 6 文件 `grep "catch (e: unknown)"` 命中（catch 收敛）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
  </acceptance_criteria>
  <done>6 文件 any 清零（含 SettingsPage scheduler 通道对接、DevicesPage catch 收敛）；tsc web 严格模式 + noUnusedLocals 全绿；Phase 4 信封 .rows 读路径保留无回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC（既有） | 本 plan 不新增 IPC 通道、不改 IPC 签名（scheduler 通道若 preload 已有则仅 electron.d.ts 类型补全；若 preload 缺失则配套补桥，handler 在主进程侧已存在，非新攻击面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | electron.d.ts 类型契约 | accept | 类型层建模不改运行时行为；IPC handler 仍由 authGuard `secure()` 包装鉴权（CONVENTIONS §2 红线），类型收窄不削弱运行时校验。 |
| T-05-01-02 | Information Disclosure | catch 块 e.message 渲染 | mitigate | `(e instanceof Error ? e.message : String(e))` 仅显示错误 message；IPC 层 `sanitizeMessage` 已在主进程脱敏 SQL/路径（CONVENTIONS §2），渲染层只收截断 message。本 plan 不改变此链路。 |
| T-05-01-03 | Spoofing | scheduler 通道（新增类型） | accept | scheduler handler 在 schedulerIpc.ts 已存在并经 `secure()` 包装（CONCERNS SEC-1）；本 plan 仅补类型标注 + 必要时 preload 桥，不新增特权 handler。 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals + noUnusedParameters 全绿，FE-01/02 验收红线）
- `npx vitest run` exit 0（既有 25 测试不回归）
- `npx esbuild electron/main.ts ... --bundle`（主进程打包不回归，确认 preload.ts 改动若有则主进程编译过）
- electron.d.ts grep `: any|Promise<any>` 残留 0
- 6 文件 any 清零（grep 基线 before/after 对比，D-5-2 grep 限定 src/ + electron.d.ts，不含后端）
</verification>

<success_criteria>
- electron.d.ts 全建模（23 处 any → 0，含 scheduler 通道新增）
- src/types/{kb,ai}.ts 建立，缺 DTO 补齐
- 4 IP Tab + SettingsPage + DevicesPage any 清零（Phase 4 信封读 .rows 无回退）
- tsc web 严格模式 + noUnusedLocals 全绿
- FE-01（AIPage）与 FE-04（KB）的类型前置依赖就绪（ChatMessage/KbImage 等 DTO 可被 Wave 2 引用）
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-refactor-types/05-01-SUMMARY.md`
</output>
