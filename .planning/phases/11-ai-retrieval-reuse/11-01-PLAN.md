---
phase: 11-ai-retrieval-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/experienceRerank.ts
  - electron/services/experienceRetrieval.ts
  - electron/services/ai.ts
  - electron/services/experienceRetrieval.test.ts
autonomous: true
requirements_addressed: [RETRIEVE-01, RETRIEVE-02]
gap_closure: false

must_haves:
  truths:
    - "每轮 AI 对话自动预取：chat() 入口先调 retrieveForAnswer，不靠 AI 自主标记（D-11-1 方案 b）"
    - "粗筛用问题原文 LIKE 宽匹配（无勾选设备）或关联设备+同分类窄查（有勾选设备），复用 listExperiences 零迁移（D-11-2/D-11-5）"
    - "精排 callAI 强 schema JSON 返每条 exp_id+score+reason，score 边界归一化复用 draftingService confidence 模式（D-11-3）"
    - "相关度阈值过滤：score < RELEVANCE_THRESHOLD 的候选不注入，防噪声（D-11-4）"
    - "read-time 两项验证：命令白名单（isCommandAllowed）+ 有效期（invalid_at），不验设备状态（D-11-6/D-11-8）"
    - "命令失支持降权标注 unsupported=true 注入，有效期失效剔除（D-11-7）"
    - "验证失败只降权/剔除，不阻塞主路径（D-11-9）"
    - "命中注入即 incReuseCount + touchLastVerifiedAt（SC2 刷新）"
    - "经验正文注入正式 callAI context，正式答返 references 元数据随 ai:chat JSON 回 renderer"
    - "renderer 永不收 attrs 密文/凭证片段（references 只含 exp_id/title/source_session_id/unsupported）"
  artifacts:
    - path: "electron/services/experienceRerank.ts"
      provides: "精排 LLM service（强 schema JSON 评分 + Drift Gate + 重试 + 反幻觉 prompt）"
      exports: ["rerank", "validateRerank", "buildRerankPrompt", "MAX_RERANK_RETRIES", "RELEVANCE_THRESHOLD"]
    - path: "electron/services/experienceRetrieval.ts"
      provides: "检索编排 service（粗筛→精排→阈值→read-time 验证→刷新计数→返注入元数据）"
      exports: ["retrieveForAnswer", "RetrieveInput", "RetrieveResult", "INJECT_LIMIT"]
    - path: "electron/services/experienceRetrieval.test.ts"
      provides: "编排+精排单测（mock callAI + 内存 mock DB）"
  key_links:
    - from: "electron/services/ai.ts chat()"
      to: "electron/services/experienceRetrieval.ts retrieveForAnswer"
      via: "chat() 入口自动预取调用（b 方案，不靠 AI 标记）"
      pattern: "retrieveForAnswer\\("
    - from: "electron/services/experienceRetrieval.ts"
      to: "electron/services/experienceService.ts listExperiences/incReuseCount/touchLastVerifiedAt"
      via: "粗筛 + 命中刷新（复用预埋接口零改动）"
      pattern: "listExperiences|incReuseCount|touchLastVerifiedAt"
    - from: "electron/services/experienceRetrieval.ts"
      to: "electron/services/experienceRerank.ts rerank"
      via: "粗筛候选喂精排打分"
      pattern: "rerank\\("
    - from: "electron/services/experienceRetrieval.ts"
      to: "electron/services/commandSafety.ts isCommandAllowed"
      via: "read-time 命令白名单验证"
      pattern: "isCommandAllowed\\("
---

<objective>
实现 Phase 11 检索复用的 main 进程 service 层：每轮 AI 对话自动预取经验（D-11-1 方案 b），SQL 粗筛（复用 listExperiences，D-11-5 零迁移）→ LLM 精排强 schema 打分（D-11-3，2 次 LLM：精排 + 正式答）→ 相关度阈值过滤（D-11-4 防噪声）→ read-time 两项验证 commandSafety 白名单 + 有效期（D-11-6，不验设备状态 D-11-8）→ 命中刷新 reuse_count/last_verified_at → 经验正文注入正式 callAI context → references 元数据随 ai:chat 返回。验证失败只降权/剔除不阻塞主路径（D-11-9）。

Purpose: RETRIEVE-01（自动检索引用）+ RETRIEVE-02（read-time 即时验证）的 main 进程落地，为 Plan 02 renderer 来源列表提供 references 数据。
Output: experienceRerank.ts（精排）+ experienceRetrieval.ts（编排）+ ai.ts chat() 串联注入 + 单测。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-retrieval-reuse/11-CONTEXT.md
@.planning/phases/11-ai-retrieval-reuse/11-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- 关键复用接口契约（executor 直接用，不需 codebase 探查） -->

From electron/services/experienceService.ts（粗筛 + 刷新，零改动复用）:
```typescript
export type ExperienceCategory = 'troubleshooting' | 'best_practices' | 'product' | 'env'
export type ExperienceStatus = 'draft' | 'confirmed' | 'published' | 'invalid'

export interface ListExperiencesOpts {
  category?: ExperienceCategory
  status?: ExperienceStatus
  deviceId?: string | string[]    // 单值或多选数组（IN 占位 OR-join）
  includeInvalid?: boolean        // 默认 false：只命中 invalid_at IS NULL OR > now
  limit?: number                  // null/0/负→100；> MAX_BATCH(1000) throw
  offset?: number
  search?: string                 // SQL LIKE title/content 参数化（D-11-2 粗筛宽匹配用）
  severity?: string
  tags?: string[]
}
// 返回 { rows: Experience[], total: number }，Experience 已 rowToExperience 解密 attrs（delete attrs_enc）
// 注意：粗筛默认 includeInvalid=false 即天然过滤失效（D-11-7 有效期失效剔除靠此默认）

export function listExperiences(opts: ListExperiencesOpts): PaginatedResult<any>
export function incReuseCount(id: string): void                              // reuse_count = reuse_count + 1
export function touchLastVerifiedAt(id: string): void                        // last_verified_at = datetime('now','localtime')
// Experience 行字段：id, title, content, category, status, source_session_id, invalid_at, reuse_count, last_verified_at, attrs(明文对象), tags[]
```

From electron/services/commandSafety.ts（read-time 命令验证，零适配）:
```typescript
export function isCommandAllowed(
  command: string,
  whitelist: string[]
): { allowed: boolean; reason: string }
// 三层：分隔符注入拦截 → 黑名单首词 → 白名单首词严格相等
```

From electron/services/ai.ts（LLM 调用 + 配置 + 白名单 + chat 编排注入点）:
```typescript
export async function callAI(
  config: Record<string, string>,
  messages: Array<{ role: string; content: string }>
): Promise<string>   // ai.ts:258，裸 fetch 非 streaming，无 response_format（prompt 层强 schema JSON）

export function getAiConfig(): Record<string, string> | null   // ai.ts:22，未配返 null
export function getCommandWhitelist(): string[]                 // ai.ts:126，喂 isCommandAllowed 第二参

// chat() 编排注入点（ai.ts:673-807）：
//   入口（:678-681）校验 config + 抛 '请先配置 AI 服务（API Key 未设置）'
//   :683 whitelist = getCommandWhitelist()
//   :724-727 fullMessages = [{system}, ...messages]
//   :729 const aiReply = await callAI(config, fullMessages)   ← 首轮正式答（KB_SEARCH 前提）
//   :731-787 [KB_SEARCH] 自主标记二轮注入（既有，Phase 11 不抄触发方式）
//   :800-807 commands 为空时返 finalAiReply 或 JSON {type:'kb_answer',content,references}
// Phase 11 注入位置：在 :729 callAI 之前（b 方案自动预取），调 retrieveForAnswer 拿 injected，
//   把经验正文拼进 systemPrompt（或 fullMessages[0].content），正式答用同一次 callAI 即可带经验。
```

From electron/services/draftingService.ts（精排强 schema 范式，复刻同模式）:
```typescript
export const MAX_DRAFT_RETRIES = 3   // 模块级常量
// extractJsonArray(raw) 私有函数：剥 ```json 包裹 + 取首 [ 末 ]（draftingService.ts:97-104）
// validateDrafts schema Gate：JSON.parse + Array.isArray + 逐条枚举/边界校验（:106-164）
// confidence 边界归一化：'85%'→0.85 / '0.9'→0.9 / 'high'→NaN fail / 1.5→超界 fail（:144-150）
// 重试循环 for attempt 1..MAX_DRAFT_RETRIES（:178-184），全失败 throw
// 反幻觉 SYSTEM_PROMPT（:59-73）：禁编造 + 输出格式严格 JSON 数组 + 空结果返 []
```
</interfaces>

<analogs>
<!-- PATTERNS.md 已给 file:line，executor 必读类比文件复制模式 -->

1. 编排骨架（experienceRetrieval.ts retrieveForAnswer）：
   抄 electron/services/experienceDrafting.ts:70-156（summarizeSessionForUi）
   - 多阶段串行 + empty 早返（:76 messages 空→return empty）
   - demoMode 判定（:79-81 config null/apiKey 空→返 demoMode=true 不抛错）
   - 中间结果喂下一阶段（:91 draftSession → :100 findExistingForDraft → :104 judgeVerdicts）
   - 落库失败降级 createSystemLog（:131-145 relateDevice try/catch + 日志兜底）→ Phase 11 命中刷新失败仿此「不阻塞主路径」
   返回值结构化结果抄 DraftingResult（experienceDrafting.ts:38-44）

2. 精排强 schema（experienceRerank.ts）：
   抄 electron/services/draftingService.ts:97-185（extractJsonArray + validateDrafts + 重试 draftSession）
   - extractJsonArray 复用同逻辑（剥 ```json 包裹）
   - score 边界归一化复用 confidence 边界模式（:144-150）
   - MAX_RERANK_RETRIES=3 模块级常量（仿 :21 MAX_DRAFT_RETRIES）
   - 反幻觉 prompt 仿 SYSTEM_PROMPT（:59-73）结构

3. chat() 二轮注入：
   抄 electron/services/ai.ts:724-727（fullMessages 拼装）+ :765-773（followUpMessages 注入模式）
   - Phase 11 触发前置（:729 callAI 前先 retrieveForAnswer），不抄 :731 [KB_SEARCH] 自主标记协议
   - 返回值 references JSON 仿 :803-804（{type:'kb_answer',content,references} 扩 references 联合类型）

4. 单测 mock 模式：
   抄 electron/services/draftingService.test.ts:13-18（vi.mock('./ai') callAI + getAiConfig）+ experienceService.test.ts 内存 mock DB（_setExperienceDbGetter 注入）
</analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 精排 service experienceRerank.ts（强 schema LLM 评分）</name>
  <files>electron/services/experienceRerank.ts, electron/services/experienceRetrieval.test.ts</files>
  <read_first>
    - .planning/phases/11-ai-retrieval-reuse/11-CONTEXT.md（D-11-3 精排承担理解方案 Y / D-11-4 阈值防噪声）
    - .planning/phases/11-ai-retrieval-reuse/11-PATTERNS.md §Pattern 2（draftingService 范式 file:line）
    - electron/services/draftingService.ts:21,59-104,106-185（MAX_DRAFT_RETRIES / extractJsonArray / validateDrafts / confidence 边界 :144-150 / draftSession 重试 :166-185）
    - electron/services/draftingService.test.ts:13-60（vi.mock('./ai') + validConfig 模式）
    - ./CLAUDE.md Conventions（Service 函数式无 class 无 MK / 模块级常量 UPPER_SNAKE_CASE）
  </read_first>
  <behavior>
    - validateRerank: 返 ```json [...] 包裹 → 剥离取数组；非数组 fail；每条 exp_id 必须在候选集 exp_ids 中（防 LLM 编造）；score 边界复用 confidence 归一化（'0.85'→0.85 / '85%'→0.85 / 'high'→NaN fail / 1.5→超界 fail）；reason 字符串必填
    - rerank: demoMode（config null/apiKey 空）→ throw '请先配置 AI 服务（API Key 未设置）'（与 draftSession 一致，不静默返空）；3 次重试 MAX_RERANK_RETRIES；全失败 throw 'AI 精排失败（已重试 3 次）：{lastError}'
    - rerank: 候选为空 → 直接返 [] 不调 LLM（短路）
    - buildRerankPrompt: 每条候选给 exp_id+title+content 前 N 字（N=150），让 LLM 回填 score 不丢映射（仿 buildVerdictPrompt draft_index 引用模式）
    - RELEVANCE_THRESHOLD 模块级常量导出（建议 0.6，可配置——但本期硬编码模块常量，二期再提 env）
  </behavior>
  <action>
    新建 electron/services/experienceRerank.ts，函数式无 class 无 MK（CONVENTIONS Pattern 1b：本 service 不读写加密列，候选经 listExperiences 已解密 attrs 明文）。

    导出模块级常量：MAX_RERANK_RETRIES = 3（仿 draftingService.ts:21）；RELEVANCE_THRESHOLD = 0.6（D-11-4 阈值，planner 定值 0.6 可调）。

    导出接口：
    - RerankEntry: { exp_id: string; score: number; reason: string }
    - RerankInput: { userMessage: string; candidates: Array<{ exp_id: string; title: string; content_preview: string }>; demoMode?: boolean }

    RERANK_SYSTEM_PROMPT（仿 draftingService.ts:59-73 SYSTEM_PROMPT 结构，反幻觉红线）：明示「对每条候选经验结合用户问题判相关度并打分 score 0-1」「禁止编造 exp_id（只能用候选列表中既有 id）」「严格输出 JSON 数组不得有任何额外文字」「每条对象字段 exp_id/score(0-1)/reason」「全部不相关返回空数组 []」。

    buildRerankPrompt(input)（仿 draftingService.ts:200-223 buildVerdictPrompt）：user 段拼「用户问题：{userMessage}」「候选经验列表（按 exp_id 引用）：每条 - exp_id | 标题 | 内容前150字」。

    extractJsonArray(raw)（从 draftingService.ts:97-104 复刻同逻辑，私有函数不导出）：取首 [ 末 ]，找不到边界 throw '输出未找到 JSON 数组边界'。

    validateRerank(raw, candidateExpIds: Set<string>)（仿 draftingService.ts:106-164 validateDrafts）：JSON.parse(extractJsonArray) → Array.isArray → 逐条校验：exp_id 必须在 candidateExpIds（防编造，不在则 fail 'exp_id 不在候选集'）；score 边界复用 confidence 归一化模式（typeof string 时 '85%'→/100 parseFloat / '0.9'→parseFloat，NaN/<0/>1 fail）；reason typeof string 必填。返 { ok: true, entries: RerankEntry[] } | { ok: false, error: string }。

    rerank(input)（仿 draftingService.ts:166-185 draftSession）：demoMode=true → 返 []（注意：与 draftSession 一致，demoMode 是上层 chat() 不抛错路径；rerank 自身在非 demoMode 下 config 缺失才 throw）。非 demoMode 时校验 getAiConfig() null/apiKey 空 → throw '请先配置 AI 服务（API Key 未设置）'。candidates.length===0 → 返 [] 短路。否则 for attempt 1..MAX_RERANK_RETRIES：callAI(config, [{system:RERANK_SYSTEM_PROMPT},{user:buildRerankPrompt}]) → validateRerank(raw, candidateExpIds) → ok 即返 entries，否则记 lastError。全失败 throw 'AI 精排失败（已重试 {MAX_RERANK_RETRIES} 次）：{lastError}'。

    注意 callAI 签名是 callAI(config, messages)（ai.ts:258，config 是 getAiConfig() 返回的 Record<string,string>，非 messages 内嵌）。

    单测 electron/services/experienceRetrieval.test.ts（与编排 service 共用一测试文件，先写 rerank 部分用例）：vi.mock('./ai', () => ({ callAI, getAiConfig, getCommandWhitelist }))。覆盖：validateRerank ```json 包裹剥离 / 非数组 fail / exp_id 编造 fail / score '85%' 归一化 / score 'high' NaN fail / score 1.5 超界 fail；rerank demoMode 返 [] / 候选空返 [] / 第1次 fail 第2次成功 / 3 次全失败 throw。
  </action>
  <verify>
    <automated>npx vitest run electron/services/experienceRetrieval.test.ts -t "rerank\|validateRerank" --reporter=default 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - electron/services/experienceRerank.ts 存在且 `grep -E "export (function|const|interface) (rerank|validateRerank|buildRerankPrompt|MAX_RERANK_RETRIES|RELEVANCE_THRESHOLD|RerankEntry|RerankInput)" experienceRerank.ts` 全命中
    - `grep -c "class " electron/services/experienceRerank.ts` 输出 0（函数式无 class）
    - `grep -c "encField\|decField\|MK" electron/services/experienceRerank.ts` 输出 0（不碰加密列 Pattern 1b）
    - validateRerank 对 score='85%' 归一化为 0.85、score='high' fail、score=1.5 fail（vitest 用例断言）
    - rerank 3 次重试全失败 throw 包含 'AI 精排失败（已重试 3 次）'（vitest 用例断言）
    - vitest experienceRetrieval.test.ts rerank/validateRerank 分组全部 pass，退出码 0
  </acceptance_criteria>
  <done>精排 service 导出 rerank/validateRerank/buildRerankPrompt/MAX_RERANK_RETRIES/RELEVANCE_THRESHOLD，强 schema JSON Drift Gate + 重试 + 反幻觉 prompt 落地，rerank/validateRerank 单测全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 编排 service experienceRetrieval.ts + chat() 串联注入（b 自动预取）</name>
  <files>electron/services/experienceRetrieval.ts, electron/services/ai.ts, electron/services/experienceRetrieval.test.ts</files>
  <read_first>
    - .planning/phases/11-ai-retrieval-reuse/11-CONTEXT.md（D-11-1 b 自动预取 / D-11-2 粗筛窄查 / D-11-6 两项验证 / D-11-7 降级策略 / D-11-9 不阻塞主路径 / D-11-11 references 从注入记录拿）
    - .planning/phases/11-ai-retrieval-reuse/11-PATTERNS.md §Pattern 1 + §Pattern 3 + §Pattern 4（experienceDrafting 编排骨架 + read-time 验证 + chat 二轮注入 file:line）
    - electron/services/experienceDrafting.ts:38-44,70-156（DraftingResult 结构 + summarizeSessionForUi 多阶段串行 + demoMode/empty 早返 + 落库失败降级日志）
    - electron/services/experienceService.ts:35-43,125-140,258,559-571（_setExperienceDbGetter 测试钩子 / ListExperiencesOpts / listExperiences / incReuseCount / touchLastVerifiedAt）
    - electron/services/commandSafety.ts:14-52（isCommandAllowed 三层校验）
    - electron/services/ai.ts:673-807（chat() 入口校验 + fullMessages 拼装 :724-727 + [KB_SEARCH] 二轮注入 :731-787 + :803-804 kb_answer references JSON 返回）
    - electron/services/experienceService.test.ts（内存 mock DB 注入模式参考，_setExperienceDbGetter）
    - electron/services/experienceRetrieval.test.ts（Task 1 已建，本 task 追加编排用例）
  </read_first>
  <behavior>
    - retrieveForAnswer: 空库（listExperiences rows.length===0）→ 返 { injected:[], reranked:[], finalAnswer:userMessage, demoMode:false } 不调精排 LLM（D-11 discretion 空库短路）
    - retrieveForAnswer: demoMode（getAiConfig null/apiKey 空）→ 返 demoMode:true injected:[]（不抛错，与 experienceDrafting 一致）
    - 粗筛策略（D-11-2）：input.deviceIds 非空数组 → listExperiences({ deviceId: deviceIds, includeInvalid:false, limit: INJECT_LIMIT*4 })（按关联设备窄查，多设备 IN 占位）；input.deviceIds 空 → listExperiences({ search: input.userMessage, includeInvalid:false, limit: INJECT_LIMIT*4 })（问题原文全库宽匹配）
    - 精排调 rerank({ userMessage, candidates: rows 映射 {exp_id:id,title,content_preview:content.slice(0,150)} })，返 RerankEntry[]
    - 阈值过滤：score >= RELEVANCE_THRESHOLD 的保留（D-11-4），取 top INJECT_LIMIT 条（建议 INJECT_LIMIT=5，参考 Phase 8 W-4 ≤50 截断但经验注入更精简）
    - read-time 验证每条保留候选（D-11-6 两项）：
      (a) 有效期：粗筛 includeInvalid=false 已过滤，但窗口跨天二次确认 row.invalid_at（null 或 > now 才有效），失效 → 剔除（D-11-7）
      (b) 命令白名单：从 row.content + row.attrs?.resolution 正文扫描提取类命令文本（正则 /(display|show|ping|traceroute|debug|terminal)\s[\w-]+/g 提取，无结构化 command 字段 D-11 discretion），逐条 isCommandAllowed(cmd, whitelist)；任一命令 allowed=false → 标 unsupported=true（降权不剔除，D-11-7）；无命令 → unsupported=false
    - 命中注入即 incReuseCount(id) + touchLastVerifiedAt(id)，失败 try/catch + console.warn 降级不阻塞（D-11-9，仿 experienceDrafting.ts:131-145 落库失败降级模式，但经验检索刷新失败不写 createSystemLog 因非关键路径，console.warn 够）
    - 返回 RetrieveResult：{ demoMode, injected: Array<{exp_id,title,content,source_session_id,unsupported}>, reranked: RerankEntry[]（供调试）, finalAnswer: userMessage（编排不答，正式答交 chat() 的 callAI） }
    - chat() 串联（ai.ts 修改）：在 :678 config 校验后、:724 fullMessages 拼装前，调 retrieveForAnswer({ userMessage: messages 最后一条 user content, deviceIds })；若 result.injected.length>0，把经验正文拼进 systemPrompt（追加「以下是经验库中检索到的相关经验（仅供参考，回答末尾无需标注）：\n[经验1: title（⚠ 此条命令已失支持）]\ncontent...」）；正式答 callAI 用同一次（不二轮，经验已在 system）；返回时若 injected.length>0，JSON.stringify({ type:'exp_answer', content: finalAiReply, references: injected.map(e=>({exp_id,title,source_session_id,unsupported})) })；与既有 kb_answer 分支并存（references 联合类型，Plan 02 renderer 扩消费）
    - 信任边界（T1/T3）：精排 prompt 只送 exp_id+title+content 前150字（不送 attrs 凭证字段）；references 回 renderer 只含 exp_id/title/source_session_id/unsupported（不含 attrs 密文）
  </behavior>
  <action>
    新建 electron/services/experienceRetrieval.ts，函数式无 class 无 MK（Pattern 1b）。

    import（仿 experienceDrafting.ts:1-8 全相对路径）：
    - from './ai': callAI, getAiConfig, getCommandWhitelist
    - from './experienceService': listExperiences, incReuseCount, touchLastVerifiedAt
    - from './commandSafety': isCommandAllowed
    - from './experienceRerank': rerank, RELEVANCE_THRESHOLD
    - from './systemLog': createSystemLog（刷新失败降级用，仿 experienceDrafting.ts:8）

    导出模块级常量 INJECT_LIMIT = 5（经验注入条数上限，planner 定值 5——比 Phase 8 W-4 ≤50 窄，因经验注入要精不要多，防 context 溢出）。MAX_CANDIDATES = INJECT_LIMIT * 4 = 20（粗筛捞候选上限，喂精排）。

    导出接口 RetrieveInput: { userMessage: string; deviceIds?: string[] }；RetrieveResult: { demoMode: boolean; injected: Array<{ exp_id: string; title: string; content: string; source_session_id?: string | null; unsupported: boolean }>; reranked: Array<{ exp_id: string; score: number; reason: string }>; finalAnswer: string }。

    retrieveForAnswer(input) 实现（仿 experienceDrafting.ts:70-156 编排骨架）：
    1. empty 结果对象（injected:[], reranked:[], finalAnswer: input.userMessage, demoMode:false）
    2. demoMode 判定：const config = getAiConfig(); if (!config || !config.apiKey) return { ...empty, demoMode: true }（仿 experienceDrafting.ts:79-81）
    3. 粗筛（D-11-2）：const opts = input.deviceIds && input.deviceIds.length > 0 ? { deviceId: input.deviceIds, includeInvalid: false, limit: MAX_CANDIDATES } : { search: input.userMessage, includeInvalid: false, limit: MAX_CANDIDATES }；const { rows } = listExperiences(opts)；if (rows.length === 0) return empty（空库短路）
    4. 精排：const candidates = rows.map(r => ({ exp_id: r.id, title: r.title, content_preview: (r.content || '').slice(0, 150) }))；const entries = await rerank({ userMessage: input.userMessage, candidates })（rerank 内部已处理 demoMode/重试）
    5. 阈值 + 截断：const passed = entries.filter(e => e.score >= RELEVANCE_THRESHOLD).sort((a,b) => b.score - a.score).slice(0, INJECT_LIMIT)
    6. read-time 验证 + 注入构造：const whitelist = getCommandWhitelist()；const injected = []；for (const e of passed) { const row = rows.find(r => r.id === e.exp_id); if (!row) continue; (a) 有效期二次确认：if (row.invalid_at) { const inv = new Date(row.invalid_at).getTime(); if (!isNaN(inv) && inv <= Date.now()) continue; }（失效剔除 D-11-7） (b) 命令扫描：const text = (row.content || '') + ' ' + (row.attrs?.resolution || '') + ' ' + (row.attrs?.root_cause || ''); const cmds = [...text.matchAll(/(?:display|show|ping|traceroute|debug|terminal|interface)\s+[\w-/]+/g)].map(m => m[0].trim()); const unsupported = cmds.length > 0 && cmds.every(c => !isCommandAllowed(c, whitelist).allowed)（有命令且全失支持才标 unsupported，D-11-7）；实际上更精确：cmds.some 失支持即标（保守，宁可多标）；用 cmds.some(c => !isCommandAllowed(c, whitelist).allowed) && cmds.length>0 (c) 命中刷新：try { incReuseCount(row.id); touchLastVerifiedAt(row.id); } catch (err) { console.warn('[experienceRetrieval] refresh failed', row.id, (err as Error).message); }（D-11-9 不阻塞） (d) injected.push({ exp_id: row.id, title: row.title, content: row.content, source_session_id: row.source_session_id ?? null, unsupported }) }
    7. return { demoMode: false, injected, reranked: passed, finalAnswer: input.userMessage }

    ai.ts chat() 修改（ai.ts:673-807）：
    - 顶部 import: import { retrieveForAnswer } from './experienceRetrieval'（仿 ai.ts 现有 service import 风格）
    - 在 :684 execMode 后、:687 systemPrompt 构建前（或 :724 fullMessages 拼装前），插入：
      const userMessage = messages[messages.length - 1]?.content || ''
      const retrieval = await retrieveForAnswer({ userMessage, deviceIds })
      let expReferences: Array<{ exp_id: string; title: string; source_session_id: string | null; unsupported: boolean }> = []
      if (retrieval.injected.length > 0) {
        const expContext = retrieval.injected.map((e, i) => `[经验${i + 1}: ${e.title}${e.unsupported ? '（⚠ 此条命令已失支持，请提示用户手动执行或更新白名单）' : ''}]\n${e.content}`).join('\n\n')
        systemPrompt += `\n\n以下是经验库中检索到的相关经验（仅供参考，回答末尾无需标注来源）：\n${expContext}`
        expReferences = retrieval.injected.map(e => ({ exp_id: e.exp_id, title: e.title, source_session_id: e.source_session_id ?? null, unsupported: e.unsupported }))
      }
    - 在 :800-807 commands 为空分支：if (kbReferences.length > 0) return JSON.stringify({type:'kb_answer',...}) 之后追加：if (expReferences.length > 0) return JSON.stringify({ type: 'exp_answer', content: finalAiReply, references: expReferences })（注意：KB 与经验 references 不互斥，但本期 exp_answer 独立类型，Plan 02 renderer 按 references 字段渲染联合类型；若 kbReferences 与 expReferences 同时存在，优先返 exp_answer 含两者 references——planner 定：合并 references 数组，type 仍 'exp_answer'，renderer 联合类型兼容）
    - 注意 retrieveForAnswer 是 async，chat() 已是 async（ai.ts:673），await 无需改签名。
    - 错误隔离：retrieveForAnswer 整体 try/catch 包裹（粗筛/精排异常不阻塞 chat 主路径，D-11-9），catch 时 console.warn + expReferences 保持 []，chat 继续正常答（无经验注入）。

    单测 electron/services/experienceRetrieval.test.ts（追加编排用例，复用 Task 1 的 vi.mock('./ai')）：需 mock listExperiences/incReuseCount/touchLastVerifiedAt（vi.mock('./experienceService')）+ isCommandAllowed（vi.mock('./commandSafety')）。覆盖：retrieveForAnswer 空库返 empty 不调精排；demoMode 返 demoMode:true；有勾选设备走 deviceId 窄查分支；无勾选走 search 宽匹配分支；精排 score<阈值过滤掉；有效期失效剔除；命令全失支持标 unsupported=true；incReuseCount 失败不阻塞（try/catch）；命中即 incReuseCount+touchLastVerifiedAt 各调一次。
    chat() 串联测试不在本 task（ai.ts chat 涉及 fetch+DB 重，留给集成验证，本 task 聚焦 service 层可测函数）。
  </action>
  <verify>
    <automated>npx vitest run electron/services/experienceRetrieval.test.ts --reporter=default 2>&1; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - electron/services/experienceRetrieval.ts 存在且 `grep -E "export (async function|interface|const) (retrieveForAnswer|RetrieveInput|RetrieveResult|INJECT_LIMIT|MAX_CANDIDATES)" experienceRetrieval.ts` 全命中
    - `grep -c "class " electron/services/experienceRetrieval.ts` 输出 0
    - `grep -c "encField\|decField" electron/services/experienceRetrieval.ts` 输出 0（不碰加密列）
    - `grep -n "retrieveForAnswer" electron/services/ai.ts` 至少 1 处（chat 串联注入点）
    - `grep -n "exp_answer" electron/services/ai.ts` 至少 1 处（返回类型）
    - retrieveForAnswer 空库返 injected:[] 且不调 rerank（vitest 断言 callAIMock not called）
    - retrieveForAnswer demoMode 返 demoMode:true（vitest 断言）
    - retrieveForAnswer 有勾选设备时 listExperiences 被调含 deviceId（vitest 断言 opts.deviceId）
    - retrieveForAnswer 命中即 incReuseCount + touchLastVerifiedAt 各调一次（vitest 断言）
    - retrieveForAnswer 命令全失支持标 unsupported=true（vitest 断言）
    - vitest experienceRetrieval.test.ts 全部 pass，退出码 0
    - npx tsc -p tsconfig.web.json 无新增 error（ai.ts 改动类型安全）；npm run build:electron-main 成功（esbuild 打包 ai.ts + 新 service 无解析错误）
  </acceptance_criteria>
  <done>编排 service retrieveForAnswer 落地（粗筛→精排→阈值→read-time 两项验证→刷新计数→返注入元数据），chat() 串联 b 自动预取注入 + exp_answer references 返回，编排单测全绿，tsc + build:electron-main 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main（ai:chat） | 用户问题文本经 ai:chat 入参跨边界，进入 chat()→retrieveForAnswer 粗筛/精排 |
| main→LLM（精排 callAI） | 候选经验摘要送外部 LLM，凭证片段若混入会泄漏 |
| main→renderer（ai:chat 返回 JSON） | references 元数据回 renderer，attrs 密文不得越界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure (PII→LLM) | experienceRerank.buildRerankPrompt / experienceRetrieval 精排候选构造 | mitigate | 精排 prompt 只送 exp_id+title+content 前150字，**不送 attrs 凭证字段**（attrs.resolution 含命令可送但不送 attrs 里的账号/密码/IP）；候选经 listExperiences 已解密 attrs，但精排构造 content_preview 时只取 row.content 不取 row.attrs 凭证；可复用 Phase 8 piiMask 兜底但本期 content 是已确认 published 经验正文（经人工确认无明文凭证，红线③ 兜底），风险评估为低 |
| T-11-02 | Tampering (命令白名单绕过) | experienceRetrieval read-time isCommandAllowed | mitigate | 复用 commandSafety.isCommandAllowed 三层校验（分隔符注入拦截→黑名单→白名单严格相等），正文命令扫描保守：cmds.some 失支持即标 unsupported（宁可多标降权不漏放）；命令提取正则限定只读首词（display/show/ping/traceroute/debug/terminal/interface），不提取变更类 |
| T-11-03 | Information Disclosure (引用泄露私有经验) | ai.ts chat() exp_answer references | mitigate | references 只含 exp_id/title/source_session_id/unsupported，**不含 attrs 密文/凭证片段**；与现有 stripEncColumns 边界一致（renderer 永不收 _enc 列）；references 数据从 retrieveForAnswer.injected 记录拿（D-11-11 不需 AI 标记，避免 AI 把正文凭证抄进 references） |
| T-11-04 | Injection (经验库 SQL 注入) | experienceRetrieval 粗筛 listExperiences | mitigate | 复用 listExperiences 参数化（? 占位 + params.push，Phase 10 已防 T-10-01）；userMessage 经 search 参数走 LIKE 占位不拼字符串；精排 prompt 不拼用户原始输入进 SQL（只进 LLM messages） |
| T-11-05 | Denial of Service (检索阻塞主路径) | experienceRetrieval retrieveForAnswer / chat() 串联 | mitigate | D-11-9 不阻塞：retrieveForAnswer 内刷新失败 try/catch + console.warn；chat() 串联整体 try/catch，异常时 expReferences=[] 继续正常答；空库短路不调精排 LLM；INJECT_LIMIT=5 + MAX_CANDIDATES=20 防 context 溢出 |
| T-11-06 | Spoofing/Tampering (LLM 编造 exp_id) | experienceRerank.validateRerank | mitigate | validateRerank 校验 exp_id 必须在候选集 candidateExpIds 中（防 LLM 编造不存在的经验 id 注入），不在则 fail 触发重试 |
</threat_model>

<verification>
四绿门禁（沿用 Phase 7-10 模式）：
1. `npx tsc -p tsconfig.web.json` 无新增 error（strict + noUnusedLocals）
2. `npm run build` (vite renderer build) 成功
3. `npm run build:electron-main` (esbuild main/preload 打包) 成功，新 service experienceRerank.ts/experienceRetrieval.ts + ai.ts 改动无解析错误
4. `npx vitest run` 全绿（含新 experienceRetrieval.test.ts 用例 + 既有 200 用例零回归）

手动验证（service 层，不需实机 UI）：
- 精排 schema Gate：mock callAI 返 ```json [{...}]``` 包裹 + 编造 exp_id → validateRerank fail 重试
- 编排空库短路：mock listExperiences 返 rows:[] → retrieveForAnswer 不调 rerank 返 empty
- read-time 验证：mock row.invalid_at 过期 → 剔除；mock cmds 全失支持 → unsupported=true
</verification>

<success_criteria>
- SC1（RETRIEVE-01）：每轮 AI 对话自动预取经验，SQL 粗筛（listExperiences search/deviceId）+ LLM 精排（rerank 强 schema 打分）+ 阈值过滤，经验正文注入正式 callAI context ✓
- SC2（RETRIEVE-02，修订两项）：read-time 验证 commandSafety 白名单 + 有效期，命令失支持降权 unsupported 标注、有效期失效剔除，命中刷新 incReuseCount/touchLastVerifiedAt，不验设备状态（D-11-8），不阻塞主路径（D-11-9）✓
- SC4：检索默认 includeInvalid=false 只命中有效经验（invalid_at IS NULL OR > now），与浏览页软失效一致 ✓
- 红线：零迁移零新表（D-11-5，不上 FTS5）✓；零加密列触碰（精排/编排 service 函数式无 MK Pattern 1b）✓；renderer 永不收 attrs 密文（references 只元数据）✓
</success_criteria>

<output>
Create `.planning/phases/11-ai-retrieval-reuse/11-01-SUMMARY.md` when done
</output>
