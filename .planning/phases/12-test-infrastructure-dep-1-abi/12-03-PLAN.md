---
phase: 12-test-infrastructure-dep-1-abi
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - tests/electron/handleLeak.real.test.ts
  - .github/workflows/build-smoke.yml
autonomous: false
requirements_addressed: [TEST-02]
requirements: [TEST-02]

must_haves:
  truths:
    - "句柄泄漏专项测试构造异常场景（对端 RST / stream error / 长时间运行）验各 service cleanup 路径无泄漏"
    - "build-smoke.yml CI 扩展（方案 CI-A：mock 套件移到 rebuild 前，test:electron 挂 rebuild 后）锁真路径回归网"
    - "ELECTRON_RUN_AS_NODE 在 GHA windows-latest 实跑确认不需 xvfb/--no-sandbox"
    - "CI 时长增量实测记录"
    - "生产代码零改动（git diff --exit-code electron/ 成功）"
  artifacts:
    - path: "tests/electron/handleLeak.real.test.ts"
      provides: "句柄泄漏专项测试（异常场景 cleanup 集中验证）"
      min_lines: 40
    - path: ".github/workflows/build-smoke.yml"
      provides: "CI 扩展（test:electron 挂 rebuild 后，mock 套件移 rebuild 前）"
      contains: "test:electron"
  key_links:
    - from: "tests/electron/handleLeak.real.test.ts"
      to: "tests/electron/_helpers/handleLeakDetector.ts"
      via: "expectNoHandleLeak 注册 afterEach + 构造异常场景"
      pattern: "expectNoHandleLeak"
    - from: ".github/workflows/build-smoke.yml"
      to: "package.json test:electron"
      via: "CI step npm run test:electron 挂 rebuild:native 之后"
      pattern: "npm run test:electron"
---

<objective>
落地句柄泄漏专项测试（异常场景集中验证，补强 Plan 12-02 的正常/timeout 路径覆盖）+ build-smoke.yml CI 扩展（方案 CI-A：mock 与真路径分段，锁两条回归网）。闭合 Phase 6 SC#4 + Phase 3 长时间运行 defer 项。

Purpose: TEST-02 的句柄检测除了正常/timeout 路径（Plan 12-02 覆盖），还需异常场景（对端 RST、stream error、长时间运行）的专项验证，对应 Phase 3 长时间运行 defer。CI 扩展让真路径回归网进 CI，避免 DEP-1 缓解后回归网只在本地跑（SC2「CI/本地绿」精神）。

Output: 1 个句柄泄漏专项测试 + 1 个 CI workflow 扩展（需用户 plan review 确认 CI 方案 + 实跑验证 GHA 可行性）。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-CONTEXT.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-RESEARCH.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-PATTERNS.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-01-SUMMARY.md

<interfaces>
<!-- Plan 12-01 helper 契约（复用） -->

From tests/electron/_helpers/handleLeakDetector.ts（Plan 12-01 Task 2）:
```typescript
export function expectNoHandleLeak(extraAllow?: string[]): void
// 调用点取 baseline，注册 afterEach 对比 process.getActiveResourcesInfo
```

From tests/electron/_helpers/mockSshServer.ts（Plan 12-01 Task 2）:
```typescript
export function startMockSshServer(onExec: (cmd: string) => string): Promise<{ port: number, close: () => Promise<void> }>
```

<!-- 现有 CI step 序列（analog，本 plan 扩展） -->

From .github/workflows/build-smoke.yml:17-33（RESEARCH + PATTERNS 实读）:
```yaml
- run: npm ci
- run: npm run rebuild:native   # rebuild 后切 electron-ABI
- run: npm run build
- run: npm test                 # ← DEP-1 隐患：rebuild 后 plain node 跑会 ABI 崩
- name: verify native binding built for electron ABI
  run: |
    test -f node_modules/better-sqlite3/build/Release/better_sqlite3.node
    test -f dist-electron/main.js
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 句柄泄漏专项测试（异常场景 cleanup 集中验证 —— Phase 3 长时间运行 defer 闭合）</name>
  <files>tests/electron/handleLeak.real.test.ts</files>
  <read_first>
    - 被测源：electron/services/ai.ts:386-510（cleanup + execOne stream error 路径）+ arpCollector.ts:25-78（timeout destroy 路径）+ telnetExec.ts:111-157（finally destroy 路径）
    - analog：electron/services/ai.telnetRouting.test.ts:69-76（beforeEach mockClear + afterEach 生命周期范式）
    - helper 契约：tests/electron/_helpers/handleLeakDetector.ts + mockSshServer.ts + mockTelnetServer.ts（Plan 12-01 落地）
    - source-of-truth：12-RESEARCH.md §句柄泄漏检测机制（getActiveResourcesInfo + wtfnode）、§目标 cleanup 路径清单（四条路径）、§测试用例优先级 P2（异常场景）、§Common Pitfalls Pitfall 4/5（mock server close 异步 + baseline 漂移）
    - source-of-truth：12-CONTEXT.md decision #4（句柄泄漏自动化替代 Phase 6 SC#4 + Phase 3 defer 项）
    - source-of-truth：12-PATTERNS.md §handleLeakDetector（108-150）+ §afterEach 清理（539-551）
  </read_first>
  <action>
    用 Write 工具创建 tests/electron/handleLeak.real.test.ts。

    import `{ describe, it, expect, afterAll, vi } from 'vitest'`、`{ Client } from 'ssh2'`、`{ Telnet } from 'telnet-client'`、`{ startMockSshServer } from './_helpers/mockSshServer'`、`{ startMockTelnetServer } from './_helpers/mockTelnetServer'`、`{ expectNoHandleLeak } from './_helpers/handleLeakDetector'`。
    调用 `expectNoHandleLeak(['TCPWrap'])`。

    describe('句柄泄漏专项（异常场景 cleanup）') 内含多个 it，每 it 构造一种异常场景验 cleanup：

    - it 1（对端 RST —— SSH）：startMockSshServer，onExec 内对特定命令调 `stream.destroy()` 或 server 主动 close 连接模拟 RST，调 executeCommandsOnDevice/execOne，验不挂起 + afterEach 无泄漏（ssh2 Client cleanup 的 client.end/destroy 路径）。
    - it 2（stream error —— execOne）：mockSshServer onExec 触发 stream 'error' 事件，验 execOne 的 stream.close() + stream.destroy() + clearTimeout(silenceTimer) cleanup。
    - it 3（telnet 对端 EOF / 连接断）：mockTelnetServer 主动 socket.end() 模拟断连，验 executeTelnetCommand finally cleanup 的 connection.end/destroy。
    - it 4（长时间运行多次连接循环）：循环 N 次（如 5）连 mockSshServer 跑短命令，验每次 cleanup 后无累积 TCPWrap（模拟 Phase 3 长时间运行 defer 场景，afterEach 检测累积泄漏）。
    - it 5（混合 timeout + 正常）：先跑一次短 timeout 触发 timeout cleanup，再跑一次正常连接，验 timeout 路径的 destroy 不影响后续连接的句柄干净。

    afterAll 关闭所有 mock server（await close，Pitfall 4）。

    **vi.mock 策略**：同 Plan 12-02 Task 1，mock 非被测重依赖（commandSafety / knowledgeBaseService / aiExecLogger / telnetExec 分流等）让 service 干净加载，不 mock ssh2/telnet-client（被测协议走真库）。

    **Pitfall 5 重点**：每个 it 内 baseline 在调用点取（handleLeakDetector 已实现），不在 beforeAll；afterEach 前 sleep(50) 给 cleanup 异步时间。若 it 4 循环场景偶发误报，加 extraAllow=['TCPWrap'] 或调大 sleep。

    **wtfnode 诊断后备**：若某 it 报泄漏但看不出调用栈，handleLeakDetector 自动 import('wtfnode').dump() 打印 —— executor 据输出定位是哪条 cleanup 漏，修测试断言或回溯被测代码（但被测代码改动属 SC4 红线，除非确认是真 bug 否则只调测试）。
  </action>
  <verify>
    <automated>cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs run --config vitest.electron.config.ts tests/electron/handleLeak.real.test.ts（期望：5 个 it 绿，无句柄泄漏报错，wtfnode 不触发 dump） && git diff --exit-code electron/（退出码 0）</automated>
  </verify>
  <acceptance_criteria>
    - handleLeak.real.test.ts 存在，`grep -c "describe.*句柄泄漏专项" tests/electron/handleLeak.real.test.ts == 1`
    - 至少 4 个 it（grep -c "^  it(" >= 4），覆盖 RST + stream error + telnet 断连 + 循环累积至少四类异常场景
    - 含 `expectNoHandleLeak` + `startMockSshServer` + `startMockTelnetServer`（真协议库不 vi.mock）
    - 含循环场景（grep -c "for.*\|\.forEach\|循环\|loop\|N 次" >= 1，长时间运行 defer 覆盖）
    - afterAll 含 mock server close（grep -c "\.close()" >= 1）
    - `npm run test:electron -- tests/electron/handleLeak.real.test.ts` 退出码 0，无 `句柄泄漏:` 报错
    - `git diff --exit-code electron/` 退出码 0（生产零改动，被测代码 cleanup 已在生产，本测试只验证不改）
    - `npm test` 退出码 0，现有 mock 套件无回归
  </acceptance_criteria>
  <done>
    句柄泄漏专项覆盖异常场景（RST/stream error/telnet 断/循环累积），闭合 Phase 3 长时间运行 defer。Phase 6 SC#4 + Phase 3 defer 的人工 HV 项可标 resolved（CONTEXT decision #4）。
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CI 扩展方案决策（CI-A vs CI-B vs CI-C）—— 用户 plan review 定</name>
  <files>.github/workflows/build-smoke.yml</files>
  <decision>build-smoke.yml CI 扩展方案：方案 CI-A（mock 套件移 rebuild 前 + test:electron 挂 rebuild 后，分段，+~1min）/ 方案 CI-B（全量 electron.exe 跑，删 npm test step）/ 方案 CI-C（test:electron 只本地跑，CI 暂不挂）三选一。</decision>
  <context>
    RESEARCH.md §CI 扩展 给三方案 + 推荐 CI-A。关键决策点：
    - CI-A：mock 套件（plain node）放 rebuild:native **之前**（避免 rebuild 后 ABI 崩），test:electron（真路径）放 rebuild 之后。两段 CI 锁两条回归网，时长 +30-60s（真路径网络操作）。**RESEARCH 推荐**。
    - CI-B：mock 套件也挪进 test:electron（全量 electron.exe 跑），删 npm test step。CI 简化但 mock 套件也变 electron.exe 跑，时长 +更多，且改变 mock 套件运行环境（潜在 vi.mock 行为差异）。
    - CI-C：test:electron 只本地跑，CI 暂不挂。保守，但 CI 不锁真路径回归（违背 SC2「CI/本地绿」精神）。

    关键 checkpoint（RESEARCH 标 ASSUMED 未实跑）：ELECTRON_RUN_AS_NODE 在 GHA windows-latest 是否需 xvfb/--no-sandbox（基于语义推断不需，但未实跑）；CI 时长增量未实测；antivirus 误报风险（electron.exe 在 runner 上已知案例）。

    本 checkpoint 在 Task 3 落地 CI 前由用户决策。若用户选 CI-C，跳过 Task 3 的 workflow 改动（仅记 SUMMARY）。
  </context>
  <options>
    <option id="ci-a">
      <name>CI-A：mock 与真路径分段（推荐）</name>
      <pros>两条回归网都进 CI；mock 套件保留 plain node 秒级；时长代价可接受 ~+1min；RESEARCH 推荐</pros>
      <cons>需重排现有 step 顺序（npm test 移到 rebuild 前）；CI 时长增量需实测确认</cons>
    </option>
    <option id="ci-b">
      <name>CI-B：全量 electron.exe 跑</name>
      <pros>CI 简化（单一 test:electron step）；所有测试统一 electron-ABI 环境</pros>
      <cons>mock 套件运行环境改变（潜在 vi.mock 行为差异风险）；时长增量更大；需重验 244 mock 用例在 electron.exe 下全绿</cons>
    </option>
    <option id="ci-c">
      <name>CI-C：只本地跑，CI 暂不挂</name>
      <pros>零 CI 改动，零风险；保守</pros>
      <cons>违背 SC2「CI/本地绿」精神；真路径回归网只在本地，CI 不锁；DEP-1 缓解价值打折</cons>
    </option>
  </options>
  <action>暂停执行，等待用户在 plan review 时从 ci-a / ci-b / ci-c 三方案中选定 CI 扩展路径。本 task 不自行实现代码，仅产出决策供 Task 3 消费。</action>
  <verify>用户回复 ci-a / ci-b / ci-c 之一</verify>
  <done>用户选定 CI 方案并回复 resume-signal，Task 3 据此落地（ci-c 则跳过 workflow 改动仅记 SUMMARY）。</done>
  <resume-signal>Select: ci-a, ci-b, or ci-c（选 ci-c 则 Task 3 跳过 workflow 改动，仅记 SUMMARY；选 ci-a/ci-b 进 Task 3 落地）</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: build-smoke.yml CI 扩展落地（按 Task 2 用户选定方案）+ GHA 可行性实跑确认</name>
  <files>.github/workflows/build-smoke.yml</files>
  <read_first>
    - 被改文件：.github/workflows/build-smoke.yml（全文，看 steps:17-33 现有序列）
    - analog：.github/workflows/build-smoke.yml:17-33（现有 step 序列，本 task 扩展）
    - source-of-truth：12-RESEARCH.md §CI 扩展（三方案 + 推荐 CI-A + GHA windows-latest 可行性 ASSUMED）、§Pitfall（rebuild 后 npm test ABI 崩隐患）
    - source-of-truth：12-CONTEXT.md `<deferred>` CI 扩展 gray area（planner 评估成本后建议，用户 plan review 定）
    - source-of-truth：12-PATTERNS.md §.github/workflows/build-smoke.yml（449-492）
  </read_first>
  <action>
    **前置**：本 task 按 Task 2 用户选定方案落地。若用户选 ci-c，跳过本 task workflow 改动，仅记 SUMMARY「CI 暂不挂，本地跑」。

    **方案 CI-A 落地**（推荐，若用户选 ci-a）：用 Edit 工具改 .github/workflows/build-smoke.yml steps 段。重排 + 新增：
    - `npm ci`（不动）
    - `npm test`（**移到 rebuild:native 之前** —— plain node 跑 mock 套件 244 用例，不碰 native binding，避免 rebuild 后 ABI 崩，RESEARCH Pitfall）
    - `npm run rebuild:native`（不动）
    - `npm run build`（不动）
    - `npm run test:electron`（**新增** —— rebuild 后 electron.exe 跑真路径套件）
    - `verify native binding` step（不动）

    **方案 CI-B 落地**（若用户选 ci-b）：
    - `npm ci`
    - `npm run rebuild:native`（移到 test 前）
    - `npm run build`
    - `npm run test:electron`（替换原 npm test —— 全量 electron.exe 跑，需验 mock 套件 244 用例在 electron.exe 下全绿，若有 vi.mock 失效则退回 CI-A）
    - `verify native binding`

    **GHA windows-latest 可行性实跑确认（RESEARCH 标 ASSUMED，checkpoint）**：本 task 改 workflow 后，需 push 到分支触发 GHA 实跑（或本地用 `act` 模拟，但 act 对 electron.exe 支持有限，优先真 GHA）。验证：
    - ELECTRON_RUN_AS_NODE=1 electron.exe 在 windows-latest runner 能起（不需 xvfb / --no-sandbox —— 基于语义推断 RUN_AS_NODE 不走 Chromium）
    - test:electron step 退出码 0（真路径套件绿）
    - CI 总时长增量记录（对比改前改后，RESEARCH 估 +1min）
    - 若 antivirus 误报致 electron.exe 无法执行，加 `Set-ExecutionPolicy` 或路径白名单（RESEARCH 已识别此风险）

    若 GHA 实跑失败（如 xvfb 缺失 / antivirus 拦截），回到 Task 2 重新决策（可能退 ci-c 只本地跑，记 SUMMARY 暴露 GHA 局限）。
  </action>
  <verify>
    <automated>yaml-lint .github/workflows/build-smoke.yml（语法验，或 npx --yes yamllint）+ git diff --exit-code electron/（退出码 0，CI 改动不碰生产代码）</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/build-smoke.yml 含 `npm run test:electron` step（grep -c "npm run test:electron" >= 1）
    - CI-A：`npm test` step 在 `npm run rebuild:native` **之前**（grep -n 显示 test 行号 < rebuild:native 行号；或 CI-B：原 npm test 被替换为 test:electron）
    - workflow YAML 语法合法（yamllint 无 error）
    - GHA 实跑确认（push 分支触发，或记 SUMMARY 标注待下次 push 验证）：test:electron step 退出码 0，ELECTRON_RUN_AS_NODE 在 windows-latest 可行（不需 xvfb/--no-sandbox）
    - CI 时长增量记录在 SUMMARY（实测值，对比 RESEARCH 估的 +1min）
    - `git diff --exit-code electron/` 退出码 0（CI 改动不碰生产）
    - 若 GHA 实跑失败（xvfb/antivirus），SUMMARY 记录失败原因 + 退回方案（可能 ci-c）
  </acceptance_criteria>
  <done>
    build-smoke.yml CI 扩展按用户选定方案落地。真路径回归网进 CI（CI-A/B）或显式 defer 仅本地（CI-C，记 SUMMARY）。GHA windows-latest 可行性实跑确认。Phase 12 全部 SC + REQ 覆盖完毕。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA windows-latest runner ↔ electron.exe | CI runner 执行 electron.exe（ELECTRON_RUN_AS_NODE 模式），须确认不走 Chromium 不需 xvfb；antivirus 误报风险 |
| CI 日志 ↔ 公开仓库 | test:electron 输出经 CI 日志，须沿用脱敏范式不打印真实设备 IP（用 127.0.0.1 mock） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | GHA runner electron.exe antivirus 误报 | mitigate | RESEARCH 已识别此风险；实跑确认若拦截加 Set-ExecutionPolicy 或路径白名单；失败则退 ci-c 记 SUMMARY |
| T-12-10 | Information Disclosure | CI 日志含敏感信息 | accept | 测试用 127.0.0.1 mock + test/test 占位凭证，无真实设备 IP/凭证；CI 日志沿用 secure()/safe() 脱敏范式 |
| T-12-11 | Denial of Service | CI 时长增量过长 | accept | RESEARCH 估 +1min 可接受；实测记录；若过长优化测试（减少循环次数 / 并行） |
| T-12-05 | Elevation | CI test:electron 通道绕三红线 | accept | 同 T-12-05（12-01/12-02），测试通道是消费方；git diff electron/ 兜底验生产零改动 |
</threat_model>

<verification>
- 三绿门禁之一：`npm run test:electron`（含本 plan 句柄专项 + 12-01 DB + 12-02 SSH/Telnet 全部真路径套件）
- 三绿门禁之二：`npm test`（mock 套件无回归）
- 三绿门禁之三：`npm run build:electron-main`
- SC4 兜底：`git diff --exit-code electron/`
- CI-A/B 落地后 GHA 实跑确认（push 分支触发）
- TEST-02 异常场景句柄泄漏专项覆盖（闭合 Phase 3 长时间运行 defer）
- Phase 6 SC#4 + Phase 3 defer 项可标 resolved（CONTEXT decision #4）
</verification>

<success_criteria>
- TEST-02 异常场景句柄泄漏专项测试落地（RST/stream error/telnet 断/循环累积）
- CI 扩展按用户选定方案落地（CI-A 推荐 / CI-B / CI-C），GHA windows-latest 可行性实跑确认
- Phase 6 SC#4 + Phase 3 长时间运行 defer 项闭合（可标 resolved）
- 生产零改动（git diff electron/ 空）
- 现有 mock 套件无回归
- Phase 12 全部 SC（SC1-4）+ REQ（TEST-01/02）覆盖完毕
</success_criteria>

<output>
Create `.planning/phases/12-test-infrastructure-dep-1-abi/12-03-SUMMARY.md` when done（含 CI 方案最终选择 + GHA 实跑结果 + CI 时长增量 + 句柄专项覆盖的异常场景清单 + defer 项 resolved 标注建议）
</output>
