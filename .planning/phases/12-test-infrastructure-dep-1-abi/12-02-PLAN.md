---
phase: 12-test-infrastructure-dep-1-abi
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - tests/electron/ai.execCommands.real.test.ts
  - tests/electron/arpCollector.real.test.ts
  - tests/electron/telnetExec.real.test.ts
autonomous: true
requirements_addressed: [TEST-01, TEST-02]
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "SSH 真路径测试经 mockSshServer（ssh2.Server）回显，executeCommandsOnDevice / execOne / executeSSH 走真实 ssh2.Client 连 mock 对端"
    - "Telnet 真路径测试经 mockTelnetServer（net.Server echo + IAC 协商）回显，executeTelnetCommand 走真实 telnet-client 连 mock 对端"
    - "四条 cleanup 路径（executeSSH / executeCommandsOnDevice / execOne / executeTelnetCommand）的 try/finally 句柄回收经 handleLeakDetector 自动化验证无泄漏"
    - "测试用真实 ssh2/telnet-client（不 vi.mock），仅 mock 非被测重依赖（commandSafety / crypto / knowledgeBaseService / aiExecLogger）让 service 干净加载"
    - "生产代码零改动（git diff --exit-code electron/ 成功）"
  artifacts:
    - path: "tests/electron/ai.execCommands.real.test.ts"
      provides: "executeCommandsOnDevice + execOne 真路径 SSH 回归 + cleanup 句柄检测"
      min_lines: 60
    - path: "tests/electron/arpCollector.real.test.ts"
      provides: "executeSSH 真路径 SSH 回归 + ARP 解析 + cleanup 句柄检测"
      min_lines: 50
    - path: "tests/electron/telnetExec.real.test.ts"
      provides: "executeTelnetCommand 真路径 telnet 回归 + IAC 协商 + finally cleanup 句柄检测"
      min_lines: 50
  key_links:
    - from: "tests/electron/ai.execCommands.real.test.ts"
      to: "tests/electron/_helpers/mockSshServer.ts"
      via: "import startMockSshServer + 真实 ssh2.Client 连 mock 对端"
      pattern: "import.*startMockSshServer"
    - from: "tests/electron/ai.execCommands.real.test.ts"
      to: "tests/electron/_helpers/handleLeakDetector.ts"
      via: "expectNoHandleLeak() 注册 afterEach 句柄检测"
      pattern: "expectNoHandleLeak"
    - from: "tests/electron/telnetExec.real.test.ts"
      to: "tests/electron/_helpers/mockTelnetServer.ts"
      via: "import startMockTelnetServer + 真实 telnet-client 连 mock echo"
      pattern: "import.*startMockTelnetServer"
    - from: "git diff"
      to: "electron/"
      via: "phase gate 兜底验生产零改动"
      pattern: "git diff --exit-code electron/"
---

<objective>
落地 SSH（ai.ts executeCommandsOnDevice/execOne + arpCollector.ts executeSSH）与 Telnet（telnetExec.ts executeTelnetCommand）的真路径回归测试，复用 Plan 12-01 的 mockSshServer/mockTelnetServer/handleLeakDetector/realDb 四个 helper。覆盖 SC2 之 SSH/Telnet 部分 + TEST-02 四条 cleanup 路径的句柄泄漏自动化检测。

Purpose: DEP-1 缓解后，native binding 真路径回归网首次覆盖 SSH/Telnet 协议链路（此前仅 mock）。同时把 Phase 6 SC#4 + Phase 3 长期 defer 的人工 HV 句柄泄漏项转自动化（CONTEXT decision #4）。

Output: 3 个真路径测试文件，覆盖 4 条 cleanup 路径 + 真实协议回显。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-CONTEXT.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-RESEARCH.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-PATTERNS.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-01-SUMMARY.md

<interfaces>
<!-- Plan 12-01 落地的 helper 契约（直接复用，不重写） -->

From tests/electron/_helpers/mockSshServer.ts（Plan 12-01 Task 2）:
```typescript
export function startMockSshServer(onExec: (cmd: string) => string): Promise<{ port: number, close: () => Promise<void> }>
// 镜像 ssh2.Client 的 connect/authentication/exec 序列，监听 127.0.0.1 随机端口
```

From tests/electron/_helpers/mockTelnetServer.ts（Plan 12-01 Task 2）:
```typescript
export function startMockTelnetServer(onCmd: (cmd: string) => string, shellPrompt?: string): Promise<{ port: number, close: () => Promise<void> }>
// net.Server echo + IAC 协商响应，监听 127.0.0.1 随机端口
```

From tests/electron/_helpers/handleLeakDetector.ts（Plan 12-01 Task 2）:
```typescript
export function expectNoHandleLeak(extraAllow?: string[]): void
// 调用点取 baseline，注册 afterEach 对比 process.getActiveResourcesInfo，泄漏 throw + wtfnode.dump()
```

From tests/electron/_helpers/realDb.ts（Plan 12-01 Task 2）:
```typescript
export function makeRealDb(opts?: { runMigrations?: boolean }): { db: Database.Database, dbPath: string, close: () => void }
// 临时 DB 文件 + WAL + createTables/runMigrations + close 清理侧车
```

<!-- 被测生产源 cleanup 路径（TEST-02 目标）—— RESEARCH.md 已逐行核对 -->

ai.ts executeCommandsOnDevice cleanup（352-440）+ execOne cleanup（450-510）:
```typescript
// ai.ts cleanup 持 ssh2 Client + perCmdTimer
const cleanup = (): void => {
  if (perCmdTimer) { clearTimeout(perCmdTimer); perCmdTimer = undefined }
  try { client.end() } catch { /* ignore */ }
}
// execOne: stream.close() + stream.destroy() + clearTimeout(timer/silenceTimer)
```

arpCollector.ts executeSSH cleanup（25-78）:
```typescript
const cleanup = (): void => {
  if (timer) { clearTimeout(timer); timer = undefined }
  try { client.end() } catch { /* ignore */ }
}
// timeout 路径追加 client.destroy()
```

telnetExec.ts executeTelnetCommand finally cleanup（111-157）:
```typescript
// .finally(async): clearTimeout + 非 timeout 路径 connection.end() + connection.destroy()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SSH 真路径回归（executeCommandsOnDevice + execOne + executeSSH）+ cleanup 句柄检测</name>
  <files>tests/electron/ai.execCommands.real.test.ts, tests/electron/arpCollector.real.test.ts</files>
  <read_first>
    - 被测源：electron/services/ai.ts:352-510（executeCommandsOnDevice + execOne + cleanup 持 ssh2 Client/perCmdTimer/stream）
    - 被测源：electron/services/arpCollector.ts:1-78（executeSSH + cleanup + timeout destroy 路径 + ARPParser.parse）
    - analog：electron/services/ai.telnetRouting.test.ts:1-110（vi.mock ssh2 Client 范式 —— **真路径测试反向操作：不 mock ssh2，用真 Client 连 mockSshServer**）+ ai.saveChatMessage.test.ts:26-44（mock 非被测重依赖让 service 干净加载范式）
    - helper 契约：tests/electron/_helpers/mockSshServer.ts（Plan 12-01 落地）+ tests/electron/_helpers/handleLeakDetector.ts + tests/electron/_helpers/realDb.ts
    - source-of-truth：12-RESEARCH.md §真路径 mock 对端方案（ssh2.Server 双向）、§目标 cleanup 路径清单（ai.ts 352-510 + arpCollector.ts 25-78）、§测试用例优先级 P0/P1
    - source-of-truth：12-PATTERNS.md §ai.execCommands.real.test.ts（321-360）+ §arpCollector.real.test.ts（362-404）+ §vi.mock 让 service 干净加载（528-537）
    - source-of-truth：12-01-SUMMARY.md（A1 入口最终路径 + OQ#1 注入策略确认 + telnet IAC 处理 —— arpCollector 真路径持 DB，DB 注入策略同 12-01）
  </read_first>
  <action>
    用 Write 工具创建两个测试文件。

    **ai.execCommands.real.test.ts**：
    import `{ describe, it, expect, afterAll, vi, beforeEach } from 'vitest'`、`{ Client } from 'ssh2'`（**真实** import，不 vi.mock('ssh2')）、`{ startMockSshServer } from './_helpers/mockSshServer'`、`{ expectNoHandleLeak } from './_helpers/handleLeakDetector'`。
    调用 `expectNoHandleLeak(['TCPWrap'])`（TCPWrap 临时放行，因 ssh2 connect 短暂持 socket，afterEach 50ms 内应释放；若仍误报再加）。

    **vi.mock 策略（让 ai.ts 干净加载，仅 mock 非被测重依赖）**：参考 ai.saveChatMessage.test.ts:26-44 analog，mock `./commandSafety`（返 isCommandAllowed always allowed）、`./knowledgeBaseService`、`./aiExecLogger`、`../utils/telnetExec`（executeTelnetCommand 走另一 task 测，此处 mock 避免级联）、crypto 模块（若 ai.ts 顶层 import crypto）。**不 mock ssh2**（被测协议走真 Client）。

    describe 内：
    - beforeAll 起 mockSshServer：`const ssh = await startMockSshServer((cmd) => { if (cmd.includes('show version')) return 'MockDevice# show version\nVersion 1.0\n'; return 'unknown cmd\n' })`，记录 `ssh.port`。
    - afterAll `await ssh.close()`（Pitfall 4 异步 close）。
    - it 1（executeCommandsOnDevice 正常路径）：vi.mock 让 service 加载后 import executeCommandsOnDevice，调 `executeCommandsOnDevice({ host: '127.0.0.1', port: ssh.port, username: 'test', password: 'test', connectionType: 'ssh', commands: ['show version'] })`，断言返回结果含 `'Version 1.0'`（mock server 回显）。**入参透传断言**参考 ai.telnetRouting.test.ts:94-106 analog（host/port/username/password/command 透传）。
    - it 2（execOne stream cleanup —— 句柄泄漏检测核心）：构造 execOne 单命令路径，正常完成后 afterEach 经 expectNoHandleLeak 验无 TCPWrap/Timeout 残留。
    - it 3（telnet 分流反向断言）：connectionType=telnet 时 executeCommandsOnDevice 走 executeTelnetCommand（已 vi.mock），断言 ssh2.Client 未被实例化（反向：真路径不连 SSH server）。
    - it 4（异常路径 cleanup）：构造对端不响应/超时场景（如 mockSshServer onExec 故意不回 + 短 timeout），验 cleanup 触发 + afterEach 无泄漏。

    **arpCollector.real.test.ts**：
    import 同上 + `{ makeRealDb } from './_helpers/realDb'`（arpCollector 经 getDatabase 持久化 arp_entries，需 DB 注入）+ `{ setArpMasterKey } from '../electron/services/arpCollector'`（MK 注入，参考 PATTERNS §masterKey 注入）。
    **OQ#1 注入策略落地（用户 review 点）**：用 vi.mock 注入 `../database/connection` 的 getDatabase 返回 realDb.db 实例。代码形态：`vi.mock('../electron/database/connection', () => ({ getDatabase: () => realDbInstance }))` —— 在 beforeAll 调 makeRealDb 拿 realDbInstance，vi.mock 工厂引用之。**若 vi.mock 牵连过广**（ai/arpCollector/experienceService 等都 import connection，级联 mock 失效），退方案 B：在 connection.ts 加 `export function _setDbGetter(fn)` @internal（与 experienceService `_setExperienceDbGetter` 同范式 1-2 行），届时 git diff electron/ 含 connection.ts 微改，需在 SUMMARY 记录并标用户 review。executor 先试 vi.mock，失败再退方案 B。

    describe 内：
    - it 1（executeSSH 正常路径）：startMockSshServer onCmd 回显 ARP 表样例输出（参考 arpCollector 真实 ARP 输出格式），调 collectFromDevice connectionType=ssh，断言 ARPParser.parse 解析出 IP/MAC 条目。
    - it 2（executeSSH cleanup 无泄漏）：expectNoHandleLeak 验 ssh2 Client + setTimeout timer 清理。
    - it 3（executeSSH timeout 路径）：构造对端不响应 + 短 timeout，验 cleanup 触发 client.destroy() + afterEach 无泄漏 + reject `SSH timeout after Xms`。
  </action>
  <verify>
    <automated>cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs run --config vitest.electron.config.ts tests/electron/ai.execCommands.real.test.ts tests/electron/arpCollector.real.test.ts（期望：两文件所有 it 绿，加载真实 ssh2 native binding 不 throw ABI，afterEach 无句柄泄漏报错） && git diff --exit-code electron/（期望退出码 0，除非退方案 B 加 _setDbGetter 则仅 connection.ts 1-2 行微改）</automated>
  </verify>
  <acceptance_criteria>
    - ai.execCommands.real.test.ts 存在，`grep -c "from 'ssh2'" tests/electron/ai.execCommands.real.test.ts == 1`（真实 ssh2 import）
    - ai.execCommands.real.test.ts **不含** `vi.mock('ssh2'`（grep -c "vi.mock.'ssh2'" == 0，真路径不 mock ssh2，Pitfall 反向）
    - ai.execCommands.real.test.ts 含 `vi.mock('./commandSafety'` 或等价非被测依赖 mock（让 service 干净加载）+ `startMockSshServer` + `expectNoHandleLeak`
    - ai.execCommands.real.test.ts 至少 3 个 it（grep -c "^  it(" >= 3），含正常路径 + cleanup + telnet 反向断言
    - arpCollector.real.test.ts 存在，含 `startMockSshServer` + `makeRealDb`（DB 注入）+ `expectNoHandleLeak` + ARPParser.parse 相关断言
    - arpCollector.real.test.ts 含 MK 注入（grep -c "setArpMasterKey\|MK_TEST" >= 1，不用真实 masterKey）
    - 两测试文件 afterAll 含 mock server close（grep -c "ssh.close\|await.*close" >= 1 per file，Pitfall 4）
    - `npm run test:electron -- tests/electron/ai.execCommands.real.test.ts tests/electron/arpCollector.real.test.ts` 退出码 0，无 `NODE_MODULE_VERSION` ABI 错，无 `句柄泄漏:` 报错
    - `git diff --exit-code electron/` 退出码 0（方案 A vi.mock）；若退方案 B，git diff electron/ 仅含 connection.ts `_setDbGetter` 1-2 行 @internal 微改（SUMMARY 记录 + 用户 review）
    - `npm test` 退出码 0，现有 mock 套件无回归
  </acceptance_criteria>
  <done>
    SSH 真路径回归覆盖 executeCommandsOnDevice + execOne + executeSSH 三路径 + 四条 cleanup 路径中的三条（executeSSH/executeCommandsOnDevice/execOne）句柄泄漏自动化检测全绿。arpCollector 从 0 测试覆盖到有真路径。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Telnet 真路径回归（executeTelnetCommand）+ finally cleanup 句柄检测 + IAC 协商实跑确认</name>
  <files>tests/electron/telnetExec.real.test.ts</files>
  <read_first>
    - 被测源：electron/utils/telnetExec.ts:1-160（executeTelnetCommand + pickDisablePaginationCmd + pickShellPrompt + finally cleanup 持 telnet-client Telnet + setTimeout timer）
    - analog：electron/services/ai.telnetRouting.test.ts:44-50（vi.mock telnetExec importActual+spy —— **真路径测试反向：不 mock telnetExec，测它本身**）
    - helper 契约：tests/electron/_helpers/mockTelnetServer.ts（Plan 12-01 落地，含 IAC 协商处理）+ handleLeakDetector
    - source-of-truth：12-RESEARCH.md §真路径 mock 对端方案（net.Server echo）、§目标 cleanup 路径清单（telnetExec.ts 111-157 finally）、§测试用例优先级 P1、§Pitfall（mock server close 异步）
    - source-of-truth：12-PATTERNS.md §telnetExec.real.test.ts（408-445）
    - source-of-truth：12-01-SUMMARY.md（telnet IAC 处理策略 —— Plan 12-01 Task 2 落地的 IAC 响应方式，本 task 连真 telnet-client 实跑确认是否卡住）
  </read_first>
  <action>
    用 Write 工具创建 tests/electron/telnetExec.real.test.ts。

    import `{ describe, it, expect, afterAll, beforeEach } from 'vitest'`、`{ Telnet } from 'telnet-client'`（**真实** import，不 vi.mock telnet-client）、`{ executeTelnetCommand, pickDisablePaginationCmd, pickShellPrompt } from '../../electron/utils/telnetExec'`（**真路径 import 被测本体**）、`{ startMockTelnetServer } from './_helpers/mockTelnetServer'`、`{ expectNoHandleLeak } from './_helpers/handleLeakDetector'`。
    调用 `expectNoHandleLeak(['TCPWrap'])`（telnet-client 短暂持 socket）。

    describe 内：
    - beforeAll 起 mockTelnetServer：`const telnet = await startMockTelnetServer((cmd) => { if (cmd.includes('show version')) return 'Version 1.0'; return 'unknown' }, 'mock>')`。
    - afterAll `await telnet.close()`。
    - it 1（正常路径回显）：调 `executeTelnetCommand({ host: '127.0.0.1', port: telnet.port, username: 'test', password: 'test', command: 'show version', timeout: 5000 })`（具体入参签名以 telnetExec.ts 实际为准，codegraph 查 executeTelnetCommand 签名），断言返回含 `'Version 1.0'`。
    - it 2（finally cleanup 无泄漏）：正常完成后 afterEach 经 expectNoHandleLeak 验 telnet-client Telnet 实例 + setTimeout timer 清理。
    - it 3（timeout 路径 cleanup）：构造对端不响应 + 短 timeout（如 timeout: 200 + mockTelnetServer onCmd 故意延迟），验 finally cleanup 触发 connection.destroy() + reject `Telnet timeout` + afterEach 无泄漏。
    - it 4（pickDisablePaginationCmd / pickShellPrompt vendor 分流）：参考 telnetExec.ts 内 vendor 分流逻辑，断言不同 vendor 返回不同 pagination/shellPrompt 命令（业务逻辑回归）。

    **telnet IAC 协商实跑确认（Plan 12-01 Task 2 落地 IAC 处理后的验证 checkpoint）：** 本 task 连真实 telnet-client 实跑。若 it 1 connect 卡住超 testTimeout（15000ms），说明 mockTelnetServer 的 IAC 响应不完整，回到 Plan 12-01 Task 2 mockTelnetServer.ts 修 IAC 字节处理（telnet-client 默认发 WILL/WONT，server 须回 DONT/WONT 或吞掉 IAC 字节）。在 SUMMARY 记录 IAC 协商最终策略。这是 RESEARCH.md + PATTERNS.md 标记的未完全展开 checkpoint。
  </action>
  <verify>
    <automated>cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs run --config vitest.electron.config.ts tests/electron/telnetExec.real.test.ts（期望：4 个 it 绿，telnet-client connect 不卡 IAC 协商，afterEach 无泄漏） && git diff --exit-code electron/（退出码 0）</automated>
  </verify>
  <acceptance_criteria>
    - telnetExec.real.test.ts 存在，`grep -c "from 'telnet-client'" tests/electron/telnetExec.real.test.ts == 1`（真实 telnet-client import）
    - telnetExec.real.test.ts **不含** `vi.mock.*telnetExec`（grep -c "vi.mock.*telnetExec" == 0，测 telnetExec 本体不 mock 它）
    - telnetExec.real.test.ts 含 `startMockTelnetServer` + `expectNoHandleLeak` + 真实 `import.*executeTelnetCommand.*telnetExec`
    - telnetExec.real.test.ts 至少 3 个 it（正常 + cleanup + timeout），grep -c "^  it(" >= 3
    - telnetExec.real.test.ts afterAll 含 `telnet.close`（Pitfall 4）
    - `npm run test:electron -- tests/electron/telnetExec.real.test.ts` 退出码 0，connect 不卡（无超 testTimeout 15000），无 ABI 错，无句柄泄漏报错
    - telnet IAC 协商实跑通过（connect 不卡），策略记 SUMMARY；若卡住回到 12-01 mockTelnetServer 修 IAC
    - `git diff --exit-code electron/` 退出码 0
    - `npm test` 退出码 0，现有 mock 套件无回归
  </acceptance_criteria>
  <done>
    Telnet 真路径回归覆盖 executeTelnetCommand + finally cleanup（第四条 cleanup 路径）句柄泄漏自动化检测全绿。telnetExec 从 0 测试覆盖到有真路径。telnet IAC 协商实跑确认通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 真实 ssh2.Client / telnet-client ↔ mockSshServer / mockTelnetServer | 真路径测试用真实协议库连 mock 对端，mock 对端监听 127.0.0.1 loopback |
| arpCollector 真路径 ↔ 临时 DB | arpCollector 经 getDatabase 持久化 arp_entries，测试用 realDb 临时文件注入（vi.mock 或 _setDbGetter），含测试数据非真实凭证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Information Disclosure | 真路径测试日志含 mock 凭证 | accept | 测试用 test/test 占位凭证连 127.0.0.1 mock，非真实设备凭证；CI 日志沿用 secure()/safe() 脱敏范式不打印真实 IP |
| T-12-07 | Tampering | vi.mock 注入 getDatabase 牵连过广致方案 B 改生产 | mitigate | 优先 vi.mock 零侵入；退方案 B 时 git diff electron/ 仅 connection.ts 1-2 行 @internal _setDbGetter（与 _setExperienceDbGetter 同范式），用户 review 后方可提交 |
| T-12-08 | Denial of Service | mock server 未 close 致端口耗尽 | mitigate | afterAll await ssh.close()/telnet.close()（Pitfall 4），handleLeakDetector 兜底检测残留 TCPWrap |
| T-12-05 | Elevation | 真路径测试绕 commandSafety | accept | 测试 mock commandSafety 让 service 加载，但不进生产；git diff electron/ 兜底验生产 commandSafety 零改动；生产 commandSafety 仍由 npm test 既有套件回归 |
</threat_model>

<verification>
- 三绿门禁之一：`npm run test:electron`（含本 plan 落地的 SSH + Telnet 真路径套件 + 12-01 DB 套件）
- 三绿门禁之二：`npm test`（mock 套件无回归）
- 三绿门禁之三：`npm run build:electron-main`（main bundle 绿）
- SC4 兜底：`git diff --exit-code electron/`（方案 A 退出码 0；方案 B 仅 connection.ts 微改记 SUMMARY）
- TEST-02 四条 cleanup 路径全部覆盖：executeSSH / executeCommandsOnDevice / execOne（Task 1）+ executeTelnetCommand（Task 2）
- telnet IAC 实跑确认（Plan 12-01 mockTelnetServer 落地的 IAC 响应经真实 telnet-client connect 验证不卡）
</verification>

<success_criteria>
- SC2 之 SSH/Telnet 部分达成：SSH 真路径（executeCommandsOnDevice + execOne + executeSSH）+ Telnet 真路径（executeTelnetCommand）经 mock 对端回显全绿，无需真实设备
- TEST-02 达成：四条 cleanup 路径句柄泄漏自动化检测全绿（替代 Phase 6 SC#4 + Phase 3 defer 人工 HV，CONTEXT decision #4）
- OQ#1 注入策略落地确认（vi.mock 方案 A 或退方案 B 记 SUMMARY + 用户 review）
- telnet IAC 协商实跑通过
- SC4 兜底：生产零改动（或方案 B 仅 connection.ts 微改）
- 现有 mock 套件无回归
</success_criteria>

<output>
Create `.planning/phases/12-test-infrastructure-dep-1-abi/12-02-SUMMARY.md` when done（含 OQ#1 注入策略最终选择 + telnet IAC 实跑结果 + 四条 cleanup 路径覆盖确认 + git diff electron/ 验证）
</output>
