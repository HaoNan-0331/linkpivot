---
phase: 12-test-infrastructure-dep-1-abi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.electron.config.ts
  - tests/electron/_helpers/realDb.ts
  - tests/electron/_helpers/mockSshServer.ts
  - tests/electron/_helpers/mockTelnetServer.ts
  - tests/electron/_helpers/handleLeakDetector.ts
  - tests/electron/db.real.test.ts
autonomous: true
requirements_addressed: [TEST-01]
requirements: [TEST-01]

user_setup:
  - service: "npm registry（装 wtfnode）"
    why: "wtfnode 句柄泄漏诊断后备（getActiveResourcesInfo 仍主用，wtfnode 仅 dump 调用栈）"
    env_vars: []
    dashboard_config:
      - task: "装包前确认代理 127.0.0.1:10809 已开，或临时用 --registry=https://registry.npmjs.org 直连（RESEARCH.md 已验证直连可用，npmmirror proxy 当前 ECONNREFUSED）"
        location: "终端 npm config 或命令行 --registry flag"

must_haves:
  truths:
    - "npm run test:electron 在 rebuild:native 后能加载 electron-ABI better-sqlite3 native binding，不 throw ABI 冲突"
    - "test:electron script 独立挂真路径套件，未挂到既有 test script（rebuild 后 plain node npm test 仍绿跑 mock 套件）"
    - "DB 真路径测试经真实 better-sqlite3 prepared statement 跑 INSERT/SELECT/UPDATE/DELETE + createTables + runMigrations 连跑两次幂等"
    - "四个测试 helper（realDb/mockSshServer/mockTelnetServer/handleLeakDetector）作为接口契约被 Plan 12-02/12-03 复用"
    - "生产代码零改动（git diff --exit-code electron/ 成功，SC4 红线兜底）"
    - "现有 mock 套件（npm test）全绿无回归（16/17 文件 244 用例不动）"
  artifacts:
    - path: "vitest.electron.config.ts"
      provides: "真路径套件独立 vitest 配置"
      contains: "include: ['tests/electron/**/*.test.ts']"
    - path: "package.json"
      provides: "test:electron script（+1，不改既有任何 script）"
      contains: "test:electron"
    - path: "tests/electron/_helpers/realDb.ts"
      provides: "临时 DB 文件 + WAL + createTables/runMigrations + close 清理侧车文件"
      exports: ["makeRealDb"]
    - path: "tests/electron/_helpers/mockSshServer.ts"
      provides: "ssh2.Server 内存级 SSH 对端，监听 127.0.0.1 随机端口"
      exports: ["startMockSshServer"]
    - path: "tests/electron/_helpers/mockTelnetServer.ts"
      provides: "net.Server telnet echo 对端 + IAC 协商响应"
      exports: ["startMockTelnetServer"]
    - path: "tests/electron/_helpers/handleLeakDetector.ts"
      provides: "process.getActiveResourcesInfo snapshot 对比 + wtfnode best-effort 诊断"
      exports: ["expectNoHandleLeak"]
    - path: "tests/electron/db.real.test.ts"
      provides: "getDatabase 真路径 CRUD + 迁移幂等回归"
      min_lines: 40
  key_links:
    - from: "package.json test:electron"
      to: "vitest.electron.config.ts"
      via: "cross-env ELECTRON_RUN_AS_NODE=1 electron.exe vitest.mjs --config"
      pattern: "test:electron.*ELECTRON_RUN_AS_NODE.*vitest\\.electron\\.config"
    - from: "tests/electron/db.real.test.ts"
      to: "tests/electron/_helpers/realDb.ts"
      via: "import { makeRealDb }"
      pattern: "import.*makeRealDb.*realDb"
    - from: "git diff"
      to: "electron/"
      via: "phase gate 兜底检查生产代码零改动"
      pattern: "git diff --exit-code electron/"
---

<objective>
建立 DEP-1 ABI 缓解的测试基础设施主干：用 ELECTRON_RUN_AS_NODE=1 electron.exe 跑 vitest 加载 @electron/rebuild 重建的 native binding（SC1），落地 DB 真路径回归（SC2 之 DB 部分），并产出四个测试 helper 作为 Plan 12-02/12-03 的接口契约（SSH/Telnet/句柄检测的真路径测试均复用）。

Purpose: 这是 Phase 12 的地基 plan。SC4 红线（生产零改动）下唯一可接受的产物是测试通道 + 工具配置。本 plan 把后续 plan 依赖的 helper 契约先定下来，避免 12-02/12-03 各自探索产生接口漂移。

Output: 1 个 vitest config + 1 个 npm script + 4 个 helper + 1 个 DB 真路径测试 + wtfnode 装包 + SC4 git diff 兜底。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-CONTEXT.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-RESEARCH.md
@.planning/phases/12-test-infrastructure-dep-1-abi/12-PATTERNS.md

<interfaces>
<!-- 现有接口契约 —— executor 直接用，不需探索 codebase -->

From electron/database/connection.ts（生产建库路径，realDb helper 复刻其 pragma 序列，但不调它 —— 它 import 了 electron app/backupScheduler，测试侧绕开）:
```typescript
// connection.ts:16-19
export function getDatabase(): Database.Database {
  if (!db) throw new Error('Database not initialized')
  return db
}
// connection.ts:21-35 initDatabase（realDb 复刻 pragma 但用 os.tmpdir() 路径 + 不调 app.getPath）
db.pragma('journal_mode = WAL')
db.pragma('foreign_keys = ON')
db.pragma('busy_timeout = 5000')
db.pragma('wal_autocheckpoint = 1000')
```

From electron/services/experienceService.ts:35-45（@internal db getter 注入范式 —— 本 plan 不采用，因 OQ#1 决策优先 vi.mock 零侵入，详见 Task 2 action）:
```typescript
let dbGetter: () => Database.Database = getDatabase
export function _setExperienceDbGetter(fn: () => Database.Database): void { dbGetter = fn }
```

From vitest.config.ts（analog，本 plan 新 config 与其物理隔离）:
```typescript
// 现有 plain Node mock 套件 config —— 严禁新 config include 重复采集 electron/**
include: ['tests/**/*.test.ts', 'electron/**/*.test.ts']
```

From package.json:13-16（现有 scripts，本 plan 只追加 test:electron，不改任一既有）:
```
"rebuild:native": "electron-rebuild -f -w better-sqlite3 -w ssh2"
"test": "vitest run"
"test:watch": "vitest"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 装 wtfnode + 落地双 vitest config + test:electron script（SC1 主干）</name>
  <files>package.json, package-lock.json, vitest.electron.config.ts</files>
  <read_first>
    - 被改文件：package.json（全文，已读 1-57，看 scripts:6-17 + devDependencies:35-52）
    - analog：vitest.config.ts（全文 1-18，新 config 复刻 defineConfig + environment=node 结构，改 include + 删 server.deps.inline + 加 timeout）
    - source-of-truth：12-RESEARCH.md §electron-vite 评估（否决迁移，最小侵入方案）、§Pattern 1/2（test:electron script 形态 + 双 config 物理隔离）、§Pitfall 2（直连 electron.exe 路径非 CLI）、§Pitfall 6（新 config include 严禁含 electron/**）、§Package Legitimacy Audit（wtfnode [OK]）、§Environment Availability（装包需代理或直连 registry）
    - source-of-truth：12-PATTERNS.md §vitest.electron.config.ts（落地形态 60-70）+ §package.json scripts（93-104）
  </read_first>
  <action>
    分三步，严格按序：

    第一步（装 wtfnode）：执行 `npm install -D wtfnode`。RESEARCH.md 已验证 npmmirror proxy 当前 ECONNREFUSED，装包前确认代理 127.0.0.1:10809 已开；若代理未开，临时用 `npm install -D wtfnode --registry=https://registry.npmjs.org` 直连（已验证可用）。装包后 package.json devDependencies 出现 `"wtfnode": "^0.10.1"`，package-lock.json 同步更新。不装 electron-vite / electron-vitest（已否决，RESEARCH §electron-vite 评估）。

    第二步（新建 vitest.electron.config.ts）：用 Write 工具创建。内容包含 `import { defineConfig } from 'vitest/config'`，导出 defineConfig 对象，test 段含：`environment: 'node'`、`include: ['tests/electron/**/*.test.ts']`（严格限定，严禁含 `electron/**` —— Pitfall 6）、`testTimeout: 15000`（ssh2/telnet 网络操作）、`hookTimeout: 10000`。**不写** `server.deps.inline`（真路径用 electron-ABI 真 binding，不 inline 转换，Pitfall 6）。

    第三步（package.json scripts 追加 test:electron）：在 scripts 段追加一行（用 Edit 工具，不改既有任何 script）：
    `"test:electron": "cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs run --config vitest.electron.config.ts"`
    关键点（Pitfall 2）：直连 `node_modules/electron/dist/electron.exe` 路径，**不用** `electron` CLI（CLI 拉起 BrowserWindow）；`cross-env` 已在 devDependencies（package.json:43），复用零新增。

    **A1 checkpoint（RESEARCH.md Assumptions Log A1，待验证）：** vitest 4 入口路径 `node_modules/vitest/vitest.mjs` 是 RESEARCH 语义推断未实跑。executor 落地后须先验入口能否被 electron.exe 调起：执行 `cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs --version`，期望输出 vitest 版本号 4.1.x。若该路径不存在或报错，fallback 试 `node_modules/vitest/dist/cli.js` 或 `node_modules/.bin/vitest`（vitest 4 标准 bin），相应修正 test:electron script 的入口路径段。fallback 选定后在 SUMMARY.md 记录实际入口。
  </action>
  <verify>
    <automated>npm run test:electron -- --reporter=verbose 2>&1 | head -20（期望：vitest 启动 + 因 tests/electron/ 暂无测试文件而报 "No test files found" 或 0 test passed —— 这一步只验 electron.exe 能加载 vitest 入口 + 读到 vitest.electron.config.ts，不验测试内容；A1 入口若错则此命令报 MODULE_NOT_FOUND 或 EISDIR，需 fallback）</automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies 含 `"wtfnode"` 键（grep -v '^#' package.json | grep -c '"wtfnode"' == 1）
    - package.json scripts 含 `"test:electron"` 键，且其值含 `ELECTRON_RUN_AS_NODE=1` 与 `electron.exe` 与 `vitest.electron.config.ts` 三标识符（grep -v '^#' package.json | grep -c 'test:electron.*ELECTRON_RUN_AS_NODE.*electron\\.exe.*vitest\\.electron\\.config' == 1）
    - package.json 现有 `test` script 值仍为 `"vitest run"` 未变（grep -v '^#' package.json | grep -c '"test": "vitest run"' == 1）
    - package.json 无 `electron-vite` / `electron-vitest` 任一键（grep -E 'electron-vite' package.json 退出码非 0）
    - vitest.electron.config.ts 文件存在，含 `include: ['tests/electron/**/*.test.ts']` 且**不含** `electron/**` 字样的 include（grep -c "tests/electron/\\*\\*/\\*.test\\.ts" vitest.electron.config.ts == 1；grep -c "include.*electron/\\*\\*" vitest.electron.config.ts == 0）
    - vitest.electron.config.ts 含 `testTimeout: 15000` 与 `hookTimeout: 10000`
    - vitest.electron.config.ts **不含** `server` 段或 `deps.inline`（grep -c 'deps' vitest.electron.config.ts == 0）
    - `cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs --version` 输出含 `4.1`（A1 入口验证通过；若 fallback，SUMMARY.md 记录实际入口路径）
  </acceptance_criteria>
  <done>
    wtfnode 装入 devDependencies；vitest.electron.config.ts 独立 config 落地（物理隔离 mock 套件）；test:electron script 挂上 electron.exe + ELECTRON_RUN_AS_NODE 入口；A1 checkpoint 实跑确认 vitest 4 入口可被 electron.exe 调起（或 fallback 已记）。后续 plan 复用此通道。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 落地四个测试 helper（realDb / mockSshServer / mockTelnetServer / handleLeakDetector —— Plan 12-02/12-03 接口契约）</name>
  <files>tests/electron/_helpers/realDb.ts, tests/electron/_helpers/mockSshServer.ts, tests/electron/_helpers/mockTelnetServer.ts, tests/electron/_helpers/handleLeakDetector.ts</files>
  <read_first>
    - analog：electron/database/connection.ts:21-35（initDatabase pragma 序列，realDb 复刻 WAL/foreign_keys/busy_timeout/wal_autocheckpoint 但用 os.tmpdir 路径）
    - analog：electron/services/experienceService.ts:35-45（_setExperienceDbGetter @internal 范式 —— OQ#1 参考，本 plan 不改生产）
    - analog：electron/services/arpCollector.ts:1, 25-75（ssh2.Client 被测用法 —— mockSshServer 须镜像的 connect/exec/authentication 序列）
    - analog：electron/utils/telnetExec.ts:111-157（telnet-client 消费方协议序列 —— mockTelnetServer 须响应的 loginPrompt/passwordPrompt/shellPrompt）
    - analog：electron/services/connection.ts:5（import net from 'net' —— mockTelnetServer 复用）
    - analog：tests/unit/migrationHelpers.test.ts:1-14（最小 util/helper 函数式 export 范式）
    - analog：electron/services/ai.telnetRouting.test.ts:1, 69-76（vitest import + beforeEach/afterEach 生命周期范式）
    - source-of-truth：12-RESEARCH.md §真路径 mock 对端方案（mockSshServer 219-241 / mockTelnetServer 244-265 / realDb 268-286）、§句柄泄漏检测机制（handleLeakDetector 167-191）、§Common Pitfalls Pitfall 3/4/5（侧车文件清理 / mock server close 异步 / baseline 漂移）
    - source-of-truth：12-PATTERNS.md §各 helper 落地形态（108-150 handleLeakDetector / 152-185 mockSshServer / 188-233 mockTelnetServer / 237-286 realDb）
  </read_first>
  <action>
    用 Write 工具创建 4 个 helper 文件，置于 tests/electron/_helpers/ 目录。

    **realDb.ts**：import `Database from 'better-sqlite3'`、`fs from 'fs'`、`path from 'path'`、`os from 'os'`。导出 `makeRealDb(opts?: { runMigrations?: boolean })` 返回 `{ db: Database.Database, dbPath: string, close: () => void }`。实现：dbPath 用 `path.join(os.tmpdir(), 'nt-test-' + Date.now() + '-' + Math.random().toString(36).slice(2) + '.db')`（唯一防并发撞，Pitfall 3）；`new Database(dbPath)`；复刻 connection.ts pragma 四行（journal_mode=WAL / foreign_keys=ON / busy_timeout=5000 / wal_autocheckpoint=1000）；调 `createTables(db)` 与 `runMigrations(db)`（从 electron/database/init.ts 与 migrations.ts import，传 db 参数 —— 这些函数若签名不接受 db 参数则需在测试内联同等 DDL，executor 先 codegraph 查 createTables/runMigrations 签名确认）。close() 严格顺序：`db.close()` → `fs.unlinkSync(dbPath)` → `fs.unlinkSync(dbPath + '-wal')` → `fs.unlinkSync(dbPath + '-shm')`，后三个包 try/catch 容错 ENOENT（Pitfall 3 侧车文件残留）。

    **mockSshServer.ts**：import `{ Server } from 'ssh2'`、`crypto from 'crypto'`。导出 `startMockSshServer(onExec: (cmd: string) => string): Promise<{ port: number, close: () => Promise<void> }>`。实现：用 `crypto.generateKeyPairSync('rsa', { modulusLength: 2048 })` 生成随机测试 hostKey（**不写真实凭证**，安全域威胁 T-12-01）；`new Server({ hostKeys: [privateKeyPem] }, clientHandler)`；clientHandler 内 `client.on('authentication', ctx => ctx.accept())`（接受任意凭证）、`client.on('ready', () => client.on('session', accept => { const session = accept(); session.on('exec', (accept, _, { command }) => { const stream = accept(); stream.end(onExec(command)) }) }))`；`server.listen(0, '127.0.0.1', ...)`（端口 0 随机分配，**仅 loopback**，T-12-02）；resolve `{ port: server.address().port, close: () => new Promise(res => server.close(() => res())) }`（close 返回 Promise 等 close 回调，Pitfall 4）。

    **mockTelnetServer.ts**：import `net from 'net'`。导出 `startMockTelnetServer(onCmd: (cmd: string) => string, shellPrompt?: string): Promise<{ port: number, close: () => Promise<void> }>`，shellPrompt 默认 `'mock>'`。实现：`net.createServer(socket => { socket.write(shellPrompt); socket.on('data', buf => { const cmd = buf.toString(); /* telnet IAC 协商处理（checkpoint 见下）*/ const trimmed = cmd.replace(/\r?\n$/, '').trim(); if (trimmed) socket.write(onCmd(trimmed) + '\r\n' + shellPrompt) }) })`；`server.listen(0, '127.0.0.1', ...)`；close 同 mockSshServer。

    **telnet IAC 协商 checkpoint（RESEARCH + PATTERNS 标记未完全展开）：** telnet-client connect 默认发 IAC WILL/WONT 协商字节（0xFF 0xFB/0xFC 子序列）。若 mock server 不响应 DONT/WONT 或不显式吞掉 IAC 字节，telnet-client connect 会卡住。executor 须在 socket.on('data') 内识别 buf[0]===0xFF 的 IAC 序列并回 `0xFF 0xFE`（DONT）或 `0xFF 0xFC`（WONT）最小协商响应，或显式忽略 IAC 字节只处理明文命令。落地后在 Task 3（Plan 12-02 telnetExec.real.test）连真实 telnet-client 实跑验证；若 connect 卡住超 testTimeout，回到本 task 修 IAC 响应。在 SUMMARY.md 记录最终 IAC 处理策略。

    **handleLeakDetector.ts**：import `{ afterEach } from 'vitest'`。导出 `expectNoHandleLeak(extraAllow: string[] = [])`。实现：函数内**调用点**（非 beforeAll）取 `const baseline = process.getActiveResourcesInfo()`（数组，Pitfall 5：紧贴被测代码执行前取，不在 beforeAll）；`const allowDefault = ['Timeout', 'GetAddrInfoReqWrap']`（放行 vitest runner 自身常见句柄）；`const allow = new Set([...allowDefault, ...extraAllow])`；注册 `afterEach(async () => { await new Promise(r => setTimeout(r, 50))（给 ssh2.end() 异步 EOF 时间，Pitfall 4）; const after = process.getActiveResourcesInfo(); const leaked = after.filter(h => !allow.has(h) && !baseline.includes(h)); if (leaked.length > 0) { const wtf = await import('wtfnode').catch(() => null); if (wtf) (wtf as any).dump(); throw new Error('句柄泄漏: ' + JSON.stringify(leaked)) } })`。
  </action>
  <verify>
    <automated>npx tsc --noEmit tests/electron/_helpers/*.ts 2>&1 | head -30（验类型通过；若 tsconfig 限定 include 范围不含 tests/，改用 `npx tsc --noEmit -m esnext --target es2022 --moduleResolution bundler --esModuleInterop tests/electron/_helpers/*.ts` 单独 type-check 这 4 个文件）</automated>
  </verify>
  <acceptance_criteria>
    - tests/electron/_helpers/realDb.ts 存在且 `export function makeRealDb`（grep -c 'export function makeRealDb' == 1）
    - realDb.ts 含 `os.tmpdir()` 与 `Date.now()` 与 `Math.random()`（唯一文件名，Pitfall 3）+ 三个 `-wal`/`-shm` unlink（grep -c "dbPath + '-wal'" >= 1 && grep -c "dbPath + '-shm'" >= 1）
    - realDb.ts 含 try/catch 容错 ENOENT（grep -c 'catch' >= 1）
    - realDb.ts 含四行 pragma（journal_mode.*WAL / foreign_keys.*ON / busy_timeout / wal_autocheckpoint 各 == 1）
    - mockSshServer.ts 存在且 `export function startMockSshServer`，含 `new Server` 与 `hostKeys` 与 `crypto.generateKeyPairSync`（随机 hostKey，T-12-01 不写死凭证）+ `server.listen(0, '127.0.0.1'`（仅 loopback，T-12-02）+ `close` 返回 `new Promise`（Pitfall 4 异步 close）
    - mockSshServer.ts **不含** 写死的 password/privateKey 字面凭证（grep -iE 'password.*=.*["'\''][a-zA-Z0-9]{4,}' mockSshServer.ts 退出码非 0）
    - mockTelnetServer.ts 存在且 `export function startMockTelnetServer`，含 `net.createServer` 与 `server.listen(0, '127.0.0.1'`，且 data handler 内含 IAC 字节处理（`0xFF` 或 `255` 字面量出现，证明 IAC 协商已落地，T-12 checkpoint）
    - handleLeakDetector.ts 存在且 `export function expectNoHandleLeak`，含 `process.getActiveResourcesInfo()` 与 `afterEach` 与 `setTimeout(r, 50)`（Pitfall 4/5）+ `import('wtfnode').catch(() => null)`（best-effort 诊断）
    - 四个文件 tsc type-check 无 error（除非 tsconfig include 范围限制，则在 SUMMARY 记录并改用单文件 type-check 命令）
  </acceptance_criteria>
  <done>
    四个 helper 落地，构成 Plan 12-02（SSH/Telnet 真路径）与 12-03（句柄泄漏）的接口契约。realDb 用临时文件 + 真实 better-sqlite3 + 真实迁移；mockSshServer 用 ssh2.Server + 随机 hostKey + loopback；mockTelnetServer 含 IAC 协商；handleLeakDetector 用 getActiveResourcesInfo snapshot + wtfnode 后备。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DB 真路径回归测试（SC2 之 DB 部分）+ SC4 生产零改动 git diff 兜底</name>
  <files>tests/electron/db.real.test.ts</files>
  <read_first>
    - 被改文件：tests/electron/db.real.test.ts（新建，无现状）
    - analog：tests/unit/migrationHelpers.test.ts:1-14（DB 桩 API shape + vitest import 范式）+ electron/database/migrations.test.ts:100-168（迁移幂等断言结构 —— 连跑两次第二次 no-op）
    - analog：tests/electron/_helpers/realDb.ts（Task 2 刚落地，makeRealDb 契约）
    - source-of-truth：12-RESEARCH.md §测试用例优先级 P0（db.real.test.ts getDatabase 真路径 CRUD + 迁移幂等）、§Validation Architecture §Phase Requirements → Test Map（TEST-01 DB 行）
    - source-of-truth：12-CONTEXT.md decision #3（渐进保留 mock 套件，真路径并行新增，npm test 与 test:electron 物理隔离）
  </read_first>
  <action>
    用 Write 工具创建 tests/electron/db.real.test.ts。

    import：`{ describe, it, expect, afterEach } from 'vitest'`、`{ makeRealDb } from './_helpers/realDb'`。

    describe('DB 真路径回归（electron-ABI better-sqlite3）')，内含至少三个 it：

    it 1（真路径 CRUD）：调 `const { db, close } = makeRealDb()`，建一张测试表 `db.exec('CREATE TABLE t (id INTEGER PRIMARY KEY, x TEXT)')`，prepared statement 复用 `const ins = db.prepare('INSERT INTO t (x) VALUES (?)')`，`ins.run('a')` + `ins.run('b')`；`const rows = db.prepare('SELECT * FROM t ORDER BY id').all()`，`expect(rows).toHaveLength(2)` 且 `expect(rows[0].x).toBe('a')`；UPDATE `db.prepare('UPDATE t SET x = ? WHERE id = ?').run('c', 1)` 验 `rows[0].x === 'c'`；DELETE `db.prepare('DELETE FROM t WHERE id = ?').run(2)` 验剩余 1 行；afterEach close()。

    it 2（迁移幂等）：`const { db, close } = makeRealDb({ runMigrations: true })`，验迁移已跑（如验 user_version >= 1 或某迁移后表/列存在，具体查 migrations.ts MIGRATION_HEAD 当前值）；**连跑第二次** makeRealDb 在同一 db 上再调 runMigrations（或新开 makeRealDb 复用同 dbPath —— 但 Pitfall 3 文件名唯一，故第二次用新 makeRealDb 验 fresh-install 路径迁移 idempotent；或同 db 调 runMigrations 两次验 no-op）。断言：第二次 runMigrations 不抛（hasColumn 守卫命中），关键表结构未变。具体断言结构参考 migrations.test.ts:101-113 analog。

    it 3（WAL 模式生效）：`const { db, close } = makeRealDb()`，`const mode = db.pragma('journal_mode') as Array<{journal_mode: string}>`，`expect(mode[0].journal_mode.toLowerCase()).toBe('wal')`。

    afterEach 内 close()（realDb.close() 自带 db.close + unlink 侧车）。

    **OQ#1 决策记录（RESEARCH.md Open Question #1，planner 已决，用户 review 点）：** 本测试**不**经 getDatabase() 单例（因 connection.ts import electron app 等重依赖，vi.mock 整模块牵连过广），而是直接持有 makeRealDb() 返回的真实 better-sqlite3 实例跑 CRUD/迁移。这验证了「electron-ABI better-sqlite3 在 electron.exe 内可加载 + 真实 CRUD + 迁移」（TEST-01 核心断言）。对**消费 getDatabase 的 service**（ai/arpCollector/experienceService）的真路径测试（Plan 12-02），用 vi.mock 注入 `../database/connection` 的 getDatabase 返回 realDb 实例 —— 该策略在 Plan 12-02 Task action 内落地。**零生产代码改动**（SC4 红线最优解）。executor 若发现 makeRealDb 调 createTables/runMigrations 时这些函数签名不接受 db 参数（需 codegraph 验），fallback 在测试内联等价 DDL，SUMMARY 记录。

    **SC4 兜底（独立验证，不写测试文件，写入 plan 验收命令）：** phase gate 必跑 `git diff --exit-code electron/`（生产代码零改动）。唯一允许的例外（本 plan 不触发）：若 OQ#1 退到方案 B 加 `_setDbGetter`，则 git diff electron/ 仅含 connection.ts 1-2 行 @internal 微改 —— 本 plan 选方案 vi.mock/A 不触发此例外。
  </action>
  <verify>
    <automated>cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs run --config vitest.electron.config.ts tests/electron/db.real.test.ts（期望：3 个 it 全绿，加载真实 better-sqlite3 native .node 不 throw ABI 冲突） && git diff --exit-code electron/（期望：退出码 0，生产代码零改动 SC4 兜底） && npm test（期望：现有 mock 套件 244 用例全绿无回归）</automated>
  </verify>
  <acceptance_criteria>
    - tests/electron/db.real.test.ts 存在，`grep -c "describe.*DB 真路径回归" tests/electron/db.real.test.ts == 1`
    - db.real.test.ts 至少 3 个 it（grep -c "^  it(" >= 3）
    - db.real.test.ts 用真实 prepared statement（grep -c "db.prepare" >= 3 —— INSERT/SELECT/UPDATE/DELETE 复用 statement）
    - db.real.test.ts 验迁移幂等（grep -c "runMigrations" >= 2 或验 user_version 二次 no-op 的断言）
    - db.real.test.ts 验 WAL（grep -c "journal_mode" >= 1 且断言 toBe('wal')）
    - `npm run test:electron -- tests/electron/db.real.test.ts` 退出码 0，输出含 better-sqlite3 加载无 ABI 错（无 `NODE_MODULE_VERSION` / `was compiled against a different Node.js version` 字样）
    - `git diff --exit-code electron/` 退出码 0（生产代码零改动 SC4 红线，本 plan 选 vi.mock 不触 _setDbGetter 微改）
    - `npm test` 退出码 0，现有 mock 套件用例数不减少（grep "Test Files" 输出含 16+ 文件 / 244+ tests，无回归）
    - vitest.electron.config.ts include 限定 tests/electron/**，故 npm test（plain node）**不采集** db.real.test.ts（验：`npm test 2>&1 | grep -c "db.real"` == 0，物理隔离 Pitfall 6）
  </acceptance_criteria>
  <done>
    DB 真路径回归落地（CRUD + 迁移幂等 + WAL），证明 SC1（electron.exe 加载 electron-ABI native binding）+ SC2 之 DB 部分。SC4 git diff 兜底验证生产零改动。现有 mock 套件无回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| electron.exe (ELECTRON_RUN_AS_NODE) ↔ 测试通道 | electron.exe 当 Node 运行时跑 vitest，加载 electron-ABI native binding；测试通道是新增边界，须确保不暴露生产 IPC/DB/masterKey 给测试日志 |
| mock server (ssh2.Server / net.Server) ↔ loopback | mock 对端监听 127.0.0.1 随机端口，仅本地；禁止 0.0.0.0 监听外部 |
| 临时 DB 文件 ↔ 系统 temp 目录 | 真路径测试用 os.tmpdir() 临时 DB，含测试数据（非真实凭证），afterAll 强删 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | mockSshServer hostKey | mitigate | 用 crypto.generateKeyPairSync 随机生成测试 hostKey，不写死真实凭证；tests/ 不进 electron-builder 安装包（排除规则不变） |
| T-12-02 | Tampering / Elevation | mockSshServer / mockTelnetServer listen 地址 | mitigate | server.listen(0, '127.0.0.1') 严格 loopback，禁止 0.0.0.0；端口 0 随机分配防撞 |
| T-12-03 | Information Disclosure | 临时 DB 文件残留 | mitigate | realDb.close() 严格删 dbPath + -wal + -shm（try/catch ENOENT）；唯一文件名防并发撞；测试不用真实 masterKey（用 MK_TEST_KEY 范式） |
| T-12-04 | Tampering | wtfnode devDep 供应链 | mitigate | wtfnode slopcheck [OK] + 仅 devDependencies 不进生产打包（electron-builder.yml 排除规则不变）；装包经代理或 --registry 直连验证 |
| T-12-SC | Tampering | npm 装包（wtfnode） | mitigate | slopcheck 已 [OK]；装包前确认代理或直连 registry（RESEARCH 已验证） |
| T-12-05 | Elevation | test:electron 通道绕三红线 | accept | 测试通道是消费方不碰生产 IPC/加密/commandSafety；git diff electron/ 兜底验生产零改动（SC4）；三红线改动后须仍生效由 npm test 既有套件回归 |
</threat_model>

<verification>
- 三绿门禁之一：`npm run test:electron`（真路径套件，本 plan 落地 DB 测试）
- 三绿门禁之二：`npm test`（mock 套件无回归）
- 三绿门禁之三：`npm run build:electron-main`（main bundle 构建绿，验 package.json 改动未破坏 esbuild 外部化清单）
- SC4 红线兜底：`git diff --exit-code electron/` 退出码 0
- A1 入口验证：`cross-env ELECTRON_RUN_AS_NODE=1 node_modules/electron/dist/electron.exe node_modules/vitest/vitest.mjs --version` 输出 4.1.x
- OQ#1 注入策略：本 plan 选 vi.mock / 直持 realDb 实例（零生产改动），Plan 12-02 service 真路径测试用 vi.mock 注入 getDatabase；若 12-02 发现 vi.mock 牵连过广，退方案 B 加 _setDbGetter（届时 git diff electron/ 含 connection.ts 1-2 行 @internal 微改，需用户 review 批准）
</verification>

<success_criteria>
- SC1 达成：npm run test:electron 加载 electron-ABI better-sqlite3 不 throw ABI 冲突（A1 实跑验证通过）
- SC2 之 DB 部分达成：db.real.test.ts 真路径 CRUD + 迁移幂等 + WAL 全绿
- SC4 兜底达成：git diff electron/ 空（生产零改动）
- 现有 mock 套件无回归（npm test 全绿 244 用例）
- 四个 helper 接口契约落地（Plan 12-02/12-03 复用）
- wtfnode 装入 devDependencies（不进生产打包）
- A1 checkpoint 实跑确认 vitest 入口（或 fallback 已记 SUMMARY）
- telnet IAC checkpoint 在 mockTelnetServer 已落地（待 Plan 12-02 telnetExec.real 连真 telnet-client 实跑确认）
</success_criteria>

<output>
Create `.planning/phases/12-test-infrastructure-dep-1-abi/12-01-SUMMARY.md` when done（含 A1 入口最终路径、OQ#1 注入策略确认、telnet IAC 处理策略、git diff electron/ 验证结果、wtfnode 装包方式）
</output>
