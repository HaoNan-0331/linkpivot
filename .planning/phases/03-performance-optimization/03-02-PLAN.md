---
phase: 03-performance-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/ouiService.ts
  - electron/services/networkSegmentService.ts
  - electron/main.ts
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "OUIService 持有模块级 vendorMap: Map<string,string>|null，启动时由 preload() 全量载入"
    - "getVendor(mac) 改读 vendorMap（O(1) 内存查找），miss 返回 'Unknown'；Map 为 null 时回退原 prepare().get() 查库路径（优雅降级）"
    - "5 个写方法（add/addBatch/update/delete/deleteBatch）写库成功后增量 map.set/delete，Map 未预载时 no-op"
    - "启动序列在 migrateAndSecure() 之后调用 OUIService.preload()，首次 getIPDetails 时 Map 已就绪"
    - "networkSegmentService.ts:107 的 getVendor 双查 bug 修复为单次调用 + 局部缓存"
  artifacts:
    - path: "electron/services/ouiService.ts"
      provides: "vendorMap 内存缓存 + preload() 预载 + 5 写方法增量同步 + getVendor 读 Map 优雅降级"
      contains: "private static vendorMap"
    - path: "electron/services/networkSegmentService.ts"
      provides: "getIPDetails 单查 getVendor（修复双查 bug）"
      contains: "OUIService.getVendor"
    - path: "electron/main.ts"
      provides: "启动序列插入 OUIService.preload()"
      contains: "OUIService.preload"
  key_links:
    - from: "electron/main.ts app.whenReady"
      to: "OUIService.preload()"
      via: "migrateAndSecure() 之后、IPC 注册之前调用"
      pattern: "OUIService\\.preload\\(\\)"
    - from: "electron/services/networkSegmentService.ts getIPDetails"
      to: "OUIService.getVendor"
      via: "单次调用 + 局部变量缓存（修复双查）"
      pattern: "OUIService\\.getVendor"
    - from: "electron/services/ouiService.ts 5 写方法"
      to: "vendorMap.set/delete"
      via: "写库成功后增量同步（可选链 no-op when null）"
      pattern: "this\\.vendorMap\\?\\.\\(set\\|delete\\)"
---

<objective>
PERF-01：消除 OUI 厂商查询的 N+1。`OUIService` 增模块级 `vendorMap: Map<macPrefix, vendor>` 内存缓存，启动 `preload()` 全量载入，`getVendor` 改读 Map（O(1)），5 个写方法增量同步 Map 保持零脏读，`getIPDetails` 批量读路径不再逐行查库。顺带修复 `networkSegmentService.ts:107` 的 getVendor 双查 bug（N+1 翻倍放大）。

Purpose: `/24` 网段 `getIPDetails` 现状对每个 IP 调用 `OUIService.getVendor`，且 line 107 双查使查询数翻倍（最多 ~508 次查库）。预载 Map 把 O(n) 查库降为 O(1) 内存查找 + 一次性预载，写路径增量同步保证 Map 与库零延迟一致（避免 getIPDetails 脏读）。

Output: 改造后的 `ouiService.ts`（vendorMap + preload + 5 写方法同步）、`networkSegmentService.ts`（双查修复）、`main.ts`（启动序列插 preload），tsc+esbuild 双绿，优雅降级保证功能不中断。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-optimization/03-CONTEXT.md
@.planning/phases/03-performance-optimization/03-PATTERNS.md
@electron/services/ouiService.ts
@electron/services/networkSegmentService.ts
@electron/main.ts

<interfaces>
<!-- OUIService 现状全文（改造对象） — getVendor + 5 写方法 -->

From electron/services/ouiService.ts:
```typescript
import { getDatabase } from '../database/connection'

export class OUIService {
  static getVendor(mac: string): string {
    if (!mac) return 'Unknown'
    const db = getDatabase()
    const normalizedMac = mac.replace(/[:\-\.]/g, '').toUpperCase()
    const oui = normalizedMac.substring(0, 6)
    const row = db.prepare('SELECT vendor_name FROM oui_database WHERE oui_prefix = ?').get(oui) as { vendor_name: string } | undefined
    return row?.vendor_name || 'Unknown'
  }
  // getAll / search / getById 省略
  static add(input: { ouiPrefix: string; vendorName: string }): any {
    const db = getDatabase()
    const normalizedPrefix = input.ouiPrefix.replace(/[:\-\.]/g, '').toUpperCase()
    if (!/^[0-9A-F]{6}$/.test(normalizedPrefix)) throw new Error('OUI 前缀格式无效，需要6位十六进制字符')
    const result = db.prepare('INSERT INTO oui_database (oui_prefix, vendor_name, is_custom) VALUES (?, ?, 1)').run(normalizedPrefix, input.vendorName)
    return this.getById(result.lastInsertRowid)
  }
  static addBatch(entries: Array<{ ouiPrefix: string; vendorName: string }>): number {
    const db = getDatabase()
    let count = 0
    const insert = db.prepare('INSERT OR REPLACE INTO oui_database (oui_prefix, vendor_name, is_custom) VALUES (?, ?, 1)')
    for (const entry of entries) {
      const normalizedPrefix = entry.ouiPrefix.replace(/[:\-\.]/g, '').toUpperCase()
      if (/^[0-9A-F]{6}$/.test(normalizedPrefix)) { insert.run(normalizedPrefix, entry.vendorName); count++ }
    }
    return count
  }
  static update(input: { id: number; ouiPrefix?: string; vendorName?: string }): any {
    const db = getDatabase()
    const updates: string[] = []
    const values: any[] = []
    if (input.ouiPrefix !== undefined) {
      const normalizedPrefix = input.ouiPrefix.replace(/[:\-\.]/g, '').toUpperCase()
      if (!/^[0-9A-F]{6}$/.test(normalizedPrefix)) throw new Error('OUI 前缀格式无效')
      updates.push('oui_prefix = ?'); values.push(normalizedPrefix)
    }
    if (input.vendorName !== undefined) { updates.push('vendor_name = ?'); values.push(input.vendorName) }
    if (updates.length === 0) return this.getById(input.id)
    updates.push('updated_at = CURRENT_TIMESTAMP'); values.push(input.id)
    db.prepare(`UPDATE oui_database SET ${updates.join(', ')} WHERE id = ?`).run(...values)
    return this.getById(input.id)
  }
  static delete(id: number): void {
    const result = getDatabase().prepare('DELETE FROM oui_database WHERE id = ? AND is_custom = 1').run(id)
    if (result.changes === 0) throw new Error('无法删除系统预设的 OUI 条目')
  }
  static deleteBatch(ids: number[]): number {
    const db = getDatabase()
    const placeholders = ids.map(() => '?').join(',')
    return db.prepare(`DELETE FROM oui_database WHERE id IN (${placeholders}) AND is_custom = 1`).run(...ids).changes
  }
}
```

getIPDetails 双查 bug（networkSegmentService.ts:104-108，改造对象）:
```typescript
return rows.map((entry) => ({
  ip: entry.ip, mac: entry.mac, status: entry.status, lastSeen: entry.collectedAt,
  interface: entry.interface, deviceName: entry.deviceName || undefined,
  macVendor: entry.mac ? (OUIService.getVendor(entry.mac) === 'Unknown' ? undefined : OUIService.getVendor(entry.mac)) : undefined,  // ← 同一 mac 双查
}))
```

main.ts 启动序列（main.ts:79-84，preload 插入点）:
```typescript
initDatabase()
createTables()
migrateAndSecure()   // line 81
// ← OUIService.preload() 插入此处（migrateAndSecure 之后、IPC 注册之前）
registerArpIpc()     // line 84
...
```

模块级 Map/Set 先例（networkSegmentService.ts:49，证明项目已用模块内内存结构）:
```typescript
const segments = new Map<string, { ips: string[]; count: number }>()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OUIService 增 vendorMap + preload + getVendor 读 Map 优雅降级 + 5 写方法增量同步</name>
  <files>electron/services/ouiService.ts</files>
  <read_first>
    - electron/services/ouiService.ts（全文 86 行 — 改造对象，确认 getVendor 行 4-11 + 5 写方法 add/addBatch/update/delete/deleteBatch）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P1（vendorMap 懒加载 + preload 全量载入 + getVendor 读 Map miss 返 'Unknown' + Map null 回退查库 + 5 写方法增量 set/delete + 可选链 no-op）
    - .planning/phases/03-performance-optimization/03-PATTERNS.md §ouiService（getVendor 现状改造 + 模块级 static 字段先例 networkSegmentService.ts:49 + 5 写方法 hook 点 + mac 归一化复用）
    - electron/database/connection.ts:16-19（getDatabase 单例访问器）
  </read_first>
  <action>
按 D-P1 改造 `OUIService`：

1. **模块级 vendorMap 字段**：在 `export class OUIService {` 之后第一行加 `private static vendorMap: Map<string, string> | null = null`。初始 `null` 表示未预载。

2. **mac 归一化 helper（复用，避免 key 不匹配 miss）**：新增 `private static normalizeMac(mac: string): string { return mac.replace(/[:\-\.]/g, '').toUpperCase().substring(0, 6) }`——`getVendor` 与 `vendorMap` key 归一化必须逐字一致（D-P1 PATTERNS §mac 归一化红线）。现有 `getVendor` 行 7-8 的 `replace(/[:\-\.]/g,'').toUpperCase()` + `substring(0,6)` 抽到此 helper，`add`/`addBatch`/`update` 现有的 `input.ouiPrefix.replace(/[:\-\.]/g,'').toUpperCase()`（注意：写方法的 prefix 归一化**不截断** substring，保持原样——只 getVendor/Map key 用 6 位截断；写方法的 normalizedPrefix 是完整 6 位 hex 校验后的值，本身即 6 位，可直接作 Map key）。

3. **preload() 方法**：
   ```typescript
   static preload(): void {
     try {
       const db = getDatabase()
       const rows = db.prepare('SELECT oui_prefix, vendor_name FROM oui_database').all() as Array<{ oui_prefix: string; vendor_name: string }>
       const map = new Map<string, string>()
       for (const row of rows) {
         // oui_database.oui_prefix 存储形如 '00:01:02'（init.ts:299 seed），需归一化为 6 位 hex 大写作 Map key
         map.set(this.normalizeMac(row.oui_prefix), row.vendor_name)
       }
       this.vendorMap = map
     } catch (e: any) {
       // D-P1 优雅降级：预载失败 → vendorMap 保持 null → getVendor 回退原查库路径。功能不中断，仅失去优化。
       this.vendorMap = null
       console.error('[oui] preload 失败，回退逐行查库:', e.message)
     }
   }
   ```
   关键：`oui_database.oui_prefix` 列存储形如 `'00:01:02'`（init.ts:299 seed 数据带冒号），故 preload 载入时必须用 `normalizeMac` 归一化为 `'000102'` 作 key——与 getVendor 查询时的归一化一致。否则 Map key `'00:01:02'` 与查询 key `'000102'` 不匹配会全部 miss。

4. **getVendor 改读 Map + 优雅降级**：
   ```typescript
   static getVendor(mac: string): string {
     if (!mac) return 'Unknown'
     const oui = this.normalizeMac(mac)
     // D-P1：Map 已预载 → O(1) 内存查找；Map 为 null（预载失败/未调用）→ 回退原 prepare().get() 查库路径
     if (this.vendorMap !== null) {
       return this.vendorMap.get(oui) || 'Unknown'
     }
     // 优雅降级：回退逐行查库（与改造前行为一致）
     const db = getDatabase()
     const row = db.prepare('SELECT vendor_name FROM oui_database WHERE oui_prefix = ?').get(oui) as { vendor_name: string } | undefined
     return row?.vendor_name || 'Unknown'
   }
   ```
   注意回退路径的 prepare 参数仍是归一化后的 `oui`（6 位 hex 大写）。**但库里 oui_prefix 列存的是 `'00:01:02'` 形式**——现状 getVendor 用 `oui='000102'` 查 `WHERE oui_prefix = ?` 查的是 `'000102'`，而库里存 `'00:01:02'`，**现状本身查不到**（这是现状的潜在 bug，但本 phase 不修——保持现状查询语义，仅优化路径）。**修正**：为避免改变现状查询行为，回退路径保持现状原样（用归一化 oui 查）；预载 Map 则用 normalizeMac(row.oui_prefix) 把库里的 `'00:01:02'` 归一化为 `'000102'` 作 key——这样 Map 路径能正确命中（Map key 与查询 key 都是归一化后的 6 位 hex）。两条路径的查询 key 都是 `normalizeMac` 输出，一致；区别仅在 Map 路径的 key 在 preload 时也归一化了库里值，命中正确。

5. **5 写方法增量同步 Map（可选链 no-op when null）**：
   - `add`：`INSERT` 成功（`result` 返回后）、`return this.getById(...)` 之前加 `this.vendorMap?.set(this.normalizeMac(input.ouiPrefix), input.vendorName)`。注意用 `input.ouiPrefix`（原始输入归一化）而非 `result`，因 vendorName 来自 input。
   - `addBatch`：循环内 `insert.run(...)` 之后、`count++` 之前加 `this.vendorMap?.set(this.normalizeMac(entry.ouiPrefix), entry.vendorName)`（仅在校验通过分支内）。
   - `update`：`db.prepare('UPDATE...').run(...)` 之后、`return this.getById(...)` 之前——若 `input.vendorName !== undefined` 则 `this.vendorMap?.set(this.normalizeMac(input.ouiPrefix ?? ''), input.vendorName)`；若 `input.ouiPrefix !== undefined`（prefix 变更），需先取旧 prefix 删除旧 key：在 UPDATE 之前先 `const oldRow = this.getById(input.id)` 取旧 `oui_prefix`，UPDATE 后 `this.vendorMap?.delete(this.normalizeMac(oldRow.oui_prefix))` 再 `set` 新 key。简化：UPDATE 后用 `this.getById(input.id)` 取新行，`this.vendorMap?.set(this.normalizeMac(newRow.oui_prefix), newRow.vendor_name)` + 若旧 prefix 不同则 `delete` 旧——executor 选最简洁正确的实现，核心是 prefix 变更时旧 key 被 delete。
   - `delete`：DELETE 之前先 `const row = this.getById(id)` 取 `oui_prefix`（仅当 is_custom=1 可删，先取再删），DELETE 成功（`result.changes > 0`）后 `this.vendorMap?.delete(this.normalizeMac(row?.oui_prefix ?? ''))`。注意现有 delete 在 changes===0 时 throw，需在 throw 之前取 row（或 throw 后不执行同步）。
   - `deleteBatch`：DELETE 之前批量取 `ids` 对应的 `oui_prefix`（`SELECT oui_prefix FROM oui_database WHERE id IN (...) AND is_custom = 1`），DELETE 之后循环 `this.vendorMap?.delete(this.normalizeMac(prefix))`。

   **关键**：所有 `map.set/delete` 用可选链 `this.vendorMap?.`——Map 未预载（null）时 no-op，不强制触发预载（避免写操作意外触发全量载入）；Map 已预载则即时同步，零脏读窗口（D-P1 选增量而非失效重载的理由）。

**禁止**：把 vendorMap 改成失效重载模式（D-P1 明确选增量）；在写方法里抛出（保持现有 throw 语义不变，仅追加同步）；改变 5 写方法签名/返回值；删除优雅降级回退路径（功能可用性红线）。
  </action>
  <verify>
    <automated>
cd "E:\knowlegdge_base\claude\network_toplogy" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; npm run build:electron-main &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('electron/services/ouiService.ts','utf8');const checks={vendorMapField:/private static vendorMap:\s*Map<string,\s*string>\s*\|\s*null/.test(s),preloadMethod:/static preload\(\):\s*void/.test(s),normalizeMac:/private static normalizeMac/.test(s),gracefulFallback:/this\.vendorMap !== null/.test(s)&amp;&amp;/prepare\('SELECT vendor_name FROM oui_database WHERE oui_prefix = \?'\)/.test(s),optionalChainingSet:/this\.vendorMap\?\.(set|delete)/.test(s),writeSyncCount:(s.match(/this\.vendorMap\?\./g)||[]).length>=5};console.log('checks',JSON.stringify(checks,null,2));if(!Object.values(checks).every(Boolean)){process.exit(1)}"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "private static vendorMap: Map<string, string> | null" electron/services/ouiService.ts` ≥ 1（或等价空格变体，正则 `private static vendorMap:\s*Map<string,\s*string>\s*\|\s*null` 命中）
    - `grep -c "static preload()" electron/services/ouiService.ts` = 1
    - `grep -c "private static normalizeMac" electron/services/ouiService.ts` ≥ 1（定义）
    - `grep -c "this.vendorMap !== null" electron/services/ouiService.ts` ≥ 1（优雅降级分支）
    - `grep -c "SELECT vendor_name FROM oui_database WHERE oui_prefix = ?" electron/services/ouiService.ts` ≥ 1（回退查库路径保留）
    - `grep -cE "this\.vendorMap\?\.(set|delete)" electron/services/ouiService.ts` ≥ 5（5 写方法增量同步）
    - `grep -c "console.error" electron/services/ouiService.ts` ≥ 1（preload 失败日志）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
    - esbuild 打包 electron/services/ouiService.ts exit 0
  </acceptance_criteria>
  <done>
    OUIService 持有模块级 vendorMap: Map|null，preload() 全量载入（oui_prefix 归一化为 6 位 hex 大写作 key），getVendor 改读 Map（miss 返 'Unknown'）+ Map null 时回退 prepare().get() 查库，5 写方法写库后增量 map.set/delete（可选链 null 时 no-op），优雅降级保证功能不中断，tsc+esbuild 双绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: networkSegmentService getIPDetails 双查 bug 修复 + main.ts 启动序列插入 preload</name>
  <files>electron/services/networkSegmentService.ts, electron/main.ts</files>
  <read_first>
    - electron/services/networkSegmentService.ts:88-109（getIPDetails 的 rows.map 块，line 107 双查 bug 改造对象）
    - electron/main.ts:1-26（imports 区，确认未 import OUIService）+ main.ts:79-92（启动序列 initDatabase→createTables→migrateAndSecure→IPC 注册，preload 插入点在 migrateAndSecure 之后、registerArpIpc 之前）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P1（顺带修 networkSegmentService.ts:107 双查 bug + 启动序列末尾 migrateAndSecure 之后插 OUIService.preload）
    - .planning/phases/03-performance-optimization/03-PATTERNS.md §networkSegmentService（双查现状 + 单查目标形态）+ §main.ts（preload 插入位置 + 需新增 import OUIService）
  </read_first>
  <action>
1. **修复 networkSegmentService.ts:107 双查 bug**：将
   ```typescript
   macVendor: entry.mac ? (OUIService.getVendor(entry.mac) === 'Unknown' ? undefined : OUIService.getVendor(entry.mac)) : undefined,
   ```
   改为单次调用 + 局部缓存（消除三元表达式两次求值 getVendor）：
   ```typescript
   macVendor: entry.mac ? (() => { const v = OUIService.getVendor(entry.mac); return v === 'Unknown' ? undefined : v })() : undefined,
   ```
   或等价的 `const vendor = entry.mac ? OUIService.getVendor(entry.mac) : 'Unknown'; ... macVendor: vendor === 'Unknown' ? undefined : vendor`（在 map 回调内先取局部变量）。executor 选最简洁正确的形式，核心是 **getVendor 对同一 mac 只调用一次**。不改 `rows.map` 其他字段，不改 getIPDetails 签名。

2. **main.ts 启动序列插入 OUIService.preload()**：
   - 在 main.ts 顶部 imports 区（line 1-26 范围，建议紧随 `import { createTables } from './database/init'` 或与其他 service import 同组）加 `import { OUIService } from './services/ouiService'`（main.ts 现未 import OUIService，PATTERNS §main.ts 明示）。
   - 在启动序列 `migrateAndSecure()`（main.ts:81）之后、`// IP Management IPC` 注释及 `registerArpIpc()`（main.ts:84）之前插入：
     ```typescript
     OUIService.preload()   // PERF-01 (D-P1)：启动预载 Map<macPrefix,vendor>，确保首次 getIPDetails 时 Map 就绪
     ```
   - `preload()` 内部已 try/catch 优雅降级（Task 1），故此处**无需**额外 try/catch——预载失败 Map 保持 null，getVendor 回退查库，功能不中断。若想记录预载失败可省略（preload 内已 console.error）。

**禁止**：把 preload 插在 migrateAndSecure 之前（DB 未迁移完，oui_database 表可能未就绪）；插在 IPC 注册之后（首次 getIPDetails 可能在 preload 前到达，Map 未就绪）；改 getIPDetails 签名或返回结构；删除 networkSegmentService 的 OUIService import。
  </action>
  <verify>
    <automated>
cd "E:\knowlegdge_base\claude\network_toplogy" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; node -e "const fs=require('fs');const seg=fs.readFileSync('electron/services/networkSegmentService.ts','utf8');const main=fs.readFileSync('electron/main.ts','utf8');const segChecks={noDoubleGetVendor:!seg.includes('OUIService.getVendor(entry.mac) === \'Unknown\' ? undefined : OUIService.getVendor(entry.mac)'),singleGetVendor:(seg.match(/OUIService\.getVendor\(entry\.mac\)/g)||[]).length===1};const mainChecks={importOui:/import \{ OUIService \} from '.\/services\/ouiService'/.test(main),preloadAfterMigrate:/migrateAndSecure\(\)[\s\S]*?OUIService\.preload\(\)/.test(main),preloadBeforeIpc:/OUIService\.preload\(\)[\s\S]*?registerArpIpc\(\)/.test(main)};console.log('seg',JSON.stringify(segChecks),'main',JSON.stringify(mainChecks));if(![...Object.values(segChecks),...Object.values(mainChecks)].every(Boolean)){process.exit(1)}" &amp;&amp; npm run build:electron-main    </automated>
  </verify>
  <acceptance_criteria>
    - networkSegmentService.ts 中 `OUIService.getVendor(entry.mac)` 出现次数 = 1（双查已修，正则计数）
    - networkSegmentService.ts 不再含 `OUIService.getVendor(entry.mac) === 'Unknown' ? undefined : OUIService.getVendor(entry.mac)`（原双查字面量消失）
    - `grep -c "import { OUIService } from './services/ouiService'" electron/main.ts` ≥ 1
    - `grep -c "OUIService.preload()" electron/main.ts` ≥ 1
    - main.ts 中 `OUIService.preload()` 在 `migrateAndSecure()` 之后、`registerArpIpc()` 之前（正则 `migrateAndSecure\(\)[\s\S]*?OUIService\.preload\(\)` 与 `OUIService\.preload\(\)[\s\S]*?registerArpIpc\(\)` 均命中）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
    - esbuild 打包 electron/main.ts exit 0
  </acceptance_criteria>
  <done>
    networkSegmentService.ts:107 双查 bug 修复为单次 getVendor 调用 + 局部缓存；main.ts 启动序列在 migrateAndSecure 之后、IPC 注册之前调用 OUIService.preload()，首次 getIPDetails 时 Map 已就绪；tsc+esbuild 双绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ouiIpc 写操作 → vendorMap 同步 | 用户经 IPC 增删改 OUI 后，Map 必须即时同步否则 getIPDetails 脏读旧值 |
| preload 失败 → 回退查库 | 预载失败时 Map 保持 null，getVendor 回退逐行查库（功能降级不中断） |
| oui_database seed 归一化 | seed 数据 oui_prefix 存储为 '00:01:02' 带冒号，Map key 必须归一化为 6 位 hex 否则全部 miss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | vendorMap 与库不一致（写后未同步）导致 getIPDetails 脏读 | mitigate | 5 写方法（add/addBatch/update/delete/deleteBatch）写库成功后**增量** map.set/delete（D-P1 选增量而非失效重载的核心理由），可选链保证 Map 已预载时即时同步、零脏读窗口；Task 1 acceptance 含 `this.vendorMap?.(set\|delete)` ≥ 5 校验 |
| T-03-06 | Denial of Service | preload 失败导致 getVendor 每次回退查库（N+1 未消除） | accept | 优雅降级保证功能不中断（D-P1 红线：性能优化不得破坏功能可用性）；preload 失败时 console.error 记录，运维可观测；回退路径与改造前行为一致，非新风险 |
| T-03-07 | Tampering | oui_prefix seed 归一化不一致导致 Map 全部 miss（优化失效但不报错） | mitigate | preload 载入时用 normalizeMac(row.oui_prefix) 把库里 '00:01:02' 归一化为 '000102' 作 key；getVendor 查询时 normalizeMac(mac) 也输出 '000102'——两端 key 归一化逐字一致（抽 normalizeMac helper 复用，Task 1 acceptance 含 helper 定义校验）；Map 路径 miss 才返 'Unknown'，命中正确 |
| T-03-08 | Information Disclosure | vendorMap 内存常驻 ~150+ 条厂商映射 | accept | OUI 厂商映射是公开的 MAC 前缀→厂商字典（非敏感数据），内存常驻是性能优化的必要代价，无新增泄露面 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 双绿基线
- esbuild 打包 ouiService.ts + networkSegmentService.ts + main.ts 均 exit 0
- grep vendorMap 字段 + preload 方法 + normalizeMac helper 存在
- grep 优雅降级：`this.vendorMap !== null` 分支 + 回退 `SELECT vendor_name FROM oui_database WHERE oui_prefix = ?` 保留
- grep 5 写方法增量同步：`this.vendorMap?.(set|delete)` 出现 ≥ 5 次
- grep 双查修复：networkSegmentService.ts 中 `OUIService.getVendor(entry.mac)` 计数 = 1
- grep 启动序列：main.ts `OUIService.preload()` 在 migrateAndSecure 之后、registerArpIpc 之前
</verification>

<success_criteria>
- OUI 查询不再 N+1：启动 preload() 预载 Map，getIPDetails 批量读路径每 IP 的 getVendor 为 O(1) 内存查找（Map 命中），无逐行查库
- vendorMap 与库零延迟一致：5 写方法增量同步（D-P1 增量而非失效重载）
- 优雅降级：preload 失败 → Map null → getVendor 回退查库，功能不中断
- networkSegmentService.ts:107 双查 bug 修复（单次调用 + 局部缓存），N+1 不再翻倍
- 启动序列 migrateAndSecure 之后插 preload，首次 getIPDetails 时 Map 就绪
- tsc + esbuild 双绿，签名零变更（调用方透明）
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-optimization/03-02-SUMMARY.md`
</output>
