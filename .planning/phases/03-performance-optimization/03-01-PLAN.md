---
phase: 03-performance-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/anomalyService.ts
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "processARPEntries 整批写库包在单个 db.transaction 内，一次 COMMIT 替代逐条 autocommit"
    - "循环内 4 处 db.prepare(...) 提到循环外，循环内复用 prepared statement"
    - "isIPExcluded 不再每行查 excluded_ips 全表，改为事务前一次性预载内存结构后纯内存判定"
    - "单条 createBinding/recordChange 失败仍被条目级 try/catch 跳过继续，不触发整批 ROLLBACK（向后兼容红线）"
    - "processARPEntries 返回的 changes[] 数组结构/内容与改造前一致（事务化对调用方透明，签名不变）"
  artifacts:
    - path: "electron/services/anomalyService.ts"
      provides: "事务化 + prepared statement 复用的 processARPEntries + 预载 excluded Set 的 isIPExcluded"
      contains: "db.transaction"
  key_links:
    - from: "electron/services/anomalyService.ts processARPEntries"
      to: "db.transaction(() => { for... })"
      via: "整批单事务包裹"
      pattern: "db\\.transaction"
    - from: "electron/ipc/arpIpc.ts + schedulerService.ts processARPEntries 调用点"
      to: "AnomalyService.processARPEntries"
      via: "签名不变的批量调用"
      pattern: "processARPEntries\\("
---

<objective>
PERF-02：将 `AnomalyService.processARPEntries` 的逐条 autocommit 写库改造为整批单事务 + prepared statement 复用，并消除 `isIPExcluded` 的隐含 N+1 全表扫。

Purpose: ARP 周期采集一次批量处理数十至上百条目，当前每条目 4 处 `db.prepare()` + 1 次 `excluded_ips` 全表扫，提交开销与解析开销随条目数线性放大。事务化把 N 次提交压成 1 次，prepared 复用消除重复解析，预载 Set 把 excluded 判定从 O(n×m) 降到 O(n)。

Output: 改造后的 `electron/services/anomalyService.ts`（单文件），tsc+esbuild 双绿，向后兼容（条目级 try/catch 保留，单条失败不 abort 整批）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-optimization/03-CONTEXT.md
@.planning/phases/03-performance-optimization/03-PATTERNS.md
@electron/services/anomalyService.ts
@electron/database/init.ts

<interfaces>
<!-- 现状 processARPEntries / createBinding / recordChange / isIPExcluded 全文（改造对象，executor 直接据此重构） -->

From electron/services/anomalyService.ts（改造前）:
```typescript
static processARPEntries(entries: Array<{ ip: string; mac: string }>): IPMACChange[] {
  const db = getDatabase()
  const changes: IPMACChange[] = []
  const now = new Date().toISOString()
  for (const entry of entries) {
    const { ip, mac } = entry
    if (this.isIPExcluded(ip)) continue                                      // 隐含 N+1：每行查 excluded_ips 全表
    const currentBinding = db.prepare('SELECT id, mac FROM ip_mac_bindings WHERE ip = ? AND is_active = 1').get(ip) as { id: number; mac: string } | undefined  // 循环内 prepare #1
    if (currentBinding) {
      if (currentBinding.mac !== mac) {
        const change = this.recordChange(ip, currentBinding.mac, mac, 'mac_changed')
        if (change) changes.push(change)
        db.prepare('UPDATE ip_mac_bindings SET is_active = 0 WHERE id = ?').run(currentBinding.id)  // 循环内 prepare #2
        this.createBinding(db, ip, mac, now)
      } else {
        db.prepare('UPDATE ip_mac_bindings SET last_seen = ? WHERE id = ?').run(now, currentBinding.id)  // 循环内 prepare #3
      }
    } else {
      const oldBinding = db.prepare('SELECT mac FROM ip_mac_bindings WHERE ip = ? ORDER BY last_seen DESC LIMIT 1').get(ip) as { mac: string } | undefined  // 循环内 prepare #4
      if (oldBinding) {
        const change = this.recordChange(ip, null, mac, 'ip_reused')
        if (change) changes.push(change)
      }
      this.createBinding(db, ip, mac, now)
    }
  }
  return changes
}

private static isIPExcluded(ip: string): boolean {
  const db = getDatabase()
  const excluded = db.prepare('SELECT ip_or_cidr FROM excluded_ips').all() as Array<{ ip_or_cidr: string }>  // 每行全表扫
  for (const rule of excluded) {
    const pattern = rule.ip_or_cidr
    if (pattern.includes('/')) { if (this.ipInCIDR(ip, pattern)) return true }
    else if (pattern.includes('*')) { const regex = new RegExp('^' + pattern.replace(/\./g, '\\.').replace(/\*/g, '.*') + '$'); if (regex.test(ip)) return true }
    else if (pattern === ip) return true
  }
  return false
}

private static createBinding(db: any, ip: string, mac: string, now: string): void {
  try { db.prepare('INSERT INTO ip_mac_bindings (ip, mac, first_seen, last_seen, is_active) VALUES (?, ?, ?, ?, 1)').run(ip, mac, now, now) }
  catch { db.prepare('UPDATE ip_mac_bindings SET last_seen = ?, is_active = 1 WHERE ip = ? AND mac = ?').run(now, ip, mac) }
}

private static recordChange(ip: string, oldMac: string | null, newMac: string | null, changeType: ChangeType): IPMACChange | null {
  const db = getDatabase()
  try {
    const result = db.prepare("INSERT INTO ip_mac_changes (ip, old_mac, new_mac, change_type, detected_at) VALUES (?, ?, ?, ?, datetime('now'))").run(ip, oldMac, newMac, changeType)
    return { id: result.lastInsertRowid as number, ip, oldMac, newMac, changeType, detectedAt: new Date().toISOString(), acknowledged: false, acknowledgedAt: null, notes: null }
  } catch (e: any) { console.error('[anomaly] recordChange 插入失败:', ip, e.message); return null }
}
```

事务化批量写先例（electron/database/init.ts:346-351，PERF-02 直接镜像）:
```typescript
const stmt = db.prepare('INSERT OR IGNORE INTO oui_database (oui_prefix, vendor_name) VALUES (?, ?)')  // prepared 提到循环外
const insertMany = db.transaction(() => {
  for (const [prefix, vendor] of entries) { stmt.run(prefix, vendor) }   // 循环内复用
})
insertMany()   // 一次 COMMIT
```

调用方（事务化对调用方透明，签名不变）:
- electron/ipc/arpIpc.ts:37,59 — `AnomalyService.processARPEntries(...)`
- electron/services/schedulerService.ts:64 — `AnomalyService.processARPEntries(...)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: processARPEntries 事务化 + prepared statement 复用 + isIPExcluded 预载 Set</name>
  <files>electron/services/anomalyService.ts</files>
  <read_first>
    - electron/services/anomalyService.ts（全文 146 行 — 改造对象，确认 isIPExcluded/ipInCIDR/ipToNumber/createBinding/recordChange/processARPEntries 现状）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P2（事务化 + 条目级 try/catch 保留 + isIPExcluded 预载 + 事务边界覆盖）
    - .planning/phases/03-performance-optimization/03-PATTERNS.md §anomalyService（事务化批量写先例 init.ts:346-351 + 改造目标形态 + 条目级 try/catch 现状行 73-87）
    - electron/database/init.ts:346-351（initDefaultOUIData 事务化批量写先例，db.transaction(()=>{for...stmt.run()})）
  </read_first>
  <action>
按 D-P2 改造 `processARPEntries` 与 `isIPExcluded`，**不改变签名、不改变 changes[] 累积语义、不改变"单条失败跳过继续"的尽力而为语义**：

1. **isIPExcluded 预载（消除 N+1 全表扫）**：新增 `private static preloadExcludedSet(db: Database.Database): { ips: Set<string>; cidrs: string[]; wildcards: string[] }`，一次性 `db.prepare('SELECT ip_or_cidr FROM excluded_ips').all()`，按规则类型分桶——普通 IP（不含 `/` 也不含 `*`）入 `Set<string>`、CIDR（含 `/`）入 `cidrs: string[]`、通配（含 `*`）入 `wildcards: string[]`。新增 `private static isIPExcludedCached(ip: string, excluded: { ips: Set<string>; cidrs: string[]; wildcards: string[] }): boolean`，循环内纯内存判定：`excluded.ips.has(ip)` || `excluded.cidrs.some(c => this.ipInCIDR(ip, c))` || `excluded.wildcards.some(w => new RegExp('^' + w.replace(/\./g,'\\.').replace(/\*/g,'.*') + '$').test(ip))`。**保留原 `isIPExcluded(ip)` 公开/私有方法不动**（`checkIPExcluded` 行 143 仍调用它，避免破坏对外 API）——原 `isIPExcluded` 内部改为 `return this.isIPExcludedCached(ip, this.preloadExcludedSet(getDatabase()))` 以复用同一判定逻辑（单次调用点不再有 N+1，因只调用一次）。

2. **prepared statement 提到循环外**：在 `processARPEntries` 内、事务之前 prepare 4 个 statement 并命名（建议 `stmtCurrentBinding` / `stmtDeactivate` / `stmtUpdateLastSeen` / `stmtOldBinding`），SQL 字符串与现状逐字一致：
   - `SELECT id, mac FROM ip_mac_bindings WHERE ip = ? AND is_active = 1`
   - `UPDATE ip_mac_bindings SET is_active = 0 WHERE id = ?`
   - `UPDATE ip_mac_bindings SET last_seen = ? WHERE id = ?`
   - `SELECT mac FROM ip_mac_bindings WHERE ip = ? ORDER BY last_seen DESC LIMIT 1`

3. **整批单事务**：把整个 `for (const entry of entries)` 循环包进 `const runBatch = db.transaction(() => { ...循环... }); runBatch()`，一次 COMMIT。事务开始前先 `const excluded = this.preloadExcludedSet(db)` 预载（预载本身也在事务可见性边界内更安全——放事务外也可，但放事务内保证读到的 excluded_ips 与本次写一致；选择放事务**外**预载、事务**内**仅做写 + 读 binding，避免预载 SELECT 与写混在同一事务增加锁持有时间——按 init.ts 先例预载在 transaction 外、循环复用在 transaction 内）。

4. **循环内复用 prepared + isIPExcludedCached**：循环体改用 `stmtCurrentBinding.get(ip)` / `stmtDeactivate.run(currentBinding.id)` / `stmtUpdateLastSeen.run(now, currentBinding.id)` / `stmtOldBinding.get(ip)`；`if (this.isIPExcluded(ip)) continue` 改为 `if (this.isIPExcludedCached(ip, excluded)) continue`。`createBinding`/`recordChange` 调用保持不变（见 Task 2 处理其内部 prepare）。

5. **条目级 try/catch（强制保留，D-P2 红线）**：整个循环体包一层 `try { ...原循环体... } catch (e: any) { console.error('[anomaly] processARPEntries 条目处理失败:', ip, e.message); continue }`——**单条失败被捕获后 continue，不让 throw 冒泡到 transaction 回调触发整批 ROLLBACK**。这是 better-sqlite3 事务下"尽力而为"语义的正确实现：成功条目随最终 COMMIT 提交，失败条目被跳过（与改造前 recordChange/createBinding 吞错语义一致，PROJECT.md 向后兼容红线）。注意：createBinding/recordChange **自身**已有的内部 try/catch（行 73-87）保持不动——它们吞掉的是 INSERT 冲突等可恢复错误；外层条目级 try/catch 兜底的是其他未预期错误（如 binding 查询异常）。

6. **事务边界覆盖确认**：currentBinding 查询、UPDATE、createBinding（INSERT/UPDATE）、recordChange（INSERT ip_mac_changes）全部落在 `db.transaction` 回调内——保证绑定状态与变更记录原子一致。`changes[]` 数组照常在回调内 `.push`（闭包捕获，事务结束后返回）。

7. **类型**：`preloadExcludedSet` 与 `isIPExcludedCached` 的参数类型用 `Database.Database`（需 `import type Database from 'better-sqlite3'`，参考 migrations.ts:1 的 import 写法）。`excluded` 结构对象类型可抽 `type ExcludedRules = { ips: Set<string>; cidrs: string[]; wildcards: string[] }` 内联于文件顶部（IPMACChange 之后）。

**禁止**：改变 `processARPEntries` 签名、改变返回的 IPMACChange 结构、把 createBinding 改成 throw（必须保留吞错）、删除 recordChange 的 console.error、把条目级 try/catch 去掉（会改变失败语义 → 整批 ROLLBACK → 违背向后兼容）。
  </action>
  <verify>
    <automated>
cd "E:\knowlegdge_base\claude\network_toplogy" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; npm run build:electron-main &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('electron/services/anomalyService.ts','utf8');const checks=[s.includes('db.transaction'),s.includes('preloadExcludedSet'),s.includes('isIPExcludedCached'),(s.match(/db\.prepare\(/g)||[]).length &lt;= 4 || s.includes('stmtCurrentBinding'),!s.includes('if (this.isIPExcluded(ip)) continue')];console.log('checks',JSON.stringify(checks));if(!checks.every(Boolean)){process.exit(1)}" &amp;&amp; git diff --stat electron/services/anomalyService.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "db.transaction" electron/services/anomalyService.ts` ≥ 1（整批事务存在）
    - `grep -c "preloadExcludedSet" electron/services/anomalyService.ts` ≥ 2（定义 + 调用）
    - `grep -c "isIPExcludedCached" electron/services/anomalyService.ts` ≥ 2（定义 + 循环内调用）
    - `grep -c "continue" electron/services/anomalyService.ts` ≥ 2（excluded 跳过 + 条目级 catch 跳过）
    - `grep -c "console.error" electron/services/anomalyService.ts` ≥ 2（recordChange 原有 + 新条目级 catch 各 1）
    - `grep -c "processARPEntries" electron/services/anomalyService.ts` ≥ 1 且签名行仍为 `static processARPEntries(entries: Array<{ ip: string; mac: string }>): IPMACChange[]`
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0（类型绿）
    - esbuild 打包 electron/services/anomalyService.ts exit 0（打包绿）
    - 循环内不再出现裸 `db.prepare('...').run/get`（4 个 statement 提到循环外命名变量）
  </acceptance_criteria>
  <done>
    processARPEntries 整批写库为单事务（db.transaction 包裹 for 循环），4 处 prepared statement 提到循环外命名复用，isIPExcluded 改为事务前预载 Set/CIDR/通配分桶后循环内纯内存判定，条目级 try/catch 保留（单条失败 continue 不触发整批 ROLLBACK），返回的 changes[] 结构与签名不变，tsc+esbuild 双绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: createBinding/recordChange 纳入事务边界 + 回归验证</name>
  <files>electron/services/anomalyService.ts</files>
  <read_first>
    - electron/services/anomalyService.ts（Task 1 改造后状态 — 确认 createBinding 行 73-79 / recordChange 行 81-87 仍自行 getDatabase 或接收 db 参数）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P2 "事务边界覆盖"（createBinding/recordChange 内部 prepare 调用纳入同一事务边界，db 句柄已在循环内可用）
    - .planning/phases/03-performance-optimization/03-PATTERNS.md §anomalyService "条目级 try/catch 现状"（createBinding 接收 db 参数行 73、recordChange 自行 getDatabase 行 82 — better-sqlite3 单连接同步，事务边界覆盖正确）
  </read_first>
  <action>
确认/微调 `createBinding` 与 `recordChange` 在 Task 1 事务边界内正确执行（D-P2 "事务边界覆盖"红线）：

1. **createBinding(db, ...)**：已接收 `db` 参数（现状行 73），Task 1 事务内 `this.createBinding(db, ip, mac, now)` 传入的是同一事务 db 句柄——其内部 `db.prepare('INSERT...').run` 与 `db.prepare('UPDATE...').run` 自动落入外层事务。**无需改签名**，确认调用点传入的是 `processARPEntries` 顶部的 `db`（即 `getDatabase()` 返回的同一连接）即可。

2. **recordChange(...)**：现状自行 `const db = getDatabase()`（行 82）。better-sqlite3 是单连接同步绑定，`getDatabase()` 在事务内调用返回的是**同一个** db 实例（模块级单例 `db`，connection.ts:10），故 recordChange 的 INSERT 自动落入外层事务。**无需改签名、无需传 db**——但需确认 connection.ts 的 `db` 是模块级单例（已确认，connection.ts:10 `let db: Database.Database | null`）。在 recordChange 顶部加一行注释 `// 事务边界：getDatabase() 返回模块级单例 db，processARPEntries 事务内的调用自动落入同一事务（better-sqlite3 单连接同步）` 以固化此不变量。

3. **回归验证脚本（临时，验证后删除）**：写一个临时脚本 `temp_verify_perf02.ts`（用后即删，遵循 CLAUDE.md 临时脚本规范），用 typed db mock（参考 02-01 hasColumn 测试的 typed mock 惯例，规避 better-sqlite3 的 NODE_MODULE_VERSION 冲突）验证：
   - 传入 3 条目（1 个 mac_changed、1 个 new binding、1 个 excluded IP），断言 `changes[]` 长度与类型正确（excluded 不产生 change）
   - 断言 `db.transaction` 被调用恰好 1 次（一次 COMMIT）
   - 断言 `db.prepare` 调用次数：4 个 statement 复用（不随条目数线性增长）+ excluded 预载 1 次（而非每条目 1 次）
   - 模拟 createBinding 抛错时，外层条目级 catch 捕获、其他条目仍正常累积、changes 不含失败条目（验证"尽力而为"语义）
   脚本放在 `electron/services/` 下运行 `npx tsx electron/services/temp_verify_perf02.ts`（或等价方式），**验证通过后立即删除脚本**（`rm electron/services/temp_verify_perf02.ts`，遵循 CLAUDE.md "临时脚本用后即删"）。

**禁止**：把 recordChange 改成接收 db 参数（不必要，单连接同步保证已覆盖）；删除 createBinding/recordChange 内部 try/catch（吞错语义是 ARP 采集合理设计）；为本次事务化新增 IPC 通道或改 processARPEntries 签名（Phase 4 才动 IPC）。
  </action>
  <verify>
    <automated>
cd "E:\knowlegdge_base\claude\network_toplogy" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('electron/services/anomalyService.ts','utf8');if(!s.includes('better-sqlite3 单连接同步')){console.error('missing invariant comment');process.exit(1)};console.log('invariant comment present')" &amp;&amp; git diff --stat electron/services/anomalyService.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "better-sqlite3 单连接同步" electron/services/anomalyService.ts` ≥ 1（事务边界不变量注释已固化）
    - `grep -c "private static createBinding(db" electron/services/anomalyService.ts` = 1（签名未改）
    - `grep -c "const db = getDatabase()" electron/services/anomalyService.ts` 中 recordChange 仍自行 getDatabase（签名未改）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
    - 临时验证脚本已删除：`test ! -f electron/services/temp_verify_perf02.ts`（或 glob 不命中）
    - esbuild 打包 exit 0
  </acceptance_criteria>
  <done>
    createBinding/recordChange 在 processARPEntries 事务边界内正确执行（单连接同步保证），事务边界覆盖不变量以注释固化，回归脚本验证 changes 累积/单次事务/prepared 复用/单条失败跳过均符合预期，临时脚本已删除，tsc+esbuild 双绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedulerService/arpIpc → processARPEntries | ARP 采集条目（设备返回的 ip/mac）经此入口写库，跨进程/设备数据在此进入 DB 事务 |
| 事务边界 vs 单条失败 | 单条异常若冒泡会触发整批 ROLLBACK，改变既有"尽力而为"语义（兼容性风险） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | processARPEntries 事务 ROLLBACK 语义 | mitigate | 条目级 try/catch 强制保留（捕获后 continue，不让 throw 冒泡到 transaction 回调），保证单条失败不 abort 整批——与改造前 recordChange/createBinding 吞错语义一致（D-P2 红线）。Task 1 acceptance 含 `continue` + `console.error` 计数校验 |
| T-03-02 | Repudiation | 单条失败被静默跳过无记录 | mitigate | 条目级 catch 内 `console.error('[anomaly] processARPEntries 条目处理失败:', ip, e.message)` 记录失败 ip 与原因（与 recordChange 现有 console.error 同模式），不静默吞错 |
| T-03-03 | Information Disclosure | excluded_ips 预载到内存 | accept | excluded_ips 是用户配置的排除规则（IP/CIDR），非敏感凭证；内存常驻仅本次 processARPEntries 调用周期，调用结束可回收；无新增泄露面 |
| T-03-04 | Denial of Service | 事务持锁时间过长阻塞其他写 | mitigate | excluded 预载放事务外（Task 1 步骤 3 明示），事务内仅做 binding 查询/写/变更记录，锁持有时间最小化；better-sqlite3 同步事务 + busy_timeout=5000（connection.ts:27）已防饥饿 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 双绿基线（Phase 1 立的回归参照）
- `npx esbuild electron/main.ts --bundle ...`（或单独打包 anomalyService.ts）exit 0
- grep 事务化痕迹：`grep "db.transaction" electron/services/anomalyService.ts` ≥ 1
- grep prepared 复用：循环内无裸 `db.prepare('...').run/get`，4 statement 提到循环外命名变量
- grep N+1 消除：`isIPExcludedCached` + `preloadExcludedSet` 存在；原 `isIPExcluded` 不在循环内逐行调用
- 回归：processARPEntries 签名 `static processARPEntries(entries: Array<{ ip: string; mac: string }>): IPMACChange[]` 未变（调用方 arpIpc/schedulerService 透明）
</verification>

<success_criteria>
- processARPEntries 整批写库为单 db.transaction（一次 COMMIT），4 处 prepared statement 提到循环外复用，isIPExcluded 改为预载 Set 后纯内存判定
- 条目级 try/catch 保留：单条 createBinding/recordChange 失败被捕获 continue，不触发整批 ROLLBACK（向后兼容红线）
- changes[] 累积语义与签名不变，调用方零改动
- tsc + esbuild 双绿，无新增 type error
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-optimization/03-01-SUMMARY.md`
</output>
