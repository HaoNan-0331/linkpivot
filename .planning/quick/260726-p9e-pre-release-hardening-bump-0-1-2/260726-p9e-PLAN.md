---
phase: quick-260726-p9e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/utils/webSecurity.ts
  - electron/main.ts
  - electron/services/connection.ts
  - electron-builder.yml
  - package.json
  - package-lock.json
  - .gitignore
  - CHANGELOG.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "外部协议（非 http/https）的 URL 三处入口（hardenWindow / 全局 web-contents-created / openWebSafe）一律 deny，行为一致无漂移"
    - "生产 CSP 收紧到 connect-src 'self'（去 https: 通配）+ object-src 'none' + base-uri 'self' + frame-ancestors 'none'"
    - "app.whenReady 启动期抛错走 .catch → dialog.showErrorBox + app.quit，不卡渲染层 loading"
    - "openRDP 改 execFile + shell:false，杜绝 shell 拼接注入面"
    - "before-quit 的 closeDatabase 包 try/catch 不再 throw"
    - "telnet-client 与 better-sqlite3/ssh2 同标准 unpack"
    - "package.json version 0.1.1 → 0.1.2，author wanghaonan，lockfile 同步"
    - "CHANGELOG 顶部新增 [0.1.2] - 2026-07-26 版本头"
    - ".gitignore 追加 master.key / *.pem / *.pfx / *.p12 / *.key 兜底"
  artifacts:
    - path: "electron/utils/webSecurity.ts"
      provides: "openExternalSafe 共享函数 + hardenWindow 复用"
      contains: "openExternalSafe"
    - path: "electron/main.ts"
      provides: "CSP 收紧 + 全局 handler 用 openExternalSafe + whenReady .catch + before-quit try/catch"
      contains: "dialog"
    - path: "electron/services/connection.ts"
      provides: "openWebSafe 转用 openExternalSafe + openRDP 改 execFile"
      contains: "execFile"
    - path: "electron-builder.yml"
      provides: "telnet-client asarUnpack"
      contains: "telnet-client"
    - path: "package.json"
      provides: "version 0.1.2 + author"
      contains: "0.1.2"
    - path: "CHANGELOG.md"
      provides: "[0.1.2] 版本头"
      contains: "[0.1.2]"
  key_links:
    - from: "electron/utils/webSecurity.ts:openExternalSafe"
      to: "electron/main.ts 全局 web-contents-created handler"
      via: "import { openExternalSafe }"
      pattern: "openExternalSafe"
    - from: "electron/utils/webSecurity.ts:openExternalSafe"
      to: "electron/services/connection.ts:openWebSafe"
      via: "import { openExternalSafe }"
      pattern: "openExternalSafe"
    - from: "electron/services/connection.ts:openRDP"
      to: "child_process.execFile"
      via: "execFile('mstsc', [tmpPath], { shell: false })"
      pattern: "execFile\\('mstsc'"
---

<objective>
pre-release hardening：落地 pre-release-audit 的 GO-WITH-FIXES 发版前必修 10 项（H1 + M1~M5 + L5 + L11 + L13 + L14）并将 version 0.1.1 → 0.1.2，发版可上。

Purpose: 28 findings 审计中仅剩这 10 项属"发版前必修"，余者（M6/M7/L1~L16 子集）按既定 defer 归发版后迭代。本 plan 闭环硬编码安全/健壮/打包正确性/发版元数据四类，发版基线干净。
Output: 7 个源文件改动 + 1 个 CHANGELOG 版本头，三绿门禁（tsc web strict + esbuild 主进程 + vitest）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@electron/main.ts
@electron/utils/webSecurity.ts
@electron/services/connection.ts
@electron-builder.yml
@package.json
@.gitignore
@CHANGELOG.md

<interfaces>
<!-- 三处 openExternal 调用点的当前实现，executor 直接对照改 -->

electron/utils/webSecurity.ts（hardenWindow setWindowOpenHandler，line 13-16）：
```typescript
win.webContents.setWindowOpenHandler(({ url }) => {
  shell.openExternal(url)   // ← 任意协议直接放行（H1 漏洞点）
  return { action: 'deny' }
})
```

electron/main.ts:76（全局 web-contents-created handler）：
```typescript
contents.setWindowOpenHandler(({ url }) => { shell.openExternal(url); return { action: 'deny' } })
// ← 同 H1 漏洞点
```

electron/services/connection.ts:96-106（已有 http/https 白名单的 openWebSafe，H1 抽共享源）：
```typescript
export function openWebSafe(url: string) {
  try {
    const parsed = new URL(url)
    if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {
      throw new Error(`不支持的协议: ${parsed.protocol}`)
    }
    shell.openExternal(url)
  } catch {
    throw new Error('无效的 URL')
  }
}
```

electron/main.ts:1 import（无 dialog，M2 必须补）：
```typescript
import { app, BrowserWindow, ipcMain, session, shell } from 'electron'
```

electron/main.ts:69 生产 CSP（M1 收紧）：
```typescript
"default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' https:",
```

electron/main.ts:56-185 whenReady（M2 末尾加 .catch）：
```typescript
app.whenReady().then(() => {
  // ... 全部启动逻辑 ...
  app.on('activate', () => { ... })
})   // ← 末尾无 .catch
```

electron/main.ts:189 before-quit（L5 包 try/catch）：
```typescript
app.on('before-quit', () => { BackupScheduler.stop(); closeDatabase() })
```

electron/services/connection.ts:390-404 openRDP（M5 exec → execFile，line 2 import 现为 `import { exec } from 'child_process'`，executor 须确认全文件 exec 仅此一处用法后改 import）：
```typescript
const cmd = `mstsc "${tmpPath}"`
exec(cmd, (err) => {
  if (err) throw new Error(`启动 RDP 失败: ${err.message}`)
})
```

electron/services/acl.ts:30 风格参照（execFileSync shell:false）：
```typescript
execFileSync(<bin>, [<args>], { shell: false })
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: H1+M1+M2+M5+L5 — 安全/健壮修复（main.ts / webSecurity.ts / connection.ts）</name>
  <files>electron/utils/webSecurity.ts, electron/main.ts, electron/services/connection.ts</files>
  <behavior>
  - H1: shell.openExternal 三处入口（hardenWindow / 全局 web-contents-created / openWebSafe）经 openExternalSafe 统一白名单，非 http/https 一律 deny
  - M1: 生产 CSP connect-src 仅 'self'（去 https:）；新增 object-src 'none'; base-uri 'self'; frame-ancestors 'none'
  - M2: whenReady 末尾 .catch → dialog.showErrorBox('启动失败', err.message) + app.quit
  - M5: openRDP 改 execFile('mstsc', [tmpPath], { shell: false })，device.id 为 UUID 零行为变化
  - L5: before-quit 的 closeDatabase() 包 try/catch + console.error('[before-quit] closeDatabase failed')
  </behavior>
  <action>
  **H1 抽 openExternalSafe 共享函数（DRY，防三处漂移）：**

  在 `electron/utils/webSecurity.ts` 新增导出函数：
  ```typescript
  /**
   * 统一的外链打开安全门：仅放行 http/https，其余协议 deny。
   * 三处入口（hardenWindow setWindowOpenHandler / 全局 web-contents-created handler / openWebSafe）共用，
   * 杜绝 shell.openExternal 直接吞 file:/javascript:/data:/custom-protocol 等危险协议。
   * throws：协议非法（URL 解析失败或非 http/https）抛 Error，由调用方决定如何处理（deny / IPC 报错）。
   */
  export function openExternalSafe(url: string): void {
    let parsed: URL
    try {
      parsed = new URL(url)
    } catch {
      throw new Error('无效的 URL')
    }
    if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {
      throw new Error(`不支持的协议: ${parsed.protocol}`)
    }
    shell.openExternal(url)
  }
  ```

  然后将同文件 `hardenWindow` 内 setWindowOpenHandler 改为：
  ```typescript
  win.webContents.setWindowOpenHandler(({ url }) => {
    try { openExternalSafe(url) } catch (e) {
      console.warn('[hardenWindow] blocked openExternal:', (e as Error).message)
    }
    return { action: 'deny' }
  })
  ```
  （deny 是兜底，单条 url 协议非法不阻断调用流程，仅 console.warn 留痕。）

  **main.ts 全局 web-contents-created handler（line 75-77）改用 openExternalSafe：**
  - line 1 import 行：`import { openExternalSafe } from './utils/webSecurity'`（webSecurity 已 import，追加入参；若已是具名 import hardenWindow 则扩展为 `import { hardenWindow, openExternalSafe } from './utils/webSecurity'`）。
  - line 76 handler：
    ```typescript
    contents.setWindowOpenHandler(({ url }) => {
      try { openExternalSafe(url) } catch (e) {
        console.warn('[global] blocked openExternal:', (e as Error).message)
      }
      return { action: 'deny' }
    })
    ```

  **connection.ts openWebSafe（line 96-106）改为复用 openExternalSafe：**
  - import 行：`import { hardenWindow, openExternalSafe } from '../utils/webSecurity'`（line 9 已 import hardenWindow，追加 openExternalSafe）。
  - 函数体收敛为转发（保留对外 Error 语义不变，调用方 main.ts:153 IPC handler / connection.ts:44 openTerminal 不受影响）：
    ```typescript
    export function openWebSafe(url: string) {
      openExternalSafe(url)   // 抛 Error（'无效的 URL' 或 '不支持的协议: ...'），与旧语义一致
    }
    ```
  注意：旧实现 catch 后统一抛 '无效的 URL'，新实现按 openExternalSafe 抛原协议错误（'无效的 URL' / '不支持的协议: ...'），IPC handler 经 authGuard secure 异常脱敏后向 renderer 仅透出通用消息，对调用方零行为回归（renderer 不依赖该错误文本分支）。

  **M1 生产 CSP 收紧（main.ts line 69）：**
  将 CSP 字符串替换为：
  ```typescript
  "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'",
  ```
  依据：渲染层无合法外联（AI/Vision 调用全在主进程 fetch，不经过渲染层 CSP），去 https: 通配收窄 connect-src；追加 object-src 'none'（禁插件/Flash/PDF embed）、base-uri 'self'（防 base 注入）、frame-ancestors 'none'（防 clickjacking 嵌入）。dev 模式 line 61-64 已跳过注入，不影响 vite HMR。

  **M2 whenReady .catch（main.ts line 185）：**
  - line 1 import 行补 `dialog`：`import { app, BrowserWindow, dialog, ipcMain, session, shell } from 'electron'`。
  - line 185 闭合 `})` 改为：
    ```typescript
    }).catch((err: unknown) => {
      const msg = (err && (err as Error).message) ? (err as Error).message : String(err)
      console.error('[startup] fatal:', err)
      try { dialog.showErrorBox('启动失败', msg) } catch (_e) { /* dialog 不可用时降级 */ }
      app.quit()
    })
    ```
  依据：启动期任意 throw（initDatabase / migrateAndSecure / createTables / OUIService.preload / IPC register 抛错）当前会让 promise 链 unhandled，渲染层卡 loading；加 .catch 后立即弹框 + 退出，用户可见失败原因。

  **M5 openRDP 改 execFile（connection.ts line 390-404）：**
  - line 2 import：先 grep 确认 `exec` 在本文件**仅 openRDP 一处**使用（用 codegraph_callers 或 grep `exec(` in connection.ts）。确认后改为 `import { execFile } from 'child_process'`（删 exec 加 execFile）。若存在其他 exec 调用，则改为 `import { exec, execFile } from 'child_process'`（双 import 保留），不得删未停用的符号。
  - line 400-403 替换：
    ```typescript
    execFile('mstsc', [tmpPath], { shell: false }, (err) => {
      if (err) throw new Error(`启动 RDP 失败: ${err.message}`)
    })
    ```
  依据：`device.id` 为 UUID（crypto.randomUUID），无 shell 元字符；execFile + shell:false 与同文件 acl.ts:30 execFileSync(shell:false) 风格统一，消除 shell 拼接注入面（即使 tmpPath 是受控路径，零行为变化）。

  **L5 before-quit closeDatabase 包 try/catch（main.ts line 189）：**
  ```typescript
  app.on('before-quit', () => {
    BackupScheduler.stop()
    try { closeDatabase() }
    catch (e) { console.error('[before-quit] closeDatabase failed:', (e && (e as Error).message) || e) }
  })
  ```
  依据：before-quit 抛错会中断退出流程或吞 WAL checkpoint，try/catch 保证退出路径平稳。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npx esbuild electron/main.ts electron/preload.ts electron/terminal-preload.ts electron/services/connection.ts electron/utils/webSecurity.ts --platform=node --format=cjs --bundle --external:better-sqlite3 --external:ssh2 --external:telnet-client --external:electron --external:pdfjs-dist --external:iconv-lite --external:fs --external:path --external:net --external:os --external:child_process --outdir=/tmp/p9e-esbuild-check --log-level=error && npx vitest run</automated>
  </verify>
  <done>
  - tsc web strict + noUnusedLocals 全绿
  - esbuild 主进程打包全绿（main/preload/terminal-preload/connection/webSecurity）
  - vitest 25/25 不回归
  - 三处 shell.openExternal 调用全部经 openExternalSafe（grep `shell.openExternal` 在 main.ts/webSecurity.ts 应仅出现在 openExternalSafe 实现内一处；connection.ts 内 0 处）
  - openRDP 用 execFile + shell:false（grep `execFile('mstsc'` 命中 1）
  - CSP 含 `connect-src 'self'`（不含 `https:`）+ `object-src 'none'` + `base-uri 'self'` + `frame-ancestors 'none'`
  - whenReady 链尾有 `.catch` 含 `dialog.showErrorBox` + `app.quit`
  - before-quit 的 closeDatabase 包 try/catch
  </done>
</task>

<task type="auto">
  <name>Task 2: M3+M4+L11+L13+L14 + version bump — 打包/发版配置与元数据</name>
  <files>electron-builder.yml, package.json, package-lock.json, .gitignore, CHANGELOG.md</files>
  <action>
  **version bump（package.json line 3）：** `"version": "0.1.1"` → `"version": "0.1.2"`。

  **L13 author（package.json line 54）：** `"author": ""` → `"author": "wanghaonan"`。

  **L11 lockfile 同步：** 执行 `npm install --package-lock-only`（不重装依赖，仅把 package-lock.json 根 `version` 刷为 0.1.2）。完成后 grep 确认 package-lock.json 顶部 `"version": "0.1.2"`。

  **M4 telnet-client asarUnpack（electron-builder.yml line 6-9）：** 在 asarUnpack 列表追加一行（与 better-sqlite3/ssh2 同标准，原生模块必须 unpack 否则运行时找不到 .node）：
  ```yaml
  asarUnpack:
    - "**/*.node"
    - "node_modules/better-sqlite3/**/*"
    - "node_modules/ssh2/**/*"
    - "node_modules/telnet-client/**/*"
  ```
  依据：BUILD-01 已锁 telnet-client exact 2.2.13 为原生编译依赖（与 better-sqlite3 同类漂移面），但 asarUnpack 漏配（STATE.md line 66 决策与 electron-builder.yml 不一致）。补齐后三类原生依赖 unpack 标准统一。

  **L14 .gitignore 追加兜底（防误提交密钥/证书）：** 在文件末尾追加：
  ```
  # 安全兜底：禁止误提交密钥/证书
  master.key
  *.pem
  *.pfx
  *.p12
  *.key
  ```
  依据：项目 CLAUDE.md GitHub 上传规范要求敏感信息检查（私有仓库列出提醒），.gitignore 兜底是第一道防线（.codegraph/ 已有先例）。

  **M3 CHANGELOG 顶部新增版本头：** 在 `# CHANGELOG` 之后、`## 2026-07-25` 之前插入新版本头（按现有日期倒序惯例，2026-07-26 在 2026-07-25 之前）：
  ```markdown
  ## [0.1.2] - 2026-07-26

  ### pre-release hardening（GO-WITH-FIXES 发版前必修 10 项）

  本次发版纳入 pre-release-audit（5 维度 28 findings：0 critical / 1 high / 7 medium / 16 low，verdict GO-WITH-FIXES）发版前必修项 + 历史已落地修复归并：

  - **[H1 high·安全] shell.openExternal 协议白名单**：抽 `electron/utils/webSecurity.ts:openExternalSafe` 共享函数（http/https 白名单，其余 deny），`hardenWindow` setWindowOpenHandler / `main.ts` 全局 web-contents-created handler / `connection.ts:openWebSafe` 三处复用，杜绝 file:/javascript:/custom-protocol 等危险协议经外链入口逃逸。
  - **[M1 medium·安全] 生产 CSP 收紧**：connect-src `'self'`（去 https: 通配，渲染层无合法外联，AI/Vision 调用全在主进程 fetch）；追加 `object-src 'none'; base-uri 'self'; frame-ancestors 'none'`（禁插件 embed / 防 base 注入 / 防 clickjacking）。dev 模式跳过 CSP 注入不影响 vite HMR。
  - **[M2 medium·健壮] 启动期异常不卡渲染层**：`app.whenReady().then(...)` 末尾加 `.catch` → `dialog.showErrorBox('启动失败', err.message)` + `app.quit()`，启动期 DB/migrate/IPC register 任意 throw 不再吞错卡 loading。
  - **[M4 medium·打包正确性] telnet-client asarUnpack**：`electron-builder.yml` asarUnpack 追加 `node_modules/telnet-client/**/*`，与 better-sqlite3/ssh2 同标准（原生编译依赖必须 unpack），修正 BUILD-01 决策与打包配置不一致。
  - **[M5 medium·安全] openRDP 去 shell 拼接**：`connection.ts:openRDP` 由 `exec(\`mstsc "${tmpPath}"\`)`（exec+shell 拼接）改 `execFile('mstsc', [tmpPath], { shell: false })`，与同文件 acl.ts:30 execFileSync(shell:false) 风格统一，消除 shell 注入面（device.id 为 UUID 零行为变化）。
  - **[L5 low·健壮] before-quit closeDatabase try/catch**：`main.ts` before-quit 的 `closeDatabase()` 包 try/catch + `console.error('[before-quit] closeDatabase failed')`，保证退出路径平稳不中断 WAL checkpoint。
  - **[L11 low·一致性] package-lock.json 根版本刷 0.1.2**：`npm install --package-lock-only` 同步 lockfile version，与 package.json 对齐。
  - **[L13 low·元数据] package.json author**：`""` → `"wanghaonan"`。
  - **[L14 low·安全防御] .gitignore 兜底**：追加 `master.key / *.pem / *.pfx / *.p12 / *.key`，防误提交密钥/证书。

  历史已落地修复归并本次发版（详见对应日期条目）：
  - **[kb-db-malformed] 资料库 FTS5 shadow 损坏**：事务化（updateChunk/deleteChunk/mergeChunks/splitChunk/deleteDocument 显式 db.transaction）+ 启动自愈（main.ts integrity-check → rebuild → 不阻塞 init）。
  - **[exec-cmd-concat] H3C 多命令 exec 字符串粘连**：`ai.ts:executeCommandsOnDevice` 重构为每命令独立 SSH 连接，物理隔离杜绝 vty 命令粘连，discovery 素材完整。
  - **[Phase 5 FE-01~04] KB 类型化与 ChunkContent 图片渲染**：src/types/kb.ts 强类型 + imageCache LRU + ChunkContent [图片N] → 图片渲染（260705-sj1 quick 闭环）。
  - **[v1.0 milestone] 6 phase / 16 plan / 14 REQ 全交付**：BUILD-01 / ARCH-01/02 / PERF-01~04 / DATA-01 / FE-01~04 / ROBUST-01/02。

  发版后迭代（本 quick 显式排除，归下一 milestone / 后续 quick）：
  M6/M7 渲染层 any（electron 服务层）、L7 db any、L10 复杂度、L1 弱 SSH 算法、L2 ai limit、L3 captcha、L4 Login、L6 authGuard、L8/L9 渲染层、L12 rebuild 锁定、L15 xterm、L16 ssh2 license。

  验证：tsc web strict + noUnusedLocals 全绿；esbuild 主进程打包全绿；vitest 25/25 全绿。
  ```
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.version!=='0.1.2'){process.exit(1)} if(p.author!=='wanghaonan'){process.exit(2)}" && node -e "const y=require('js-yaml').load(require('fs').readFileSync('electron-builder.yml','utf8')); if(!y.asarUnpack.includes('node_modules/telnet-client/**/*')){process.exit(3)}" 2>/dev/null || grep -q "telnet-client" electron-builder.yml && grep -q "## \[0.1.2\] - 2026-07-26" CHANGELOG.md && grep -q "master.key" .gitignore && node -e "const l=require('./package-lock.json'); if(l.version!=='0.1.2'){console.error('lockfile version', l.version); process.exit(4)}"</automated>
  </verify>
  <done>
  - package.json version === '0.1.2'，author === 'wanghaonan'
  - package-lock.json 根 version === '0.1.2'（npm install --package-lock-only 已执行）
  - electron-builder.yml asarUnpack 含 'node_modules/telnet-client/**/*'
  - .gitignore 含 master.key / *.pem / *.pfx / *.p12 / *.key
  - CHANGELOG.md 顶部新增 '## [0.1.2] - 2026-07-26' 版本头
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → external (openExternal) | 渲染层/外链点击经 shell.openExternal 打开系统浏览器，越界点 |
| file:// / custom-protocol URL → shell.openExternal | 危险协议（file/javascript/data/custom）试图经外链入口逃逸（H1 漏洞） |
| 启动期 → 渲染层 | 启动期 throw 未捕获导致渲染层卡 loading（M2） |
| before-quit → DB close | closeDatabase 抛错中断退出（L5） |
| openRDP → child_process | shell 拼接 tmpPath（受控但理论注入面，M5） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p9e-01 | E (EoP) / I (Injection) | shell.openExternal 三处入口（H1） | mitigate | 抽 openExternalSafe 共享函数，URL 解析后仅放行 http/https，其余 deny；三处复用防漂移 |
| T-p9e-02 | I (Injection) | openRDP shell 拼接（M5） | mitigate | exec → execFile + shell:false，零行为变化（device.id UUID） |
| T-p9e-03 | T (Tampering) | 生产 CSP（M1） | mitigate | connect-src 'self' + object-src/base-uri/frame-ancestors 收紧，限缩渲染层 XSS 爆炸半径 |
| T-p9e-04 | D (DoS) | 启动期未捕获 throw（M2） | mitigate | whenReady .catch → dialog.showErrorBox + app.quit |
| T-p9e-05 | D (DoS) | before-quit closeDatabase throw（L5） | mitigate | try/catch + console.error 不中断退出 |
| T-p9e-06 | I (Info Disclosure) | 密钥/证书误提交（L14） | mitigate | .gitignore 兜底 master.key / *.pem / *.pfx / *.p12 / *.key |
| T-p9e-07 | T (Tampering) | telnet-client 在 asar 内找不到 .node（M4） | mitigate | asarUnpack 追加 telnet-client，与 better-sqlite3/ssh2 同标准 |
</threat_model>

<verification>
三绿门禁全过：
1. `npx tsc -p tsconfig.web.json --noEmit` — web strict + noUnusedLocals 全绿（Task 1 代码改动）
2. esbuild 主进程打包（main / preload / terminal-preload / connection / webSecurity）全绿 — 验证 import dialog / execFile / openExternalSafe 改动无语法/解析错误
3. `npx vitest run` — 25/25 不回归（crypto/auth/pagination 等单测）
4. Task 1 done criteria 的 grep 断言逐条核验（三处 openExternalSafe / execFile('mstsc' / CSP 字面量 / .catch dialog / before-quit try/catch）
5. Task 2 done criteria 的 node/grep 断言逐条核验（version 0.1.2 / author / telnet-client unpack / [0.1.2] CHANGELOG / master.key gitignore / lockfile version）

不打包安装包（electron:build 耗时长且非本 quick 验证目标）；asarUnpack 改动由 grep 验证配置正确性，打包验证归发版 HV。
</verification>

<success_criteria>
- 10 项必修全部落地（H1 / M1 / M2 / M4 / M5 / L5 / L11 / L13 / L14 + version bump）
- 三绿门禁（tsc web strict / esbuild 主进程 / vitest 25/25）
- shell.openExternal 三处统一走 openExternalSafe，无裸调
- CSP 收紧到 connect-src 'self'（无 https: 通配）+ object-src/base-uri/frame-ancestors 三件套
- openRDP 用 execFile + shell:false
- package.json + package-lock.json version 一致为 0.1.2
- telnet-client 与 better-sqlite3/ssh2 同标准 asarUnpack
- CHANGELOG 顶部新增 [0.1.2] 版本头列出本次纳入项 + 历史 + 发版后迭代
- 临时脚本/调试文件用完即删（项目规范）
</success_criteria>

<output>
完成后创建 `.planning/quick/260726-p9e-pre-release-hardening-bump-0-1-2/260726-p9e-SUMMARY.md`。
</output>
