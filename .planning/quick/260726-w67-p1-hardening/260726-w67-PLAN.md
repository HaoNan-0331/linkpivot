---
quick_id: 260726-w67
slug: p1-hardening
date: 2026-07-26
status: complete
mode: quick
---

# Quick Task 260726-w67: P1 medium 风险加固

## Task

审计 P1 推荐行动的 3 项 medium 修复（见 `.planning/audits/2026-07-26-doc-code-audit.md` §7）：

- **P1-a** auth:* 4 IPC handler 改 `safe()` 包装（异常脱敏，登录前 handler 不鉴权）
- **P1-b** dev/prod 判定锚定 `app.isPackaged`（消除 NODE_ENV 隐式依赖，CSP/loadURL 更可靠）
- **P1-c** package.json 加 `rebuild:native` 显式步骤接入 electron:build + GitHub Actions 冒烟 job

## Tasks

### Task 1: main.ts
- import 加 `safe`
- line 46/62 `process.env.NODE_ENV === 'development'` → `!app.isPackaged`
- auth:getCaptcha / auth:login / auth:isFirstRun / auth:initAdmin 4 handler 改 `safe(...)`

### Task 2: package.json
- `rebuild:native` = `electron-rebuild -f -w better-sqlite3 -w ssh2`
- `electron:build` 前置接入 `rebuild:native`

### Task 3: .github/workflows/build-smoke.yml（新）
- windows 冒烟：npm ci + rebuild:native + build + test + 验证 native .node 存在

## Verify

- esbuild build:electron-main 1.8mb ✓
- vitest 55/55 ✓（safe 包装器已有 authGuard.test.ts 覆盖）
- tsc -p tsconfig.web.json exit 0 ✓
