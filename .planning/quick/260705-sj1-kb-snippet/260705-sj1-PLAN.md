---
phase: quick-260705-sj1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/knowledgeBaseService.ts
  - src/types/kb.ts
  - src/components/pages/KnowledgeBasePage.tsx
autonomous: true
requirements:
  - KB-SEARCH-IMG-SNIPPET
---

<objective>
让 KB 检索结果中的 `[图片N]` 标记渲染为真实图片，而非纯文本（Phase 5 FE-04 defer 项，PROJECT.md:56）。

Purpose: 当前检索结果仅 `r.content.slice(0,300)` 纯文本，`[图片N]` 标记原样泄漏；复用既有 `getDocument` 的 image attach 模式与 `ChunkContent` 渲染组件，使检索结果与文档详情一致的图片渲染体验。

Output:
- 后端 `search` 4 条返回路径统一 attach `images: {id, file_path, description}[]`
- 前端 `KbSearchResult.images` 类型字段
- 前端检索卡 body 用 `ChunkContent` 渲染（替换纯文本 div）
</objective>

<execution_context>
@.planning/STATE.md
@.planning/PROJECT.md
</execution_context>

<context>
@electron/services/knowledgeBaseService.ts
@src/components/pages/KnowledgeBasePage.tsx
@src/types/kb.ts
@src/components/pages/kb/imageCache.ts

<interfaces>
<!-- 复用资产 1：getDocument 的 image attach 模式（line 86-94），search 直接套用 -->
knowledgeBaseService.ts:86-94
```typescript
const chunks = db.prepare(
  'SELECT id, chunk_index, title, content, char_count, level, image_ids FROM kb_chunks WHERE document_id = ? ORDER BY chunk_index'
).all(docId) as any[]
for (const chunk of chunks) {
  if (chunk.image_ids) {
    try {
      const ids = JSON.parse(chunk.image_ids) as string[]
      chunk.images = db.prepare('SELECT id, file_path, description FROM kb_images WHERE id IN (' + ids.map(() => '?').join(',') + ')').all(...ids)
    } catch { chunk.images = [] }
  } else {
    chunk.images = []
  }
}
```

<!-- 复用资产 2：ChunkContent 组件（line 34-97），已支持 [图片N] → imageCache.getImage → 缩略图 + 预览 -->
KnowledgeBasePage.tsx:34
```typescript
function ChunkContent({ content, images }: { content: string; images: KbImage[] })
```
- 自带 `maxHeight:300, overflow:auto` 容器
- `content.matchAll(/\[图片(\d+)\]/g)` → 按 `[图片N]` 顺序映射 `images[N-1]`
- `getImage(img.file_path, signal)` 经 LRU 缓存（imageCache.ts:20）

<!-- search 当前 SQL（line 663-666）：已 SELECT c.image_ids，但 4 条返回路径中仅 line 720 attach images，且只 SELECT id,description 缺 file_path -->
search 返回路径清单（line 687 / 713 / 720 / 733）：
- line 687 fallback（无 AI config）：未 attach images
- line 713 fallback（AI 返回空 indices）：未 attach images
- line 720 AI pick：attach images 但 `SELECT id, description`（缺 file_path，ChunkContent 无法渲染）
- line 733 catch：`c.images = []`（空数组）

<!-- KbSearchResult（src/types/kb.ts:42-48）：无 images 字段，需补 -->
<!-- KbImage（src/types/kb.ts:5-10）：已含 file_path，直接复用 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 后端 search 4 条返回路径统一 attach images（含 file_path）</name>
  <read_first>
    - electron/services/knowledgeBaseService.ts:86-94（getDocument image attach 模式）
    - electron/services/knowledgeBaseService.ts:659-739（search 全函数，4 条返回路径）
  </read_first>
  <action>
在 `electron/services/knowledgeBaseService.ts` 的 `search` 函数（line 659）内做两处改造：

1. **抽取本地 helper（紧贴 search 上方，line ~658）**：复刻 getDocument line 91-94 的 attach 模式，但 SELECT 必须含 `file_path`（ChunkContent 渲染必需）：

```typescript
function attachImages(db: ReturnType<typeof getDatabase>, chunk: any) {
  if (chunk.image_ids) {
    try {
      const ids = JSON.parse(chunk.image_ids) as string[]
      chunk.images = ids.length
        ? db.prepare('SELECT id, file_path, description FROM kb_images WHERE id IN (' + ids.map(() => '?').join(',') + ')').all(...ids)
        : []
    } catch { chunk.images = [] }
  } else {
    chunk.images = []
  }
  return chunk
}
```

注意：`ids.length === 0` 时跳过 SQL（避免 `IN ()` 语法错），返回空数组。getDocument line 91-94 未做此守卫属历史隐患，本次不回改（scope 收紧）。

2. **统一 4 条返回路径调用 attachImages**：
   - line 687 fallback（无 AI config）：`return allChunks.slice(0, topK).map(c => { c.document = {...}; return attachImages(db, c) })`
   - line 713 fallback（AI 返回空 indices）：同上
   - line 720 AI pick 路径：删除现有 line 724-729 的内联 attach 块，改 `chunk.document = {...}; return attachImages(db2, chunk)`（注意此处用 db2，保留不动）
   - line 733 catch：`c.document = {...}; return attachImages(db, c)`（替代硬编码 `c.images = []`，使 catch 路径也能渲染已知图片）

禁止改动：search 函数签名、SQL 主查询（line 663-666 已含 image_ids，不动）、AI pick prompt、ragQuery（line 741+，下游自行处理 images）。
  </action>
  <verify>
    <automated>cd "E:/knowlegdge_base/claude/network_toplogy" && node -e "require('esbuild').buildSync({entryPoints:['electron/services/knowledgeBaseService.ts'],bundle:false,format:cjs,platform:'node',outfile:'__t.js',logLevel:'error'}); require('fs').unlinkSync('__t.js'); console.log('esbuild-ok')" && grep -n "SELECT id, file_path, description FROM kb_images" electron/services/knowledgeBaseService.ts | grep -c "attachImages\|file_path" >/dev/null && echo "grep-ok"</automated>
  </verify>
  <done>
    - esbuild 打包 knowledgeBaseService.ts 通过
    - search 函数内 `SELECT id, file_path, description FROM kb_images` 出现在 attachImages helper（仅 1 处定义，不再内联于 line 724-729）
    - search 4 条返回路径均调用 attachImages（grep `attachImages(db` 至少 3 处 + `attachImages(db2` 1 处）
    - ragQuery / getDocument / 其他 KB 函数未被改动
  </done>
</task>

<task type="auto">
  <name>Task 2: 前端 KbSearchResult 类型补 images + 检索卡用 ChunkContent 渲染</name>
  <read_first>
    - src/types/kb.ts:1-48（KbImage line 5-10、KbSearchResult line 42-48）
    - src/components/pages/KnowledgeBasePage.tsx:34-97（ChunkContent 签名 + 用法）
    - src/components/pages/KnowledgeBasePage.tsx:437-463（检索 Card 当前纯文本渲染）
  </read_first>
  <action>
1. **src/types/kb.ts:42-48** `KbSearchResult` 接口新增可选 images 字段（向后兼容，旧调用方 images=undefined 不崩）：

```typescript
export interface KbSearchResult {
  id: string
  title?: string
  content?: string
  document?: { title: string }
  score?: number
  images?: KbImage[]   // FE-04 defer：检索结果 attach 图片，供 ChunkContent 渲染 [图片N]
}
```

字段顺序保持现有，仅追加 `images?: KbImage[]`（复用 line 5-10 的 KbImage，已含 file_path）。注释中文，与文件既有注释风格一致。

2. **src/components/pages/KnowledgeBasePage.tsx:449-461** 检索结果 Card body：用 `ChunkContent` 替换纯文本 div。

替换前（line 457-459）：
```tsx
<div style={{ maxHeight: 80, overflow: 'hidden' }}>
  {r.content?.slice(0, 300)}{(r.content?.length ?? 0) > 300 ? '...' : ''}
</div>
```

替换后：
```tsx
<ChunkContent content={r.content || ''} images={r.images || []} />
```

UX 说明（planner 裁量已决）：ChunkContent 自带 `maxHeight:300 overflow:auto`（line 67），比原 `maxHeight:80 overflow:hidden` 高，但检索场景用户需看图片，滚动浏览优于截断隐藏；检索卡数量少（topK=5），不会过长。`images` 用 `r.images || []` 守卫 undefined（旧响应无此字段时 ChunkContent 走 `images.length === 0` 分支，`[图片N]` 降级为灰底 placeholder span，不崩）。

禁止改动：ChunkContent 组件本身（line 34-97 不动）、imageCache.ts、searchResults state 类型推断、handleSearch、Card title（line 450-456 不动）。
  </action>
  <verify>
    <automated>cd "E:/knowlegdge_base/claude/network_toplogy" && npx tsc -p tsconfig.web.json --noEmit && grep -c "images?: KbImage\[\]" src/types/kb.ts && grep -n "ChunkContent content={r.content" src/components/pages/KnowledgeBasePage.tsx | grep -q "images={r.images" && echo "render-ok"</automated>
  </verify>
  <done>
    - `tsc -p tsconfig.web.json --noEmit` 全绿（含 noUnusedLocals）
    - `KbSearchResult` 含 `images?: KbImage[]` 字段（grep 计数 = 1，注释行不算）
    - 检索 Card body 使用 `<ChunkContent content={r.content || ''} images={r.images || []} />`（grep 命中）
    - 原 `r.content?.slice(0, 300)` 纯文本 div 已删除（grep `slice(0, 300)` 在 KnowledgeBasePage.tsx 中 0 命中或仅出现在其他无关位置）
    - ChunkContent 组件、imageCache.ts 未被修改
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main→renderer（IPC） | search 返回的 images 数组跨进程传输，含 file_path（本地路径字符串） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-sj1-01 | Information Disclosure | kb.search IPC 返回 images[].file_path | accept | file_path 仅 main 进程用于本地读取，renderer 仅传回 getImage(file_path) → IPC kb.getImageData；路径不外泄、不入日志。与 getDocument 现有行为一致（line 86-94 同样返回 file_path），不引入新暴露面。 |
| T-sj1-02 | Tampering | JSON.parse(chunk.image_ids) 解析异常 | mitigate | attachImages helper try/catch 守卫，parse 失败 fallback `images=[]`（复用 getDocument line 92-95 模式）。 |
</threat_model>

<verification>
1. `node esbuild` 打包 knowledgeBaseService.ts 通过（main 侧）
2. `tsc -p tsconfig.web.json --noEmit` 全绿（renderer 侧严格模式 + noUnusedLocals）
3. `npx vitest run`（若 KB 有既有测试）全绿，无回归
4. 手动验证（execute-plan 阶段非本 plan 内）：上传含图片的 docx → 等 ready → 检索关键词命中含 `[图片N]` 的 chunk → 检索卡内 `[图片N]` 渲染为缩略图，点击放大正常
</verification>

<success_criteria>
- 检索结果 Card body 渲染 `[图片N]` 为真实缩略图（非纯文本标记）
- 后端 search 4 条返回路径（无 AI config fallback / 空 indices fallback / AI pick / catch）行为一致：均 attach 含 file_path 的 images 数组
- KbSearchResult 类型扩展向后兼容（images 可选）
- 仅改 3 文件指定区域，KB 其他功能（upload/list/detail/merge/split/ragQuery）零改动
- 三绿：esbuild / tsc / vitest
</success_criteria>

<output>
完成后创建 `.planning/quick/260705-sj1-kb-snippet/260705-sj1-SUMMARY.md`，记录改动行号、verify 输出、手动验证图片渲染截图说明。
</output>
