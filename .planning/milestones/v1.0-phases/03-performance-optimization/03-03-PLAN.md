---
phase: 03-performance-optimization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/init.ts
  - electron/database/migrations.ts
autonomous: true
requirements: [PERF-03, PERF-04]

must_haves:
  truths:
    - "kb_chunks_au UPDATE trigger 带 WHEN 条件（content/title/image_ids 未变时不重索引）"
    - "新装库（init.ts DDL）与现有库（v7 迁移）两处 trigger 定义逐字一致，均带相同 WHEN"
    - "v7 迁移遵循 D-07 原子步骤（db.transaction 包 DDL+user_version=7）+ D-14 幂等守卫（sqlite_master 查 trigger sql 是否含 WHEN，已含 no-op）"
    - "MIGRATION_HEAD 由 6 推进到 7，MIGRATIONS 注册表新增第 7 项"
    - "两个真实条件幂等跳过点（initDefaultOUIData count>0 / runMigrations version≥HEAD）有可观测日志证明'跳过'行为（二次启动跳过日志可见）"
    - "现有库（已建无 WHEN 的 trigger）启动时由 v7 自动 DROP+CREATE 升级到带 WHEN 版本，无人工干预"
  artifacts:
    - path: "electron/database/init.ts"
      provides: "kb_chunks_au DDL 加 WHEN（fresh-install）+ initDefaultOUIData(count>0) 条件跳过日志"
      contains: "WHEN OLD.content IS NOT NEW.content"
    - path: "electron/database/migrations.ts"
      provides: "v7 迁移（DROP+CREATE trigger 带 WHEN + 幂等守卫）+ MIGRATION_HEAD=7 + runMigrations(version≥HEAD) 条件跳过日志"
      contains: "kb_chunks_au"
  key_links:
    - from: "electron/database/init.ts:273 kb_chunks_au DDL"
      to: "WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids"
      via: "fresh-install 直接建带 WHEN 的 trigger"
      pattern: "WHEN OLD\\.content IS NOT NEW\\.content"
    - from: "electron/database/migrations.ts v7"
      to: "DROP TRIGGER IF EXISTS kb_chunks_au + CREATE TRIGGER ... WHEN ..."
      via: "现有库迁移升级（D-07 原子 + D-14 幂等守卫）"
      pattern: "DROP TRIGGER IF EXISTS kb_chunks_au"
    - from: "electron/database/migrations.ts MIGRATION_HEAD"
      to: "7"
      via: "v7 落地后版本推进"
      pattern: "MIGRATION_HEAD = 7"
---

<objective>
PERF-03 + PERF-04（合并：两者都改 init.ts 与 migrations.ts，合并避免同文件跨 wave 冲突）。

**PERF-03**：FTS UPDATE 触发器 `kb_chunks_au` 加 `WHEN` 条件（content/title/image_ids 未变时不删+插重索引）。落地两处定义逐字一致：(a) init.ts DDL（fresh-install）；(b) 新增 v7 迁移（现有库 DROP+CREATE）。MIGRATION_HEAD 6→7。

**PERF-04**：init 启动路径幂等化加固 + **两个真实条件跳过点**（initDefaultOUIData count>0 / runMigrations version≥HEAD）加可观测日志，二次冷启动跳过日志可见（success criteria 硬指标）。不引入 worker thread，DDL/OUI seed 保持主线程同步。冷启动耗时的 before/after 计时由 Plan 03-02 的 main.ts `performance.now()` 日志行提供（03-03 不碰 main.ts，仅引用）。

Purpose: kb_chunks 任何 UPDATE（含仅改 chunk_index/level/char_count 等非 FTS 字段）现状都触发 trigger 删+插 FTS 重索引，开销随 UPDATE 频率放大；加 WHEN 后仅 FTS 来源字段变化才重索引。二次启动已基本幂等（CREATE TABLE IF NOT EXISTS / count>0 / version≥HEAD 三重跳过），但跳过事件无可观测日志——PERF-04 在**两个能真实证明"跳过"的条件早返回点**加日志让"已跳过"可验证（createTables 无单一跳过判定，不加日志，避免装饰性日志）。

Output: 改造后的 `init.ts`（trigger DDL 加 WHEN + initDefaultOUIData 跳过日志）、`migrations.ts`（v7 + HEAD=7 + runMigrations 跳过日志），tsc+esbuild 双绿，向后兼容（现有库自动迁移、trigger 定义两处一致、失败语义不变）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-performance-optimization/03-CONTEXT.md
@.planning/phases/03-performance-optimization/03-PATTERNS.md
@.planning/phases/02-architecture-db-migration/02-CONTEXT.md
@electron/database/init.ts
@electron/database/migrations.ts
@electron/services/systemLog.ts

<interfaces>
<!-- kb_chunks_au trigger 现状（init.ts:273-280，PERF-03 改造对象） -->

From electron/database/init.ts:261-280（FTS trigger 块，_au 行 273 缺 WHEN）:
```sql
CREATE TRIGGER IF NOT EXISTS kb_chunks_ai AFTER INSERT ON kb_chunks BEGIN ... END;
CREATE TRIGGER IF NOT EXISTS kb_chunks_ad AFTER DELETE ON kb_chunks BEGIN ... END;
CREATE TRIGGER IF NOT EXISTS kb_chunks_au AFTER UPDATE ON kb_chunks BEGIN   -- ← 行 273：缺 WHEN
  INSERT INTO kb_chunks_fts(kb_chunks_fts, rowid, title, content, image_desc)
    VALUES ('delete', old.rowid, old.title, old.content,
      (SELECT GROUP_CONCAT(description, ' ') FROM kb_images WHERE chunk_id = old.id));
  INSERT INTO kb_chunks_fts(rowid, title, content, image_desc)
    VALUES (new.rowid, new.title, new.content,
      (SELECT GROUP_CONCAT(description, ' ') FROM kb_images WHERE chunk_id = new.id));
END;
```

<!-- v6 迁移先例（migrations.ts:130-162，v7 镜像此模式处理 trigger） -->

From electron/database/migrations.ts（v6 trigger/表重建 + sqlite_master sql-content 守卫 + DROP+CREATE，v7 对 trigger 同构）:
```typescript
const v6 = (db: Database.Database): void => {
  const logSchema = (db.prepare("SELECT sql FROM sqlite_master WHERE type='table' AND name='ai_system_logs'").get() as { sql?: string } | undefined)?.sql || ''
  if (logSchema.includes("'warning'")) { return }   // D-14 幂等守卫
  const step = db.transaction(() => {
    db.exec("DROP TABLE IF EXISTS ai_system_logs_new")
    db.exec(`CREATE TABLE ai_system_logs_new (...)`)
    db.exec(`INSERT INTO ai_system_logs_new SELECT ... FROM ai_system_logs;`)
    db.exec(`DROP TABLE ai_system_logs;`)
    db.exec(`ALTER TABLE ai_system_logs_new RENAME TO ai_system_logs;`)
    db.pragma('user_version = 6')
  })
  step()
}

export const MIGRATION_HEAD = 6   // ← 改为 7
const MIGRATIONS: MigrationStep[] = [
  ... { version: 6, name: '...', run: v6 },
  // ← 新增 { version: 7, name: 'kb_chunks_au FTS UPDATE trigger add WHEN', run: v7 }
]

export function runMigrations(): void {
  const db = getDatabase()
  const current = (db.pragma('user_version') as Array<{ user_version: number }>)[0]?.user_version ?? 0
  if (current >= MIGRATION_HEAD) return   // ← 行 187：PERF-04 跳过日志点
  ...
}
```

PERF-04 真实条件幂等跳过点（init.ts / migrations.ts）:
- initDefaultOUIData count>0 早返回（init.ts:294-295）— **真实跳过条件**，加日志
- runMigrations version≥HEAD 早返回（migrations.ts:187）— **真实跳过条件**，加日志
- createTables()（init.ts:3）— 20+ 表混合 CREATE TABLE IF NOT EXISTS，**无单一"全跳过"判定**，**不加日志**（Warning 2：装饰性日志无法证明"跳过"，删之）

createSystemLog 用法（systemLog.ts:17-43，type CHECK 现含 discovery/acl/migration/backup，v6 已 widen 含 warning）:
```typescript
createSystemLog({ type: 'migration', status: 'success', errorMessage: '[startup] ... 跳过（已幂等）' })
```

Phase 2 先例（D-07 步骤原子性 + D-14 幂等守卫，v7 必须遵循）:
- D-07: `db.transaction(() => {DDL; db.pragma('user_version = N')})`，DDL 与版本号同事务提交
- D-14: 幂等守卫查 sqlite_master sql-content，已含目标特征则 no-op（v5 查 'rdp'、v6 查 'warning'、v7 查 trigger sql 含 'WHEN'）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PERF-03 — init.ts kb_chunks_au DDL 加 WHEN + migrations.ts v7 迁移（DROP+CREATE + 幂等守卫 + HEAD=7）</name>
  <files>electron/database/init.ts, electron/database/migrations.ts</files>
  <read_first>
    - electron/database/init.ts:252-281（FTS5 虚表 + 3 trigger，_au 行 273-280 缺 WHEN 的改造对象；注意 _ai/_ad 不变）
    - electron/database/migrations.ts 全文（v5 行 80-128 / v6 行 130-162 是 trigger/表重建+幂等守卫先例；MIGRATION_HEAD 行 16；MIGRATIONS 注册表 行 164-171；runMigrations 行 182-210）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P3（WHEN 覆盖 content+title+image_ids + 落地两处一致 + v7 遵循 D-07/D-14 + MIGRATION_HEAD 6→7）
    - .planning/phases/03-performance-optimization/03-PATTERNS.md §init.ts（FTS trigger 现状 + 改造目标 WHEN 形态）+ §migrations.ts（v7 target 形态 + 守卫用 type='trigger' AND name='kb_chunks_au' 查 WHEN + 注册表/HEAD 推进）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §PATTERNS caveat（CREATE TRIGGER IF NOT EXISTS 对已存在但定义不同的 trigger 不替换 → 现有库必须 v7 先 DROP 再 CREATE；两处定义逐字一致）
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md §D-07（步骤原子性）+ §D-14/D-15（幂等守卫，v7 同模式）
  </read_first>
  <action>
按 D-P3 落地 trigger WHEN，**两处定义必须逐字一致**（PATTERNS caveat 红线）：

1. **init.ts:273 fresh-install DDL 加 WHEN**：把 `kb_chunks_au` 的 `CREATE TRIGGER IF NOT EXISTS kb_chunks_au AFTER UPDATE ON kb_chunks BEGIN` 改为（在 `ON kb_chunks` 与 `BEGIN` 之间插入 WHEN 子句）：
   ```sql
   CREATE TRIGGER IF NOT EXISTS kb_chunks_au AFTER UPDATE ON kb_chunks
   WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids
   BEGIN
     INSERT INTO kb_chunks_fts(kb_chunks_fts, rowid, title, content, image_desc)
       VALUES ('delete', old.rowid, old.title, old.content,
         (SELECT GROUP_CONCAT(description, ' ') FROM kb_images WHERE chunk_id = old.id));
     INSERT INTO kb_chunks_fts(rowid, title, content, image_desc)
       VALUES (new.rowid, new.title, new.content,
         (SELECT GROUP_CONCAT(description, ' ') FROM kb_images WHERE chunk_id = new.id));
   END;
   ```
   BEGIN...END 内的两条 INSERT 与现状逐字一致，仅在 trigger 头加 WHEN。`_ai`（INSERT）和 `_ad`（DELETE）trigger **不动**（INSERT/DELETE 无 WHEN 概念）。注意保留 `IF NOT EXISTS`（fresh-install 防重复；现有库由 v7 处理）。

2. **migrations.ts 新增 v7 迁移**：在 v6 定义之后（行 162 后）、`const MIGRATIONS` 之前加 `const v7`，镜像 v6 的"幂等守卫 + db.transaction 包 DROP+CREATE + user_version"模式：
   ```typescript
   const v7 = (db: Database.Database): void => {
     // PERF-03：kb_chunks_au UPDATE trigger 加 WHEN（content/title/image_ids 未变不重索引）
     // 幂等守卫 D-14 第二形式：sqlite_master 查 trigger sql 是否已含 WHEN（与 v5 查 'rdp'、v6 查 'warning' 同构）
     const triggerSql = (db.prepare(
       "SELECT sql FROM sqlite_master WHERE type='trigger' AND name='kb_chunks_au'"
     ).get() as { sql?: string } | undefined)?.sql || ''
     if (triggerSql.includes('WHEN')) {
       return // trigger 已带 WHEN，no-op（幂等重跑 D-14）
     }
     const step = db.transaction(() => {
       db.exec('DROP TRIGGER IF EXISTS kb_chunks_au')
       db.exec(`CREATE TRIGGER kb_chunks_au AFTER UPDATE ON kb_chunks
   WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids
   BEGIN
     INSERT INTO kb_chunks_fts(kb_chunks_fts, rowid, title, content, image_desc)
       VALUES ('delete', old.rowid, old.title, old.content,
         (SELECT GROUP_CONCAT(description, ' ') FROM kb_images WHERE chunk_id = old.id));
     INSERT INTO kb_chunks_fts(rowid, title, content, image_desc)
       VALUES (new.rowid, new.title, new.content,
         (SELECT GROUP_CONCAT(description, ' ') FROM kb_images WHERE chunk_id = new.id));
   END`)
       db.pragma('user_version = 7')
     })
     step()
   }
   ```
   关键点：
   - **v7 用 `CREATE TRIGGER`（不带 IF NOT EXISTS）**——因前置已 `DROP TRIGGER IF EXISTS`，此时必不存在，直接 CREATE。PATTERNS caveat：`CREATE TRIGGER IF NOT EXISTS` 对已存在但定义不同的 trigger 不替换，故现有库必须先 DROP 再 CREATE。
   - **守卫查 `type='trigger' AND name='kb_chunks_au'`** 的 sql 是否含 `WHEN`（与 v5/v6 查表/CHECK 的 sql-content 同模式）。
   - **不需要 foreign_key_check**（trigger 改动不涉表数据完整性，区别于 v5/v6 表重建，PATTERNS §migrations.ts 明示）。
   - **v7 的 CREATE TRIGGER DDL 必须与 init.ts fresh-install DDL 逐字一致**（同一 WHEN 条件、同一两条 INSERT）——PATTERNS caveat 红线，两处定义不一致会导致 fresh-install 与 migrated 库行为分叉。

3. **MIGRATION_HEAD 推进 + 注册表新增**：
   - `export const MIGRATION_HEAD = 6` → `export const MIGRATION_HEAD = 7`
   - `MIGRATIONS` 数组末尾（v6 之后）加 `{ version: 7, name: 'kb_chunks_au FTS UPDATE trigger add WHEN (skip non-FTS-field updates)', run: v7 }`

4. **D-07 原子性确认**：v7 的 DROP+CREATE+user_version 在同一 `db.transaction` 内（步骤 2 已包），DDL 与版本号同事务提交（D-07）。失败则 better-sqlite3 自动 ROLLBACK，DB 停留 v6，runMigrations 的 catch（行 191-208）写 system log + 抛出中止（D-08）。

**禁止**：给 _ai/_ad trigger 加 WHEN（INSERT/DELETE 无 WHEN 概念）；v7 用 CREATE TRIGGER IF NOT EXISTS 而不先 DROP（caveat：不替换已存在定义）；v7 的 trigger DDL 与 init.ts DDL 不一致；删 foreign_key_check（v7 本就不需要，v5/v6 各自保留各自的）；改 MIGRATIONS 已有 v1-v6 项。
  </action>
  <verify>
    <automated>
cd "E:\knowlegdge_base\claude\network_toplogy" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; node -e "const fs=require('fs');const init=fs.readFileSync('electron/database/init.ts','utf8');const mig=fs.readFileSync('electron/database/migrations.ts','utf8');const WHEN='WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids';const checks={initHasWHEN:init.includes(WHEN),initAuCount:(init.match(/kb_chunks_au/g)||[]).length,migV7HasDROP:/DROP TRIGGER IF EXISTS kb_chunks_au/.test(mig),migV7HasWHEN:mig.includes(WHEN),migV7HasVersion7:/db\.pragma\('user_version = 7'\)/.test(mig),migV7Guard:/type='trigger' AND name='kb_chunks_au'/.test(mig)&amp;&amp;/triggerSql\.includes\('WHEN'\)/.test(mig),migHead7:/MIGRATION_HEAD = 7/.test(mig),migRegistry7:/version: 7.*kb_chunks_au FTS UPDATE trigger add WHEN/.test(mig),noForeignKeyCheckInV7:true};console.log(JSON.stringify(checks,null,2));if(!checks.initHasWHEN||!checks.migV7HasDROP||!checks.migV7HasWHEN||!checks.migV7HasVersion7||!checks.migV7Guard||!checks.migHead7||!checks.migRegistry7){process.exit(1)}" &amp;&amp; git diff --stat electron/database/init.ts electron/database/migrations.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids" electron/database/init.ts` ≥ 1（fresh-install DDL 加 WHEN）
    - `grep -c "WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids" electron/database/migrations.ts` ≥ 1（v7 CREATE TRIGGER 加 WHEN）
    - `grep -c "DROP TRIGGER IF EXISTS kb_chunks_au" electron/database/migrations.ts` ≥ 1（v7 先 DROP）
    - `grep -c "user_version = 7" electron/database/migrations.ts` ≥ 1（v7 版本推进）
    - `grep -c "type='trigger' AND name='kb_chunks_au'" electron/database/migrations.ts` ≥ 1（D-14 幂等守卫）
    - `grep -c "triggerSql.includes('WHEN')" electron/database/migrations.ts` ≥ 1（守卫判定逻辑）
    - `grep -c "MIGRATION_HEAD = 7" electron/database/migrations.ts` = 1（HEAD 推进，不再有 = 6）
    - `grep -c "version: 7, name: 'kb_chunks_au FTS UPDATE trigger add WHEN" electron/database/migrations.ts` ≥ 1（注册表新增第 7 项）
    - init.ts 中 `kb_chunks_ai` 与 `kb_chunks_ad` trigger 定义未被修改（grep 仍命中原 INSERT/DELETE trigger 头，不含 WHEN）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
    - esbuild 打包 electron/main.ts（含 init/migrations 传递依赖）exit 0
  </acceptance_criteria>
  <done>
    init.ts:273 kb_chunks_au DDL 加 WHEN（fresh-install）；migrations.ts 新增 v7 迁移（幂等守卫查 trigger sql 含 WHEN 则 no-op + db.transaction 包 DROP+CREATE+user_version=7）；两处 trigger 定义逐字一致；MIGRATION_HEAD=7，注册表加第 7 项；_ai/_ad 不变；tsc+esbuild 双绿。
  </done>
</task>

<task type="auto">
  <name>Task 2: PERF-04 — 两个真实条件幂等跳过点加可观测日志（initDefaultOUIData count>0 / runMigrations version≥HEAD）</name>
  <files>electron/database/init.ts, electron/database/migrations.ts</files>
  <read_first>
    - electron/database/init.ts:293-296（initDefaultOUIData count>0 早返回点 — 真实跳过条件，加日志对象）
    - electron/database/migrations.ts:182-188（runMigrations 的 current>=MIGRATION_HEAD 早返回点，行 187 — 真实跳过条件，加日志对象）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P4（幂等机制现状已存在，核心工作是加启动跳过可观测日志；type 复用 migration 避免 v8；启动早期 system_logs 表未就绪回退 console）
    - .planning/phases/03-performance-optimization/03-PATTERNS.md §init.ts "PERF-04 幂等跳过日志点" + §Shared Patterns "createSystemLog"（用法先例：try/catch 包裹 + type=migration + 回退 console.log）
    - electron/services/systemLog.ts:17-49（createSystemLog 签名 + truncate；type CHECK 现含 discovery/acl/migration/backup + warning，复用 migration 无需 v8）
    - electron/database/migrations.ts:196-202（runMigrations 现有 createSystemLog 用法先例 — try/catch 包裹）
  </read_first>
  <action>
按 D-P4 在**两个真实条件幂等跳过点**加可观测日志，**type 复用 `migration`（CHECK 已含，无需 v8 迁移扩 CHECK）**，启动早期 system_logs 表未就绪时回退 `console.log`。

**关键决策（Warning 2）：不加 createTables 日志。** createTables（init.ts:3）整体是 `CREATE TABLE IF NOT EXISTS`（20+ 表混合存在/不存在），无单一"全跳过"判定——若加日志只能写"幂等完成"而非"全部跳过"，grep 只验字符串存在无法区分"表被跳过"vs"表被创建"，是装饰性日志。两个真实条件跳过日志（initDefaultOUIData `count>0` return、runMigrations `version≥HEAD` early return）已真实证明跳过行为，足以满足 SC#4 "二次启动跳过日志可见"，createTables 日志删之。

1. **runMigrations version≥HEAD 跳过日志（migrations.ts:187）**：在 `if (current >= MIGRATION_HEAD) return` 的 return 之前加跳过日志：
   ```typescript
   if (current >= MIGRATION_HEAD) {
     // PERF-04：迁移已最新跳过（可观测日志，二次启动可见）
     try {
       createSystemLog({ type: 'migration', status: 'success', errorMessage: `[startup] runMigrations 跳过：user_version=${current} 已达 HEAD=${MIGRATION_HEAD}，无待执行迁移` })
     } catch { console.log(`[startup] runMigrations 跳过：user_version=${current} 已达 HEAD=${MIGRATION_HEAD}（system_logs 未就绪回退 console）`) }
     return
   }
   ```
   migrations.ts 已 import createSystemLog（行 4），直接用。try/catch 回退保证启动早期表未就绪不崩。

2. **initDefaultOUIData count>0 跳过日志（init.ts:294-295）**：在 `if (count > 0) return` 改为先日志后 return。注意 `initDefaultOUIData` 现状未 import createSystemLog——在 init.ts 顶部加 `import { createSystemLog } from '../services/systemLog'`（若 init.ts 已 import 则跳过）。改造：
   ```typescript
   function initDefaultOUIData(db: any) {
     const count = (db.prepare('SELECT COUNT(*) as cnt FROM oui_database').get() as any).cnt
     if (count > 0) {
       // PERF-04：OUI seed 已存在跳过（可观测日志，二次启动可见）
       try {
         createSystemLog({ type: 'migration', status: 'success', errorMessage: `[startup] initDefaultOUIData 跳过：oui_database 已有 ${count} 行 seed，不重复插入` })
       } catch { console.log(`[startup] initDefaultOUIData 跳过：oui_database 已有 ${count} 行（system_logs 未就绪回退 console）`) }
       return
     }
     ... // 原 seed 逻辑不动
   }
   ```

3. **type 复用 migration（D-P4 discretion 决策）**：`ai_system_logs.type` CHECK 现含 `discovery/acl/migration/backup`（init.ts:86 + v6 已 widen）——启动跳过日志复用 `migration` type，避免为"startup_skip" 新 type 扩 CHECK（否则需 v8 迁移，D-P4 判定不值得，CONTEXT §discretion 明示）。status 用 `success`（跳过是正常成功路径，非 warning）。

4. **冷启动 before/after 证据来源（Warning 1 联动）**：本 plan 不碰 main.ts（编辑权归 Plan 02）。冷启动耗时的 before/after 计时由 **Plan 03-02 在 main.ts 加的 `performance.now()` 日志行** `console.log('[startup] DB+OUI init Xms')` 提供。executor 在本 plan 完成后，于 `03-03-SUMMARY.md` 引用 Plan 03-02 该日志行的 before/after 数值（无需自行重测或手工秒表），证明 PERF-03/04（FTS WHEN + init 跳过）对冷启动的实际影响。

**禁止**：为启动跳过日志新增 type 扩 CHECK（否则需 v8，D-P4 不值得）；不加 try/catch 直接调 createSystemLog（启动早期表未就绪会抛）；改 createTables/initDefaultOUIData/runMigrations 的幂等判定逻辑（D-P4 现状已幂等，只加日志）；**加 createTables 日志（Warning 2：无单一跳过判定，装饰性日志无法证明"跳过"）**；修改 main.ts（编辑权归 Plan 02）。
  </action>
  <verify>
    <automated>
cd "E:\knowlegdge_base\claude\network_toplogy" &amp;&amp; npx tsc -p tsconfig.web.json --noEmit &amp;&amp; node -e "const fs=require('fs');const init=fs.readFileSync('electron/database/init.ts','utf8');const mig=fs.readFileSync('electron/database/migrations.ts','utf8');const checks={initImportSysLog:/import \{ createSystemLog \} from '\.\.\/services\/systemLog'/.test(init),initOuiSkipLog:/initDefaultOUIData 跳过/.test(init),migRunMigSkipLog:/runMigrations 跳过/.test(mig),noCreateTablesLog:!/createTables 完成/.test(init),typeMigrationCountInit:(init.match(/type: 'migration'/g)||[]).length,typeMigrationCountMig:(mig.match(/type: 'migration'/g)||[]).length,consoleFallback:/system_logs 未就绪回退 console/.test(init)&amp;&amp;/system_logs 未就绪回退 console/.test(mig)};console.log(JSON.stringify(checks,null,2));const pass=checks.initImportSysLog&amp;&amp;checks.initOuiSkipLog&amp;&amp;checks.migRunMigSkipLog&amp;&amp;checks.noCreateTablesLog&amp;&amp;(checks.typeMigrationCountInit+checks.typeMigrationCountMig)>=2&amp;&amp;checks.consoleFallback;if(!pass){process.exit(1)}" &amp;&amp; git diff --stat electron/database/init.ts electron/database/migrations.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { createSystemLog } from '../services/systemLog'" electron/database/init.ts` ≥ 1（init.ts 新增 import，或确认已存在）
    - `grep -c "initDefaultOUIData 跳过" electron/database/init.ts` ≥ 1（OUI seed count>0 真实条件跳过日志）
    - `grep -c "runMigrations 跳过" electron/database/migrations.ts` ≥ 1（version≥HEAD 真实条件跳过日志）
    - **`grep -c "createTables 完成" electron/database/init.ts` = 0（Warning 2：createTables 装饰性日志已删，无单一跳过判定不加日志）**
    - init.ts + migrations.ts 中两个真实条件跳过点的 `type: 'migration'` 出现 ≥ 2 次（initDefaultOUIData + runMigrations 各 1）
    - `grep -c "system_logs 未就绪回退 console" electron/database/init.ts` ≥ 1 且 `grep -c "system_logs 未就绪回退 console" electron/database/migrations.ts` ≥ 1（两文件均有 console 回退）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
    - esbuild 打包 exit 0
    - 不新增 v8 迁移（grep "user_version = 8" 命中 0）
  </acceptance_criteria>
  <done>
    initDefaultOUIData(count>0) / runMigrations(version≥HEAD) 两个真实条件幂等跳过点均有可观测日志（type=migration 复用 CHECK，无需 v8），启动早期 system_logs 未就绪时回退 console.log，二次启动跳过日志可见（success criteria 硬指标达成）；createTables 装饰性日志已删（Warning 2）；冷启动 before/after 证据引用 Plan 03-02 的 main.ts performance.now() 日志行；tsc+esbuild 双绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh-install DDL vs 现有库 v7 迁移 | 两处 trigger 定义必须逐字一致，否则 fresh-install 与 migrated 库行为分叉 |
| v7 迁移失败 → trigger 丢失 | 迁移中途失败若不回滚会留下"DROP 已执行、CREATE 未执行"的中间态（trigger 丢失致 FTS 不再更新） |
| 启动早期 system_logs 表未就绪 | createTables 之前/之中调 createSystemLog 可能因表不存在抛错 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | v7 迁移失败致 kb_chunks_au trigger 丢失（FTS 不再更新） | mitigate | v7 的 DROP+CREATE+user_version 包在单个 db.transaction 内（D-07），任一步 throw → better-sqlite3 自动 ROLLBACK（DROP 也回滚，trigger 保持原状）；runMigrations 的 catch（migrations.ts:191-208）写 system log + 抛出中止（D-08），DB 停留 v6，trigger 仍在（旧的无 WHEN 版本），FTS 仍更新（仅失去 WHEN 优化，非数据损坏） |
| T-03-10 | Tampering | 两处 trigger 定义不一致（fresh-install vs migrated 库行为分叉） | mitigate | Task 1 acceptance 用同一 WHEN 字面量字符串校验两文件均命中；action 步骤 2 明示"v7 的 CREATE TRIGGER DDL 必须与 init.ts fresh-install DDL 逐字一致"为红线；WHEN 条件 + 两条 INSERT 在两处逐字复制 |
| T-03-11 | Denial of Service | 启动早期 system_logs 表未就绪，createSystemLog 抛错致启动崩溃 | mitigate | 两个跳过日志点均 try/catch 包裹，catch 回退 `console.log`（D-P4 明示回退路径）；try/catch 兜底任何未预期错误；Task 2 acceptance 含两文件 console 回退校验 |
| T-03-12 | Repudiation | 迁移/启动跳过事件无记录，无法审计冷启动路径 | mitigate | PERF-04 核心交付：**两个真实条件跳过点**（initDefaultOUIData count>0 / runMigrations version≥HEAD）加 createSystemLog（type=migration）可观测日志，二次启动跳过日志可见（success criteria 硬指标）；type 复用 migration 避免 v8 扩 CHECK 的过度工程（D-P4 discretion） |
| T-03-13 | Information Disclosure | 启动日志含 user_version / oui seed 行数等内部状态 | accept | user_version 与 oui seed 行数是 DB schema/数据规模元数据，非敏感凭证；日志存 ai_system_logs（已 ACL 收紧 D-10 仅当前用户可读）+ console（本地运维可见），无新增泄露面 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 双绿基线
- esbuild 打包 exit 0
- grep WHEN 条件：init.ts + migrations.ts 均含 `WHEN OLD.content IS NOT NEW.content OR OLD.title IS NOT NEW.title OR OLD.image_ids IS NOT NEW.image_ids`（两处定义逐字一致）
- grep v7 迁移：`DROP TRIGGER IF EXISTS kb_chunks_au` + `user_version = 7` + `type='trigger' AND name='kb_chunks_au'` 守卫 + `triggerSql.includes('WHEN')` 判定
- grep HEAD 推进：`MIGRATION_HEAD = 7`（不再 = 6）
- grep 注册表：`version: 7, name: 'kb_chunks_au FTS UPDATE trigger add WHEN'`
- grep 真实条件跳过日志：initDefaultOUIData 跳过 / runMigrations 跳过 两个点均有 createSystemLog + console 回退
- grep createTables 装饰性日志已删：`createTables 完成` 命中 0（Warning 2）
- grep 无 v8：`user_version = 8` 命中 0（type 复用 migration，不扩 CHECK）
- _ai/_ad trigger 未被修改（仅 _au 加 WHEN）
- 冷启动 before/after 证据：引用 Plan 03-02 的 main.ts `[startup] DB+OUI init Xms` 日志行（本 plan 不碰 main.ts）
</verification>

<success_criteria>
- FTS UPDATE 触发器带 WHEN：`grep "WHEN OLD.content IS NOT NEW.content" init.ts + migrations.ts` 两处均命中（PERF-03 success criteria 3）
- 现有库自动迁移：v7 迁移 DROP+CREATE 升级 trigger 到带 WHEN 版本，幂等守卫（D-14）防重跑
- MIGRATION_HEAD=7，注册表加第 7 项，两处 trigger 定义逐字一致
- init 跳过可观测：**两个真实条件跳过点**（initDefaultOUIData count>0 / runMigrations version≥HEAD）均有日志，二次启动跳过日志可见（PERF-04 success criteria 4 硬指标）；createTables 无单一跳过判定不加装饰性日志（Warning 2）
- type 复用 migration，不新增 v8 迁移扩 CHECK（D-P4 discretion）
- 冷启动加速证据：引用 Plan 03-02 main.ts performance.now() 日志行（phase goal "冷启动加速" + SC#4 自动化证据）
- tsc + esbuild 双绿，向后兼容（现有库迁移不丢 trigger、DDL/OUI seed 主线程同步、失败语义不变）
</success_criteria>

<output>
After completion, create `.planning/phases/03-performance-optimization/03-03-SUMMARY.md`
</output>
