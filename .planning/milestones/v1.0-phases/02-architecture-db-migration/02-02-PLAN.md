---
phase: 02-architecture-db-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/backup.ts
  - electron/database/acl.ts
autonomous: true
requirements: [ARCH-02]
tags: [backup-config, acl, file-permissions, cross-platform]

must_haves:
  truths:
    - "BackupConfig 类型对齐 ScheduleConfig（enabled/intervalMinutes/lastRun/nextRun）并扩展 retention 字段（periodicRetention=7, premigrationRetention=5）"
    - "restrictFilePermissions(path) 跨平台收紧文件权限：Windows 用 icacls /inheritance:r /grant:r 当前用户:(F)，Unix 用 fs.chmod 0o600"
    - "ACL 失败非致命（写 system log 警告后继续，不抛异常）—— 数据已在应用层 AES+safeStorage 加密，ACL 是纵深防御层"
    - "restrictFilePermissions 对不存在的文件优雅处理（备份目录刚创建时 sidecar 可能不存在）—— 静默跳过 + 不崩溃"
  artifacts:
    - path: "src/types/backup.ts"
      provides: "BackupConfig / BackupStatus / UpdateBackupInput 类型"
      contains: "export interface BackupConfig"
    - path: "electron/database/acl.ts"
      provides: "restrictFilePermissions(path) 跨平台 ACL 收紧 helper"
      contains: "export function restrictFilePermissions"
  key_links:
    - from: "electron/database/acl.ts"
      to: "electron/services/systemLog.ts createSystemLog"
      via: "import { createSystemLog } from '../services/systemLog'"
      pattern: "createSystemLog\\("
    - from: "src/types/backup.ts"
      to: "（消费方）electron/services/backupScheduler.ts BackupConfig"
      via: "import type { BackupConfig } from '../../src/types/backup'"
      pattern: "BackupConfig"
---

<objective>
创建 ARCH-02 的两个无依赖叶模块：(1) `src/types/backup.ts` 的 BackupConfig 类型（对齐 ScheduleConfig + 扩展 retention，供 Plan 03 BackupScheduler 使用）；(2) `electron/database/acl.ts` 的跨平台 `restrictFilePermissions(path)` helper（Windows icacls / Unix chmod 0600，失败非致命 D-13）。

Purpose: 为 Plan 03（BackupScheduler + connection.ts 集成）提供类型契约和 ACL 工具，使两个叶模块先就位可并行于 Plan 01。
Output: backup.ts（类型）、acl.ts（ACL helper）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-architecture-db-migration/02-CONTEXT.md
@.planning/phases/02-architecture-db-migration/02-PATTERNS.md

<interfaces>
<!-- 现有契约 -->

From src/types/oui.ts (第 27-44 行，BackupConfig 类型模板):
```typescript
export interface ScheduleConfig {
  id: number
  enabled: boolean
  intervalMinutes: number
  lastRun: string | null
  nextRun: string | null
}
export interface SchedulerStatus {
  isRunning: boolean
  isTaskRunning: boolean
  config: ScheduleConfig
}
export interface UpdateScheduleInput {
  enabled?: boolean
  intervalMinutes?: number
}
```

From electron/services/systemLog.ts (ACL 失败非致命日志):
```typescript
export function createSystemLog(log: {
  type: string; status: string; deviceIds?: string; deviceNames?: string;
  promptText?: string; aiResponse?: string; parsedResult?: string; errorMessage?: string;
}): string
// 字段超 16000 字符自动截断
```

From electron/database/connection.ts (DB 路径根，ACL target 解析来源):
```typescript
// connection.ts:13  const dbPath = path.join(app.getPath('userData'), 'topology.db')
// sidecars: dbPath + '-wal', dbPath + '-shm'
// backups dir: path.join(app.getPath('userData'), 'backups')
```

Node 内建（acl.ts 使用）：
- `import { execFileSync } from 'child_process'`（Windows icacls，同步执行避免竞态）
- `import fs from 'fs'`（Unix chmod + 文件存在检查）
- `import os from 'os'`（os.userInfo().username 当前用户）
- `process.env.USERNAME`（Windows 当前用户 fallback）
- `process.platform`（'win32' | 'darwin' | 'linux' 分支）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 src/types/backup.ts（BackupConfig 类型，对齐 ScheduleConfig + retention）</name>
  <files>src/types/backup.ts</files>

  <read_first>
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md（D-01 默认 24h、D-02 双桶 retention 7/5、D-05 BackupConfig 对齐 ScheduleConfig）
    - .planning/phases/02-architecture-db-migration/02-PATTERNS.md §src/types/backup.ts（exact analog：oui.ts:27-44 + retention 扩展）
    - src/types/oui.ts（第 27-44 行 ScheduleConfig/SchedulerStatus/UpdateScheduleInput，逐字段对齐）
  </read_first>

  <action>
创建 `src/types/backup.ts`：

```typescript
/**
 * 备份配置与状态类型（D-05：对齐 ScheduleConfig，扩展 retention）。
 * DB 持久化行（backup_config 表，id=1 singleton），字段映射见 BackupScheduler.getConfig。
 */
export interface BackupConfig {
  id: number
  /** 是否启用定时备份（默认 true，D-01：app ready 即启动 24h 周期备份） */
  enabled: boolean
  /** 定时备份间隔（分钟），默认 1440 = 24h（D-01） */
  intervalMinutes: number
  lastRun: string | null
  nextRun: string | null
  /** 周期桶滚动保留份数，默认 7（D-02） */
  periodicRetention: number
  /** 迁移桶滚动保留份数，默认 5（D-02，独立裁剪不混入周期桶） */
  premigrationRetention: number
}

export interface BackupStatus {
  isRunning: boolean
  isTaskRunning: boolean
  config: BackupConfig
}

export interface UpdateBackupInput {
  enabled?: boolean
  intervalMinutes?: number
  periodicRetention?: number
  premigrationRetention?: number
}

/** BackupConfig 默认值（首次 getConfig 时 lazy-seed 用） */
export const DEFAULT_BACKUP_CONFIG = {
  enabled: true,
  intervalMinutes: 1440,
  periodicRetention: 7,
  premigrationRetention: 5,
} as const
```

关键点：
- 对齐 ScheduleConfig 的 id/enabled/intervalMinutes/lastRun/nextRun（字段名一致）
- 扩展 periodicRetention / premigrationRetention（D-02 双桶）
- DEFAULT_BACKUP_CONFIG.enabled = **true**（D-01：默认启用 24h 周期备份，与 scheduler_config 默认 enabled=false 不同——备份是数据安全核心价值，默认开）
- DEFAULT_BACKUP_CONFIG.intervalMinutes = **1440**（D-01：24h，不是 scheduler 的 60）
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && test -f src/types/backup.ts && grep -q "export interface BackupConfig" src/types/backup.ts</automated>
  </verify>

  <acceptance_criteria>
    - `src/types/backup.ts` 文件存在
    - `grep -q "export interface BackupConfig" src/types/backup.ts` 命中
    - `grep -q "periodicRetention: number" src/types/backup.ts` 命中（D-02 retention）
    - `grep -q "premigrationRetention: number" src/types/backup.ts` 命中（D-02 retention）
    - `grep -q "intervalMinutes: number" src/types/backup.ts` 命中（对齐 ScheduleConfig）
    - `grep -q "DEFAULT_BACKUP_CONFIG" src/types/backup.ts` 命中（默认值常量）
    - `grep -q "intervalMinutes: 1440" src/types/backup.ts` 命中（D-01 默认 24h）
    - `grep -q "enabled: true" src/types/backup.ts` 命中（默认启用，与 scheduler_config 不同）
    - `grep -q "periodicRetention: 7" src/types/backup.ts` 命中（D-02）
    - `grep -q "premigrationRetention: 5" src/types/backup.ts` 命中（D-02）
    - `npx tsc -p tsconfig.web.json --noEmit` 退出码 0
  </acceptance_criteria>

  <done>backup.ts 导出 BackupConfig（对齐 ScheduleConfig + periodicRetention=7/premigrationRetention=5）+ BackupStatus + UpdateBackupInput + DEFAULT_BACKUP_CONFIG（enabled=true, intervalMinutes=1440）；tsc 全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 electron/database/acl.ts（跨平台 restrictFilePermissions helper）</name>
  <files>electron/database/acl.ts</files>

  <read_first>
    - .planning/phases/02-architecture-db-migration/02-CONTEXT.md（D-10 保护范围、D-11 跨平台机制 icacls/chmod、D-12 强制时机幂等、D-13 失败非致命）
    - .planning/phases/02-architecture-db-migration/02-PATTERNS.md §acl.ts（No Analog Found，net-new；D-11 命令封装）+ §Shared Patterns「System log」
    - electron/database/connection.ts（第 13 行 dbPath 根，ACL target 来源）
    - electron/services/systemLog.ts（第 17-43 行 createSystemLog 签名，D-13 失败日志）
  </read_first>

  <action>
创建 `electron/database/acl.ts`：

```typescript
import { execFileSync } from 'child_process'
import fs from 'fs'
import os from 'os'
import { createSystemLog } from '../services/systemLog'

/**
 * 跨平台收紧单个文件权限为「仅当前用户可读写」（D-10/D-11/D-12）。
 * - Windows: icacls "<path>" /inheritance:r /grant:r "<currentUser>:(F)"
 *   （剥离继承、仅当前用户完全控制、替换显式项）
 * - Unix/macOS: fs.chmod(path, 0o600)
 *
 * 幂等（D-12）：每次 app 启动对活跃 db 文件 + 每个备份创建后立即调用，无 sentinel。
 * 非致命（D-13）：失败（权限不足/非管理员/文件不存在）写 system log 警告后继续，不抛异常——
 *   数据已在应用层 AES-256-GCM + safeStorage 加密，ACL 是纵深防御第二层。
 *
 * @param filePath 待收紧的文件绝对路径
 * @param label 日志标识（如 'topology.db' / 'topology.db-wal' / 备份文件名），便于 system log 定位
 */
export function restrictFilePermissions(filePath: string, label: string): void {
  // 文件不存在则静默跳过（WAL/SHM sidecar 在 checkpoint 后可能不存在；备份目录刚建时为空）
  if (!fs.existsSync(filePath)) return

  try {
    if (process.platform === 'win32') {
      const currentUser = process.env.USERNAME || os.userInfo().username
      if (!currentUser) {
        throw new Error('无法确定当前 Windows 用户名（USERNAME / os.userInfo 均为空）')
      }
      // /inheritance:r 剥离继承；/grant:r 替换（非追加）显式 ACE，仅当前用户完全控制
      execFileSync('icacls', [filePath, '/inheritance:r', '/grant:r', `${currentUser}:(F)`], {
        stdio: ['ignore', 'ignore', 'ignore'],
        shell: false,
        timeout: 10000,
      })
    } else {
      // Unix/macOS：仅 owner 读写
      fs.chmodSync(filePath, 0o600)
    }
  } catch (err) {
    // D-13：非致命——写 system log 警告后继续，不抛异常
    const msg = (err as Error).message
    try {
      createSystemLog({
        type: 'acl',
        status: 'warning',
        errorMessage: `文件权限收紧失败 [${label}] (${filePath}): ${msg}。数据已在应用层加密，ACL 为纵深防御层，不影响数据安全。`,
      })
    } catch {
      // 日志失败亦不抛（避免 createSystemLog 自身故障阻塞调用方）
    }
  }
}

/**
 * 收紧指定目录下所有文件权限（D-10：userData/backups/ 下所有备份文件同等保护）。
 * 幂等、非致命（委托 restrictFilePermissions）。
 * @param dirPath 目录绝对路径
 */
export function restrictDirPermissions(dirPath: string): void {
  if (!fs.existsSync(dirPath)) return
  let entries: string[]
  try {
    entries = fs.readdirSync(dirPath)
  } catch (err) {
    try {
      createSystemLog({
        type: 'acl',
        status: 'warning',
        errorMessage: `备份目录读取失败 (${dirPath}): ${(err as Error).message}`,
      })
    } catch { /* 非致命 */ }
    return
  }
  for (const entry of entries) {
    const fullPath = `${dirPath}/${entry}`
    try {
      const stat = fs.statSync(fullPath)
      if (stat.isFile()) {
        restrictFilePermissions(fullPath, `backups/${entry}`)
      }
    } catch {
      // 单文件失败已由 restrictFilePermissions 内部记录，此处跳过
    }
  }
}
```

关键约束（务必逐条遵守）：
- **Windows 用 `execFileSync`（同步）**，不用 spawn/exec 异步——ACL 收紧必须在迁移/启动流程继续前完成，避免竞态；`shell: false` 防注入；`timeout: 10000` 防挂死
- **icacls 参数精确**：`/inheritance:r /grant:r "${currentUser}:(F)"`（D-11 逐字）—— `:r` 修饰符至关重要（replace 而非 append，否则历史宽松 ACE 残留）
- **currentUser 取值顺序**：`process.env.USERNAME || os.userInfo().username`（Windows USERNAME 优先，os.userInfo 在 Windows 返回 username 字段）
- **Unix 分支**：`fs.chmodSync(filePath, 0o600)`（0o600 = owner rw，无 group/other 位）
- **文件不存在静默跳过**（不写日志）—— db-shm/db-wal 在 WAL checkpoint 后可能不存在，属正常
- **所有失败 try/catch 包裹 createSystemLog**（mirrors Phase 6 ROBUST-02 intent：日志失败不阻塞受保护路径）
- type='acl', status='warning'（D-13 警告级别，非 error，因数据已加密）
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npm run build:electron-main 2>&1 | tail -3 && test -f electron/database/acl.ts && grep -q "export function restrictFilePermissions" electron/database/acl.ts</automated>
  </verify>

  <acceptance_criteria>
    - `electron/database/acl.ts` 文件存在
    - `grep -q "export function restrictFilePermissions(filePath: string, label: string)" electron/database/acl.ts` 命中
    - `grep -q "export function restrictDirPermissions(dirPath: string)" electron/database/acl.ts` 命中（D-10 备份目录批量收紧）
    - `grep -q "icacls" electron/database/acl.ts` 命中（D-11 Windows 分支）
    - `grep -q "/inheritance:r" electron/database/acl.ts` 命中（D-11 剥离继承）
    - `grep -q "/grant:r" electron/database/acl.ts` 命中（D-11 replace 显式项）
    - `grep -q "0o600" electron/database/acl.ts` 命中（D-11 Unix chmod）
    - `grep -q "execFileSync" electron/database/acl.ts` 命中（同步执行避免竞态）
    - `grep -q "os.userInfo().username" electron/database/acl.ts` 命中（D-11 当前用户）
    - `grep -q "import { createSystemLog } from '../services/systemLog'" electron/database/acl.ts` 命中（D-13 失败日志）
    - `grep -q "type: 'acl'" electron/database/acl.ts` 命中（日志类型）
    - `grep -c "createSystemLog" electron/database/acl.ts` >= 2（restrictFilePermissions + restrictDirPermissions 各一处）
    - `npx tsc -p tsconfig.web.json --noEmit` 退出码 0
    - `npm run build:electron-main` 退出码 0（acl.ts 进 main 进程，esbuild 必须能 bundle child_process/fs/os）
  </acceptance_criteria>

  <done>acl.ts 导出 restrictFilePermissions（Windows icacls /inheritance:r /grant:r 当前用户:(F) + Unix chmod 0o600，非致命 D-13，文件不存在静默跳过）+ restrictDirPermissions（备份目录批量）；tsc + esbuild 双绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB 文件 / 备份文件 ↔ 同机其他用户/进程 | 敏感数据副本（加密凭证、AI 日志、设备清单）以文件形式落盘 userData/；默认 ACL 可能允许同机其他用户读取 |
| ACL helper ↔ 外部 shell（icacls） | Windows 分支调用系统命令，参数构造不当可能命令注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Information Disclosure | DB 文件/备份/WAL sidecar 默认权限宽松，同机其他用户可读敏感数据副本 | mitigate | D-10/D-11/D-12：restrictFilePermissions 收紧为仅当前用户（Win icacls /inheritance:r /grant:r user:(F)，Unix 0o600）；覆盖 topology.db + -wal + -shm + backups/ 全部文件；每次启动幂等重收紧 + 每备份创建即收紧 |
| T-2-07 | Elevation of Privilege | Windows icacls 调用参数注入（恶意路径/用户名构造 shell 元字符） | mitigate | execFileSync + shell:false（参数数组传递，不经 shell 解析）；filePath/label 仅来自受控代码（app.getPath + 固定文件名），非用户输入；currentUser 来自 env/os 内建 API 非外部输入 |
| T-2-08 | Denial of Service | ACL 收紧失败（非管理员/权限不足）导致应用崩溃，用户无法使用 | mitigate | D-13：失败 try/catch 写 system log(type='acl', status='warning') 后继续，不抛异常；数据已 AES+safeStorage 加密，ACL 是纵深防御第二层，缺失不阻断功能 |
| T-2-09 | Tampering | 同机其他用户篡改 DB 文件 | mitigate | restrictFilePermissions Win 剥离继承 + 仅当前用户 (F)（无其他用户写位）；Unix 0o600（仅 owner rw）；每次启动重收紧修正历史宽松权限（D-12a 幂等） |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` 退出码 0
- `npm run build:electron-main` 退出码 0（child_process/fs/os 在 electron main bundle 内）
- `codegraph_search restrictFilePermissions` 命中 acl.ts
- 手动：Windows 上对临时文件调用 restrictFilePermissions 后 `icacls "<file>"` 输出仅当前用户，无继承项；非管理员账户失败时 system log 出现 type='acl' warning 且应用不崩溃
</verification>

<success_criteria>
- ARCH-02 类型契约（BackupConfig）+ ACL 工具（restrictFilePermissions/restrictDirPermissions）就位，供 Plan 03 集成
- D-02 双桶 retention 字段、D-11 跨平台命令、D-13 非致命失败全部落地
- Phase 1 构建基线不破坏
</success_criteria>

<output>
After completion, create `.planning/phases/02-architecture-db-migration/02-02-SUMMARY.md`
</output>
