---
phase: 05-frontend-refactor-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/electron.d.ts
  - src/types/ai.ts
  - src/components/ip-management/ArpTab.tsx
  - src/components/ip-management/AnomalyTab.tsx
  - src/components/ip-management/NetworkTab.tsx
  - src/components/ip-management/OuiTab.tsx
  - src/components/pages/SettingsPage.tsx
  - src/components/pages/DevicesPage.tsx
autonomous: true
requirements: [FE-02]

must_haves:
  truths:
    - "src/types/electron.d.ts 全部 Promise<any> 与裸 any 被替换为复用 src/types 的具体 DTO（device/topology/network/anomaly/oui/ai）"
    - "ArpTab/AnomalyTab/NetworkTab/OuiTab 4 文件的 api:any props 与 useState<any*> 收敛为 ElectronAPI 切片 + src/types DTO"
    - "SettingsPage 删除 (window as any).api 绕过，schedulerConfig/schedulerStatus 收为 ScheduleConfig/SchedulerStatus 类型（electron.d.ts 新增 scheduler 通道，签名对齐 preload.ts 真实暴露的 getConfig/updateConfig/runNow/getStatus）"
    - "DevicesPage 4 处 catch (e:any) 收为 catch (e:unknown) + 窄化"
    - "tsc -p tsconfig.web.json exit 0（strict + noUnusedLocals 全绿）"
  artifacts:
    - path: "src/types/electron.d.ts"
      provides: "ElectronAPI 强类型契约（消除 23 处 any，含新增 scheduler 通道，签名匹配 preload.ts/schedulerIpc.ts 真实 handler）"
      contains: "scheduler:"
    - path: "src/types/ai.ts"
      provides: "AI 会话/消息 Row DTO（ChatMessage role 收为联合类型、ChatSession、DiscoverResult）"
      contains: "interface ChatMessage"
    - path: "src/components/ip-management/ArpTab.tsx"
      provides: "ARP Tab 强类型 props/state"
    - path: "src/components/pages/SettingsPage.tsx"
      provides: "Settings 页强类型（无 (window as any).api，scheduler 走 window.api.scheduler.*）"
  key_links:
    - from: "src/components/ip-management/ArpTab.tsx"
      to: "src/types/electron.d.ts"
      via: "import type { ElectronAPI } / ARPEntry"
      pattern: "ElectronAPI|ARPEntry"
    - from: "src/components/pages/SettingsPage.tsx"
      to: "src/types/oui.ts"
      via: "import type { ScheduleConfig, SchedulerStatus, UpdateScheduleInput }"
      pattern: "ScheduleConfig|SchedulerStatus"
    - from: "src/types/electron.d.ts"
      to: "src/types/ai.ts"
      via: "import type ChatMessage/ChatSession/DiscoverResult"
      pattern: "from './ai'"
---

<objective>
FE-02 类型契约 foundation：以 `src/types/electron.d.ts` 建模为锚，复用/补全 `src/types` DTO，收敛 4 个 IP Tab + SettingsPage + DevicesPage 的 `any`。

Purpose: electron.d.ts 是 `window.api` 的唯一类型契约源头（D-5-2/D-5-3）——先收它（一处修复多处受益），下游所有 call site 的 props/state/catch 才能引用真实 DTO。本 plan 是 FE-01（AIPage）、FE-04（KB 缓存）的类型前置依赖，故置于 Wave 1。

Output: electron.d.ts 全建模 + 新建 src/types/ai.ts 补缺 DTO + 4 IP Tab + SettingsPage + DevicesPage 清理。**不触碰** AIPage.tsx（FE-01 独占）、KnowledgeBasePage.tsx（FE-02 KB 部分 + FE-04 同文件，归 05-04，**KB DTO 在 05-04 就近定义**）、后端 services（milestone 外）、mock-api.ts（dev-only）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-refactor-types/05-CONTEXT.md
@.planning/phases/05-frontend-refactor-types/05-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

@src/types/electron.d.ts
@src/types/device.ts
@src/types/topology.ts
@src/types/network.ts
@src/types/arp.ts
@src/types/anomaly.ts
@src/types/oui.ts
@src/types/pagination.ts
@src/components/ip-management/ArpTab.tsx
@src/components/ip-management/AnomalyTab.tsx
@src/components/ip-management/NetworkTab.tsx
@src/components/ip-management/OuiTab.tsx
@src/components/pages/SettingsPage.tsx
@src/components/pages/DevicesPage.tsx
@electron/preload.ts
@electron/ipc/schedulerIpc.ts
</context>

<interfaces>
<!-- 既有 DTO 复用清单（PATTERNS §12，planner 须引用这些导出名，不重复发明） -->

src/types/device.ts:
  - ConnectionType / DeviceType / Device / CreateDeviceDTO / UpdateDeviceDTO
src/types/topology.ts:
  - TopologyNodeData / TopologyNode / TopologyEdgeData / TopologyEdge / Topology
src/types/network.ts:
  - NetworkSegment / IPUsage / IPDetail / CreateNetworkInput / UpdateNetworkInput
src/types/arp.ts:
  - ARPEntry / ARPCollectionResult / ARPScanProgress
src/types/anomaly.ts:
  - ChangeType / IPMACBinding / IPMACChange / ChangeStats / ExcludedIP / CreateExcludedIPInput
src/types/oui.ts:
  - OUIEntry / CreateOUIInput / UpdateOUIInput / OUIStats / ScheduleConfig / SchedulerStatus / UpdateScheduleInput
src/types/pagination.ts:
  - PaginatedResult<T>（Phase 4 建，list 通道泛型参数复用）

<!-- preload.ts 真实暴露签名（已核实，electron.d.ts 须对齐这些，不臆造） -->
preload.ts scheduler 桥（line 104-109，逐字）:
  - getConfig: () => ipcRenderer.invoke('scheduler:getConfig')
  - updateConfig: (data: unknown) => ipcRenderer.invoke('scheduler:updateConfig', data)
  - runNow: () => ipcRenderer.invoke('scheduler:runNow')
  - getStatus: () => ipcRenderer.invoke('scheduler:getStatus')
  **注意：无 saveConfig / start / stop（plan-checker issue 2 修正）**
schedulerIpc.ts handler（line 6-19，逐字，对齐 invoke channel）:
  - scheduler:getConfig / scheduler:updateConfig(data) / scheduler:runNow / scheduler:getStatus（共 4 个，均经 secure() 包装）

<!-- SettingsPage scheduler 真实调用面（已核实 line 41/72/84/93，决定 DTO 字段） -->
  - api.scheduler.getConfig() → ScheduleConfig（oui.ts:27，字段 enabled/intervalMinutes/lastRun/nextRun）
  - api.scheduler.getStatus() → SchedulerStatus（oui.ts:35，字段 isRunning/isTaskRunning/config）
  - api.scheduler.updateConfig({ enabled?, intervalMinutes? }) → ScheduleConfig（返回更新后 config，line 72/84 setSchedulerConfig(config)）
  - api.scheduler.runNow() → { success: boolean; message: string; stats?: ... }（line 93-95 result.success/result.message）

<!-- electron.d.ts 当前 gap（待补，已核实文件 173 行）：
  - 3 个 PaginatedResult<any> 泛型参数须收为 IPDetail / IPMACChange / OUIEntry
  - ChatMessage.role: string → 收为 'user' | 'assistant'
  - device/topology/network/anomaly/oui 非 list 通道全 Promise<any> 待建模
  - 缺 scheduler 通道（preload.ts:104-109 已暴露 4 个，electron.d.ts 漏标；SettingsPage 用 (window as any).api 绕过）
  - kb.* 通道归 05-04 建模（KB DTO 下沉 05-04 就近定义，本 plan 不碰 kb）
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: electron.d.ts 全建模 + 新建 src/types/ai.ts 补缺 DTO + scheduler 通道（对齐 preload 真实签名）</name>
  <files>src/types/electron.d.ts, src/types/ai.ts</files>
  <read_first>
    - src/types/electron.d.ts（当前 173 行，23 处 any，逐通道建模对象）
    - src/types/device.ts / topology.ts / network.ts / arp.ts / anomaly.ts / oui.ts / pagination.ts（复用 DTO 源，确认导出符号名）
    - **electron/preload.ts（已核实，line 104-109 scheduler 真实暴露 4 通道：getConfig/updateConfig/runNow/getStatus；line 38-58 ai 通道真实参数；line 110-123 kb 通道——本 plan 不建模 kb，归 05-04）**
    - **electron/ipc/schedulerIpc.ts（已核实，4 个 handler 全经 secure() 包装；updateConfig 入参校验 interval_minutes/enabled，但 renderer 侧传驼峰 intervalMinutes——本 plan 只建模 renderer 所见驼峰字段 UpdateScheduleInput，主进程校验不一致属既有问题不在 FE-02 scope）**
    - src/components/pages/SettingsPage.tsx（已核实 scheduler 调用面 line 41/72/84/93：getConfig/getStatus/updateConfig/runNow；line 15 `(window as any).api` 绕过点）
    - src/components/pages/AIPage.tsx（确认 ai.* 通道调用面：line 52 device.list、line 73 listSessions、line 87 createSession、line 96 getSessionMessages、line 118 chat、line 167 confirmCommand——为 ai.ts DTO 字段取证）
    - electron/services/ai.ts（DB row 字段取证，仅读不改）
    - 05-PATTERNS.md §11（electron.d.ts 通道清单逐字）
  </read_first>
  <action>
    **原则（D-5-3）：复用 src/types 既有 DTO，缺 DTO 就近补 interface XxxRow 到 src/types/<domain>.ts，不在 electron.d.ts 内联，不重复发明类型。**

    **(1) 新建 src/types/ai.ts**（补 AI 行 DTO + 收 ChatMessage.role，字段从 electron/services/ai.ts DB prepare 语句取证）：
    ```typescript
    // 复用 electron.d.ts 既有的 ChatMessage/ChatSession，但收 role
    export interface ChatMessage {
      id: string
      role: 'user' | 'assistant'
      content: string
      deviceId: string | null
      createdAt: string
    }
    export interface ChatSession {
      id: string
      title: string
      deviceId: string | null
      createdAt: string
    }
    export interface DiscoverResult {
      nodes: Array<Record<string, unknown>>
      edges: Array<Record<string, unknown>>
      failedDevices: Array<{ deviceId: string; deviceName: string; error: string }>
    }
    ```
    > electron.d.ts 既有 ChatMessage（line 37-43）/ChatSession（line 45-50）须迁移到此文件并收 role 联合类型；electron.d.ts 改为 `import type { ChatMessage, ChatSession, DiscoverResult } from './ai'`。AIPage 的 handleSend JSON.parse(reply) 分支 confirm_required/kb_answer 等运行时解析，不在此静态建模（属运行时窄化）。executor 须以 electron/services/ai.ts 的 prepare/INSERT 语句与 AIPage.tsx 消费面（line 96 getSessionMessages、line 118 chat 入参 messages）为准确认字段——若 DB row 含额外字段（如 sessionId），补到 ChatMessage。

    **(2) 重写 electron.d.ts**（替换 23 处 any，逐通道复用 DTO；**kb.* 通道不在本 task 建模，归 05-04**——本 plan 完成 electron.d.ts 时 kb.* 仍可保留 `Promise<any>` 占位或临时 import 05-04 将建的 kb.ts，因 05-04 在 Wave 2、本 plan 在 Wave 1，**为避免跨 wave 依赖，本 plan 保持 kb.* 现有 `Promise<any>` 不动**，05-04 Task 1 接力收 kb.* 通道类型）：
    - 顶部 import 整合：`import type { Device, CreateDeviceDTO, UpdateDeviceDTO } from './device'`、`Topology`、`NetworkSegment/IPDetail/IPUsage/CreateNetworkInput/UpdateNetworkInput`、`ARPEntry`（保留）、`IPMACChange/ChangeStats/ExcludedIP/IPMACBinding/CreateExcludedIPInput`、`OUIEntry/OUIStats/CreateOUIInput/UpdateOUIInput/CreateOUIInput/ScheduleConfig/SchedulerStatus/UpdateScheduleInput`、`ChatMessage/ChatSession/DiscoverResult`、`PaginatedResult`。
    - 删除文件内联的 ChatMessage（line 37-43）/ChatSession（line 45-50），改 import 自 ./ai。AIConfig/AIExecLog/AISystemLog 保留在本文件（与 AI 主进程紧耦合，无独立 domain）。
    - **device**: `list: () => Promise<Device[]>`、`create: (data: CreateDeviceDTO) => Promise<Device>`、`update: (id: string, data: UpdateDeviceDTO) => Promise<Device>`、`getById: (id: string) => Promise<Device | null>`。（preload.ts:14 还有 `delete`，保留 `Promise<void>`。）
    - **topology**: `list: () => Promise<Topology[]>`、`getById: (id: string) => Promise<Topology | null>`、`create: (data: Partial<Topology>) => Promise<Topology>`、`update: (id: string, data: Partial<Pick<Topology,'nodes'|'edges'>>) => Promise<void>`、`exportJson: (id: string) => Promise<string>`、`importJson: (json: string) => Promise<Topology>`。（preload.ts:22-24 真实暴露，对齐。）
    - **ai**: `chat: (messages: Array<{ role: 'user'|'assistant'; content: string }>, deviceIds?: string[], sessionId?: string) => Promise<string>`、`discoverTopology: (deviceIds: string[]) => Promise<DiscoverResult>`（收 nodes/edges any[]）、`getLogs: (limit?: number) => Promise<AIExecLog[]>`、`getChatHistory: () => Promise<ChatMessage[]>`、`saveMessage: (role: 'user'|'assistant', content: string, deviceId?: string|null, sessionId?: string|null) => Promise<void>`、`createSession/listSessions/getSessionMessages/deleteSession/updateSessionTitle` 用 ChatSession/ChatMessage（对齐 preload.ts:52-56 真实通道集）。
    - **kb**: **保持现状 `Promise<any>` 不动**（归 05-04 Task 1 建模）。本 task 不修改 kb.* 通道（line 108-121）。
    - **network**: `getAll: () => Promise<NetworkSegment[]>`、`getById: (id: number) => Promise<NetworkSegment | null>`、`create: (data: CreateNetworkInput) => Promise<NetworkSegment>`、`update: (data: UpdateNetworkInput) => Promise<NetworkSegment>`、`autoDiscover: () => Promise<NetworkSegment[]>`、`getIPUsage: (networkId: number) => Promise<IPUsage>`、`getIPDetails: (...) => Promise<PaginatedResult<IPDetail>>`（泛型 any → IPDetail，Phase 4 已建）。
    - **anomaly**: `getChanges: (...) => Promise<PaginatedResult<IPMACChange>>`（泛型 any → IPMACChange，Phase 4 已建）、`getStats: () => Promise<ChangeStats>`、`getBindingHistory: (ip: string) => Promise<IPMACBinding[]>`、`getExcludedIPs: () => Promise<ExcludedIP[]>`、`addExcludedIP: (data: CreateExcludedIPInput) => Promise<ExcludedIP>`。
    - **oui**: `getAll: (...) => Promise<PaginatedResult<OUIEntry>>`（泛型 any → OUIEntry，Phase 4 已建）、`search: (keyword: string) => Promise<OUIEntry[]>`、`getById: (id: number) => Promise<OUIEntry | null>`、`add: (data: CreateOUIInput) => Promise<OUIEntry>`、`addBatch: (entries: CreateOUIInput[]) => Promise<number>`、`update: (data: UpdateOUIInput) => Promise<OUIEntry>`、`getAllVendors: () => Promise<Array<{ ouiPrefix: string; vendorName: string }>>`、`getStats: () => Promise<OUIStats>`。（注意 oui.ts 用驼峰 `ouiPrefix/vendorName`，对齐 preload.ts:96 返回。）
    - **connection / auth**: 保留现有签名（已无 any，line 55-60/77-86 不动）。
    - **新增 scheduler 通道（issue 2 修正：对齐 preload.ts:104-109 + schedulerIpc.ts 真实签名，不臆造 saveConfig/start/stop）**：
      ```typescript
      // 复用 oui.ts 的 ScheduleConfig/SchedulerStatus/UpdateScheduleInput
      scheduler: {
        getConfig: () => Promise<ScheduleConfig>
        updateConfig: (data: UpdateScheduleInput) => Promise<ScheduleConfig>
        runNow: () => Promise<{ success: boolean; message: string; stats?: { devices: number; entries: number; changes: number } }>
        getStatus: () => Promise<SchedulerStatus>
      }
      ```
      > **签名依据（已核实）：** preload.ts:104-109 暴露 4 通道（getConfig/updateConfig/runNow/getStatus）；schedulerIpc.ts:6-19 同 4 handler；SettingsPage.tsx:41/72/84/93 调用面匹配——`updateConfig({enabled, intervalMinutes})` 返回 config（line 72 `setSchedulerConfig(config)`），`runNow()` 返回 `{success, message}`（line 93-95）。**无 saveConfig/start/stop**（plan-checker issue 2 指出的臆造已删除）。`runNow` 的 stats 字段对齐 schedulerService.ts:27 `{devices, entries, changes}`，可选。UpdateScheduleInput 已在 oui.ts:41 定义（`enabled?/intervalMinutes?`），直接复用。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | grep -v "^[[:space:]]*$" | head -30 && echo "=== electron.d.ts 非 kb 通道 any 残留（kb.* 保留 any 归 05-04） ===" && grep -n ": any\|Promise<any>" src/types/electron.d.ts | grep -v "kb:" | grep -v "^.*//" | head</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^//' src/types/electron.d.ts | grep -c ": any\|Promise<any>"` 输出仅含 kb.* 通道行（Phase 1 占位，05-04 接力清零）；非 kb 通道 any 残留 0
    - `grep "scheduler:" src/types/electron.d.ts` 命中（新增 scheduler 通道）
    - `grep "updateConfig\|runNow" src/types/electron.d.ts` 命中（issue 2 真实签名）
    - `grep "saveConfig\|start:\|stop:" src/types/electron.d.ts | grep -i scheduler` 无命中（臆造签名已删，issue 2）
    - `ls src/types/ai.ts` 文件存在；`ls src/types/kb.ts` **不存在**（KB DTO 下沉 05-04，issue 1 方案 B）
    - `grep "interface ChatMessage" src/types/ai.ts` 命中
    - `grep "role: 'user' | 'assistant'" src/types/ai.ts` 命中（ChatMessage.role 收敛）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
  </acceptance_criteria>
  <done>electron.d.ts 非 kb 通道 any 全部替换为 src/types DTO；scheduler 通道按 preload/schedulerIpc 真实签名补全（getConfig/updateConfig/runNow/getStatus）；src/types/ai.ts 建立；kb.* 通道保留待 05-04；tsc web 严格模式全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 4 个 IP Tab + SettingsPage + DevicesPage 清 any（after 绝对值验收，issue 4）</name>
  <files>src/components/ip-management/ArpTab.tsx, src/components/ip-management/AnomalyTab.tsx, src/components/ip-management/NetworkTab.tsx, src/components/ip-management/OuiTab.tsx, src/components/pages/SettingsPage.tsx, src/components/pages/DevicesPage.tsx</files>
  <read_first>
    - Task 1 完成后的 src/types/electron.d.ts（已建模，是本 task 的类型源；注意 kb.* 仍 any，但本 task 6 文件不消费 kb.*）
    - src/components/ip-management/ArpTab.tsx（**after 目标 0**；点位 PATTERNS §13 line 5/9/10/11/16/17/28/50/59/81/141，Table render rowKey）
    - src/components/ip-management/{AnomalyTab,NetworkTab,OuiTab}.tsx（**after 目标 0**，同模式）
    - src/components/pages/SettingsPage.tsx（**after 目标 0**；已核实 line 15 `(window as any).api`、line 25/26 useState any、line 35/65/76/86/96 catch any；line 41/72/84/93 scheduler 调用面对齐 Task 1 建模的 4 通道）
    - src/components/pages/DevicesPage.tsx（**after 目标 0**；已核实 4 处全在 catch：line 22/33/44/64）
    - 05-PATTERNS.md §13（4 Tab 点位）+ §14（SettingsPage/DevicesPage 点位）
    - src/types/oui.ts（ScheduleConfig/SchedulerStatus/UpdateScheduleInput，已确认字段 enabled/intervalMinutes/lastRun/nextRun + isRunning/isTaskRunning/config）+ src/types/network.ts/anomaly.ts/oui.ts/arp.ts（DTO 复用）
  </read_first>
  <action>
    **(A) 4 个 IP Tab（统一模式，D-5-3 复用 DTO）：**
    - **ArpTab.tsx**：
      - `interface ArpTabProps { api: any }` → `interface ArpTabProps { api: ElectronAPI }`（import type { ElectronAPI } from '@/types/electron'，或更细切片用 `Pick<ElectronAPI, 'device'|'arp'|'anomaly'>`——Claude's Discretion，建议 ElectronAPI 全量保持简单）。
      - `useState<any[]>(results)` → `useState<ARPEntry[]>([])`（import ARPEntry from '@/types/arp'；若实际结果行含额外字段如 vendor，executor 以 IPC 真实返回为准微调为 `Array<ARPEntry & { vendor?: string }>`）。
      - `useState<any>(stats)` → `useState<ARPScanProgress | null>(null)` 或具体统计 DTO。
      - `useState<any[]>(devices)` → `useState<Device[]>([])`。
      - `api.device.list().then((list: any[]) => { setDevices(list.filter((d: any) => ...)) })` → 去掉 `(list: any[])` 与 `(d: any)` 标注（Device[] 已强类型），filter 内 `d.connectionType` 等访问由类型守卫。
      - `catch (e: any) { message.error('...' + e.message) }` → `catch (e: unknown) { message.error('...: ' + (e instanceof Error ? e.message : String(e))) }`（全仓库 catch 统一此模式，PATTERNS §Shared Patterns 错误处理）。
      - Table 列 `render: (_: any, record: any)` → `render: (_: unknown, record: ARPEntry)`（Ant Design Table render 签名 `_` 用 unknown 兼容，record 用行 DTO）。
      - `rowKey={(row: any) => ...}` → `rowKey={(row: ARPEntry) => ...}` 或去标注。
    - **AnomalyTab.tsx**：`api: any` → ElectronAPI；`useState<any[]>(changes)` → `useState<IPMACChange[]>`；`catch (e:any)` → `unknown`+窄化。**Phase 4 已适配点 line 33 `setChanges(c.rows)` 保留**，仅收 rows 元素类型。
    - **NetworkTab.tsx**：`api: any` → ElectronAPI；`useState<any[]>(segments/details)` → `NetworkSegment[]` / `IPDetail[]`；catch 收 unknown。**Phase 4 line 34/43 `details.rows` 保留**。
    - **OuiTab.tsx**：`api: any` → ElectronAPI；`useState<any[]>(entries)` → `OUIEntry[]`；catch 收 unknown。**Phase 4 line 23 `setEntries(e.rows)` 保留**。

    **(B) SettingsPage.tsx（issue 2 落地：scheduler 走 window.api.scheduler.*，对齐 Task 1 的 4 通道签名）：**
    - **删除 line 15 `const api = (window as any).api`**，全文 `api.scheduler.*` 改 `window.api.scheduler.*`（Task 1 已在 electron.d.ts 建 scheduler 通道，4 方法签名匹配 preload.ts:104-109）。注意 `api` 仅用于 scheduler（line 41/72/84/93），其余已用 `window.api.*`（line 33/61/63），删除后无悬挂引用。
    - `useState<any>(schedulerConfig)` → `useState<ScheduleConfig>({} as ScheduleConfig)`（import from '@/types/oui'；{} as ScheduleConfig 保持既有渲染不崩，因 line 157 `schedulerConfig.enabled`/line 159 `schedulerConfig.intervalMinutes`/line 163 `schedulerConfig.lastRun` 均访问字段）。
    - `useState<any>(schedulerStatus)` → `useState<SchedulerStatus>({} as SchedulerStatus)`（line 165 `schedulerStatus.isTaskRunning`）。
    - line 72 `api.scheduler.updateConfig({ enabled, intervalMinutes: schedulerConfig.intervalMinutes })` → `window.api.scheduler.updateConfig({ enabled, intervalMinutes: schedulerConfig.intervalMinutes })`（Task 1 建模 updateConfig 入参 UpdateScheduleInput，匹配；返回 ScheduleConfig 赋值 setSchedulerConfig 匹配）。
    - line 84 同理 updateConfig({ intervalMinutes: value })。
    - line 93 `api.scheduler.runNow()` → `window.api.scheduler.runNow()`（返回 {success, message}，line 94-95 result.success/result.message 匹配）。
    - line 41 `Promise.all([api.scheduler.getConfig(), api.scheduler.getStatus()])` → `window.api.scheduler.*`。
    - 5 处 `catch (e: any) { ... }`（line 35/65/76/86/96）→ `catch (e: unknown) { message.error(... + (e instanceof Error ? e.message : String(e))) }`。
    - line 44 `catch { /* ignore */ }` 已是 unknown 无标注，保留。

    **(C) DevicesPage.tsx（4 处全在 catch）：**
    - line 22/33/44/64 四处 `catch (e: any) { message.error(e.message) }` → `catch (e: unknown) { message.error(e instanceof Error ? e.message : String(e)) }`。已用 Device/CreateDeviceDTO 的部分不动。

    **统一原则（PATTERNS §Shared Patterns 错误处理）：** 所有 catch 子句 strict 模式默认 unknown，删 `: any` 即编译通过；`e.message` 须窄化为 `(e instanceof Error ? e.message : String(e))`，全仓库统一此模式。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "=== 6 文件 any 残留（after 绝对值，目标全 0） ===" && for f in src/components/ip-management/ArpTab.tsx src/components/ip-management/AnomalyTab.tsx src/components/ip-management/NetworkTab.tsx src/components/ip-management/OuiTab.tsx src/components/pages/SettingsPage.tsx src/components/pages/DevicesPage.tsx; do echo "$(grep -v '^[[:space:]]*//' "$f" | grep -cE ':\s*any(\[\])?\b|as any|<any>')  $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - **after 绝对值验收（issue 4）：** 以下 6 文件 grep 输出**均为 0**（不依赖 before 计数）：
      - `grep -v '^[[:space:]]*//' src/components/ip-management/ArpTab.tsx | grep -cE ':\s*any(\[\])?\b|as any|<any>'` = 0
      - 同命令对 AnomalyTab/NetworkTab/OuiTab/SettingsPage/DevicesPage 输出均 = 0
    - `grep "(window as any)" src/components/pages/SettingsPage.tsx` 无命中（line 15 绕过已删，issue 2）
    - `grep "window.api.scheduler" src/components/pages/SettingsPage.tsx` 命中（scheduler 走类型化通道，issue 2）
    - `grep "catch (e: unknown)" src/components/pages/SettingsPage.tsx src/components/pages/DevicesPage.tsx` 命中（catch 收敛）
    - `npx tsc -p tsconfig.web.json --noEmit` exit 0
  </acceptance_criteria>
  <done>6 文件 any after 全清零（不依赖 before 计数，issue 4）；含 SettingsPage scheduler 对接 Task 1 真实签名（getConfig/updateConfig/runNow/getStatus，issue 2）、DevicesPage catch 收敛；tsc web 严格模式 + noUnusedLocals 全绿；Phase 4 信封 .rows 读路径保留无回退。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC（既有） | 本 plan 不新增 IPC 通道、不改 IPC 签名（scheduler 通道 preload.ts:104-109 已暴露、schedulerIpc.ts handler 已存在并经 secure() 包装；本 plan 仅补 electron.d.ts 类型标注 + SettingsPage 调用面去 (window as any).api 绕过，非新攻击面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | electron.d.ts 类型契约 | accept | 类型层建模不改运行时行为；IPC handler 仍由 authGuard `secure()` 包装鉴权（CONVENTIONS §2 红线），类型收窄不削弱运行时校验。 |
| T-05-01-02 | Information Disclosure | catch 块 e.message 渲染 | mitigate | `(e instanceof Error ? e.message : String(e))` 仅显示错误 message；IPC 层 `sanitizeMessage` 已在主进程脱敏 SQL/路径（CONVENTIONS §2），渲染层只收截断 message。本 plan 不改变此链路。 |
| T-05-01-03 | Spoofing | scheduler 通道（新增类型） | accept | scheduler handler 在 schedulerIpc.ts:6-19 已存在并经 `secure()` 包装；本 plan 仅补类型标注对齐 preload.ts:104-109 真实暴露，不新增特权 handler、不改 IPC 签名。 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（strict + noUnusedLocals + noUnusedParameters 全绿，FE-01/02 验收红线）
- `npx vitest run` exit 0（既有 25 测试不回归）
- `npx esbuild electron/main.ts ... --bundle`（主进程打包不回归；本 plan 不改 preload.ts/handler）
- electron.d.ts grep `: any|Promise<any>` 残留仅限 kb.* 通道行（05-04 接力清零）
- 6 文件 any after 全清零（grep 绝对值 = 0，不依赖 before，D-5-2 grep 限定 src/ + electron.d.ts，不含后端）
</verification>

<success_criteria>
- electron.d.ts 非 kb 通道全建模（scheduler 通道对齐 preload.ts/schedulerIpc.ts 真实签名 getConfig/updateConfig/runNow/getStatus）
- src/types/ai.ts 建立（ChatMessage role 收联合类型、ChatSession、DiscoverResult）
- 4 IP Tab + SettingsPage + DevicesPage any after 全清零（Phase 4 信封读 .rows 无回退）
- SettingsPage 删除 (window as any).api 绕过，scheduler 走 window.api.scheduler.*
- tsc web 严格模式 + noUnusedLocals 全绿
- FE-01（AIPage）的类型前置依赖就绪（ChatMessage 联合类型可被 Wave 2 引用）；FE-04（KB）的 kb.* 通道建模与 KB DTO 归 05-04 同 plan 反推
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-refactor-types/05-01-SUMMARY.md`
</output>
