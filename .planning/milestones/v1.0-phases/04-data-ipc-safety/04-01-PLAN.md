---
phase: 04-data-ipc-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/pagination.ts
  - electron/utils/pagination.ts
  - electron/ipc/networkIpc.ts
  - electron/ipc/ouiIpc.ts
  - electron/ipc/anomalyIpc.ts
  - electron/services/networkSegmentService.ts
  - electron/services/ouiService.ts
  - electron/services/anomalyService.ts
  - electron/preload.ts
autonomous: true
requirements: [DATA-01]
user_setup: []

must_haves:
  truths:
    - "network:getIPDetails 默认返回 ≤2000 行且经信封包裹（rows/total/truncated），硬上限 50000"
    - "oui:getAll 默认返回 ≤5000 行且经信封包裹，硬上限 50000"
    - "anomaly:getChanges 保留默认 100、硬上限 10000，新增 offset 参数，返回信封"
    - "limit/offset 在 IPC 网关层校验（整数+钳制），service 层只接收安全值"
    - "preload.ts 三个 list 通道签名暴露 limit/offset 参数"
    - "getIPDetails 保留 PERF-01 vendorMap 读路径（不退化为逐行查库）"
  artifacts:
    - path: "src/types/pagination.ts"
      provides: "PaginatedResult<T> 信封类型（renderer 共用）"
      contains: "PaginatedResult"
    - path: "electron/utils/pagination.ts"
      provides: "共享 validateLimit/validateOffset helper"
      contains: "validateLimit"
    - path: "electron/services/networkSegmentService.ts"
      provides: "getIPDetails 返回 PaginatedResult（JS 过滤后 slice）"
    - path: "electron/services/ouiService.ts"
      provides: "getAll(limit, offset) SQL LIMIT/OFFSET + 信封"
    - path: "electron/services/anomalyService.ts"
      provides: "getChanges(..., offset) + 信封"
  key_links:
    - from: "electron/ipc/networkIpc.ts"
      to: "NetworkSegmentService.getIPDetails"
      via: "validateLimit/validateOffset 网关校验后转发"
      pattern: "validateLimit.*validateOffset"
    - from: "electron/preload.ts"
      to: "ipcRenderer.invoke('network:getIPDetails'"
      via: "新增 limit/offset 透传"
      pattern: "getIPDetails.*limit.*offset"
---

<objective>
为 3 个 list 通道（network:getIPDetails / oui:getAll / anomaly:getChanges）落地 hybrid 分页契约：网关层校验 limit/offset → service 层应用默认 cap + 硬上限 + 截断信封 `{ rows, total, truncated }`。preload 同步暴露 limit/offset 签名。export 通道与本计划无文件重叠（归 04-02）。

Purpose: 实现 D-4-1~D-4-4 + D-4-6 — 让 list 通道 payload 有界，且运维不静默漏看 rogue 设备/IP（信封告知截断）。
Output: 1 个共享类型 + 1 个共享校验 helper + 3 个 IPC 文件 + 3 个 service 文件 + preload 签名。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-ipc-safety/04-CONTEXT.md

<interfaces>
<!-- 现有先例：anomalyIpc.ts:7-11 validateLimit — 复用模式 -->
```typescript
function validateLimit(limit: any): number {
  const n = Number(limit)
  if (!Number.isInteger(n) || n < 1 || n > 10000) return 100
  return n
}
```

<!-- 现有 service 签名（待改造） -->
```typescript
// networkSegmentService.ts:88 — JS 端 CIDR 过滤 + search + vendor map
static getIPDetails(networkId, searchIp?, searchMac?, sortBy='ip', sortOrder='asc'): any[]
// ouiService.ts:57 — 全量无 limit
static getAll(): any[]
// anomalyService.ts:147 — 已有 LIMIT ? 无 OFFSET ?
static getChanges(unacknowledgedOnly=false, limit=100): any[]
```

<!-- 信封类型（本 plan 新建，renderer+main 共用） -->
```typescript
// src/types/pagination.ts
export interface PaginatedResult<T> {
  rows: T[]
  total: number      // 过滤后、应用 limit 前的总数
  truncated: boolean // rows 是否被 cap 截断
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 共享类型 + 共享校验 helper（先建契约）</name>
  <files>src/types/pagination.ts, electron/utils/pagination.ts</files>
  <read_first>
    - electron/ipc/anomalyIpc.ts:7-11（validateLimit 先例模式）
    - src/types/network.ts（类型文件风格参照）
  </read_first>
  <behavior>
    - validateLimit(undefined, 2000, 50000) === 2000（默认值）
    - validateLimit(150, 2000, 50000) === 150（合法透传）
    - validateLimit(999999, 2000, 50000) === 50000（超硬上限钳制）
    - validateLimit(0, 2000, 50000) === 2000（<1 非法回落默认）
    - validateLimit(1.5, 2000, 50000) === 2000（非整数回落默认）
    - validateLimit('abc', 2000, 50000) === 2000（非数字回落默认）
    - validateOffset(undefined) === 0（默认）
    - validateOffset(50) === 50（合法透传）
    - validateOffset(-1) === 0（负数回落 0）
    - validateOffset(1.5) === 0（非整数回落 0）
  </behavior>
  <action>
    1. 创建 `src/types/pagination.ts`：导出 `export interface PaginatedResult<T> { rows: T[]; total: number; truncated: boolean }`。字段口径：`total` = 应用 search/filter 后、应用 limit 前的总数；`truncated` = `rows.length < total`（被 cap 截断）。
    2. 创建 `electron/utils/pagination.ts`：导出两个纯函数：
       ```typescript
       export function validateLimit(limit: any, defaultValue: number, maxCeiling: number): number {
         const n = Number(limit)
         if (!Number.isInteger(n) || n < 1 || n > maxCeiling) return defaultValue
         return n
       }
       export function validateOffset(offset: any): number {
         const n = Number(offset)
         if (!Number.isInteger(n) || n < 0) return 0
         return n
       }
       ```
       注意：硬上限钳制策略 = 落回默认值（与 anomalyIpc 先例一致：超界 → 默认，非钳到 ceiling）。理由：与既有 validateLimit 行为统一，且默认值本身已是安全有界值。
    3. 严格 TS：所有参数/返回显式标注类型，无 any 残留（pagination.ts 全 typed；pagination.ts.ts 无 unused）。
    不改 anomalyIpc.ts 内的本地 validateLimit（Task 3 会改为调用共享 helper）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>两个文件存在；PaginatedResult 导出；validateLimit/validateOffset 满足上述全部 behavior 用例；tsc 无新增报错。</done>
</task>

<task type="auto">
  <name>Task 2: getIPDetails 改信封（JS 过滤后 slice，D-4-6）+ oui:getAll SQL 分页</name>
  <files>electron/services/networkSegmentService.ts, electron/services/ouiService.ts, electron/ipc/networkIpc.ts, electron/ipc/ouiIpc.ts</files>
  <read_first>
    - electron/services/networkSegmentService.ts:88-113（getIPDetails 全量，含 PERF-01 getVendor 调用 line 106）
    - electron/services/ouiService.ts:57-60（getAll 全量无 limit）
    - electron/ipc/networkIpc.ts:38-39（handler 现签名）
    - electron/ipc/ouiIpc.ts:8（handler 无参数）
    - electron/utils/pagination.ts（Task 1 产物）
    - src/types/pagination.ts（Task 1 产物）
  </read_first>
  <action>
    **networkSegmentService.ts getIPDetails（D-4-6：JS 端过滤，分页在过滤后数组）**：
    1. 签名改为 `static getIPDetails(networkId, searchIp?, searchMac?, sortBy='ip', sortOrder='asc', limit=2000, offset=0): PaginatedResult<any>`。默认 limit=2000、offset=0（向后兼容：旧调用不传 limit/offset → 默认有界）。
    2. 保持现有 SQL 全量读 + JS `ipInCIDR` 过滤 + searchIp/searchMac 过滤逻辑不变。
    3. **关键：保留 PERF-01 vendor 读路径** — 过滤后数组仍走 `rows.map(entry => { const vendor = entry.mac ? OUIService.getVendor(entry.mac) : 'Unknown'; ... })`，**不得改为逐行查库**。先 map 出完整对象数组，再分页。
    4. 分页在 map 后的对象数组上做：`const total = mapped.length; const rows = mapped.slice(offset, offset + limit); return { rows, total, truncated: rows.length < total }`。
    5. import `PaginatedResult` from '../../../src/types/pagination'（确认相对路径，参照项目既有 main→src types 引用惯例；若 tsconfig 路径别名可用则用别名）。

    **ouiService.ts getAll（SQL 下推分页）**：
    1. 签名 `static getAll(limit=5000, offset=0): PaginatedResult<any>`。默认 5000。
    2. SQL 改为 `SELECT id, oui_prefix, vendor_name, is_custom, created_at, updated_at FROM oui_database ORDER BY vendor_name, oui_prefix LIMIT ? OFFSET ?`（prepared statement `?` 绑定 limit/offset，不用字符串拼接）。
    3. total 单独查：`SELECT COUNT(*) as c FROM oui_database`（未应用 limit 的全表计数）。
    4. 返回 `{ rows, total, truncated: rows.length < total }`。

    **networkIpc.ts handler**：
    - 签名加 `limit?, offset?`；body：`const safeLimit = validateLimit(limit, 2000, 50000); const safeOffset = validateOffset(offset); return NetworkSegmentService.getIPDetails(networkId, searchIp, searchMac, sortBy, sortOrder, safeLimit, safeOffset)`。import validateLimit/validateOffset from '../utils/pagination'。

    **ouiIpc.ts handler**：
    - `secure((_e, limit?: number, offset?: number) => OUIService.getAll(validateLimit(limit, 5000, 50000), validateOffset(offset)))`。import 同上。

    **D-4-6 ip_status 增长核实结论（必须写入 CHANGELOG / SUMMARY）**：已查 `ipStatusService.ts:34-41` endCollection — `ip_status` 仅有软标记 `status='deprecated'`（last_seen 过期行），**无物理 DELETE/purge**，故表随活跃 IP 累积单调增长。D-4-6 JS 过滤+slice 限定了 **payload**（信封有界），但**未限定 DB 全表读**。这是已知 trade-off：物理清理 ip_status 属独立 phase（不在 DATA-01 范围）。本 plan 不静默假设有界。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npx esbuild electron/main.ts --bundle --platform=node --format=cjs --outfile=.tmp/esbuild-check-04-01.js --external:better-sqlite3 --external:ssh2 --external:telnet-client 2>&1 | head -5 && rm -f .tmp/esbuild-check-04-01.js</automated>
  </verify>
  <done>
    - `grep -n "PaginatedResult" electron/services/networkSegmentService.ts` 命中（getIPDetails 返回信封）
    - `grep -n "LIMIT ? OFFSET ?" electron/services/ouiService.ts` 命中
    - `grep -n "validateLimit(limit, 2000, 50000)" electron/ipc/networkIpc.ts` 命中
    - `grep -n "validateLimit(limit, 5000, 50000)" electron/ipc/ouiIpc.ts` 命中
    - `grep -n "OUIService.getVendor" electron/services/networkSegmentService.ts` 仍命中（PERF-01 读路径保留）
    - tsc + esbuild 双绿
  </done>
</task>

<task type="auto">
  <name>Task 3: anomaly:getChanges 补 offset + 信封 + preload 同步签名</name>
  <files>electron/services/anomalyService.ts, electron/ipc/anomalyIpc.ts, electron/preload.ts</files>
  <read_first>
    - electron/services/anomalyService.ts:147-153（getChanges 已有 LIMIT ? 无 OFFSET ?）
    - electron/ipc/anomalyIpc.ts:7-11,20-21（本地 validateLimit + handler）
    - electron/preload.ts:71-75,87（getIPDetails/anomaly.getChanges/oui.getAll 暴露签名）
    - electron/utils/pagination.ts（Task 1 产物，复用）
  </read_first>
  <action>
    **anomalyService.ts getChanges**：
    1. 签名 `static getChanges(unacknowledgedOnly=false, limit=100, offset=0): PaginatedResult<any>`。
    2. SQL 加 `OFFSET ?`：`... ORDER BY detected_at DESC LIMIT ? OFFSET ?`，绑定 limit、offset。
    3. total 单独查（带相同 WHERE acknowledged 条件）：`SELECT COUNT(*) as c FROM ip_mac_changes [WHERE acknowledged = 0]`。
    4. 返回 `{ rows: mapped, total, truncated: rows.length < total }`。保留现有 `.map(row => ({ ...row, acknowledged: row.acknowledged === 1 }))`。

    **anomalyIpc.ts**：
    1. 删除本地 `validateLimit`（line 7-11），改 import `{ validateLimit, validateOffset } from '../utils/pagination'`。
    2. handler 改：`secure((_e, unacknowledgedOnly?: boolean, limit?: number, offset?: number) => AnomalyService.getChanges(unacknowledgedOnly, validateLimit(limit, 100, 10000), validateOffset(offset)))`。anomaly 维持默认 100、硬上限 10000（D-4-3/D-4-4）。

    **preload.ts（三通道签名同步，向后兼容：新参数可选）**：
    - line 71-72 getIPDetails：`(networkId: number, searchIp?: string, searchMac?: string, sortBy?: string, sortOrder?: string, limit?: number, offset?: number) => ipcRenderer.invoke('network:getIPDetails', networkId, searchIp, searchMac, sortBy, sortOrder, limit, offset)`
    - line 75 anomaly.getChanges：`(unacknowledgedOnly?: boolean, limit?: number, offset?: number) => ipcRenderer.invoke('anomaly:getChanges', unacknowledgedOnly, limit, offset)`
    - line 87 oui.getAll：`(limit?: number, offset?: number) => ipcRenderer.invoke('oui:getAll', limit, offset)`
    新参数均可选（默认 undefined → 网关层落回默认 cap），旧调用方零改动即可编译。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo "TSC_OK" && npx esbuild electron/main.ts --bundle --platform=node --format=cjs --outfile=.tmp/esbuild-check.js --external:better-sqlite3 --external:ssh2 --external:telnet-client >/dev/null 2>&1 && echo "ESBUILD_OK" && rm -f .tmp/esbuild-check.js</automated>
  </verify>
  <done>
    - `grep -n "OFFSET ?" electron/services/anomalyService.ts` 命中
    - `grep -c "function validateLimit" electron/ipc/anomalyIpc.ts` 返回 0（本地 helper 已删，改用共享）
    - `grep -n "from '../utils/pagination'" electron/ipc/anomalyIpc.ts` 命中
    - `grep -n "offset" electron/preload.ts` 命中 ≥3 处（三个通道）
    - `grep -n "validateLimit(limit, 100, 10000)" electron/ipc/anomalyIpc.ts` 命中
    - tsc + esbuild 双绿
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → IPC gateway | untrusted limit/offset 从渲染层传入，必须网关校验 |
| IPC gateway → service | 仅安全整数值到达 service / SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure (unbounded payload / DoS) | network:getIPDetails / oui:getAll / anomaly:getChanges handlers | mitigate | 网关层 validateLimit 强制默认 cap（2000/5000/100）+ 硬上限钳制（50000/50000/10000），server-side 强制，不信 renderer |
| T-04-02 | Tampering (param injection / malformed limit/offset) | 所有 list handler 的 limit/offset 参数 | mitigate | validateLimit/validateOffset 强制 Number.isInteger + 范围校验；SQL 用 prepared statement `?` 绑定 LIMIT/OFFSET（oui/anomaly），绝不字符串拼接 |
| T-04-03 | Repudiation / Information Disclosure (静默截断藏 rogue 设备) | 3 个 list 通道返回值 | mitigate | PaginatedResult 信封 `{ rows, total, truncated }` 明确告知截断，运维可经 offset 翻页，不静默藏数据（核心价值：设备安全可控） |
| T-04-04 | Denial of Service (ip_status 全表读膨胀) | networkSegmentService.getIPDetails 全量读 ip_status | accept | ip_status 仅有软 deprecated 标记无物理 purge（ipStatusService.ts:34-41），表单调增长。D-4-6 JS 过滤+slice 限定 payload 不限 DB 读。物理清理越界 DATA-01（独立 phase）。本 plan 在 SUMMARY 显式记录此 trade-off，不静默假设有界 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（严格 + noUnusedLocals 绿）
- electron main esbuild 打包 exit 0
- 三个 list 通道 handler 均含 validateLimit 网关校验
- 三个 service 返回 PaginatedResult 信封
- preload 三通道签名含 limit/offset
- getIPDetails 保留 OUIService.getVendor（PERF-01 不回归）
- 共享 helper 文件存在，anomalyIpc 本地 validateLimit 已删
</verification>

<success_criteria>
- DATA-01 success criteria #1：network:getIPDetails / oui:getAll / anomaly:getChanges 三通道均可 grep 到 limit/offset 参数（第 4 通道 export:arpTable 归 04-02）
- DATA-01 success criteria #3（部分）：preload 签名变更向后兼容（新参数可选，旧调用编译通过 + 默认行为有界）
- D-4-1~D-4-4 + D-4-6 全部落地
- ip_status 增长 trade-off 在 SUMMARY 显式记录
</success_criteria>

<output>
完成后创建 `.planning/phases/04-data-ipc-safety/04-01-SUMMARY.md`，必须包含 ip_status 无物理 purge 的 trade-off 说明（T-04-04）。
</output>
