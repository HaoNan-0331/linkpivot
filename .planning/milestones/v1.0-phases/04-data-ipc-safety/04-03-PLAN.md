---
phase: 04-data-ipc-safety
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/types/electron.d.ts
  - src/components/ip-management/NetworkTab.tsx
  - src/components/ip-management/OuiTab.tsx
  - src/components/ip-management/AnomalyTab.tsx
autonomous: true
requirements: [DATA-01]
user_setup: []

must_haves:
  truths:
    - "NetworkTab.tsx 读 api.network.getIPDetails(...).rows（信封）而非裸数组"
    - "OuiTab.tsx 读 api.oui.getAll(...).rows"
    - "AnomalyTab.tsx 读 api.anomaly.getChanges(...).rows"
    - "tsc -p tsconfig.web.json 严格 + noUnusedLocals 绿"
    - "渲染层默认调用不传 limit/offset（落回后端默认 cap），现有列表正常显示"
  artifacts:
    - path: "src/types/electron.d.ts"
      provides: "window.api 三通道返回类型标注 PaginatedResult"
    - path: "src/components/ip-management/NetworkTab.tsx"
      provides: "读信封 .rows"
    - path: "src/components/ip-management/OuiTab.tsx"
      provides: "读信封 .rows"
    - path: "src/components/ip-management/AnomalyTab.tsx"
      provides: "读信封 .rows"
  key_links:
    - from: "src/components/ip-management/NetworkTab.tsx"
      to: "api.network.getIPDetails"
      via: "解构 .rows"
      pattern: "\\.rows"
---

<objective>
渲染层 3 个 Tab 消费方适配 04-01 引入的 `PaginatedResult` 信封：改读 `.rows`。同步 `src/types/electron.d.ts` 的 window.api 返回类型。属 DATA-01 success criteria #3「现有调用方适配新签名，无回归」。本 plan 仅做最小适配（让现有列表读到数据 + 编译绿），完整翻页 UI 留 Phase 5。

Purpose: 让 04-01 的信封契约在渲染层真正落地，否则 Tab 拿到 `{rows,total,truncated}` 当数组用会运行时崩。
Output: 3 个 Tab 文件 + electron.d.ts 类型。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-data-ipc-safety/04-CONTEXT.md
@.planning/phases/04-data-ipc-safety/04-01-SUMMARY.md

<interfaces>
<!-- 04-01 产出的信封类型（src/types/pagination.ts） -->
```typescript
export interface PaginatedResult<T> {
  rows: T[]
  total: number
  truncated: boolean
}
```

<!-- 渲染层消费点（待改读 .rows） -->
```typescript
// NetworkTab.tsx:31,43
const [usage, details] = await Promise.all([api.network.getIPUsage(id), api.network.getIPDetails(id, searchIp, searchMac)])
setIpDetails(details)   // details 现为 PaginatedResult → 需改 details.rows

// OuiTab.tsx:22
const [e, s] = await Promise.all([api.oui.getAll(), api.oui.getStats()])
setEntries(e)   // e 现为 PaginatedResult → 需改 e.rows

// AnomalyTab.tsx:29
const [s, c, e] = await Promise.all([api.anomaly.getStats(), api.anomaly.getChanges(), api.anomaly.getExcludedIPs()])
setChanges(c)   // c 现为 PaginatedResult → 需改 c.rows
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: electron.d.ts 三通道返回类型标注 PaginatedResult</name>
  <files>src/types/electron.d.ts</files>
  <read_first>
    - src/types/electron.d.ts 全文（确认 window.api 类型定义结构与现有 any 标注）
    - src/types/pagination.ts（04-01 产出的 PaginatedResult）
  </read_first>
  <action>
    在 electron.d.ts 中为三个 list 通道的返回类型补 `Promise<PaginatedResult<any>>`（或更精确的元素类型，若文件已有可复用）：
    - `getIPDetails(...): Promise<PaginatedResult<any>>`
    - `oui.getAll(...): Promise<PaginatedResult<any>>`
    - `anomaly.getChanges(...): Promise<PaginatedResult<any>>`
    import `PaginatedResult` from './pagination'。其余通道（getIPUsage/getStats 等）类型不动。
    若 electron.d.ts 当前对这些通道无显式返回类型（隐式 any），则补上显式标注以让下游解构 `.rows` 类型安全。
    注意 noUnusedLocals：import 的 PaginatedResult 必须被使用，不留悬空 import。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo TSC_OK</automated>
  </verify>
  <done>
    - `grep -n "PaginatedResult" src/types/electron.d.ts` 命中（≥3 处或 1 处复用）
    - tsc 绿
  </done>
</task>

<task type="auto">
  <name>Task 2: 三 Tab 读信封 .rows（向后兼容最小适配）</name>
  <files>src/components/ip-management/NetworkTab.tsx, src/components/ip-management/OuiTab.tsx, src/components/ip-management/AnomalyTab.tsx</files>
  <read_first>
    - src/components/ip-management/NetworkTab.tsx:26-45（selectSegment + searchIPs 调用点）
    - src/components/ip-management/OuiTab.tsx:19-34（loadAll 调用点）
    - src/components/ip-management/AnomalyTab.tsx:24-36（loadData 调用点）
    - src/types/pagination.ts（信封结构）
  </read_first>
  <action>
    **NetworkTab.tsx**：
    - line 29-33 `selectSegment`：`const [usage, details] = await Promise.all([...]); setIpUsage(usage); setIpDetails(details.rows)`（details 现为信封，取 `.rows`）。
    - line 41-44 `searchIPs`：`setIpDetails((await api.network.getIPDetails(selectedId, searchIp, searchMac)).rows)`。
    - **不传 limit/offset**（默认 → 后端 2000 cap；本阶段最小适配，翻页 UI 留 Phase 5）。

    **OuiTab.tsx**：
    - line 22 `loadAll`：`const [e, s] = await Promise.all([api.oui.getAll(), api.oui.getStats()]); setEntries(e.rows); setStats(s)`。

    **AnomalyTab.tsx**：
    - line 27-33 `loadData`：`const [s, c, e] = await Promise.all([...]); setStats(s); setChanges(c.rows); setExcludedIPs(e)`。

    三处均只加 `.rows`，不改其他逻辑、不引入分页 UI。setIpDetails/setEntries/setChanges 的 state 类型若显式标注为 `any[]`，则 `.rows: any[]` 兼容；若标注为具体 interface 且报错，则保持 any[] 不变（FE-02 的 any→types 归 Phase 5，本阶段不扩 scope）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && echo TSC_OK</automated>
  </verify>
  <done>
    - `grep -n "\.rows" src/components/ip-management/NetworkTab.tsx` 命中（≥2 处：selectSegment + searchIPs）
    - `grep -n "e.rows" src/components/ip-management/OuiTab.tsx` 命中
    - `grep -n "c.rows" src/components/ip-management/AnomalyTab.tsx` 命中
    - tsc 严格 + noUnusedLocals 绿
    - 无新增 any 扩散（仅复用既有 any[] state）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC 返回 → renderer state | 信封对象进入 React state，渲染层必须正确解构 .rows 否则 UI 崩 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Denial of Service (渲染层把信封当数组迭代致运行时崩) | NetworkTab/OuiTab/AnomalyTab 解构 | mitigate | 三处显式取 `.rows`，tsc 类型标注 PaginatedResult 强制编译期校验，运行时不再误迭代信封对象 |
| T-04-09 | Information Disclosure (默认 cap 截断后渲染层无提示) | 三 Tab 列表显示 | accept | 本阶段最小适配仅读 .rows；`truncated` 提示 UI 属翻页交互，归 Phase 5（CONTEXT deferred 明示）。信封字段已透传到 renderer，Phase 5 可直接消费，无信息丢失 |
</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` exit 0（严格 + noUnusedLocals）
- 三 Tab 均 grep 到 `.rows`
- electron.d.ts 三通道返回类型为 PaginatedResult
- 无运行时回归（默认调用 → 后端默认 cap → .rows 正常渲染）
</verification>

<success_criteria>
- DATA-01 success criteria #3 完整落地：现有调用方（3 Tab）适配新信封签名，无回归（编译绿 + 默认调用行为有界且正常显示）
- 04-01 信封契约端到端贯通（IPC → preload → renderer）
- 不越界 Phase 5（不引入翻页 UI，仅最小 .rows 适配）
</success_criteria>

<output>
完成后创建 `.planning/phases/04-data-ipc-safety/04-03-SUMMARY.md`。
</output>
