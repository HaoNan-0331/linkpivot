---
phase: 06-robustness-resource-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/discovery.ts
autonomous: true
requirements:
  - ROBUST-02
tags:
  - discovery
  - error-context
  - safe-log
  - observability

must_haves:
  truths:
    - "discovery 两处 JSON parse（command parse 与 topology parse）失败时，抛出的 Error message 含「原始片段: ${raw.slice(0,200)}」，运维可定位 AI 漂移/截断/转义错误"
    - "command parse 失败补落 createSystemLog（status:failed，含 promptText + aiResponse + enriched errorMessage），与 topology parse 对齐——两处 parse 失败均落审计日志"
    - "discovery 5 处 createSystemLog 调用（line 116/126/240/258/266）全部经 safeLog helper 包裹，DB 写库失败仅 console.warn 不中断发现主流程"
    - "line 258 嵌套陷阱切断：catch 内 line 266 的二次 createSystemLog 自身抛错被 safeLog 独立 try/catch 吞掉，不中断主流程"
    - "静态三绿门禁通过：tsc -p tsconfig.web.json + electron main esbuild 打包 + vitest run 全 exit 0"
    - "对外 API 零改：discoverTopology / discoverTopologyInner 签名与 throw 语义不变（enriched Error 仍属 Error 实例，message 更详细但 throw 行为不变）"
  artifacts:
    - path: "electron/services/discovery.ts"
      provides: "enriched parse error + safeLog helper + 5 处 createSystemLog 非致命包裹"
      contains: "safeLog"
  key_links:
    - from: "electron/services/discovery.ts command parse catch (line 142-144)"
      to: "createSystemLog 审计日志"
      via: "safeLog({type:'discovery', status:'failed', promptText, aiResponse, errorMessage: enriched})"
      pattern: "原始片段"
    - from: "electron/services/discovery.ts topology parse catch (line 265-274)"
      to: "enriched errorMessage"
      via: "errorMessage 含 `JSON 解析失败: ${err.message} | 原始片段: ${raw.slice(0,200)}`"
      pattern: "原始片段"
    - from: "electron/services/discovery.ts 5 处 createSystemLog"
      to: "safeLog helper"
      via: "替换调用点为 safeLog(entry)"
      pattern: "safeLog\\("
---

<objective>
落地 ROBUST-02（per D-6-3 / D-6-4）：在 discovery.ts 内（独占文件，与 06-01 零重叠）做两件事——
1. 两处 JSON parse（command parse line 136-144 / topology parse line 250-274）失败抛 enriched Error，message 含 `原始片段: ${raw.slice(0,200)}`，command parse 补 createSystemLog 与 topology parse 对齐（D-6-3）。
2. 5 处 createSystemLog 裸调用（line 116/126/240/258/266）全部经局部 safeLog helper 包裹，DB 写库失败 console.warn 兜底不中断主流程（D-6-4，含 line 258 嵌套陷阱切断）。

满足 ROADMAP Phase 6 SC#2（JSON parse 错误上下文）+ SC#3（createSystemLog try/catch）。

Purpose: SC#2 杜绝定位盲区（运维可定位 AI 漂移/截断/转义错误，满足「原始内容片段 + 位置」）；SC#3 杜绝日志写库失败中断发现主流程（PROJECT.md 核心价值「设备/数据安全可控」+ Phase 3 D-P4「可观测性优先」，console.warn 兜底非纯静默）。

Output: 重构后的 discovery.ts；safeLog helper 局限 discovery 模块内（不跨模块，跨模块 defer）；enriched errorMessage 复用 ai_system_logs.errorMessage 现有 TEXT 字段（systemLog truncate 上限 16000，slice 200 + err.message < 500 远低于截断线，无信息丢失）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-robustness-resource-safety/06-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@electron/services/discovery.ts
@electron/services/systemLog.ts

<interfaces>
<!-- discovery.ts 当前 5 处 createSystemLog 调用点（D-6-4 全部包 safeLog） -->

```typescript
// line 116-122: command AI 调用失败
createSystemLog({
  type: 'discovery', status: 'failed',
  deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
  promptText: commandPromptText,
  errorMessage: `AI 命令生成失败: ${err.message}`,
})

// line 126-132: command AI 成功
createSystemLog({
  type: 'discovery', status: 'success',
  deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
  promptText: commandPromptText,
  aiResponse: commandAiResponse,
  parsedResult: `阶段1: AI命令生成`,
})

// line 240-247: topology AI 调用失败
createSystemLog({
  type: 'discovery', status: 'failed',
  deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
  promptText: topologyPromptText,
  errorMessage: `AI 拓扑分析失败: ${err.message}`,
})

// line 258-264: topology parse 成功（在 try line 251-264 内！嵌套陷阱：若此 createSystemLog 抛，被 line 265 catch → line 266 二次 createSystemLog）
createSystemLog({
  type: 'discovery', status: 'success',
  deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
  promptText: topologyPromptText,
  aiResponse,
  parsedResult: JSON.stringify(parsed, null, 2),
})

// line 266-272: topology parse 失败（catch 内二次调用，自身抛则中断主流程）
createSystemLog({
  type: 'discovery', status: 'failed',
  deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
  promptText: topologyPromptText,
  aiResponse,
  errorMessage: `JSON 解析失败: ${err.message}`,  // D-6-3: 现状无原始片段，需补 slice(0,200)
})
```

<!-- 两处 JSON parse 当前实现（D-6-3 enriched 改造对象） -->

```typescript
// line 136-144: command JSON parse（catch 直接 throw 无上下文无 createSystemLog）
try {
  let jsonStr = commandAiResponse.trim()
  const codeBlockMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/)
  if (codeBlockMatch) jsonStr = codeBlockMatch[1].trim()
  const parsed = JSON.parse(jsonStr)
  deviceCommands = parsed.devices || []
} catch (err: any) {
  throw new Error(`AI 命令结果解析失败: ${err.message}`)  // D-6-3: 补 slice + 补 createSystemLog
}

// line 250-274: topology JSON parse（catch 有 createSystemLog 但 errorMessage 无原始片段）
try {
  let jsonStr = aiResponse.trim()
  const codeBlockMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/)
  if (codeBlockMatch) jsonStr = codeBlockMatch[1].trim()
  parsed = JSON.parse(jsonStr)
  createSystemLog({ ... parsedResult: JSON.stringify(parsed, null, 2) })  // line 258 嵌套陷阱
} catch (err: any) {
  createSystemLog({ ... errorMessage: `JSON 解析失败: ${err.message}` })  // line 266, D-6-3 补 slice
  throw new Error(`AI 分析结果解析失败: ${err.message}`)  // D-6-3 补 slice
}
```

<!-- createSystemLog 签名（被调用方，本 plan 不改其实现，仅 discovery 侧包 safeLog） -->

From electron/services/systemLog.ts:
```typescript
export function createSystemLog(log: {
  type: string
  status: string
  deviceIds?: string
  deviceNames?: string
  promptText?: string
  aiResponse?: string
  parsedResult?: string
  errorMessage?: string
}): string
// truncate 上限 MAX_LOG_FIELD_LEN = 16000，slice(0,200) + err.message < 500 远低于截断线（D-6-3 不扩 schema 依据）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: discovery 内抽 safeLog helper + 5 处 createSystemLog 替换为 safeLog 包裹（per D-6-4，SC#3 + 嵌套陷阱切断）</name>
  <files>electron/services/discovery.ts</files>

  <read_first>
    - electron/services/discovery.ts（看 5 处 createSystemLog 调用点 line 116/126/240/258/266 实际形态而非假设，特别注意 line 258 在 try line 251-264 内的嵌套结构）
    - .planning/phases/06-robustness-resource-safety/06-CONTEXT.md §D-6-4（safeLog 策略：5 处全包 + 局部 helper + console.warn 兜底 + line 258 嵌套陷阱切断）
    - .planning/codebase/CONCERNS.md §BUG-5 line 78+（5 处 createSystemLog 裸调用风险，line 258 嵌套陷阱分析）
    - electron/services/systemLog.ts（确认 createSystemLog 签名 + truncate 16000 上限，本 task 不改 systemLog.ts）
    - .planning/phases/03-performance-optimization/03-CONTEXT.md §D-P4（可观测性原则先例，console.warn 兜底遵循此原则）
  </read_first>

  <action>
在 discovery.ts 顶部（import 之后、`export interface DiscoveryFailedDevice` 之前或 `let discoveryInProgress` 之前，executor 裁量位置但须模块级）新增局部 safeLog helper（D-6-4 锁定函数体）：

```typescript
/**
 * discovery 模块局部非致命日志 helper（D-6-4）。
 * DB 写库失败时 console.warn 兜底（可观测，非纯静默，遵循 Phase 3 D-P4 可观测性原则），
 * 不中断发现主流程（SC#3）。局限于 discovery.ts，不跨模块（跨模块统一 safeLog defer）。
 */
function safeLog(entry: Parameters<typeof createSystemLog>[0]): string | undefined {
  try {
    return createSystemLog(entry)
  } catch (e: any) {
    console.warn('[safeLog] discovery 日志写库失败', e?.message)
    return undefined
  }
}
```

注意 helper 签名细节：
- 入参类型复用 `Parameters<typeof createSystemLog>[0]`（与 createSystemLog 入参结构完全一致，避免重复定义 interface；createSystemLog 入参已是可选字段对象，safeLog 接受同结构）。
- 返回 `string | undefined`（成功返回 log id，失败返回 undefined）——调用方不依赖返回值（5 处现状均不取返回值），故 undefined 安全。
- console.warn 格式 `[safeLog] discovery 日志写库失败` + error message（D-6-4 锁定文案前缀，executor 不改前缀）。

将 discovery.ts 内 5 处 `createSystemLog({...})` 调用全部替换为 `safeLog({...})`，对象字面量结构原样保留（字段不变）：
- line 116-122（command AI 失败）→ `safeLog({ type: 'discovery', status: 'failed', deviceIds: deviceIdsStr, deviceNames: deviceNamesStr, promptText: commandPromptText, errorMessage: \`AI 命令生成失败: ${err.message}\` })`
- line 126-132（command AI 成功）→ `safeLog({ type: 'discovery', status: 'success', deviceIds: deviceIdsStr, deviceNames: deviceNamesStr, promptText: commandPromptText, aiResponse: commandAiResponse, parsedResult: \`阶段1: AI命令生成\` })`
- line 240-247（topology AI 失败）→ `safeLog({ type: 'discovery', status: 'failed', ..., errorMessage: \`AI 拓扑分析失败: ${err.message}\` })`
- line 258-264（topology parse 成功，在 try 内）→ `safeLog({ ... parsedResult: JSON.stringify(parsed, null, 2) })` —— **嵌套陷阱切断**：safeLog 内 try/catch，此调用抛错被吞为 console.warn，不再触发外层 line 265 catch → line 266 二次 createSystemLog 链。
- line 266-272（topology parse 失败，catch 内二次调用）→ `safeLog({ ... errorMessage: enriched })`（enriched 见 Task 2）—— 此处自身是 catch handler 内调用，safeLog 包裹后自身抛错也被吞，彻底切断「日志写库失败中断主流程」链。

**禁做的事（红线）：**
- 不改 createSystemLog 自身实现（systemLog.ts 不动，仅 discovery 侧包 safeLog）。
- 不抽全局 safeLog util（跨模块 defer，本阶段仅 discovery 局部）。
- 不改 5 处 createSystemLog 的对象字段（type/status/deviceIds/deviceNames/promptText/aiResponse/parsedResult/errorMessage 原样保留）。
- 不改 discoverTopology / discoverTopologyInner 签名与 throw 语义。
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
- `electron/services/discovery.ts` 含字面量 `function safeLog`（grep `function safeLog` 命中 1 处）。
- `electron/services/discovery.ts` 含字面量 `[safeLog] discovery 日志写库失败`（grep 命中 1 处，console.warn 兜底文案）。
- `electron/services/discovery.ts` 含字面量 `console.warn`（grep 命中 ≥ 1，在 safeLog helper 内）。
- `electron/services/discovery.ts` 中 `createSystemLog({` 直接调用计数为 0（grep `createSystemLog({` 命中 0——5 处全部替换为 safeLog）；`createSystemLog` 仅在 import 行与 safeLog helper 函数体内出现（grep `createSystemLog` 总命中 = import 1 + helper 内 1 = 2）。
- `electron/services/discovery.ts` 含字面量 `safeLog(` 调用计数 ≥ 5（grep `safeLog(` 命中 ≥ 5，5 处替换 + helper 定义内的 `createSystemLog` 不计；若用 `safeLog({` 精确 grep 应命中 5）。
- `electron/services/discovery.ts` 仍含 `import { createSystemLog } from './systemLog'`（grep 命中，import 不动）。
- `electron/services/discovery.ts` 仍含字面量 `阶段1: AI命令生成`、`AI 命令生成失败`、`AI 拓扑分析失败`（grep 命中，3 处 errorMessage/parsedResult 文案零改）。
- `npx tsc -p tsconfig.web.json` exit 0（`Parameters<typeof createSystemLog>[0]` 类型推导无 error）。
- `npx vitest run` exit 0，既有 25 项测试无回归。
- electron main esbuild 打包 exit 0（整 plan verification 验证）。
  </acceptance_criteria>

  <done>
safeLog helper 落地（console.warn 兜底）；5 处 createSystemLog 全部替换为 safeLog；line 258 嵌套陷阱经 safeLog 内 try/catch 切断；import createSystemLog 保留；三绿门禁过。
  </done>
</task>

<task type="auto">
  <name>Task 2: 两处 JSON parse 失败 enriched Error（含「原始片段: slice(0,200)」）+ command parse 补 safeLog（per D-6-3，SC#2）</name>
  <files>electron/services/discovery.ts</files>

  <read_first>
    - electron/services/discovery.ts（看 command parse line 136-144 / topology parse line 250-274 当前实现而非假设；注意 command parse catch line 142-144 直接 throw 无 safeLog，topology parse catch line 265-274 有 safeLog 但 errorMessage 无原始片段）
    - .planning/phases/06-robustness-resource-safety/06-CONTEXT.md §D-6-3（enriched errorMessage 模板：`AI 命令结果解析失败: ${err.message} | 原始片段: ${raw.slice(0,200)}`；command parse 补 createSystemLog 与 topology parse 对齐；enrichParseError helper 是否抽委托 planner）
    - .planning/codebase/CONCERNS.md §BUG-4 line 71+（parse 失败错误上下文不足分析）
    - electron/services/systemLog.ts（确认 truncate 16000 上限，slice 200 + err.message < 500 远低于截断线，enriched errorMessage 不超阈值）
    - Task 1 完成后的 discovery.ts（safeLog helper 已存在，本 task 调用 safeLog 而非 createSystemLog）
  </read_first>

  <action>
**抽局部 enrichParseError helper（D-6-3 委托，executor 裁量抽 vs 内联——建议抽，两处同模式去重）：**

在 safeLog helper 之后新增：

```typescript
/**
 * 包装 JSON.parse 失败为带原始片段的 enriched Error（D-6-3，SC#2）。
 * SyntaxError 自带 position（"Unexpected token ... in JSON at position N"），
 * 但不含原始内容——补 slice(0,200) 让运维定位 AI 漂移/截断/转义错误。
 * 局限于 discovery.ts（两处 parse 同模式去重）。
 */
function enrichParseError(prefix: string, raw: string, err: unknown): Error {
  const errMessage = err instanceof Error ? err.message : String(err)
  return new Error(`${prefix}: ${errMessage} | 原始片段: ${(raw || '').slice(0, 200)}`)
}
```

**改造 command parse（line 136-144）：**

将 catch 块从直接 throw 改为补 safeLog（与 topology parse 对齐）+ 抛 enriched Error。`raw` 取 `commandAiResponse`（command parse 的原始 AI 返回）：

```typescript
let deviceCommands: Array<{ deviceId: string; deviceName: string; vendor: string; commands: string[] }>
let commandRaw = commandAiResponse  // 保留原始引用给 enrichParseError
try {
  let jsonStr = commandAiResponse.trim()
  const codeBlockMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/)
  if (codeBlockMatch) jsonStr = codeBlockMatch[1].trim()
  const parsed = JSON.parse(jsonStr)
  deviceCommands = parsed.devices || []
} catch (err: any) {
  // D-6-3：补 safeLog 与 topology parse 对齐（两处 parse 失败均落审计日志，运维可追溯）
  safeLog({
    type: 'discovery', status: 'failed',
    deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
    promptText: commandPromptText,
    aiResponse: commandRaw,
    errorMessage: `AI 命令结果解析失败: ${err.message} | 原始片段: ${(commandRaw || '').slice(0, 200)}`,
  })
  throw enrichParseError('AI 命令结果解析失败', commandRaw, err)
}
```

**改造 topology parse（line 250-274）：**

- catch 内 safeLog（line 266-272）的 errorMessage 补 `| 原始片段: ${(aiResponse || '').slice(0, 200)}`（D-6-3 同步加 slice）。
- catch 末尾 throw 改用 enrichParseError：

```typescript
let parsed: any
try {
  let jsonStr = aiResponse.trim()
  const codeBlockMatch = jsonStr.match(/```(?:json)?\s*([\s\S]*?)```/)
  if (codeBlockMatch) jsonStr = codeBlockMatch[1].trim()
  parsed = JSON.parse(jsonStr)

  // Log second AI call - topology analysis（safeLog 已在 Task 1 替换，此处原样保留 safeLog 调用）
  safeLog({
    type: 'discovery', status: 'success',
    deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
    promptText: topologyPromptText,
    aiResponse,
    parsedResult: JSON.stringify(parsed, null, 2),
  })
} catch (err: any) {
  // D-6-3：errorMessage 补原始片段 slice(0,200)（现状仅 ${err.message}）
  safeLog({
    type: 'discovery', status: 'failed',
    deviceIds: deviceIdsStr, deviceNames: deviceNamesStr,
    promptText: topologyPromptText,
    aiResponse,
    errorMessage: `JSON 解析失败: ${err.message} | 原始片段: ${(aiResponse || '').slice(0, 200)}`,
  })
  throw enrichParseError('AI 分析结果解析失败', aiResponse, err)
}
```

**注意不变量：**
- 仍 throw Error 实例（enrichParseError 返回 new Error(...)），discoverTopologyInner 的 throw 行为不变，调用方（IPC 层）catch 不变。
- enriched message 文案 prefix 与现状一致：`AI 命令结果解析失败` / `AI 分析结果解析失败`（保留原 prefix，仅追加 ` | 原始片段: ...`）。
- topology parse catch 内的 safeLog errorMessage prefix `JSON 解析失败`（与现状一致），仅追加 slice。
- enrichParseError 与 safeLog 均模块级局部 helper，不 export。

**安全红线（信息泄露评估，见 threat_model T-06-02-01）：**
- raw.slice(0,200) 可能含 AI 返回的设备凭证/拓扑敏感信息片段。本工具为单用户桌面场景，ai_system_logs 仅本地 DB（用户自机），无网络上报；errorMessage 经 systemLog truncate 16000 上限保护。运维定位 AI 漂移的业务需求（SC#2 字面「原始内容片段 + 位置」）优先于「日志层零原始片段」假设——本 phase 不引入日志脱敏层（跨模块 defer）。残留风险 accept，见 threat_model。

**禁做的事：**
- 不改 commandPromptText / topologyPromptText 变量定义与赋值。
- 不改 deviceCommands / parsed 类型与后续消费逻辑。
- 不扩 ai_system_logs schema（enriched errorMessage 复用 errorMessage TEXT 字段）。
  </action>

  <verify>
    <automated>npx tsc -p tsconfig.web.json && npx vitest run</automated>
  </verify>

  <acceptance_criteria>
- `electron/services/discovery.ts` 含字面量 `function enrichParseError`（grep 命中 1 处）。
- `electron/services/discovery.ts` 含字面量 `原始片段:`（grep 命中 ≥ 3：enrichParseError 内 1 + command parse safeLog 1 + topology parse safeLog 1）。
- `electron/services/discovery.ts` 含字面量 `.slice(0, 200)`（grep `slice(0, 200)` 命中 ≥ 2：enrichParseError 内 + 两处 safeLog errorMessage 内；具体计数取决于 executor 是否在 safeLog errorMessage 内联 slice 还是统一走 enrichParseError 文案——若 safeLog errorMessage 内联 slice 且 throw 走 enrichParseError，则 slice 命中 ≥ 3）。
- `electron/services/discovery.ts` command parse catch 块含 `safeLog({`（grep `AI 命令结果解析失败` 命中，且其后跟 safeLog 调用而非直接 throw）。
- `electron/services/discovery.ts` 不再含 `throw new Error(\`AI 命令结果解析失败: ${err.message}\`)`（裸 throw 无原始片段的旧形态，grep `AI 命令结果解析失败: ${err.message})` 精确匹配命中 0）。
- `electron/services/discovery.ts` 不再含 `errorMessage: \`JSON 解析失败: ${err.message}\`` 后不接 ` | 原始片段`（grep `JSON 解析失败: \${err.message}\`` 旧形态命中 0；新形态 `JSON 解析失败: ${err.message} | 原始片段:` 命中 1）。
- `electron/services/discovery.ts` 仍含 `throw enrichParseError('AI 命令结果解析失败'` 与 `throw enrichParseError('AI 分析结果解析失败'`（grep 命中各 1）。
- `npx tsc -p tsconfig.web.json` exit 0。
- `npx vitest run` exit 0，既有 25 项测试无回归。
- electron main esbuild 打包 exit 0。
  </acceptance_criteria>

  <done>
两处 JSON parse 失败抛 enriched Error（含「原始片段: slice(0,200)」）；command parse 补 safeLog 与 topology parse 对齐；enrichParseError helper 抽取；throw 仍为 Error 实例（语义不变）；三绿门禁过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI 远程返回 → 本进程 | AI 返回文本（commandAiResponse/aiResponse）可能含敏感数据片段；enriched errorMessage 含 raw.slice(0,200) 跨越此边界落入 ai_system_logs |
| 本进程 → ai_system_logs（本地 DB） | createSystemLog 同步写 DB；DB 锁/异常抛出经 safeLog 兜底，不反向中断发现主流程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | enriched errorMessage 含 raw.slice(0,200) → 可能含设备凭证/拓扑敏感信息片段写入 ai_system_logs.errorMessage | accept | 残留风险可接受：(1) 本工具单用户桌面场景，ai_system_logs 仅本地 better-sqlite3 DB（用户自机），无网络上报；(2) errorMessage 经 systemLog truncate 16000 上限保护，slice(0,200) + err.message < 500 远低于截断线；(3) SC#2 字面要求「原始内容片段 + 位置」是运维定位 AI 漂移的业务需求，优先于「日志层零原始片段」；(4) 引入日志脱敏层（正则抹除凭证模式）是更大重构，跨模块 defer。 |
| T-06-02-02 | Denial of Service（日志写库失败中断主流程） | 5 处 createSystemLog 裸调用，DB 异常抛出中断发现主流程（SC#3 缓解对象） | mitigate | safeLog helper try/catch 包裹 5 处调用，DB 写库失败 console.warn 兜底，不抛出到调用方。 |
| T-06-02-03 | 异常安全（line 258 嵌套陷阱衍生新中断面） | line 258 createSystemLog 在 try line 251-264 内，自身抛触发外层 catch line 265 → line 266 二次 createSystemLog 自身抛则中断主流程 | mitigate | 每处 createSystemLog 经 safeLog 独立 try/catch（D-6-4 显式要求），切断嵌套链；line 266 的 safeLog 自身抛被自身 try/catch 吞为 console.warn，不二次中断。 |
| T-06-02-04 | Integrity | enriched errorMessage 不影响 JSON.parse 本身的解析结果；throw 仍为 Error 实例，调用方 catch (err: any) err.message 行为兼容 | mitigate | enrichParseError 返回 `new Error(...)`，instanceof Error 不变；errorMessage prefix 与现状一致（`AI 命令结果解析失败` / `AI 分析结果解析失败` / `JSON 解析失败`），仅追加 ` \| 原始片段:`。 |

无 high 级威胁未 mitigate。T-06-02-01 accept 有 4 条理由说明（单用户桌面 + 本地 DB + 业务需求优先 + 脱敏层 defer）。
</threat_model>

<verification>
## 三绿门禁（与 Phase 2/3/4/5 一致）
1. `npx tsc -p tsconfig.web.json` exit 0（严格模式 + noUnusedLocals；`Parameters<typeof createSystemLog>[0]` 类型推导无 error）。
2. electron main esbuild 打包 exit 0。
3. `npx vitest run` exit 0（既有 25 项 unit 测试无回归；本 plan 不新增测试——safeLog/enrichParseError 是纯 helper 但依赖 createSystemLog（DB）与 AI 返回，DEP-1 下 typed mock 价值有限，可不加单测；若 executor 裁量加 enrichParseError 纯逻辑单测，参照 02-01 hasColumn typed mock 先例，但不强制）。

## 静态 grep 验收点（SC#2/SC#3 字面 + D-6-5 静态部分）
- discovery.ts 含 `function safeLog`、`[safeLog] discovery 日志写库失败`、`function enrichParseError`、`原始片段:`、`.slice(0, 200)`。
- discovery.ts `createSystemLog({` 直接调用计数 0（5 处全替换为 safeLog）。
- discovery.ts command parse catch 含 `safeLog(` 且 throw 走 `enrichParseError`（不再裸 throw）。
- discovery.ts topology parse catch errorMessage 含 `| 原始片段:`。

## 回归零改验证
- discovery.ts: discoverTopology / discoverTopologyInner 签名零改；DiscoveryResult / DiscoveryFailedDevice interface 零改；callAI / executeCommandsOnDevice / getDeviceByIdInternal 调用语义零改。
- systemLog.ts: createSystemLog 实现零改（truncate 16000 不动）。
- 与 06-01 零文件冲突：本 plan 仅 discovery.ts，06-01 仅 arpCollector.ts + ai.ts。
</verification>

<success_criteria>
- [ ] SC#2（ROADMAP Phase 6）：discovery JSON parse 失败携带原始内容片段 + 位置（grep `原始片段:` 与 `.slice(0, 200)` 命中）✓
- [ ] SC#3（ROADMAP Phase 6）：discovery 5 处 createSystemLog 经 try/catch 包裹（grep `createSystemLog({` 命中 0，全部走 safeLog）✓
- [ ] command parse 补 safeLog 与 topology parse 对齐（D-6-3 对齐而非新需求）✓
- [ ] line 258 嵌套陷阱切断（每处独立 try/catch，D-6-4）✓
- [ ] 对外 API 零改：discoverTopology/discoverTopologyInner 签名与 throw 语义不变（throw 仍为 Error 实例）✓
- [ ] 静态三绿门禁全过（tsc + esbuild + vitest）✓
- [ ] SC#3/SC#4 人工 HV 步骤（discovery parse 失败 HV）写入 06-HUMAN-UAT.md（D-6-5）✓
</success_criteria>

<output>
完成后创建 `.planning/phases/06-robustness-resource-safety/06-02-SUMMARY.md`，记录：safeLog helper 签名/console.warn 文案、enrichParseError 抽取决策（抽 vs 内联）、5 处替换点位、enriched errorMessage 模板、grep 验收命中证据、三绿门禁输出、与 06-01（ROBUST-01）零文件冲突确认、T-06-02-01 信息泄露 accept 决策理由。
</output>
