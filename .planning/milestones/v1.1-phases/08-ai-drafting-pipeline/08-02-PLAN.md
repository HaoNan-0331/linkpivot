---
phase: 08-ai-drafting-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/services/draftingService.ts
  - electron/services/draftingService.test.ts
autonomous: true
requirements: [DRAFT-04]

must_haves:
  truths:
    - "AI 起草 service 接收脱敏会话正文 + 关联设备，经 callAI（不改签名）产出 1~N 条 draft 草稿（每条含 category/title/content/tags/attrs/confidence/reasoning/duplication_verdict/duplicate_of_exp_id）"
    - "LLM 输出经代码层 JSON.parse + schema 校验（分类枚举锁死 troubleshooting/best_practices/product/env + 分类模板字段 + gap/confidence/duplication_verdict/duplicate_of_exp_id），校验失败重试最多 3 次"
    - "AI 判定无可总结内容时返回空草稿数组（不强产空条目）"
    - "每条草稿缺数据字段标 'gap' 不强填"
    - "起草 prompt 反幻觉：明确禁止输出 [CMD] 执行命令、分类不超枚举、字段不瞎编"
    - "DEMO_MODE 降级：未配置 AI 或 AI 不可用时，可选返回 mock 空草稿不阻塞（断点续传交 Plan 03 IPC 层）"
    - "judgeVerdicts 两阶段复判函数：阶段 A draftSession 纯起草得 drafts[]（existingSummaries=[]）→ 阶段 B 编排层按每条 draft.category 窄查喂 judgeVerdicts 复判 verdict + dupId（D-02 窄化语义，避免 context 溢出，W-4）"
    - "validateDrafts confidence 边界：'85%' → 0.85 通过；'high'（非法非数值/非百分比）→ fail 触发重试（W-2）"
  artifacts:
    - path: "electron/services/draftingService.ts"
      provides: "draftSession（纯起草）+ judgeVerdicts（两阶段复判）+ validateDrafts + buildDraftingPrompt 函数式"
      exports: ["draftSession", "judgeVerdicts", "validateDrafts", "buildDraftingPrompt"]
    - path: "electron/services/draftingService.test.ts"
      provides: "mock callAI 的 schema 校验/重试/分类枚举/gap/空草稿/confidence 边界/judgeVerdicts 复判单测"
  key_links:
    - from: "electron/services/draftingService.ts"
      to: "electron/services/ai.ts callAI"
      via: "import callAI（不改其签名）"
      pattern: "import.*callAI.*from.*'\\.\\./services/ai'|import.*\\{ callAI \\}.*from.*'\\./ai'"
    - from: "electron/services/draftingService.ts"
      to: "electron/services/experienceService.ts ExperienceCategory"
      via: "import 分类枚举类型"
      pattern: "import.*ExperienceCategory.*from.*'./experienceService'"
---

<objective>
Phase 8 起草引擎落地（D-01 + D-04 起草侧 + DRAFT-04 强 schema JSON 需求 + W-4 两阶段复判）：

本 plan 提供两个独立可测函数：

1. **`draftSession(input)`**（阶段 A 纯起草）：输入脱敏会话正文 + 关联设备（existingSummaries 通常传 []，由阶段 B 复判接管查重判定），构建强约束 JSON prompt → 调 `callAI`（**不改 callAI 签名**，D-01）→ 代码层 `JSON.parse` + schema 校验（分类枚举锁死 + 分类模板字段 + gap/confidence/duplication_verdict/duplicate_of_exp_id）→ 校验失败重试最多 3 次 → 返回 1~N 条结构化草稿或空数组（无可总结内容不强产，SC1）。
2. **`judgeVerdicts(input)`**（阶段 B 复判，W-4 两阶段方案核心）：输入阶段 A 的 drafts[] + 编排层按每条 draft.category 窄查得的 existingByCategory 存量映射，独立 prompt 让 LLM 对每条 draft 基于「其自身 category 的同分类存量」判定 ADD/UPDATE/NOOP + 命中 exp_id。**复用窄化检索语义（D-02 同 category + 同 deviceId），避免单次起草 4×1000 context 溢出**——每条 draft 只看其 category 同分类存量（≤50 条/分类截断由编排层保证）。

本 plan 不落库、不调 IPC、不脱敏（脱敏交 Plan 01 piiMask，落库交 Plan 03 IPC 经 createExperience）——纯「构建 prompt → 调 LLM → 校验 → 返回」的可测函数。Plan 03 负责阶段 A+B 串联 + 落库 + UI。

Purpose: 落实 D-01（LLM JSON 强 schema，schema Drift Gate 在代码层）+ D-04 起草反幻觉（不输出执行命令）+ SC1/SC4（产出 1~N 条草稿 / 分类枚举锁死 / 缺数据标 gap）+ W-4 两阶段窄化复判（D-02 正确性 + 防 context 溢出）。
Output: 1 个函数式 service 含 4 导出函数（draftSession/judgeVerdicts/validateDrafts/buildDraftingPrompt，带单测，mock callAI）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md

# Phase 7 基线（callAI 契约 + ExperienceCategory 枚举复用）
@electron/services/ai.ts
@electron/services/experienceService.ts
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/INTEGRATIONS.md

<interfaces>
<!-- callAI 契约（ai.ts:229，本 plan import 调用，不改签名，D-01 红线） -->
```typescript
// electron/services/ai.ts（已存在，本 plan 仅 import callAI + getAiConfig）
export async function callAI(
  config: Record<string, string>,
  messages: Array<{ role: string; content: string }>
): Promise<string>
// 注意：返回 string（非 streaming，非 JSON mode）—— LLM 返回的字符串可能是 JSON 数组也可能是带 markdown 包裹的 JSON，
// 本 service 必须 JSON.parse 前先剥离 ```json ... ``` 包裹（D-01 schema Drift Gate 在代码层）。

export function getAiConfig(): Record<string, string> | null
//   返回 { provider, apiKey, baseUrl, modelName, ... }；apiKey 为空 → 返回 null 或 apiKey='' → 起草层 DEMO_MODE 降级
```

```typescript
// electron/services/experienceService.ts（已存在，本 plan 仅 import 分类枚举 + ExperienceAttrs 模板）
export type ExperienceCategory = 'troubleshooting' | 'best_practices' | 'product' | 'env'
export interface ExperienceAttrs {
  symptoms?: string
  root_cause?: string
  resolution?: string
  prevention?: string
  severity?: 'critical' | 'high' | 'medium' | 'low' | 'info'
}
```

```typescript
// electron/utils/piiMask.ts（Plan 01 产物，wave1 同波——本 plan 在 wave1，piiMask 由 03 串接时调用；
//   本 plan 起草 service 假设入参 maskedConversation 已脱敏，不重复脱敏，避免双重替换误伤）
// 实际：Plan 03 IPC 层负责调 maskConversationText 后传入 draftSession.input.maskedConversation。
export function maskConversationText(text: string): string
```

```typescript
// electron/services/duplicateDetector.ts（Plan 01 产物，wave1 同波——Plan 03 编排层调 findExistingForDraft
//   按 draft.category 窄查后传给 judgeVerdicts.input.existingByCategory；本 plan 不直接 import duplicateDetector）
export interface ExistingExperienceSummary { exp_id: string; title: string; content_preview: string }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: draftingService 函数式（buildDraftingPrompt + validateDrafts + draftSession 纯起草 + judgeVerdicts 两阶段复判，TDD RED→GREEN）</name>
  <files>electron/services/draftingService.ts, electron/services/draftingService.test.ts</files>
  <read_first>
    - electron/services/ai.ts（callAI line 229 签名 + getAiConfig line 21；不改 callAI）
    - electron/services/experienceService.ts（ExperienceCategory 4 枚举 + ExperienceAttrs 模板字段：symptoms/root_cause/resolution/prevention/severity）
    - .planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md（D-01 prompt 强约束 + schema 校验 + 重试 N 次建议 2~3；D-03 ADD/UPDATE/NOOP 落库语义起草侧只产出 verdict 不落库；D-04 反幻觉不输出 [CMD]）
    - .planning/codebase/CONVENTIONS.md（函数式 service + 模块级常量 UPPER_SNAKE_CASE）
    - .planning/designs/2026-08-01-experience-summary-design.md §4 起草流程 + §6 错误处理（重试退避 + DEMO_MODE 降级）
  </read_first>
  <behavior>
    **buildDraftingPrompt(input)：** 返回 `{ system: string, user: string }`。
      - system prompt 含：(a) 角色定位「你是网络运维经验提炼助手」；(b) 反幻觉红线「**禁止输出 [CMD]/[KB_SEARCH] 等执行标记，禁止编造命令，禁止虚构分类/字段，缺数据必须标 "gap" 不强填」；(c) 分类固定枚举 troubleshooting/best_practices/product/env（逐字列出，禁止超出）；(d) 分类模板字段定义（troubleshooting 必填 symptoms/root_cause/resolution/severity；其他类 attrs 可为空对象）；(e) 输出格式契约「**严格输出 JSON 数组**，每条对象含字段：category/title/content/tags/attrs/confidence/reasoning/duplication_verdict/duplicate_of_exp_id，整个回复只能是 JSON 数组，不能有其它文字」；(f) 无可总结内容时「返回空数组 []」；(g) ADD/UPDATE/NOOP 判定规则（参考 existing 列表，命中标 duplicate_of_exp_id）。
      - user prompt 含：(a) 「以下是脱敏后的会话正文：」+ maskedConversation；(b) 「关联设备 id 列表：」+ deviceIds（可空）；(c) 「同分类+设备已有经验列表（标题+前150字摘要+exp_id）：」+ existingSummaries（可空——阶段 A 纯起草时为 []，LLM 自然全标 ADD；阶段 B 复判交 judgeVerdicts）；(d) 「请按 system 约束输出 JSON 数组」。

    **validateDrafts(raw)：** 输入 callAI 返回的 string，输出 `{ ok: true, drafts: DraftDraft[] } | { ok: false, error: string }`。
      - 剥离 ```json ... ``` 与首尾多余文字（找第一个 `[` 到最后一个 `]`）
      - JSON.parse 失败 → `{ ok:false, error:'JSON 解析失败' }`
      - 非数组 → `{ ok:false, error:'输出非数组' }`
      - 逐条校验：category ∈ 4 枚举（不在则该条丢弃或整体 fail——选整体 fail 触发重试）；title/content 非空字符串；attrs 字段（troubleshooting 类必填 severity 且 ∈ 5 枚举，缺/非法 → 该条 fail）；缺字段值 === 'gap' 视为合法（不强填）；duplication_verdict ∈ 'ADD'|'UPDATE'|'NOOP'；duplicate_of_exp_id 当 verdict='UPDATE' 必填、当 'ADD'/'NOOP' 必须为空/null；confidence ∈ [0,1] 数值（**W-2 边界**：若 LLM 返百分比字符串如 '85%' → parseFloat 去 % / 100 = 0.85 通过；若返非数值非百分比字符串如 'high' → parseFloat 得 NaN → fail 触发重试）→ 任一条 fail → `{ ok:false, error:'<字段> 校验失败：<detail>' }`（整体 fail 触发重试）

    **draftSession(input)（阶段 A 纯起草）：** 异步函数，返回 `Promise<DraftDraft[]>`。
      - input: `{ maskedConversation: string, deviceIds?: string[], existingSummaries: ExistingExperienceSummary[], demoMode?: boolean }`
      - 两阶段编排约定：阶段 A 时 existingSummaries 传 []（编排层 Plan 03 调用时不带查重结果，让 LLM 纯起草，verdict 自然全 ADD）→ 阶段 B 编排层按每条 draft.category 窄查后调 judgeVerdicts 复判覆盖 verdict。draftSession 本身仍支持带 existingSummaries 的单次判定模式（向后兼容，但 Plan 03 采用两阶段）。
      - demoMode=true（未配 AI / AI 不可用）→ 直接返回 []（不抛错，不阻塞；SC5 断点续传交 IPC 层处理）
      - 否则 getAiConfig() → config/apiKey 空 → 抛 `new Error('请先配置 AI 服务（API Key 未设置）')`（让 Plan 03 IPC secure 包装脱敏后透出 renderer）
      - 重试循环：最多 `MAX_DRAFT_RETRIES = 3` 次。每次：(a) buildDraftingPrompt → messages = [{role:'system',...},{role:'user',...}]；(b) `const raw = await callAI(config, messages)`；(c) `validateDrafts(raw)` → ok 则 return drafts；(d) !ok 记录最后一次 error，进下次重试
      - 全部重试失败 → throw `new Error('AI 起草失败（已重试 3 次）：' + lastError)`（IPC 层 secure 脱敏透出）

    **judgeVerdicts(input)（阶段 B 复判，W-4 两阶段核心）：** 异步函数，返回 `Promise<DraftDraft[]>`。
      - input: `{ drafts: DraftDraft[], existingByCategory: Record<ExperienceCategory, ExistingExperienceSummary[]>, demoMode?: boolean }`
      - 对 drafts 逐条：取 draft.category 对应的 existingByCategory[category]（同分类存量，编排层已 ≤50 条截断），构建独立复判 prompt 喂 LLM，让 LLM 对每条 draft 判 ADD/UPDATE/NOOP + 命中 exp_id
      - 复判 prompt（buildVerdictPrompt）：system「你是经验查重判定助手。对每条草稿，参考其同分类已有经验列表，判定 duplication_verdict（ADD/UPDATE/NOOP）+ duplicate_of_exp_id。严格输出 JSON 数组，每条含 draft_index(0-based) + verdict + duplicate_of_exp_id」；user 含 drafts[] 摘要（index+title+category+content 前80字）+ 按 category 分组的 existingByCategory 列表
      - 复判返回经 validateVerdicts 校验（每条 draft_index 有效 + verdict ∈ 枚举 + UPDATE/NOOP 时 dupId 必填 + ADD 时 dupId null），校验失败重试 MAX_DRAFT_RETRIES=3 次
      - 复判结果回填 drafts：按 draft_index 把新 verdict + dupId 覆盖原 draft（阶段 A 全 ADD 的初值被覆盖）；LLM 未返某 draft_index → 保持原 ADD 初值（保守新增）
      - demoMode=true → 直接返回原 drafts（verdict 保持阶段 A 初值，不调 LLM）

    **DraftDraft 类型：** category/title/content/tags/attrs/confidence/reasoning/duplication_verdict/duplicate_of_exp_id（与 LLM 输出字段 1:1，落库时由 Plan 03 映射到 createExperience ExperienceInput + duplicateOfExpId）

    单元覆盖（mock callAI + getAiConfig）：
      1. happy path：mock callAI 返合法 JSON 数组（1 条 troubleshooting + 1 条 product），断言 draftSession 返 2 条
      2. JSON 含 ```json 包裹 + 首尾多余文字 → validateDrafts 剥离后 ok
      3. callAI 返非数组 → 重试 3 次后 throw
      4. callAI 返数组但 category='random'（超枚举）→ 重试 3 次后 throw
      5. callAI 返 troubleshooting 缺 severity → fail 重试
      6. callAI 第一次返坏 JSON，第二次返合法 → 第 2 次成功（验证重试机制）
      7. callAI 返 [] 空数组（无可总结）→ draftSession 返 []（不强产，SC1）
      8. demoMode=true → 直接返 []，不调 callAI（验证 mock 未被调用，vi.fn toHaveBeenCalledTimes=0）
      9. getAiConfig 返 null → 抛「请先配置 AI 服务」
      10. ADD 草稿 duplicate_of_exp_id 必须为空、UPDATE 必填、NOOP 不落库（NOOP 由 Plan 03 IPC 层过滤不调 createExperience，本 service 仍返回 NOOP 条目供 UI 提示）
      11. **W-2 confidence 边界**：validateDrafts 收 `confidence: '85%'` → 转 0.85 通过；收 `confidence: 'high'`（非法）→ fail（触发重试）；收 `confidence: '0.9'`（字符串数值）→ 转 0.9 通过；收 `confidence: 1.5`（超界）→ fail
      12. **judgeVerdicts 复判**：mock callAI 返 `[{draft_index:0, verdict:'UPDATE', duplicate_of_exp_id:'exp-old-1'}]`，drafts=[{category:'troubleshooting',...}]，existingByCategory={troubleshooting:[{exp_id:'exp-old-1',...}],...}，断言复判后 drafts[0].duplication_verdict='UPDATE' 且 duplicate_of_exp_id='exp-old-1'
      13. **judgeVerdicts demoMode**：demoMode=true 不调 callAI，原 drafts 保持（verdict 不变）
  </behavior>
  <action>
    RED：先写 electron/services/draftingService.test.ts。用 `vi.mock('./ai', () => ({ callAI: vi.fn(), getAiConfig: vi.fn() }))` mock，每个 test 用 `mocked(callAI).mockResolvedValueOnce('<raw>')` / `mockRejectedValueOnce` 控制返回。覆盖上述 13 case（含 W-2 confidence 边界 4 sub-case + judgeVerdicts 复判 2 case）。

    GREEN：实现 electron/services/draftingService.ts（函数式，无 class、无 MK 持有——本 service 不读写加密列，纯调 LLM + 校验）：
    ```ts
    import { callAI, getAiConfig } from './ai'
    import type { ExperienceCategory, ExperienceAttrs } from './experienceService'

    /**
     * AI 起草 service（Phase 8 D-01/D-03/D-04 起草侧 + DRAFT-04 + W-4 两阶段复判）。
     *
     * 两阶段编排（W-4，D-02 窄化语义 + 防 context 溢出）：
     * - 阶段 A：draftSession 纯起草（existingSummaries=[]）→ drafts[]（verdict 全 ADD 初值）
     * - 阶段 B：judgeVerdicts 复判（编排层按每条 draft.category 窄查喂 LLM）→ 覆盖 verdict + dupId
     * 避免单次起草「4 枚举 category × 设备全量预查」context 溢出（最坏 4000 条摘要）。
     *
     * 不落库、不脱敏、不调 IPC——纯「prompt→LLM→校验→返回」可测函数。
     * 落库交 Plan 03 IPC 层（createExperience status='draft' + duplicateOfExpId）；脱敏交 Plan 01 piiMask（IPC 层先调）；
     * 查重交 Plan 01 duplicateDetector（编排层按 draft.category 窄查，结果传 judgeVerdicts.existingByCategory）。
     *
     * 反幻觉（D-04，红线）：prompt 明确禁止 [CMD]/[KB_SEARCH] 执行标记、禁止编造命令、分类不超枚举、缺数据标 'gap'。
     *
     * 函数式（无 class、无 MK）——本 service 不读写加密列，与 CONVENTIONS Pattern 1b（无加密列 service）一致。
     */

    export const MAX_DRAFT_RETRIES = 3

    const VALID_CATEGORIES: ExperienceCategory[] = ['troubleshooting', 'best_practices', 'product', 'env']
    const VALID_SEVERITIES = ['critical', 'high', 'medium', 'low', 'info']
    const VALID_VERDICTS = ['ADD', 'UPDATE', 'NOOP'] as const

    export interface ExistingExperienceSummary {
      exp_id: string
      title: string
      content_preview: string
    }

    export interface DraftDraft {
      category: ExperienceCategory
      title: string
      content: string
      tags: string[]
      attrs: ExperienceAttrs | Record<string, never>
      confidence: number
      reasoning: string
      duplication_verdict: 'ADD' | 'UPDATE' | 'NOOP'
      duplicate_of_exp_id: string | null
    }

    export interface DraftSessionInput {
      maskedConversation: string
      deviceIds?: string[]
      existingSummaries: ExistingExperienceSummary[]
      demoMode?: boolean
    }

    /** W-4 两阶段复判输入：drafts（阶段 A 产出）+ 按分类窄查的同分类存量映射（编排层填充）。 */
    export interface JudgeVerdictsInput {
      drafts: DraftDraft[]
      existingByCategory: Record<ExperienceCategory, ExistingExperienceSummary[]>
      demoMode?: boolean
    }

    const SYSTEM_PROMPT = [
      '你是网络运维经验提炼助手。回顾运维对话，提炼可复用经验。',
      '【反幻觉红线】禁止输出 [CMD]、[KB_SEARCH] 等执行标记；禁止编造命令；禁止虚构分类或字段；缺数据字段值必须填字符串 "gap"，严禁瞎编或强填。',
      '【分类固定枚举】只允许：troubleshooting、best_practices、product、env，禁止超出此枚举。',
      '【分类模板字段】',
      '- troubleshooting：attrs 必须含 severity（critical/high/medium/low/info），可含 symptoms/root_cause/resolution/prevention。',
      '- best_practices / product / env：attrs 可为空对象 {}。',
      '【判定规则】参考"已有经验列表"（阶段 A 通常为空，故全标 ADD；阶段 B 复判交 judgeVerdicts）。',
      '- ADD（新增，与存量不重复）→ duplicate_of_exp_id 必须为 null',
      '- UPDATE（命中存量需补充/更新）→ duplicate_of_exp_id 填命中 exp_id',
      '- NOOP（与存量重复，无新增价值）→ duplicate_of_exp_id 填命中 exp_id（提示跳过，不落库）',
      '【输出格式】严格输出 JSON 数组，不得有任何额外文字或解释。每条对象字段：',
      'category, title, content, tags(字符串数组), attrs(对象), confidence(0-1 数值), reasoning(字符串), duplication_verdict(ADD/UPDATE/NOOP), duplicate_of_exp_id(exp_id 字符串或 null)。',
      '若对话无可总结经验，返回空数组 []。',
    ].join('\n')

    export function buildDraftingPrompt(input: DraftSessionInput): { system: string; user: string } {
      const deviceLine = (input.deviceIds && input.deviceIds.length > 0)
        ? input.deviceIds.join(', ')
        : '（无关联设备）'
      const existingLine = input.existingSummaries.length > 0
        ? input.existingSummaries.map((s) => `- exp_id: ${s.exp_id} | 标题: ${s.title} | 摘要: ${s.content_preview}`).join('\n')
        : '（同分类+设备暂无存量——阶段 A 纯起草，全标 ADD；查重复判交 judgeVerdicts）'
      const user = [
        '以下是脱敏后的会话正文：',
        input.maskedConversation,
        '',
        `关联设备 id 列表：${deviceLine}`,
        '',
        '同分类+设备已有经验列表（标题+前150字摘要+exp_id）：',
        existingLine,
        '',
        '请按 system 约束输出 JSON 数组。',
      ].join('\n')
      return { system: SYSTEM_PROMPT, user }
    }

    /** 剥离 ```json 包裹与首尾多余文字，提取第一个 [ 到最后一个 ]。 */
    function extractJsonArray(raw: string): string {
      const first = raw.indexOf('[')
      const last = raw.lastIndexOf(']')
      if (first === -1 || last === -1 || last <= first) {
        throw new Error('输出未找到 JSON 数组边界')
      }
      return raw.slice(first, last + 1)
    }

    export function validateDrafts(raw: string): { ok: true; drafts: DraftDraft[] } | { ok: false; error: string } {
      let arr: any[]
      try {
        arr = JSON.parse(extractJsonArray(raw))
      } catch (e: any) {
        return { ok: false, error: 'JSON 解析失败: ' + (e?.message || String(e)) }
      }
      if (!Array.isArray(arr)) return { ok: false, error: '输出非数组' }

      const drafts: DraftDraft[] = []
      for (let i = 0; i < arr.length; i++) {
        const d = arr[i]
        if (!d || typeof d !== 'object') return { ok: false, error: `第 ${i + 1} 条非对象` }
        if (!VALID_CATEGORIES.includes(d.category)) return { ok: false, error: `第 ${i + 1} 条 category 非法: ${d.category}` }
        if (typeof d.title !== 'string' || !d.title.trim()) return { ok: false, error: `第 ${i + 1} 条 title 为空` }
        if (typeof d.content !== 'string' || !d.content.trim()) return { ok: false, error: `第 ${i + 1} 条 content 为空` }
        const tags = Array.isArray(d.tags) ? d.tags.filter((t: any) => typeof t === 'string') : []
        const attrs = (d.attrs && typeof d.attrs === 'object') ? d.attrs : {}
        if (d.category === 'troubleshooting') {
          if (!attrs.severity || !VALID_SEVERITIES.includes(attrs.severity)) {
            return { ok: false, error: `第 ${i + 1} 条 troubleshooting 缺合法 severity` }
          }
        }
        if (!VALID_VERDICTS.includes(d.duplication_verdict)) {
          return { ok: false, error: `第 ${i + 1} 条 duplication_verdict 非法: ${d.duplication_verdict}` }
        }
        const verdict: 'ADD' | 'UPDATE' | 'NOOP' = d.duplication_verdict
        const dupId = d.duplicate_of_exp_id
        if (verdict === 'ADD') {
          if (dupId !== null && dupId !== undefined && dupId !== '') {
            return { ok: false, error: `第 ${i + 1} 条 ADD 时 duplicate_of_exp_id 必须为 null` }
          }
        } else {
          if (typeof dupId !== 'string' || !dupId.trim()) {
            return { ok: false, error: `第 ${i + 1} 条 ${verdict} 时 duplicate_of_exp_id 必须填命中 exp_id` }
          }
        }
        // W-2 confidence 边界：'85%' → 0.85；'0.9' → 0.9；'high' → NaN fail；1.5 → 超界 fail
        let confidence = d.confidence
        if (typeof confidence === 'string') {
          confidence = confidence.endsWith('%') ? parseFloat(confidence) / 100 : parseFloat(confidence)
        }
        if (typeof confidence !== 'number' || isNaN(confidence) || confidence < 0 || confidence > 1) {
          return { ok: false, error: `第 ${i + 1} 条 confidence 非法（须 0-1 数值，支持 '85%' 百分比或 '0.85' 字符串）: ${d.confidence}` }
        }
        drafts.push({
          category: d.category,
          title: d.title,
          content: d.content,
          tags,
          attrs,
          confidence,
          reasoning: typeof d.reasoning === 'string' ? d.reasoning : '',
          duplication_verdict: verdict,
          duplicate_of_exp_id: verdict === 'ADD' ? null : String(dupId),
        })
      }
      return { ok: true, drafts }
    }

    export async function draftSession(input: DraftSessionInput): Promise<DraftDraft[]> {
      if (input.demoMode) return []
      const config = getAiConfig()
      if (!config || !config.apiKey) {
        throw new Error('请先配置 AI 服务（API Key 未设置）')
      }
      const { system, user } = buildDraftingPrompt(input)
      const messages = [
        { role: 'system', content: system },
        { role: 'user', content: user },
      ]
      let lastError = 'unknown'
      for (let attempt = 1; attempt <= MAX_DRAFT_RETRIES; attempt++) {
        const raw = await callAI(config, messages)
        const result = validateDrafts(raw)
        if (result.ok) return result.drafts
        lastError = result.error
      }
      throw new Error(`AI 起草失败（已重试 ${MAX_DRAFT_RETRIES} 次）：${lastError}`)
    }

    // ---------- W-4 阶段 B：judgeVerdicts 两阶段复判 ----------

    const VERDICT_SYSTEM_PROMPT = [
      '你是经验查重判定助手。对每条草稿，参考其同分类已有经验列表，判定 duplication_verdict。',
      '【判定规则】',
      '- ADD（与同分类存量不重复）→ duplicate_of_exp_id = null',
      '- UPDATE（命中同分类存量需补充/更新）→ duplicate_of_exp_id 填命中 exp_id',
      '- NOOP（与同分类存量重复，无新增价值）→ duplicate_of_exp_id 填命中 exp_id',
      '【输出格式】严格输出 JSON 数组，每条对象含：draft_index(草稿在输入 drafts[] 中的 0-based 下标), verdict(ADD/UPDATE/NOOP), duplicate_of_exp_id(exp_id 或 null)。',
      '每条输入草稿必须输出一条判定，不得遗漏。',
    ].join('\n')

    /** 构建复判 prompt：drafts[] 摘要（index+title+category+content 前80字）+ 按 category 分组的同分类存量。 */
    export function buildVerdictPrompt(input: JudgeVerdictsInput): { system: string; user: string } {
      const draftsLine = input.drafts.map((d, i) =>
        `- draft_index: ${i} | category: ${d.category} | 标题: ${d.title} | 内容前80字: ${d.content.slice(0, 80)}`
      ).join('\n')
      const existingBlocks = (Object.keys(input.existingByCategory) as ExperienceCategory[])
        .filter((cat) => input.existingByCategory[cat].length > 0)
        .map((cat) => {
          const sums = input.existingByCategory[cat]
            .map((s) => `  - exp_id: ${s.exp_id} | 标题: ${s.title} | 摘要: ${s.content_preview}`)
            .join('\n')
          return `【分类 ${cat}】同分类已有经验：\n${sums}`
        })
        .join('\n\n')
      const user = [
        '以下是待判定的草稿列表（按 draft_index 引用）：',
        draftsLine,
        '',
        '以下是按草稿自身 category 分组的同分类已有经验（仅参考草稿同分类项判定，跨分类不匹配）：',
        existingBlocks || '（全部分类暂无存量——所有草稿判 ADD）',
        '',
        '请按 system 约束对每条草稿输出判定 JSON 数组。',
      ].join('\n')
      return { system: VERDICT_SYSTEM_PROMPT, user }
    }

    interface VerdictEntry {
      draft_index: number
      verdict: 'ADD' | 'UPDATE' | 'NOOP'
      duplicate_of_exp_id: string | null
    }

    /** 校验复判返回：每条 draft_index 有效 + verdict 枚举 + UPDATE/NOOP 时 dupId 必填 + ADD 时 null。 */
    export function validateVerdicts(raw: string, draftCount: number): { ok: true; entries: VerdictEntry[] } | { ok: false; error: string } {
      let arr: any[]
      try {
        arr = JSON.parse(extractJsonArray(raw))
      } catch (e: any) {
        return { ok: false, error: '复判 JSON 解析失败: ' + (e?.message || String(e)) }
      }
      if (!Array.isArray(arr)) return { ok: false, error: '复判输出非数组' }
      const entries: VerdictEntry[] = []
      const seen = new Set<number>()
      for (let i = 0; i < arr.length; i++) {
        const v = arr[i]
        if (!v || typeof v !== 'object') return { ok: false, error: `复判第 ${i + 1} 条非对象` }
        const idx = typeof v.draft_index === 'number' ? v.draft_index : parseInt(v.draft_index, 10)
        if (!Number.isInteger(idx) || idx < 0 || idx >= draftCount) {
          return { ok: false, error: `复判第 ${i + 1} 条 draft_index 越界: ${v.draft_index}` }
        }
        if (seen.has(idx)) return { ok: false, error: `复判 draft_index=${idx} 重复` }
        seen.add(idx)
        if (!VALID_VERDICTS.includes(v.verdict)) {
          return { ok: false, error: `复判 draft_index=${idx} verdict 非法: ${v.verdict}` }
        }
        const verdict: 'ADD' | 'UPDATE' | 'NOOP' = v.verdict
        const dupId = v.duplicate_of_exp_id
        if (verdict === 'ADD') {
          if (dupId !== null && dupId !== undefined && dupId !== '') {
            return { ok: false, error: `复判 draft_index=${idx} ADD 时 duplicate_of_exp_id 必须为 null` }
          }
        } else {
          if (typeof dupId !== 'string' || !dupId.trim()) {
            return { ok: false, error: `复判 draft_index=${idx} ${verdict} 时 duplicate_of_exp_id 必须填命中 exp_id` }
          }
        }
        entries.push({ draft_index: idx, verdict, duplicate_of_exp_id: verdict === 'ADD' ? null : String(dupId) })
      }
      return { ok: true, entries }
    }

    /**
     * W-4 阶段 B 复判：对 drafts[] 每条按其 category 同分类存量判 ADD/UPDATE/NOOP + 命中 exp_id。
     * 编排层（Plan 03）按每条 draft.category 调 findExistingForDraft 窄查后填充 existingByCategory（≤50 条/分类截断）。
     * demoMode=true → 不调 LLM，原 drafts 保持（verdict 维持阶段 A 初值）。
     * 复判失败重试 MAX_DRAFT_RETRIES=3 次；LLM 未返某 draft_index → 该条保守保持原 ADD 初值。
     */
    export async function judgeVerdicts(input: JudgeVerdictsInput): Promise<DraftDraft[]> {
      if (input.demoMode) return input.drafts
      // 无任何同分类存量 → 全 ADD，不调 LLM（短路优化）
      const hasAny = (Object.values(input.existingByCategory) as ExistingExperienceSummary[][]).some((arr) => arr.length > 0)
      if (!hasAny) return input.drafts

      const config = getAiConfig()
      if (!config || !config.apiKey) {
        throw new Error('请先配置 AI 服务（API Key 未设置）')
      }
      const { system, user } = buildVerdictPrompt(input)
      const messages = [
        { role: 'system', content: system },
        { role: 'user', content: user },
      ]
      let lastError = 'unknown'
      for (let attempt = 1; attempt <= MAX_DRAFT_RETRIES; attempt++) {
        const raw = await callAI(config, messages)
        const result = validateVerdicts(raw, input.drafts.length)
        if (result.ok) {
          // 回填：按 draft_index 覆盖 verdict + dupId；未覆盖的保守保持原值
          const out = input.drafts.map((d) => ({ ...d }))
          for (const e of result.entries) {
            out[e.draft_index].duplication_verdict = e.verdict
            out[e.draft_index].duplicate_of_exp_id = e.duplicate_of_exp_id
          }
          return out
        }
        lastError = result.error
      }
      throw new Error(`AI 复判失败（已重试 ${MAX_DRAFT_RETRIES} 次）：${lastError}`)
    }
    ```
    跑测试至全绿后 commit。
  </action>
  <verify>
    <automated>npx vitest run electron/services/draftingService.test.ts</automated>
  </verify>
  <done>
    - grep -c "export const MAX_DRAFT_RETRIES = 3" electron/services/draftingService.ts = 1
    - grep -c "export function buildDraftingPrompt" electron/services/draftingService.ts = 1
    - grep -c "export function validateDrafts" electron/services/draftingService.ts = 1
    - grep -c "export async function draftSession" electron/services/draftingService.ts = 1
    - grep -c "export async function judgeVerdicts" electron/services/draftingService.ts = 1（W-4 两阶段复判函数落地）
    - grep -c "export function buildVerdictPrompt" electron/services/draftingService.ts = 1
    - grep -c "export function validateVerdicts" electron/services/draftingService.ts = 1
    - grep -c "JudgeVerdictsInput" electron/services/draftingService.ts ≥ 2（interface + 函数入参）
    - grep -c "import.*callAI.*getAiConfig.*from.*'\\./ai'" electron/services/draftingService.ts = 1（callAI 签名不改，D-01）
    - grep -cE "禁止输出 \[CMD\]" electron/services/draftingService.ts = 1（反幻觉 prompt 落地，D-04）
    - grep -c "troubleshooting、best_practices、product、env" electron/services/draftingService.ts = 1（分类枚举锁死进 prompt，DRAFT-04）
    - grep -cE "EXP-DRAFT-JSON|raw\\.json|response_format" electron/services/draftingService.ts = 0（不改 callAI 签名、不依赖 provider JSON mode，反向守卫）
    - grep -cE "\\b(encrypt|decrypt)\\(" electron/services/draftingService.ts = 0（不涉加密，反向守卫）
    - grep -c "85%" electron/services/draftingService.test.ts ≥ 1（W-2 confidence 百分比边界单测落地）
    - grep -c "'high'" electron/services/draftingService.test.ts ≥ 1（W-2 confidence 非法字符串边界单测落地）
    - draftingService.test.ts 全 PASS（13 case：happy/包裹剥离/非数组重试/超枚举重试/缺 severity 重试/第2次成功/空数组不强产/demoMode 直接返/未配 AI 抛错/ADD-UPDATE-NOOP dupId 规则 + W-2 confidence 边界 4 sub-case + judgeVerdicts 复判 2 case）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider → draftingService.validateDrafts / validateVerdicts | LLM 返回的 raw string 是不可信外部输入（可能含注入标记/超枚举分类/瞎编命令），必须经代码层 schema 校验，分类锁死/字段校验/反 [CMD] |
| draftingService → Plan 03 IPC 落库 | draftSession / judgeVerdicts 返回的 DraftDraft[] 不直接落库，Plan 03 IPC 层经 createExperience（status='draft' 硬编码 + duplicateOfExpId）落库，draft 永不自动 published（红线③） |
| 编排层 existingByCategory → judgeVerdicts prompt | 编排层按分类窄查 + 每分类 ≤50 条截断喂 judgeVerdicts，防 4×1000 context 溢出（W-4） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering（LLM 越权输出 [CMD] 执行命令） | SYSTEM_PROMPT 反幻觉 + validateDrafts content 不解析命令 | mitigate | prompt 明确「禁止输出 [CMD]/[KB_SEARCH]」；validateDrafts 只取 title/content 字符串不执行；draft 落 status=draft 永不自动 published；commandSafety 联动交 Phase 11（本 phase 不实现但 prompt 已反幻觉） |
| T-08-07 | Tampering（LLM 输出超枚举分类/瞎编字段） | validateDrafts 分类枚举锁死 + 模板字段校验 | mitigate | VALID_CATEGORIES 4 枚举严格 includes 校验；troubleshooting severity 5 枚举校验；duplication_verdict ADD/UPDATE/NOOP 枚举；confidence 0-1 数值校验（含 W-2 百分比/字符串转换边界）；任一 fail 整体重试 |
| T-08-08 | Tampering（LLM 返回非 JSON / 混杂 markdown） | extractJsonArray + JSON.parse + 重试 | mitigate | 剥离 ```json 包裹与首尾多余文字提取 [..]，JSON.parse 失败 → 重试 MAX_DRAFT_RETRIES=3；不依赖 provider JSON mode（D-01） |
| T-08-09 | Information Disclosure（会话明文泄露 LLM provider） | draftSession 入参 maskedConversation | accept-but-mitigated | 入参须由 Plan 03 IPC 层先调 piiMask.maskConversationText 脱敏后传入（D-04，T-08-01）；本 service 不重复脱敏避免双重替换误伤；attrs 凭证片段由 createExperience attrs_enc 加密落盘（Phase 7） |
| T-08-10 | Denial of Service（LLM 限流/超时致总结丢失） | draftSession/judgeVerdicts 重试 + Plan 03 source_session_id 幂等 | mitigate | 失败重试 3 次（无指数退避避免叠加等待）；source_session_id 幂等断点续传在 IPC 层（SC5，Plan 03 落地） |
| T-08-11 | Elevation of Privilege（draft 越权改 status/audit） | draftSession/judgeVerdicts 不落库 | mitigate | 两函数仅返回 DraftDraft[]，不经 updateExperience；Plan 03 落库只走 createExperience（status='draft' 硬编码 + duplicateOfExpId 门面写入，Phase 7 CR-01 已收紧 update 白名单） |
| T-08-20 | Denial of Service（单次起草 4×1000 存量摘要 context 溢出） | judgeVerdicts 两阶段窄化复判 | mitigate | W-4 两阶段：阶段 A 纯起草不喂存量；阶段 B 按每条 draft.category 窄查喂同分类存量（编排层 ≤50 条/分类截断），最坏 prompt 存量段 ≤ 50×4×150字 = 30KB，可控；judgeVerdicts 短路优化（全分类无存量时不调 LLM 直接返全 ADD） |

</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` EXIT 0（noUnusedLocals 全绿，VALID_VERDICTS/VALID_CATEGORIES/judgeVerdicts 等全被使用）
- `npm run build:electron-main` EXIT 0（draftingService 经 esbuild bundle）
- `npx vitest run` 全 PASS（既有 75 + Plan 01 新增 + draftingService 新增 13 case 含 W-2 边界 + judgeVerdicts，不破坏基线）
- callAI 签名零改动（grep 反向守卫无 response_format/json_object 注入）
</verification>

<success_criteria>
- draftSession 函数式落地（阶段 A 纯起草），调 callAI 不改签名，prompt 强约束 JSON（分类枚举/模板/反幻觉/gap）
- judgeVerdicts 两阶段复判函数落地（阶段 B），按 draft.category 窄化喂同分类存量，W-4 D-02 正确性 + 防 context 溢出
- validateDrafts 代码层 schema Drift Gate（D-01），分类枚举锁死、troubleshooting severity 强制、duplication_verdict 校验、ADD-UPDATE dupId 规则、W-2 confidence 边界（'85%'/'high'/超界）
- 重试 3 次机制（D-01），LLM 返 [] 空数组不强产（SC1），demoMode 降级不阻塞
- 三绿门禁全绿，既有测试不破坏
</success_criteria>

<output>
完成后创建 `.planning/phases/08-ai-drafting-pipeline/08-02-SUMMARY.md`，记录：draftingService 导出函数清单（draftSession 阶段A纯起草 / judgeVerdicts 阶段B复判 / validateDrafts / buildDraftingPrompt / buildVerdictPrompt / validateVerdicts）+ DraftDraft 字段契约（供 Plan 03 落库映射 createExperience + duplicateOfExpId）+ JudgeVerdictsInput 契约（编排层按 draft.category 窄查填充 existingByCategory）+ SYSTEM_PROMPT 反幻觉要点 + W-2 confidence 边界规则 + 重试策略 + 偏差（若有）。
</output>
