---
phase: 08-ai-drafting-pipeline
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - electron/services/experienceDrafting.ts
  - electron/services/experienceDrafting.test.ts
  - electron/ipc/experienceDraftingIpc.ts
  - electron/preload.ts
  - electron/main.ts
  - src/types/experience.ts
  - src/types/electron.d.ts
  - src/components/pages/ai/ChatInput.tsx
  - src/components/pages/ai/useAIChat.ts
  - src/components/pages/AIPage.tsx
autonomous: true
requirements: [DRAFT-01, DRAFT-02, DRAFT-03, DRAFT-04]

must_haves:
  truths:
    - "用户在 AI 对话窗点「经验总结」按钮后，触发 experience:summarizeSession IPC（secure 包装），异步执行：读 chat_history → PII 脱敏 → 两阶段起草（阶段A纯起草 + 阶段B按分类窄查复判）→ 落 draft 库"
    - "按钮在会话有内容时可点，点击转 loading，异步起草完成弹通知（antd message），无可总结内容提示「该会话无可总结经验」而非强产空条目"
    - "送 LLM 前会话正文经 piiMask.maskConversationText 脱敏（凭证/IP/MAC），LLM 永不收明文凭证"
    - "ADD 草稿经 createExperience(status='draft') 落库；UPDATE 草稿经 createExperience(status='draft', duplicateOfExpId=命中旧id) 单语句原子落库（标注与 draft 行共存亡，B-2）；NOOP 不落库只记提示"
    - "编排层不裸 SQL 写 duplicate_of_exp_id（B-1），唯一写入路径是 experienceService.createExperience 门面（grep 反向守卫 = 0）"
    - "两阶段起草（W-4）：阶段 A draftSession(existingSummaries=[]) 纯起草得 drafts[] → 阶段 B 按每条 draft.category 调 findExistingForDraft 窄查（≤50 条/分类截断）→ judgeVerdicts 复判 verdict + dupId → 落库"
    - "同一 session 可多次总结（追加不覆盖），每次总结独立批次（source_session_id 相同但生成独立 draft 行）"
    - "main.ts 经 setExperienceMasterKey 注入（Phase 7 已注入，drafting 走 callAI 复用 setAiMasterKey，不需新注入）"
    - "experience:summarizeSession channel 全 secure 包装（鉴权 + 异常脱敏），未登录 throw「未登录或会话已过期」"
    - "renderer 经 window.api.experience.summarizeSession(sessionId) 调用，channel 与 ipcMain.handle 第一参逐字一致"
  artifacts:
    - path: "electron/services/experienceDrafting.ts"
      provides: "summarizeSessionForUi 函数式（两阶段编排：读会话→脱敏→阶段A起草→阶段B窄查复判→落库→返回 DraftingResult）"
      exports: ["summarizeSessionForUi"]
    - path: "electron/services/experienceDrafting.test.ts"
      provides: "SC5 追加不覆盖 + sourceSessionId 透传 + NOOP 不落库 单测（W-3）"
    - path: "electron/ipc/experienceDraftingIpc.ts"
      provides: "registerExperienceDraftingIpc：experience:summarizeSession channel secure 包装"
      exports: ["registerExperienceDraftingIpc"]
    - path: "electron/preload.ts"
      provides: "experience.summarizeSession 白名单方法"
    - path: "src/types/electron.d.ts"
      provides: "experience.summarizeSession 类型声明 + DraftingResult 类型"
    - path: "src/components/pages/ai/ChatInput.tsx"
      provides: "「经验总结」按钮（会话有内容可点，转 loading）"
  key_links:
    - from: "electron/ipc/experienceDraftingIpc.ts"
      to: "electron/services/experienceDrafting.ts summarizeSessionForUi"
      via: "import 调用"
      pattern: "import.*summarizeSessionForUi.*from.*'\\.\\./services/experienceDrafting'"
    - from: "electron/services/experienceDrafting.ts"
      to: "electron/services/ai.ts getChatHistory"
      via: "import 读会话明文"
      pattern: "import.*getChatHistory.*from.*'./ai'"
    - from: "electron/services/experienceDrafting.ts"
      to: "electron/utils/piiMask.ts maskConversationText"
      via: "import 脱敏"
      pattern: "import.*maskConversationText.*from.*'\\.\\./utils/piiMask'"
    - from: "electron/services/experienceDrafting.ts"
      to: "electron/services/duplicateDetector.ts findExistingForDraft"
      via: "import 阶段B窄查"
      pattern: "import.*findExistingForDraft.*from.*'./duplicateDetector'"
    - from: "electron/services/experienceDrafting.ts"
      to: "electron/services/draftingService.ts draftSession + judgeVerdicts"
      via: "import 两阶段起草"
      pattern: "import.*draftSession.*judgeVerdicts.*from.*'./draftingService'"
    - from: "electron/services/experienceDrafting.ts"
      to: "electron/services/experienceService.ts createExperience（含 duplicateOfExpId）+ relateDevice"
      via: "import 门面落库（不裸 SQL）"
      pattern: "import.*createExperience.*from.*'./experienceService'"
    - from: "src/components/pages/ai/ChatInput.tsx"
      to: "window.api.experience.summarizeSession"
      via: "useAIChat.handleSummarize 调用"
      pattern: "summarizeSession"
---

<objective>
Phase 8 端到端串联落地（D-01/02/03/04 四需求 + 5 条 SC 全覆盖 + B-1/B-2/W-3/W-4 修订）：

把 Plan 01（piiMask + duplicateDetector + createExperience 扩展）+ Plan 02（draftingService 含两阶段 draftSession/judgeVerdicts）+ Phase 7（createExperience 门面 + secure IPC）串成完整 pipeline，并暴露给 renderer：

1. **`experienceDrafting.ts`** 编排 service（**W-4 两阶段起草**）：
   - 读 chat_history（ai.ts getChatHistory 解密明文）→ PII 脱敏（Plan 01 piiMask）→ 关联设备收集（chat_sessions.device_id + chat_history.device_id 去重）
   - **阶段 A**：调 `draftSession({ maskedConversation, deviceIds, existingSummaries: [] })` 纯起草得 drafts[]（verdict 全 ADD 初值）
   - **阶段 B**：对 drafts[] 涉及的每个 distinct category，调 `findExistingForDraft({ category, deviceIds })` 窄查同分类+设备存量（≤50 条/分类截断），组装 `existingByCategory` 映射，调 `judgeVerdicts({ drafts, existingByCategory })` 复判覆盖 verdict + dupId
   - 落库（**B-1 + B-2 方案 A**）：ADD → `createExperience({..., status='draft' 隐含})`；UPDATE → `createExperience({..., duplicateOfExpId: 命中旧id})` 单语句原子（标注与 draft 行共存亡，失败即 throw 中断该条 draft，不吞错）；NOOP 跳过只记提示
   - 返回 `DraftingResult{ created:[], updated:[], noop:[], empty:boolean, demoMode:boolean }`
2. **`experienceDraftingIpc.ts`** IPC 网关：`experience:summarizeSession` channel 经 `secure` 包装。
3. **preload/electron.d.ts/main.ts** 暴露 + 注册。
4. **AIPage UI**：「经验总结」按钮（ChatInput 区）+ useAIChat 加 handleSummarize（loading 状态 + antd message 通知 + 无可总结提示）。
5. **`experienceDrafting.test.ts`**（W-3）：编排 service 最小单测——SC5 同 sessionId 两次总结各生独立 id 行（追加不覆盖）+ sourceSessionId 透传到 createExperience 入参 + NOOP 不调 createExperience。

本 plan 是 phase 8 唯一触碰 IPC 边界与 renderer 的 plan，红线（secure 包装 / status='draft' 硬编码不经 update / 脱敏前置 / 不裸 SQL 写 dup_id）全在此落地。

Purpose: 让用户点「经验总结」按钮即可端到端跑通 pipeline，产出 draft 态草稿入库（Phase 9 确认弹窗的输入），完整覆盖 SC1~SC5。
Output: 1 编排 service + 1 编排单测 + 1 IPC 模块 + preload/main 注入 + 1 DTO + 1 按钮 + useAIChat 扩展。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md

# Phase 7 IPC/preload/main 基线（强依赖，复刻其模式）
@electron/ipc/experienceIpc.ts
@electron/preload.ts
@electron/main.ts
@electron/utils/authGuard.ts
@electron/services/experienceService.ts
@electron/services/experienceService.test.ts
@electron/services/ai.ts
@src/types/experience.ts
@src/types/electron.d.ts
@src/components/pages/AIPage.tsx
@src/components/pages/ai/useAIChat.ts
@src/components/pages/ai/ChatInput.tsx
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Plan 01 + 02 产物（wave1 已建，本 plan import 调用） -->
```typescript
// electron/utils/piiMask.ts（Plan 01）
export function maskConversationText(text: string): string

// electron/services/duplicateDetector.ts（Plan 01）
export function findExistingForDraft(input: { category: ExperienceCategory; deviceIds?: string[] }): ExistingExperienceSummary[]
//   返回 [{ exp_id, title, content_preview(≤150字) }]，自动过滤已失效
//   W-4 阶段 B：编排层按每条 draft.category 调此函数窄查同分类+设备存量

// electron/services/draftingService.ts（Plan 02，含 W-4 两阶段）
export async function draftSession(input: {
  maskedConversation: string
  deviceIds?: string[]
  existingSummaries: ExistingExperienceSummary[]   // 阶段 A 传 []
  demoMode?: boolean
}): Promise<DraftDraft[]>
//   DraftDraft = { category, title, content, tags, attrs, confidence, reasoning,
//                  duplication_verdict: 'ADD'|'UPDATE'|'NOOP', duplicate_of_exp_id: string|null }
//   阶段 A 纯起草：existingSummaries=[] → drafts[]（verdict 全 ADD 初值）
//   demoMode=true → 返 []；未配 AI → throw；重试 3 次失败 → throw

export async function judgeVerdicts(input: {
  drafts: DraftDraft[]
  existingByCategory: Record<ExperienceCategory, ExistingExperienceSummary[]>
  demoMode?: boolean
}): Promise<DraftDraft[]>
//   W-4 阶段 B 复判：按每条 draft.category 同分类存量判 ADD/UPDATE/NOOP + 命中 exp_id
//   回填覆盖原 drafts verdict + dupId；未返的 draft_index 保守保持原 ADD 初值
//   全分类无存量 → 短路不调 LLM 直接返原 drafts（全 ADD）
//   demoMode=true → 不调 LLM 直接返原 drafts
```

```typescript
// electron/services/ai.ts（Phase 7 已存在，本 plan import 读会话明文）
export function getChatHistory(sessionId?: string): Array<{
  id: string; role: string; content: string; deviceId: string | null; createdAt: string
}>
//   content 已 decField 解密为明文（送 LLM 前必须经 piiMask 脱敏）

// electron/services/experienceService.ts（Phase 7 + Plan 01 Task 4 扩展，本 plan import 门面落库）
export function createExperience(input: ExperienceInput): any
//   ExperienceInput = { title, category, content, tags?, sourceSessionId?, attrs?, duplicateOfExpId? }
//   status 默认 'draft'（硬编码）；duplicateOfExpId 不传/传 null → 写 NULL（向后兼容）
//   单语句原子：duplicate_of_exp_id 列与 draft 行同 INSERT 写入（B-2 共存亡）
//   B-1：编排层唯一写 dup_id 路径，不裸 SQL UPDATE
export function relateDevice(experienceId: string, deviceId: string, relationType?: string): void
```

```typescript
// electron/utils/authGuard.ts（Phase 7 已存在，本 plan import secure 包装）
export function secure(handler: (e: any, ...args: any[]) => any): (e: any, ...args: any[]) => Promise<any>
```

```typescript
// chat_sessions 表（init.ts:78）：{ id, title, device_id, created_at }
// chat_history 表（init.ts:69）：{ id, role, content_enc, device_id, session_id, created_at }
// experiences 表（Phase 7 + Plan 01 v9）：新增 duplicate_of_exp_id TEXT nullable（经 createExperience 门面写）
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: experienceDrafting.ts 两阶段编排 service（读会话→脱敏→阶段A起草→阶段B窄查复判→门面落库→返回 DraftingResult）+ W-3 单测</name>
  <files>electron/services/experienceDrafting.ts, electron/services/experienceDrafting.test.ts</files>
  <read_first>
    - electron/services/ai.ts（getChatHistory line 192 读会话明文 + getAiConfig line 21 判 demoMode）
    - electron/services/experienceService.ts（createExperience line 165 扩展后签名含 duplicateOfExpId + relateDevice line 317 + ExperienceInput 字段；Plan 01 Task 4 已加 duplicateOfExpId 可选参数）
    - electron/services/experienceService.test.ts（mock DB + _setExperienceDbGetter 模式，本 service mock createExperience/relateDevice 函数即可）
    - electron/utils/piiMask.ts（Plan 01 maskConversationText）
    - electron/services/duplicateDetector.ts（Plan 01 findExistingForDraft 签名 + ExistingExperienceSummary）
    - electron/services/draftingService.ts（Plan 02 draftSession 阶段A + judgeVerdicts 阶段B + DraftDraft + JudgeVerdictsInput）
    - .planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md（D-02 查重 / D-03 ADD/UPDATE/NOOP 落库 / SC5 source_session_id 幂等追加不覆盖 / SC1 无可总结不强产）
    - .planning/codebase/CONVENTIONS.md（Service 封装红线：编排层不裸 SQL，落库必经 service 门面）
  </read_first>
  <behavior>
    - summarizeSessionForUi(sessionId) 异步编排：
      1. 读 sessionId 全部会话明文（getChatHistory），空会话 → 返 empty=true 不强产
      2. 判 demoMode（getAiConfig null/apiKey 空）→ 返 empty=true + demoMode=true（UI 提示去配置）
      3. PII 脱敏（maskConversationText），原始 messages 不动
      4. 关联设备收集（chat_history.device_id 去重）
      5. **阶段 A**：调 draftSession({ maskedConversation, deviceIds, existingSummaries: [] }) 纯起草得 drafts[]
      6. SC1：drafts.length === 0 → 返 empty=true（无可总结不强产）
      7. **阶段 B**：对 drafts[] 涉及的 distinct categories，逐个调 findExistingForDraft({ category, deviceIds }) 窄查（每分类 ≤50 条截断防 context 溢出），组装 existingByCategory: Record<category, summaries>，调 judgeVerdicts({ drafts, existingByCategory }) 复判覆盖 verdict + dupId
      8. 落库（D-03 + B-1/B-2）：
         - NOOP → 不调 createExperience，noop[] 记提示
         - ADD → createExperience({ title, category, content, tags, sourceSessionId: sessionId, attrs })，关联设备
         - UPDATE → createExperience({ ..., duplicateOfExpId: d.duplicate_of_exp_id }) 单语句原子写 dup_id（**不裸 SQL UPDATE**，**不 try/catch 吞错**——CREATE 失败即 throw 中断该条 draft 落库，保证标注与 draft 行共存亡 B-2）
    - W-3 单测覆盖（mock draftingService.draftSession + judgeVerdicts + experienceService.createExperience + relateDevice）：
      (a) SC5 追加不覆盖：同 sessionId 两次 summarize，mock createExperience 每次返不同 uuid，断言两次各生独立行（不被覆盖）
      (b) sourceSessionId 透传：断言 createExperience 入参含 sourceSessionId === sessionId
      (c) NOOP 不落库：mock drafts 含 1 条 NOOP，断言 createExperience 调用次数 = 0（仅 NOOP 时）
      (d) UPDATE 透传 dup_id：mock drafts 含 1 条 UPDATE（dup_id='exp-old-1'），断言 createExperience 入参含 duplicateOfExpId='exp-old-1'
      (e) demoMode：getAiConfig 返 null → 不调 draftSession，返 empty + demoMode=true
  </behavior>
  <action>
    RED：先写 electron/services/experienceDrafting.test.ts（vitest）。mock 三层依赖：
    ```ts
    vi.mock('./ai', () => ({
      getChatHistory: vi.fn(),
      getAiConfig: vi.fn(),
    }))
    vi.mock('./draftingService', () => ({
      draftSession: vi.fn(),
      judgeVerdicts: vi.fn(),
      MAX_DRAFT_RETRIES: 3,
    }))
    vi.mock('./duplicateDetector', () => ({
      findExistingForDraft: vi.fn(),
    }))
    vi.mock('./experienceService', () => ({
      createExperience: vi.fn(),
      relateDevice: vi.fn(),
    }))
    vi.mock('../utils/piiMask', () => ({
      maskConversationText: vi.fn((t: string) => t),  // 透传，测试不验脱敏细节
    }))
    ```
    覆上述 (a)~(e) 5 case：
    - (a) SC5：mocked(createExperience).mockResolvedValueOnce({id:'e1',...}).mockResolvedValueOnce({id:'e2',...})，两次 summarizeSessionForUi({sessionId:'s1'})，断言两次 created/updated 行 id 不同（追加不覆盖）。
    - (b) 透传：expect(mocked(createExperience)).toHaveBeenCalledWith(expect.objectContaining({ sourceSessionId: 's1' }))。
    - (c) NOOP：mock draftSession 返 [{duplication_verdict:'NOOP', duplicate_of_exp_id:'old', ...}]，mock judgeVerdicts 透传同值，断言 createExperience toHaveBeenCalledTimes(0) + result.noop.length===1。
    - (d) UPDATE：mock drafts 含 UPDATE dup_id='exp-old-1'，断言 createExperience 入参 objectContaining({ duplicateOfExpId: 'exp-old-1' })。
    - (e) demoMode：mocked(getAiConfig).mockReturnValue(null)，断言 draftSession 未被调 + result.demoMode===true。

    GREEN：实现 electron/services/experienceDrafting.ts（函数式编排 service，无 class、无 MK 持有——调用面加密列已由下游 service 处理）：

    ```ts
    import { getChatHistory, getAiConfig } from './ai'
    import { maskConversationText } from '../utils/piiMask'
    import { findExistingForDraft } from './duplicateDetector'
    import { draftSession, judgeVerdicts } from './draftingService'
    import type { ExistingExperienceSummary, DraftDraft } from './draftingService'
    import { createExperience, relateDevice } from './experienceService'
    import type { ExperienceCategory, ExperienceAttrs } from './experienceService'

    /**
     * 经验总结编排 service（Phase 8 D-01/02/03/04 + 5 SC + B-1/B-2/W-3/W-4 修订）。
     *
     * W-4 两阶段起草（避免 4×1000 context 溢出 + D-02 窄化语义）：
     * - 阶段 A：draftSession(existingSummaries=[]) 纯起草 → drafts[]（verdict 全 ADD 初值）
     * - 阶段 B：按 drafts 涉及的 distinct category 调 findExistingForDraft 窄查（≤50 条/分类截断）
     *           → existingByCategory 映射 → judgeVerdicts 复判覆盖 verdict + dupId
     *
     * D-03 落库语义（B-1 + B-2 方案 A，单语句原子）：
     * - ADD → createExperience({..., sourceSessionId}) + relateDevice；duplicateOfExpId 不传（写 NULL）
     * - UPDATE → createExperience({..., sourceSessionId, duplicateOfExpId: 命中旧id}) 单语句原子写 dup_id
     *   （CREATE 失败即 throw 中断该条 draft，标注与 draft 行共存亡 B-2；不 try/catch 吞错）
     * - NOOP → 不落库，noop[] 提示
     *
     * B-1 Service 封装红线：编排层不裸 SQL UPDATE duplicate_of_exp_id，唯一写入路径是
     *   experienceService.createExperience 门面（grep 反向守卫 = 0）。
     *
     * SC1：draftSession 返 [] → result.empty=true，IPC/UI 提示「该会话无可总结经验」不强产。
     * SC5：同 session 多次总结独立批次（每次生独立 draft 行，source_session_id 相同但行 id 不同，追加不覆盖）。
     *
     * demoMode：getAiConfig 返 null 或 apiKey 空 → 视为未配 AI，返 empty + demoMode=true 不抛错。
     *
     * 函数式（无 class、无 MK）——加密列由下游 experienceService/ai.ts 处理，本 service 纯编排。
     */

    /** W-4 阶段 B 每分类窄查的存量截断上限（防 context 溢出）。 */
    const MAX_EXISTING_PER_CATEGORY = 50

    export interface DraftingResult {
      empty: boolean                // SC1：无可总结内容（draftSession 返 [] 或 demoMode）
      demoMode: boolean             // 是否走了 demoMode 降级（未配 AI）
      created: Array<{ exp_id: string; title: string; category: ExperienceCategory }>      // ADD 落库
      updated: Array<{ exp_id: string; title: string; category: ExperienceCategory; duplicate_of_exp_id: string }>  // UPDATE 落库
      noop: Array<{ duplicate_of_exp_id: string; reasoning: string }>                       // NOOP 跳过提示
    }

    /** 把脱敏会话明文按对话格式拼成单段文本（role: content 每行）。 */
    function buildConversationText(messages: Array<{ role: string; content: string; deviceId: string | null }>): string {
      return messages.map((m) => `${m.role}: ${m.content}`).join('\n')
    }

    /** 收集会话关联设备 id（chat_history.device_id 去重，过滤 null）。 */
    function collectDeviceIds(messages: Array<{ role: string; content: string; deviceId: string | null }>): string[] {
      const set = new Set<string>()
      for (const m of messages) {
        if (m.deviceId) set.add(m.deviceId)
      }
      return Array.from(set)
    }

    export interface SummarizeSessionInput {
      sessionId: string
    }

    export async function summarizeSessionForUi(input: SummarizeSessionInput): Promise<DraftingResult> {
      const { sessionId } = input
      const empty: DraftingResult = { empty: true, demoMode: false, created: [], updated: [], noop: [] }

      // 1. 读会话明文（getChatHistory 已 decField 解密）
      const messages = getChatHistory(sessionId)
      if (messages.length === 0) return empty

      // 2. 判 demoMode（未配 AI）—— 不抛错，UI 提示
      const config = getAiConfig()
      const demoMode = !config || !config.apiKey
      if (demoMode) return { ...empty, demoMode: true }

      // 3. PII 脱敏（D-04，T-08-01）—— 送 LLM 前主进程做，原始 chat_history 明文不动
      const conversationText = buildConversationText(messages)
      const maskedConversation = maskConversationText(conversationText)

      // 4. 关联设备收集
      const deviceIds = collectDeviceIds(messages)

      // 5. W-4 阶段 A：纯起草（existingSummaries=[] → verdict 全 ADD 初值，查重复判交阶段 B）
      const draftsA: DraftDraft[] = await draftSession({ maskedConversation, deviceIds, existingSummaries: [] })

      // 6. SC1：无可总结不强产
      if (draftsA.length === 0) return empty

      // 7. W-4 阶段 B：按 drafts 涉及的 distinct category 窄查同分类存量，组装 existingByCategory
      const distinctCategories = Array.from(new Set(draftsA.map((d) => d.category))) as ExperienceCategory[]
      const existingByCategory = {} as Record<ExperienceCategory, ExistingExperienceSummary[]>
      for (const cat of distinctCategories) {
        const sums = findExistingForDraft({ category: cat, deviceIds })
        existingByCategory[cat] = sums.slice(0, MAX_EXISTING_PER_CATEGORY)  // ≤50 条/分类截断防 context 溢出
      }
      // 复判覆盖 verdict + dupId（judgeVerdicts 内部短路：全分类无存量时直接返原 drafts 不调 LLM）
      const drafts: DraftDraft[] = await judgeVerdicts({ drafts: draftsA, existingByCategory })

      // 8. 落库（D-03 + B-1/B-2 方案 A）
      const created: DraftingResult['created'] = []
      const updated: DraftingResult['updated'] = []
      const noop: DraftingResult['noop'] = []

      for (const d of drafts) {
        if (d.duplication_verdict === 'NOOP') {
          noop.push({ duplicate_of_exp_id: d.duplicate_of_exp_id || '', reasoning: d.reasoning })
          continue
        }
        // B-1 + B-2 方案 A：经门面 createExperience 落库，UPDATE 时传 duplicateOfExpId 单语句原子写 dup_id
        // 不 try/catch 吞错——CREATE 失败即 throw 中断该条 draft，标注与 draft 行共存亡（B-2）
        const exp = createExperience({
          title: d.title,
          category: d.category,
          content: d.content,
          tags: d.tags,
          sourceSessionId: sessionId,    // SC5：source_session_id 溯源，同 session 多次总结生独立行
          attrs: (d.category === 'troubleshooting') ? (d.attrs as ExperienceAttrs) : null,
          duplicateOfExpId: d.duplication_verdict === 'UPDATE' ? d.duplicate_of_exp_id : null,
        })
        // 关联设备（draft 关联的设备 = 会话期间涉及的设备，全部 primary 关联）
        for (const did of deviceIds) {
          try { relateDevice(exp.id, did, 'primary') } catch { /* 设备关联失败不阻塞 draft 入库，日志兜底；relateDevice 独立于 createExperience 原子单元，关联缺失可 Phase 10 手动补 */ }
        }
        if (d.duplication_verdict === 'ADD') {
          created.push({ exp_id: exp.id, title: exp.title, category: exp.category })
        } else {
          // UPDATE：duplicate_of_exp_id 已由 createExperience 单语句原子写入 v9 新列（B-1 门面 + B-2 原子）
          updated.push({ exp_id: exp.id, title: exp.title, category: exp.category, duplicate_of_exp_id: d.duplicate_of_exp_id || '' })
        }
      }

      return { empty: false, demoMode: false, created, updated, noop }
    }
    ```

    关键决策落地：
    - **W-4 两阶段**：阶段 A draftSession(existingSummaries=[]) 纯起草 → 阶段 B 按 drafts 的 distinct category 窄查 + judgeVerdicts 复判。避免单次起草「4 枚举 × 设备全量预查」context 溢出，复用 08-01 findExistingForDraft 窄化检索粒度（D-02 同 category + 同 deviceId）。
    - **B-1 + B-2 方案 A**：UPDATE 经 createExperience({duplicateOfExpId}) 单语句原子写 dup_id，不裸 SQL UPDATE（B-1 门面红线），不 try/catch 吞错（B-2 共存亡——CREATE 失败即 throw 中断该条 draft）。relateDevice 的 try/catch 保留因设备关联独立于 dup_id 原子单元（关联缺失可 Phase 10 手动补，不影响 draft 行 + dup_id 已共存亡）。
    - **SC1**：`messages.length === 0`（空会话）或 `draftsA.length === 0`（LLM 判无可总结）→ `empty=true`，UI 提示「该会话无可总结经验」，不强产。
    - **SC5**：同 sessionId 多次总结，每次 createExperience 生独立行（uuid 不同），source_session_id 相同——追加不覆盖。
    - **D-04**：`maskConversationText` 在送 draftSession（即 LLM）前调用，原始 messages 不动。
    - **D-03**：ADD/UPDATE 都走 createExperience（status='draft' 硬编码，不经 updateExperience，红线③）。
    跑测试至全绿后 commit。
  </action>
  <verify>
    <automated>npx vitest run electron/services/experienceDrafting.test.ts</automated>
  </verify>
  <done>
    - grep -c "export async function summarizeSessionForUi" electron/services/experienceDrafting.ts = 1
    - grep -c "export interface DraftingResult" electron/services/experienceDrafting.ts = 1
    - grep -c "maskConversationText" electron/services/experienceDrafting.ts = 1（D-04 脱敏前置落地）
    - grep -c "findExistingForDraft" electron/services/experienceDrafting.ts ≥ 1（W-4 阶段B窄查复用，D-02）
    - grep -c "draftSession" electron/services/experienceDrafting.ts ≥ 1（W-4 阶段A起草复用）
    - grep -c "judgeVerdicts" electron/services/experienceDrafting.ts ≥ 1（W-4 阶段B复判复用）
    - grep -c "existingSummaries: \[\]" electron/services/experienceDrafting.ts ≥ 1（W-4 阶段A纯起草，不带存量）
    - grep -c "createExperience" electron/services/experienceDrafting.ts ≥ 1（D-03 落库走 create，status='draft' 硬编码于 experienceService）
    - grep -c "duplicateOfExpId" electron/services/experienceDrafting.ts ≥ 1（B-1 + B-2 方案 A：门面传 dup_id 单语句原子）
    - grep -c "sourceSessionId: sessionId" electron/services/experienceDrafting.ts ≥ 1（SC5 source_session_id 溯源）
    - grep -c "MAX_EXISTING_PER_CATEGORY = 50" electron/services/experienceDrafting.ts = 1（W-4 每分类截断防 context 溢出）
    - grep -cE "UPDATE experiences SET duplicate_of_exp_id" electron/services/experienceDrafting.ts = 0（B-1 反向守卫：编排层不裸 SQL 写 dup_id，唯一路径是 createExperience 门面）
    - grep -cE "getDatabase\\(\\)\\.prepare" electron/services/experienceDrafting.ts = 0（B-1 反向守卫：编排层不直接 getDatabase 裸 SQL，全经 service 门面）
    - grep -cE "updateExperience\\(" electron/services/experienceDrafting.ts = 0（红线③：不经 update 改 status/audit，反向守卫）
    - grep -cE "\\b(encrypt|decrypt)\\(" electron/services/experienceDrafting.ts = 0（不裸调加密，反向守卫；加密经下游 service）
    - experienceDrafting.test.ts 全 PASS（W-3：SC5 追加不覆盖 + sourceSessionId 透传 + NOOP 不落库 + UPDATE 透传 dup_id + demoMode 5 case）
  </done>
</task>

<task type="auto">
  <name>Task 2: experienceDraftingIpc.ts（experience:summarizeSession secure 包装）+ preload 暴露 + main.ts 注册 + DTO/类型声明</name>
  <files>electron/ipc/experienceDraftingIpc.ts, electron/preload.ts, electron/main.ts, src/types/experience.ts, src/types/electron.d.ts</files>
  <read_first>
    - electron/ipc/experienceIpc.ts（Phase 7 secure 包装 + ipcMain.handle 模式，本 plan 复刻）
    - electron/preload.ts line 124-135（experience 块 10 方法，本 plan 追加 summarizeSession 第 11 个）
    - electron/main.ts line 28-29（import setExperienceMasterKey/registerExperienceIpc）+ line 136（registerExperienceIpc 调用点），本 plan 在其后追加 registerExperienceDraftingIpc
    - src/types/electron.d.ts line 202-213（experience 块 10 方法声明，本 plan 追加 summarizeSession + DraftingResult）
    - src/types/experience.ts（追加 DraftingResult DTO）
    - electron/utils/authGuard.ts（secure 包装语义：未登录 throw「未登录或会话已过期」在 try 之外）
  </read_first>
  <action>
    1. 创建 electron/ipc/experienceDraftingIpc.ts（复刻 experienceIpc.ts secure 模式）：
       ```ts
       import { ipcMain } from 'electron'
       import { summarizeSessionForUi } from '../services/experienceDrafting'
       import { secure } from '../utils/authGuard'

       /**
        * Experience 起草 IPC 网关（Phase 8 Plan 03）。
        *
        * 安全红线（延续 Phase 7 SEC-01/02）：
        * - experience:summarizeSession 经 secure 鉴权 + 异常脱敏包装。起草属登录后特权操作
        *   （读 chat_history 明文 + 调 LLM + 落 draft），无登录前场景。
        * - 返回 DraftingResult 不含会话原文（仅含落库 draft 的 exp_id/title/category + NOOP 提示），
        *   会话原文回链交 Phase 9（renderer 永不收会话明文经此 channel）。
        * - 异常（未配 AI / LLM 限流 / 重试耗尽）经 secure sanitizeMessage 脱敏后透出 renderer。
        *
        * channel 命名遵循全仓 camelCase 事实约定（与 experience:list/listDevices 一致）。
        */

       export function registerExperienceDraftingIpc() {
         ipcMain.handle('experience:summarizeSession', secure((_e, sessionId: string) => {
           if (!sessionId || typeof sessionId !== 'string') {
             throw new Error('sessionId 无效')
           }
           return summarizeSessionForUi({ sessionId })
         }))
       }
       ```

    2. 在 electron/preload.ts 的 experience 块（line 124-135），在 `listDevices` 之后追加第 11 个方法：
       ```ts
       summarizeSession: (sessionId: string) => ipcRenderer.invoke('experience:summarizeSession', sessionId),
       ```

    3. 在 electron/main.ts：
       - 顶部 import 区（line 28-29 附近，紧跟 registerExperienceIpc import 之后）追加：
         ```ts
         import { registerExperienceDraftingIpc } from './ipc/experienceDraftingIpc'
         ```
       - 在 line 136 `registerExperienceIpc()` 之后追加：
         ```ts
         registerExperienceDraftingIpc()
         ```
       （注意：不需要新 setXxxMasterKey 注入——drafting 走 callAI 复用 setAiMasterKey，落库复用 setExperienceMasterKey，二者 Phase 7 已注入。）

    4. 在 src/types/experience.ts 末尾追加 DraftingResult DTO（与 service 层 DraftingResult 字段一致，renderer 类型安全）：
       ```ts
       /** experience:summarizeSession 返回（Phase 8 Plan 03）。renderer 不收会话原文，仅收 draft 落库结果。 */
       export interface DraftingResult {
         empty: boolean
         demoMode: boolean
         created: Array<{ exp_id: string; title: string; category: ExperienceCategory }>
         updated: Array<{ exp_id: string; title: string; category: ExperienceCategory; duplicate_of_exp_id: string }>
         noop: Array<{ duplicate_of_exp_id: string; reasoning: string }>
       }
       ```
       （注：Phase 8 起草 service 层 DraftingResult 无 retryExhausted 字段——draftSession/judgeVerdicts 失败即 throw 经 secure 脱敏透出，不静默返 retryExhausted=true；DTO 与 service 字段对齐，不加冗余字段。）

    5. 在 src/types/electron.d.ts：
       - line 10 import 追加 `DraftingResult`：
         ```ts
         import type { Experience, ExperienceInput, ExperienceUpdateInput, ExperienceListResult, ExperienceListInput, ExperienceRelatedDevice, DraftingResult } from './experience'
         ```
       - experience 块（line 202-213）在 `listDevices` 之后追加第 11 个方法声明：
         ```ts
         summarizeSession: (sessionId: string) => Promise<DraftingResult>
         ```
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npm run build:electron-main</automated>
  </verify>
  <done>
    - grep -c "ipcMain.handle('experience:summarizeSession'" electron/ipc/experienceDraftingIpc.ts = 1
    - grep -c "secure(" electron/ipc/experienceDraftingIpc.ts ≥ 1（secure 包装落地，SEC-01）
    - grep -c "export function registerExperienceDraftingIpc" electron/ipc/experienceDraftingIpc.ts = 1
    - grep -c "summarizeSession:" electron/preload.ts = 1
    - grep -c "import { registerExperienceDraftingIpc }" electron/main.ts = 1
    - grep -c "registerExperienceDraftingIpc()" electron/main.ts = 1（紧跟 registerExperienceIpc() 之后）
    - grep -c "export interface DraftingResult" src/types/experience.ts = 1
    - grep -c "summarizeSession:" src/types/electron.d.ts = 1
    - ipc↔preload channel 三向逐字一致：`grep -oE "'experience:[a-zA-Z]+'" electron/ipc/experienceDraftingIpc.ts electron/ipc/experienceIpc.ts electron/preload.ts | sort -u` 含 'experience:summarizeSession'
    - tsc strict + electron-main build 双绿
  </done>
</task>

<task type="auto">
  <name>Task 3: AIPage「经验总结」按钮 + useAIChat.handleSummarize + 异步 loading + 完成通知 + 无可总结提示</name>
  <files>src/components/pages/ai/ChatInput.tsx, src/components/pages/ai/useAIChat.ts, src/components/pages/AIPage.tsx</files>
  <read_first>
    - src/components/pages/ai/ChatInput.tsx（现有 TextArea + 发送 Button，本 plan 追加「经验总结」Button）
    - src/components/pages/ai/useAIChat.ts（现有 8 状态 + 7 handler，本 plan 追加 summarizing 状态 + handleSummarize handler；return 块 line 164-182 追加）
    - src/components/pages/AIPage.tsx（ChatInput 调用面 line 89-94，本 plan 透传 summarizing/handleSummarize + onSummarize）
    - src/types/electron.d.ts（experience.summarizeSession 类型声明，Task 2 已加）
    - antd message（useAIChat 已 import `import { message } from 'antd'`，复用通知）
    - .planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md（SC1 无可总结提示 / design §5 按钮 loading + 完成弹通知）
  </read_first>
  <action>
    1. 修改 src/components/pages/ai/ChatInput.tsx：在 props 接口追加 `summarizing?: boolean`、`onSummarize?: () => void`，并在 TextArea/Button 行下方追加「经验总结」按钮（会话有内容才可点，复用 antd Button + ThunderboltOutlined 图标）：
       ```tsx
       import { Input, Button } from 'antd'
       import { SendOutlined, ThunderboltOutlined } from '@ant-design/icons'

       const { TextArea } = Input

       interface ChatInputProps {
         value: string
         loading: boolean
         onChange: (v: string) => void
         onSend: () => void
         summarizing?: boolean
         onSummarize?: () => void
         canSummarize?: boolean   // 会话有内容才可点
       }

       export default function ChatInput({ value, loading, onChange, onSend, summarizing, onSummarize, canSummarize }: ChatInputProps) {
         return (
           <div style={{ marginTop: 12, display: 'flex', gap: 8, alignItems: 'flex-end' }}>
             <TextArea
               value={value}
               onChange={(e) => onChange(e.target.value)}
               placeholder="输入消息... (Enter 发送, Shift+Enter 换行)"
               autoSize={{ minRows: 1, maxRows: 4 }}
               onPressEnter={(e) => {
                 if (!e.shiftKey) { e.preventDefault(); onSend() }
               }}
               disabled={loading}
             />
             <Button type="primary" icon={<SendOutlined />} onClick={onSend} loading={loading} disabled={!value.trim()}>
               发送
             </Button>
             {onSummarize && (
               <Button
                 icon={<ThunderboltOutlined />}
                 onClick={onSummarize}
                 loading={summarizing}
                 disabled={!canSummarize || loading || summarizing}
               >
                 经验总结
               </Button>
             )}
           </div>
         )
       }
       ```

    2. 修改 src/components/pages/ai/useAIChat.ts：
       - 追加状态：`const [summarizing, setSummarizing] = useState(false)`
       - 追加 handler `handleSummarize`（在 handleConfirm 之后、return 之前）：
         ```ts
         const handleSummarize = useCallback(async () => {
           if (!currentSessionId || summarizing) return
           setSummarizing(true)
           try {
             const result = await window.api.experience.summarizeSession(currentSessionId)
             if (result.demoMode) {
               message.warning('未配置 AI 服务，无法总结（请先在「系统设置」配置 AI）')
             } else if (result.empty) {
               message.info('该会话无可总结经验')
             } else {
               const parts: string[] = []
               if (result.created.length > 0) parts.push(`新增 ${result.created.length} 条草稿`)
               if (result.updated.length > 0) parts.push(`更新 ${result.updated.length} 条草稿`)
               if (result.noop.length > 0) parts.push(`跳过 ${result.noop.length} 条重复`)
               message.success(`经验总结完成：${parts.join('，')}（草稿待确认）`)
             }
           } catch (e: unknown) {
             message.error(e instanceof Error ? e.message : String(e))
           } finally {
             setSummarizing(false)
           }
         }, [currentSessionId, summarizing])
         ```
       - return 块追加：`summarizing,` 和 `handleSummarize,`
       - 同时计算 canSummarize：在 return 中追加 `canSummarize: messages.length > 0,`（会话有内容才可点，SC1 强约束）。

    3. 修改 src/components/pages/AIPage.tsx：在 ChatInput 调用面（line 89-94）透传新 props：
       ```tsx
       <ChatInput
         value={chat.input}
         loading={chat.loading}
         onChange={chat.setInput}
         onSend={chat.handleSend}
         summarizing={chat.summarizing}
         onSummarize={chat.handleSummarize}
         canSummarize={chat.canSummarize}
       />
       ```

    关键决策：
    - **SC1**：`canSummarize = messages.length > 0`（空会话按钮 disabled），且 LLM 返 empty → message.info「该会话无可总结经验」（不强产空条目）。
    - **异步 loading**：summarizing 状态控制按钮 loading + disabled，防重复点击。
    - **完成通知**：antd message.success 汇总 created/updated/noop 计数，提示「草稿待确认」（Phase 9 弹窗入口）。
    - **demoMode**：未配 AI 时 message.warning 提示用户去配置（不崩溃）。
    - 不在 phase 8 做确认弹窗（Phase 9）—— 本 plan 只通知「草稿待确认」计数，draft 详情浏览交 Phase 9/10。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit</automated>
  </verify>
  <done>
    - grep -c "经验总结" src/components/pages/ai/ChatInput.tsx = 1（按钮文案落地）
    - grep -c "ThunderboltOutlined" src/components/pages/ai/ChatInput.tsx = 1（按钮图标）
    - grep -c "summarizing" src/components/pages/ai/useAIChat.ts ≥ 3（state 声明 + handler 引用 + return）
    - grep -c "handleSummarize" src/components/pages/ai/useAIChat.ts ≥ 2（定义 + return）
    - grep -c "window.api.experience.summarizeSession" src/components/pages/ai/useAIChat.ts = 1（IPC 调用）
    - grep -c "该会话无可总结经验" src/components/pages/ai/useAIChat.ts = 1（SC1 无可总结提示）
    - grep -c "草稿待确认" src/components/pages/ai/useAIChat.ts = 1（完成通知）
    - grep -c "canSummarize" src/components/pages/AIPage.tsx = 1（透传）
    - grep -c "summarizing={chat.summarizing}" src/components/pages/AIPage.tsx = 1
    - tsc strict EXIT 0（无 noUnusedLocals 报错）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → experience:summarizeSession IPC | renderer 调用必须经 secure 包装（鉴权 + 异常脱敏），sessionId 入参校验非空字符串 |
| main 进程 chat_history 明文 → piiMask → LLM | 会话明文驻留 main（已被 at-rest 加密），脱敏只针对送 LLM 的副本；renderer 经此 channel 只收 DraftingResult（不含会话原文） |
| LLM 输出 → draftingService.validateDrafts/validateVerdicts → 门面落库 | LLM 输出经 Plan 02 schema 校验，draft 落 status='draft'（不经 update 改 status/audit），UPDATE 经 createExperience 门面 duplicateOfExpId 单语句原子写 dup_id（不裸 SQL） |
| 编排层 existingByCategory → judgeVerdicts prompt | 阶段 B 按分类窄查 + 每分类 ≤50 条截断喂 judgeVerdicts，防 4×1000 context 溢出（W-4） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Spoofing（IPC 鉴权绕过） | experience:summarizeSession | mitigate | secure 包装（鉴权 + 异常脱敏），未登录 throw「未登录或会话已过期」在 try 之外不被脱敏覆盖；sessionId 非空字符串校验 |
| T-08-13 | Information Disclosure（会话明文泄露 renderer） | summarizeSessionForUi 返回值 | mitigate | DraftingResult 只含 exp_id/title/category/duplicate_of_exp_id/reasoning，不含会话原文；renderer 经此 channel 永不收 chat_history 明文（会话原文回链交 Phase 9） |
| T-08-14 | Information Disclosure（PII 泄露 LLM provider） | summarizeSessionForUi maskConversationText 调用顺序 | mitigate | maskConversationText 在调 draftSession/judgeVerdicts（送 LLM）前执行（D-04），原始 messages 不动；T-08-01 脱敏 util 落地保证 |
| T-08-15 | Elevation of Privilege / 架构违规（编排层裸 SQL 写 duplicate_of_exp_id 绕门面） | summarizeSessionForUi 落库路径 | mitigate | B-1 修订：duplicate_of_exp_id 唯一写入路径是 createExperience 门面（duplicateOfExpId 可选参数，单语句原子 INSERT 含该列）；编排层 grep 反向守卫 `UPDATE experiences SET duplicate_of_exp_id` = 0 + `getDatabase().prepare` = 0；不经 updateExperience（Phase 7 CR-01 白名单不含 status/audit/有效期）|
| T-08-16 | Tampering（标注丢失：UPDATE draft 入库但 dup_id 未写） | summarizeSessionForUi UPDATE 落库 | mitigate | B-2 修订：createExperience 单语句原子写 dup_id（CREATE + dup_id 同 INSERT），不 try/catch 吞错——CREATE 失败即 throw 中断该条 draft 落库，标注与 draft 行共存亡；SC3 UPDATE 标注命中条目 + D-03a Phase 9 据 draft 定位旧条目双重保障 |
| T-08-17 | Denial of Service（LLM 限流/超时致总结丢失） | summarizeSessionForUi 重试 + source_session_id 幂等 | mitigate | draftSession/judgeVerdicts 内部各重试 3 次；source_session_id 溯源支持用户重复点按钮重跑（SC5，追加不覆盖，每次生独立 draft 行） |
| T-08-18 | Repudiation（起草操作无溯源） | createExperience sourceSessionId | mitigate | 每个 draft 落库必填 sourceSessionId=sessionId（SC5 溯源），Phase 9 确认弹窗可回链产生它的原始会话 |
| T-08-21 | Denial of Service（阶段 B 窄查存量爆 context） | summarizeSessionForUi existingByCategory 收集 | mitigate | W-4 修订：阶段 B 按 drafts 的 distinct category 调 findExistingForDraft 窄查，每分类 ≤50 条截断（MAX_EXISTING_PER_CATEGORY=50），最坏 prompt 存量段 ≤ 50×4×150字 = 30KB 可控；judgeVerdicts 短路优化（全分类无存量时不调 LLM） |

</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` EXIT 0（renderer + DTO 全绿，noUnusedLocals 通过）
- `npm run build:electron-main` EXIT 0（experienceDrafting + experienceDraftingIpc + main.ts 经 esbuild bundle）
- `npx vitest run` 全 PASS（既有基线 + Plan 01/02 新增 + experienceDrafting.test.ts W-3 新增 5 case 不破坏；编排 service 单测覆盖 SC5 追加不覆盖 + sourceSessionId 透传 + NOOP 不落库 + UPDATE 透传 dup_id + demoMode）
- ipc↔preload channel 三向逐字一致（grep + sort -u 校验）
</verification>

<success_criteria>
- experience:summarizeSession IPC secure 包装，preload 暴露 + main 注册 + DTO/类型声明全落地
- 编排 service 串联 Plan 01/02 + Phase 7（两阶段起草）：读会话→脱敏→阶段A纯起草→阶段B按分类窄查复判→门面落库→返 DraftingResult
- ADD/UPDATE 走 createExperience（status='draft'），UPDATE 经门面 duplicateOfExpId 单语句原子写 dup_id（B-1 不裸 SQL + B-2 共存亡，不吞错），NOOP 跳过
- W-4 两阶段起草落地：阶段 A draftSession(existingSummaries=[]) + 阶段 B findExistingForDraft 窄查 + judgeVerdicts 复判，每分类 ≤50 条截断防 context 溢出
- W-3 编排 service 单测覆盖 SC5 追加不覆盖 + sourceSessionId 透传 + NOOP 不落库
- AIPage「经验总结」按钮：会话有内容可点、loading、完成通知、无可总结提示、demoMode 提示
- SC1~SC5 全覆盖：1（产出/无可总结提示）2（PII 脱敏前置）3（查重 ADD/UPDATE/NOOP 经两阶段复判）4（Plan 02 强 schema 起草）5（source_session_id 幂等追加）
- 三绿门禁全绿，既有测试不破坏
</success_criteria>

<output>
完成后创建 `.planning/phases/08-ai-drafting-pipeline/08-03-SUMMARY.md`，记录：summarizeSessionForUi 两阶段编排契约（阶段A draftSession 纯起草 + 阶段B findExistingForDraft 窄查 + judgeVerdicts 复判）+ DraftingResult 字段 + B-1/B-2 门面落库路径（createExperience duplicateOfExpId 单语句原子，不裸 SQL 不吞错）+ W-3 单测覆盖 + IPC channel 清单（11 个 experience:* 含 summarizeSession）+ AIPage 按钮交互 + Phase 8 整体 SC1~SC5 落地证据 + 偏差（若有）。
</output>
