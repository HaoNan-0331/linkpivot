---
phase: 08-ai-drafting-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/migrations.ts
  - electron/database/init.ts
  - electron/utils/piiMask.ts
  - electron/utils/piiMask.test.ts
  - electron/services/duplicateDetector.ts
  - electron/services/duplicateDetector.test.ts
  - electron/services/experienceService.ts
  - electron/services/experienceService.test.ts
autonomous: true
requirements: [DRAFT-02, DRAFT-03]

must_haves:
  truths:
    - "experiences 表新增 duplicate_of_exp_id TEXT nullable 列，迁移幂等可重跑（hasColumn 守卫，重跑 no-op）"
    - "送 LLM 前的会话正文副本中，凭证（password/passwd/pwd/secret/token/apiKey/api_key/key/密码/口令/凭证 后跟值）正则全脱敏为 ****，IPv4 保留尾4，MAC 保留尾4"
    - "原始 chat_history 明文不被 PII 脱敏改动（脱敏只针对副本）"
    - "草稿关联设备时查「同 category + 同 deviceId（任一）」有效存量；无设备时查「同 category 全库」"
    - "查重返回「标题 + 内容前 150 字摘要 + exp_id」列表，供起草 LLM 判定 ADD/UPDATE/NOOP（无硬阈值）"
    - "experienceService.createExperience 接受可选 duplicateOfExpId，CREATE 语句单语句原子写入 duplicate_of_exp_id 列（B-1 不裸 SQL + B-2 原子性同源解决）"
  artifacts:
    - path: "electron/database/migrations.ts"
      provides: "v9 迁移：experiences 加 duplicate_of_exp_id TEXT（hasColumn 幂等守卫）"
      contains: "duplicate_of_exp_id"
    - path: "electron/database/init.ts"
      provides: "fresh-install experiences DDL 含 duplicate_of_exp_id 列（与迁移逐字一致）"
      contains: "duplicate_of_exp_id TEXT"
    - path: "electron/utils/piiMask.ts"
      provides: "maskConversationText 等导出（凭证/IPv4/MAC 分级脱敏）"
      exports: ["maskConversationText", "maskCredentials", "maskIpv4", "maskMac"]
    - path: "electron/services/duplicateDetector.ts"
      provides: "findExistingForDraft 函数式（查同分类+设备存量，返摘要列表）"
      exports: ["findExistingForDraft"]
    - path: "electron/services/experienceService.ts"
      provides: "createExperience 扩展签名（接受可选 duplicateOfExpId，CREATE 语句含该列单语句原子）"
      contains: "duplicateOfExpId"
  key_links:
    - from: "electron/services/duplicateDetector.ts"
      to: "experienceService.listExperiences"
      via: "import 复用 Phase 7 列表查询"
      pattern: "import.*listExperiences.*from.*experienceService"
    - from: "electron/services/experienceDrafting.ts（Plan 03）"
      to: "experienceService.createExperience（含 duplicateOfExpId）"
      via: "Plan 03 编排层经门面落库 + 标注 dup_id，不裸 SQL UPDATE"
      pattern: "createExperience.*duplicateOfExpId|createExperience.*\\{[^}]*duplicateOfExpId"
---

<objective>
Phase 8 起草 pipeline 的地基层落地：① v9 幂等迁移加 `experiences.duplicate_of_exp_id TEXT` 列（D-03a，支撑 UPDATE 命中关联 + 二期经验↔经验关联）；② PII 脱敏 util（D-04，送 LLM 前主进程做，分级凭证/IP/MAC）；③ 查重 service（D-02，同分类+设备优先，喂起草 LLM 摘要列表）；④ **扩展 `experienceService.createExperience` 接受可选 `duplicateOfExpId`（B-1 + B-2 方案 A，单语句原子写 duplicate_of_exp_id 列）**。

这四个产物是 Plan 02（LLM 起草 service）与 Plan 03（IPC + UI）的上游依赖，自身不触发 LLM、不落 draft（仅扩展落库门面签名），纯可测函数 + 一次性迁移 + 一次 service 签名扩展。

Purpose: 把 D-04（PII 红线）、D-02（查重判定）、D-03a（duplicate_of_exp_id 数据模型）三件基础工作前置，**并把 B-1（Service 封装红线：起草编排层不裸 SQL）+ B-2（原子性：标注与 draft 行共存亡）的解决方案前置到门面层**——让 Plan 03 编排层只调 `createExperience({..., duplicateOfExpId})` 一次调用即原子落库 + 标注，无需裸 UPDATE、无需 try/catch 吞错。
Output: 1 个迁移步骤 + 2 个纯函数 util/service（带单测）+ 1 处 experienceService.createExperience 签名扩展（带单测）。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md

# Phase 7 数据层基线（强依赖，复用其产物与模式）
@.planning/phases/07-experience-data-layer-security-baseline/07-01-SUMMARY.md
@.planning/phases/07-experience-data-layer-security-baseline/07-REVIEW-FIX.md
@electron/services/experienceService.ts
@electron/services/experienceService.test.ts
@electron/database/migrations.ts
@electron/database/migrationHelpers.ts
@electron/database/init.ts
@electron/utils/crypto.ts
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Phase 7 experienceService 函数式导出清单（查重 service 复用此调用面） -->
<!-- 提取自 electron/services/experienceService.ts，executor 不需重新探查 -->

```typescript
// electron/services/experienceService.ts（Phase 7 产物，已存在，本 plan Task 4 扩展 createExperience 签名）
export function listExperiences(opts: ListExperiencesOpts): PaginatedResult<any>
//   opts: { category?, status?, deviceId?, includeInvalid?, limit?, offset? }
//   返回 { rows: ExperienceRow[], total, truncated }
//   rows[] 每行含：id, title, category, content, tags(string[]), status, source_session_id,
//                  attrs, valid_at, invalid_at, last_verified_at, reuse_count, created_at, updated_at
//   注意：listExperiences 内部 MAX_BATCH=1000 强制，limit > MAX_BATCH throw
//   注意：默认 includeInvalid=false，自动过滤 `invalid_at IS NULL OR invalid_at > now`

// 当前签名（Phase 7，line 165）：
//   export function createExperience(input: ExperienceInput): any
//   input: { title, category, content, tags?, sourceSessionId?, attrs? }
//   INSERT INTO experiences (id, title, category, content, tags, status, source_session_id, attrs_enc, valid_at)
//     VALUES (?, ?, ?, ?, ?, 'draft', ?, ?, datetime('now','localtime'))
//   返回 getExperience(id)（status='draft' 硬编码）
//
// 本 plan Task 4 扩展后签名（B-1 + B-2 方案 A）：
//   export function createExperience(input: ExperienceInput): any
//   input 增可选字段 duplicateOfExpId?: string | null
//   INSERT INTO experiences (id, title, category, content, tags, status, source_session_id, attrs_enc, valid_at, duplicate_of_exp_id)
//     VALUES (?, ?, ?, ?, ?, 'draft', ?, ?, datetime('now','localtime'), ?)
//   第 10 个列 = input.duplicateOfExpId ?? null
//   单语句原子：CREATE 成功即标注同写入，失败 throw → 整条不落库（B-2 共存亡）
//   反向守卫：Plan 03 编排层 grep -c 'UPDATE experiences SET duplicate_of_exp_id' electron/services/experienceDrafting.ts = 0

export const MAX_BATCH = 1000
export type ExperienceCategory = 'troubleshooting' | 'best_practices' | 'product' | 'env'
```

```typescript
// electron/database/migrationHelpers.ts（已存在，本 plan 直接 import）
export function hasColumn(db: Database.Database, table: string, col: string): boolean
```

```typescript
// electron/database/migrations.ts（已存在 MIGRATION_HEAD=8 + v1..v8，本 plan 追加 v9）
export const MIGRATION_HEAD = 8  // ← 本 plan 改为 9
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v9 迁移加 experiences.duplicate_of_exp_id 列 + fresh-install DDL 同步</name>
  <files>electron/database/migrations.ts, electron/database/init.ts</files>
  <read_first>
    - electron/database/migrations.ts（v8 模式范例：sqlite_master sql-content 守卫 + db.transaction + MIGRATIONS 数组项）
    - electron/database/migrationHelpers.ts（hasColumn 签名）
    - electron/database/init.ts（experiences fresh-install DDL 块，含 attrs_enc/source_session_id 列）
    - .planning/codebase/CONVENTIONS.md §6 幂等迁移守卫（hasColumn / sqlite_master 特征串）
  </read_first>
  <behavior>
    - 迁移重跑幂等：已在 experiences 表含 duplicate_of_exp_id 列时 v9 为 no-op（不抛错、不重复 ALTER）
    - fresh-install 库（user_version=0）经 v9 后 experiences 表必含 duplicate_of_exp_id 列
    - 列 nullable TEXT，无 NOT NULL、无 DEFAULT、无 CHECK（不影响历史行，不动 status 约束）
    - 迁移失败抛出时 better-sqlite3 自动 ROLLBACK（db.transaction 包裹）
  </behavior>
  <action>
    1. 在 electron/database/migrations.ts：
       - 顶部 `export const MIGRATION_HEAD = 8` 改为 `export const MIGRATION_HEAD = 9`。
       - 在 `const v8 = ...` 之后、`const MIGRATIONS` 数组之前，新增 `const v9 = (db: Database.Database): void => { ... }`，函数体：
         ```ts
         const v9 = (db: Database.Database): void => {
           // Phase 8（D-03a）：experiences 加 duplicate_of_exp_id TEXT nullable 列。
           // 支撑起草 UPDATE 命中关联（Phase 9 确认时据 draft.duplicate_of_exp_id 定位旧条目）+ 二期经验↔经验关联预留。
           // 幂等守卫 D-14 第一形式：hasColumn 判定（与 v1/v2/v3/v4 同构，纯 ALTER ADD COLUMN）。
           // 不动 status 枚举（沿用 Phase 7 四态）、不动现有列、不加 CHECK/DEFAULT。
           const step = db.transaction(() => {
             if (!hasColumn(db, 'experiences', 'duplicate_of_exp_id')) {
               db.exec('ALTER TABLE experiences ADD COLUMN duplicate_of_exp_id TEXT')
             }
             db.pragma('user_version = 9')
           })
           step()
         }
         ```
       - 在 `MIGRATIONS: MigrationStep[]` 数组末尾追加：
         ```ts
         { version: 9, name: 'experiences.duplicate_of_exp_id (Phase 8 drafting UPDATE hit link)', run: v9 },
         ```
    2. 在 electron/database/init.ts：找到 fresh-install experiences 建表 DDL 块（含 `source_session_id TEXT` 行），在其下方紧跟追加 `duplicate_of_exp_id TEXT,`（保持两路径 schema 一致，注释同 v8 注释要求「以下 DDL 必须与 init.ts fresh-install 块逐字一致」）。该列加在 source_session_id 之后、valid_at 之前（顺序与迁移 ALTER 自然追加位置无需严格一致，但列名 + 类型 + nullable 必须一致）。
    3. 不动 experiences 现有任何列（title/category/content/tags/status/attrs_enc/valid_at/invalid_at/last_verified_at/reuse_count/created_at/updated_at）与 status CHECK 约束、不动 exp_device_rel 表、不动其它任何表。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json --noEmit && npm run build:electron-main</automated>
  </verify>
  <done>
    - grep -c "MIGRATION_HEAD = 9" electron/database/migrations.ts = 1
    - grep -c "const v9 = " electron/database/migrations.ts = 1
    - grep -c "version: 9" electron/database/migrations.ts = 1
    - grep -c "ALTER TABLE experiences ADD COLUMN duplicate_of_exp_id TEXT" electron/database/migrations.ts = 1
    - grep -c "hasColumn(db, 'experiences', 'duplicate_of_exp_id')" electron/database/migrations.ts = 1
    - grep -c "duplicate_of_exp_id TEXT" electron/database/init.ts = 1
    - tsc strict + electron-main build 双绿
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PII 脱敏 util（D-04 分级凭证/IPv4/MAC，TDD RED→GREEN）</name>
  <files>electron/utils/piiMask.ts, electron/utils/piiMask.test.ts</files>
  <read_first>
    - electron/services/ai.ts（getChatHistory 返回解密明文消息，line 192-209；getAiConfigMasked line 39-51 是 `****xxxx` 既有脱敏模式参考，但本 util 场景不同不复用其代码）
    - electron/utils/crypto.ts（encField/decField 范例，确认本 util 不涉加密——纯字符串 transform）
    - .planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md（D-04 分级脱敏正则规范逐字）
    - .planning/codebase/CONVENTIONS.md（util 文件 camelCase.ts，命名导出）
  </read_first>
  <behavior>
    - 凭证脱敏：含 `password`/`passwd`/`pwd`/`secret`/`token`/`apiKey`/`api_key`/`key`/`密码`/`口令`/`凭证` 关键词后跟分隔符（空格/冒号/等号/引号）与值，值整体替换为 `****`（不保留尾4）
      - 例：`password: admin123` → `password: ****`；`密码=Abc!23` → `密码=****`；`api_key: sk-xxx` → `api_key: ****`
      - 例：`username admin password p@ss` → `username admin password ****`
    - IPv4 保留尾4：前三段 `(\d{1,3}\.){3}` → `***.***.***.`，末段保留。例 `192.168.1.1` → `***.***.***.1`；`10.20.30.40` → `***.***.***.40`
    - MAC 保留尾4：前两段 `([0-9A-Fa-f]{2}:){2}` → `**:**:**:`，后四段保留。例 `AA:BB:CC:DD:EE:FF` → `**:**:**:DD:EE:FF`
    - maskConversationText 串联三步：先凭证脱敏（避免把凭证值当 IP/MAC 误伤），再 IPv4，再 MAC，单次正则全局替换
    - 不修改入参（返回新字符串）
    - 空字符串/纯空白返空字符串
    - 无 PII 的文本原样返回（不影响 AI 阅读）
    - 单元覆盖：含密码+IP+MAC 的混合文本三者全脱敏；纯 IP 不被凭证误吞；纯 MAC 不被 IP 误吞；IPv4 末段为多数字（如 100/233）正确保留；MAC 大小写混合正确
  </behavior>
  <action>
    RED：先写 electron/utils/piiMask.test.ts（vitest，对齐 experienceService.test.ts 测试规避 better-sqlite3 的纯字符串 util 无需 mock DB 思路），覆盖上述 8+ 行为 case（凭证 3 例、IPv4 3 例含尾段多位数字、MAC 2 例含大小写、混合串联 1 例、空字符串/无 PII 2 例）。

    GREEN：实现 electron/utils/piiMask.ts：
    ```ts
    /**
     * PII 脱敏 util（Phase 8 D-04）。
     * 送 LLM 起草前的会话正文副本专用——原始 chat_history 明文不动（Phase 9 原始会话回链用明文）。
     * 不复用 ai.ts getAiConfigMasked 的 `****xxxx`（那是 apiKey 给 renderer 的脱敏，场景不同）。
     *
     * 分级（D-04 逐字）：
     * - 凭证（最敏感）全脱敏 ****：password/passwd/pwd/secret/token/apiKey/api_key/key/密码/口令/凭证 后跟值
     * - IPv4 保留尾4：前三段掩码，末段保留（LLM 可区分不同设备）
     * - MAC 保留尾4：前两段掩码，后四段保留
     *
     * 纯字符串 transform，不涉加密、不读写 DB、不依赖 masterKey。
     */

    // 凭证关键词后跟分隔符与值，值替换为 ****。值 = 分隔符后到下一个空白/行尾/引号闭合的连续非空白串。
    // 关键词词界隔离，避免把 username 里的 'name' 误匹配（用 \b + 显式关键词列表 + 不含 username/login）。
    // 关键词后允许的分隔符：: = 空格（最常见「password: xxx」「api_key=xxx」「密码 xxx」），引号包裹的值（"xxx"/'xxx'）单独处理。
    const CRED_KEYWORDS = 'password|passwd|pwd|secret|token|apiKey|api_key|key|密码|口令|凭证'
    // 关键词 + 分隔符 + 非空白值（含引号值整体替换）
    const CRED_RE = new RegExp(
      `(?:${CRED_KEYWORDS})\\s*[:=]\\s*(?:"[^"]*"|'[^']*'|\\S+)`,
      'gi'
    )

    // IPv4 前三段掩码，末段保留
    const IPV4_RE = /(\d{1,3}\.){3}(\d{1,3})/g

    // MAC 前 6 位（两段 0xHH:）掩码，后 12 位（四段）保留
    const MAC_RE = /(?:[0-9A-Fa-f]{2}:){2}([0-9A-Fa-f]{2}:[0-9A-Fa-f]{2}:[0-9A-Fa-f]{2}:[0-9A-Fa-f]{2})/g

    /** 凭证脱敏：关键词 + 分隔符 + 值整体替换为「关键词+分隔符+****」。 */
    export function maskCredentials(text: string): string {
      return text.replace(CRED_RE, (full) => {
        // full 形如 "password: admin123" / "api_key='sk-xxx'" → 保留关键词+分隔符，值替换 ****
        // 找到最后一个分隔符位置（: = 或空白），保留其左侧
        const m = full.match(/^(\S*?\s*[:=]\s*)(.*)$/s)
        if (m) return `${m[1]}****`
        return full.replace(/(?<=[:=\s])\S+$/s, '****')
      })
    }

    /** IPv4 脱敏：前三段掩码，末段保留。 */
    export function maskIpv4(text: string): string {
      return text.replace(IPV4_RE, '***.***.***.$2')
    }

    /** MAC 脱敏：前两段掩码，后四段保留。 */
    export function maskMac(text: string): string {
      return text.replace(MAC_RE, '**:**:**:$1')
    }

    /** 串联三步（顺序：凭证 → IPv4 → MAC，避免凭证值被 IP/MAC 误伤或反之）。 */
    export function maskConversationText(text: string): string {
      if (!text || !text.trim()) return text || ''
      let out = maskCredentials(text)
      out = maskIpv4(out)
      out = maskMac(out)
      return out
    }
    ```
    注意：`IPV4_RE` 用 `$2` 反向引用末段；`MAC_RE` 用 `$1` 反向引用后四段。grep 守卫用词界避免注释自匹配（见 acceptance）。
    跑测试至全绿后 commit。
  </action>
  <verify>
    <automated>npx vitest run electron/utils/piiMask.test.ts</automated>
  </verify>
  <done>
    - grep -c "export function maskConversationText" electron/utils/piiMask.ts = 1
    - grep -c "export function maskCredentials" electron/utils/piiMask.ts = 1
    - grep -c "export function maskIpv4" electron/utils/piiMask.ts = 1
    - grep -c "export function maskMac" electron/utils/piiMask.ts = 1
    - grep -cE "password\|passwd\|pwd\|secret\|token\|apiKey\|api_key\|密码\|口令\|凭证" electron/utils/piiMask.ts ≥ 1（关键词清单存在）
    - piiMask.test.ts 全 PASS（凭证 3 例 + IPv4 含尾段多位数字 + MAC 大小写 + 混合串联 + 空字符串 + 无 PII 原样返回）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 查重 service（D-02 同分类+设备，函数式，TDD RED→GREEN）</name>
  <files>electron/services/duplicateDetector.ts, electron/services/duplicateDetector.test.ts</files>
  <read_first>
    - electron/services/experienceService.ts（listExperiences 签名 + ListExperiencesOpts + rowToExperience 返回字段：id/title/category/content/...；MAX_BATCH；VALID_CATEGORIES）
    - .planning/phases/07-experience-data-layer-security-baseline/07-01-SUMMARY.md（experienceService 调用契约）
    - .planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md（D-02 查重判定规范逐字：同 category + 同 deviceId 任一 / 无设备则同 category 全库 / 喂「标题+前150字摘要+exp_id」列表 / 无硬阈值）
    - electron/services/experienceService.test.ts（内存 mock DB 模式 + _setExperienceDbGetter 钩子，本 service 查重走 listExperiences 已抽象掉 DB，可直接 mock listExperiences 函数）
  </read_first>
  <behavior>
    - 输入 `{ category, deviceIds?: string[] }`：
      - 有 deviceIds（非空数组）→ 对每个 deviceId 调 listExperiences({ category, deviceId, includeInvalid:false, limit:MAX_BATCH })，合并去重（同一 exp_id 多设备命中只保留一条）
      - 无 deviceIds（空数组/undefined）→ listExperiences({ category, includeInvalid:false, limit:MAX_BATCH }) 全库同分类
    - 输出 `ExistingExperienceSummary[]`：`{ exp_id, title, content_preview }`，content_preview = content 前 150 字符（slice(0,150)）
    - 无硬相似度阈值、不做 ADD/UPDATE/NOOP 判定（判定交 Plan 02 起草 LLM；本 service 只负责把候选存量「喂」给起草 prompt）
    - 自动过滤已失效（includeInvalid:false，listExperiences 内部已实现 bi-temporal 过滤）
    - 调 listExperiences 越界（limit > MAX_BATCH）不发生——本 service 永远传 MAX_BATCH=1000
    - 返回顺序：按 listExperiences 默认（created_at DESC），不做额外排序
    - 单元覆盖：有设备命中 2 设备去重；无设备全库同分类；truncated 不影响（取 rows）；空结果返空数组；非法 category 不进本 service（起草层已校验，本 service 信任入参）
  </behavior>
  <action>
    RED：先写 electron/services/duplicateDetector.test.ts。用 `vi.mock('./experienceService', ...)` 替换 listExperiences 返回受控 rows（每行含 id/title/content/category/status/...），断言 findExistingForDraft 返回的 summary 含 exp_id/title/content_preview(≤150字)。覆盖 5+ case：
      1. deviceIds=['d1','d2'] → mock listExperiences 对 d1 返 [{id:'e1',title:'t1',content:'c'.repeat(200)}]、对 d2 返 [{id:'e1',title:'t1',content:'c2'},{id:'e2',title:'t2',content:'c3'}]，断言合并去重后 2 条（e1,e2），e1 content_preview = 'c'.repeat(150)（截到 150 字）
      2. deviceIds=[] → 走全库分支，mock 返 3 条，断言 3 条 summary
      3. deviceIds=undefined → 同空数组分支
      4. mock 返空 → 断言返 []
      5. content 长度 < 150（如 'short'）→ content_preview='short'（不补齐不强填）

    GREEN：实现 electron/services/duplicateDetector.ts（函数式，无 class、无 MK 持有——本 service 不读写加密列，纯查询聚合）：
    ```ts
    import { listExperiences, MAX_BATCH } from './experienceService'
    import type { ExperienceCategory } from './experienceService'

    /**
     * 查重 service（Phase 8 D-02）。
     * 起草前查存量候选，喂起草 LLM 判 ADD/UPDATE/NOOP。
     *
     * D-02 策略：
     * - 草稿关联设备 → 查「同 category + 同 deviceId（任一）」有效存量
     * - 无设备关联 → 查「同 category 全库」
     * - 喂 AI 形式 = 「标题 + 内容前 150 字摘要 + exp_id」列表
     * - 无硬相似度阈值（信任 LLM 判定，红线③ 人工确认兜底）
     * - 自动过滤已失效（includeInvalid=false，复用 listExperiences bi-temporal 过滤）
     *
     * 函数式（无 class、无 MK 持有）——本 service 不读写加密列，
     * 只经 experienceService.listExperiences 聚合，与 CONVENTIONS Pattern 1b（无加密列 service）一致。
     */

    const PREVIEW_LEN = 150

    export interface FindExistingInput {
      category: ExperienceCategory
      deviceIds?: string[]
    }

    export interface ExistingExperienceSummary {
      exp_id: string
      title: string
      content_preview: string
    }

    /** 查同分类+设备（或全库）有效存量，返回「标题+前150字摘要+exp_id」列表供起草 LLM 判定。 */
    export function findExistingForDraft(input: FindExistingInput): ExistingExperienceSummary[] {
      const { category, deviceIds } = input
      const has = Array.isArray(deviceIds) && deviceIds.length > 0

      const seen = new Map<string, ExistingExperienceSummary>()

      const collect = (rows: any[]): void => {
        for (const r of rows) {
          if (seen.has(r.id)) continue
          seen.set(r.id, {
            exp_id: r.id,
            title: r.title || '',
            content_preview: typeof r.content === 'string' ? r.content.slice(0, PREVIEW_LEN) : '',
          })
        }
      }

      if (has) {
        for (const did of deviceIds!) {
          const res = listExperiences({ category, deviceId: did, includeInvalid: false, limit: MAX_BATCH, offset: 0 })
          collect(res.rows)
        }
      } else {
        const res = listExperiences({ category, includeInvalid: false, limit: MAX_BATCH, offset: 0 })
        collect(res.rows)
      }

      return Array.from(seen.values())
    }
    ```
    跑测试至全绿后 commit。
  </action>
  <verify>
    <automated>npx vitest run electron/services/duplicateDetector.test.ts</automated>
  </verify>
  <done>
    - grep -c "export function findExistingForDraft" electron/services/duplicateDetector.ts = 1
    - grep -c "export interface ExistingExperienceSummary" electron/services/duplicateDetector.ts = 1
    - grep -c "PREVIEW_LEN = 150" electron/services/duplicateDetector.ts = 1
    - grep -c "slice(0, PREVIEW_LEN)" electron/services/duplicateDetector.ts = 1
    - grep -c "import.*listExperiences.*MAX_BATCH.*from.*'./experienceService'" electron/services/duplicateDetector.ts = 1
    - grep -cE "\\b(encrypt|decrypt)\\(" electron/services/duplicateDetector.ts = 0（不涉加密，反向守卫）
    - duplicateDetector.test.ts 全 PASS（去重/无设备全库/空数组/undefined/空结果/<150字 6 case）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: 扩展 experienceService.createExperience 接受可选 duplicateOfExpId（B-1 + B-2 方案 A，单语句原子，TDD RED→GREEN）</name>
  <files>electron/services/experienceService.ts, electron/services/experienceService.test.ts</files>
  <read_first>
    - electron/services/experienceService.ts（createExperience line 165 现有签名 + ExperienceInput interface line 81 + INSERT 语句；validateAndStringifyAttrs；getExperience 返回路径）
    - electron/services/experienceService.test.ts（内存 mock DB + _setExperienceDbGetter 模式 + 现有 createExperience 测试 case）
    - .planning/phases/08-ai-drafting-pipeline/08-CONTEXT.md（D-03a duplicate_of_exp_id 列语义：UPDATE 草稿填命中旧 id，Phase 9 据此定位旧条目）
    - .planning/codebase/CONVENTIONS.md（Service 封装红线：数据访问必经 service 门面，编排层不裸 SQL）
    - .planning/phases/07-experience-data-layer-security-baseline/07-REVIEW-FIX.md（CR-01：updateExperience 白名单已收紧 status/audit/有效期，但 duplicate_of_exp_id 属 Phase 8 新列、Phase 7 白名单本就不含；本任务在 create 入口而非 update 入口扩展，天然不绕 CR-01）
  </read_first>
  <behavior>
    - createExperience 接受可选 input.duplicateOfExpId?: string | null（不传/传 null → 写 NULL，向后兼容 Phase 7 所有既有调用方零改动）
    - CREATE 语句单语句原子：duplicate_of_exp_id 列与 draft 行同 INSERT 写入（CREATE 成功即标注同写入，CREATE 失败 throw → 整条不落库，标注与 draft 行共存亡——B-2 原子性）
    - 经 service 门面写入（不裸 SQL）——B-1 不绕 experienceService 数据访问门面
    - duplicateOfExpId 传字符串但内部 prepare 参数化（无 SQL 注入）；不校验指向的 exp_id 是否存在（信任 Plan 03 编排层传入 LLM 判定结果 + Phase 9 人工确认兜底，本 service 不做存在性 FK——experiences 表本就无 self-FK 约束）
    - 既有调用方（无 duplicateOfExpId 字段）行为不变：写 NULL，回归全绿
    - 单元覆盖：(a) 不传 duplicateOfExpId → 行 duplicate_of_exp_id 列为 NULL（向后兼容）；(b) 传 'exp-xyz' → 行该列为 'exp-xyz'；(c) 传 null → 列为 NULL；(d) troubleshooting 类 + duplicateOfExpId 同时传 → severity 校验 + dup_id 写入两不误
  </behavior>
  <action>
    RED：在 electron/services/experienceService.test.ts 追加 4 个 createExperience 测试 case（在现有 createExperience describe 块内追加，复用既有 mock DB + setExperienceMasterKey 钩子）：
    ```ts
    describe('createExperience duplicateOfExpId（Phase 8 B-1/B-2 方案 A）', () => {
      it('不传 duplicateOfExpId 时列写 NULL（向后兼容 Phase 7）', () => {
        const exp = createExperience({ title: 't', category: 'best_practices', content: 'c' })
        const raw = mockDbRef.current.tables.get('experiences').rows.get(exp.id)
        expect(raw.duplicate_of_exp_id).toBeNull()
      })
      it('传 duplicateOfExpId 字符串时列写该值', () => {
        const exp = createExperience({ title: 't', category: 'best_practices', content: 'c', duplicateOfExpId: 'exp-old-123' })
        const raw = mockDbRef.current.tables.get('experiences').rows.get(exp.id)
        expect(raw.duplicate_of_exp_id).toBe('exp-old-123')
      })
      it('传 null 时列写 NULL', () => {
        const exp = createExperience({ title: 't', category: 'product', content: 'c', duplicateOfExpId: null })
        const raw = mockDbRef.current.tables.get('experiences').rows.get(exp.id)
        expect(raw.duplicate_of_exp_id).toBeNull()
      })
      it('troubleshooting 类 + duplicateOfExpId 同时传：severity 校验 + dup_id 写入两不误', () => {
        const exp = createExperience({
          title: 't', category: 'troubleshooting', content: 'c',
          attrs: { severity: 'high' }, duplicateOfExpId: 'exp-old-456',
        })
        const raw = mockDbRef.current.tables.get('experiences').rows.get(exp.id)
        expect(raw.duplicate_of_exp_id).toBe('exp-old-456')
        expect(exp.attrs.severity).toBe('high')
      })
    })
    ```
    （若 mock DB 的 INSERT 模拟只取 SQL 中列出的列，需确认 mock 能识别新增的第 10 列占位——参考现有 mock prepare.run 实现，必要时 mock 透传多出的一个参数到行存储。）

    GREEN：在 electron/services/experienceService.ts 修改 createExperience：
    1. `ExperienceInput` interface（line 81-88）追加可选字段：
       ```ts
       export interface ExperienceInput {
         title: string
         category: ExperienceCategory
         content: string
         tags?: string[]
         sourceSessionId?: string | null
         attrs?: ExperienceAttrs | null
         /** Phase 8 D-03a：UPDATE 草稿标注命中的旧 exp_id。不传/传 null → 写 NULL（向后兼容 Phase 7）。 */
         duplicateOfExpId?: string | null
       }
       ```
    2. createExperience 函数体（line 165-179）INSERT 语句加第 10 列 + 第 10 个占位参数：
       ```ts
       export function createExperience(input: ExperienceInput): any {
         if (!VALID_CATEGORIES.includes(input.category)) {
           throw new Error(`非法 category: ${input.category}`)
         }
         const id = uuidv4()
         const attrsStr = validateAndStringifyAttrs(input.category, input.attrs)
         const attrsEnc = attrsStr ? encField(attrsStr, MK) : null
         const tags = JSON.stringify(input.tags ?? [])
         const dupId = input.duplicateOfExpId ?? null
         const conn = db()
         conn.prepare(
           `INSERT INTO experiences (id, title, category, content, tags, status, source_session_id, attrs_enc, valid_at, duplicate_of_exp_id)
            VALUES (?, ?, ?, ?, ?, 'draft', ?, ?, datetime('now','localtime'), ?)`
         ).run(id, input.title, input.category, input.content, tags, input.sourceSessionId ?? null, attrsEnc, dupId)
         return getExperience(id)
       }
       ```
    3. 不动 updateExperience（CR-01 白名单仍不含 duplicate_of_exp_id——Phase 8 起草只走 create 入口写该列，不经 update；Phase 9/10 若需改该列届时另议）。
    4. 不动 rowToExperience（SELECT * 已含新列，但 rowToExperience 不显式映射 duplicate_of_exp_id——该列不喂 renderer 经 IPC 脱敏回链，留 main 进程内部 Phase 9 消费；getExperience 返回值含该列字段无害，renderer DTO 不声明该字段自动忽略）。

    关键决策：
    - **方案 A（单语句原子）优于方案 B（setDuplicateOfExpId + transaction）**：单 INSERT 含 dup_id 列，无 transaction 复杂度，CREATE 失败即 throw 整条不落库（B-2 原子性同源保证）；不需要独立的 setDuplicateOfExpId 函数（B-1 fix_hint 提到的方案 B 在方案 A 下被吸收）。
    - **门面扩展而非编排层裸 SQL**：duplicate_of_exp_id 列写入必经 experienceService.createExperience 门面（B-1），Plan 03 编排层只调 `createExperience({..., duplicateOfExpId})`，grep 反向守卫 `UPDATE experiences SET duplicate_of_exp_id` electron/services/experienceDrafting.ts = 0。
    - **不校验 FK 存在性**：experiences 表无 self-FK 约束（Phase 7 DDL 未加），duplicateOfExpId 指向的旧 exp_id 可能后续被标 invalid（Phase 9）但不物理删除，故无悬空 FK 风险；存在性校验交 Phase 9 人工确认。
    跑测试至全绿后 commit。
  </action>
  <verify>
    <automated>npx vitest run electron/services/experienceService.test.ts</automated>
  </verify>
  <done>
    - grep -c "duplicateOfExpId" electron/services/experienceService.ts ≥ 3（interface 字段 + INSERT 局部变量 + .run 参数）
    - grep -c "duplicate_of_exp_id" electron/services/experienceService.ts = 2（INSERT 列名 + 注释，正向落地）
    - grep -c "VALUES (?, ?, ?, ?, ?, 'draft', ?, ?, datetime('now','localtime'), ?)" electron/services/experienceService.ts = 1（INSERT 第 10 列占位，B-2 单语句原子）
    - grep -cE "UPDATE experiences SET duplicate_of_exp_id" electron/services/experienceService.ts = 0（本任务不经 UPDATE 写该列，反向守卫；该列只在 create 入口写）
    - experienceService.test.ts 全 PASS（既有 ~20 case + 新增 4 case duplicateOfExpId 不破坏基线）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat_history 明文 → PII 脱敏 util → LLM 副本 | 会话明文经 PII 脱敏后才送 LLM；原始明文驻留 main 进程内存（已被 AES-256-GCM at-rest 加密落盘），脱敏只针对 LLM 副本 |
| experienceService.listExperiences → duplicateDetector | 查重经既有 service（已脱敏回填 attrs_enc、已 delete 密文列），duplicateDetector 不直接读加密列 |
| experienceService.createExperience → experiences 表 | duplicate_of_exp_id 列经门面 CREATE 单语句写入（参数化），Plan 03 编排层不裸 SQL；门面是唯一写该列的入口 |
| 迁移 ALTER → experiences 历史 22+ 列 | duplicate_of_exp_id 加列幂等，throw 即 ROLLBACK，不动 status 约束/不动现有列 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure（PII 泄露 LLM provider） | piiMask.maskConversationText | mitigate | 凭证关键词正则全脱敏 `****`（不保留尾4）+ IPv4/MAC 仅保留尾4；脱敏在「送 LLM 前」主进程做（Plan 02 起草 service 调用），原始 chat_history 明文不动（D-04）|
| T-08-02 | Information Disclosure（凭证值被 IP/MAC 正则误保留） | piiMask 串联顺序 | mitigate | 串联顺序固定「凭证 → IPv4 → MAC」，凭证先脱敏为 **** 后 IP/MAC 正则不会在 **** 里匹到 IP/MAC；单测覆盖混合文本三者全脱敏 |
| T-08-03 | Tampering（迁移破坏历史 status/数据） | migrations.v9 | mitigate | hasColumn 幂等守卫 + db.transaction（throw ROLLBACK）+ 纯 ALTER ADD COLUMN nullable TEXT 无 CHECK/DEFAULT，不动 status 枚举、不动现有列 |
| T-08-04 | Information Disclosure（查重泄露 attrs 加密凭证片段） | duplicateDetector | accept | duplicateDetector 只取 id/title/content（明文，Phase 7 已决策 content 明文支撑 FTS5），不取 attrs_enc；attrs 凭证片段不喂 LLM（T-07-04 既有取舍延续） |
| T-08-05 | Denial of Service（查重拉全表爆内存） | duplicateDetector | mitigate | limit:MAX_BATCH=1000 强制（listExperiences 内 throw），单次起草查重候选 ≤1000 条 × 150 字摘要 ≤ 150KB，可控 |
| T-08-19 | Tampering / 架构违规（编排层裸 SQL 写 duplicate_of_exp_id 绕门面） | experienceService.createExperience 扩展签名 | mitigate | duplicate_of_exp_id 列写入唯一入口是 createExperience CREATE 语句（参数化 prepared statement），Plan 03 编排层 grep 反向守卫不裸 SQL UPDATE；B-1 Service 封装红线落地 |

</threat_model>

<verification>
- `npx tsc -p tsconfig.web.json --noEmit` EXIT 0（noUnusedLocals 全绿，duplicateDetector/experienceService 无未用 import）
- `npm run build:electron-main` EXIT 0（含 v9 迁移 + 新 util/service + createExperience 扩展经 esbuild bundle）
- `npx vitest run` 全 PASS（既有 75 测试 + piiMask 新增 + duplicateDetector 新增 + experienceService 新增 4 case，不破坏基线）
- grep 守卫全部命中（见各 task done）
</verification>

<success_criteria>
- v9 迁移幂等可重跑（hasColumn 守卫），fresh-install 与遗留库两路径 schema 一致含 duplicate_of_exp_id
- PII 脱敏 util 凭证全脱敏/IPv4/MAC 尾4，单测全绿，纯字符串 transform 不涉加密
- 查重 service 复用 Phase 7 listExperiences，同分类+设备优先、无设备全库、喂摘要列表无硬阈值
- createExperience 扩展接受 duplicateOfExpId，单语句原子写入 v9 新列（B-1 不裸 SQL + B-2 共存亡同源解决），既有调用方向后兼容
- 三绿门禁全绿，既有 75 测试不破坏
</success_criteria>

<output>
完成后创建 `.planning/phases/08-ai-drafting-pipeline/08-01-SUMMARY.md`，记录：v9 迁移落地点 + piiMask 导出函数清单 + duplicateDetector.findExistingForDraft 调用契约 + **createExperience 扩展签名（duplicateOfExpId 可选参数，单语句原子，供 Plan 03 编排层不裸 SQL）** + 偏差（若有）。
</output>
