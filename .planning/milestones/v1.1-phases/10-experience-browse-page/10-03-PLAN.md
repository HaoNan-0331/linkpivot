---
phase: 10-experience-browse-page
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - src/components/knowledge/ExperienceTab.tsx
  - src/components/knowledge/ExperienceDetailModal.tsx
  - src/components/pages/KnowledgeBasePage.tsx
autonomous: false
requirements: [BROWSE-01, BROWSE-02, BROWSE-03, BROWSE-04]
tags: [renderer, ui, table, modal, tabs]

must_haves:
  truths:
    - "知识库页顶部展示 AntD Tabs（文档 | 经验），默认 Tab=文档，切换到经验展示 ExperienceTab"
    - "经验 Tab 展示 Table 列表（9 列锁定顺序：标题/分类/严重程度/标签/关联设备/状态/有效期/最后验证/操作），点标题蓝字打开详情 Modal"
    - "用户可按分类/严重度/状态/关联设备（多选）/标签（多选）多维筛选 + 关键词搜索（onPressEnter + 300ms 防抖）+ 「显示已失效」Switch 切换 includeInvalid（UI-SPEC §3 设备 Select mode=multiple 锁定）"
    - "关联设备列（第 5 列）显示「N 台」+ 无关联显示「全局」灰 Tag（UI-SPEC §2 锁定渲染；数据来自 record.device_count，10-01 子查询零 N+1 带出）"
    - "用户可手动新增经验（顶部「新增经验」primary 按钮 → ExperienceEditForm Modal 空表单 → createExperience status='published'）"
    - "用户可编辑经验（行操作「编辑」→ ExperienceEditForm Modal 预填 → updateExperience）"
    - "用户可标失效（行操作 Popconfirm 软确认 → experience:invalidate）/ 恢复有效（experience:restore）/ 物理删除（Popconfirm 硬确认不可恢复 → experience:delete）"
    - "经验详情 Modal（width 900）展示元数据（来源会话回链 + 关联设备 + 复用次数 + 最后验证 + 有效期 + 创建/更新时间）+ 正文 + troubleshooting attrs 模板字段块"
    - "详情 Modal 内「查看原始会话」按钮复用 Phase 9 SessionMessagesModal 子 Modal 叠层"
    - "draft 态经验不进浏览页（默认视图仅 published/invalid；Phase 9 待确认草稿专属）"
  artifacts:
    - path: "src/components/knowledge/ExperienceTab.tsx"
      provides: "经验 Tab 主组件（筛选 bar 含设备多选 + Table 9 列含关联设备 N 台渲染 + 行操作 + 新增/编辑/详情 Modal 编排）"
      min_lines: 250
      contains: "window.api.experience.list"
    - path: "src/components/knowledge/ExperienceDetailModal.tsx"
      provides: "经验详情只读 Modal（width 900 + 元数据 + 正文 + attrs 模板 + 复用 SessionMessagesModal）"
      min_lines: 120
      contains: "SessionMessagesModal"
    - path: "src/components/pages/KnowledgeBasePage.tsx"
      provides: "Tabs 容器改造（文档 | 经验），现有内容移入文档 pane，经验 pane 懒加载 ExperienceTab"
      contains: "<Tabs"
  key_links:
    - from: "src/components/knowledge/ExperienceTab.tsx"
      to: "window.api.experience.list"
      via: "loadExperiences useEffect（传 deviceId string[] 多选）"
      pattern: "window\\.api\\.experience\\.list"
    - from: "src/components/knowledge/ExperienceTab.tsx"
      to: "src/components/knowledge/ExperienceEditForm.tsx"
      via: "import ExperienceEditForm + Modal 包裹"
      pattern: "import ExperienceEditForm"
    - from: "src/components/knowledge/ExperienceTab.tsx"
      to: "src/components/knowledge/ExperienceDetailModal.tsx"
      via: "openDetail(record.id) state"
      pattern: "ExperienceDetailModal"
    - from: "src/components/pages/KnowledgeBasePage.tsx"
      to: "src/components/knowledge/ExperienceTab.tsx"
      via: "Tabs exp pane children"
      pattern: "ExperienceTab"
    - from: "src/components/knowledge/ExperienceDetailModal.tsx"
      to: "src/components/pages/ai/SessionMessagesModal.tsx"
      via: "查看原始会话叠层"
      pattern: "SessionMessagesModal"
---

<objective>
Phase 10 UI 层（BROWSE-01/02/03/04 落地）：KnowledgeBasePage 改造为 Tabs（文档 | 经验）+ ExperienceTab 列表/筛选/搜索/手动 CRUD/标失效/恢复/详情 Modal。消费 10-01 的 service/IPC（含 deviceId 多选 + device_count 子查询）与 10-02 的 ExperienceEditForm 公共组件。严格遵循 UI-SPEC §2/§3 锁定渲染（设备多选 Select + 关联设备列「N 台/全局」）。

Purpose: 把 Phase 7-9 沉淀的经验资产变成可人工主动管理的列表（ROADMAP Phase 10 Goal）。用户可在知识库「经验」板块独立浏览、筛选、搜索、手动维护经验，并不只依赖 AI 总结。

Output: 2 新文件（ExperienceTab.tsx + ExperienceDetailModal.tsx）+ 1 改造文件（KnowledgeBasePage.tsx Tabs 容器）。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-experience-browse-page/10-CONTEXT.md
@.planning/phases/10-experience-browse-page/10-UI-SPEC.md
@.planning/phases/10-experience-browse-page/10-PATTERNS.md
@.planning/phases/09-human-review-confirmation/09-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- ExperienceTab 调用的 IPC 面（10-01 已落地，含 deviceId 多选 + device_count） -->
window.api.experience.list(opts?: ExperienceListInput): Promise<ExperienceListResult>
  - ExperienceListInput: { category?, status?, deviceId?: string | string[], includeInvalid?, limit?, offset?, search?, severity?, tags? }
  - ExperienceListResult: { rows: Experience[], total: number, truncated: boolean }
window.api.experience.create(input): Promise<Experience>     // input 含 status='published'（手动新增）
window.api.experience.update(id, fields): Promise<Experience>  // CR-01 白名单（不含 status）
window.api.experience.delete(id): Promise<void>               // hard DELETE
window.api.experience.invalidate(id): Promise<Experience>     // 软失效（落 invalid_at）
window.api.experience.restore(id): Promise<Experience>        // 10-01 新增（清 invalid_at + status 回 published）
window.api.experience.listDevices(expId): Promise<ExperienceRelatedDevice[]>
window.api.experience.getSessionMessages(sessionId): Promise<SessionMessage[]>  // Phase 9 既有

<!-- ExperienceEditForm（10-02 已落地） -->
import ExperienceEditForm, { validateDraft } from './ExperienceEditForm'
<ExperienceEditForm initialValue={exp} onSubmit={...} onCancel={...} />  // 编辑态
<ExperienceEditForm onSubmit={...} onCancel={...} />                      // 新增态（initialValue undefined）

<!-- SessionMessagesModal（Phase 9 既有，09-03-SUMMARY） -->
import SessionMessagesModal from '../pages/ai/SessionMessagesModal'
<SessionMessagesModal open={bool} sessionId={string|null} onClose={() => ...} />

<!-- Experience DTO（10-01 已扩 severity + device_count 字段） -->
interface Experience {
  id, title, category, content, tags: string[], status, source_session_id?,
  attrs?: { severity?, symptoms?, root_cause?, resolution?, prevention? } | null,
  valid_at, invalid_at?, last_verified_at?, reuse_count, created_at, updated_at,
  duplicate_of_exp_id?,
  severity?: 'critical'|'high'|'medium'|'low'|'info'|null,   // 10-01 明文列 + attrs fallback
  device_count?: number                                       // 10-01 子查询带出（0=无关联=「全局」）
}

<!-- UI-SPEC 锁定契约（必逐项落实，禁止偏离） -->
- Table 9 列顺序：标题<a> / 分类 Tag blue 100 / 严重程度 Tag 90 / 标签 / 关联设备 120 / 状态 100 / 有效期 180 / 最后验证 150 / 操作 180
- **第 5 列「关联设备」锁定渲染（UI-SPEC §2，禁止改单选/查看链接）**：record.device_count > 0 显「N 台」（如「3 台」）+ tooltip 列名（可选，详情 Modal 看完整列表）；record.device_count === 0（或 undefined）显 `<Tag>全局</Tag>`（灰 default 色）
- **筛选 bar 设备 Select 锁定 mode="multiple"（UI-SPEC §3，禁止改单选）**：传 string[] 给 service opts.deviceId（10-01 已支持 string|string[]），多选 OR-join 在 service 层 IN 占位参数化
- 筛选 bar 顺序：新增经验 primary → 搜索 Input 240 → 分类 Select 120 → 严重度 Select 120 → 状态 Select 120 → 设备 Select 160 多选 → 标签 Select 160 mode multiple → 显示已失效 Switch
- Modal 尺寸：编辑表单 640 / 详情 900
- severity Tag 语义色：critical=red / high=volcano / medium=orange / low=gold / info=blue
- 状态 Tag：有效=success 绿「有效」/ 失效=default 灰「已失效」（不染红）
- 行操作按状态切换：有效经验「编辑/标失效/删除」；失效经验「编辑/恢复有效/删除」
- 列表分页 pageSize 20
- copy：搜索 placeholder「搜索经验标题或正文」/ Empty「暂无经验 AI 对话后点『经验总结』自动沉淀，或点击右上角『新增经验』手动录入。」/ message.error「加载经验列表失败: 」/ message.success「保存成功」/「已标记为失效」/「已恢复为有效」
- Popconfirm 硬删除：title「确认删除」description「将彻底删除经验『{title}』，操作不可恢复。」okText「删除」cancelText「取消」
- Popconfirm 软失效：title「标记为失效？」description「失效后将从默认视图剔除，但仍可查/可恢复，不会物理删除。」okText「标失效」cancelText「取消」
- 红线③ copy：footer「保存」（禁止「确认入库」/「发布并待审」）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 ExperienceDetailModal（只读详情，width 900 + 元数据 + 复用 SessionMessagesModal）</name>
  <files>src/components/knowledge/ExperienceDetailModal.tsx</files>
  <read_first>
    - src/components/pages/KnowledgeBasePage.tsx L464-531（文档详情 Modal width=900 footer=null 范式 + 元数据 strong label 行 + ChunkContent pre-wrap maxHeight overflow auto）
    - src/components/pages/ai/SessionMessagesModal.tsx L1-40（props 契约 { open, sessionId, onClose }，空会话边界「原会话已不可查」已内置）
    - src/components/pages/ai/ReviewConfirmModal.tsx L293-297（SessionMessagesModal 叠层用法范式）
    - src/components/pages/ai/ReviewConfirmModal.tsx L256-258（Tag 色：分类 blue / UPDATE orange 范式）
    - src/types/experience.ts（Experience DTO + ExperienceRelatedDevice，含 device_count 字段）
    - .planning/phases/10-experience-browse-page/10-PATTERNS.md §ExperienceDetailModal.tsx 段（L191-234）
    - .planning/phases/10-experience-browse-page/10-UI-SPEC.md §6 详情 Modal 元数据展示 + §Color severity 语义色映射表
  </read_first>
  <action>
新建 `src/components/knowledge/ExperienceDetailModal.tsx`。

**Props**：`{ open: boolean, experience: Experience | null, onClose: () => void, onEdit?: () => void, onInvalidate?: () => void, onRestore?: () => void, onDelete?: () => void }`（编辑/失效/恢复/删除回调可选，由调用方 ExperienceTab 传入；详情 Modal 底部 Space 按钮触发这些回调后由调用方关闭详情 Modal 并打开对应 Modal/Popconfirm）。

**结构**：
- `<Modal title={experience ? '经验详情 - ' + experience.title : '经验详情'} open={open} onCancel={onClose} footer={null} width={900}>`（title 文案 UI-SPEC copy contract）
- 顶部 Space：标题（strong 600）+ 状态 Tag（有效=success 绿「有效」/ 失效=default 灰「已失效」，UI-SPEC Color 状态不染红）+ 分类 Tag（blue，中文 label 故障排查/最佳实践/产品/环境）+ severity Tag（troubleshooting 类显，语义色 critical=red/high=volcano/medium=orange/low=gold/info=blue；非 troubleshooting 类不显）
- 元数据行（strong label + 值，沿用 KB L473-482 `<span><strong>X：</strong>{v}</span>` 范式）：
  - 来源会话：source_session_id 截短（前 8 字符）+ 「查看原始会话」Button（仅 source_session_id 存在时显；点击 setSessionModalSessionId(source_session_id) 打开叠层）
  - 关联设备：调 `window.api.experience.listDevices(experience.id)` 取 ExperienceRelatedDevice[]，显设备名逗号分隔；无关联显「全局」（灰）
  - 复用次数：reuse_count
  - 最后验证：last_verified_at ?? 「未验证」（灰）
  - 有效期：valid_at ~ (invalid_at ?? 「至今」)，失效则 invalid_at 红字
  - 创建时间：created_at / 更新时间：updated_at（yyyy-MM-dd HH:mm 格式化）
- 正文 content：`<div style={{ whiteSpace: 'pre-wrap', maxHeight: 400, overflow: 'auto' }}>`（KB L67 ChunkContent 范式，UI-SPEC §6 maxHeight 400）
- troubleshooting 类 attrs 模板字段块（cat === 'troubleshooting' &&）：每字段 strong label + 值（故障现象 symptoms / 根本原因 root_cause / 解决办法 resolution / 预防措施 prevention，中文 label 与 Phase 9 cd87077 一致）
- 底部 Space（onEdit/onInvalidate 或 onRestore/onDelete 回调按钮，文案与行操作一致：「编辑」/「标失效」或「恢复有效」/「删除」）
- 叠层 SessionMessagesModal：`<SessionMessagesModal open={!!sessionModalSessionId} sessionId={sessionModalSessionId} onClose={() => setSessionModalSessionId(null)} />`（复用 Phase 9 既有组件，import 路径 `'../pages/ai/SessionMessagesModal'`）

加载关联设备用 useEffect（experience 变化时调 listDevices，setDevices state）。

格式红线：2 空格 / 单引号 / 无分号 / 尾随逗号；AntD 桶式 import；icon from '@ant-design/icons'。
  </action>
  <verify>
    <automated>
cd "E:/knowlegdge_base/claude/network_toplogy" && npx tsc -p tsconfig.web.json --noEmit && npx vite build
    </automated>
  </verify>
  <acceptance_criteria>
- `test -f src/components/knowledge/ExperienceDetailModal.tsx` exit 0
- `grep -c "width={900}" src/components/knowledge/ExperienceDetailModal.tsx` 返回 1
- `grep -c "footer={null}" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥1
- `grep -c "SessionMessagesModal" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥2（import + 使用）
- `grep -c "经验详情" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥1（title 文案）
- `grep -c "查看原始会话" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥1
- `grep -c "volcano" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥1（severity high 语义色）
- `grep -c "whiteSpace: 'pre-wrap'" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥1（正文 pre-wrap）
- `grep -c "window.api.experience.listDevices" src/components/knowledge/ExperienceDetailModal.tsx` 返回 ≥1
- tsc + vite build 全 exit 0
  </acceptance_criteria>
  <done>ExperienceDetailModal 落地（width 900 footer null + 元数据 strong label 行 + 正文 pre-wrap maxHeight 400 + troubleshooting attrs 模板块 + 复用 SessionMessagesModal 叠层 + severity 语义色 Tag），三绿门禁 tsc+vite build 全绿。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 ExperienceTab（筛选 bar 含设备多选 + Table 9 列含关联设备 N 台渲染 + 行操作三能力 + 新增/编辑/详情 Modal 编排）</name>
  <files>src/components/knowledge/ExperienceTab.tsx</files>
  <read_first>
    - src/components/pages/KnowledgeBasePage.tsx 全文 1-550（重点 L121-131 loadDocuments 范式 / L137 useEffect 触发 / L308-373 columns 定义 / L361-369 Popconfirm 删除 / L316 `<a>` 点行打开详情 / L378 Space wrap 容器 / L433 pagination pageSize 20 / L439-445 搜索 onPressEnter / L127 message.error 范式 / L233 message.success）
    - src/components/knowledge/ExperienceEditForm.tsx（10-02 落地，确认 props 签名 onSubmit/onCancel/initialValue）
    - src/components/knowledge/ExperienceDetailModal.tsx（本 plan Task 1 落地，确认 props 签名）
    - src/types/experience.ts（Experience DTO 含 device_count / ExperienceListInput 含 deviceId string|string[]）
    - .planning/phases/10-experience-browse-page/10-PATTERNS.md §ExperienceTab.tsx 段（L32-95，10 段复用要点 + delta）
    - .planning/phases/10-experience-browse-page/10-UI-SPEC.md §1-5（Tabs 改造 / Table 9 列含第 5 列 N 台/全局 / 筛选 bar 含设备多选 mode multiple / Modal 尺寸 / 公共组件）+ §Copywriting Contract（全 copy 锁定）
  </read_first>
  <action>
新建 `src/components/knowledge/ExperienceTab.tsx`（默认导出函数组件）。

**state**：list: Experience[] / loading: boolean / 筛选 state（search string, category, severity, status, **deviceId: string[]**（多选数组，UI-SPEC §3 锁定 mode multiple）, tags: string[], includeInvalid boolean）/ editModalOpen / editingExp: Experience | null（null=新增态）/ detailExp: Experience | null（详情 Modal）。

**筛选 bar（UI-SPEC §3 锁定顺序，`<Space wrap style={{ marginBottom: 16 }}>` 容器）**：
1. 「新增经验」Button type="primary"（左锚）→ onClick setEditingExp(null) + setEditModalOpen(true)
2. 搜索 Input（width 240, allowClear, placeholder「搜索经验标题或正文」, onPressEnter + onChange 防抖 300ms 触发 setSearch）
3. 分类 Select（width 120, placeholder「全部分类」, options 故障排查/最佳实践/产品/环境 + 全部选项）
4. 严重度 Select（width 120, placeholder「全部严重度」, options 致命/高/中/低/提示 value 存英文）
5. 状态 Select（width 120, placeholder「全部状态」, options 有效/已失效）
6. **设备 Select（width 160, mode="multiple", placeholder「筛选设备」, 拉 device.list 全设备或 ssh/telnet filter，onChange setDeviceId(value: string[])）** —— UI-SPEC §3 锁定 mode multiple，**禁止改单选**；多选数组直传 service opts.deviceId（10-01 已支持 string|string[]，service 层 IN 占位 OR-join 参数化）
7. 标签 Select（width 160, mode="multiple", placeholder「筛选标签」, options 从已有经验 tags 聚合或固定常用集）
8. 「显示已失效」Switch（label「显示已失效」, 默认 off, onChange setIncludeInvalid）

**useEffect 触发 loadExperiences（依赖 search/category/severity/status/deviceId/tags/includeInvalid）**：防抖 300ms（搜索）/ onChange 即触发（Select/Switch），沿用 KB L137 范式。

**loadExperiences（KB L121-131 范式 + delta 调 experience.list 传 deviceId string[]）**：
- `const res = await window.api.experience.list({ search, category, severity, status, deviceId: deviceIdArr.length ? deviceIdArr : undefined, tags: tagsArr.length ? tagsArr : undefined, includeInvalid, limit: 100, offset: 0 })` —— deviceId 传 string[]（10-01 service 已支持多选 IN 占位 OR-join）；空数组传 undefined（不筛）
- `setList(res.rows)` / `setLoading(false)` try/catch/finally，catch 内 `message.error('加载经验列表失败: ' + (err as Error).message)`

**Table columns（UI-SPEC §2 锁定 9 列顺序，每列 dataIndex + width + render）**：
1. title（`<a onClick={() => openDetail(record.id)}>` 蓝字点开详情）
2. category（width 100, Tag blue 中文 label）
3. severity（width 90, troubleshooting 类显 severity Tag 语义色；非 troubleshooting 显「—」灰）
4. tags（多个 Tag default 横排，超 3 个折叠 `+N`）
5. **关联设备（width 120, UI-SPEC §2 锁定渲染，禁止改「查看」链接）**：
   - `render: (_, record) => (record.device_count && record.device_count > 0) ? <span>{record.device_count} 台</span> : <Tag>全局</Tag>`
   - 数据来自 `record.device_count`（10-01 listExperiences 子查询带出，零 N+1）
   - device_count > 0 显「N 台」（如「3 台」）；device_count === 0 或 undefined 显 `<Tag>全局</Tag>`（灰 default 色，UI-SPEC §2「无关联显 全局 灰 Tag」）
   - 完整设备名列表在详情 Modal 查看（点行打开 detail，调 listDevices 显设备名逗号分隔）
6. status+invalid_at（width 100, 有效=success 绿「有效」/ 失效=default 灰「已失效」，判定：invalid_at 非 null 且 ≤ now 为失效）
7. valid_at/invalid_at（width 180, `valid_at ~ invalid_at` 或 `valid_at ~ 至今`，失效显 invalid_at）
8. last_verified_at（width 150, yyyy-MM-dd HH:mm 或「未验证」灰）
9. 操作（width 180, Space wrap size="small" 行操作按钮）

**行操作（UI-SPEC §2 行操作 + copy contract，按状态切换）**：
- 「编辑」（default + EditOutlined）→ setEditingExp(record) + setEditModalOpen(true)
- 「标失效」（default，仅 valid 时显）→ Popconfirm（软，title「标记为失效？」description「失效后将从默认视图剔除，但仍可查/可恢复，不会物理删除。」okText「标失效」cancelText「取消」，onConfirm → experience.invalidate(record.id) → message.success('已标记为失效') → loadExperiences()）
- 「恢复有效」（default，仅 invalid 时显）→ experience.restore(record.id) → message.success('已恢复为有效') → loadExperiences()（轻量，无 Popconfirm）
- 「删除」（danger + DeleteOutlined）→ Popconfirm（硬，title「确认删除」description={`将彻底删除经验『${record.title}』，操作不可恢复。`} okText「删除」cancelText「取消」，onConfirm → experience.delete(record.id) → message.success('已删除') → loadExperiences()）

**Modal 编排**：
- 编辑/新增 Modal：`<Modal title={editingExp ? '编辑经验' : '新增经验'} open={editModalOpen} footer={null} width={640} onCancel={closeEdit}><ExperienceEditForm initialValue={editingExp ?? undefined} onSubmit={handleSubmitEdit} onCancel={closeEdit} /></Modal>`（width 640 UI-SPEC §4）
- handleSubmitEdit：新增态（editingExp === null）→ createExperience({...fields, status: 'published'})；编辑态 → updateExperience(editingExp.id, fields)（fields 不含 status，CR-01 白名单）；成功后 message.success('保存成功') + closeEdit + loadExperiences()
- 详情 Modal：`<ExperienceDetailModal open={!!detailExp} experience={detailExp} onClose={() => setDetailExp(null)} onEdit={...} onInvalidate={...} onRestore={...} onDelete={...} />`

**Table props**：`<Table columns={columns} rowKey="id" dataSource={list} loading={loading} size="small" pagination={{ pageSize: 20 }} />`（pageSize 20 UI-SPEC + KB L433 一致）。

**空状态**：Table 自带 Empty（无数据时），或 locale={{ emptyText: <Empty description="暂无经验 AI 对话后点『经验总结』自动沉淀，或点击右上角『新增经验』手动录入。" /> }}（UI-SPEC Empty copy contract）。

draft 不进浏览页：loadExperiences 不显式过滤 status，但 Table dataSource 前端 filter `record.status !== 'draft'`（draft 是 Phase 9 待确认草稿，不应进浏览页，前端 filter 最简且不改 service）。

格式红线：2 空格 / 单引号 / 无分号 / 尾随逗号；AntD 桶式 import { Table, Button, Input, Select, Switch, Space, Modal, Tag, Popconfirm, Empty, message, Spin }；icon from '@ant-design/icons'（EditOutlined, DeleteOutlined）。
  </action>
  <verify>
    <automated>
cd "E:/knowlegdge_base/claude/network_toplogy" && npx tsc -p tsconfig.web.json --noEmit && npx vite build && npx vitest run
    </automated>
  </verify>
  <acceptance_criteria>
- `test -f src/components/knowledge/ExperienceTab.tsx` exit 0
- `grep -c "window.api.experience.list" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1
- `grep -c "新增经验" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（primary CTA）
- `grep -c "搜索经验标题或正文" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（搜索 placeholder）
- `grep -c "显示已失效" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（Switch label）
- `grep -c "pageSize: 20" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（分页）
- `grep -c "ExperienceEditForm" src/components/knowledge/ExperienceTab.tsx` 返回 ≥2（import + 使用）
- `grep -c "ExperienceDetailModal" src/components/knowledge/ExperienceTab.tsx` 返回 ≥2（import + 使用）
- `grep -c "window.api.experience.delete" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（物理删除）
- `grep -c "window.api.experience.invalidate" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（标失效）
- `grep -c "window.api.experience.restore" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（恢复）
- `grep -c "window.api.experience.create" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（手动新增）
- `grep -c "status: 'published'" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（手动新增直 published）
- `grep -c "将彻底删除经验" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（硬删除 Popconfirm description）
- `grep -c "失效后将从默认视图剔除" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（软失效 Popconfirm description）
- `grep -c "已标记为失效" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1
- `grep -c "已恢复为有效" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1
- `grep -c "加载经验列表失败" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（error copy）
- `grep -c "暂无经验" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（Empty copy）
- `grep -c "r.status !== 'draft'" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1（draft 不进浏览页）
- **`grep -c "mode=\"multiple\"" src/components/knowledge/ExperienceTab.tsx` 返回 ≥2**（设备 Select + 标签 Select 都 mode multiple，UI-SPEC §3 锁定；禁止改单选）
- **`grep -c "全局" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1**（关联设备列无关联显全局灰 Tag，UI-SPEC §2 锁定）
- **`grep -c "device_count" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1**（关联设备列用 record.device_count 渲染 N 台，10-01 子查询带出）
- **`grep -c "台" src/components/knowledge/ExperienceTab.tsx` 返回 ≥1**（「N 台」文案，UI-SPEC §2 锁定）
- **`grep -c "deviceId" src/components/knowledge/ExperienceTab.tsx` 返回 ≥2**（state + 传 opts，多选数组）
- Table columns 数组含 9 个列对象（grep `dataIndex:` 在 ExperienceTab.tsx 出现 ≥8 次，操作列无 dataIndex）
- tsc + vite build + vitest run 全 exit 0（175 测试 + 新增 browse 用例无回归）
  </acceptance_criteria>
  <done>ExperienceTab 落地（筛选 bar 8 元素锁定顺序含设备多选 mode multiple + Table 9 列含第 5 列「N 台/全局」渲染用 record.device_count + 行操作三能力按状态切换 + 新增/编辑/详情 Modal 编排 + draft 前端过滤 + 全 copy 中文锁定），UI-SPEC §2/§3 锁定渲染全落实，三绿门禁全绿。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: 实机验证 ExperienceTab UI 全链路（Tabs 切换/设备多选/关联设备列 N 台全局渲染/三能力/详情 Modal/Phase 9 不回归）</name>
  <action>用户按 <how-to-verify> 步骤实机验证后，返回 approved 或描述问题；无代码改动。</action>
  <what-built>
ExperienceTab + ExperienceDetailModal 已落地（自动 build/tsc/vitest 全绿），但需用户实机验证：
- 知识库页 Tabs 切换（文档 | 经验）正常
- 经验 Tab 列表加载（published/invalid 显示，draft 不显示）
- 筛选 bar 8 元素交互（搜索防抖 + Select/Switch 触发刷新 + **设备 Select 多选**）
- **关联设备列（第 5 列）渲染：有关联显「N 台」+ 无关联显「全局」灰 Tag**
- 手动新增 → ExperienceEditForm Modal → 保存直 published → 列表刷新
- 编辑 → 预填 → update → 刷新
- 三能力：标失效（Popconfirm 软）/ 恢复有效 / 物理删除（Popconfirm 硬不可恢复）
- 详情 Modal（width 900）+ 「查看原始会话」叠层 SessionMessagesModal
- severity Tag 语义色（critical=red/high=volcano/medium=orange/low=gold/info=blue）
- 状态 Tag（有效=绿/失效=灰，不染红）
  </what-built>
  <how-to-verify>
1. 启动应用：`npm run dev`（或既有启动命令）
2. 登录后进入「知识库」一级菜单
3. 顶部应见 Tabs「文档 | 经验」，默认「文档」Tab
4. 切换到「经验」Tab：
   - 若库内有 published 经验（Phase 9 confirmed 产生的），列表展示 9 列
   - **关联设备列：有关联经验的该行显「N 台」（如「3 台」），无关联的显「全局」灰 Tag**
   - 若空，显 Empty「暂无经验 AI 对话后点『经验总结』自动沉淀，或点击右上角『新增经验』手动录入。」
5. 点「新增经验」→ 弹出 ExperienceEditForm Modal（width 640，footer「保存」）：
   - 填标题 + 选分类 troubleshooting + 必填 severity/symptoms/resolution → 「保存」可点
   - 故意清空 severity → 「保存」按钮 disabled + 红色错误提示「缺 严重程度」
   - 保存成功 → message「保存成功」+ 列表刷新出现新条目（status=published）
6. 点某条标题蓝字 → 详情 Modal（width 900）打开，元数据行（含关联设备名列表）+ 正文 + troubleshooting attrs 模板块齐全；点「查看原始会话」→ 叠层 SessionMessagesModal（若有 source_session_id）
7. 行操作测试：
   - 有效经验：「编辑」/「标失效」（Popconfirm 软确认文案）/「删除」（Popconfirm 硬确认「不可恢复」）
   - 标失效后 → 状态 Tag 变灰「已失效」+ 默认视图（Switch off）应剔除
   - 打开「显示已失效」Switch → 失效条目重新出现 → 「恢复有效」按钮显 → 点击 → 状态回绿
   - 删除 → Popconfirm 确认 → 物理删除（列表移除）
8. 筛选 bar：搜索关键词（标题/正文匹配）+ 分类 Select + 严重度 Select + 状态 Select + **设备 Select 多选（选 2+ 设备，OR 命中任一关联经验）** + 标签 Select + 显示已失效 Switch 各自触发刷新
9. 严重度 Tag 颜色：critical=红 / high=volcano 橙红 / medium=橙 / low=金 / info=蓝
10. 验证 Phase 9 不回归：回 AI 对话页，点「经验总结」→ ReviewConfirmModal 仍正常打开 + 表单编辑 + 质量门标红 + 批量确认全链路正常
  </how-to-verify>
  <resume-signal>Type "approved" 或描述需修复的问题（特别关注：Tabs 切换 / draft 不进浏览页 / **关联设备列 N 台/全局 渲染** / **设备 Select 多选** / 三能力按钮按状态切换 / 详情 Modal 元数据完整 / Phase 9 不回归）</resume-signal>
</task>

<task type="auto">
  <name>Task 3: KnowledgeBasePage 改造为 Tabs 容器（文档 | 经验）</name>
  <files>src/components/pages/KnowledgeBasePage.tsx</files>
  <read_first>
    - src/components/pages/KnowledgeBasePage.tsx 全文（重点顶层 `<div style={{ padding: 24 }}>` + 现有资料库 Card + 检索测试 Card 结构 + import 区）
    - src/components/pages/AIPage.tsx（挂载子组件 + 透传范式，09-03-SUMMARY L16）
    - src/components/knowledge/ExperienceTab.tsx（本 plan Task 2 落地，确认默认导出）
    - .planning/phases/10-experience-browse-page/10-PATTERNS.md §KnowledgeBasePage.tsx 段（L236-262）
    - .planning/phases/10-experience-browse-page/10-UI-SPEC.md §1 KnowledgeBasePage 改造
  </read_first>
  <action>
改造 `src/components/pages/KnowledgeBasePage.tsx` 顶部包一层 AntD `<Tabs>`（UI-SPEC §1）：

1. import 区：`import { Tabs } from 'antd'`（桶式 import 加 Tabs）+ `import ExperienceTab from '../knowledge/ExperienceTab'`（路径从 pages/ 到 knowledge/ 是 `'../knowledge/ExperienceTab'`）

2. 顶层 `<div style={{ padding: 24 }}>` 保留（UI-SPEC Spacing lg=24）

3. 现有「资料库」Card + 「检索测试」Card 整体移入 Tabs docs pane children（**不动其内部逻辑**）：
```
<Tabs
  defaultActiveKey="docs"
  items={[
    { key: 'docs', label: '文档', children: <>{/* 现有 资料库 Card + 检索测试 Card */}</> },
    { key: 'exp', label: '经验', children: <ExperienceTab /> },
  ]}
/>
```

4. 「经验」pane 懒加载：`const [expTabLoaded, setExpTabLoaded] = useState(false)` + `<Tabs onChange={(key) => { if (key === 'exp') setExpTabLoaded(true) }} ...>` + exp pane children 改 `{expTabLoaded ? <ExperienceTab /> : null}`（首次切到 exp 才挂载，避免默认 Tab 文档时无谓加载经验列表）

5. 路由 / 侧边栏不动（仍一级菜单「知识库」下，UI-SPEC §1）

唯一结构性改造 = 现有两 Card 外面包一层 Tabs。其余不动。
  </action>
  <verify>
    <automated>
cd "E:/knowlegdge_base/claude/network_toplogy" && npx tsc -p tsconfig.web.json --noEmit && npx vite build && npm run build:electron-main && npx vitest run
    </automated>
  </verify>
  <acceptance_criteria>
- `grep -c "import { Tabs" src/components/pages/KnowledgeBasePage.tsx` 返回 1（或 Tabs 在既有 antd import 桶内）
- `grep -c "import ExperienceTab" src/components/pages/KnowledgeBasePage.tsx` 返回 1
- `grep -c "defaultActiveKey=\"docs\"" src/components/pages/KnowledgeBasePage.tsx` 返回 1
- `grep -c "key: 'docs', label: '文档'" src/components/pages/KnowledgeBasePage.tsx` 返回 1
- `grep -c "key: 'exp', label: '经验'" src/components/pages/KnowledgeBasePage.tsx` 返回 1
- `grep -c "expTabLoaded" src/components/pages/KnowledgeBasePage.tsx` 返回 ≥2（state + 使用）
- 既有文档库功能不回归（资料库 Card + 检索测试 Card 仍在 docs pane，vitest 175 测试 + 新增 browse 用例全绿）
- tsc + vite build + build:electron-main + vitest run 全 exit 0
  </acceptance_criteria>
  <done>KnowledgeBasePage 顶部包 Tabs（文档 | 经验），默认 Tab=文档，经验 Tab 懒加载 ExperienceTab，现有文档库功能零回归，三绿门禁全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→IPC | ExperienceTab 调 window.api.experience.{list,create,update,delete,invalidate,restore,listDevices,getSessionMessages}（经 secure IPC，10-01 落地） |
| renderer 组件间 | KnowledgeBasePage → ExperienceTab → ExperienceEditForm/ExperienceDetailModal/SessionMessagesModal（props 传 Experience DTO 已脱敏） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Tampering | 物理删除 experience:delete | mitigate | Popconfirm 硬确认（title「确认删除」+ description「将彻底删除经验『{title}』，操作不可恢复。」+ danger 按钮），UI-SPEC copy 强提示不可恢复；service deleteExperience 是设计意图（D-10-3 三能力，已发布经验可物理删） |
| T-10-12 | Elevation | 行操作 restore/invalidate 触发器 | mitigate | 按钮按状态切换（有效显「标失效」/失效显「恢复有效」），且均经 secure IPC（10-01）；restore 受控接口只清 invalid_at+status，不开放任意字段修改 |
| T-10-13 | Information | 详情 Modal 展示 attrs 解密值 | accept | attrs 经 service rowToExperience 解密回填 + delete attrs_enc（信任边界红线）；详情是设计意图（用户需查看经验全文），不含凭证字段（凭证走 **** 脱敏另径） |
| T-10-14 | Information | SessionMessagesModal 会话原文回链 | accept | 复用 Phase 9 既有组件（已通过 T-09-13 防 DoS 边界 + 空会话「原会话已不可查」提示），本 plan 不重写不引入新面 |
| T-10-15 | Denial | 关联设备列渲染性能（N+1） | mitigate | **10-01 已落地 device_count 子查询（单次 SQL 带出，零 N+1）；本 plan 关联设备列用 record.device_count 直读渲染（「N 台/全局」），不在 render 内调 listDevices**；详情 Modal 单条点开才调 listDevices 显设备名；pageSize 20 + limit 100 上限；service MAX_BATCH=1000 兜底。CLAUDE.md「优先最优秀方案」选 SQL 子查询方案 |
| T-10-16 | Tampering | draft 经验泄露进浏览页 | mitigate | 前端 filter `record.status !== 'draft'` 兜底（draft 是 Phase 9 待确认草稿专属，UI-SPEC §1 / CONTEXT D-10-3 明确 draft 不进浏览页） |
| T-10-18 | Tampering | 设备 Select 多选 deviceId[] 透传 service | mitigate | UI-SPEC §3 锁定 mode multiple；多选 string[] 经 IPC 透传到 service listExperiences，service 层 `IN (` + `map(()=>'?').join(',')` + `)` 占位参数化（10-01 已落地，T-10-01），renderer 不拼接 SQL，无注入面 |
</threat_model>

<verification>
- 三绿门禁：tsc + vite build + build:electron-main + vitest run 全 exit 0（175 测试 + 新增 browse 用例无回归）
- BROWSE-01：KnowledgeBasePage Tabs（文档 | 经验）切换正常，经验 Tab 展示列表
- BROWSE-02：筛选 bar 8 元素（搜索/分类/严重度/状态/**设备多选**/标签/显示已失效）+ Table 9 列锁定顺序 + **第 5 列「N 台/全局」渲染（UI-SPEC §2 锁定）**
- BROWSE-03：手动新增/编辑（ExperienceEditForm Modal + 直 published）
- BROWSE-04：标失效/恢复/物理删除三能力（Popconfirm 软硬区分）
- 详情 Modal（SC5）：元数据 + 来源会话回链 + 复用次数 + 最后验证 + attrs 模板
- 红线③ copy：footer「保存」无「确认入库」措辞；手动新增直 published 是人工录入例外
- Phase 9 不回归：ReviewConfirmModal + ReviewConfirmEditForm + SessionMessagesModal 链路正常
- 人工 checkpoint（blocking）全链路验证 approved
</verification>

<success_criteria>
- KnowledgeBasePage Tabs 改造完成（BROWSE-01）
- ExperienceTab 列表 + 多维筛选（含设备多选）+ 关键词搜索落地（BROWSE-02）
- 关联设备列「N 台/全局」渲染（UI-SPEC §2 锁定，数据来自 record.device_count 子查询零 N+1）
- 手动新增/编辑 Modal 走 ExperienceEditForm 公共组件（BROWSE-03）
- 标失效/恢复/物理删除三能力全要（BROWSE-04 + SC4）
- 经验详情 Modal 元数据 + 来源会话回链（SC5）
- Phase 7-9 既有功能零回归（175 测试 + 新增 browse 用例全绿）
- 人工 checkpoint approved
</success_criteria>

<output>
Create `.planning/phases/10-experience-browse-page/10-03-SUMMARY.md` when done
</output>
