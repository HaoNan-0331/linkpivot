---
phase: 11-ai-retrieval-reuse
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/pages/ai/types.ts
  - src/components/pages/ai/useAIChat.ts
  - src/components/pages/ai/ChatMessageList.tsx
autonomous: true
requirements_addressed: [RETRIEVE-03]
gap_closure: false

must_haves:
  truths:
    - "AI 回答末尾渲染来源列表（经验 + 会话引用），数据从精排注入记录拿不需 AI 标记（D-11-10/D-11-11）"
    - "ChatMsg.references 扩联合类型：kb / experience / session 三种（types.ts）"
    - "useAIChat.handleSend 消费 exp_answer 返回类型，references 注入消息（与 kb_answer 并存）"
    - "经验引用点击打开 ExperienceDetailModal（复用 Phase 10，不新建 D-11-12）"
    - "会话引用点击打开 SessionMessagesModal（复用 Phase 9，不新建 D-11-12）"
    - "命令失支持经验显「⚠ 命令已失支持」标注（D-11-7，warning 色 Tag 不引新色）"
    - "renderer 永不收 attrs 密文（references 只含 exp_id/title/source_session_id/unsupported 元数据）"
  artifacts:
    - path: "src/components/pages/ai/types.ts"
      provides: "ChatMsg.references 联合类型（kb/experience/session）"
      contains: "ReferenceItem"
    - path: "src/components/pages/ai/useAIChat.ts"
      provides: "exp_answer 返回类型消费 + references 注入消息"
      contains: "exp_answer"
    - path: "src/components/pages/ai/ChatMessageList.tsx"
      provides: "末尾来源列表渲染（按 ref.kind 分流）+ 点击回查 Modal 触发"
      contains: "ExperienceDetailModal"
  key_links:
    - from: "src/components/pages/ai/useAIChat.ts handleSend"
      to: "ai:chat 返回 JSON exp_answer 分支"
      via: "JSON.parse + parsed.type==='exp_answer' 消费 references"
      pattern: "exp_answer"
    - from: "src/components/pages/ai/ChatMessageList.tsx"
      to: "src/components/knowledge/ExperienceDetailModal.tsx"
      via: "经验引用点击 → window.api.experience.get(expId) → 打开 Modal"
      pattern: "ExperienceDetailModal"
    - from: "src/components/pages/ai/ChatMessageList.tsx"
      to: "src/components/pages/ai/SessionMessagesModal.tsx"
      via: "会话引用点击 → 打开 Modal（sessionId 直传）"
      pattern: "SessionMessagesModal"
---

<objective>
实现 Phase 11 引用溯源的 renderer 层（RETRIEVE-03）：AI 回答末尾渲染来源列表（经验 + 会话引用），数据从 Plan 01 编排层 retrieveForAnswer.injected 记录拿（D-11-11，不需 AI 标记）。ChatMsg.references 扩联合类型（kb/experience/session），ChatMessageList 按 ref.kind 分流渲染，经验引用点击复用 Phase 10 ExperienceDetailModal（window.api.experience.get 拉详情），会话引用点击复用 Phase 9 SessionMessagesModal（零适配），命令失支持显「⚠ 命令已失支持」warning Tag（D-11-7）。

Purpose: RETRIEVE-03（引用溯源 + 可回查）落地，闭环 Phase 11 三条 REQ。
Output: types.ts references 联合类型 + useAIChat exp_answer 消费 + ChatMessageList 末尾来源列表 + 两个 Modal 复用接入。
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ai-retrieval-reuse/11-01-PLAN.md
@.planning/phases/11-ai-retrieval-reuse/11-CONTEXT.md
@.planning/phases/11-ai-retrieval-reuse/11-PATTERNS.md
@./CLAUDE.md

<interfaces>
<!-- Plan 01 产出的 main 进程契约 + renderer 复用组件 props -->

From Plan 01（ai.ts chat() 返回 JSON，ai:chat 经 preload 已暴露）:
```typescript
// ai.ts exp_answer 返回类型（与 kb_answer 并存）
// JSON.stringify({ type: 'exp_answer', content: finalAiReply, references: expReferences })
// expReferences: Array<{ exp_id: string; title: string; source_session_id: string | null; unsupported: boolean }>
// 注意：references 字段名与 kb_answer 一致，renderer 联合类型兼容
```

From src/components/pages/ai/types.ts（当前定义，本 plan 扩 references 联合）:
```typescript
export interface ChatMsg {
  id?: string
  role: 'user' | 'assistant'
  content: string
  createdAt?: string
  references?: Array<{ docTitle: string; chunkTitle: string; docId: string }>   // ← 当前 KB 专用，扩联合
}
```

From src/components/pages/ai/useAIChat.ts:139-154（references 消费点）:
```typescript
const parsed = JSON.parse(reply) as ConfirmData & { type: string; content?: string; references?: ChatMsg['references'] }
if (parsed.type === 'kb_answer') {
  setMessages([...newMessages, { role: 'assistant', content: parsed.content || '', references: parsed.references }])
  // ← 本 plan 扩：parsed.type === 'exp_answer' 走同分支（references 联合类型兼容）
}
```

From src/components/knowledge/ExperienceDetailModal.tsx:58-66（Phase 10，复用不新建 D-11-12）:
```typescript
interface ExperienceDetailModalProps {
  open: boolean
  experience: Experience | null     // 需先 window.api.experience.get(expId) 拉对象传入
  onClose: () => void
  onEdit?: () => void
  onInvalidate?: () => void
  onRestore?: () => void
  onDelete?: () => void
}
// 拉详情：const [exp, setExp] = useState<Experience|null>(null)
//   useEffect(() => { if (open && expId) window.api.experience.get(expId).then(setExp) }, [open, expId])
```

From src/components/pages/ai/SessionMessagesModal.tsx:16-36（Phase 9，零适配复用 D-11-12）:
```typescript
interface SessionMessagesModalProps {
  open: boolean
  sessionId: string | null     // 直传 sessionId，内部已 window.api.experience.getSessionMessages
  onClose: () => void
}
// 零适配：props 传 sessionId 即可，内部 useEffect 自动拉会话原文
```

From preload.ts:124-141（experience namespace 已暴露，零改动）:
```typescript
window.api.experience.get(id)               // 拉 Experience 详情（喂 ExperienceDetailModal）
window.api.experience.getSessionMessages(sessionId)  // SessionMessagesModal 内部已调
```
</interfaces>

<analogs>
<!-- PATTERNS.md §Pattern 7 + §8 已给 file:line，executor 必读类比 -->

1. references 联合类型扩（types.ts）：
   自身 types.ts:19-25 当前 references 是 `Array<{docTitle;chunkTitle;docId}>`（KB 专用）。
   扩为 ReferenceItem 联合（kb/experience/session），保留 KB 既有字段不破坏。

2. 末尾来源列表渲染（ChatMessageList.tsx）：
   自身 ChatMessageList.tsx:61-71 已渲染 msg.references KB 来源（BookOutlined + docTitle — chunkTitle）。
   Phase 11 扩：按 ref.kind 分流——kb 保持既有渲染；experience 显 title + 点击打开 ExperienceDetailModal（先 experience.get 拉详情）；session 显 title + 点击打开 SessionMessagesModal（sessionId 直传）。
   unsupported 标注：experience 引用若 unsupported=true 显 warning 色 Tag「⚠ 命令已失支持」（仿 ExperienceDetailModal.tsx:34-40 SEVERITY_TAG 模式，但用 warning/gold 色，**禁止新色**——CONVENTIONS + UI-SPEC Color 锁定）。

3. useAIChat exp_answer 消费：
   useAIChat.ts:147-151 kb_answer 分支扩条件：`parsed.type === 'kb_answer' || parsed.type === 'exp_answer'`（references 联合类型兼容，同一 setMessages 分支）。

4. Modal 拉详情模式：
   ExperienceDetailModal 需 Experience 对象传入——仿 ExperienceDetailModal.tsx:87-91 拉 devices 的 useEffect 模式（window.api.experience.get(expId).then(setExp)），在 ChatMessageList 内或提升到 AIPage 管理 state。
</analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts references 联合类型 + useAIChat exp_answer 消费</name>
  <files>src/components/pages/ai/types.ts, src/components/pages/ai/useAIChat.ts</files>
  <read_first>
    - .planning/phases/11-ai-retrieval-reuse/11-CONTEXT.md（D-11-10 末尾来源列表 / D-11-11 references 从注入记录拿不需 AI 标记 / D-11-12 复用 Modal 不新建）
    - .planning/phases/11-ai-retrieval-reuse/11-PATTERNS.md §Pattern 8（types.ts references 联合扩 file:line）
    - src/components/pages/ai/types.ts:19-25（当前 ChatMsg.references 定义）
    - src/components/pages/ai/useAIChat.ts:139-154（kb_answer references 消费分支）
    - ./CLAUDE.md Conventions（PascalCase.tsx 组件 / 类型命名）
  </read_first>
  <action>
    types.ts 修改：
    - 新增导出 ReferenceItem 联合类型（保留 KB 既有字段不破坏向后兼容）：
      type ReferenceItem =
        | { kind: 'kb'; docTitle: string; chunkTitle: string; docId: string }
        | { kind: 'experience'; expId: string; title: string; unsupported?: boolean }
        | { kind: 'session'; sessionId: string; title: string }
    - ChatMsg.references 改为 `references?: ReferenceItem[]`（联合类型，KB 既有消费方需适配 kind 分流，但本期 KB 来源由 useAIChat 注入时无 kind 字段——需在 useAIChat kb_answer 分支给 references 补 kind:'kb'，或在联合里把 kb 的 kind 设为可选默认 kb；planner 定：显式 kind 必填，useAIChat kb_answer 分支 map 补 kind:'kb' 保证类型安全）。

    useAIChat.ts 修改（:139-154）：
    - parsed 类型断言 references 改为 ReferenceItem[]（已扩联合）
    - kb_answer 分支：references 注入时 map 补 kind:'kb'（既有 kbReferences 是 {docTitle,chunkTitle,docId}，map 为 {kind:'kb',docTitle,chunkTitle,docId}）——但 kb_answer 的 references 来自 ai.ts KB_SEARCH 分支（ai.ts:758-762 kbReferences 无 kind），最稳是在 useAIChat 消费时统一 map 补 kind。注意：kb_answer 分支本期不动 ai.ts KB_SEARCH 逻辑（避免回归），只在 useAIChat.ts:148 setMessages 时 map 补 kind:'kb'。
    - 扩 exp_answer 分支：`if (parsed.type === 'kb_answer' || parsed.type === 'exp_answer')` 走同一 setMessages 分支（references 联合类型兼容，exp_answer 的 references 已是 {exp_id,title,source_session_id,unsupported} 形态——需 map 为 ReferenceItem：{ kind:'experience', expId: r.exp_id, title: r.title, unsupported: r.unsupported }；若 source_session_id 非空额外 push 一条 { kind:'session', sessionId: r.source_session_id, title: '原始会话' }，D-11-10 末尾列表含会话引用）。
    - 具体：在 kb_answer/exp_answer 分支内，根据 parsed.type 分别 map references 为 ReferenceItem[]（kb 补 kind:'kb'；exp 拆 experience + session 两类），再 setMessages。
    - parsed references 字段类型放宽为 any 或定义 ExpReferenceRaw 类型（{exp_id,title,source_session_id,unsupported}），map 时类型安全。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json 2>&1 | grep -E "types\.ts|useAIChat\.ts" | grep -v '^$'; echo "TSC_EXIT=$?"; npx vitest run --reporter=default 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "ReferenceItem|kind: 'kb'|kind: 'experience'|kind: 'session'" src/components/pages/ai/types.ts` 全命中
    - `grep -n "exp_answer" src/components/pages/ai/useAIChat.ts` 至少 1 处
    - `grep -n "kind: 'kb'" src/components/pages/ai/useAIChat.ts` 至少 1 处（kb_answer 分支补 kind）
    - npx tsc -p tsconfig.web.json 零 error（联合类型 + map 类型安全）
    - npx vitest run 既有用例零回归（types/useAIChat 无单测，靠 tsc + ChatMessageList 集成验证）
  </acceptance_criteria>
  <done>ChatMsg.references 扩 ReferenceItem 联合类型（kb/experience/session），useAIChat 消费 exp_answer + kb_answer 双分支 map 注入类型安全 references，tsc 全绿零回归。</done>
</task>

<task type="auto">
  <name>Task 2: ChatMessageList 末尾来源列表渲染 + 点击回查 Modal 复用</name>
  <files>src/components/pages/ai/ChatMessageList.tsx</files>
  <read_first>
    - .planning/phases/11-ai-retrieval-reuse/11-CONTEXT.md（D-11-10 末尾列表 / D-11-12 复用 Modal 不新建）
    - .planning/phases/11-ai-retrieval-reuse/11-PATTERNS.md §Pattern 7（ChatMessageList references 渲染 file:line + Modal 复用）
    - src/components/pages/ai/ChatMessageList.tsx:1-85（当前 references 渲染 :61-71 + imports）
    - src/components/knowledge/ExperienceDetailModal.tsx:58-91（props 签名 + 拉 devices useEffect 模式参考）
    - src/components/pages/ai/SessionMessagesModal.tsx:16-36（props 签名，零适配）
    - src/types/experience.ts（Experience 类型，ExperienceDetailModal props 用）
    - ./CLAUDE.md Conventions（PascalCase.tsx / 禁新色——用 antd Tag warning/gold 既有色）
  </read_first>
  <action>
    ChatMessageList.tsx 修改（扩 :61-71 references 渲染分支）：

    顶部 imports 扩：
    - import ExperienceDetailModal from '../../knowledge/ExperienceDetailModal'（复用 Phase 10，D-11-12 不新建）
    - import SessionMessagesModal from './SessionMessagesModal'（复用 Phase 9，D-11-12 不新建）
    - import type { Experience } from '@/types/experience'（喂 ExperienceDetailModal experience props）
    - import type { ReferenceItem } from './types'（references 联合类型）
    - antd Tag 已可用（ChatMessageList 当前未 import Tag，需加 import { Tag } from 'antd'；warning 色用<Tag color="warning">或 color="gold"，**禁止自定义新色**）

    组件内加 state（useState）管理两个 Modal：
    - const [detailExp, setDetailExp] = useState<Experience | null>(null)（ExperienceDetailModal experience props）
    - const [detailOpen, setDetailOpen] = useState(false)
    - const [sessionModalId, setSessionModalId] = useState<string | null>(null)（SessionMessagesModal sessionId）
    - const [sessionModalOpen, setSessionModalOpen] = useState(false)
    - const [loadingDetail, setLoadingDetail] = useState(false)

    点击处理函数：
    - openExperience(expId: string)：setLoadingDetail(true) → window.api.experience.get(expId).then(e => { setDetailExp(e); setDetailOpen(true) }).finally(() => setLoadingDetail(false))（仿 ExperienceDetailModal.tsx 拉 devices useEffect 模式，但改为点击触发）
    - openSession(sessionId: string)：setSessionModalId(sessionId); setSessionModalOpen(true)（SessionMessagesModal 零适配，sessionId 直传）

    references 渲染分支（:61-71 扩）按 ref.kind 分流：
    - kind==='kb'：保持既有渲染（BookOutlined + docTitle — chunkTitle），不动
    - kind==='experience'：渲染可点击行（cursor:pointer + onClick openExperience(ref.expId)），显「📖 {ref.title}」+ 若 ref.unsupported 显 <Tag color="warning">⚠ 命令已失支持</Tag>（D-11-7，warning 色 antd 既有，禁新色）；鼠标悬停 title 属性提示「点击查看经验详情」
    - kind==='session'：渲染可点击行（cursor:pointer + onClick openSession(ref.sessionId)），显「💬 {ref.title}」；title 属性「点击查看原始会话」
    - 顶部「参考来源：」标题保留（既有 :63），experience/session 引用统一在此列表（D-11-10 末尾列表，D-11-11 列全部注入的不区分 AI 实际用到几条）

    组件 JSX 末尾（与既有 return 同级，messages.map 之后、chatEndRef 之前）渲染两个 Modal：
    - <ExperienceDetailModal open={detailOpen} experience={detailExp} onClose={() => setDetailOpen(false)} />（onEdit/onInvalidate 等可选回调不传，浏览只读场景；D-11-12 复用不新建）
    - <SessionMessagesModal open={sessionModalOpen} sessionId={sessionModalId} onClose={() => setSessionModalOpen(false)} />（零适配）

    loadingDetail 时点击行可显 Spin 提示（可选，点击体验；非必须，体验差再加）。

    注意：ChatMessageList 当前是 default function export（:11），加 state 需确保 React hooks 顺序合法（useState 在函数顶部，不在 map 内）。
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.web.json 2>&1 | grep -E "ChatMessageList" | grep -v '^$'; echo "TSC_EXIT=$?"; npm run build 2>&1 | tail -5; npx vitest run --reporter=default 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ExperienceDetailModal" src/components/pages/ai/ChatMessageList.tsx` 至少 2 处（import + JSX 使用）
    - `grep -n "SessionMessagesModal" src/components/pages/ai/ChatMessageList.tsx` 至少 2 处（import + JSX 使用）
    - `grep -E "kind === 'experience'|kind === 'session'|kind === 'kb'" src/components/pages/ai/ChatMessageList.tsx` 全命中（分流渲染）
    - `grep -n "window.api.experience.get" src/components/pages/ai/ChatMessageList.tsx` 至少 1 处（点击拉详情）
    - `grep -n "unsupported" src/components/pages/ai/ChatMessageList.tsx` 至少 1 处（命令失支持标注）
    - `grep -cE "color=\"#[0-9a-fA-F]{3,6}\"" src/components/pages/ai/ChatMessageList.tsx` 未新增自定义色（warning/gold 走 antd Tag color 属性枚举，不是 hex）——若既有有 hex 不增量即可
    - npx tsc -p tsconfig.web.json 零 error
    - npm run build（vite renderer）成功，ChatMessageList 打包无解析错误
    - npx vitest run 既有用例零回归
  </acceptance_criteria>
  <done>ChatMessageList 末尾来源列表按 ref.kind 分流渲染（kb/experience/session），经验引用点击拉详情开 ExperienceDetailModal，会话引用点击开 SessionMessagesModal（零适配），unsupported 显 warning Tag，tsc + vite build + vitest 全绿零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main→renderer（ai:chat 返回 JSON references） | references 元数据跨 IPC 边界回 renderer，attrs 密文不得越界 |
| renderer→main（experience.get 拉详情） | 点击经验引用触发 experience.get(expId)，expId 来自 references（main 注入记录） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Information Disclosure (引用泄露私有经验 attrs) | ChatMessageList references 渲染 + ExperienceDetailModal | mitigate | references 只含 exp_id/title/source_session_id/unsupported（Plan 01 已约束，不含 attrs 密文）；ExperienceDetailModal 经 experience.get 走既有 secure IPC + stripEncColumns 边界（Phase 7 已防），renderer 永不收 _enc 列；与浏览页详情 Modal 同信任级别（Phase 10 已验证） |
| T-11-08 | Tampering (expId 篡改触发越权 get) | ChatMessageList openExperience 点击 | mitigate | expId 来自 main 进程注入的 references 记录（非用户可编辑输入），experience.get 经 secure IPC 鉴权（Phase 7 SEC-01），且 getExperience 只返本人库内经验（单机桌面无多租户）；风险极低 accept |
| T-11-09 | Information Disclosure (会话原文泄露) | SessionMessagesModal 会话引用回查 | mitigate | sessionId 来自 references.source_session_id（main 注入），SessionMessagesModal 经 experience:getSessionMessages secure IPC（Phase 9 已防 SEC-01），与 Phase 9 既有 ReviewConfirmModal 回链同信任级别；零适配复用不引入新边界 |
</threat_model>

<verification>
四绿门禁（沿用 Phase 7-10 模式）：
1. `npx tsc -p tsconfig.web.json` 无新增 error（strict + noUnusedLocals，联合类型 + Modal props 类型安全）
2. `npm run build` (vite renderer build) 成功，ChatMessageList + types + useAIChat 打包无解析错误
3. `npm run build:electron-main` 成功（本 plan 不改 main，但跑全量门禁确认零回归）
4. `npx vitest run` 全绿（既有 200+ 用例零回归，本 plan 无新增单测——renderer UI 渲染靠 tsc + build + 人工 checkpoint 验证）

人工验证（end-of-phase checkpoint，本 plan 完成后整体 Phase 11 验证时做）：
- 与 Phase 10 published 经验对话提问 → AI 回答末尾显来源列表 → 点击经验引用开 ExperienceDetailModal → 点击会话引用开 SessionMessagesModal
- 命令失支持经验显「⚠ 命令已失支持」warning Tag
</verification>

<success_criteria>
- SC3（RETRIEVE-03）：AI 回答附引用来源（经验 exp_id / 会话 sessionId），末尾来源列表渲染，点击可回查经验详情（ExperienceDetailModal）或原始会话（SessionMessagesModal）✓
- D-11-10：末尾来源列表（AI 正文正常写 + 末尾附参考）✓
- D-11-11：references 从精排注入记录拿（renderer 直接消费 ai:chat 返回 JSON，不需 AI 标记）✓
- D-11-12：点击回查复用 ExperienceDetailModal + SessionMessagesModal，零新建 Modal ✓
- D-11-7：命令失支持显 warning Tag 标注（不引新色）✓
- 红线：renderer 永不收 attrs 密文（references 只元数据 + 详情走既有 secure IPC）✓
</success_criteria>

<output>
Create `.planning/phases/11-ai-retrieval-reuse/11-02-SUMMARY.md` when done
</output>
